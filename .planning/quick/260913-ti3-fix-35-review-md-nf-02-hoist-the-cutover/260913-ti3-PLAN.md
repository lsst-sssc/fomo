---
phase: quick-260913-ti3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - solsys_code/management/commands/cutover_classical_allocations.py
  - solsys_code/tests/test_cutover_classical_allocations.py
  - docs/runbooks/telescope_runs_calendar.rst
  - docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb  # conditional -- only if Task 3's cell audit finds a committed cutover cell whose recorded output this change alters; see Task 3 step 2
autonomous: true
requirements:
  - NF-02

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A legacy blank-url event whose derived observing night falls outside the window the schedule line itself implies is reported under a dedicated named reason (window_mismatch), NOT under the generic 'other' unexpected-error bucket, on BOTH the --dry-run path and the real path"
    - "--dry-run applies all three per-event preconditions the real path applies -- window containment, in-run claimed-night collision, and existing-ALLOC-url collision -- so a dry run can no longer exit 0 over a fixture the immediately following real run rejects"
    - "A dry run and the immediately following real run over the same fixture agree on: the 'events re-keyed:' count, the 'unexplained:' count, every per-reason breakdown line, and exit status (both raise CommandError, or neither does)"
    - "The dry-run path evaluates the window bounds from the previewed fields (what the real pass would write), not from a stale pre-existing CampaignRun row whose window may differ"
    - "An out-of-window event is left byte-identical on both paths: url stays blank and no CalendarEventMeta companion row is created for it"
    - "The three preconditions live in exactly ONE helper called by both branches, so a future fourth check cannot be added to only one path"
    - "The real path's conversion behavior is otherwise unchanged: every existing test class in test_cutover_classical_allocations.py stays green, and the window-mismatch stderr line still contains the operator-recognisable phrase it contained before"
    - "The module docstring's reason vocabulary and the operator runbook's cutover reason list both name window_mismatch with its operator action, in the same style the existing key_collision and foreign_attribution entries use"
    - "python manage.py test solsys_code.tests.test_cutover_classical_allocations passes in full, and pre-commit's pinned ruff and ruff-format are clean on both changed Python files"
  artifacts:
    - solsys_code/management/commands/cutover_classical_allocations.py
    - solsys_code/tests/test_cutover_classical_allocations.py
    - docs/runbooks/telescope_runs_calendar.rst
  key_links:
    - "The real path's window bounds come from the CampaignRun insert_or_create_campaign_run() just wrote, which is written FROM the same `fields` dict the dry-run branch previews. Passing the bounds out of `fields` on both paths is what makes the two passes provably read the same window, and is what lets the check still run on the dry-run path when no CampaignRun exists yet (run is None)"
    - "The existing-ALLOC-url probe is the ONE precondition that legitimately cannot run when run is None on the dry-run path: an ALLOC:{run_pk}:{night} url cannot already exist for a run that has no primary key. That asymmetry preserves parity rather than breaking it -- when the real pass then CREATES the run, its fresh primary key cannot collide with any pre-existing row either. When a CampaignRun already exists for the group, the dry run sees the same primary key the real pass will update, so the probe runs identically on both"
    - "Counting is what diverged before, not just checking: the dry-run branch added an out-of-window night to the claimed set and counted it as a would-be re-key. Because the hoisted helper RAISES before returning a night, the claimed-set insert and the re-key counter are both unreachable for a failing event on either path -- the parity follows from the control flow, not from remembering to mirror two statements"
    - "The check must raise BEFORE update_calendar_event_key_and_fields()/adopt_event_into_run() inside the existing per-event savepoint, exactly as today -- that ordering is what keeps a rejected event byte-identical (D-18)"
    - "A downstream sweep is what makes this load-bearing: project_allocation()'s convergence step classifies an ALLOC: url outside its run's window as stale and DELETES it, so a dry run that green-lights an out-of-window night hands the operator a row the next sweep removes -- turning this command's 'never removes a CalendarEvent row' guarantee into 'hands the next sweep a row to remove'"
---

<objective>
Close 35-REVIEW.md NF-02 (BLOCKER): `cutover_classical_allocations --dry-run` exits zero over
a fixture the immediately following real run rejects, because the WR-11 rebuild of the
dry-run branch copied only two of the real path's THREE per-event preconditions -- the
WR-08 window-containment check was left on the real path alone.

Fix it structurally rather than by copying a third check: hoist all three preconditions into
one helper called by both branches, so the two paths cannot drift again and a future fourth
check cannot be added to only one of them. Give the window mismatch its own named reason
(`window_mismatch`) instead of reporting it as a generic unexpected error, and make the
dry-run branch evaluate the window from the previewed fields rather than from a stale
pre-existing run row.

Purpose: a dry run exists so the operator can clear every unexplainable row BEFORE running
for real. A dry run that reports "unexplained: 0" while the real run raises `CommandError`
hides the one thing it exists to surface -- and the module docstring already promises the
opposite in as many words.

Output: a single `_check_event_night()` helper used by both branches; a `_WINDOW_MISMATCH`
reason with a `_REASON_LABELS` entry; a dry-run branch reading the previewed window; three
new tests (the NF-02 fixture, a dry-run/real parity regression guard, and byte-identity on
both paths); the module docstring and the operator runbook both naming the new reason; and a
recorded audit outcome for the paired demo notebook.
</objective>

<execution_context>
@/home/tlister/git/fomo_devel/.claude/gsd-core/workflows/execute-plan.md
@/home/tlister/git/fomo_devel/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/home/tlister/git/fomo_devel/CLAUDE.md
@/home/tlister/git/fomo_devel/.planning/STATE.md
@/home/tlister/git/fomo_devel/.planning/phases/35-allocation-layer-classical-cutover/35-REVIEW.md
@/home/tlister/git/fomo_devel/solsys_code/management/commands/cutover_classical_allocations.py
@/home/tlister/git/fomo_devel/solsys_code/tests/test_cutover_classical_allocations.py
@/home/tlister/git/fomo_devel/docs/runbooks/telescope_runs_calendar.rst
</context>

<interface_context>
Observed live at planning time (line numbers verified after the WR-08/WR-11/NF-03 quick
tasks shifted them; re-confirm before editing, they may shift again if quick task 260913-ti1
lands first -- it touches `campaign_reconciler.py` and `allocation_projector.py` only, so
there is no file overlap with this plan and either execution order is safe):

`solsys_code/management/commands/cutover_classical_allocations.py` (469 lines):
- L110-118  reason constants, ending `_KEY_COLLISION = 'key_collision'` then `_OTHER = 'other'`
- L120-128  `_REASON_LABELS` dict, in report order
- L131-139  `class _KeyCollisionError(Exception)` with its rationale docstring
- L155      `class Command(BaseCommand)`
- L329-333  dry-run run resolution: `existing_run = CampaignRun.objects.filter(source_identifier=key).first()`,
            `action = preview_campaign_run_action(existing_run, fields)`, `run = existing_run`;
            else branch `run, action = insert_or_create_campaign_run({'source_identifier': key}, fields)`
- L349-350  `site_zone = ZoneInfo(site.timezone)` and `claimed_nights: set[date] = set()` -- already
            hoisted above the branch by WR-11, both paths share them
- L352-383  dry-run per-event loop (two collision checks only; `except _KeyCollisionError` then
            broad `except Exception`; then `claimed_nights.add(night)` / `events_rekeyed += 1`)
- L385-440  real per-event loop inside `with transaction.atomic():` per-event savepoint --
            window check at L402-406 raising a bare `ValueError`, then the two collision
            checks, then the re-key writes; `claimed_nights.add(night)` / `events_rekeyed += 1`
            only after the savepoint's `with` block exits
- L459      summary breakdown line: `f'  unexplained ({category}): {count} -- {_REASON_LABELS[category]}'`
            -- every category emitted MUST have a `_REASON_LABELS` entry or this raises `KeyError`

`fields` (built at L~272-288) carries `'window_start': window_start` and
`'window_end': window_end`, both derived from `_iter_run_nights(parsed)` -- i.e. the schedule
line's own day range. On the real path `insert_or_create_campaign_run()` writes exactly these
onto the run, which is why `fields` and `run.window_*` are the same values there.

`solsys_code/tests/test_cutover_classical_allocations.py` (807 lines):
- `CutoverClassicalAllocationsTestBase` (L42) -- `setUpTestData` creates `cls.campaign`
  (TargetList '3I/ATLAS'), `cls.other_campaign`, `cls.ntt` (obscode 809, tz America/Santiago),
  `cls.blank_tz_site`
- `_make_legacy_event(*, source_line, start_time, end_time, status='allocation', telescope='NTT',
  instrument='EFOSC2', target_list=None, dark_line=_DARK_LINE, title=None, description=None)`
- `_make_three_night_group(**overrides)` -> 3 blank-url events at 23:00 UTC on `_THREE_NIGHTS`
- `_THREE_NIGHT_LINE = 'NTT EFOSC2 allocation 9-12 July'`; `_THREE_NIGHTS = [July 9, 10, 11]`
  of `date.today().year` (ESO noon-to-noon convention drops the closing boundary)
- `TestWindowContainmentGuard` (L589) -- the WR-08 real-path test; asserts
  `"falls outside the run's window"` appears in stderr. It does NOT currently assert a reason
  category, so nothing pins the old `other` classification.
- `TestKeyCollisionDetection` (L707) -- `_first_night_span()` helper returning
  `(night, start_time, end_time)` for `_THREE_NIGHTS[0]`; shows the pre-existing-run +
  pre-existing-ALLOC-event fixture shape and the `'unexplained (key_collision): 1'` assertion style
- `TestDryRunMatchesRealRun` (L257) -- the existing dry/real agreement test, asserts on
  substrings of the summary line

`solsys_code/campaign_utils.py:860` -- `preview_campaign_run_action(run, fields) -> str`
returns `'created'` when `run is None`, else `'updated'`/`'unchanged'` by the same field-diff
rule `insert_or_create_campaign_run()` uses. Unchanged by this plan.

`docs/runbooks/telescope_runs_calendar.rst` (1479 lines), two operator-facing reason lists:
- L~906-935 "Always run ``--dry-run`` first and read the unexplained list." -- the "What it
  deliberately leaves alone" enumeration, where `key_collision` is bolded and given its
  operator action
- L~1390-1418 "A reported unexplainable event during the classical cutover" -- the **Cause:**
  enumeration and the **Fix:** operator-action sentence, both of which name `key_collision`
</interface_context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Hoist the three per-event preconditions into one helper and give the window mismatch its own reason</name>
  <files>solsys_code/management/commands/cutover_classical_allocations.py, solsys_code/tests/test_cutover_classical_allocations.py</files>
  <behavior>
    Write this test FIRST and watch it fail (RED) before touching the command module. New
    test class in `solsys_code/tests/test_cutover_classical_allocations.py`, following
    `TestWindowContainmentGuard`'s fixture shape:

    - `TestDryRunAppliesTheWindowCheck` -- fixture: `self._make_three_night_group()` plus one
      extra event built with `_make_legacy_event(source_line=_THREE_NIGHT_LINE, ...)` whose
      `start_time` is 23:00 UTC on July 20 of `date.today().year` (well outside the July
      9..11 window the schedule line implies), `target_list=self.campaign`.
      - Test A: `call_command('cutover_classical_allocations', '--dry-run', ...)` raises
        `CommandError`; the captured stdout contains `unexplained (window_mismatch): 1` and
        `events re-keyed: 3` (the outlier is NOT counted as a would-be re-key); stderr
        contains `falls outside the run's window`; `CampaignRun.objects.count() == 0`; the
        outlier still has `url == ''` and no `CalendarEventMeta` row.
      - Test B: the immediately following real `call_command(...)` over the same fixture
        also raises `CommandError` and also reports `unexplained (window_mismatch): 1` and
        `events re-keyed: 3` -- the same category the dry run named, so the real path's
        reclassification away from the generic bucket is pinned too.

    Both assertions on the reason category fail today (the real path reports the generic
    bucket; the dry run reports nothing at all and exits 0). Confirm both failures before
    implementing.
  </behavior>
  <action>
Read the current file first -- line numbers in `<interface_context>` were observed at
planning time and may have shifted.

Step 1 (RED). Add the `TestDryRunAppliesTheWindowCheck` class described in the behavior block above, to
`solsys_code/tests/test_cutover_classical_allocations.py`, placed after
`TestWindowContainmentGuard`. Run
`python manage.py test solsys_code.tests.test_cutover_classical_allocations` and record that
both new tests fail for the reasons stated (dry run exits 0 / real run names the generic
bucket). Do not proceed until you have seen the red.

Step 2. In `cutover_classical_allocations.py`, add the new reason constant next to
`_KEY_COLLISION` -- `_WINDOW_MISMATCH = 'window_mismatch'` -- with a short comment saying why
it is not folded into the generic bucket: an out-of-window derived night is a known, named
condition with its own operator action (correct the event's stored start time, or the
schedule line's date range, so the two agree), not an unexpected error, and the module's
contract is that every printed reason tells the operator what to DO. Add its
`_REASON_LABELS` entry -- `"derived observing night falls outside the run's own window"` --
placed before the generic entry so the report order stays meaningful. The summary breakdown
loop indexes `_REASON_LABELS[category]` directly, so a missing entry is a KeyError at report
time, not a silent gap.

Step 3. Add `class _WindowMismatchError(Exception)` alongside `_KeyCollisionError`, with a
docstring in the same register as its neighbour: raised by the shared precondition helper
when an event's independently-derived observing night lies outside the window the group's
schedule line implies; a dedicated `except` clause ahead of the broad catch-all routes it to
the named reason rather than the generic one.

Step 4. Add a module-level helper -- the whole point of this change -- placed after
`_extract_source_line()` and before `class Command`:

  `def _check_event_night(event, run, site_zone, window_start, window_end, claimed_nights) -> date:`

It performs, in this order, the three preconditions that today are split across two branches,
and returns the resolved observing night only if all three hold:
  (a) resolve the night with `observing_night(event.start_time, site_zone)`;
  (b) if the night is not inside the inclusive `window_start`..`window_end` range, raise
      `_WindowMismatchError` -- keep the message wording byte-identical to the real path's
      current message so the existing `TestWindowContainmentGuard` stderr assertion and the
      operator's eye both still find it: `f"derived night {night} falls outside the run's
      window {window_start}..{window_end}"`;
  (c) if the night is already in `claimed_nights`, raise `_KeyCollisionError` with the
      existing in-run message;
  (d) when `run is not None`, probe `CalendarEvent.objects.filter(url=allocation_night_url(run,
      night)).exclude(pk=event.pk).first()` and raise `_KeyCollisionError` with the existing
      already-held message if a holder exists.

Give it a Google-style docstring (Args/Returns/Raises, per this repo's convention) that states
the two load-bearing facts a future reader needs: (1) the window bounds are parameters rather
than read off `run`, so both branches provably evaluate the SAME window -- the dry run passes
the previewed values, the real path passes the values it just wrote -- and so the check still
applies on the dry-run path when no `CampaignRun` exists yet; (2) step (d) is the one
precondition that is legitimately skipped when `run is None`, because a run with no primary
key can hold no `ALLOC:` url in the table, and that skip preserves parity rather than breaking
it -- when the real pass then creates the run, its fresh primary key cannot collide with any
pre-existing row either.

Step 5. Rewrite BOTH per-event loops to call the helper and nothing else for their checks.

Dry-run branch: call
`_check_event_night(event, run, site_zone, fields['window_start'], fields['window_end'],
claimed_nights)` -- note it reads the window out of `fields`, i.e. what the real pass would
write, NOT off `existing_run`, whose window may be stale from an earlier import. Real branch:
same call, same arguments, inside the existing per-event savepoint and still positioned ahead
of `update_calendar_event_key_and_fields()` / `adopt_event_into_run()` so a rejected event
stays byte-identical. Delete the inline checks both branches now duplicate -- when you are
done, the window comparison and both collision probes exist in the helper only.

Give each branch the same three-clause except ladder, in this order:
`except _WindowMismatchError` -> `_mark_unexplained([event], _WINDOW_MISMATCH, str(exc))`;
`except _KeyCollisionError` -> the existing key-collision handling; `except Exception` -> the
existing catch-all. In both branches the claimed-night insert and the re-key counter must stay
where they are -- after a successful path only -- so a rejected event is counted by neither.
The real branch keeps its `claimed_nights.add(night)` outside the savepoint's `with` block,
exactly as today.

Step 6. Update the module docstring. In the sentence enumerating the reasons an event or its
group can be left unexplained, add the out-of-window case in the same voice as its neighbours,
naming what the operator does about it. Then extend the closing `--dry-run` paragraph with the
contract this change establishes: a dry run applies every per-event precondition the real pass
applies, and the two passes agree on the re-key count, the unexplained count, every per-reason
count and the exit status -- which is the property that makes the promise in the preceding
sentence true rather than aspirational.

Step 7 (GREEN). Re-run the test module; the two new tests pass and every pre-existing test
class stays green, unmodified. Commit as
`fix(quick-260913-ti3): apply every per-event precondition on both cutover paths`.
  </action>
  <verify>
    <automated>cd /home/tlister/git/fomo_devel &amp;&amp; python manage.py test solsys_code.tests.test_cutover_classical_allocations 2>&amp;1 | tail -5 | grep -q 'OK'</automated>
    <automated>cd /home/tlister/git/fomo_devel &amp;&amp; test "$(grep -v '^\s*#' solsys_code/management/commands/cutover_classical_allocations.py | grep -c 'window_start <= night')" -eq 1</automated>
    <automated>cd /home/tlister/git/fomo_devel &amp;&amp; test "$(grep -v '^\s*#' solsys_code/management/commands/cutover_classical_allocations.py | grep -c '_check_event_night(')" -ge 3</automated>
    <automated>cd /home/tlister/git/fomo_devel &amp;&amp; grep -v '^\s*#' solsys_code/management/commands/cutover_classical_allocations.py | grep -q "fields\['window_start'\]"</automated>
  </verify>
  <done>
    The test module passes in full. The window comparison appears exactly once in
    non-comment code (inside the helper); `_check_event_night(` appears exactly three times
    (its definition plus one call per branch); the dry-run branch reads the window from the
    previewed fields. The window-mismatch stderr line still contains the phrase the existing
    WR-08 test asserts, and both new tests pin the named category on both paths.
  </done>
</task>

<task type="auto">
  <name>Task 2: Pin dry-run/real parity as a regression guard across all three preconditions</name>
  <files>solsys_code/tests/test_cutover_classical_allocations.py</files>
  <behavior>
    New test class `TestDryRunAndRealRunAgree`, following `TestKeyCollisionDetection`'s
    fixture style (it already demonstrates the pre-existing-run and pre-existing-ALLOC-event
    setup this needs).

    Fixture -- one `Source line:` group that trips all three preconditions at once:
    - Pre-create the group's `CampaignRun` with the `source_identifier` computed the way
      `TestKeyCollisionDetection.test_existing_url_collision_...` computes it
      (`_source_identifier(parse_run_line(_THREE_NIGHT_LINE), _THREE_NIGHTS[0],
      _THREE_NIGHTS[-1])`), but give it a DELIBERATELY WIDER window than the schedule line
      implies -- `window_start=date(year, 7, 1)`, `window_end=date(year, 7, 31)`. This is what
      makes requirement (3) load-bearing: an implementation that read the window off the
      pre-existing run would wave the outlier through on the dry-run path while the real pass
      rejected it. Add a comment saying exactly that, so a later reader cannot "tidy" the wide
      window away.
    - Pre-create a `CalendarEvent` holding `allocation_night_url(run, _THREE_NIGHTS[1])` (the
      import-ran-first row), so the July 10 legacy event hits the existing-url probe.
    - Legacy blank-url events, all `source_line=_THREE_NIGHT_LINE`,
      `target_list=self.campaign`, each starting 23:00 UTC on its own evening date: July 9;
      a SECOND July 9 (in-run collision); July 10 (existing-url collision); July 11; and
      July 20 (window mismatch).

    Expected outcome, identical on both passes: `events re-keyed: 2` (July 9 first claimant
    and July 11), `unexplained: 3` with `key_collision: 2` and `window_mismatch: 1`, and
    `CommandError` raised.

    - `test_dry_run_and_real_run_report_identical_counts_and_reasons`: run `--dry-run`
      capturing stdout, then the real run capturing stdout, both inside
      `assertRaises(CommandError)`. Parse each summary into a comparable form rather than
      asserting substrings one at a time -- extract the `events re-keyed: N`,
      `unexplained: N` and `runs created/updated/unchanged` numbers from the `Done` line, and
      collect every `unexplained ({category}): {count}` line into a dict -- then assert the
      dry-run and real-run structures are equal with a single `assertEqual`. That single
      equality is the guard: any future check added to one branch only breaks it immediately,
      which is the whole reason the helper was hoisted.
    - `test_out_of_window_event_is_byte_identical_after_both_passes`: after the dry run,
      assert the July 20 event still has `url == ''` and no `CalendarEventMeta` row; after the
      real run, assert the same. Also assert the pre-existing ALLOC row still holds its url
      and that every `ALLOC:`-prefixed url in the table is still unique.
  </behavior>
  <action>
Add the `TestDryRunAndRealRunAgree` class described in the behavior block above, placed after
`TestKeyCollisionDetection`. Reuse the base class fixtures (`self.campaign`, `self.ntt`) and
the module constants (`_THREE_NIGHT_LINE`, `_THREE_NIGHTS`); do not introduce a new
`Observatory` or a new schedule line. No `Target` is needed here (a classical `CampaignRun`
carries `target=None`), but if one is ever added use `NonSiderealTargetFactory` -- never the
sidereal factory (CLAUDE.md: FOMO is exclusively non-sidereal).

Write a small module-level or class-level parsing helper for the summary text rather than
repeating regex inline in both assertions, and give it a one-line docstring saying it exists so
the parity assertion compares structures rather than a hand-maintained list of substrings.

Give the class a docstring naming 35-REVIEW.md NF-02 and stating the contract in one sentence:
the dry run and the immediately following real run agree on every counter, every reason
category and the exit status -- the property that would have caught the WR-11 rebuild copying
only two of the real path's three checks.

Run `python manage.py test solsys_code.tests.test_cutover_classical_allocations` -- all
classes green. Commit as
`test(quick-260913-ti3): pin dry-run/real parity across all three cutover preconditions`.
  </action>
  <verify>
    <automated>cd /home/tlister/git/fomo_devel &amp;&amp; python manage.py test solsys_code.tests.test_cutover_classical_allocations 2>&amp;1 | tail -5 | grep -q 'OK'</automated>
    <automated>cd /home/tlister/git/fomo_devel &amp;&amp; python manage.py test solsys_code.tests.test_cutover_classical_allocations.TestDryRunAndRealRunAgree 2>&amp;1 | tail -5 | grep -q 'OK'</automated>
    <automated>cd /home/tlister/git/fomo_devel &amp;&amp; ! grep -qE '(^|[^n])SiderealTargetFactory' solsys_code/tests/test_cutover_classical_allocations.py</automated>
  </verify>
  <done>
    `TestDryRunAndRealRunAgree` passes and asserts dry-run/real equality of the full counter
    and reason-category structure plus exit status over a fixture tripping all three
    preconditions; the out-of-window event is proven byte-identical after both passes; the
    pre-existing run's deliberately wider window is present and commented; the whole module
    is green; no sidereal target factory is used.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add the new reason to the operator runbook, audit the paired notebook, and run the quality gates</name>
  <files>docs/runbooks/telescope_runs_calendar.rst</files>
  <action>
Step 1 -- runbook (CLAUDE.md paired-docs rule: this is a behavior change to
`cutover_classical_allocations.py`, so the operator-facing reason list is part of the
deliverable, not follow-up). Edit both reason lists in
`docs/runbooks/telescope_runs_calendar.rst`, matching the existing bolded `key_collision`
style rather than inventing a new one:

  (a) In the "Always run ``--dry-run`` first and read the unexplained list." section
      (around L906-935), add a bolded `window_mismatch` item to the enumeration of what the
      command deliberately leaves alone: the event's own derived observing night falls outside
      the window its own schedule line implies (an off-by-one sub-night boundary, or a stored
      start time that disagrees with the line's date range). State the operator action --
      correct the event's stored start time in the Django admin, or correct the schedule
      line's date range and re-import, so the two agree -- and state WHY it is refused rather
      than converted: re-keying it would write an `ALLOC:` url outside the run's own window,
      which the next ``reconcile_campaign_runs`` sweep classifies as stale and deletes. While
      you are in this paragraph, add one sentence recording that ``--dry-run`` applies every
      per-event check the real run applies, so its counts, its reason breakdown and its exit
      status match what the real run then reports -- that is what makes "always run
      ``--dry-run`` first" worth the operator's time.
  (b) In the "A reported unexplainable event during the classical cutover" troubleshooting
      section (around L1390-1418), add `window_mismatch` to the **Cause:** enumeration and its
      corrective action to the **Fix:** sentence, both in the same voice the neighbouring
      `key_collision` clauses use.

Keep the reStructuredText conventions of the surrounding text (double-backtick literals,
bolded reason names, existing line-wrap width).

Step 2 -- paired notebook audit (CLAUDE.md requires the outcome recorded either way).
`docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb` is the paired notebook for
`cutover_classical_allocations.py`. Audit it rather than assuming: list every cell that
invokes the cutover command and inspect the committed output of each for a reason breakdown
line. A planning-time probe found its cutover cells exercise only the `no_source_line` reason
and no window-mismatch or collision output, which would mean this change alters none of its
recorded output and no re-execution is needed -- CONFIRM that rather than inheriting it. If
confirmed, add the notebook to neither the commit nor the diff and record the audit outcome
and its evidence (which cells, what their recorded output shows) in the SUMMARY. If any
committed cell output WOULD change, re-execute with
`jupyter nbconvert --to notebook --execute --inplace` and commit the notebook with output, as
this repo does for everything under `pre_executed/`.

Step 3 -- quality gates. Run, over the files actually changed by this plan:
`pre-commit run ruff --files solsys_code/management/commands/cutover_classical_allocations.py
solsys_code/tests/test_cutover_classical_allocations.py` and the same file list through
`pre-commit run ruff-format --files ...`. Use pre-commit, not a bare `ruff` on PATH -- D-07:
pre-commit pins the enforced version (v0.2.1) and an unpinned binary reports findings the gate
does not have. Fix anything either reports (120-col limit, single quotes) and re-run the test
module afterwards.

Commit as `docs(quick-260913-ti3): document the window_mismatch cutover reason`.
  </action>
  <verify>
    <automated>cd /home/tlister/git/fomo_devel &amp;&amp; test "$(grep -c 'window_mismatch' docs/runbooks/telescope_runs_calendar.rst)" -ge 2</automated>
    <automated>cd /home/tlister/git/fomo_devel &amp;&amp; pre-commit run ruff --files solsys_code/management/commands/cutover_classical_allocations.py solsys_code/tests/test_cutover_classical_allocations.py</automated>
    <automated>cd /home/tlister/git/fomo_devel &amp;&amp; pre-commit run ruff-format --files solsys_code/management/commands/cutover_classical_allocations.py solsys_code/tests/test_cutover_classical_allocations.py</automated>
    <automated>cd /home/tlister/git/fomo_devel &amp;&amp; python manage.py test solsys_code.tests.test_cutover_classical_allocations 2>&amp;1 | tail -5 | grep -q 'OK'</automated>
  </verify>
  <done>
    Both runbook reason lists name `window_mismatch` with its operator action in the existing
    style, and the dry-run section records the parity contract. The paired notebook audit is
    done with its outcome and evidence recorded in the SUMMARY (re-executed and committed if
    any recorded output changed, explicitly confirmed unaffected if not). Pinned ruff and
    ruff-format are clean on both changed Python files and the test module is still green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator -> management command | An operator decides, from this command's dry-run report, whether the database is safe to convert. The report IS the boundary: a dry run that under-reports grants consent the operator did not knowingly give. |
| command -> CalendarEvent table | The command re-keys third-party rows in place. `CalendarEvent.url` carries no unique constraint, so the database enforces nothing -- correctness rests entirely on this command's own per-event preconditions. |
| command -> next reconcile sweep | An `ALLOC:` url written outside its run's window is handed to `project_allocation()`'s convergence step, which deletes it as stale. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-ti3-01 | Information disclosure (to the operator: withheld) | `Command.handle` dry-run branch | high | mitigate | Hoist all three preconditions into `_check_event_night()` called by both branches, so the dry run cannot under-report the real pass's rejections (Task 1); pin with a full-structure parity assertion (Task 2) |
| T-ti3-02 | Tampering (data loss via a downstream sweep) | out-of-window `ALLOC:` url + `project_allocation()` convergence | high | mitigate | The window check now runs on both paths and raises before any write, so no out-of-window url is ever created and the next sweep has nothing to delete (Task 1 step 5) |
| T-ti3-03 | Repudiation (an action with no recognisable reason) | `_OTHER` bucket swallowing a named condition | medium | mitigate | Dedicated `_WINDOW_MISMATCH` reason with a `_REASON_LABELS` entry, module-docstring vocabulary, and runbook operator action (Tasks 1 and 3) |
| T-ti3-04 | Tampering (stale read) | dry-run window read off a pre-existing `CampaignRun` | medium | mitigate | Both branches pass the window out of `fields`; the parity fixture's deliberately wider pre-existing window makes any regression fail the test (Tasks 1 and 2) |
| T-ti3-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs in this plan -- no new dependency is added, so the package-legitimacy gate does not apply |
</threat_model>

<verification>
1. `python manage.py test solsys_code.tests.test_cutover_classical_allocations` -- full module
   green, including every pre-existing class unmodified. Never `./manage.py`, and never the
   full unfiltered suite (`test_views.TestEphemeris` segfaults in native ASSIST).
2. `TestDryRunAndRealRunAgree` proves the two passes agree on every counter, every reason
   category and exit status over a fixture tripping all three preconditions.
3. The window comparison and both collision probes exist in exactly one place in the command
   module (the helper); the dry-run branch reads the window from the previewed fields.
4. `pre-commit run ruff --files ...` and `pre-commit run ruff-format --files ...` clean on both
   changed Python files.
5. Runbook names `window_mismatch` in both operator-facing reason lists; paired-notebook audit
   outcome recorded in the SUMMARY either way.
</verification>

<success_criteria>
- A dry run can no longer exit 0 over a fixture the immediately following real run rejects.
- An out-of-window derived night is reported as `window_mismatch` on both paths, never as a
  generic unexpected error, and the event is left byte-identical on both.
- The three preconditions live in one helper used by both branches.
- The real path's conversion behavior is unchanged apart from the reason category.
- Module docstring and operator runbook both carry the new reason with its operator action.
</success_criteria>

<output>
Create `.planning/quick/260913-ti3-fix-35-review-md-nf-02-hoist-the-cutover/260913-ti3-SUMMARY.md` when done.
Every commit message ends with:
Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01MBQXMHen2DLP5owNpwRMKr
</output>
