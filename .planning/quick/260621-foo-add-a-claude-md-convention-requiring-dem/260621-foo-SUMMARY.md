---
phase: quick
plan: 01
subsystem: docs
tags: [claude-md, convention, demo-notebooks]
dependency-graph:
  requires: []
  provides: [demo-notebook-companion-convention]
  affects: [planner, plan-checker, executor, verifier]
tech-stack:
  added: []
  patterns: []
key-files:
  created: []
  modified:
    - CLAUDE.md
decisions:
  - "Codified the demo-notebook-companion gap (hit twice via quick tasks 260619-f7u and 260620-v9x) as a standing CLAUDE.md convention rather than relying on per-plan vigilance."
metrics:
  duration: ~5 min
  completed: 2026-06-21
status: complete
---

# Quick Task 260621-foo: Add CLAUDE.md demo-notebook-companion convention Summary

Added a convention bullet to CLAUDE.md's first (hand-authored) Conventions section requiring
that demo notebooks paired with `telescope_runs.py`, `load_telescope_runs.py`, and
`sync_lco_observation_calendar.py` be scoped into `files_modified` and kept in sync whenever the
paired module's behavior changes.

## What Was Built

Inserted a new bullet immediately after the existing "Planning-doc terminology" bullet and
before the `<!-- GSD:project-start -->` marker in `CLAUDE.md`. The bullet:

- States demo notebook companions are part of the deliverable, not optional polish.
- Names all three module/notebook pairs (`telescope_runs.py` ↔ `telescope_runs_demo.ipynb`,
  `load_telescope_runs.py` ↔ `load_telescope_runs_demo.ipynb`,
  `sync_lco_observation_calendar.py` ↔ `sync_lco_observation_calendar_demo.ipynb`), all under
  `docs/notebooks/pre_executed/`.
- Requires plans changing one of these modules' behavior to include the paired notebook in
  `files_modified` and regenerate it via `jupyter nbconvert --to notebook --execute --inplace`.
- Notes the list should be extended when new modules get their own demo notebook.
- References both prior quick-task fixes for this exact gap (260619-f7u for Phase 5,
  260620-v9x for Phase 6).
- Calls out responsibilities for planner, plan-checker, executor, and verifier subagents.

The second, GSD-managed Conventions section (sourced from CONVENTIONS.md, between the
`GSD:conventions-start`/`GSD:conventions-end` markers) was left untouched, as required — it is
regenerated by tooling and a hand-edit there would be lost.

## Deviations from Plan

None - plan executed exactly as written.

## Verification

- `grep -n 'Demo notebook companions are part of the deliverable' CLAUDE.md` → matches at line 106
  (inside the first Conventions section, before the second `## Conventions` heading at line 259).
- `grep -n '## Conventions' CLAUDE.md` → confirms exactly two headings (lines 87 and 259); the new
  bullet text appears only once, between them.
- `git diff CLAUDE.md` → confirms the only change is a 22-line addition in the correct location;
  no other part of CLAUDE.md was modified.

## Out-of-Scope Note

A pre-existing uncommitted change to `src/fomo/settings.py` (unrelated LCO_APIKEY edit) was
present in the working tree before this task started. It was left untouched, unstaged, and
uncommitted throughout — confirmed via `git status --short` before and after the commit.

## Self-Check: PASSED

- FOUND: CLAUDE.md (bullet present at line 106, confirmed via grep)
- FOUND: commit fe378de (confirmed via `git log --oneline -1`)
- FOUND: src/fomo/settings.py left modified/unstaged (confirmed via `git status --short`)
