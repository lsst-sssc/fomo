---
phase: quick-260618-h97
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [.gitignore]
autonomous: true
requirements: [QUICK-260618-h97]
must_haves:
  truths:
    - "git status no longer shows src/data/ as an untracked directory"
    - "Files written under src/data/ (Django MEDIA_ROOT) are ignored by git"
  artifacts:
    - path: ".gitignore"
      provides: "ignore rule for Django MEDIA_ROOT (src/data/)"
      contains: "src/data/"
  key_links:
    - from: ".gitignore"
      to: "src/data/ (MEDIA_ROOT from src/fomo/settings.py:176)"
      via: "ignore pattern"
      pattern: "src/data/"
---

<objective>
Add an ignore rule for `src/data/` to `.gitignore` so that downloaded
DataProduct files (Django's MEDIA_ROOT) are never accidentally committed.

Purpose: `MEDIA_ROOT = os.path.join(BASE_DIR, 'data')` (src/fomo/settings.py:176)
resolves to `src/data/` inside the git checkout. It accumulates downloaded
DataProduct files (e.g. ATLAS forced-photometry CSVs like
`src/data/NGC1566/none/atlas_phot_ngc1566.csv`) that should never be tracked.
This is a stopgap; relocating MEDIA_ROOT outside the repo is a separate future
discussion and is explicitly out of scope here.

Output: One new ignore entry in `.gitignore`, placed near the existing
`docs/notebooks/data/**` rule, following that file's conventions.
</objective>

<execution_context>
@/home/tlister/git/fomo_devel/.claude/gsd-core/workflows/execute-plan.md
@/home/tlister/git/fomo_devel/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.gitignore
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add src/data/ ignore rule to .gitignore</name>
  <files>.gitignore</files>
  <action>Add a new ignore entry for Django's MEDIA_ROOT directory. Insert it
  immediately after the existing `docs/notebooks/data/**` line (currently line
  160) so related data-ignore rules stay grouped. Add a short comment above the
  entry explaining what it is, matching the comment style used elsewhere in the
  file (a `#` header line above the pattern). Use the comment text:
  `# Django MEDIA_ROOT — downloaded DataProduct files (src/fomo/settings.py)`
  and the ignore pattern `src/data/` (trailing slash to scope to the directory).
  Do NOT remove or untrack any existing files (src/data/ is currently untracked,
  nothing is committed). Do NOT modify settings.py or any other file.</action>
  <verify>
    <automated>cd /home/tlister/git/fomo_devel && grep -q '^src/data/$' .gitignore && git check-ignore src/data/NGC1566/none/atlas_phot_ngc1566.csv</automated>
  </verify>
  <done>`.gitignore` contains the `src/data/` pattern with a descriptive
  comment near the `docs/notebooks/data/**` rule; `git check-ignore` confirms a
  representative file under src/data/ is ignored; `git status` no longer lists
  `src/data/` as untracked.</done>
</task>

</tasks>

<verification>
- `grep -n 'src/data/' .gitignore` shows the new entry near line 160.
- `git check-ignore src/data/NGC1566/none/atlas_phot_ngc1566.csv` exits 0
  (path is ignored).
- `git status --porcelain` no longer reports `src/data/`.
- No changes to `src/fomo/settings.py` or any non-.gitignore file.
</verification>

<success_criteria>
`src/data/` is ignored by git, matching FOMO's existing .gitignore conventions,
with no other files touched and no tracked files removed.
</success_criteria>

<output>
Create `.planning/quick/260618-h97-add-src-data-to-gitignore-this-directory/260618-h97-01-SUMMARY.md` when done
</output>
