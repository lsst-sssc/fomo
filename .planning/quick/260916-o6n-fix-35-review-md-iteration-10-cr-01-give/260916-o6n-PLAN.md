---
phase: quick-260916-o6n
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - solsys_code/allocation_projector.py
  - solsys_code/tests/test_allocation_projector.py
  - docs/runbooks/telescope_runs_calendar.rst
  - docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb
autonomous: true
requirements: [ALLOC-03, CR-01-ITER10]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A human-confirmed `ALLOC:` night whose retirement was declined still receives the ordinary title/description/target_list refresh on the same sweep, so a later `mark_cancelled`/`mark_weather_failure` reaches it — the property `allocation_night_description()`'s docstring states and 35-REVIEW.md iteration 10 CR-01 found broken."
    - "The refresh reaches the night through the `receiver_on_run_observation_save()` path alone, with no explicit `reconcile_run()` call (the review's trace steps 2-3)."
    - "The refreshed night keeps its primary key, `start_time`, `end_time`, its stored dark-window description line, and its `confirmed_by`/`confirmed_at` stamp byte-identical."
    - "The refresh is reported under `updated` on the sweep that changes the labels and `unchanged` on every sweep after, alongside `detach_declined` — never under `retired` or `created`."
    - "A `--dry-run` preview reports the identical `detach_declined` + `updated`/`unchanged` pair and writes nothing."
    - "The declined-retirement refresh makes zero `sun_event()` calls, so D-13's no-astropy-on-an-existing-night rule and the bounded cost of the repeating decline both hold."
    - "The surviving night stays in `retired_urls` and is NOT added to `active_urls`, so it is excluded from the D-14 convergence step and never reads as live in the `RUN:`-namespace convergence."
    - "An unconfirmed retirement still deletes the night and still counts `retired: 1` — the control case the fall-through must not weaken."
    - "The runbook's `detach_declined` section states the counter pair for the declined-retirement cause, exactly as the `remint_declined` section already states its own."
    - "`reconcile_campaign_runs_demo.ipynb` demonstrates the refreshed `[CANCELLED]` title on a declined-retirement night with real executed output."
  artifacts:
    - "solsys_code/allocation_projector.py — `_label_fields()` and `_refresh_labels()` helpers; the retirement decline falls through to `_refresh_labels()`"
    - "solsys_code/tests/test_allocation_projector.py — `TestDeclinedRetirementStillUpdatesLabels` (7 tests)"
    - "docs/runbooks/telescope_runs_calendar.rst — counter-pair paragraph in the `detach_declined` section"
    - "docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb — extended CR-05 retirement-guard cell (execution_count 20) with executed output"
  key_links:
    - "retirement decline branch -> `_refresh_labels()` -> `update_calendar_event_key_and_fields()` + `_link_event_to_run()`"
    - "`_label_fields()` is the single builder of the three label fields, shared by the decline branch, the plain-update preview and the plain-update write — so the two paths cannot drift apart"
    - "`retired_urls` (never `active_urls`) is what keeps the surviving night out of both convergence steps"
    - "`preserved_dark_window_line(existing)` is what keeps the refresh free of a `sun_event()` call (D-13)"
---

<objective>
Close 35-REVIEW.md iteration 10 CR-01 (critical, open; authorised as a quick task in
`.planning/STATE.md` Blockers/Concerns, "Carried forward from Phase 35", first bullet).

Plan 35-23's CR-05 guard made a human-confirmed `ALLOC:` night survive its own retirement
(the delete is declined, `detach_declined` is counted, `retired` is withheld), but the
branch still ends in an unconditional `continue`, so the surviving night never reaches the
plain-update path that writes `title`/`description`/`target_list`. A later `mark_cancelled`
therefore never reaches a confirmed retired night — silently, permanently, under no
counter. That is exactly the defect plan 35-23's own CR-04 fix removed from the re-mint
branch one branch over; this plan gives the retirement decline the same fall-through.

Purpose: restore the property `allocation_night_description()`'s docstring states — "reused
deliberately so a staff `mark_cancelled`/`mark_weather_failure` action reaches allocation
nights the same way it reaches container events" — for the one class of night that can now
outlive its own retirement.

Output: a shared `_refresh_labels()` helper used by both the decline branch and the
plain-update path, a 7-test `TestDeclinedRetirementStillUpdatesLabels` class, the runbook's
`detach_declined` counter pair, and a re-executed `reconcile_campaign_runs_demo.ipynb`
(CLAUDE.md paired-docs rule).

Governing decisions this plan honours unchanged: **D-13** (`sun_event()` runs only for a
night being created or re-minted — the declined-retirement refresh reuses the stored
dark-window line and pays no astropy call), **D-14** (convergence excludes nights already
handled as retired — `retired_urls`, never `active_urls`), and **D-07** (ruff and
ruff-format gates run through pre-commit, whose pinned v0.2.1 is the enforced version).

Explicitly NOT in scope, and deliberately untouched:
- **WR-01** (`allocation_projector.py:1437-1441`, the declined-re-mint provenance-token
  seam) — deferred by the owner at UAT 2026-09-16 (`.planning/STATE.md` line 389). This
  plan must not gate, move or "improve" that write. Its own decision on the analogous
  question is stated below and is a *different* answer for a *different* path: the
  declined-retirement refresh records **no** provenance at all.
- **WR-04** (the step-4 staleness warning wording advisory).
- **WR-02, WR-03, WR-05, WR-06, WR-07** and the carried-forward WR-08..WR-11 / IN-01 /
  IN-03 review residue. In particular WR-07 (`load_telescope_runs`'s night summary omits
  both decline counters) is NOT closed here.
- `solsys_code/campaign_reconciler.py` and `ReconcileResult`: no new counter is required,
  because the refresh reuses the existing `updated`/`unchanged` fields. That file must not
  appear in this task's diff.
</objective>

<execution_context>
@/home/tlister/git/fomo_devel/.claude/gsd-core/workflows/execute-plan.md
@/home/tlister/git/fomo_devel/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/phases/35-allocation-layer-classical-cutover/35-REVIEW.md
@solsys_code/allocation_projector.py
@solsys_code/tests/test_allocation_projector.py
@docs/runbooks/telescope_runs_calendar.rst
</context>

<interface_context>
Verified live at HEAD `cbf62ea` (not quoted from the review — the review's `0bc1ccd`
numbers were re-confirmed and are unchanged):

`solsys_code/allocation_projector.py`
- `project_allocation(run, *, dry_run=False) -> tuple[ReconcileResult, set[str], set[str]]`
  at **:1014**; the per-night loop starts **:1106**.
- The retirement branch: **:1118**-**:1213**. `existing_deletable` is computed **:1182**,
  the deletes fire **:1198-1202**, `retired` is withheld on a decline **:1207-1208**, the
  IN-05 comment is **:1209-1212**, the unconditional `continue` is **:1213**.
- The plain-update path's field build is **:1399-1403**; the dry-run preview is
  **:1405-1420**; the create write is **:1422-1433**; the update write is **:1434-1441**;
  `totals[action] += 1` is **:1442**.
- Available in module scope, already imported at **:44-60**: `insert_or_create_calendar_event`,
  `preview_calendar_event_action`, `update_calendar_event_key_and_fields` (from
  `solsys_code.calendar_utils`); `_clearable_declined_and_unattributed`, `_link_event_to_run`,
  `_may_write`, `event_description`, `ReconcileResult` (from
  `solsys_code.campaign_reconciler`); `CalendarEventMeta`, `CampaignRun`; `sun_event`.
- `allocation_night_title(run) -> str` (**:197**) returns `'NTT EFOSC2'`, or
  `'[CANCELLED] NTT EFOSC2'` / `'[WEATHERED] ...'` via
  `campaign_reconciler.RUN_STATUS_CALENDAR_PREFIX` (**campaign_reconciler.py:76-79**,
  keyed on `CampaignRun.RunStatus.CANCELLED` / `.WEATHER_TECH_FAILURE`).
- `allocation_night_description(run, dark_line) -> str` (**:211**).
- `preserved_dark_window_line(event) -> str | None` (**:232**) returns the stored
  description's first line when it starts with `_DARK_WINDOW_PREFIX`
  (`'Dark window (-15 deg, UTC): '`, **:66**), else `None`.

`solsys_code/calendar_utils.py`
- `update_calendar_event_key_and_fields(event, url, fields) -> tuple[CalendarEvent, str]`
  (**:648**) — action is `'updated'` or `'unchanged'`; it compares `{**fields, 'url': url}`.
- `preview_calendar_event_action(event, fields) -> str` (**:678**) — `'created'` when
  `event is None`, else `'updated'`/`'unchanged'` using the identical `getattr(event, f) != v`
  rule, and it compares `fields` only (no `url` key). Since the decline branch's `existing`
  was fetched by `url=url`, the url term is equal on both sides, so preview and real write
  agree exactly.

`solsys_code/campaign_reconciler.py`
- `_link_event_to_run(event, run)` writes only the companion row's `run` field — never
  `confirmed_by`/`confirmed_at`/`is_verified` (pinned by
  `TestDeclinedRemintStillUpdatesLabels.test_declined_night_confirmation_stamp_survives_the_fall_through`).

`solsys_code/tests/test_allocation_projector.py` — fixtures to reuse, not re-invent:
- `AllocationProjectorTestBase.setUpTestData()` creates five `Observatory` rows;
  `self.chilean_site` (obscode `809`, `America/Santiago`) is `_make_run`'s default site.
- `self._make_run(**overrides)` → an APPROVED, `CLASSICAL_FILE`-sourced, campaign-less
  `CampaignRun` with `telescope_instrument='NTT/EFOSC2'`, `window_start=date(2026,7,9)`,
  `window_end=date(2026,7,11)`.
- `self._link_record(run, *, scheduled_start=None, scheduled_end=None, facility='LCO', status='COMPLETED')`
  → creates a `NonSiderealTargetFactory` target (CLAUDE.md), an `ObservationRecord` and the
  `CampaignRunObservation` link, returning `(record, link)`. **Creating the link alone fires
  `receiver_on_run_observation_save()`**, which reaches `project_allocation()` — no explicit
  sweep needed.
- Already imported at module top: `date`, `datetime`, `time`, `timedelta`,
  `timezone as dt_timezone`, `patch`, `uuid4`, `User`, `timezone` (django.utils),
  `CalendarEvent`, `ObservationRecord`, `NonSiderealTargetFactory`, `_DARK_WINDOW_PREFIX`,
  `allocation_events`, `allocation_night_title`, `reconcile_run`, `CalendarEventMeta`,
  `CampaignRun`, `CampaignRunObservation`, `sun_event`. **No new imports are needed.**
- Reference shapes to mirror: `TestDeclinedRemintStillUpdatesLabels` (**:2086-2237**, the
  CR-04 class this one is the twin of), `TestRetirePathAllocationEventGuard`
  (**:519-678**, the CR-05 class whose fixtures produce the declined retirement), and
  `TestNoSunEventRecompute` (**:1049-1098**, the `patch('solsys_code.allocation_projector.sun_event')`
  idiom).

`docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb` — 47 cells. Cell **index
38** is the markdown heading `## A confirmed allocation night survives its own retirement (plan 35-23, CR-05)`;
cell **index 39** is its code cell (`execution_count` **20**), which already builds
`confirmed_retire_run`, `retire_night`, `retire_url`, `retire_pk_before`,
`retire_staff_user`, `retire_record`, `retire_link`, `retire_result`. Cell index 2 copies
`src/fomo_db.sqlite3` to a fresh `tempfile.mkdtemp()` scratch file, exports
`FOMO_DATABASE_PATH` **before** `django.setup()`, asserts the resolved DB is the scratch
copy, and migrates it to head — so routing is automatic and the developer database is never
opened for writing. Cell index 46 removes the scratch directory.
</interface_context>

<!--
planner-discipline-allow: detach_declined

Rationale: `detach_declined` is the production name of the `ReconcileResult` counter this
fix reports under, so it necessarily appears throughout the plan's prose, task actions,
tests and runbook text. The one negative grep that names it (`-eq 0`) targets a DIFFERENT
file — `solsys_code/management/commands/load_telescope_runs.py`, which this plan never
edits and which must keep omitting the counter because aggregating it there is WR-07, out
of scope. No comment this plan authors lands in that file, so the plan's own prose cannot
self-invalidate that gate.
-->

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: the declined retirement falls through to a shared label refresh, proven end-to-end</name>
  <files>solsys_code/allocation_projector.py, solsys_code/tests/test_allocation_projector.py</files>

  <read_first>
    - `solsys_code/allocation_projector.py` lines 1106-1220 (the per-night loop head and the
      whole retirement branch) and 1320-1442 (the CR-04 fall-through comment and the entire
      plain-update path).
    - `solsys_code/tests/test_allocation_projector.py` lines 100-145 (`_make_run`,
      `_link_record`), 519-678 (`TestRetirePathAllocationEventGuard`), 1049-1098
      (`TestNoSunEventRecompute`), 2086-2237 (`TestDeclinedRemintStillUpdatesLabels`).
    - 35-REVIEW.md lines 110-186 (CR-01, the specification) and 190-246 (WR-01, the
      neighbouring seam this task must leave alone).
  </read_first>

  <behavior>
    Write `TestDeclinedRetirementStillUpdatesLabels` FIRST and watch it fail against the
    current unconditional `continue`, then make it pass. Seven tests, all in
    `solsys_code/tests/test_allocation_projector.py`, placed immediately after
    `TestRetirePathAllocationEventGuard` ends (line 678, before
    `class TestTakeoverBlockedCountedOnce`) so the CR-05 guard and its CR-01 follow-up read
    together. Every test uses `night = date(2026, 7, 9)`,
    `run = self._make_run(window_start=night, window_end=night)`,
    `scheduled_start = datetime(2026, 7, 9, 23, 30, tzinfo=dt_timezone.utc)` and
    `self._link_record(run, scheduled_start=scheduled_start, scheduled_end=scheduled_start + timedelta(hours=1))`,
    exactly as `TestRetirePathAllocationEventGuard` does. Every `User.objects.create()`
    username is unique across the class (prefix `cr01-iter10-`).

    1. `test_declined_retirement_still_receives_a_cancelled_title` — mint the night, capture
       `pk`/`start_time`/`end_time` and the stored description's first line, stamp
       `confirmed_by`/`confirmed_at` via
       `CalendarEventMeta.objects.filter(event=event).update(...)`, link the record (the
       retirement is declined here, from the receiver), then set
       `run.run_status = CampaignRun.RunStatus.CANCELLED`,
       `run.save(update_fields=['run_status'])` and `result = reconcile_run(run)`. Assert:
       `event_after.title == allocation_night_title(run)`;
       `event_after.title.startswith('[CANCELLED]')`; `pk`, `start_time`, `end_time` and
       `event_after.description.split('\n', 1)[0]` all identical to the captured values;
       `result.detach_declined == 1`; `result.updated == 1`; `result.retired == 0`;
       `result.created == 0`; `result.remint_declined == 0`. Read the marker text from
       `allocation_night_title(run)` as the primary assertion and treat the literal prefix
       check as the secondary one, matching the CR-04 test's own comment.
    2. `test_declined_retirement_refresh_reaches_the_night_through_the_receiver_alone` — the
       review's trace steps 2-3 with NO explicit sweep: mint, confirm, set
       `run_status = CANCELLED` and save BEFORE linking, then link inside
       `with self.assertLogs('solsys_code.allocation_projector', level='WARNING') as log_ctx:`.
       Assert the event at the captured pk still exists, its title starts with
       `'[CANCELLED]'`, both boundaries are unchanged, and exactly one logged record's
       `getMessage()` contains `'retire declined'`.
    3. `test_declined_retirement_confirmation_stamp_survives_the_refresh` — after the
       cancelled sweep, `CalendarEventMeta.objects.get(event_id=pk_before)` still has
       `confirmed_by_id == staff_user.pk`, a non-None `confirmed_at`, and `run_id == run.pk`.
    4. `test_declined_retirement_reports_detach_declined_alongside_updated_then_unchanged` —
       first post-cancellation sweep: `detach_declined == 1`, `updated == 1`,
       `unchanged == 0`; second sweep with nothing else changed: `detach_declined == 1`,
       `unchanged == 1`, `updated == 0`, `retired == 0`.
    5. `test_declined_retirement_dry_run_parity_for_the_counter_pair` — after the
       cancellation, `dry_result = reconcile_run(run, dry_run=True)` reports
       `detach_declined == 1`, `updated == 1`, `retired == 0`, `created == 0`, and the stored
       event's `title`/`pk`/`start_time`/`end_time` are still the pre-cancellation values
       (the preview wrote nothing); then `real_result = reconcile_run(run)` reports the
       identical `detach_declined` and `updated` values.
    6. `test_declined_retirement_refresh_never_calls_sun_event` — run the post-cancellation
       sweep inside `with patch('solsys_code.allocation_projector.sun_event') as mock_sun_event:`;
       assert `mock_sun_event.assert_not_called()` AND `result.updated == 1`, so the gate is
       not vacuous (D-13, and the bounded cost of a decline that repeats on every sweep).
    7. `test_unconfirmed_retirement_still_deletes_and_counts_retired` — the control case: no
       confirmation stamp, `run_status = CANCELLED` set before linking; the night's event no
       longer exists after the link, and an explicit `reconcile_run(run)` reports
       `retired == 1`, `detach_declined == 0`, `updated == 0`. This pins that the
       fall-through did not weaken the deletable path.

    The class docstring states: the twin of `TestDeclinedRemintStillUpdatesLabels` for the
    retirement branch (35-REVIEW.md iteration 10 CR-01); the decline refuses only the DELETE,
    never the label refresh, which is how a staff `mark_cancelled`/`mark_weather_failure`
    action reaches an allocation night at all; and that this path records no provenance and
    pays no `sun_event()` call, deliberately.
  </behavior>

  <action>
Make the tests above pass with three edits to `solsys_code/allocation_projector.py` and
nothing else.

**Edit 1 — two new module-private helpers.** Insert them immediately BEFORE
`def project_allocation(` (currently line 1014), after `_sync_observation_attribution()`
ends. Google-style docstrings, single quotes, 120-col, matching this module's style.

`_label_fields(run: CampaignRun, dark_line: str | None) -> dict[str, Any]` returns the
three-key mapping `{'title': allocation_night_title(run), 'description':
allocation_night_description(run, dark_line), 'target_list': run.campaign}`. Its docstring
says it is the single builder of the three non-destructive label fields every update path in
this module writes, so the decline branch, the dry-run preview and the real write cannot
drift apart on what "refreshing the labels" means.

`_refresh_labels(run: CampaignRun, existing: CalendarEvent, url: str, dark_line: str | None,
*, dry_run: bool) -> tuple[str, CalendarEvent | None]` builds the fields via
`_label_fields()`, and: under `dry_run`, returns
`(preview_calendar_event_action(existing, fields), None)` having written nothing; otherwise
calls `update_calendar_event_key_and_fields(existing, url, fields)`, then
`_link_event_to_run(event, run)`, and returns `(action, event)`. Its docstring states, as
its contract, the four things it never does: it never writes `start_time`/`end_time`, never
changes the primary key, never touches `confirmed_by`/`confirmed_at`/`is_verified` (only
`_link_event_to_run()`'s single `run` field write), and never calls `sun_event()` or
`_record_sub_night_provenance()` — the caller owns both of those decisions. Note in the
docstring that `preview_calendar_event_action()` omits the `url` term that
`update_calendar_event_key_and_fields()` includes, which is exact rather than approximate
here because both call sites pass the same `url` the `existing` row was fetched by.

**Edit 2 — the retirement decline falls through.** Replace the end of the retirement branch.
The anchor to replace, verbatim from the current file, is the `if existing_deletable:` block
at lines 1207-1213 together with the IN-05 comment between them and the bare `continue`.
After the edit the branch must: (a) keep the existing "retired counts a night that went
away" comment at 1203-1206 unchanged; (b) `continue` immediately after
`totals['retired'] += 1` for the deletable case; (c) for the declined case, call
`_refresh_labels(run, existing, url, preserved_dark_window_line(existing), dry_run=dry_run)`,
add the returned action to `totals`, then `continue`.

Preserve the existing IN-05 sentence about never adding to `active_urls`, and extend it with
the second reason CR-01 supplies: adding the url to `active_urls` would also make this
superseded night read as live in the `RUN:`-namespace convergence, and `retired_urls` already
excludes it from the D-14 step.

Add a comment block above the `_refresh_labels()` call recording, with the CR-01 citation
and the iteration number, that: the declined night SURVIVES its own retirement so it must
keep receiving the three label fields; this is the same two-way split CR-04 gave the re-mint
decline one branch over, for the same reason; `existing_deletable` is True whenever `existing
is None`, so reaching this line proves `existing` is a surviving row and no extra null check
is needed; and the decline refuses only the destructive half.

Also record the provenance decision this plan makes, in that same comment block, because the
review asks for it explicitly: this path deliberately does NOT call
`_record_sub_night_provenance()`. It neither mints nor re-mints and makes no `sun_event()`
call, so it has proved nothing about the stored boundaries; recording the run's current
token here would claim a fact this write never established. Name WR-01 as the finding that
raises that same class of false claim against the re-mint decline's own write further down,
and state that WR-01 is deferred by the owner and left untouched by this change.
`preserved_dark_window_line(existing)` is what keeps the stored dark-window line verbatim, so
D-13's "no `sun_event()` on an existing night" still holds on this path.

**Edit 3 — the plain-update path uses the same two helpers.** Two scoped replacements, both
inside the `else:` branch that handles `existing is not None`:

- Replace the four-line `fields = {...}` literal at 1399-1403 with a single
  `fields = _label_fields(run, dark_line)` call. Leave the whole `refresh_dark_window` block
  above it (1357-1398) and the dry-run preview block below it (1405-1420) exactly as they
  are — including the accepted-divergence comment and its
  `test_preview_may_over_report_updated_by_one_on_a_site_correction` reference.
- In the real-write `else:` branch at 1434-1436, replace the
  `update_calendar_event_key_and_fields(...)` + `_link_event_to_run(...)` pair with
  `action, event = _refresh_labels(run, existing, url, dark_line, dry_run=dry_run)`. Note in
  a short comment that `dry_run` is necessarily False at this line (the preview block above
  already returned), so the argument is passed for one-writer consistency rather than for
  branching. Leave the `if refresh_dark_window: _record_sub_night_provenance(...)` write and
  its T-35-24-02 comment immediately below completely untouched — that is WR-01's seam, and
  it stays outside the helper precisely so the helper can be reused by a path that must not
  record provenance. `totals[action] += 1` at 1442 stays as is.

Do not modify `solsys_code/campaign_reconciler.py`, `ReconcileResult`, `_remint_decline_reason()`,
`_clearable_declined_and_unattributed()`, `_span_needs_remint()`, the legacy-event guard
inside the retirement branch, or `solsys_code/management/commands/load_telescope_runs.py`.
No new counter, no new `ReconcileResult` field.
  </action>

  <verify>
    <automated>cd /home/tlister/git/fomo_devel && python manage.py test solsys_code.tests.test_allocation_projector.TestDeclinedRetirementStillUpdatesLabels -v 2 2>&1 | tail -30</automated>
    <fails_when>The declined-retirement night's title never picks up the `[CANCELLED]` prefix; or its pk, boundaries, stored dark-window line or `confirmed_by`/`confirmed_at` stamp change across the refresh; or the counter pair is not `detach_declined: 1` + `updated: 1` then `unchanged: 1`; or the dry-run preview disagrees with the real run or writes to the database; or the refresh calls `sun_event()`; or the receiver-only path leaves the title stale; or an unconfirmed retirement stops deleting and stops counting `retired: 1`.</fails_when>

    <automated>cd /home/tlister/git/fomo_devel && python manage.py test solsys_code.tests.test_allocation_projector.TestRetirePathAllocationEventGuard solsys_code.tests.test_allocation_projector.TestRetirePathLegacyEventGuard solsys_code.tests.test_allocation_projector.TestDeclinedRemintStillUpdatesLabels solsys_code.tests.test_allocation_projector.TestNoSunEventRecompute solsys_code.tests.test_allocation_projector.TestSetWindowSiteCorrection solsys_code.tests.test_allocation_projector.TestFinalConvergenceGuard -v 1 2>&1 | tail -15</automated>
    <fails_when>The fall-through regressed a neighbouring branch: the CR-05 retirement guard, the legacy-row guard, the CR-04 re-mint fall-through, the D-13 no-recompute contract, the WR-05 site-correction dark-window refresh, or the D-14 convergence partition.</fails_when>

    <automated>cd /home/tlister/git/fomo_devel && CALLS=$(grep -o '= _refresh_labels(' solsys_code/allocation_projector.py | wc -l | tr -d ' ') && DEFS=$(grep -o 'def _refresh_labels(' solsys_code/allocation_projector.py | wc -l | tr -d ' ') && BUILDER=$(grep -o 'def _label_fields(' solsys_code/allocation_projector.py | wc -l | tr -d ' ') && echo "call sites=$CALLS refresh defs=$DEFS builder defs=$BUILDER" && test "$CALLS" -eq 2 && test "$DEFS" -eq 1 && test "$BUILDER" -eq 1 && echo 'OK: one helper, shared by exactly the two intended call sites'</automated>
    <fails_when>`_refresh_labels()` was inlined twice instead of shared (the drift CR-01 is a consequence of), or the plain-update path was left on its own copy of the write.</fails_when>

    <automated>cd /home/tlister/git/fomo_devel && TOUCHED=$(git diff --name-only -- solsys_code/campaign_reconciler.py solsys_code/management/commands/load_telescope_runs.py) && echo "out-of-scope files touched: ${TOUCHED:-none}" && test -z "$TOUCHED" && echo 'OK: no counter plumbing changed, WR-07 untouched'</automated>
    <fails_when>A new `ReconcileResult` counter or a `load_telescope_runs` aggregation change crept in — both explicitly out of scope, and both would widen this quick task into WR-07.</fails_when>

    <automated>cd /home/tlister/git/fomo_devel && pre-commit run ruff --files solsys_code/allocation_projector.py solsys_code/tests/test_allocation_projector.py && pre-commit run ruff-format --files solsys_code/allocation_projector.py solsys_code/tests/test_allocation_projector.py</automated>
    <fails_when>Lint or format drift against the pinned ruff v0.2.1 the pre-commit gate enforces (D-07) — single quotes, 120 columns.</fails_when>
  </verify>

  <done>
`_refresh_labels()` exists once and is called from exactly two sites (the retirement decline
and the plain-update write); the retirement decline no longer ends in an unconditional
`continue`; all seven new tests pass; the six neighbouring test classes stay green; the
declined-retirement path records no provenance and makes no `sun_event()` call; and
`campaign_reconciler.py` / `load_telescope_runs.py` are untouched.
  </done>

  <reversibility rating="reversible">A localised branch change plus one helper in a single
  module, fully covered by tests — revertable with `git revert` and no data migration.</reversibility>
</task>

<task type="auto">
  <name>Task 2: paired docs — the runbook states the counter pair, the notebook demonstrates the refresh</name>
  <files>docs/runbooks/telescope_runs_calendar.rst, docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb</files>

  <precondition>The developer database `src/fomo_db.sqlite3` exists (notebook cell 2 raises
  `RuntimeError` without it), and `jupyter nbconvert` is on PATH.</precondition>

  <read_first>
    - `docs/runbooks/telescope_runs_calendar.rst` lines 1160-1245 — the whole
      `detach_declined` section (1178-1208) and the `remint_declined` section (1210-1242)
      whose counter-pair paragraph (1221-1228) is the model to mirror.
    - Notebook cells 38 and 39 (`execution_count` 20) — inspect them with a JSON read, not
      by guessing; cell 39's local variable names are reused by the new block.
  </read_first>

  <action>
CLAUDE.md's paired-docs rule makes both artifacts part of this deliverable, not follow-up
polish. The behaviour change is in `allocation_projector.py`, whose paired notebook is
`docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb` (the v2.2 campaign
lifecycle / reconciler demo), and the affected `docs/runbooks/` page is
`telescope_runs_calendar.rst`.

**Runbook.** Insert one new paragraph into the `detach_declined` section, positioned AFTER
the existing paragraph that begins "For a declined attribution release, there is nothing for
an operator to do" and BEFORE the paragraph that begins "``remint_declined`` counts a night
whose boundaries would have changed". Wrap to the surrounding prose width (about 70
columns). The paragraph must open with the exact sentence-opening
`A declined retirement ALSO counts under` and must state, in the runbook's own operator
voice: that a declined retirement also counts under ``updated`` or ``unchanged`` on the same
sweep — the same pair the ``remint_declined`` section below already states for its own
decline, and for the same reason, since the night survives and only the delete is declined;
that this is what keeps a later **Mark cancelled** / **Mark weather/technical failure**
action reaching a night whose retirement was declined, so the entry picks up its
``[CANCELLED]`` / ``[WEATHERED]`` prefix on the next sweep instead of sitting on the calendar
as an ordinary observing night for as long as the confirmation stands; that the night's
primary key, start time, end time and ``confirmed_by`` / ``confirmed_at`` stamp are never
touched by that refresh and no sun-event calculation is paid for it; and that the
declined-retirement warning therefore repeats on every sweep until the confirmation is
cleared, which is the standing report for a night the sweep is refusing to delete, not a
fault. Change nothing else in the file — the `remint_declined` section's own wording, the
post-upgrade deploy note and the `load_telescope_runs` section all stay as they are (WR-05
and WR-07 are out of scope).

**Notebook.** Extend the existing executed CR-05 retirement-guard demo rather than adding a
new standalone section, so the surviving-night property and its label-refresh consequence
read as one story.

Edit the notebook through a throwaway Python script written to your scratchpad directory
(NOT into the repository), using `nbformat.read(path, as_version=4)` /
`nbformat.write(nb, path)` so the file stays schema-valid. Hand-editing the escaped JSON is
not worth the risk. The script must first assert its anchors — cell index 38 is a markdown
cell whose source starts with `## A confirmed allocation night survives its own retirement`,
and cell index 39 is a code cell with `execution_count == 20` — and raise rather than write
if either fails.

To cell 38's markdown, append one short paragraph: 35-REVIEW.md iteration 10's CR-01 found
that the night this guard keeps alive was then never refreshed again, so a later
`Mark cancelled` never reached it; the block below now also proves the surviving night keeps
receiving its ordinary title/description/campaign-label refresh, with its primary key, both
boundaries and its confirmation stamp preserved, reported as `detach_declined` alongside
`updated`. Also append `, and still gets its labels refreshed (iteration 10, CR-01)` to that
cell's heading line so the notebook's own table of contents stays honest.

To cell 39's code, append a block that continues from the variables already bound in that
cell (`confirmed_retire_run`, `retire_pk_before`, `retire_staff_user`). It must: import
`allocation_night_title` from `solsys_code.allocation_projector`; capture the night's
current `title`, `start_time` and `end_time` and print the title; set
`confirmed_retire_run.run_status = CampaignRun.RunStatus.CANCELLED` and save with
`update_fields=['run_status']` (the state the calendar's **Mark cancelled** action writes);
run `reconcile_run(confirmed_retire_run)` and print the result; re-read the event by
`pk=retire_pk_before` and print the new title; then assert the title equals
`allocation_night_title(confirmed_retire_run)`, starts with `'[CANCELLED]'`, both boundaries
are unchanged, the companion row's `confirmed_by_id` still matches `retire_staff_user.pk`,
and the result reports `detach_declined == 1`, `updated == 1`, `retired == 0`. Every assert
carries a message naming the property. Finish with a `print()` whose text contains the
verbatim phrase `still receives the ordinary label refresh` — the verify gate below greps
THIS cell's own stored output for that phrase, so the wording is load-bearing, not
decoration — stating in prose that a declined retirement still receives the ordinary label
refresh, keeping its primary key, both boundaries and its confirmation stamp while its title
picks up the `[CANCELLED]` prefix. Then run one more `reconcile_run()`, assert
`detach_declined == 1` and `unchanged == 1`, and print that second result to show the sweep
has converged.

Then regenerate the notebook **from `docs/notebooks/pre_executed/`** (cell 2 resolves the
repo root as `Path.cwd().parents[2]` and raises if that is wrong):
`jupyter nbconvert --to notebook --execute --inplace reconcile_campaign_runs_demo.ipynb`.
Cell 2 already copies `src/fomo_db.sqlite3` to a fresh `tempfile.mkdtemp()` scratch file,
exports `FOMO_DATABASE_PATH` before `django.setup()`, asserts the resolved database is that
copy and migrates it to head, and cell 46 removes the scratch directory — so do not pre-copy
a database, do not export `FOMO_DATABASE_PATH` yourself, and never sweep the developer
database. If execution fails, fix the appended cell and re-run the whole notebook; do not
hand-write outputs. The executed outputs ARE the deliverable: `pre_executed/` is excluded
from the `jupyter-nb-clear-output` pre-commit hook, so the committed copy keeps its output.
  </action>

  <verify>
    <automated>cd /home/tlister/git/fomo_devel && test "$(grep -c 'A declined retirement ALSO counts under' docs/runbooks/telescope_runs_calendar.rst)" -ge 1 && python3 -c "
t = open('docs/runbooks/telescope_runs_calendar.rst').read()
i = t.index('A declined retirement ALSO counts under')
j = t.index('counts a night whose boundaries would have changed')
assert i < j, 'the new paragraph must sit inside the detach_declined section, before remint_declined'
seg = t[i:j]
for token in ('\`\`updated\`\`', '\`\`unchanged\`\`', '\`\`[CANCELLED]\`\`'):
    assert token in seg, f'detach_declined section does not state {token}'
print('OK: detach_declined section states its counter pair')
"</automated>
    <fails_when>The runbook's `detach_declined` section still leaves an operator to guess which counter a declined retirement's label refresh appears under, or the paragraph landed in the wrong section.</fails_when>

    <automated>cd /home/tlister/git/fomo_devel && python3 -c "
import json
nb = json.load(open('docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb'))
unexecuted = [i for i, c in enumerate(nb['cells']) if c['cell_type'] == 'code' and (c.get('execution_count') is None or not c.get('outputs'))]
assert not unexecuted, f'unexecuted or placeholder code cells: {unexecuted}'
demo = [c for c in nb['cells'] if c['cell_type'] == 'code' and 'confirmed_retire_run' in ''.join(c['source'])]
assert len(demo) == 1, f'expected exactly one CR-05 retirement-guard code cell, found {len(demo)}'
cell_out = ''.join(''.join(o.get('text', [])) for o in (demo[0].get('outputs') or []))
assert 'still receives the ordinary label refresh' in cell_out, 'the retirement-guard cell does not demonstrate the CR-01 label refresh'
assert '[CANCELLED]' in cell_out, 'the retirement-guard cell output shows no refreshed [CANCELLED] title'
assert 'detach_declined=1' in cell_out.replace(' ', ''), 'the retirement-guard cell output shows no detach_declined=1 beside the refresh'
assert 'updated=1' in cell_out.replace(' ', ''), 'the retirement-guard cell output shows no updated=1 beside the decline'
print(f'OK: {len(nb[\"cells\"])} cells, every code cell executed, CR-01 property demonstrated in the retirement-guard cell')
"</automated>
    <fails_when>The notebook was edited but not re-executed, or an output was hand-written, or the appended block does not actually show the refreshed `[CANCELLED]` title beside the `detach_declined` count.</fails_when>

    <automated>cd /home/tlister/git/fomo_devel && python3 -c "
import json, re
nb = json.load(open('docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb'))
setup = [c for c in nb['cells'] if c['cell_type'] == 'code' and 'FOMO_DATABASE_PATH' in ''.join(c['source'])]
assert len(setup) == 1, f'expected exactly one Django-setup cell, found {len(setup)}'
out = ''.join(''.join(o.get('text', [])) for o in (setup[0].get('outputs') or []))
m = re.search(r\"Resolved database: '([^']+)'\", out)
assert m, f'the setup cell recorded no resolved database in its output: {out!r}'
resolved = m.group(1)
assert 'fomo-notebook-db-' in resolved, f'this run was NOT routed to a mkdtemp scratch copy: {resolved!r}'
assert not resolved.endswith('src/fomo_db.sqlite3'), f'this run resolved to the developer database: {resolved!r}'
print(f'OK: routed to a fresh scratch copy — {resolved}')
"</automated>
    <fails_when>The stored output proves the run was not routed to a fresh `mkdtemp` scratch copy — i.e. the notebook swept the developer database, the `FOMO_DATABASE_PATH` breach 34-UAT.md already charged once. (`src/fomo_db.sqlite3` is gitignored, so a `git status` check on it can never catch this; the routing has to be read out of the executed output.)</fails_when>

    <automated>cd /home/tlister/git/fomo_devel && pre-commit run sphinx-build --files docs/runbooks/telescope_runs_calendar.rst 2>&1 | tail -20</automated>
    <fails_when>The new reStructuredText breaks the Sphinx build the pre-commit gate runs on every commit.</fails_when>
  </verify>

  <done>
The `detach_declined` section names `updated`/`unchanged` and the `[CANCELLED]`/`[WEATHERED]`
consequence; the notebook's CR-05 cell (`execution_count` 20) demonstrates the refreshed
title with real executed output plus the `detach_declined: 1` / `updated: 1` pair; every code
cell still carries a non-null `execution_count` and at least one output; Sphinx builds; and
`src/fomo_db.sqlite3` is unmodified.
  </done>
</task>

<task type="auto">
  <name>Task 3: full-suite verification and the CR-01 closure record</name>
  <files>solsys_code/tests/test_allocation_projector.py, solsys_code/allocation_projector.py</files>

  <action>
Run the wider suite for every module that reads `project_allocation()`'s counters or its
retirement branch, and confirm nothing else moved. Fix any regression in the files this plan
already owns; if a failure points at a module outside `files_modified`, stop and report it
rather than widening the change.

Never invoke `python manage.py test solsys_code` bare and never run
`solsys_code.tests.test_views.TestEphemeris` — it segfaults in native ASSIST. Use the
explicit module list in the verify commands below.

Finally, confirm by reading the diff that the three out-of-scope seams are genuinely
untouched: `allocation_projector.py`'s `_record_sub_night_provenance()` call on the
plain-update path (WR-01, deferred at UAT 2026-09-16), the step-4 staleness warning wording
(WR-04 advisory), and `load_telescope_runs.py`'s night summary (WR-07). Report their status
in the SUMMARY's review-disposition note so the next reader sees they were considered and
deliberately left, not missed.
  </action>

  <verify>
    <automated>cd /home/tlister/git/fomo_devel && python manage.py test solsys_code.tests.test_allocation_projector solsys_code.tests.test_allocation_projector_signals solsys_code.tests.test_campaign_reconciler solsys_code.tests.test_reconcile_campaign_runs -v 1 2>&1 | tail -20</automated>
    <fails_when>The fall-through changed a counter, an ownership decision or a convergence outcome that any allocation, signal-receiver, reconciler or reconcile-command test depends on.</fails_when>

    <automated>cd /home/tlister/git/fomo_devel && python manage.py test solsys_code.tests.test_load_telescope_runs solsys_code.tests.test_cutover_classical_allocations solsys_code.tests.test_campaign_run_observation solsys_code.tests.test_calendar_event_meta_links solsys_code.tests.test_write_and_reconcile -v 1 2>&1 | tail -15</automated>
    <fails_when>A downstream consumer of the retirement branch's counters (the ingest command's night summary, the cutover command, the link model's receivers, the companion-row links, or the write-and-reconcile round trip) changed behaviour.</fails_when>

    <automated>cd /home/tlister/git/fomo_devel && pre-commit run ruff --all-files && pre-commit run ruff-format --all-files</automated>
    <fails_when>Any file in the tree fails the pinned ruff v0.2.1 lint or format gate (D-07).</fails_when>

    <automated>cd /home/tlister/git/fomo_devel && python manage.py shell -c "
from solsys_code.campaign_reconciler import ReconcileResult
fields = ReconcileResult._fields
assert len(fields) == 12, f'ReconcileResult gained or lost a counter: {fields}'
assert fields[6:8] == ('detach_declined', 'remint_declined'), f'counter order moved: {fields}'
print(f'OK: no new counter — {len(fields)} fields, order intact')
"</automated>
    <fails_when>A new `ReconcileResult` counter was added instead of reusing `updated`/`unchanged`, or the `detach_declined`/`remint_declined` positions moved — either would ripple into `campaign_reconciler.py`, the two management commands' summary lines and the runbook, well beyond this quick task.</fails_when>

    <automated>cd /home/tlister/git/fomo_devel && RAW=$(git status --porcelain -- solsys_code/ docs/ src/) && TRACKED=$(printf '%s\n' "$RAW" | grep -v '^?? ' | grep -c . || true) && printf '%s\n' "$RAW" | grep -v '^?? ' || true; echo "uncommitted tracked changes: $TRACKED (0 is expected once the per-task commits have landed)" && test "$TRACKED" -le 4</automated>
    <fails_when>The change spread beyond `allocation_projector.py`, its test module, the runbook and the notebook — more than four tracked files under `solsys_code/`, `docs/` or `src/` are dirty.</fails_when>

    <automated>cd /home/tlister/git/fomo_devel && PROV=$(grep -v '^[[:space:]]*#' solsys_code/allocation_projector.py | grep -o '_record_sub_night_provenance(event' | wc -l | tr -d ' ') && echo "provenance definition + call sites (comments stripped): $PROV — must stay 4 (1 def + re-mint + create + plain-update)" && test "$PROV" -eq 4 && WR7=$(grep -c 'detach_declined' solsys_code/management/commands/load_telescope_runs.py || true) && echo "decline-counter mentions in the ingest command: $WR7" && test "$WR7" -eq 0 && echo 'OK: WR-01 seam and WR-07 both untouched'</automated>
    <fails_when>`_record_sub_night_provenance()` gained or lost a call site — meaning `_refresh_labels()` absorbed the WR-01 seam the owner deferred at UAT 2026-09-16, or the declined-retirement refresh started recording a provenance token it never proved — or `load_telescope_runs.py` started aggregating a decline counter, which is WR-07 and out of scope here. Comments are stripped before counting so the branch's own explanatory comment cannot satisfy or break this gate.</fails_when>
  </verify>

  <done>
All nine test modules green; both ruff gates clean tree-wide; the diff is confined to the
four files in `files_modified`; and the WR-01 / WR-04 / WR-07 seams are confirmed untouched
with their disposition recorded in the SUMMARY.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| staff admin / calendar action → `CampaignRun` write → `post_save` receiver → `project_allocation()` | An authenticated staff write (`Mark cancelled`, an attribution confirmation, an `ObservationRecord` link) is the only trigger that reaches this code; no unauthenticated input crosses into it. |
| automated sweep → a human-confirmed `CalendarEvent` / `CalendarEventMeta` row | The sweep writes rows a person has explicitly claimed. This is the boundary the whole decline mechanism exists to police, and the one this change writes across for the first time. |

## STRIDE Threat Register

ASVS level 1; blocking threshold `high`. No threat below is rated high or critical, so no
blocking checkpoint is required.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-Q260916O6N-01 | Tampering | `_refresh_labels()` writing onto a human-confirmed `CalendarEvent` | medium | mitigate | The helper's field mapping is closed to `title`/`description`/`target_list` plus the `url` it was fetched by; `_link_event_to_run()` writes only the companion row's `run`. Pinned by `test_declined_retirement_confirmation_stamp_survives_the_refresh` and by the pk/boundary equality assertions in tests 1, 2 and 5. |
| T-Q260916O6N-02 | Repudiation | a sweep action reported under no counter | medium | mitigate | The refresh is reported under `updated`/`unchanged` alongside `detach_declined`, asserted in test 4, and the pair is written into the runbook's `detach_declined` section so an operator can reconcile the printed summary against what happened. |
| T-Q260916O6N-03 | Denial of Service | astropy `sun_event()` cost on a decline that repeats every sweep | low | mitigate | The refresh passes `preserved_dark_window_line(existing)` and never calls `sun_event()`; pinned non-vacuously by test 6 (`assert_not_called()` plus `updated == 1`). |
| T-Q260916O6N-04 | Tampering | false provenance token recorded for boundaries this path never minted | low | mitigate | `_record_sub_night_provenance()` is deliberately excluded from `_refresh_labels()` and stays at its existing plain-update call site; the reason is recorded in the branch comment, naming WR-01 as the finding that raises the same class of claim on the neighbouring path. |
| T-Q260916O6N-05 | Elevation of Privilege | an automated sweep completing a delete a human declined | low | accept | Out of reach of this change: the delete decision is `_clearable_declined_and_unattributed()`'s and is not modified. Test 7 pins that the deletable path still deletes and the declined path still does not. |
| T-Q260916O6N-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs and no new dependency in this task — every import used (`nbformat`, `jupyter`, `pre-commit`, `django`) is already installed and already in `pyproject.toml`. The package-legitimacy gate does not fire. |
</threat_model>

<verification>
1. `TestDeclinedRetirementStillUpdatesLabels` (7 tests) passes, and the six neighbouring
   guard/convergence/no-recompute classes stay green.
2. `_refresh_labels()` is defined once and called from exactly two sites.
3. Nine test modules green; neither `campaign_reconciler.py` nor `load_telescope_runs.py`
   appears in the diff.
4. `pre-commit run ruff --all-files` and `pre-commit run ruff-format --all-files` clean.
5. `pre-commit run sphinx-build` clean; the `detach_declined` section names its counter pair.
6. Every notebook code cell carries a non-null `execution_count` and real output, the new
   output shows the `[CANCELLED]` title beside `detach_declined=1`, and
   `src/fomo_db.sqlite3` is unmodified.
</verification>

<success_criteria>
35-REVIEW.md iteration 10 CR-01 is closed on its binding property: a declined retirement
still receives the ordinary label refresh on every sweep AND on the receiver path, with pk,
both boundaries and the confirmation stamp preserved, reported under `updated`/`unchanged`
alongside `detach_declined` — with that pair written into the runbook and demonstrated with
real executed output in the paired notebook. WR-01, WR-04, WR-07 and the remaining review
residue are untouched and their disposition is recorded.
</success_criteria>

<output>
Create `.planning/quick/260916-o6n-fix-35-review-md-iteration-10-cr-01-give/260916-o6n-SUMMARY.md`
when done. Include a short review-disposition note stating that WR-01 (the declined-re-mint
provenance seam at the plain-update path), WR-04 (the staleness-warning wording advisory) and
WR-07 (`load_telescope_runs`'s night summary) were in the same code, were considered, and were
deliberately left — WR-01 per the owner's UAT 2026-09-16 deferral — so the next reader does not
read them as this task's oversight.
</output>
