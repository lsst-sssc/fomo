---
phase: quick-260619-ml8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .pre-commit-config.yaml
  - solsys_code/management/commands/sync_lco_observation_calendar.py
  - solsys_code/tests/test_sync_lco_observation_calendar.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "pre-commit jupyter-nb-clear-output hook no longer strips output from docs/notebooks/pre_executed/*.ipynb"
    - "CalendarEvent description Window (UTC) line renders timestamps without a redundant +00:00 offset"
    - "test_sync_lco_observation_calendar passes and ruff stays clean"
  artifacts:
    - path: ".pre-commit-config.yaml"
      provides: "corrected exclude path for pre_executed notebooks"
      contains: "^docs/notebooks/pre_executed"
    - path: "solsys_code/management/commands/sync_lco_observation_calendar.py"
      provides: "offset-free Window (UTC) description rendering"
  key_links: []
---

<objective>
Fix two independent, already-diagnosed bugs found while working on sync_lco_observation_calendar:

1. The pre-commit `jupyter-nb-clear-output` hook excludes `^docs/pre_executed`, but pre-executed notebooks actually live at `docs/notebooks/pre_executed/`. The mismatched regex never matches, so the hook strips just-regenerated output from pre-executed documentation notebooks (confirmed data loss this session).
2. The CalendarEvent `description` string's `Window (UTC):` line renders timezone-aware UTC datetimes with `.isoformat()`, producing a redundant `+00:00` suffix on a line already labelled UTC.

Purpose: Stop pre-commit from destroying pre-executed notebook output, and clean up a cosmetic redundancy in the calendar event description.
Output: Corrected `.pre-commit-config.yaml` exclude, offset-free description rendering, and any matching test assertion updates.
</objective>

<execution_context>
@/home/tlister/git/fomo_devel/.claude/gsd-core/workflows/execute-plan.md
@/home/tlister/git/fomo_devel/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.pre-commit-config.yaml
@solsys_code/management/commands/sync_lco_observation_calendar.py
@solsys_code/tests/test_sync_lco_observation_calendar.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix pre-commit exclude path for pre_executed notebooks</name>
  <files>.pre-commit-config.yaml</files>
  <action>In the `jupyter-nb-clear-output` hook entry (around line 20), change `exclude: ^docs/pre_executed` to `exclude: ^docs/notebooks/pre_executed` so the hook actually excludes the real pre-executed notebook location (`docs/notebooks/pre_executed/`). Do NOT change `files: \.ipynb$`, the `entry`, `stages`, or anything else in that hook entry. This is a single-line change.</action>
  <verify>
    <automated>grep -q 'exclude: \^docs/notebooks/pre_executed' .pre-commit-config.yaml && ! grep -q 'exclude: \^docs/pre_executed$' .pre-commit-config.yaml</automated>
  </verify>
  <done>The exclude regex reads `^docs/notebooks/pre_executed`; the old `^docs/pre_executed` value is gone.</done>
</task>

<task type="auto">
  <name>Task 2: Render Window (UTC) description without +00:00 offset</name>
  <files>solsys_code/management/commands/sync_lco_observation_calendar.py, solsys_code/tests/test_sync_lco_observation_calendar.py</files>
  <action>In `_build_event_fields` (description assignment around line 148-152), change the `Window (UTC):` line so the window timestamps render without the trailing `+00:00` offset. Replace `{start_time.isoformat()}` with `{start_time.strftime('%Y-%m-%dT%H:%M:%S')}` and `{end_time.isoformat()}` with `{end_time.strftime('%Y-%m-%dT%H:%M:%S')}`. Do NOT change `start_time`/`end_time` themselves, the returned dict's `start_time`/`end_time` keys, or any other call site — only this one f-string's rendering. Then in `solsys_code/tests/test_sync_lco_observation_calendar.py`, review `test_sync_05_d05_description_contains_proposal_status_and_window` (around line 294-315): its current `assertIn('2026-09-01', desc)` / `assertIn('2026-09-02', desc)` substring checks still pass with the new format, but grep the whole test file for any assertion on `'+00:00'` or the literal isoformat-with-offset string and update it to the no-offset format if found. If no such assertion exists, leave the tests as-is.</action>
  <verify>
    <automated>grep -q "strftime('%Y-%m-%dT%H:%M:%S')" solsys_code/management/commands/sync_lco_observation_calendar.py && ! grep -n 'Window (UTC).*isoformat' solsys_code/management/commands/sync_lco_observation_calendar.py</automated>
  </verify>
  <done>The `Window (UTC):` description line uses strftime without an offset suffix; any test assertion that depended on the `+00:00` form is updated; date-substring assertions still pass.</done>
</task>

<task type="auto">
  <name>Task 3: Verify tests and lint pass</name>
  <files></files>
  <action>Run the sync command test suite and the ruff quality gates to confirm both fixes are clean. Do NOT touch the demo notebook (`docs/notebooks/pre_executed/sync_lco_observation_calendar_demo.ipynb`) — its output is regenerated separately by the orchestrator after these source fixes land.</action>
  <verify>
    <automated>./manage.py test solsys_code.tests.test_sync_lco_observation_calendar 2>&1 | tail -5 && ruff check . && ruff format --check .</automated>
  </verify>
  <done>`./manage.py test solsys_code.tests.test_sync_lco_observation_calendar` passes; `ruff check .` and `ruff format --check .` report no issues.</done>
</task>

</tasks>

<verification>
- `.pre-commit-config.yaml` excludes `^docs/notebooks/pre_executed` (not `^docs/pre_executed`).
- `_build_event_fields` description `Window (UTC):` line renders timestamps with no `+00:00` suffix.
- `test_sync_lco_observation_calendar` passes; ruff check and format checks are clean.
</verification>

<success_criteria>
Both bugs fixed: pre-commit no longer strips pre_executed notebook output, and the calendar event description's UTC window line is offset-free. Test suite and ruff gates pass. The demo notebook is untouched.
</success_criteria>

<output>
Create `.planning/quick/260619-ml8-fix-pre-commit-jupyter-nb-clear-output-e/260619-ml8-SUMMARY.md` when done
</output>
