---
phase: quick-260623-su3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - solsys_code/management/commands/sync_lco_observation_calendar.py
  - solsys_code/tests/test_sync_lco_observation_calendar.py
autonomous: true
requirements:
  - TELESCOPE-01
user_setup: []

must_haves:
  truths:
    - "A placed record resolving to ('coj','1m0') gets the verified label COJ-1m0, not [UNVERIFIED] 1m0"
    - "A placed record resolving to ('coj','0m4') gets the verified label COJ-0m4"
    - "A placed record resolving to ('ogg','0m4') gets the verified label OGG-0m4"
    - "tlv remains absent from SITE_TELESCOPE_MAP (no regression of the 07-01 operator decision)"
  artifacts:
    - path: "solsys_code/management/commands/sync_lco_observation_calendar.py"
      provides: "SITE_TELESCOPE_MAP with complete coj/ogg aperture-class coverage"
      contains: "('coj', '1m0'): 'COJ-1m0'"
    - path: "solsys_code/tests/test_sync_lco_observation_calendar.py"
      provides: "Regression assertions for the 3 newly-added (site, class) pairs"
      contains: "_derive_telescope('coj', '1m0a')"
  key_links:
    - from: "solsys_code/management/commands/sync_lco_observation_calendar.py"
      to: "solsys_code/tests/test_sync_lco_observation_calendar.py"
      via: "_derive_telescope reads SITE_TELESCOPE_MAP; tests assert resolved labels"
      pattern: "_derive_telescope\\('coj', '1m0a'\\)"
---

<objective>
Close the SITE_TELESCOPE_MAP completeness gap found in Phase 7 UAT Test 1 (07-UAT.md
Gaps section): a real placed record (observation_id=4213127) resolved successfully via the
live LCO API to `site='coj', telescope='1m0a'` (aperture class `1m0`), but
`_derive_telescope('coj', '1m0')` returned `None` because the dict has no `('coj','1m0')`
entry — so the record fell back to `[UNVERIFIED] 1m0` instead of the correct verified
`COJ-1m0` label.

Add the 3 missing entries — `('coj','1m0') -> 'COJ-1m0'`, `('coj','0m4') -> 'COJ-0m4'`,
`('ogg','0m4') -> 'OGG-0m4'` — confirmed against the authoritative public source
https://lco.global/observatory/sites/mpccodes/ (combining the SITEID column with the first
3 characters of TELID, deduped across all rows).

Purpose: every real (site, aperture_class) pair an LCO/SOAR placed record can resolve to
must map through SITE_TELESCOPE_MAP to its correct verified `SITECODE-CLASS` label, so
correctly-resolved live records stop falling to the `[UNVERIFIED]` fallback bucket.

Output: a widened SITE_TELESCOPE_MAP (10 -> 13 entries, still 7 sites) plus regression test
assertions for the 3 new pairs.

Scope guardrails:
- This is a pure dict-completeness fix. It does NOT change any decision logic, fallback
  behavior, title-prefix rules, counter semantics, or the `_derive_telescope` /
  `_aperture_class_from_telescope_code` parsing — it only widens correct-label coverage for
  sites whose extra aperture classes weren't previously enumerated.
- `tlv` (Wise Observatory) stays dropped. It is intentionally absent (confirmed missing from
  this installation's `LCOSettings.get_sites()` / `SOARSettings.get_sites()` per the Phase 7
  Task 1 operator-decision in 07-01-SUMMARY.md). The public MPC-codes table lists a
  historical/possible MPC code, not confirmation that this FOMO installation's API key can
  reach a `tlv` telescope live, so it does NOT override that decision. Do NOT re-add `tlv`.
- No demo-notebook regeneration required. Per CLAUDE.md's demo-notebook-companion convention,
  `sync_lco_observation_calendar_demo.ipynb` only needs an update when the command's
  documented behavior paths change (success / fallback / counter). This fix changes none of
  those paths — it only adds correct-label coverage for sites the notebook's fixtures don't
  exercise (the notebook uses coj/ogg 2m0 and lsc 1m0, all already mapped). So the notebook
  is intentionally NOT in this plan's files_modified. The executor should not second-guess
  this.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/07-live-telescope-label-resolution-with-fallback-failure-report/07-UAT.md
@solsys_code/management/commands/sync_lco_observation_calendar.py
@solsys_code/tests/test_sync_lco_observation_calendar.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the 3 missing (site, aperture_class) entries to SITE_TELESCOPE_MAP</name>
  <files>solsys_code/management/commands/sync_lco_observation_calendar.py</files>
  <behavior>
    After adding the entries, these resolutions must succeed (exercised by Task 2's tests):
    - `_derive_telescope('coj', '1m0a')` returns `'COJ-1m0'` (was `None`)
    - `_derive_telescope('coj', '0m4a')` returns `'COJ-0m4'` (was `None`)
    - `_derive_telescope('ogg', '0m4b')` returns `'OGG-0m4'` (was `None`)
    Unchanged: `_derive_telescope('coj', '2m0a')` still returns `'COJ-2m0'`;
    `_derive_telescope('tlv', '1m0a')` still returns `None` (tlv stays unmapped).
  </behavior>
  <action>
    In SITE_TELESCOPE_MAP (currently the 10-entry dict at module level), add exactly three
    new key/value pairs: ('coj', '1m0') -> 'COJ-1m0', ('coj', '0m4') -> 'COJ-0m4', and
    ('ogg', '0m4') -> 'OGG-0m4'. Place the two new coj entries next to the existing
    ('coj', '2m0') entry and the new ogg entry next to the existing ('ogg', '2m0') entry so
    each site's classes group together visually.

    Add a code comment immediately above (or inline with) these three new entries citing
    https://lco.global/observatory/sites/mpccodes/ as the confirmation source, and noting
    that these three are CONFIRMED against that authoritative public source (combining the
    SITEID column with the first 3 chars of TELID, deduped) — NOT [ASSUMED]. This is
    stronger evidence than the original Plan 07-01 entries, which cited operator-confirmation
    only. State in the comment that coj (Siding Spring) hosts 0m4/1m0/2m0 and ogg (Haleakala)
    hosts 2m0/0m4, which is why these classes were missing.

    Do NOT add a `tlv` entry — it stays intentionally absent (07-01 operator decision;
    confirmed absent from installed LCOSettings/SOARSettings; the public table does not
    override that). Do NOT alter the existing module docstring comment's claim that the dict
    covers the 7 real sites — the site COUNT is unchanged (still 7 sites: ogg/elp/lsc/cpt/
    coj/tfn/sor); only coj and ogg gain extra aperture classes. You may extend that comment
    block to mention coj/ogg now carry their full multi-class inventory per the public table,
    but keep the "7 real sites" / tlv-excluded framing intact.

    No existing test asserts an exact dict LENGTH (verified: the only dict-shape test,
    test_telescope_01_verified_dict_covers_all_sites, asserts the SET of sites
    {ogg,elp,lsc,cpt,coj,tfn,sor} and a label regex, both of which still hold after adding
    three entries that introduce no new site and match the SITECODE-CLASS pattern). So no
    count constant needs updating in this task. Do not invent or add a length assertion.
  </action>
  <verify>
    <automated>cd /home/tlister/git/fomo && python -c "from solsys_code.management.commands.sync_lco_observation_calendar import SITE_TELESCOPE_MAP as m; assert m[('coj','1m0')]=='COJ-1m0'; assert m[('coj','0m4')]=='COJ-0m4'; assert m[('ogg','0m4')]=='OGG-0m4'; assert m[('coj','2m0')]=='COJ-2m0'; assert ('tlv','1m0') not in m; assert {s for s,_ in m}=={'ogg','elp','lsc','cpt','coj','tfn','sor'}; print('ok')"</automated>
  </verify>
  <done>
    SITE_TELESCOPE_MAP contains all three new entries, ('coj','2m0') is unchanged, no tlv
    key exists, the site set is still exactly the 7 real sites, and a comment cites
    https://lco.global/observatory/sites/mpccodes/ as the confirmation source for the new
    entries.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add regression assertions for the 3 newly-mapped (site, class) pairs</name>
  <files>solsys_code/tests/test_sync_lco_observation_calendar.py</files>
  <behavior>
    The new assertions must FAIL against the pre-fix dict (proving they guard the gap) and
    PASS once Task 1's entries are present:
    - `_derive_telescope('coj', '1m0a') == 'COJ-1m0'`
    - `_derive_telescope('coj', '0m4a') == 'COJ-0m4'`
    - `_derive_telescope('ogg', '0m4b') == 'OGG-0m4'`
  </behavior>
  <action>
    Add a new dedicated test method to the TestSyncLcoObservationCalendar class (a focused
    regression test reads more clearly than overloading the existing dict-coverage test).
    Name it descriptively, e.g.
    `test_telescope_01_coj_ogg_full_aperture_class_coverage`, with a docstring referencing
    the Phase 7 UAT Test 1 gap (a real placed record resolving to ('coj','1m0') must yield
    the verified COJ-1m0 label, not the [UNVERIFIED] fallback).

    In the body, assert exactly the three resolutions:
      self.assertEqual(_derive_telescope('coj', '1m0a'), 'COJ-1m0')
      self.assertEqual(_derive_telescope('coj', '0m4a'), 'COJ-0m4')
      self.assertEqual(_derive_telescope('ogg', '0m4b'), 'OGG-0m4')
    These use the real telescope-code form (3-char class + 1-char dome-instance suffix) so
    they exercise the full _aperture_class_from_telescope_code -> SITE_TELESCOPE_MAP path,
    matching how a live API block's 'telescope' value arrives.

    `_derive_telescope` is already imported at the top of this test module (line 22), so no
    new import is needed. Do NOT modify the existing
    test_telescope_01_verified_dict_covers_all_sites or
    test_telescope_01_aperture_class_from_telescope_code methods — their assertions remain
    valid as-is.
  </action>
  <verify>
    <automated>cd /home/tlister/git/fomo && ./manage.py test solsys_code.tests.test_sync_lco_observation_calendar 2>&1 | tail -20</automated>
  </verify>
  <done>
    The new regression test method exists and passes; the full
    test_sync_lco_observation_calendar suite is green.
  </done>
</task>

</tasks>

<verification>
Run the full command test suite and both quality gates on the two changed files:

```bash
cd /home/tlister/git/fomo
./manage.py test solsys_code.tests.test_sync_lco_observation_calendar
ruff check solsys_code/management/commands/sync_lco_observation_calendar.py solsys_code/tests/test_sync_lco_observation_calendar.py
ruff format --check solsys_code/management/commands/sync_lco_observation_calendar.py solsys_code/tests/test_sync_lco_observation_calendar.py
```

All three must pass:
- Full `test_sync_lco_observation_calendar` suite green (existing + new regression test).
- `ruff check` clean on both changed files.
- `ruff format --check` clean on both changed files.
</verification>

<success_criteria>
- SITE_TELESCOPE_MAP gains exactly 3 entries: ('coj','1m0')->'COJ-1m0', ('coj','0m4')->
  'COJ-0m4', ('ogg','0m4')->'OGG-0m4'; a comment cites
  https://lco.global/observatory/sites/mpccodes/ as the confirmation source.
- `tlv` remains absent; the dict still covers exactly the 7 real sites.
- A new regression test asserts the three resolved labels and passes.
- The full test_sync_lco_observation_calendar suite is green; ruff check and ruff
  format --check are clean on both changed files.
- No notebook regenerated (intentionally out of scope — documented behavior paths unchanged).
</success_criteria>

<output>
Create `.planning/quick/260623-su3-fix-site-telescope-map-completeness-gap-/260623-su3-SUMMARY.md` when done
</output>
