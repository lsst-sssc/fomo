---
phase: quick-260619-f7u
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/notebooks/pre_executed/sync_lco_observation_calendar_demo.ipynb
  - .planning/PROJECT.md
autonomous: true
requirements: [SELECT-02, SELECT-03, SELECT-04, SELECT-05]

must_haves:
  truths:
    - "The existing sync_lco_observation_calendar_demo.ipynb additionally demonstrates a comma-list --proposal A,B selecting only matching-proposal records, with a decoy non-matching proposal that produces no event [SELECT-02]"
    - "The notebook demonstrates --proposal all (lowercase) syncing every fixture record regardless of proposal [SELECT-03]"
    - "The notebook demonstrates one invocation producing CalendarEvents for both an LCO and a SOAR fixture record together [SELECT-04]"
    - "The notebook surfaces the per-facility summary line showing separate LCO and SOAR counts [D-08]"
    - "A markdown cell notes SELECT-05 (no shared facility instance) is verified by test_select_05_soar_record_uses_soar_facility_instance, not demonstrated in the notebook [SELECT-05]"
    - "The notebook's Summary table lists SELECT-02/03/04/05 in the same form the existing table lists SELECT-01/SYNC-01..04/TERM-01"
    - "The notebook's teardown cell removes every newly-created CalendarEvent and ObservationRecord fixture"
    - "PROJECT.md documents that Stage (issue #37 feature-stage grouping) and Phase (GSD execution-phase count) are intentionally different numbering granularities, with the Stage-to-Phase mapping spelled out"
  artifacts:
    - path: "docs/notebooks/pre_executed/sync_lco_observation_calendar_demo.ipynb"
      provides: "Phase 5 multi-proposal/multi-facility demo cells (SELECT-02/03/04/05) appended to the existing Stage 3 notebook, plus updated Summary table and teardown"
      contains: "SELECT-02"
    - path: ".planning/PROJECT.md"
      provides: "Stage-vs-Phase numbering clarification note in/before the Demo Notebooks section"
      contains: "Stage"
  key_links:
    - from: "docs/notebooks/pre_executed/sync_lco_observation_calendar_demo.ipynb"
      to: "solsys_code/management/commands/sync_lco_observation_calendar.py"
      via: "call_command('sync_lco_observation_calendar', '--proposal', ...)"
      pattern: "sync_lco_observation_calendar"
---

<objective>
Close the Phase 5 documentation gap by (1) extending the existing Stage 3 demo notebook
`docs/notebooks/pre_executed/sync_lco_observation_calendar_demo.ipynb` to also demonstrate
Phase 5's multi-proposal (comma-list and `ALL`, case-insensitive) and multi-facility
(LCO + SOAR) sync behavior — satisfying SELECT-02/03/04/05 — and (2) documenting the
Stage-vs-Phase numbering mapping in `.planning/PROJECT.md`.

Purpose: The notebook currently only demonstrates Phase 4's single-proposal / LCO-only
behavior (SELECT-01, SYNC-01..04, TERM-01); Phase 5 shipped multi-proposal/multi-facility
support with passing tests but no notebook coverage. The PROJECT.md note resolves the
recurring confusion between issue #37 "Stage N" labels (used in notebook headers) and GSD
"Phase N" execution counts.

Output: Two documentation/notebook edits. No application code, settings, or test changes.
</objective>

<execution_context>
@/home/tlister/git/fomo_devel/.claude/gsd-core/workflows/execute-plan.md
@/home/tlister/git/fomo_devel/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@docs/notebooks/pre_executed/sync_lco_observation_calendar_demo.ipynb
@solsys_code/management/commands/sync_lco_observation_calendar.py
@solsys_code/tests/test_sync_lco_observation_calendar.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the demo notebook with SELECT-02/03/04/05 cells, updated Summary table, and teardown</name>
  <files>docs/notebooks/pre_executed/sync_lco_observation_calendar_demo.ipynb</files>
  <read_first>
    - docs/notebooks/pre_executed/sync_lco_observation_calendar_demo.ipynb (full file — mirror the existing cell conventions: Django-setup boilerplate already present in cell c3d4e5a3; the io.StringIO()/call_command pattern in cell a7b8c9e7; the fixture-creation pattern in cell e5f6a7c5 using NonSiderealTargetFactory + get_or_create; the CalendarEvent-by-url inspection in cell c9d0e1f9; the Summary table in cell b4c5d6e4; the teardown cell d6e7f8a6)
    - solsys_code/management/commands/sync_lco_observation_calendar.py (handle() queryset facility__in=['LCO','SOAR']; _parse_proposal_arg; _build_event_fields requires parameters keys 'proposal','start','end','instrument_type','site'; per-facility summary line "LCO: created: N, updated: N, ... | SOAR: created: N, ...")
    - solsys_code/tests/test_sync_lco_observation_calendar.py (lines 370-430: test_select_02 uses proposals 'A','B','C' + decoy 'AB'; test_select_03 uses 'PROPA','PROPB','PROPC' with one facility='SOAR'; test_select_04 uses proposal 'SHARED' with one facility='LCO' + one facility='SOAR'; _create_record default site='coj' which maps to telescope 'FTS' — SOAR records must still carry a valid 'site' key so _build_event_fields succeeds)
    - .planning/PROJECT.md (Demo Notebooks section: notebooks are DB-dependent, pre-commit clears output cells, manual execution required; Django setup boilerplate)
    - CLAUDE.md (NonSiderealTargetFactory only — never SiderealTargetFactory; FOMO is solar-system-only)
  </read_first>
  <action>
This is a JUPYTEXT-style `.ipynb` read via the Read tool — it is real JSON on disk. Edit it as
JSON (preserve the `nbformat`/`nbformat_minor`/`metadata`/`cells` structure and each cell's
`cell_type`/`source`/`metadata`/`outputs`/`execution_count` keys). New code cells use
`cell_type` "code", `outputs` empty list, `execution_count` null; new markdown cells use
`cell_type` "markdown". Give each new cell a fresh unique `id`. Do NOT create a new notebook
file (user-confirmed: extend this one). Do NOT add a separate Django-setup cell — the existing
boilerplate (cell c3d4e5a3) already ran earlier in the notebook and its imports plus
`django.setup()` are in scope for the appended cells.

Insert the new content AFTER the existing "No-churn idempotency" cell (a3b4c5d3) and BEFORE the
existing "## Summary" markdown cell (b4c5d6e4). The existing Summary table and the existing
teardown cell (d6e7f8a6) are then UPDATED in place (see steps e and f). Structure:

(a) Markdown section header cell: "## Phase 5: multi-proposal and multi-facility sync (LCO + SOAR)".
Briefly explain that Phase 5 generalized the command so `--proposal` accepts a comma-separated
list or the case-insensitive `ALL` token, and a single run now covers both `facility='LCO'` and
`facility='SOAR'` ObservationRecords. Note that the cells below create their own fixture records
(distinct observation_ids from the Phase-4 demo above) so they do not collide with the
`demo-900001` record, and that all of them are removed by the teardown cell at the end.

(b) SELECT-02 — comma-list, no substring leakage. One markdown cell + one code cell.
Create four LCO fixture ObservationRecords via ObservationRecord.objects.get_or_create with
distinct observation_ids (e.g. 'demo-602001'..'demo-602004') and proposals 'PHASE5-A',
'PHASE5-B', 'PHASE5-C', and a decoy 'PHASE5-AB'. Each `parameters` dict mirrors the Phase-4 demo
fixture shape exactly: keys 'proposal', 'start', 'end', 'instrument_type', 'site' (use
site='coj' so _build_event_fields resolves telescope 'FTS'; reuse the same start/end window
strings as the Phase-4 fixture). Reuse the existing `demo_target` and `demo_user` from cell
e5f6a7c5 (already in scope) — do NOT create new Targets/Users. Run
call_command('sync_lco_observation_calendar', '--proposal', 'PHASE5-A,PHASE5-B', stdout=io.StringIO(), stderr=io.StringIO()).
Then assert/print that CalendarEvents exist for the A and B records (look them up by
`LCOFacility().get_observation_url('demo-602001')` etc.) and that NO event exists for the
decoy 'PHASE5-AB' record and the unselected 'PHASE5-C' record — demonstrating substring-safe
`__in` matching. Print a short PASS/FAIL line for each, following the print-style of the
existing inspection cells (no bare asserts that would abort the notebook silently — prefer
printing the boolean result so the reader sees it).

(c) SELECT-03 — `--proposal all` (lowercase) syncs everything. One markdown cell + one code cell.
Create one additional fixture with a fresh proposal that was NOT selected in step (b) (e.g.
observation_id 'demo-603001', proposal 'PHASE5-D') AND one `facility='SOAR'` fixture (e.g.
'demo-603002', proposal 'PHASE5-SOAR', facility='SOAR', site='coj'). Run
call_command(... '--proposal', 'all', ...) (lowercase, to demonstrate case-insensitivity).
Then print that every fixture record created so far in the Phase-5 section — including the
previously-unselected 'PHASE5-C', the decoy 'PHASE5-AB', the new 'PHASE5-D', and the SOAR
record — now has a CalendarEvent (look each up by its get_observation_url). This shows the
`ALL` sentinel drops the proposal filter entirely.

(d) SELECT-04 + D-08 — single run covers both LCO and SOAR, per-facility summary line.
One markdown cell + one code cell. Create one fresh LCO fixture and one fresh SOAR fixture
sharing the same proposal (e.g. 'demo-604001' facility='LCO' and 'demo-604002' facility='SOAR',
both proposal 'PHASE5-BOTH', site='coj'). First print
ObservationRecord.objects.get(observation_id='demo-604002').facility to confirm it persisted as
'SOAR' (guard against a silently-wrong fixture). Run a single
call_command(... '--proposal', 'PHASE5-BOTH', stdout=stdout_buf, stderr=io.StringIO()),
capturing stdout. Print that CalendarEvents exist for BOTH records (by get_observation_url),
and print `stdout_buf.getvalue()` — call out in the surrounding markdown that the summary line
shows separate `LCO:` and `SOAR:` created/updated/unchanged/skipped counts (D-08, the visible
side effect of multi-facility dispatch).

(e) SELECT-05 note — markdown only, NO code. One markdown cell. Explain that SELECT-05 (each
record is dispatched through its own facility instance — a SOAR record never goes through a
reused LCOFacility instance) cannot be meaningfully demonstrated in a notebook because LCO and
SOAR `get_observation_url()` return byte-identical strings, so a black-box event/url check
cannot discriminate which class handled the record. State that SELECT-05 is instead verified by
the discriminating spy test `test_select_05_soar_record_uses_soar_facility_instance` in
`solsys_code/tests/test_sync_lco_observation_calendar.py` (one-line pointer). Do NOT attempt
unittest.mock patching inside the notebook.

(f) Update the existing "## Summary" markdown cell (b4c5d6e4): change the intro sentence to say
the notebook now also covers Phase 05's SELECT-02/03/04/05, and ADD four rows to the existing
requirements table, in the same column format (Requirement | Description | Demonstrated by):
  - SELECT-02 | comma-list --proposal matches exactly the listed codes with no substring match on a decoy | the SELECT-02 cells (decoy 'PHASE5-AB' gets no event)
  - SELECT-03 | --proposal all (any casing) syncs every record regardless of proposal | the SELECT-03 cells (lowercase all syncs everything)
  - SELECT-04 | a single run produces CalendarEvents for both an LCO and a SOAR record | the SELECT-04 cells (one invocation, both facilities)
  - SELECT-05 | SOAR records dispatched through a SOARFacility instance, never a reused LCOFacility | not demonstrated here (identical url strings); verified by test_select_05_soar_record_uses_soar_facility_instance
Keep the existing six rows (SELECT-01/SYNC-01..04/TERM-01) intact.

(g) Update the existing teardown cell (d6e7f8a6): in addition to the existing cleanup of the
Phase-4 `demo-900001` event/record, delete every Phase-5 fixture's CalendarEvent and
ObservationRecord. Build the list of Phase-5 observation_ids used in steps (b)-(d) (e.g.
'demo-602001'..'demo-602004', 'demo-603001', 'demo-603002', 'demo-604001', 'demo-604002') and,
for each, delete CalendarEvent.objects.filter(url=LCOFacility().get_observation_url(oid)) and
ObservationRecord.objects.filter(observation_id=oid). Keep the existing Target/User deletion
(the shared demo_target/demo_user are reused, so they are still cleaned up once at the end — do
not delete them earlier). Update the teardown markdown cell (c5d6e7f5) text to mention the
Phase-5 fixtures are also removed. Preserve the existing deletion-order note (record before user).
  </action>
  <verify>
    <automated>cd /home/tlister/git/fomo_devel && python3 .planning/quick/260619-f7u-phase-5-notebook-gap-update-sync-lco-obs/verify_nb.py</automated>
  </verify>
  <acceptance_criteria>
    - The notebook parses as valid JSON and every cell keeps a valid cell_type ('code' or 'markdown').
    - New code cells have empty outputs and execution_count null (consistent with the pre-commit output-clearing convention).
    - Cells demonstrating SELECT-02 (comma-list + decoy 'PHASE5-AB' producing no event), SELECT-03 (lowercase all syncing everything incl. a SOAR record), and SELECT-04 (single run, one LCO + one SOAR record) are present, using call_command with io.StringIO buffers per the existing pattern.
    - A markdown-only SELECT-05 cell points to test_select_05_soar_record_uses_soar_facility_instance and does NOT use unittest.mock.
    - At least one fixture uses facility='SOAR'; no new Target fixtures are created (demo_target reused) — and any Target fixture that were created would use NonSiderealTargetFactory, never SiderealTargetFactory.
    - The Summary table has four new rows (SELECT-02/03/04/05) in the existing column format, with the original six rows intact.
    - The teardown cell removes every Phase-5 fixture's CalendarEvent and ObservationRecord plus the existing Phase-4 cleanup.
  </acceptance_criteria>
  <done>The existing notebook is extended with valid-JSON Phase-5 SELECT-02/03/04/05 demo cells, an updated Summary table, and a teardown that cleans up all new fixtures; output cells are clear.</done>
</task>

<task type="auto">
  <name>Task 2: Document the Stage-vs-Phase numbering mapping in PROJECT.md</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md (the "## Demo Notebooks" section around line 127, and "## Current State" lines 13-19 / "What This Is" lines 3-11 for the existing Stage-to-Phase facts to restate accurately)
    - CLAUDE.md (Planning-doc terminology: plain English, no DB jargon such as "upsert")
  </read_first>
  <action>
Add a short clarifying note to `.planning/PROJECT.md` documenting the Stage-vs-Phase numbering
relationship. This is DOCUMENT-ONLY (user-confirmed): do not renumber anything, do not change any
existing "Stage N" label or notebook header. Place the note either as the final paragraph of the
existing "## Demo Notebooks" section or as a new short "### Stage vs Phase numbering" subsection
immediately preceding "## Demo Notebooks" (your discretion — pick whichever reads cleaner in
context). Use plain English (no "upsert"-style jargon per CLAUDE.md).

The note must state, accurately matching the existing PROJECT.md facts:
- "Stage" is the issue #37 feature-stage grouping: Stage 1 = site/ephemeris helper;
  Stage 2 = classical run ingest; Stage 3 = LCO queue sync, now being generalized to LCO + SOAR
  across v1.3's Phases 5-7; Stage 4 = future full observation-record sync for all facilities.
- "Phase" is the GSD execution-phase count.
- The two schemes are intentionally different granularities — e.g. Stage 2 spans GSD Phases 2-3,
  and Stage 3 corresponds to Phase 4 and is being extended by Phases 5-7.
- Existing notebook headers that say "Stage N" predate this clarification and are intentionally
  left as-is.

Update the "*Last updated: ...*" footer line at the bottom of PROJECT.md to note this
documentation addition (keep the existing date format).
  </action>
  <verify>
    <automated>cd /home/tlister/git/fomo_devel && python3 .planning/quick/260619-f7u-phase-5-notebook-gap-update-sync-lco-obs/verify_project.py</automated>
  </verify>
  <acceptance_criteria>
    - PROJECT.md contains a Stage-vs-Phase note (in/before the Demo Notebooks section) stating the two numbering schemes are intentionally different granularities.
    - The note maps Stage 2 to GSD Phases 2-3 and Stage 3 to Phase 4 (extended by Phases 5-7), and lists what Stages 1-4 are.
    - The note records that existing "Stage N" notebook headers predate the clarification and are left as-is.
    - No "upsert"-style DB jargon introduced (plain English per CLAUDE.md).
    - No existing Stage label, notebook header, or other content renumbered.
    - The "*Last updated*" footer line is updated to mention this documentation addition.
  </acceptance_criteria>
  <done>PROJECT.md documents the Stage-vs-Phase numbering mapping as a doc-only note, with nothing renumbered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none meaningful) | Both deliverables are documentation/notebook artifacts only. The notebook invokes the already-shipped, already-threat-modeled Phase-5 command via `call_command`; no new code path, input sink, or credential handling is introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-q-01 | Information Disclosure | Demo notebook cells printing command output | accept | The notebook prints only the command's summary line and CalendarEvent fields (title/times/url/telescope/instrument/proposal) — the same fields the Phase-4 demo already prints. The Phase-5 command's own grep gate guarantees no api_key/credentials reach stdout/stderr, so the notebook cannot surface any. No PII, no secrets. |
| T-q-SC | Tampering | npm/pip/cargo installs | accept (N/A) | No package installs — both tasks edit existing tracked files only. No legitimacy checkpoint required. |

No HIGH-severity threats. No blocking human checkpoint needed.
</threat_model>

<verification>
- Notebook parses as valid JSON with SELECT-02/03/04/05 + SOAR fixtures + cleared output cells (Task 1 automated check, `verify_nb.py`).
- PROJECT.md contains the Stage-vs-Phase note with the Stage 2 to Phases 2-3 and Stage 3 to Phases 5-7 mappings, no upsert jargon (Task 2 automated check, `verify_project.py`).
- The two `verify_*.py` helper scripts are throwaway gate scripts living in the quick-plan directory; delete them in the SUMMARY step or leave them in the (gitignored-equivalent) planning dir — they are not part of the shipped diff.
- Optional manual verification (no automated test suite for notebook content, per project convention): open the notebook in Jupyter started from `docs/notebooks/pre_executed/`, Run All, and confirm the Phase-5 cells print the expected PASS lines and per-facility summary, then the teardown cell reports a clean removal. There is intentionally no pytest for notebook content (pre-commit clears output cells).
- Lint is not applicable: `.ipynb` and `.md` are not ruff targets; the repo's pre-commit clears notebook outputs on commit (consistent project convention).
</verification>

<success_criteria>
- The existing `sync_lco_observation_calendar_demo.ipynb` additionally demonstrates SELECT-02 (comma-list with a no-event decoy), SELECT-03 (lowercase all), and SELECT-04 (single run covering one LCO + one SOAR record), surfacing the per-facility summary line (D-08).
- A markdown cell points to `test_select_05_soar_record_uses_soar_facility_instance` for SELECT-05 rather than attempting mock-based dispatch demonstration.
- The Summary table lists SELECT-02/03/04/05 alongside the existing SELECT-01/SYNC-01..04/TERM-01 rows; the teardown removes all new fixtures; output cells are clear.
- PROJECT.md documents the intentional Stage-vs-Phase numbering difference (doc-only, nothing renumbered).
- No application code, settings, or test files are modified.
</success_criteria>

<output>
Create `.planning/quick/260619-f7u-phase-5-notebook-gap-update-sync-lco-obs/260619-f7u-SUMMARY.md` when done.
</output>
