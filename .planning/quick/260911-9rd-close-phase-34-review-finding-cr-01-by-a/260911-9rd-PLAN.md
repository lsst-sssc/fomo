---
phase: quick-260911-9rd
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/34-the-observation-projector-trigger/34-REVIEW-FIX.md
  - solsys_code/tests/test_observation_projector.py
autonomous: true
requirements:
  - CR-01

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "CR-01's remaining half is recorded in 34-REVIEW-FIX.md as resolved by analysis: SOAR is scheduled through the same LCO Observation Portal, so one observation_id is one portal request and one event URL is its correct single identity -- no deferral language survives anywhere in the file"
    - "The resolution names the user's decision explicitly: no schema change; a partial unique index on tom_calendar_calendarevent(url) for non-blank urls is named as a possible later hardening that was considered and not chosen"
    - "A regression test asserts event_url() returns the identical url for one observation_id whether the record's facility is 'LCO' or 'SOAR', pinning the shared request-ID space as intended behaviour rather than a latent collision"
    - "python manage.py test solsys_code.tests.test_observation_projector passes with the new test, and ruff/ruff-format are clean on the changed test file"
  artifacts:
    - .planning/phases/34-the-observation-projector-trigger/34-REVIEW-FIX.md
    - solsys_code/tests/test_observation_projector.py
  key_links:
    - "SOARFacility subclasses LCOFacility and SOARSettings subclasses LCOSettings (tom_observations/facilities/soar.py:11,142); get_observation_url() is defined once on OCSFacility (tom_observations/facilities/ocs.py:1336) as urljoin(portal_url, '/requests/<id>'), and both FACILITIES entries in src/fomo/settings.py:230/238 set portal_url to https://observe.lco.global -- that inheritance chain is exactly why the two urls are identical, and the new test is what catches an upstream change to it"
    - "CR-02's already-shipped fix is what makes an operator-authored duplicate url land as 'unprojectable' rather than as a silent overwrite -- the resolution note depends on that fix staying in place"
---

<objective>
Close the one sub-item Phase 34's code-review fix run left open: the CR-01
facility-URL-namespace half. The user decided this closes **by analysis, with no schema
change** -- LCO and SOAR share one portal request-ID space by design, so a single event URL
per request is correct, not a collision.

Purpose: leave no open review finding behind for Phase 34, and pin the shared-ID-space
identity as intended behaviour so nobody "fixes" it later by namespacing the URL.
Output: a resolution note in `34-REVIEW-FIX.md` replacing the deferral prose, plus one
regression test in `test_observation_projector.py`.

No tracer slice applies here: this plan changes no production code and crosses no layers --
it is one planning-doc edit plus one characterization test over behaviour that already
ships. The two tasks are independent in content but share one file each, so they commit
separately.
</objective>

<execution_context>
@/home/tlister/git/fomo_devel/.claude/gsd-core/workflows/execute-plan.md
@/home/tlister/git/fomo_devel/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/34-the-observation-projector-trigger/34-REVIEW-FIX.md

Read only the named symbols, not whole files (use Serena `find_symbol` if available):
@solsys_code/observation_projector.py  -- `event_url()` (line 214) and `facility_for()` (line 58) only
@solsys_code/tests/test_observation_projector.py -- `_ObservationProjectorTestBase` (lines 25-64, the
  `_make_record` helper) and `TestProjectRecordWrites`' last test,
  `test_lco_and_soar_records_use_distinct_facility_instances_and_urls` (lines 445-460)

**Facts already established at planning time -- do not re-derive:**
- `get_observation_url()` is defined once, on `OCSFacility`
  (`tom_observations/facilities/ocs.py:1336`), as `urljoin(portal_url, f'/requests/{observation_id}')`.
- `SOARSettings(LCOSettings)` at `tom_observations/facilities/soar.py:11` and
  `SOARFacility(LCOFacility)` at `soar.py:142` -- SOAR inherits that URL builder unchanged.
- `src/fomo/settings.py:230` and `:238` both set `portal_url` to `https://observe.lco.global`.
- `ObservationRecord.observation_id` is a plain `CharField(max_length=255)` with no unique
  constraint, so two records may legitimately share an id across two facility values.
- The `post_save` receiver is wired globally, so `ObservationRecord.objects.create()` inside
  `_make_record()` already projects the record -- existing tests in this module rely on that.
</context>

Comment-text discipline: the literals below are named by Task 1's own negative-grep gate (and,
for the first, by the CLAUDE.md terminology instruction the gate enforces), so they are
allowlisted rather than avoided.

<!-- planner-discipline-allow: upsert -->
<!-- planner-discipline-allow: deferred, not -->
<!-- planner-discipline-allow: Partial by design -->
<!-- planner-discipline-allow: is intentionally not closed by this run -->
<!-- planner-discipline-allow: intentionally left for a follow-up decision -->

<tasks>

<task type="auto">
  <name>Task 1: Record the CR-01 shared-request-ID resolution in 34-REVIEW-FIX.md</name>
  <files>.planning/phases/34-the-observation-projector-trigger/34-REVIEW-FIX.md</files>
  <action>
Replace the fix report's three pieces of open-item prose with one resolution, written in
plain English (CLAUDE.md planning-doc terminology: never "upsert", never "shape").

Piece 1 -- the CR-01 entry's closing bold-led paragraph, currently lines 56-71, the only
bold-led paragraph in that entry and the one immediately following the "Two regression
tests added" sentence. Replace the whole paragraph (its heading phrase included) with a
bold-led **Resolved by analysis** paragraph carrying these five points, in this order:
(1) `SOARFacility` subclasses `LCOFacility` and `SOARSettings` subclasses `LCOSettings`
(`tom_observations/facilities/soar.py`), so SOAR observations are scheduled through the same
LCO Observation Portal and their `observation_id` values live in one shared request-ID space.
(2) An LCO record and a SOAR record carrying the same `observation_id` are therefore the same
portal request, so `https://observe.lco.global/requests/<id>` is that request's correct single
identity -- namespacing `event_url()` per facility would wrongly split one real request into
two calendar events.
(3) Every FOMO writer -- the projector, the reconciler and `load_telescope_runs` -- finds the
event by `url` and creates it only if missing, so the only remaining way to get two rows with
the same non-blank url is a person editing `url` by hand in tom_calendar's event form, which
CR-02's fix already catches and reports as `unprojectable`.
(4) The evidence: a check of the developer database on 2026-09-11 found 241 calendar events,
0 duplicate non-blank urls, and 10 blank urls. Record these figures as observed on that date;
do not re-run the query.
(5) The decision, attributed to the user: no schema change. A partial unique index on
`tom_calendar_calendarevent(url)` limited to non-blank urls stays available as a possible later
hardening, but it was considered and explicitly not chosen -- so nothing here is outstanding work.

Piece 2 -- the first bullet under `## Notes and Follow-ups`, currently lines 313-316, the one
whose bold lead-in names CR-01's facility-URL-namespace collision. Rewrite that bullet as a
one-or-two-sentence closed-item note pointing at the CR-01 entry's resolution paragraph, and
drop the sentence claiming it is the only unapplied sub-item across the 17 findings.

Piece 3 -- the `- Skipped: 0 (...)` line's parenthetical in the `**Summary:**` block, lines
23-24, which currently says the CR-01 sub-part was held back for a later decision. Keep
`Skipped: 0`; rewrite the parenthetical so it agrees with the resolution (CR-01's second half
is closed by analysis, see its entry).

Leave the YAML frontmatter byte-for-byte unchanged (`findings_in_scope: 17`, `fixed: 17`,
`skipped: 0`, `status: all_fixed`) -- this is a resolution note on an existing finding, not a
new finding and not a re-count. Change no other section of the file.

Commit with message prefix `docs(quick-260911-9rd):`, ending with the two trailer lines the
plan's `<verification>` section quotes.
  </action>
  <verify>
    <automated>bash -c 'set -e; F=.planning/phases/34-the-observation-projector-trigger/34-REVIEW-FIX.md; grep -q "^findings_in_scope: 17$" "$F"; grep -q "^fixed: 17$" "$F"; grep -q "^skipped: 0$" "$F"; grep -q "^status: all_fixed$" "$F"; grep -q "SOARFacility" "$F"; grep -q "SOARSettings" "$F"; grep -q "observe.lco.global/requests/" "$F"; grep -Eq "241 (calendar )?events" "$F"; grep -q "tom_calendar_calendarevent" "$F"; ! grep -qi "upsert" "$F"; ! grep -q "deferred, not" "$F"; ! grep -q "Partial by design" "$F"; ! grep -q "is intentionally not closed by this run" "$F"; ! grep -q "intentionally left for a follow-up decision" "$F"; echo VERIFY_OK'</automated>
  </verify>
  <done>The CR-01 entry states the shared-request-ID rationale, the evidence figures and the user's no-schema-change decision; the Notes bullet and the Summary parenthetical agree with it; no deferral wording remains in the file; frontmatter counts are untouched; the edit is committed.</done>
</task>

<task type="auto">
  <name>Task 2: Pin the shared LCO/SOAR event URL with a regression test</name>
  <files>solsys_code/tests/test_observation_projector.py</files>
  <precondition>`python manage.py test` runs in this checkout -- `src/fomo/_version.py` (gitignored, generated by setuptools_scm, needed at import time) must exist. It is absent in a fresh git worktree; if execution is happening in one, copy it from the main checkout before running the test command. Halt and report if `python manage.py test` cannot start.</precondition>
  <behavior>
    - One LCO record and one SOAR record built with the *same* `observation_id`: `event_url()`
      returns the identical string for both, because both facilities resolve the same portal.
    - That shared url is the single identity of one portal request, so exactly one
      `CalendarEvent` row exists at it after both records have been projected.
    - The existing `test_lco_and_soar_records_use_distinct_facility_instances_and_urls` (which
      uses two *different* ids) keeps passing -- distinct ids still give distinct urls.
  </behavior>
  <action>
Add one test method to `TestProjectRecordWrites` in
`solsys_code/tests/test_observation_projector.py`, placed directly after the existing
`test_lco_and_soar_records_use_distinct_facility_instances_and_urls` so the "same id, one url"
and "different ids, two urls" cases read side by side. Name it
`test_lco_and_soar_records_sharing_an_observation_id_get_one_shared_url`.

Build both records with the module's own `self._make_record()` helper, passing one shared
`observation_id` value, `facility='LCO'` for the first and `facility='SOAR'` with
`instrument_type='SOAR_GHTS_REDCAM'` for the second (mirroring the neighbouring test's SOAR
fixture). Any Target fixture comes from the class-level `cls.target`, which is already a
`NonSiderealTargetFactory` instance -- do not introduce a sidereal target (CLAUDE.md).

Resolve each record's facility through `op.facility_for()` and assert
`op.event_url(lco_record, lco_facility)` equals `op.event_url(soar_record, soar_facility)`.
Then assert `CalendarEvent.objects.filter(url=shared_url).count()` is 1, showing that the two
records converge on one event rather than on two -- that convergence is the intended result,
not a defect. Do not assert the literal `https://observe.lco.global/...` string: that value
comes from `settings.FACILITIES`, and hard-coding it would make the test fail on a deployment
that points `portal_url` elsewhere, which is not what this test is pinning.

Give the method a short docstring (this module's tests use them for review findings) saying
in plain English that SOAR is scheduled through the same LCO Observation Portal, so one
`observation_id` is one request and one url; per-facility namespacing would wrongly split it.
Keep the neighbouring test untouched.

Commit with message prefix `test(quick-260911-9rd):`, ending with the two trailer lines the
plan's `<verification>` section quotes.
  </action>
  <verify>
    <automated>bash -c 'set -e; T=solsys_code/tests/test_observation_projector.py; grep -q "def test_lco_and_soar_records_sharing_an_observation_id_get_one_shared_url" "$T"; grep -q "def test_lco_and_soar_records_use_distinct_facility_instances_and_urls" "$T"; pre-commit run ruff --files "$T"; pre-commit run ruff-format --files "$T"; python manage.py test solsys_code.tests.test_observation_projector'</automated>
  </verify>
  <done>The new test exists in `TestProjectRecordWrites`, the whole `test_observation_projector` module passes, ruff and ruff-format are clean on the file, and the change is committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator -> tom_calendar event form | The one remaining writer that can author a duplicate non-blank `CalendarEvent.url`; unauthenticated per CR-02's own repro |
| FOMO -> LCO Observation Portal | `portal_url` from `src/fomo/settings.py` is the only externally-controlled input to the identity key; unchanged by this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-q9rd-01 | Tampering | Hand-edited `CalendarEvent.url` via tom_calendar's event form | low | accept | Already mitigated by CR-02's shipped fix (`project_record()` catches `MultipleObjectsReturned` and reports `unprojectable`); Task 1 records this as the residual path and names the partial unique index as the un-chosen further hardening, so the acceptance is explicit rather than silent |
| T-q9rd-02 | Information Disclosure | The evidence figures written into `34-REVIEW-FIX.md` | low | accept | Only aggregate row counts (241 events, 0 duplicate non-blank urls, 10 blank) are recorded -- no urls, ids, proposals, targets or personal data are copied into the planning doc |
| T-q9rd-03 | Tampering | `solsys_code/tests/test_observation_projector.py` | low | accept | Test-only addition; no production module is edited, so no runtime behaviour, permission or data path changes. The test asserts current behaviour and cannot weaken a gate |

No package-manager installs occur in this plan, so no supply-chain (`T-*-SC`) row applies and
no package-legitimacy checkpoint is required.
</threat_model>

<verification>
Run from the repository root on branch `issue37-telescope-runs-calendar` (confirm with
`git branch --show-current` before any branch-implicit git command, per CLAUDE.md):

1. `python manage.py test solsys_code.tests.test_observation_projector` -- passes, new test included.
2. `pre-commit run ruff --files solsys_code/tests/test_observation_projector.py` -- clean.
3. `pre-commit run ruff-format --files solsys_code/tests/test_observation_projector.py` -- clean.
4. `git log --oneline -2` -- exactly two new commits, one `docs(quick-260911-9rd):` and one
   `test(quick-260911-9rd):`.
5. `git status --porcelain` -- no files outside the two in `files_modified` are changed.

Every commit message must end with exactly these two trailer lines:

```
Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01QehSqWN5erUd3eh2QKsGGa
```

Do **not** push. Committing runs the repo's full pre-commit gate (ruff, ruff-format,
sphinx-build, pytest-check, no-commit-to-branch); let it run rather than bypassing it.

Out of scope, explicitly: no migration, no schema change, no edit to
`solsys_code/observation_projector.py` or any other production module, and no notebook or
runbook update -- CLAUDE.md's paired-docs rule is not triggered here because no module's
behaviour changes.
</verification>

<success_criteria>
- `34-REVIEW-FIX.md` records CR-01's second half as resolved by analysis, with the SOAR-inherits-LCO
  rationale, the single-identity argument, the find-or-create/manual-edit residual path, the
  2026-09-11 evidence figures and the user's no-schema-change decision.
- No deferral or open-follow-up wording for CR-01 survives anywhere in that file, and its
  frontmatter counts remain 17/17/0 with `status: all_fixed`.
- `test_observation_projector` contains a passing test asserting one shared `event_url()` for an
  LCO and a SOAR record with the same `observation_id`, and the whole module passes.
- Exactly two commits, on `issue37-telescope-runs-calendar`, touching exactly two files, not pushed.
</success_criteria>

<output>
Create `.planning/quick/260911-9rd-close-phase-34-review-finding-cr-01-by-a/260911-9rd-SUMMARY.md` when done
</output>
