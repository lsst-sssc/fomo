---
phase: quick-260711-o71
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - solsys_code/tests/test_campaign_approval.py
  - .planning/quick/260711-o71-measure-current-test-coverage-for-solsys/coverage-before.txt
  - .planning/quick/260711-o71-measure-current-test-coverage-for-solsys/coverage-after.txt
  - .planning/quick/260711-o71-measure-current-test-coverage-for-solsys/coverage-diff.md
autonomous: true
requirements: [CR-01, CR-02]

must_haves:
  truths:
    - "A name/short_name/old_names datalist candidate (NOT a literal obscode) submitted as site_selection through the real campaigns:decide POST resolves run.site to the matching Observatory via build_site_candidates()'s obscode mapping (the exact CR-01 code path), with no placeholder fabrication."
    - "A GET of the real observatory_create.html with ?next=/?obscode= renders a hidden next input carrying next_url; POSTing only the fields extracted from the rendered form (no hand-injected next) redirects to next_url — proving the template carries the field, not just that the view logic works."
    - "Baseline (before) and post-test (after) coverage reports for solsys_code — overall total plus campaign_views.py, campaign_utils.py, and solsys_code_observatory/views.py — are captured to the quick task directory, with a before/after diff summary."
  artifacts:
    - solsys_code/tests/test_campaign_approval.py
    - .planning/quick/260711-o71-measure-current-test-coverage-for-solsys/coverage-before.txt
    - .planning/quick/260711-o71-measure-current-test-coverage-for-solsys/coverage-after.txt
    - .planning/quick/260711-o71-measure-current-test-coverage-for-solsys/coverage-diff.md
  key_links:
    - "site_selection string -> build_site_candidates() pool -> obscode -> resolve_site() Tier 1 (existing Observatory) -> run.site"
    - "observatory_create.html hidden next input -> POST next -> CreateObservatory.get_success_url() validated redirect"
---

<objective>
Measure current test coverage for the `solsys_code` Django app, add two PERMANENT regression
tests closing the informational test-coverage gap the Phase 21 verifier flagged (CR-01 and CR-02
from `21-REVIEW.md`/`21-REVIEW-FIX.md` shipped without dedicated committed regression tests), then
re-measure and report the coverage diff.

Purpose: The two review fixes (CR-01: name/short_name/old_names datalist candidate resolution via
`build_site_candidates()`; CR-02: the real `observatory_create.html` hidden `next` round-trip) are
behaviorally correct today but only verified via temporary tests that were removed. A future
refactor could silently reintroduce either bug. This task makes both permanent, mirroring the exact
rigor of the verifier's temporary end-to-end tests (see `21-VERIFICATION.md` Behavioral Spot-Checks).

Output: two new test classes in `test_campaign_approval.py`, plus before/after coverage reports and
a diff summary in the quick task directory.

Scope note (CLAUDE.md notebook rule): this plan modifies ONLY `test_campaign_approval.py`. It does
not touch any of the demo-notebook-coupled modules (`telescope_runs.py`,
`load_telescope_runs.py`, `sync_lco_observation_calendar.py`, `sync_gemini_observation_calendar.py`),
so no paired notebook update is in scope.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

# The two fixes these tests permanently guard (already shipped in Phase 21):
@.planning/phases/21-site-disambiguation-submitter-contact-opt-in/21-REVIEW-FIX.md
# The verifier's own temporary E2E approach these tests must mirror as PERMANENT tests:
@.planning/phases/21-site-disambiguation-submitter-contact-opt-in/21-VERIFICATION.md

# Code under test (read for exact call paths and mocking boundaries):
@solsys_code/campaign_views.py
@solsys_code/campaign_utils.py
@solsys_code/solsys_code_observatory/views.py
@src/templates/solsys_code_observatory/observatory_create.html

# Target file — match its existing conventions (CampaignApprovalTestBase, _make_pending_run,
# BULK_MPC_FIXTURE, _flatten_mpc_candidates patching at the view import site, _stub_to_observatory):
@solsys_code/tests/test_campaign_approval.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture baseline solsys_code coverage</name>
  <files>.planning/quick/260711-o71-measure-current-test-coverage-for-solsys/coverage-before.txt</files>
  <action>
    From the repo root, measure current `solsys_code` coverage using the already-installed
    coverage.py (7.10.4, confirmed via `pyproject.toml` and `python -c "import coverage"`; no
    install needed). Run `coverage run --source=solsys_code manage.py test solsys_code` to run the
    full Django app suite under coverage, then write the full `coverage report` output to
    `.planning/quick/260711-o71-measure-current-test-coverage-for-solsys/coverage-before.txt`. That
    report's rows already include the three files of interest (`campaign_views.py`,
    `campaign_utils.py`, `solsys_code_observatory/views.py`) plus the TOTAL line — no separate
    focused report is needed. Note: the suite imports `solsys_code.ephem_utils` transitively, which
    triggers the ~1.6 GB SPICE-kernel download on a COLD `~/.cache/sorcha/`; the kernels are already
    cached from Phase 21's verifier run, so expect only a few minutes of runtime, not a download.
    The transient `.coverage` data file coverage.py writes at the repo root is NOT a committed
    artifact — leave it out of any commit; only the `.txt`/`.md` files under the quick directory are
    deliverables. Do NOT `--fix`/reformat anything; this task only measures.
  </action>
  <verify>
    <automated>test -s .planning/quick/260711-o71-measure-current-test-coverage-for-solsys/coverage-before.txt && grep -Eq 'campaign_views\.py|TOTAL' .planning/quick/260711-o71-measure-current-test-coverage-for-solsys/coverage-before.txt</automated>
  </verify>
  <done>coverage-before.txt exists, is non-empty, and contains the TOTAL row plus the three focus-file rows from the pre-change suite run.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add permanent CR-01 and CR-02 regression tests</name>
  <files>solsys_code/tests/test_campaign_approval.py</files>
  <behavior>
    - CR-01 (name/short_name/old_names -> obscode resolution through the real decide POST):
      A `site_selection` value that is a display-string candidate (a name, short_name, or old_names
      string — NOT a literal obscode) submitted through `campaigns:decide` resolves `run.site` to the
      matching Observatory, `site_needs_review` False, and creates no extra Observatory row. This
      exercises `CampaignRunDecisionView.post()`'s `build_site_candidates().get(selection, selection)`
      obscode mapping (the CR-01 fix), which the existing `TestSiteSelectionResolution` never
      covers — its only positive case submits the literal obscode `'G37'`, which passes through the
      mapping unchanged and so does NOT exercise the display-string->obscode lookup.
    - CR-02 (real template carries the hidden `next` field):
      A GET of the real `observatory_create.html` with `?next=`/`?obscode=` renders a hidden
      `<input name="next">` carrying the `next` URL. Replaying ONLY the fields the rendered form
      itself contains (extracted from the response HTML, not hand-constructed with `next` injected)
      via POST redirects to the `next` target. This proves the template round-trip, not just that
      `get_success_url()` works when handed a manually-built POST body.
  </behavior>
  <action>
    Add TWO new permanent test classes to `solsys_code/tests/test_campaign_approval.py`, matching the
    file's existing conventions (subclass `CampaignApprovalTestBase`; reuse `_make_pending_run`,
    `BULK_MPC_FIXTURE`, `campaign_utils._flatten_mpc_candidates`, and the module-level
    `_stub_to_observatory` helper already defined near line 490; login as `staffcoordinator`/`pw` in
    `setUp`; `cache.clear()` in `setUp`/`tearDown` where a candidate pool is involved, mirroring
    `TestApprovalQueueSiteDisambiguationUI`). No `tom_targets.Target` fixture is needed
    (`CampaignRun.target` stays unset, as everywhere else in this module), so CLAUDE.md's
    NonSiderealTargetFactory rule does not arise — do NOT introduce a Target fixture.

    CR-01 class (e.g. `TestSiteSelectionNameCandidateResolution`): In `setUp`, `cache.clear()`, log in
    staff, and patch `solsys_code.campaign_views.build_site_candidates` (the view's import site, NOT
    the util module) to return an explicit pool that maps a name, a short_name, AND an old_names
    string all to obscode `'G37'` — e.g. build it as `{**_flatten_mpc_candidates(BULK_MPC_FIXTURE),
    'LDT': 'G37', 'Historic Lowell Reflector': 'G37'}` so `'Lowell Discovery Telescope'` (name, from
    the fixture's `G37` record), `'LDT'` (short_name), and `'Historic Lowell Reflector'` (old_names)
    are all present as display-string keys pointing at `'G37'`. Create a local `Observatory` row
    `obscode='G37'` (name `'Lowell Discovery Telescope'`, ground/`OPTICAL_OBSTYPE`, with lat/lon/alt
    like the existing `TestSiteSelectionResolution` G37 fixture) so `resolve_site('G37',
    create_placeholder=False)` hits Tier 1 offline (confirmed: `campaign_utils.resolve_site` returns
    the existing Observatory at the Tier-1 `Observatory.objects.get(obscode=code)` with no network
    call). Then, for each display string in the three candidate types, create a fresh pending run
    whose `site_raw` is a deliberate typo that never self-resolves (e.g. `'Lowell Discvery Tel'`),
    POST `campaigns:decide` with `{'action': 'approve', 'site_selection': <display string>}`, and
    assert: response 302, `run.site.obscode == 'G37'`, `run.site_needs_review` is False, and
    `Observatory.objects.count() == 1` (no fabrication). Use `subTest` over the three strings so a
    single failure localizes to the candidate type. Add a docstring stating this is the permanent CR-01
    regression the verifier proved via a temporary name-candidate E2E test in `21-VERIFICATION.md`.

    CR-02 class (e.g. `TestCreateObservatoryTemplateNextRoundTrip`): Add a small module-level helper
    (near `_stub_to_observatory`) that extracts the create form's own input fields from rendered HTML
    using `html.parser.HTMLParser` (stdlib — no new dependency): track whether the parser is inside
    the `<form>` whose `action` attribute contains the create URL fragment, and while inside it,
    collect every `<input>` element's `name`->`value` into a dict; stop collecting at that form's
    `</form>`. In the test: `next_url = reverse('campaigns:approval_queue')`; GET
    `reverse('solsys_code_observatory:create')` with `{'obscode': 'G37', 'next': next_url}`; assert
    200 and that the extracted-fields dict contains `'next'` equal to `next_url` (this is the
    load-bearing proof the template rendered the hidden field — NOT hand-injected). Then POST the
    exact extracted-fields dict back to the create URL, patching
    `solsys_code.solsys_code_observatory.views.MPCObscodeFetcher.query` and `.to_observatory`
    (`side_effect=_stub_to_observatory`) exactly as `TestCreateObservatoryRoundTrip` does, and assert
    `self.assertRedirects(response, next_url)` and `Observatory.objects.filter(obscode='G37').count()
    == 1`. Add a docstring stating this is the permanent CR-02 regression the verifier proved via a
    temporary real-template round-trip test in `21-VERIFICATION.md`.

    Keep both classes offline and deterministic (no live MPC/network). Run `ruff format` and
    `ruff check` on the file after editing so the quality gates stay clean (single quotes, 120 cols).
  </action>
  <verify>
    <automated>python manage.py test solsys_code.tests.test_campaign_approval 2>&1 | tail -5 && ruff check solsys_code/tests/test_campaign_approval.py && ruff format --check solsys_code/tests/test_campaign_approval.py</automated>
  </verify>
  <done>Both new test classes pass under `manage.py test solsys_code.tests.test_campaign_approval`; the CR-01 class asserts a name, a short_name, and an old_names candidate each resolve run.site via the real decide POST with no fabrication; the CR-02 class extracts the rendered form's own fields and asserts the POST redirects to next_url; ruff check and ruff format --check are clean.</done>
</task>

<task type="auto">
  <name>Task 3: Re-measure coverage and write the before/after diff</name>
  <files>.planning/quick/260711-o71-measure-current-test-coverage-for-solsys/coverage-after.txt, .planning/quick/260711-o71-measure-current-test-coverage-for-solsys/coverage-diff.md</files>
  <action>
    Re-run the identical measurement from Task 1 now that the two new tests exist: `coverage run
    --source=solsys_code manage.py test solsys_code`, then write the full `coverage report` output to
    `.planning/quick/260711-o71-measure-current-test-coverage-for-solsys/coverage-after.txt`. Then
    write a short human-readable `coverage-diff.md` in the same directory summarizing: the overall
    TOTAL coverage percentage before vs after and the delta; and the before/after/delta for each of
    the three touched files (`campaign_views.py`, `campaign_utils.py`,
    `solsys_code_observatory/views.py`), read from the two report files. Keep it to a compact table
    plus a one-line takeaway (the two new tests exercise `CampaignRunDecisionView.post()`'s
    display-string->obscode mapping and the `CreateObservatory` template `next` round-trip, so any
    delta should concentrate in `campaign_views.py` / `solsys_code_observatory/views.py`; note that a
    flat percentage is still a legitimate result if those lines were already incidentally covered — a
    permanent guard against regression is the point, not a coverage number). Do not commit the
    transient repo-root `.coverage` file.
  </action>
  <verify>
    <automated>test -s .planning/quick/260711-o71-measure-current-test-coverage-for-solsys/coverage-after.txt && test -s .planning/quick/260711-o71-measure-current-test-coverage-for-solsys/coverage-diff.md && grep -Eqi 'total' .planning/quick/260711-o71-measure-current-test-coverage-for-solsys/coverage-diff.md</automated>
  </verify>
  <done>coverage-after.txt captures the post-test suite report; coverage-diff.md summarizes overall %% before/after/delta plus the per-file before/after/delta for the three touched files, with a one-line takeaway.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| staff browser -> campaigns:decide POST | staff-submitted `site_selection` free text crosses into server-side site resolution (existing, staff-gated) |
| browser -> CreateObservatory `?next=` | attacker-controllable `next` redirect target crosses into `get_success_url()` (existing) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-o71-01 | Elevation/Redirect | `CreateObservatory.get_success_url()` open-redirect via `?next=` | medium | mitigate | Already mitigated by `url_has_allowed_host_and_scheme()`; the new CR-02 test replays the template's own rendered `next` field and asserts a same-host redirect, giving this protection permanent regression coverage (the existing `test_unsafe_next_falls_back_to_detail_redirect` covers the off-host reject case). |
| T-o71-02 | Tampering | this plan's own scope (test additions + coverage measurement) | low | accept | No production code, endpoint, schema, or dependency is added or changed; only `test_campaign_approval.py` and coverage-report artifacts. No new attack surface. |
</threat_model>

<verification>
- `python manage.py test solsys_code.tests.test_campaign_approval` passes (both new classes green, existing classes unregressed).
- `ruff check solsys_code/tests/test_campaign_approval.py` and `ruff format --check solsys_code/tests/test_campaign_approval.py` are clean.
- coverage-before.txt, coverage-after.txt, and coverage-diff.md all exist and are non-empty in the quick task directory.
</verification>

<success_criteria>
- Two permanent test classes exist in `test_campaign_approval.py`: one proving a name/short_name/old_names `site_selection` candidate resolves `run.site` through the real `campaigns:decide` POST (CR-01), one proving the real `observatory_create.html` carries the hidden `next` field through a rendered-form POST round-trip (CR-02).
- Baseline and post-test `solsys_code` coverage are captured, and a before/after diff (overall % + the three touched files) is summarized in `coverage-diff.md`.
- Quality gates (`ruff check`, `ruff format --check`) stay clean; the full `solsys_code` suite passes.
</success_criteria>

<output>
Create `.planning/quick/260711-o71-measure-current-test-coverage-for-solsys/260711-o71-SUMMARY.md` when done
</output>
