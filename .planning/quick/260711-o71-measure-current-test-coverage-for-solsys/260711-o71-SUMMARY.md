---
phase: quick-260711-o71
plan: 01
subsystem: testing
tags: [django, coverage.py, campaign-approval, regression-tests, html.parser]

requires:
  - phase: 21-site-disambiguation-submitter-contact-opt-in
    provides: "CR-01/CR-02 fixes in campaign_views.py, solsys_code_observatory/views.py, observatory_create.html (21-REVIEW-FIX.md)"
provides:
  - "Permanent regression coverage for CR-01 (site_selection name/short_name/old_names candidate resolution via the real campaigns:decide POST)"
  - "Permanent regression coverage for CR-02 (observatory_create.html's hidden next field round-trip via a real-template extraction helper)"
  - "Baseline and post-test solsys_code coverage reports plus a before/after diff summary"
affects: [solsys_code-testing, campaign-approval]

tech-stack:
  added: []
  patterns:
    - "html.parser.HTMLParser (stdlib) used to extract a rendered Django form's own input fields for a template-round-trip test, instead of hand-constructing the POST body"

key-files:
  created:
    - .planning/quick/260711-o71-measure-current-test-coverage-for-solsys/coverage-before.txt
    - .planning/quick/260711-o71-measure-current-test-coverage-for-solsys/coverage-after.txt
    - .planning/quick/260711-o71-measure-current-test-coverage-for-solsys/coverage-diff.md
  modified:
    - solsys_code/tests/test_campaign_approval.py

key-decisions:
  - "Each subTest iteration in TestSiteSelectionNameCandidateResolution uses a distinct window_start/window_end (not the shared _make_pending_run default) to avoid colliding with CampaignRun's (campaign, telescope_instrument, window_start, window_end) UniqueConstraint across the three candidate-type iterations"
  - "Flat coverage percentage (97% before and after, identical per-file Stmts/Miss/Cover for the three touched files) is documented as an expected, legitimate result -- the two new tests close a regression-coverage gap (proving the specific CR-01/CR-02 code paths), not a line-coverage gap, since adjacent existing tests already reached the same lines via similar-but-not-identical inputs"

patterns-established:
  - "Template-round-trip regression test pattern: GET the real template, extract the rendered form's own fields via a stdlib HTMLParser helper scoped to the target <form>'s action attribute, then POST exactly those extracted fields back -- proves the template itself carries a field, not just that the view logic handles a hand-built payload correctly"

requirements-completed: [CR-01, CR-02]

coverage:
  - id: D1
    description: "A name/short_name/old_names site_selection display-string candidate submitted through the real campaigns:decide POST resolves run.site to the matching Observatory via build_site_candidates()'s obscode mapping, with no placeholder fabrication (CR-01 permanent regression test)"
    requirement: "CR-01"
    verification:
      - kind: integration
        ref: "solsys_code/tests/test_campaign_approval.py#TestSiteSelectionNameCandidateResolution.test_name_short_name_and_old_names_candidates_resolve_via_real_decide_post"
        status: pass
    human_judgment: false
  - id: D2
    description: "GET of the real observatory_create.html with ?next=/?obscode= renders a hidden next input carrying next_url; POSTing only the fields extracted from the rendered form (no hand-injected next) redirects to next_url (CR-02 permanent regression test)"
    requirement: "CR-02"
    verification:
      - kind: integration
        ref: "solsys_code/tests/test_campaign_approval.py#TestCreateObservatoryTemplateNextRoundTrip.test_rendered_form_carries_next_field_and_replaying_it_redirects"
        status: pass
    human_judgment: false
  - id: D3
    description: "Baseline and post-test solsys_code coverage reports (overall TOTAL plus campaign_views.py, campaign_utils.py, solsys_code_observatory/views.py) captured with a before/after diff summary"
    verification:
      - kind: other
        ref: ".planning/quick/260711-o71-measure-current-test-coverage-for-solsys/coverage-before.txt, coverage-after.txt, coverage-diff.md"
        status: pass
    human_judgment: false

duration: 20min
completed: 2026-07-11
status: complete
---

# Quick Task 260711-o71: Measure Current Test Coverage for solsys_code Summary

**Added two permanent regression tests (CR-01 name/short_name/old_names site_selection resolution, CR-02 observatory_create.html's real-template `next` round-trip) closing Phase 21's informational test-coverage gap, plus baseline/post-test coverage reports showing a flat 97% overall with identical per-file numbers on the three touched files -- the tests close a regression-coverage gap, not a line-coverage gap.**

## Performance

- **Duration:** ~20 min
- **Tasks:** 3/3 completed
- **Files modified:** 1 (test file) + 3 artifacts (coverage-before.txt, coverage-after.txt, coverage-diff.md)

## Accomplishments

- `TestSiteSelectionNameCandidateResolution` proves a `name_utf8`, `short_name`, and `old_names` display-string candidate (not a literal obscode) each resolve `run.site` through the real `campaigns:decide` POST via `build_site_candidates().get(selection, selection)` -- the exact CR-01 code path `21-REVIEW.md`/`21-VERIFICATION.md` flagged as untested (the pre-existing `TestSiteSelectionResolution` only exercised the literal-obscode passthrough case).
- `TestCreateObservatoryTemplateNextRoundTrip` proves `observatory_create.html`'s hidden `next` field is actually rendered by the real template (extracted via a new stdlib `html.parser.HTMLParser`-based helper, `_extract_create_observatory_form_fields`) and that replaying exactly those extracted fields via POST redirects to `next_url` -- the exact CR-02 code path the verifier previously only checked with a temporary, removed test.
- Full `solsys_code` suite went from 417 to 419 tests, all green; `ruff check`/`ruff format --check` clean on the modified test file.
- Baseline and post-test coverage reports captured; `coverage-diff.md` documents that the overall TOTAL and the three touched files' `Stmts`/`Miss`/`Cover` are unchanged, with an explanation of why that's expected (adjacent pre-existing tests already reached the same lines via different inputs; these two tests add regression specificity, not new line reach).

## Task Commits

Each task was committed atomically:

1. **Task 1: Capture baseline solsys_code coverage** - `4dc4182` (chore)
2. **Task 2: Add permanent CR-01 and CR-02 regression tests** - `114a427` (test)
3. **Task 3: Re-measure coverage and write the before/after diff** - `adcd59a` (chore)

_Note: this quick task's docs commit (SUMMARY.md/STATE.md) is applied separately by the orchestrator, not by this executor._

## Files Created/Modified

- `solsys_code/tests/test_campaign_approval.py` - Added `TestSiteSelectionNameCandidateResolution` (CR-01), `_extract_create_observatory_form_fields` helper + `TestCreateObservatoryTemplateNextRoundTrip` (CR-02); added `from html.parser import HTMLParser` import.
- `.planning/quick/260711-o71-measure-current-test-coverage-for-solsys/coverage-before.txt` - Baseline `coverage report` output (417 tests, TOTAL 97%).
- `.planning/quick/260711-o71-measure-current-test-coverage-for-solsys/coverage-after.txt` - Post-test `coverage report` output (419 tests, TOTAL 97%).
- `.planning/quick/260711-o71-measure-current-test-coverage-for-solsys/coverage-diff.md` - Overall + per-file before/after/delta table and takeaway.

## Decisions Made

- Used distinct `window_start`/`window_end` values per `subTest` iteration in the CR-01 test (rather than the shared `_make_pending_run` default) to avoid tripping `CampaignRun`'s `(campaign, telescope_instrument, window_start, window_end)` `UniqueConstraint` across the three candidate-type creates within the same test method. Discovered via a real test failure (`IntegrityError: UNIQUE constraint failed`) on the first run; fixed inline (Rule 1 -- bug in the test I was writing, not a deviation from the plan's design).
- Documented the flat coverage-percentage result in `coverage-diff.md` per the plan's explicit guidance rather than treating it as a gap -- confirmed by checking `coverage report -m` for the three files post-change and cross-referencing which existing tests already reach those specific lines.

## Deviations from Plan

None - plan executed exactly as written. The `IntegrityError` encountered while writing Task 2's test was a bug in the test code itself (introduced and fixed within the same task, before any commit), not a deviation requiring documentation under Rules 1-4 -- it never reached a committed state.

## Issues Encountered

One test-authoring bug (see Decisions Made above): the first draft of `TestSiteSelectionNameCandidateResolution`'s `subTest` loop reused the same default `window_start`/`window_end` for all three iterations, colliding with `CampaignRun`'s natural-key `UniqueConstraint` on the second and third candidate. Caught immediately by running the test suite before committing; fixed by giving each iteration a distinct `window_start`/`window_end` derived from the loop index.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

CR-01 and CR-02 now have permanent, committed regression coverage in `test_campaign_approval.py`; the informational test-coverage gap `21-VERIFICATION.md` flagged is closed. No blockers. `.planning/STATE.md`'s "Quick Tasks Completed" table should record this task alongside `260705-l1v`.

---
*Quick task: 260711-o71*
*Completed: 2026-07-11*

## Self-Check: PASSED

All created/modified files exist on disk; all three task commits (`4dc4182`, `114a427`, `adcd59a`) confirmed present in `git log`.
