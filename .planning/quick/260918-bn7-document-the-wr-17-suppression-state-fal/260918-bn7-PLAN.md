---
phase: quick-260918-bn7
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/runbooks/telescope_runs_calendar.rst
autonomous: true
requirements: [WR-16, WR-17]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The runbook states that a lock-held skip leaves the crontab line's own exit status at 0 — so cron's syslog line, a systemd `OnFailure=` hook or a monitoring wrapper never records a routine tick overlap as a failure, and the line's status is only ever `run_unattended`'s (0 healthy, 1 failing)."
    - "The runbook still names `flock -n -E 99`'s own dedicated exit code 99 as the mechanism the skip detection is gated on — that fact did not change, and the prose must not be corrected into implying flock stopped returning it."
    - "The `When nothing has appeared` checklist item about repeated \"lock held\" lines no longer implies a contended tick is distinguishable by its exit status; it tells the operator the log line is the only signal, precisely because the status reads 0 like a healthy tick."
    - "The runbook names the fallback suppression-state file `fomo-unattended-state.fallback.json`, says it lives in the system temp directory (deliberately outside `FOMO_STATE_DIR`), and says when it gets written: the tick logs an error and writes there instead of failing when the primary state file cannot be written."
    - "The runbook states that the runner reads whichever of the primary and fallback state files was written most recently, so a tick that could only persist to the fallback is still honored by the next tick's notification decision — the reason an unwritable state directory no longer mails an identical failure notice every 15 minutes (D-11 failing open)."
    - "The runbook states that a later successful write to the primary location deletes the stale fallback by itself, so there is nothing for an operator to clean up by hand once the directory is fixed."
    - "The runbook states that `check_unattended`'s state-directory check is a setup-time preflight only, and that the runtime fallback — not that check — is what covers a directory going bad after it passed."
    - "A troubleshooting entry lets an operator who sees the fallback error line in the log act on it: fix the primary directory, and do not delete the fallback file while the primary is still unwritable (it is the only record of the current suppression state)."
    - "`pre-commit run sphinx-build --all-files` is clean — the edits introduce no RST error, and every new heading's `^^^^` underline is at least as long as its title."
    - "No code, test, notebook or crontab-template file is modified: `docs/runbooks/telescope_runs_calendar.rst` is the only file in the diff."
  artifacts:
    - "docs/runbooks/telescope_runs_calendar.rst — WR-16 exit-status corrections in the `Repeated \"lock held\" lines in the unattended log` troubleshooting entry and in `When nothing has appeared` item 4"
    - "docs/runbooks/telescope_runs_calendar.rst — WR-17 fallback prose in `Setting it up on a fresh host` step 1, in the `check_unattended` preflight paragraph, and in a new symptom-shaped troubleshooting entry"
  key_links:
    - "setup step 1's `FOMO_STATE_DIR` paragraph -> the new troubleshooting entry: an operator arriving from either direction learns the fallback exists and what it means"
    - "the preflight paragraph's setup-time framing -> `check_state_dir()`'s own WR-17 docstring caveat in `solsys_code/management/commands/check_unattended.py`, so doc and code state the same division of labour"
    - "the \"lock held\" entry's exit-status statement -> `cron_line()`'s `rc=0` skip tail and the same tail in `deploy/cron/fomo.crontab.example`: one contract, documented in one place and implemented in two"
---

<objective>
Close a CLAUDE.md paired-docs breach: two behavior changes landed today on `issue37-telescope-runs-calendar` as Phase 36 code-review fixes, and neither reached the runbook section that is their designated paired doc.

- **WR-17** (commit `72115bb`, `solsys_code/unattended.py`): `save_state()` now falls back to a state file outside `FOMO_STATE_DIR` instead of raising when the primary location cannot be written, and `load_state()` reads whichever file is newest. The runbook still describes the primary location as the only one.
- **WR-16** (commit `eafda16`, `check_unattended.cron_line()` + `deploy/cron/fomo.crontab.example`): the generated cron line now normalizes a lock-held skip back to exit 0 after logging the skip line. The runbook's troubleshooting entry still describes a skip as the line's non-zero status.

Purpose: `docs/runbooks/telescope_runs_calendar.rst`'s "How do I run everything unattended?" section is the *only* paired doc for `solsys_code/unattended.py`, `notifications.py`, `run_unattended.py` and `check_unattended.py` — CLAUDE.md names it explicitly in place of a demo notebook for these modules, because a runner demo could only execute with the portal, the mail backend and the heartbeat all mocked at once. So there is no notebook to update here, and no notebook should be created: this runbook section is the deliverable.

Output: prose-only edits to one file. No code, test, notebook or crontab-template change — the code side of both fixes already landed and is not in scope.
</objective>

<execution_context>
@/home/tlister/git/fomo_devel/.claude/gsd-core/workflows/execute-plan.md
@/home/tlister/git/fomo_devel/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md

Primary target file: `docs/runbooks/telescope_runs_calendar.rst` (2201 lines at planning time). The unattended section starts at the `.. _unattended-operation:` label (~line 1415); the Troubleshooting section starts at ~line 1884. Read only the ranges each task names — this is a large file and a whole-file read is not needed.

The two behavior changes are the authority for what the prose must say. Read them with:

- `git show 72115bb` — WR-17. Read `solsys_code/unattended.py`'s `_FALLBACK_STATE_PATH`, `_primary_state_path()`, `_newest_existing_state_path()`, `save_state()` and `load_state()`, plus the `check_state_dir()` docstring change in `solsys_code/management/commands/check_unattended.py`.
- `git show eafda16` — WR-16. Read `cron_line()`'s returned line and its docstring, and the same tail in `deploy/cron/fomo.crontab.example`.

Do not document from this plan's summaries alone; the commits are the source of truth for exact file names, defaults and ordering.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Correct the two places the runbook describes a lock-held skip's exit status (WR-16)</name>
  <files>docs/runbooks/telescope_runs_calendar.rst</files>
  <precondition>Both behavior changes are already in this branch's history: `git merge-base --is-ancestor eafda16 HEAD && git merge-base --is-ancestor 72115bb HEAD` succeeds. If either is not an ancestor, stop — this plan documents behavior that must already exist in the tree it is documenting.</precondition>
  <read_first>
    - `git show eafda16` — the whole commit: `cron_line()`'s returned line, its docstring, and `deploy/cron/fomo.crontab.example`'s WR-16 comment block.
    - `docs/runbooks/telescope_runs_calendar.rst` lines 2100-2145 (the `Repeated "lock held" lines in the unattended log` troubleshooting entry).
    - `docs/runbooks/telescope_runs_calendar.rst` lines 1660-1700 (the `When nothing has appeared` checklist and the locking paragraph that follows it).
  </read_first>
  <action>
Fix two stale claims about what a lock-held skip does to the *cron line's own* exit status. Anchor on the quoted sentences, not on line numbers — they shift as you edit.

**Spot A — the `Repeated "lock held" lines in the unattended log` troubleshooting entry.** Anchor: its **Cause:** paragraph, which currently reads that ``flock -n -E 99`` fails immediately rather than queuing, so every tick finding the cron guard's lock already held "exits 99 and the crontab line's tail writes a skip line instead of running". Keep the statement that flock itself returns its dedicated code 99 — per WR-16 that is still exactly how the skip is detected, and the ``-E`` support the preflight checks for is still required. What must change is the implication that the *line* reports 99. State, in this entry, that the tail sets ``rc=0`` once it has logged the skip line, normalizing the line's status back to exit 0, so the crontab line's own reported status is only ever ``run_unattended``'s — 0 healthy, 1 failing — and never a code ``run_unattended`` itself cannot produce. Give the operator the consequence: anything reading the line's exit status (cron's own syslog line, a systemd ``OnFailure=`` hook, a monitoring wrapper) no longer records a routine tick overlap as a failure, because ``run_tick()``'s contract is that contention is not a failure and the heartbeat (D-12) is the structural backstop for a lock that stays contended. Cite it as ``(WR-16, 36-REVIEW.md)`` — the same citation style this entry's **Fix:** paragraph already uses for ``(WR-14, 36-REVIEW.md)``. Leave the entry's existing true statements alone: the skip line's literal text, the gating on flock's code 99 meaning a failed tick is never mislabeled as "lock held", the one-occurrence-is-normal rule, and the whole **Fix:** paragraph about the kernel always releasing a flock.

**Spot B — `When nothing has appeared`, checklist item 4.** Anchor: the clause stating that a permanently contended cron lock "leaves this line on every tick instead of looking like a healthy no-op". Since WR-16 that clause misleads in the opposite direction: to anything reading the exit status a lock-held tick now *does* look like a healthy one (status 0), and the log line is the only place the skip is visible at all. Reword the item so it says that directly — which is the reason this checklist item exists in the first place: the operator has to read the log, because the status will not tell them. Keep the rest of item 4 unchanged: the literal skip line, the ``run_unattended: lock held -- skipping this tick`` versus ``run_unattended skipped:`` disambiguation, the one-occurrence-is-normal sentence, and the cross-reference down to the troubleshooting entry.

**Gate-sensitive wording.** The new prose must contain the literal ``rc=0``, the literal phrase "back to exit 0" (as in "normalized back to exit 0"), and the citation ``WR-16, 36-REVIEW.md``. The verify gate flattens newlines before matching, so ordinary RST wrapping is fine and you do not need to keep a phrase on one unwrapped line.

**Out of scope for this task:** do not touch ``cron_line()``, ``deploy/cron/fomo.crontab.example`` or any test — the code side of WR-16 already landed. Do not renumber, retitle or re-underline any heading. Do not add a paired notebook (see the objective).
  </action>
  <verify>
    <automated>cd /home/tlister/git/fomo_devel && RB=docs/runbooks/telescope_runs_calendar.rst && FLAT=$(tr '\n' ' ' < "$RB" | tr -s ' ') && for p in 'rc=0' 'back to exit 0' 'WR-16, 36-REVIEW.md' '-E 99'; do printf '%s' "$FLAT" | grep -qF -- "$p" || { echo "MISSING LITERAL: $p"; exit 1; }; done && test -z "$(git status --porcelain -uno -- . ':!docs/runbooks/telescope_runs_calendar.rst' ':!.planning' ':!.gsd')" && git add "$RB" && pre-commit run sphinx-build --all-files</automated>
  </verify>
  <done>Both spots read correctly against `git show eafda16`: flock's own code 99 is still named as the skip-detection mechanism, the cron line's own status is stated as 0 on a skip (0 healthy / 1 failing only), the supervisor consequence is spelled out, item 4 tells the operator the log line is the only signal, all four gate literals are present, the Sphinx build is clean, and the runbook is the only non-planning file in the diff.</done>
</task>

<task type="auto">
  <name>Task 2: Document the suppression-state fallback file and its operator handling (WR-17)</name>
  <files>docs/runbooks/telescope_runs_calendar.rst</files>
  <read_first>
    - `git show 72115bb` — the whole commit. Specifically `solsys_code/unattended.py`'s `_FALLBACK_STATE_PATH` comment block, `_primary_state_path()`, `_newest_existing_state_path()`, the rewritten `save_state()` (including the exact error-log wording and the fallback-deletion step) and `load_state()`'s new docstring paragraph; plus the `check_state_dir()` docstring change in `solsys_code/management/commands/check_unattended.py`.
    - `docs/runbooks/telescope_runs_calendar.rst` lines 1448-1465 (`Setting it up on a fresh host` step 1, including the `FOMO_STATE_DIR` paragraph).
    - `docs/runbooks/telescope_runs_calendar.rst` lines 1541-1580 (the `check_unattended` preflight paragraph).
    - `docs/runbooks/telescope_runs_calendar.rst` lines 2140-2160 (the `A failure email arrived once, then went quiet while the problem continued` entry and the entry that follows it — the insertion point for the new entry).
  </read_first>
  <action>
Document the WR-17 runtime fallback in three places. Anchor on quoted text, not line numbers.

**Spot A — `Setting it up on a fresh host`, step 1.** Anchor: the paragraph stating that ``FOMO_STATE_DIR`` (the D-11 suppression-state file's directory) defaults to ``FOMO_LOCK_DIR``, so no third directory is needed unless the host points it elsewhere. Keep that as-is and add the runtime behavior after it: if the primary state file cannot be written at tick time — the directory filled up or lost write permission for the cron account *after* setup — the tick logs an error naming both paths and writes the suppression state to ``fomo-unattended-state.fallback.json`` in the system temp directory instead of failing. Say why the fallback is deliberately outside ``FOMO_STATE_DIR``: that directory is precisely what may have gone bad, and ``run_unattended`` is a fresh process per cron tick with nothing in memory surviving between ticks, so only a file somewhere still writable can carry the suppression decision forward. Say what the runner then reads: whichever of the two files was written most recently — so the fallback tick's decision is honored, rather than the next tick deciding "newly failing" all over again and mailing the identical failure notice every 15 minutes for as long as the outage lasts (D-11 failing open). Say that a later successful write to the primary location deletes the stale fallback, so the runner reverts to reading the primary alone with no operator action. Mention that both files are written atomically and with mode 0600, which is why a state file living in a shared temp directory is not a new exposure. Cite as ``(WR-17, 36-REVIEW.md)``.

**Spot B — the `check_unattended` preflight paragraph.** Anchor: the clause listing what it reports, "whether the lock, log and suppression-state directories exist and are writable". Add that the suppression-state directory check is a setup-time preflight only: it catches an unwritable directory before an operator ever installs the cron line, but it cannot see a directory that goes bad afterwards (a full ``/var/lock`` tmpfs being the realistic trigger). That case is handled at runtime by the fallback in step 1, not by this check. Keep the paragraph's existing WR-06 caveat about the directory checks only testing the uid that ran the command — the two caveats are separate and both still apply.

**Spot C — a new troubleshooting entry.** Insert it in the Troubleshooting section immediately after the `A failure email arrived once, then went quiet while the problem continued` entry (its **Fix:** paragraph ends with the note about a *different* step starting to fail) and before `The heartbeat alerts even though the log shows a healthy tick`. The two suppression-state entries then sit together. Follow the section's established shape exactly: a symptom-shaped title, a ``^^^^`` underline at least as long as the title, then a **Cause:** paragraph and a **Fix:** paragraph. Title it after the symptom an operator actually sees — a log line saying the suppression state could not be persisted — in the same voice as its neighbours.

  - **Cause:** ``FOMO_STATE_DIR`` (default ``FOMO_LOCK_DIR``) became unwritable or full after ``check_unattended`` passed, so the tick wrote its suppression state to ``fomo-unattended-state.fallback.json`` in the system temp directory instead. State plainly what is *not* broken: the tick itself is not failed by this, no mail was lost, and no duplicate mail was sent — the suppression decision persisted, just somewhere else. The presence of that file in the temp directory is itself the signal that the primary directory needs attention.
  - **Fix:** restore the primary directory — free space, or restore write permission and ownership for the account cron runs as — and re-run ``check_unattended`` as that account (not as root) to confirm. The next tick that writes the primary file successfully deletes the fallback by itself, so there is nothing to clean up by hand. Warn against the one wrong move: do not delete the fallback file while the primary location is still unwritable. It is the only record of the current suppression state, and removing it makes the very next tick decide "newly failing" again and re-mail the failure that is already being tracked.

**Gate-sensitive wording.** The new prose must contain the literal filename ``fomo-unattended-state.fallback.json`` and the citation ``WR-17, 36-REVIEW.md``. The verify gate flattens newlines before matching, so wrap normally.

**Out of scope for this task:** no change to ``solsys_code/unattended.py``, ``check_unattended.py`` or any test — the code side of WR-17 already landed. Do not add a paired notebook (see the objective). Do not invent a temp-directory path as a guaranteed literal: the code uses ``tempfile.gettempdir()``, so describe it as the system temp directory and, at most, give ``/tmp`` as the typical value rather than a promise.
  </action>
  <verify>
    <automated>cd /home/tlister/git/fomo_devel && RB=docs/runbooks/telescope_runs_calendar.rst && FLAT=$(tr '\n' ' ' < "$RB" | tr -s ' ') && for p in 'fomo-unattended-state.fallback.json' 'WR-17, 36-REVIEW.md' 'FOMO_STATE_DIR' 'rc=0' 'back to exit 0' 'WR-16, 36-REVIEW.md'; do printf '%s' "$FLAT" | grep -qF -- "$p" || { echo "MISSING LITERAL: $p"; exit 1; }; done && test "$(grep -cF 'fomo-unattended-state.fallback.json' "$RB")" -ge 2 && test -z "$(git status --porcelain -uno -- . ':!docs/runbooks/telescope_runs_calendar.rst' ':!.planning' ':!.gsd')" && git add "$RB" && pre-commit run sphinx-build --all-files</automated>
  </verify>
  <done>All three spots read correctly against `git show 72115bb`: step 1 names the fallback file, its location, when it is written, the newest-file read rule, the per-tick-process reason and the self-deleting cleanup; the preflight paragraph is framed as setup-time only with the runtime fallback named as the other half; a new **Cause:**/**Fix:** troubleshooting entry sits between the D-11 suppression entry and the heartbeat entry, with a correctly-underlined symptom-shaped title and the do-not-delete-the-fallback warning. Every gate literal is present, the fallback filename appears in at least two places (setup + troubleshooting), the Sphinx build is clean, and the runbook is the only non-planning file in the diff.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runbook prose → published HTML | Everything written here is rendered into public docs (ReadTheDocs, and any HTML served from a checkout), so a real credential or host detail pasted into the prose becomes public. |
| operator → host filesystem | The prose instructs an operator to act on real directories and files under `/var` and the system temp directory; wrong instructions cause an operator-inflicted outage or a loosened permission. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-bn7-01 | Information disclosure | new prose in `docs/runbooks/telescope_runs_calendar.rst` | low | mitigate | Name only setting names, default paths and file names. Never paste a real `FOMO_HEARTBEAT_URL` (D-15: the ping token makes it a credential), an LCO/SOAR API key, a real host name, or log output copied from a live host. Both tasks' example text is synthetic or already-committed literals. |
| T-bn7-02 | Information disclosure | Task 2's description of a state file in the shared system temp directory | low | mitigate | Task 2 requires stating that both state files are written atomically with mode 0600, so an operator reading "it went to the temp directory" does not conclude the mode is loose or that the file should be hand-widened to be readable. |
| T-bn7-03 | Denial of service | Task 2's troubleshooting **Fix:** paragraph | low | mitigate | The **Fix:** must warn against deleting the fallback while the primary is still unwritable — the one operator action that would re-trigger the exact failure-mail loop WR-17 exists to stop. Documented as a prohibition, not left implicit. |
| T-bn7-04 | Tampering | npm/pip/cargo installs | low | accept | No task installs a package; this plan modifies one `.rst` file. The package-legitimacy gate has nothing to gate, so no `[ASSUMED]`/`[SUS]` checkpoint applies. |
</threat_model>

<verification>
Run from the repo root, with the venv active (`pre-commit` and `sphinx-build` resolve to `/home/tlister/venv/devel_fomo311_venv/bin/`):

1. **Sphinx gate (the required one).** `git add docs/runbooks/telescope_runs_calendar.rst && pre-commit run sphinx-build --all-files`. Staging first is load-bearing: `pre-commit run` stashes unstaged changes, so an unstaged edit would be built out and the hook would pass against the *old* file. Any new `WARNING: Title underline too short` or RST error in this file is a failure even though the hook itself does not run with `-W`.
2. **Literal gates.** The compound command in each task's `<automated>` block. It flattens newlines before matching, so RST rewrapping cannot break it.
3. **Diff discipline.** `git status --porcelain -uno` shows only `docs/runbooks/telescope_runs_calendar.rst` outside `.planning/`/`.gsd/`. No code, test, notebook or `deploy/cron/` change. `-uno` is deliberate: the repo root carries a pre-existing untracked `reqgroup_2682493.json` (verified present at planning time) that is not this task's doing, and the executor's own new `.planning/` files start untracked too — the gate is about *tracked* files being modified out of scope.
4. **Human check** (`workflow.human_verify_mode` is `end-of-phase`, so this is a read-through, not a blocking checkpoint): read the two edited troubleshooting entries and setup step 1 end-to-end and confirm an operator who has never seen either commit could act on them — in particular that the "lock held" entry no longer reads as though a skip is a failure, and that the fallback entry makes the do-not-delete rule unmissable.
</verification>

<success_criteria>
- Every `must_haves.truths` item holds.
- `pre-commit run sphinx-build --all-files` is clean, with the runbook staged.
- `docs/runbooks/telescope_runs_calendar.rst` is the only non-planning file in the diff.
- The CLAUDE.md paired-docs obligation for `solsys_code/unattended.py` + `check_unattended.py` is satisfied by this runbook section — with no notebook added, which CLAUDE.md explicitly does not ask for for these modules.
- Commits are atomic (one per task) and end with the required trailers, verbatim:

  `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`
  `Claude-Session: https://claude.ai/code/session_01FKGGfhzTfpfxBy8EFCUtt9`

  Suggested subjects: `docs(36): WR-16 document the lock-held exit-code normalization in the runbook` and `docs(36): WR-17 document the suppression-state fallback in the runbook`.
- Any `gsd-tools.cjs` invocation is preceded by `source ~/.nvm/nvm.sh; nvm use 22` (the shim requires Node >= 22; the default `node` on this host is too old).
</success_criteria>

<output>
Create `.planning/quick/260918-bn7-document-the-wr-17-suppression-state-fal/260918-bn7-SUMMARY.md` when done.
</output>
