---
phase: 08-telescope-label-verification-sidecar
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - solsys_code/models.py
  - solsys_code/migrations/0001_calendareventtelescopelabel.py
  - solsys_code/management/commands/sync_lco_observation_calendar.py
  - solsys_code/tests/test_sync_lco_observation_calendar.py
  - solsys_code/tests/test_load_telescope_runs.py
  - docs/notebooks/pre_executed/sync_lco_observation_calendar_demo.ipynb
autonomous: true
requirements: [DISPLAY-01]
must_haves:
  truths:
    - "After `sync_lco_observation_calendar` runs, every synced CalendarEvent has a CalendarEventTelescopeLabel row whose is_verified equals (not telescope_api_failed) for that record."
    - "Events created by load_telescope_runs have no CalendarEventTelescopeLabel row."
    - "Re-running sync on an unchanged record does not create a duplicate sidecar row and does not churn CalendarEvent.modified."
  artifacts:
    - "solsys_code/models.py contains class CalendarEventTelescopeLabel"
    - "solsys_code/migrations/0001_*.py exists (generated, first migration for the app)"
    - "sync_lco_observation_calendar.py calls CalendarEventTelescopeLabel.objects.update_or_create after the existing get_or_create"
    - "docs/notebooks/pre_executed/sync_lco_observation_calendar_demo.ipynb has an executed cell demonstrating the sidecar write"
  key_links:
    - "telescope_api_failed (popped at sync line 604) -> is_verified=not telescope_api_failed on the sidecar row"
    - "OneToOneField related_name='telescope_label_meta' -> read by calendar.html in Plan 02"
---

<objective>
Add the `CalendarEventTelescopeLabel` sidecar model and persist the live-verified-vs-fallback
telescope-label outcome from `sync_lco_observation_calendar.py`, covering DISPLAY-01.

Purpose: Turn the existing transient `telescope_api_failed` signal into a queryable, per-event
structured fact that the calendar template (Plan 02) can read to render a visual cue and tooltip,
without parsing title text.
Output: First real model + first migration for the `solsys_code` app; one new `update_or_create`
write in the sync command; sidecar-row test coverage; a regenerated demo notebook cell.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-telescope-label-verification-sidecar/08-CONTEXT.md
@.planning/phases/08-telescope-label-verification-sidecar/08-RESEARCH.md
@.planning/phases/08-telescope-label-verification-sidecar/08-PATTERNS.md
@.planning/phases/08-telescope-label-verification-sidecar/08-VALIDATION.md
@CLAUDE.md
</context>

## Artifacts this phase produces (Plan 01)

- **New model:** `solsys_code.models.CalendarEventTelescopeLabel`
  - field `event`: `OneToOneField(tom_calendar.models.CalendarEvent, on_delete=CASCADE, primary_key=True, related_name='telescope_label_meta', verbose_name='Calendar event')`
  - field `is_verified`: `BooleanField(default=True, verbose_name='Whether the telescope label was live-verified against the LCO API')`
  - `__str__` returning `'Verified'`/`'Fallback'` label text
  - reverse accessor on `CalendarEvent`: `event.telescope_label_meta` (consumed by Plan 02)
- **New migration:** `solsys_code/migrations/0001_calendareventtelescopelabel.py` (generated, not hand-written)
- **New import in sync command:** `from solsys_code.models import CalendarEventTelescopeLabel`
- **New write statement** in `sync_lco_observation_calendar.py` `handle()`: `CalendarEventTelescopeLabel.objects.update_or_create(event=event, defaults={'is_verified': not telescope_api_failed})`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add CalendarEventTelescopeLabel model and generate the first migration</name>
  <files>solsys_code/models.py, solsys_code/migrations/0001_calendareventtelescopelabel.py</files>
  <read_first>
    - solsys_code/models.py (currently all-comment; this is the app's first real model)
    - solsys_code/solsys_code_observatory/models.py (the `verbose_name`-on-every-field convention to mirror — Observatory model, fields around lines 27-40)
    - .planning/phases/08-telescope-label-verification-sidecar/08-RESEARCH.md ("Sidecar model definition" code example, "Pattern 1", Pitfall guidance)
    - .planning/phases/08-telescope-label-verification-sidecar/08-PATTERNS.md ("solsys_code/models.py — add CalendarEventTelescopeLabel" section)
  </read_first>
  <action>
    Replace the all-comment body of `solsys_code/models.py` with a real model module per D-01. Import
    `from django.db import models` and `from tom_calendar.models import CalendarEvent`. Define class
    `CalendarEventTelescopeLabel(models.Model)` with a Google-style docstring stating it is a sidecar
    recording whether a CalendarEvent's telescope label was live-verified against the LCO API or
    fallback-guessed (TELESCOPE-03/04), one row per CalendarEvent at most, and that a missing row means
    "verified" by documented default (e.g. classically-scheduled events from load_telescope_runs, which
    never go through telescope-label resolution).

    Two fields, each carrying `verbose_name` (mirroring the Observatory model convention):
    - `event = models.OneToOneField(CalendarEvent, on_delete=models.CASCADE, primary_key=True, related_name='telescope_label_meta', verbose_name='Calendar event')` — the `primary_key=True` makes the sidecar share the parent's pk and avoids a separate id column; `on_delete=models.CASCADE` removes the sidecar row automatically if its CalendarEvent is deleted (no orphan rows).
    - `is_verified = models.BooleanField(default=True, verbose_name='Whether the telescope label was live-verified against the LCO API')` — boolean-only per D-01 (no separate reason/detail field; both API-timeout and unmapped-code funnel into one signal, matching the existing telescope_api_failed contract).

    Add a `__str__` returning the string `Verified` when `self.is_verified` else `Fallback`, followed
    by ` label for ` and `self.event.title`.

    Do NOT create a new Django app and do NOT add a `templatetags/` module in this task — `solsys_code`
    already has `models.py` and `migrations/`.

    Then generate the migration by running `./manage.py makemigrations solsys_code`. Do not hand-write
    or hand-edit the migration file — Django's output for a single `OneToOneField(primary_key=True)` +
    `BooleanField` model is deterministic. Commit the generated file as-is. Confirm `./manage.py migrate`
    runs clean against the local SQLite DB.

    Keep `ruff check .` and `ruff format --check .` clean — single quotes, 120-col, Google-style docstring.
  </action>
  <verify>
    <automated>./manage.py makemigrations solsys_code --check --dry-run; ./manage.py migrate solsys_code; python -c "from solsys_code.models import CalendarEventTelescopeLabel as C; f={x.name:x for x in C._meta.get_fields()}; assert 'is_verified' in f, 'missing is_verified'; assert C._meta.pk.name=='event', C._meta.pk.name; print('model OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `solsys_code/models.py` defines `class CalendarEventTelescopeLabel(models.Model)` with fields `event` (OneToOneField, primary_key=True, related_name='telescope_label_meta', on_delete CASCADE) and `is_verified` (BooleanField, default=True), both with `verbose_name`.
    - `solsys_code/migrations/0001_calendareventtelescopelabel.py` exists and was produced by `makemigrations` (not hand-written); `./manage.py makemigrations solsys_code --check --dry-run` reports no further changes needed.
    - `./manage.py migrate solsys_code` applies cleanly.
    - `ruff check .` and `ruff format --check .` are clean.
  </acceptance_criteria>
  <done>The model and its first migration exist, migrate cleanly, and the reverse accessor `telescope_label_meta` resolves on CalendarEvent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write the sidecar row from sync_lco_observation_calendar.py and cover it with tests</name>
  <files>solsys_code/management/commands/sync_lco_observation_calendar.py, solsys_code/tests/test_sync_lco_observation_calendar.py, solsys_code/tests/test_load_telescope_runs.py</files>
  <read_first>
    - solsys_code/management/commands/sync_lco_observation_calendar.py (imports around lines 1-14; the write block at lines 603-615 where `url` and `telescope_api_failed` are popped and `get_or_create` runs; the per-record loop `for record in records:` at line 567)
    - solsys_code/tests/test_sync_lco_observation_calendar.py (imports lines 1-19; `test_sync_04_rerun_updates_in_place_no_churn_on_unchanged` around line 320; the `_observations_block_response` helper and `call_command` + `CalendarEvent.objects.get()` assertion idiom around lines 308-318)
    - solsys_code/tests/test_load_telescope_runs.py (to add a no-sidecar-row assertion using the same call_command idiom; use NonSiderealTargetFactory per CLAUDE.md if a Target fixture is needed)
    - .planning/phases/08-telescope-label-verification-sidecar/08-RESEARCH.md ("Pattern 1", Pitfalls 1, 2, 3)
    - .planning/phases/08-telescope-label-verification-sidecar/08-PATTERNS.md (sync command section and test section)
  </read_first>
  <behavior>
    - After `sync_lco_observation_calendar` runs on a record whose telescope label was live-verified (telescope_api_failed False), `CalendarEventTelescopeLabel.objects.get(event=event).is_verified` is True.
    - After it runs on a record whose telescope label fell back (telescope_api_failed True), the sidecar row's `is_verified` is False.
    - Running `load_telescope_runs` to create an event leaves `CalendarEventTelescopeLabel.objects.count()` at zero (accessing `event.telescope_label_meta` raises `CalendarEventTelescopeLabel.DoesNotExist`).
    - Re-running sync twice on an unchanged record keeps `CalendarEventTelescopeLabel.objects.count()` at 1 (no duplicate) and does not change `CalendarEvent.modified` (existing no-churn contract intact).
  </behavior>
  <action>
    Add `from solsys_code.models import CalendarEventTelescopeLabel` to the import block at the top of
    `sync_lco_observation_calendar.py` (near the existing `from tom_calendar.models import CalendarEvent`
    at line 9).

    Immediately after the existing `if created: ... else: ...` block that follows
    `event, created = CalendarEvent.objects.get_or_create(url=url, defaults=fields)` (line 615), and
    still inside the `for record in records:` loop, add a standalone statement:
    `CalendarEventTelescopeLabel.objects.update_or_create(event=event, defaults={'is_verified': not telescope_api_failed})`.
    Precede it with a short comment noting (a) it is DISPLAY-01 / Phase 8, (b) it reconciles the sidecar
    to the current telescope_api_failed signal regardless of whether CalendarEvent's own fields changed,
    (c) it is deliberately a separate statement never folded into `fields` or the `changed` comparison
    (Pitfall 3), and (d) `is_verified` reflects the outcome of the most recent sync run that included this
    record, not real-time state (Pitfall 2 staleness contract).

    Do NOT add `is_verified` as a key inside the `fields` dict, do NOT include it in the
    `changed = any(...)` comparison, and do NOT `setattr(event, 'is_verified', ...)`. The
    `update_or_create()` call does its own no-churn compare internally. Do NOT touch
    `load_telescope_runs.py` source (confirmed: it has no telescope-label-resolution concept).

    In `solsys_code/tests/test_sync_lco_observation_calendar.py`: add `from solsys_code.models import CalendarEventTelescopeLabel`
    to the import block. Add a test that, after a `call_command('sync_lco_observation_calendar', ...)` run
    against a mocked `make_request` returning a verified (mappable site/telescope) block, asserts
    `CalendarEventTelescopeLabel.objects.get(event=CalendarEvent.objects.get()).is_verified is True`.
    Add a second test using a mocked response that forces the fallback path (a record with
    `scheduled_start` set and an unmappable/missing telescope so `label_was_fallback` is True, per the
    `telescope_api_failed` definition at line 470) and assert the sidecar row's `is_verified is False`.
    Add a no-churn test mirroring `test_sync_04_rerun_updates_in_place_no_churn_on_unchanged`: run the
    sync twice on the same unchanged record and assert `CalendarEventTelescopeLabel.objects.count() == 1`
    after the second run and that the row's `event_id` (its pk) is unchanged.

    In `solsys_code/tests/test_load_telescope_runs.py`: add a test that creates an event via
    `load_telescope_runs` (using the file's existing fixture/call_command idiom) and asserts
    `CalendarEventTelescopeLabel.objects.count() == 0` and that accessing `event.telescope_label_meta`
    raises `CalendarEventTelescopeLabel.DoesNotExist`.

    Keep `ruff check .` / `ruff format --check .` clean.
  </action>
  <verify>
    <automated>./manage.py test solsys_code.tests.test_sync_lco_observation_calendar solsys_code.tests.test_load_telescope_runs -v2</automated>
  </verify>
  <acceptance_criteria>
    - `sync_lco_observation_calendar.py` imports `CalendarEventTelescopeLabel` and calls `CalendarEventTelescopeLabel.objects.update_or_create(event=event, defaults={'is_verified': not telescope_api_failed})` as a standalone statement after the existing `get_or_create`/diff/`save()` block, inside the per-record loop.
    - `is_verified` does NOT appear as a key in the `fields` dict nor in the `changed = any(...)` comparison.
    - New sync tests assert verified→`is_verified True`, fallback→`is_verified False`, and re-run→`count()==1` (no duplicate, no churn).
    - New load_telescope_runs test asserts `CalendarEventTelescopeLabel.objects.count() == 0` for a classically-scheduled event and that the reverse accessor raises `DoesNotExist`.
    - `./manage.py test solsys_code.tests.test_sync_lco_observation_calendar solsys_code.tests.test_load_telescope_runs` passes; `ruff check .` / `ruff format --check .` clean.
  </acceptance_criteria>
  <done>The sync command persists the verified/fallback outcome per record, classical events get no row, re-runs do not duplicate or churn, and all four behaviors are test-covered and green.</done>
</task>

<task type="auto">
  <name>Task 3: Regenerate the sync demo notebook to demonstrate the sidecar write (CLAUDE.md convention)</name>
  <files>docs/notebooks/pre_executed/sync_lco_observation_calendar_demo.ipynb</files>
  <read_first>
    - docs/notebooks/pre_executed/sync_lco_observation_calendar_demo.ipynb (the prior committed version — match its existing setup/fixture style, then add the new cell)
    - CLAUDE.md ("Demo notebook companions are part of the deliverable" — regenerate with executed output, committed with output; this notebook is exempt from pre-commit's output-clearing)
    - solsys_code/management/commands/sync_lco_observation_calendar.py (the new sidecar write, to demonstrate it accurately)
  </read_first>
  <action>
    Add or update a cell in `sync_lco_observation_calendar_demo.ipynb` that demonstrates the new
    DISPLAY-01 behavior: after a sync run (using the notebook's existing mocked/fixture setup pattern),
    show that the created CalendarEvent now has an associated `CalendarEventTelescopeLabel` row, and
    print/display both a verified case (`is_verified=True`) and a fallback case (`is_verified=False`),
    and note that a classically-scheduled event has no sidecar row. Keep narration in plain English
    (CLAUDE.md plain-English convention — say "create the record if missing, otherwise update it in
    place", not "upsert").

    Regenerate executed output with:
    `jupyter nbconvert --to notebook --execute --inplace docs/notebooks/pre_executed/sync_lco_observation_calendar_demo.ipynb`
    and commit the notebook WITH its executed output (this `pre_executed/` copy is committed with output;
    pre-commit clears output elsewhere but not here).
  </action>
  <verify>
    <automated>jupyter nbconvert --to notebook --execute --inplace docs/notebooks/pre_executed/sync_lco_observation_calendar_demo.ipynb && python -c "import json,sys; nb=json.load(open('docs/notebooks/pre_executed/sync_lco_observation_calendar_demo.ipynb')); src=''.join(''.join(c.get('source',[])) for c in nb['cells']); assert 'CalendarEventTelescopeLabel' in src, 'notebook does not reference the sidecar model'; print('notebook OK')"</automated>
  </verify>
  <acceptance_criteria>
    - The notebook references `CalendarEventTelescopeLabel` and demonstrates both a verified (`is_verified=True`) and a fallback (`is_verified=False`) sidecar row, plus the no-row-for-classical case.
    - The notebook was re-executed via `jupyter nbconvert --to notebook --execute --inplace` and is committed with output cells present.
    - Narration uses plain English (no "upsert"); `ruff` is not applicable to the notebook but any embedded code follows single-quote/120-col style where practical.
  </acceptance_criteria>
  <done>The paired demo notebook is in sync with the new sidecar write, re-executed, and committed with output — closing the CLAUDE.md notebook-companion convention that bit Phases 5 and 6.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LCO API → sync command | External API response is parsed into `telescope_api_failed`; only a derived boolean (`not telescope_api_failed`) reaches the new field — no raw API string is persisted into `is_verified`. |
| sync command → DB (sidecar row) | Trusted, internal management-command write off the request path; no user-supplied input reaches `is_verified`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Tampering | `is_verified` BooleanField write in sync command | low | accept | `is_verified` is computed internally from `telescope_api_failed` (trusted), never from external/user input; a BooleanField cannot carry injected markup. No mitigation code needed. |
| T-08-02 | Repudiation | Orphaned sidecar row after CalendarEvent deletion | low | mitigate | `on_delete=models.CASCADE` on the OneToOneField deletes the sidecar row automatically when its parent CalendarEvent is deleted — no orphan rows, no manual cleanup. |
| T-08-03 | Information disclosure | Demo notebook executed output | low | accept | Notebook uses mocked/fixture data, not live credentials or real proposal data; no secrets are committed in the executed output. |
| T-08-SC | Tampering | npm/pip/cargo installs | n/a | accept | This plan installs no new packages (Django `OneToOneField`/`update_or_create` are core); no package-legitimacy checkpoint required. |
</threat_model>

<verification>
- `./manage.py makemigrations solsys_code --check --dry-run` reports no pending changes (migration complete).
- `./manage.py test solsys_code.tests.test_sync_lco_observation_calendar solsys_code.tests.test_load_telescope_runs` passes.
- `./manage.py test solsys_code` (full app suite) green at wave merge.
- `ruff check .` and `ruff format --check .` clean.
- Demo notebook references `CalendarEventTelescopeLabel` and is committed with executed output.
</verification>

<success_criteria>
- Every synced CalendarEvent has a CalendarEventTelescopeLabel row with `is_verified == (not telescope_api_failed)` (ROADMAP Phase 8 success criterion 1).
- load_telescope_runs events have no sidecar row (criterion 2, write-side half).
- Re-running sync on unchanged records creates no duplicate sidecar row and does not churn CalendarEvent.modified (criterion 5).
- DISPLAY-01 fully delivered (model, migration, sync write, tests, notebook).
</success_criteria>

<output>
Create `.planning/phases/08-telescope-label-verification-sidecar/08-01-SUMMARY.md` when done.
</output>
