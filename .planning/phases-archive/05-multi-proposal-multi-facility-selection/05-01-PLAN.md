---
phase: 05-multi-proposal-multi-facility-selection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/fomo/settings.py
  - solsys_code/tests/test_sync_lco_observation_calendar.py
  - solsys_code/management/commands/sync_lco_observation_calendar.py
autonomous: true
requirements: [SELECT-02, SELECT-03, SELECT-04, SELECT-05]

must_haves:
  truths:
    - "An operator running --proposal A,B,C syncs records whose proposal is exactly A, B, or C, and a record with proposal AB is NOT matched (no substring leakage) [SELECT-02]"
    - "An operator running --proposal ALL (or all/All) syncs every LCO and SOAR ObservationRecord regardless of proposal code [SELECT-03]"
    - "A single run produces CalendarEvents for both facility=LCO and facility=SOAR records together, without two invocations [SELECT-04]"
    - "A SOAR record is processed through a SOARFacility instance, never a reused LCOFacility instance, verified by a spy asserting which class method was called [SELECT-05]"
    - "Duplicate or empty --proposal segments (A,A,B or trailing comma A,B,) are deduped and dropped, not errored [D-03]"
    - "The end-of-run summary reports created/updated/unchanged/skipped counts broken down per facility (LCO and SOAR each visible) [D-08]"
  artifacts:
    - path: "solsys_code/management/commands/sync_lco_observation_calendar.py"
      provides: "_parse_proposal_arg helper, eager LCO/SOAR dispatch dict, conditional facility__in/proposal__in queryset filter, per-facility counters, per-facility summary line"
      contains: "_parse_proposal_arg"
    - path: "src/fomo/settings.py"
      provides: "FACILITIES['SOAR'] entry so SOARSettings('SOAR') resolves a real api_key key (D-04/D-05)"
      contains: "'SOAR'"
    - path: "solsys_code/tests/test_sync_lco_observation_calendar.py"
      provides: "_create_record(facility='LCO') signature extension + SELECT-02/03/04/05 tests"
      contains: "test_select_05_soar_record_uses_soar_facility_instance"
  key_links:
    - from: "solsys_code/management/commands/sync_lco_observation_calendar.py"
      to: "tom_observations.facilities.soar.SOARFacility"
      via: "import + dispatch dict keyed by facility"
      pattern: "facilities\\.get\\(record\\.facility\\)"
    - from: "solsys_code/management/commands/sync_lco_observation_calendar.py"
      to: "ObservationRecord queryset"
      via: "facility__in plus conditional parameters__proposal__in"
      pattern: "facility__in"
---

<objective>
Generalize the existing `sync_lco_observation_calendar` management command (Phase 4: single-proposal, LCO-only) into a multi-proposal, multi-facility (LCO + SOAR) sync. One invocation must cover any combination of proposals (or the whole LCO-family network via `ALL`) across both `ObservationRecord(facility='LCO')` and `ObservationRecord(facility='SOAR')` rows, with each record processed through the facility instance matching its own `facility` value — never a single shared instance.

Purpose: Fixes the SELECT-05 dispatch bug (one `LCOFacility()` reused for every record) and unblocks v1.3's full LCO-family sync by making selection/dispatch facility-aware. This is query/selection/dispatch scope ONLY — no instrument-extraction (Phase 6) or telescope-label (Phase 7) changes.

Output: Three in-place file edits (settings, test helper + tests, command) implementing D-01..D-08 and satisfying SELECT-02/03/04/05.
</objective>

<artifacts_this_phase_produces>
Symbols this phase creates (exclude these from drift/source-grounding verification — they are new, not pre-existing):

- `_parse_proposal_arg(raw: str) -> list[str] | None` — new module-level helper in `sync_lco_observation_calendar.py` (returns deduped code list, or `None` for the `ALL` sentinel).
- `FACILITIES['SOAR']` — new dict key in `src/fomo/settings.py` (sibling of the existing `FACILITIES['LCO']`).
- `facilities` — new local variable in `handle()`: the eager dispatch dict `{'LCO': LCOFacility(), 'SOAR': SOARFacility()}`.
- `counters` — new local variable in `handle()`: per-facility counter dict `{'LCO': {...}, 'SOAR': {...}}` replacing the flat `created_count`/`updated_count`/`unchanged_count`/`skipped_count` ints.
- New import: `from tom_observations.facilities.soar import SOARFacility` (in both the command file and the test file).
- New test-helper parameter: `facility: str = 'LCO'` on `TestSyncLcoObservationCalendar._create_record`.
- New test methods: `test_select_02_comma_list_matches_any_no_substring_leakage`, `test_select_03_all_token_case_insensitive_syncs_everything`, `test_select_04_single_run_covers_both_facilities`, `test_select_05_soar_record_uses_soar_facility_instance`.
</artifacts_this_phase_produces>

<execution_context>
@/home/tlister/git/fomo_devel/.claude/gsd-core/workflows/execute-plan.md
@/home/tlister/git/fomo_devel/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-multi-proposal-multi-facility-selection/05-CONTEXT.md
@.planning/phases/05-multi-proposal-multi-facility-selection/05-RESEARCH.md
@.planning/phases/05-multi-proposal-multi-facility-selection/05-PATTERNS.md
@.planning/phases/05-multi-proposal-multi-facility-selection/05-VALIDATION.md
@solsys_code/management/commands/sync_lco_observation_calendar.py
@solsys_code/tests/test_sync_lco_observation_calendar.py
@src/fomo/settings.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave-0 scaffold — FACILITIES['SOAR'] settings entry + _create_record facility param</name>
  <files>src/fomo/settings.py, solsys_code/tests/test_sync_lco_observation_calendar.py</files>
  <read_first>
    - src/fomo/settings.py (FACILITIES dict, lines 214-237 — mirror the 'LCO' entry shape)
    - solsys_code/tests/test_sync_lco_observation_calendar.py (_create_record lines 50-68 hardcoding facility='LCO'; _parameters lines 14-41 must NOT change)
    - .planning/phases/05-multi-proposal-multi-facility-selection/05-RESEARCH.md (Pitfall 1, 2, 4; Open Question 1)
    - .planning/phases/05-multi-proposal-multi-facility-selection/05-PATTERNS.md (settings.py + _create_record sections)
    - CLAUDE.md (ruff single-quote/120-col; NonSiderealTargetFactory convention)
  </read_first>
  <action>
Two independent scaffold edits, no command-logic change yet.

(1) src/fomo/settings.py: add a new 'SOAR' key to FACILITIES, directly after 'LCO' and before 'GEM'. Per D-04/D-05 and RESEARCH.md Open Question 1, use the NARROWER reading: mirror the 'LCO' entry's literal values exactly — portal_url set to 'https://observe.lco.global' and api_key set to the empty string '' (hardcoded literal, identical to today's 'LCO'). Do NOT introduce os.getenv('LCO_API_KEY', ...) and do NOT modify the existing 'LCO' entry (that would exceed this phase's query/selection/dispatch boundary — RESEARCH.md Pitfall 1 option b). Add a short comment noting the SOAR entry reuses the same LCO Observation Portal credentials per D-05 (SOARFacility authenticates against the same API). The entry exists for api_key key resolution (D-04), NOT portal_url, which already resolves by inherited LCOSettings default even without it (Pitfall 2) — do not frame verification as "URL changes after adding this."

(2) solsys_code/tests/test_sync_lco_observation_calendar.py: add a facility: str = 'LCO' keyword parameter to _create_record, between scheduled_end and **parameter_overrides, passed through to ObservationRecord.objects.create(facility=facility, ...) replacing the hardcoded facility='LCO' (RESEARCH.md Pitfall 4 — required signature change; existing call sites omitting facility still get 'LCO'). Do NOT touch _parameters (facility is an ObservationRecord field, not a parameters key). Add import `from tom_observations.facilities.soar import SOARFacility` directly below the existing LCOFacility import, for the SELECT-05 spy test in Task 3. Reuse the existing class-level cls.target (NonSiderealTargetFactory) — no new Target fixtures.
  </action>
  <verify>
    <automated>cd /home/tlister/git/fomo_devel && ./manage.py test solsys_code.tests.test_sync_lco_observation_calendar 2>&1 | tail -5 && ./manage.py shell -c "from django.conf import settings; assert 'SOAR' in settings.FACILITIES; assert settings.FACILITIES['SOAR']['api_key'] == ''; assert settings.FACILITIES['SOAR']['portal_url'] == 'https://observe.lco.global'; print('SOAR settings OK')" && ruff check src/fomo/settings.py solsys_code/tests/test_sync_lco_observation_calendar.py && ruff format --check src/fomo/settings.py solsys_code/tests/test_sync_lco_observation_calendar.py</automated>
  </verify>
  <acceptance_criteria>
    - settings.FACILITIES['SOAR'] is a dict with 'portal_url'=='https://observe.lco.global' and 'api_key'==''.
    - settings.FACILITIES['LCO'] is byte-for-byte unchanged (no env-var migration).
    - _create_record signature includes facility: str = 'LCO' and create() uses facility=facility.
    - The full existing test module still passes (helper change is backward compatible).
    - ruff check and ruff format --check report no issues on both edited files.
  </acceptance_criteria>
  <done>FACILITIES['SOAR'] resolves a real api_key key; _create_record accepts facility=; SOARFacility imported in tests; existing suite green; lint clean.</done>
</task>

<task type="auto">
  <name>Task 2: Generalize the command — proposal parsing, multi-facility queryset, dispatch dict, per-facility summary</name>
  <files>solsys_code/management/commands/sync_lco_observation_calendar.py</files>
  <read_first>
    - solsys_code/management/commands/sync_lco_observation_calendar.py (full file — imports line 7, add_arguments lines 166-173, handle lines 175-225, the helpers _failure_prefix/_title_for/_build_event_fields which take a facility arg and need NO signature change)
    - .planning/phases/05-multi-proposal-multi-facility-selection/05-PATTERNS.md (the command-file section: imports, add_arguments, dispatch, loop, summary, and _parse_proposal_arg helper patterns)
    - .planning/phases/05-multi-proposal-multi-facility-selection/05-RESEARCH.md (Patterns 1/2/3; Anti-Patterns; Don't Hand-Roll)
    - .planning/phases/05-multi-proposal-multi-facility-selection/05-CONTEXT.md (D-01 through D-08)
  </read_first>
  <action>
Generalize handle() and add one helper. Helpers _failure_prefix/_title_for/_build_event_fields keep their current signatures (SOARFacility is-a LCOFacility; methods inherited unchanged) — do NOT change them.

(a) Import (below the existing LCOFacility import, line 7): `from tom_observations.facilities.soar import SOARFacility`.

(b) New module-level helper `_parse_proposal_arg(raw: str) -> list[str] | None`, placed alongside the other module-level helpers above class Command, Google-style docstring (Args/Returns) matching _derive_telescope. Behavior per D-01/D-02/D-03: if raw.strip().lower() == 'all' return None (ALL sentinel, case-insensitive — D-02). Otherwise split on ',', strip each segment, drop empty strings, dedupe preserving first-seen order (a dict-as-ordered-set is fine), preserve code casing exactly as typed (D-01: codes are case-SENSITIVE — never .upper()/.lower() the codes), return the list. D-03: 'A,A,B' -> ['A','B']; 'A,B,' -> ['A','B'].

(c) add_arguments (lines 166-173): keep parser.add_argument('--proposal', type=str, required=True, ...); only update the help= text to mention the comma-separated list and the ALL token. Parsing stays in handle(), not add_arguments (matches the file's existing separation).

(d) handle() (lines 186-216): replace the single `facility = LCOFacility()` with an eager dispatch dict `facilities = {'LCO': LCOFacility(), 'SOAR': SOARFacility()}` built once before the loop, both keys unconditionally (D-06 — do NOT build lazily/conditionally). Replace the four flat counters with a per-facility counters dict eagerly initialized for both keys, e.g. counters = {'LCO': {'created':0,'updated':0,'unchanged':0,'skipped':0}, 'SOAR': {...}} (D-08). Replace the queryset: `records = ObservationRecord.objects.filter(facility__in=['LCO', 'SOAR'])`, then `codes = _parse_proposal_arg(options['proposal'])` and `if codes is not None: records = records.filter(parameters__proposal__in=codes)` (omit the proposal clause entirely for ALL — SELECT-03; codes list uses __in for substring-safe matching — SELECT-02). In the loop, look up `facility = facilities.get(record.facility)`; if None (D-07 defensive — unexpected facility value), self.stderr.write a skip message containing record.observation_id and record.facility, increment that facility's skipped counter (a get-or-default bucket is fine for the unexpected-key case), and continue. Otherwise keep the existing try/_build_event_fields/(KeyError,ValueError) catch and the no-churn get_or_create + conditional save body BYTE-FOR-BYTE, only changing the counter targets from flat ints to counters[record.facility]['created'|'updated'|'unchanged'|'skipped'].

(e) Summary (lines 218-224): replace the single aggregate line with a per-facility breakdown line (D-08) showing each facility's created/updated/unchanged/skipped, e.g. 'LCO: 3 created, 1 updated, 0 unchanged, 0 skipped | SOAR: 2 created, ...'. Exact layout is discretion but every facility's four counts must each be individually visible. Keep self.stdout.write / self.stderr.write — this file uses NO logger.* calls; do NOT introduce logging.getLogger.

SECURITY (D-07 skip path, D-08 summary): log ONLY record.observation_id, record.facility, and the exception message. NEVER log facility settings, api_key, or any raw settings dict (RESEARCH.md Security Domain — credential-leakage Information Disclosure).
  </action>
  <verify>
    <automated>cd /home/tlister/git/fomo_devel && ./manage.py test solsys_code.tests.test_sync_lco_observation_calendar 2>&1 | tail -5 && grep -q "_parse_proposal_arg" solsys_code/management/commands/sync_lco_observation_calendar.py && grep -q "from tom_observations.facilities.soar import SOARFacility" solsys_code/management/commands/sync_lco_observation_calendar.py && grep -q "facility__in" solsys_code/management/commands/sync_lco_observation_calendar.py && grep -v '^#' solsys_code/management/commands/sync_lco_observation_calendar.py | grep -c "api_key" | grep -qx 0 && ruff check solsys_code/management/commands/sync_lco_observation_calendar.py && ruff format --check solsys_code/management/commands/sync_lco_observation_calendar.py</automated>
  </verify>
  <acceptance_criteria>
    - _parse_proposal_arg('all'/'All'/'ALL') returns None; _parse_proposal_arg('A,A,B') == ['A','B']; _parse_proposal_arg('A,B,') == ['A','B']; casing preserved (no upper/lower on codes).
    - handle() builds facilities = {'LCO': LCOFacility(), 'SOAR': SOARFacility()} eagerly before the loop and looks up by record.facility.
    - Queryset uses facility__in=['LCO','SOAR']; the parameters__proposal__in clause is present only when codes is not None.
    - Per-record loop dispatches via facilities.get(record.facility); unknown facility -> stderr skip + counter increment + continue (no abort).
    - Summary line shows created/updated/unchanged/skipped per facility (D-08).
    - No api_key / settings / credential value is written to stdout or stderr anywhere (grep for 'api_key' in non-comment lines returns 0).
    - The existing Phase-4 test suite still passes unchanged; ruff check + format --check clean.
  </acceptance_criteria>
  <done>Command accepts comma-list/ALL --proposal across LCO+SOAR, dispatches per record.facility, reports per-facility counts, leaks no credentials; existing tests stay green.</done>
</task>

<task type="auto">
  <name>Task 3: Add SELECT-02/03/04/05 tests (with discriminating spy for SELECT-05)</name>
  <files>solsys_code/tests/test_sync_lco_observation_calendar.py</files>
  <read_first>
    - solsys_code/tests/test_sync_lco_observation_calendar.py (the now-extended _create_record with facility=; existing test_select_01 lines 70-81 as the call_command/assert template; the new SOARFacility import from Task 1)
    - .planning/phases/05-multi-proposal-multi-facility-selection/05-PATTERNS.md (test-method patterns, especially the SELECT-05 spy/patch example)
    - .planning/phases/05-multi-proposal-multi-facility-selection/05-RESEARCH.md (Pitfall 3 — black-box url assertion cannot discriminate SELECT-05; Phase Requirements -> Test Map for exact method names)
    - .planning/phases/05-multi-proposal-multi-facility-selection/05-VALIDATION.md (Per-Task Verification Map — exact test method names)
  </read_first>
  <action>
Add four test methods to TestSyncLcoObservationCalendar, using call_command(..., stdout=io.StringIO(), stderr=io.StringIO()) and asserting on CalendarEvent rows, mirroring test_select_01's shape. Use the exact method names from VALIDATION.md.

test_select_02_comma_list_matches_any_no_substring_leakage: create records with proposals 'A', 'B', 'C' (each creates an event) PLUS a decoy proposal='AB' record. Run --proposal A,B,C. Assert events exist for the A/B/C records and NO event exists for the 'AB' record (assert by its observation_id / its get_observation_url url) — confirms substring-safe __in matching (SELECT-02).

test_select_03_all_token_case_insensitive_syncs_everything: create several records across DIFFERENT proposal codes (and include at least one facility='SOAR' record). Run --proposal all (lowercase, to prove case-insensitivity per D-02). Assert every fixture record produced an event regardless of proposal (SELECT-03).

test_select_04_single_run_covers_both_facilities: create one facility='LCO' record and one facility='SOAR' record sharing the same proposal. Single run --proposal <code>. Assert CalendarEvents exist for BOTH (e.g. via each record's get_observation_url) in the one invocation (SELECT-04). First assert ObservationRecord.objects.get(observation_id=...).facility == 'SOAR' for the SOAR fixture (Pitfall 4 guard).

test_select_05_soar_record_uses_soar_facility_instance: create a facility='SOAR' record. Patch/spy on the SOARFacility and LCOFacility get_observation_url methods as imported in the command module (e.g. patch 'solsys_code.management.commands.sync_lco_observation_calendar.SOARFacility.get_observation_url' wrapping the real method, and the LCOFacility one likewise). Run the command. Assert the SOAR spy was called (assert_called_once or assert_called) AND the LCO spy was NOT called for that SOAR record — a DISCRIMINATING assertion, not a black-box event.url check (RESEARCH.md Pitfall 3: SOAR and LCO get_observation_url return identical strings, so url alone cannot prove dispatch). This proves SELECT-05.

Reuse the existing cls.target; create SOAR records via the Task-1 _create_record(facility='SOAR', ...) parameter. Keep Google-style/test conventions; ruff-clean.
  </action>
  <verify>
    <automated>cd /home/tlister/git/fomo_devel && ./manage.py test solsys_code.tests.test_sync_lco_observation_calendar.TestSyncLcoObservationCalendar.test_select_02_comma_list_matches_any_no_substring_leakage solsys_code.tests.test_sync_lco_observation_calendar.TestSyncLcoObservationCalendar.test_select_03_all_token_case_insensitive_syncs_everything solsys_code.tests.test_sync_lco_observation_calendar.TestSyncLcoObservationCalendar.test_select_04_single_run_covers_both_facilities solsys_code.tests.test_sync_lco_observation_calendar.TestSyncLcoObservationCalendar.test_select_05_soar_record_uses_soar_facility_instance 2>&1 | tail -8 && ./manage.py test solsys_code 2>&1 | tail -5 && ruff check solsys_code/tests/test_sync_lco_observation_calendar.py && ruff format --check solsys_code/tests/test_sync_lco_observation_calendar.py</automated>
  </verify>
  <acceptance_criteria>
    - All four named tests exist and pass.
    - test_select_02 asserts the proposal='AB' decoy produced NO CalendarEvent (substring-safe).
    - test_select_03 invokes --proposal with a lowercase 'all' and asserts every fixture record (including a SOAR one) got an event.
    - test_select_04 asserts events for both an LCO and a SOAR record in one run, and asserts the SOAR fixture's .facility == 'SOAR'.
    - test_select_05 uses a spy/patch that discriminates SOARFacility vs LCOFacility method calls (not a black-box url equality), asserting the SOAR instance's method was used and the LCO instance's was not.
    - Full ./manage.py test solsys_code suite green; ruff check + format --check clean.
  </acceptance_criteria>
  <done>SELECT-02/03/04/05 each have a passing automated test; SELECT-05's test discriminates the dispatched class; full Django app suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator CLI -> ORM | The `--proposal` free-text argument crosses into an ORM `JSONField` filter (`parameters__proposal__in`). Operator-invoked, but still untrusted free text. |
| settings/credentials -> logs | `api_key` lives in `FACILITIES`; the D-07 skip path and D-08 summary write to stdout/stderr — a credential must never cross into log output. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | `--proposal` value used in `parameters__proposal__in` ORM lookup (Task 2) | accept (already mitigated) | Django ORM parameterizes `JSONField.__in` bound values (confirmed live in RESEARCH.md via `.query` inspection — no string interpolation). The comma-split/strip/dedup in `_parse_proposal_arg` is pure string manipulation with no SQL/shell/HTML sink. ASVS V5; no extra sanitization needed. |
| T-05-02 | Information Disclosure | D-07 skip-and-log path + D-08 summary line (Task 2) | mitigate | Log ONLY `record.observation_id`, `record.facility`, and the exception message. Never write `facility.facility_settings`, `api_key`, or any raw settings dict to stdout/stderr. Verified by Task 2's grep gate asserting `api_key` appears in zero non-comment lines of the command file. |
| T-05-SC | Tampering | npm/pip/cargo installs | accept (N/A) | No package installs this phase — `SOARFacility` is an existing import from already-installed `tom_observations` (RESEARCH.md Package Legitimacy Audit: N/A). No legitimacy checkpoint required. |

No HIGH-severity threats. No blocking human checkpoint needed.
</threat_model>

<verification>
- Full Django app suite: `cd /home/tlister/git/fomo_devel && ./manage.py test solsys_code` (all green, including the four new SELECT tests and all retained Phase-4 tests).
- Separate pytest suite remains green (unaffected): `python -m pytest`.
- Lint/format: `ruff check .` and `ruff format --check .` clean.
- No credential leakage: `grep -v '^#' solsys_code/management/commands/sync_lco_observation_calendar.py | grep -c api_key` returns 0.
</verification>

<success_criteria>
- `--proposal A,B,C` syncs records matching exactly A/B/C with no substring match on 'AB' (SELECT-02).
- `--proposal ALL` (any casing) syncs every LCO + SOAR record regardless of proposal (SELECT-03).
- A single run produces correct CalendarEvents for both LCO and SOAR records (SELECT-04).
- SOAR records are dispatched through a SOARFacility instance, proven by a discriminating spy test (SELECT-05).
- Per-facility summary line reports created/updated/unchanged/skipped for each facility (D-08).
- No credentials are written to logs; existing Phase-4 behavior is preserved unchanged.
</success_criteria>

<output>
Create `.planning/phases/05-multi-proposal-multi-facility-selection/05-01-SUMMARY.md` when done.
</output>
