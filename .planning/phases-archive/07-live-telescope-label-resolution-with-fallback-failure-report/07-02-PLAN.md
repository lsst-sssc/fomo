---
phase: 07-live-telescope-label-resolution-with-fallback-failure-report
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - solsys_code/management/commands/sync_lco_observation_calendar.py
  - solsys_code/tests/test_sync_lco_observation_calendar.py
  - docs/notebooks/pre_executed/sync_lco_observation_calendar_demo.ipynb
autonomous: true
requirements:
  - TELESCOPE-02
  - TELESCOPE-03
  - TELESCOPE-04
  - SYNC-06
  - SYNC-07
  - SYNC-09
user_setup: []

must_haves:
  truths:
    - "A placed record resolves its telescope label via the live API and gets a clean title on success"
    - "A placed record whose API call fails/times-out/returns an unmapped code still gets a CalendarEvent with a coarse fallback label, an [UNVERIFIED] title prefix, and a description noting the lookup failed"
    - "A banner-stage (not-yet-scheduled) record gets the coarse fallback label with NO API call and keeps its [QUEUED] prefix (no [UNVERIFIED])"
    - "The telescope_api_failed counter increments only for placed records whose API call failed/unmapped, is distinct from skipped, and appears in the per-facility summary line"
    - "A per-record API failure never aborts the run — subsequent records still sync"
    - "The failure log line is a fixed generic message that never contains the response body or API key"
  artifacts:
    - path: "solsys_code/management/commands/sync_lco_observation_calendar.py"
      provides: "Fallback decision tree wired into _build_event_fields, [UNVERIFIED] in _title_for, telescope_api_failed counter in handle()"
      contains: "telescope_api_failed"
    - path: "solsys_code/tests/test_sync_lco_observation_calendar.py"
      provides: "Integration tests for fallback/counter/no-abort/no-leak-in-log/[UNVERIFIED] prefix"
      contains: "test_sync_06_fallback_counter_distinct_from_skipped"
    - path: "docs/notebooks/pre_executed/sync_lco_observation_calendar_demo.ipynb"
      provides: "Executed demo cells for the API-success, API-failure/fallback, and new-counter paths"
      contains: "UNVERIFIED"
  key_links:
    - from: "_build_event_fields"
      to: "_resolve_placement_block"
      via: "calls the API resolver only for placed records (scheduled_start populated, D-01)"
      pattern: "_resolve_placement_block\\("
    - from: "Command.handle"
      to: "counters[...]['telescope_api_failed']"
      via: "increments on the placed+failed/unmapped condition, surfaced via the returned fields dict"
      pattern: "telescope_api_failed"
    - from: "_title_for"
      to: "[UNVERIFIED] prefix"
      via: "label_was_fallback branch between terminal-prefix and [QUEUED]"
      pattern: "\\[UNVERIFIED\\]"
---

<objective>
Wire the Plan 01 resolution helpers into the per-record sync path: build the
fallback decision tree in `_build_event_fields`, add the `[UNVERIFIED]` title
prefix in `_title_for` with the correct priority relative to terminal-state and
`[QUEUED]` prefixes (resolving D-09), add the per-facility `telescope_api_failed`
counter and surface it in the summary line (SYNC-06), guarantee a per-record API
failure never aborts the run (SYNC-07), and log only a fixed generic message on
failure (SYNC-09). Add the integration tests and update the paired demo notebook.

Purpose: This completes Phase 7's user-visible behavior. It depends on Plan 01's
`_resolve_placement_block` / `_aperture_class_from_telescope_code` / 2-arg
`_derive_telescope` already existing in the module (Wave 1).

Output: The wired command, the integration test suite, and the regenerated
executed demo notebook.
</objective>

<artifacts_this_phase_produces>
New symbols created by this plan (exclude from source-drift / acknowledgement checks — intentionally net-new):

- `_coarse_telescope_label(instrument_type) -> str` (function) — derives the coarse `1m0`/`0m4`/`2m0` fallback class token from the Phase-6-extracted instrument type
- `_unverified_prefix(...)` / the `[UNVERIFIED]` branch inside the extended `_title_for` (new `label_was_fallback: bool` parameter)
- `telescope_api_failed` counter key added to both `counters['LCO']` and `counters['SOAR']` dict literals and to the summary f-string in `Command.handle()`
- A new `telescope_api_failed: bool` key in the dict returned by `_build_event_fields` (popped before CalendarEvent kwargs, mirroring how `url` is already popped)
- Test methods: `test_telescope_03_api_failure_falls_back_not_skipped`, `test_telescope_04_fallback_label_visibly_distinguishable`, `test_sync_06_fallback_counter_distinct_from_skipped`, `test_sync_07_api_failure_does_not_abort_run`, `test_sync_09_log_line_is_fixed_generic_message`, `test_d01_banner_record_no_api_call_no_unverified_prefix`
- New demo-notebook cells in `sync_lco_observation_calendar_demo.ipynb` (API-success, API-failure/fallback, new-counter)
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-live-telescope-label-resolution-with-fallback-failure-report/07-CONTEXT.md
@.planning/phases/07-live-telescope-label-resolution-with-fallback-failure-report/07-RESEARCH.md
@.planning/phases/07-live-telescope-label-resolution-with-fallback-failure-report/07-PATTERNS.md
@.planning/phases/07-live-telescope-label-resolution-with-fallback-failure-report/07-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire fallback decision tree, [UNVERIFIED] prefix, and telescope_api_failed counter into the command (TELESCOPE-02, TELESCOPE-03, TELESCOPE-04, SYNC-06, SYNC-07, SYNC-09)</name>
  <files>solsys_code/management/commands/sync_lco_observation_calendar.py</files>
  <read_first>
    - solsys_code/management/commands/sync_lco_observation_calendar.py (FINAL state after Plan 01: the new SITE_TELESCOPE_MAP, _resolve_placement_block, _aperture_class_from_telescope_code, 2-arg _derive_telescope; plus the to-be-modified _title_for lines ~166-184, _build_event_fields lines ~226-268, Command.handle counters dict lines ~339-342 and summary f-string lines ~396-402)
    - .planning/phases/07-live-telescope-label-resolution-with-fallback-failure-report/07-RESEARCH.md (Architecture diagram decision tree; Pattern 3 _title_for priority resolving D-09; Pattern 4 counter; Pitfall 4 — unmapped-code == failure for prefix/counter; Common Pitfalls 1 caller logging discipline; Code Examples coarse-label-from-instrument confirmation at ocs.py:1317-1319 / lco.py:792)
    - .planning/phases/07-live-telescope-label-resolution-with-fallback-failure-report/07-PATTERNS.md (the existing try/except-per-record dispatch in handle() to extend; per-facility counters dict extension; SYNC-09 no-leak logging discipline; never-raise convention)
    - .planning/phases/07-live-telescope-label-resolution-with-fallback-failure-report/07-CONTEXT.md (D-01, D-02, D-06, D-07, D-08, D-09 — the locked scope rules for when the API call/counter/prefix apply)
  </read_first>
  <action>
    Modify `sync_lco_observation_calendar.py` to wire Plan 01's helpers into the
    per-record path. Implement the exact decision tree below — note Pitfall 4: an
    "API succeeded but returned an unmapped code" is the SAME fallback bucket as a
    network/timeout failure for both the counter and the title prefix.

    (a) Add `_coarse_telescope_label(instrument_type: str) -> str`: derive the coarse
    aperture-class token from the Phase-6 instrument_type string. The instrument
    codes are prefixed `2M0-`/`1M0-`/`0M4-` (e.g. `'1M0-SCICAM-SINISTRO'`,
    `'0M4-SCICAM-SBIG'`, `'2M0-SPECTRAL-AG'`, confirmed lco.py:792); map the leading
    `2M0`/`1M0`/`0M4` (case-insensitive) to `2m0`/`1m0`/`0m4` (the same D-04
    vocabulary). If the instrument_type has no recognized aperture prefix, return a
    defensible neutral token (e.g. the raw instrument_type or a `'?'` placeholder) —
    choose and document this; it only affects the coarse label string, never whether
    the record syncs. Google-style docstring, never raises.

    (b) Extend `_title_for` to take a new final parameter `label_was_fallback: bool`
    and insert the `[UNVERIFIED]` branch with the priority order from RESEARCH.md
    Pattern 3 (resolving D-09): terminal-failure prefix (existing) wins over all;
    then `[QUEUED]` for a banner record (scheduled_start is None) — banner records
    NEVER get `[UNVERIFIED]` per D-07; then `[UNVERIFIED]` for a placed record whose
    label was a fallback; then clean (placed + verified). Keep the existing branch
    bodies unchanged; only add the `label_was_fallback` branch between the
    `[QUEUED]` branch and the clean return. Update the docstring to document the new
    parameter and the D-09 priority rule explicitly (terminal beats [UNVERIFIED];
    [UNVERIFIED] never co-occurs with [QUEUED] because it only applies to placed
    records).

    (c) Rework `_build_event_fields` to implement the decision tree (replacing the
    current `telescope = _derive_telescope(record.parameters['site'])` line that
    reads the flat — often absent — 'site' key):
      1. Extract `instrument = _extract_instrument(record.parameters)` as today;
         keep the existing `InstrumentExtractionError` raise when instrument is None
         (that path is unchanged — extraction failure is a separate counter from
         telescope-API failure).
      2. Compute `coarse = _coarse_telescope_label(instrument)`.
      3. Decision tree for `telescope` and a local `label_was_fallback: bool`:
         - If `record.scheduled_start is None` (banner stage, D-01): NO API call;
           `telescope = coarse`; `label_was_fallback = False` (D-02/D-07 — a banner
           record's coarse label is NOT counted and NOT [UNVERIFIED]).
         - Else (placed): call `block = _resolve_placement_block(record.observation_id,
           facility)`. If `block is None` -> fallback: `telescope = coarse`,
           `label_was_fallback = True`. Else read `site = block.get('site')`,
           `telescope_code = block.get('telescope')`, and
           `resolved = _derive_telescope(site, telescope_code)`; if `resolved is None`
           (unmapped code, Pitfall 4 — same bucket) -> fallback: `telescope = coarse`,
           `label_was_fallback = True`; else `telescope = resolved`,
           `label_was_fallback = False`.
      4. Build `title = _title_for(record, telescope, instrument, facility,
         label_was_fallback)`.
      5. Description (TELESCOPE-04): when `label_was_fallback` is True, append a line
         noting the API lookup failed / label unverified. For the unmapped-code
         sub-case it is safe to include the unmapped code itself (it is not a
         credential or response body, Pitfall 4); for the API-call-failure sub-case
         the note MUST be generic (no exception content). The simplest compliant
         approach: a generic "Telescope label unverified: live API lookup failed or
         returned an unmapped code." line for all fallback cases — never derived from
         a caught exception.
      6. Return the existing fields dict PLUS a new `'telescope_api_failed': bool`
         key whose value is True only when `record.scheduled_start is not None AND
         label_was_fallback is True` (D-02 scope — placed + failed/unmapped only,
         never banner). This key is popped by handle() before constructing
         CalendarEvent kwargs, exactly like `url` is already popped (do NOT pass it
         to CalendarEvent.objects.get_or_create).
      Note: `_build_event_fields` must NOT log the API failure itself — logging is
      done in handle() (step d) to keep the caller-logging-discipline in one place.

    (d) Extend `Command.handle()`:
      - Add `'telescope_api_failed': 0` to BOTH `counters['LCO']` and
        `counters['SOAR']` dict literals, AND to the `counters.setdefault(...)`
        defensive-path default dict (line ~358-361) so an unexpected facility row
        does not KeyError on the new key.
      - After `_build_event_fields` returns successfully, `telescope_api_failed =
        fields.pop('telescope_api_failed')`. If True: increment
        `counters[record.facility]['telescope_api_failed'] += 1` AND write ONE fixed
        generic stderr line:
        `f'Telescope API lookup failed or returned an unmapped code for '
        f'observation_id={record.observation_id!r}; using fallback label.'`
        — this message MUST NOT interpolate any exception (SYNC-09 / Pitfall 1).
        Then continue with the normal get_or_create/update flow (SYNC-07 — the
        record still produces a CalendarEvent; the run continues).
      - Extend the summary f-string to append `, telescope_api_failed:
        {counts["telescope_api_failed"]}` after the existing `extraction_failed`
        term, same per-facility `key: N` phrasing (D-08 per-facility convention).
  </action>
  <verify>
    <automated>./manage.py test solsys_code.tests.test_sync_lco_observation_calendar 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "telescope_api_failed" solsys_code/management/commands/sync_lco_observation_calendar.py` shows the key in both counter literals, the setdefault default, the increment, the popped key in _build_event_fields' return, and the summary f-string.
    - `grep -n "\[UNVERIFIED\]" solsys_code/management/commands/sync_lco_observation_calendar.py` shows it appears only inside `_title_for`, in a branch after the `[QUEUED]` branch.
    - `_build_event_fields` calls `_resolve_placement_block` only inside the `record.scheduled_start is not None` branch (no API call on the banner-stage path).
    - The handle() stderr line for the API-failure path is a fixed string interpolating only `record.observation_id` — it does not interpolate any exception variable (no `{exc}`/`str(exc)`/`repr(exc)` on that line).
    - `record.parameters['site']` is no longer read in `_build_event_fields` (the flat-site assumption is fully removed): `grep -n "parameters\['site'\]\|parameters.get('site')" solsys_code/management/commands/sync_lco_observation_calendar.py` finds no occurrence in `_build_event_fields`.
    - `ruff check .` and `ruff format --check .` clean.
    - `./manage.py test solsys_code.tests.test_sync_lco_observation_calendar` — the Plan 01 tests plus all pre-existing tests still pass (the pre-existing `test_skip_path_missing_site_logged_and_skipped` behavior may need its expectation updated since 'site' is no longer required — adjust it in Task 2 if needed, not here).
  </acceptance_criteria>
  <done>The fallback decision tree, [UNVERIFIED] prefix with correct D-09 priority, telescope_api_failed counter, no-abort behavior, and fixed-generic failure log are wired into the command, with the full existing test file still green (or its now-obsolete 'site'-required test flagged for Task 2 update).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integration tests for fallback, counter, no-abort, fixed-message log, and [UNVERIFIED]/banner prefix behavior (TELESCOPE-03, TELESCOPE-04, SYNC-06, SYNC-07, SYNC-09)</name>
  <files>solsys_code/tests/test_sync_lco_observation_calendar.py</files>
  <read_first>
    - solsys_code/tests/test_sync_lco_observation_calendar.py (FINAL state after Plan 01 Task 3: the _observations_block_response helper; lines 442-479 test_select_05 mocking-with-spy example; lines 323-339 test_skip_path_missing_site — likely needs updating since 'site' is no longer required; the call_command + CalendarEvent.objects assertion pattern used throughout)
    - solsys_code/management/commands/sync_lco_observation_calendar.py (FINAL state after this plan's Task 1)
    - .planning/phases/07-live-telescope-label-resolution-with-fallback-failure-report/07-RESEARCH.md (Validation Architecture test names and call-site-patching strategy; Wave 0 Gaps timeout double)
    - .planning/phases/07-live-telescope-label-resolution-with-fallback-failure-report/07-PATTERNS.md (test naming + one-line-docstring convention; mock-with-spy pattern)
  </read_first>
  <behavior>
    - test_telescope_03_api_failure_falls_back_not_skipped: a PLACED record (scheduled_start/end set) with make_request patched to raise requests.exceptions.Timeout -> a CalendarEvent IS created (not skipped), its telescope field is the coarse label (e.g. '1m0' for a 1M0- instrument), and skipped count is 0.
    - test_telescope_04_fallback_label_visibly_distinguishable: same fallback scenario -> the event title starts with '[UNVERIFIED]', the telescope field is the coarse class token, and the description contains the unverified/lookup-failed note. Then re-run with make_request patched to succeed returning a mapped block -> the event UPDATES (title loses [UNVERIFIED], telescope becomes the verified 'SITE-CLASS' label) and the updated counter increments (visible flip, not silently unchanged).
    - test_sync_06_fallback_counter_distinct_from_skipped: a placed+failed record increments telescope_api_failed (=1) and leaves skipped (=0); the summary line printed to stdout contains 'telescope_api_failed: 1'.
    - test_sync_07_api_failure_does_not_abort_run: two placed records where the FIRST raises on the API call and the SECOND succeeds -> both produce CalendarEvents (the run did not abort after the first failure); no exception propagates out of call_command.
    - test_sync_09_log_line_is_fixed_generic_message: a placed record whose make_request raises ImproperCredentialsException('OCS: LEAK_MARKER_apikey_body') -> the captured stderr contains the fixed generic message and observation_id, and does NOT contain the literal leak marker token.
    - test_d01_banner_record_no_api_call_no_unverified_prefix: a banner-stage record (scheduled_start is None) -> make_request is NOT called (assert_not_called), the title keeps the '[QUEUED]' prefix and has no '[UNVERIFIED]', the telescope field is the coarse label, and telescope_api_failed stays 0.
  </behavior>
  <action>
    Add the six new test methods named exactly
    `test_telescope_03_api_failure_falls_back_not_skipped`,
    `test_telescope_04_fallback_label_visibly_distinguishable`,
    `test_sync_06_fallback_counter_distinct_from_skipped`,
    `test_sync_07_api_failure_does_not_abort_run`,
    `test_sync_09_log_line_is_fixed_generic_message`,
    `test_d01_banner_record_no_api_call_no_unverified_prefix`, each with the
    one-line requirement-ID docstring convention. Drive them through `call_command(
    'sync_lco_observation_calendar', '--proposal', ...)` and assert on
    `CalendarEvent.objects` rows and on captured stdout/stderr (the file already
    captures stdout via the call_command stdout kwarg / io.StringIO — follow the
    existing capture pattern).

    Patch `make_request` at the command import site
    (`patch('solsys_code.management.commands.sync_lco_observation_calendar.make_request',
    ...)`) using `side_effect=requests.exceptions.Timeout` /
    `side_effect=ImproperCredentialsException('OCS: LEAK_MARKER_apikey_body')` for the
    failure cases, and `return_value=_observations_block_response(...)` for the
    success cases. For the banner-no-call test, assert the patched make_request's
    `assert_not_called()`. Use `_create_record(..., scheduled_start=<aware dt>,
    scheduled_end=<aware dt>)` for placed records and the default (None) for banner
    records. Any Target fixture MUST use `NonSiderealTargetFactory`.

    Update the pre-existing `test_skip_path_missing_site_logged_and_skipped` (and any
    other test that assumed `parameters['site']` is required) to reflect the new
    behavior: a missing flat 'site' key no longer causes a skip — a placed record
    now goes through the API path and a banner record uses the coarse label. Either
    repurpose or remove that obsolete test, and ensure the full file is green.
  </action>
  <verify>
    <automated>./manage.py test solsys_code.tests.test_sync_lco_observation_calendar 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - All six new test methods exist and pass.
    - `test_sync_07_api_failure_does_not_abort_run` proves both records sync despite the first failing (2 CalendarEvents created, no exception from call_command).
    - `test_sync_09_log_line_is_fixed_generic_message` asserts the leak marker token is absent from captured stderr and the fixed generic message is present.
    - `test_d01_banner_record_no_api_call_no_unverified_prefix` asserts `make_request.assert_not_called()` and that the title has `[QUEUED]` and not `[UNVERIFIED]`.
    - `test_telescope_04_...` asserts the verified->fallback (or fallback->verified) label flip updates the event (updated counter increments, not unchanged).
    - The full `./manage.py test solsys_code` suite is green; `python -m pytest` still passes; `ruff check .` and `ruff format --check .` clean.
  </acceptance_criteria>
  <done>Fallback, counter-distinctness, no-abort, fixed-generic-log, and [UNVERIFIED]/banner-prefix behaviors are covered by passing integration tests, the obsolete missing-site test is reconciled, and the entire suite is green.</done>
</task>

<task type="auto">
  <name>Task 3: Update and regenerate the paired demo notebook (CLAUDE.md demo-notebook-companion convention)</name>
  <files>docs/notebooks/pre_executed/sync_lco_observation_calendar_demo.ipynb</files>
  <read_first>
    - docs/notebooks/pre_executed/sync_lco_observation_calendar_demo.ipynb (current cell structure from Phases 4-6 — read it to match the existing setup/fixture/run/output cell pattern)
    - solsys_code/management/commands/sync_lco_observation_calendar.py (FINAL state after this plan's Task 1 — for the exact function names/behavior the cells exercise)
    - CLAUDE.md ("Demo notebook companions" convention — what must be demonstrated and how to regenerate/commit with output)
    - .planning/phases/07-live-telescope-label-resolution-with-fallback-failure-report/07-PATTERNS.md (notebook section — the 3 required new cells; regeneration command)
  </read_first>
  <action>
    Extend the existing demo notebook with new cells exercising this phase's new
    behavior, mirroring the notebook's existing setup/fixture/run/print-output cell
    structure (the Phase 4-6 cells used `NonSiderealTargetFactory` fixtures and
    `unittest.mock.patch` on the command's collaborators — reuse that exact approach;
    any Target fixture MUST be `NonSiderealTargetFactory`). Add cells demonstrating:
      (1) A PLACED record with a successful mocked API resolution
          (`make_request` patched to return a COMPLETED block for e.g. site 'lsc',
          telescope '1m0a') producing a verified `'LSC-1m0'` telescope label and a
          clean (no-prefix) title.
      (2) A PLACED record with a mocked API failure (`make_request` side_effect a
          Timeout) producing the coarse fallback label, the `[UNVERIFIED]` title
          prefix, and the description's unverified note.
      (3) The run summary line printed output showing the new
          `telescope_api_failed: N` counter distinct from `skipped`.
    Then regenerate the notebook WITH executed output and commit it with output (this
    is the one `pre_executed/` location pre-commit does not clear):
      `jupyter nbconvert --to notebook --execute --inplace docs/notebooks/pre_executed/sync_lco_observation_calendar_demo.ipynb`
  </action>
  <verify>
    <automated>jupyter nbconvert --to notebook --execute --inplace docs/notebooks/pre_executed/sync_lco_observation_calendar_demo.ipynb && grep -l "UNVERIFIED" docs/notebooks/pre_executed/sync_lco_observation_calendar_demo.ipynb</automated>
  </verify>
  <acceptance_criteria>
    - The notebook executes cleanly via `jupyter nbconvert --to notebook --execute --inplace` with no cell errors.
    - The committed notebook contains executed output cells for all three new behaviors: a verified `SITECODE-CLASS` label (success), an `[UNVERIFIED]` fallback label (failure), and a printed summary line containing `telescope_api_failed:`.
    - `grep -c "UNVERIFIED" docs/notebooks/pre_executed/sync_lco_observation_calendar_demo.ipynb` is >= 1 (the fallback cell's executed output is present).
    - The notebook is committed WITH output (output cells are non-empty for the new cells).
  </acceptance_criteria>
  <done>The paired demo notebook demonstrates the API-success, API-failure/fallback, and new-counter paths with real executed output, regenerated and committed with output per CLAUDE.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sync command -> LCO Observation Portal API | Untrusted remote response crosses into the per-record loop via `_resolve_placement_block`. |
| caught API exception -> stderr / CalendarEvent.description | The failure log line and the fallback description must never carry the response body or API key. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Information Disclosure | `Command.handle()` API-failure stderr line | mitigate | SYNC-09/D-11 — the failure log line is a fixed string interpolating only `record.observation_id`, never the caught exception; verified by `test_sync_09_log_line_is_fixed_generic_message`. ASVS V7.4.1. |
| T-07-05 | Information Disclosure | fallback `CalendarEvent.description` note | mitigate | TELESCOPE-04 — the description's unverified note is a generic fixed string (or the safe unmapped-code value), never derived from a caught exception's response body. |
| T-07-06 | Denial of Service | a single record's API failure aborting the whole batch | mitigate | SYNC-07 — `_resolve_placement_block` never raises (returns None), and `_build_event_fields` routes None to the coarse fallback; the run continues. Verified by `test_sync_07_api_failure_does_not_abort_run`. |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed this phase (RESEARCH.md Package Legitimacy Audit: not applicable). |
</threat_model>

<verification>
- `./manage.py test solsys_code` fully green (Plan 01 + Plan 02 tests + pre-existing, with the obsolete missing-site test reconciled).
- `python -m pytest` green; `ruff check .` and `ruff format --check .` clean.
- The demo notebook regenerates cleanly with executed output for all three new behaviors.
- Summary line shows per-facility `telescope_api_failed: N` distinct from `skipped: N`.
- A placed+failed record produces an `[UNVERIFIED]`-prefixed CalendarEvent with a coarse label; a banner record keeps `[QUEUED]` and triggers no API call.
</verification>

<success_criteria>
- TELESCOPE-02/03: placed records resolve via the live API (verified label, clean title) or fall back to a coarse label without being skipped.
- TELESCOPE-04: fallback events are distinguishable (`[UNVERIFIED]` prefix + coarse telescope token + description note); label flips between runs update the event visibly.
- SYNC-06: per-facility `telescope_api_failed` counter, distinct from `skipped`, in the summary line; increments only for placed+failed/unmapped (D-02 scope).
- SYNC-07: a per-record API failure never aborts the run.
- SYNC-09: the failure log line and fallback description never contain the response body or API key.
</success_criteria>

<output>
Create `.planning/phases/07-live-telescope-label-resolution-with-fallback-failure-report/07-02-SUMMARY.md` when done.
</output>
