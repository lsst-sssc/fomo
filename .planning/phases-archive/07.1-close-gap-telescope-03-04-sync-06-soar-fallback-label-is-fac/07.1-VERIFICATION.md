---
phase: 07.1-close-gap-telescope-03-04-sync-06-soar-fallback-label-is-fac
verified: 2026-06-24T15:10:00Z
status: passed
score: 5/5 must-haves verified
behavior_unverified: 0
overrides_applied: 0
---

# Phase 07.1: Close Gap TELESCOPE-03/04/SYNC-06 (SOAR Fallback Label) Verification Report

**Phase Goal:** Every SOAR record that hits the coarse-label fallback path (live API failure,
timeout, or unmapped code) gets a correct, facility-aware `4m0` label and a clean fallback title —
not the raw SOAR instrument string duplicated — closing the gap the v1.3 milestone audit found in
TELESCOPE-03/04/SYNC-06.

**Verified:** 2026-06-24T15:10:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | A placed SOAR record whose live API call fails gets telescope label `'4m0'`, not the raw instrument string (TELESCOPE-03) | VERIFIED | `_coarse_telescope_label` (sync_lco_observation_calendar.py:274-307) now takes `facility_name` and returns `'4m0'` unconditionally when `facility_name.upper() == 'SOAR'`, before the unchanged LCO prefix logic. New test `test_telescope_03_soar_api_failure_fallback_returns_4m0_label` drives a placed `facility='SOAR'`, `instrument_type='SOAR_GHTS_REDCAM'` record through a mocked `Timeout` and asserts `event.telescope == '4m0'`. Test run individually: PASS. Confirmed against pre-fix code at commit `7149fce` that the old 1-arg function's `'soa'` prefix check never matched `{0m4,1m0,2m0,4m0}`, so this assertion would have failed pre-fix (RED-state claim verified). |
| 2 | That SOAR fallback event's title is `'[UNVERIFIED] 4m0 <instrument>'` shaped, not the doubled `'[UNVERIFIED] SOAR_GHTS_REDCAM SOAR_GHTS_REDCAM'` (TELESCOPE-04) | VERIFIED | Same test asserts `event.title.startswith('[UNVERIFIED] 4m0 ')` and `'SOAR_GHTS_REDCAM SOAR_GHTS_REDCAM' not in event.title`. Demo notebook cell 39's executed output (`title : [UNVERIFIED] 4m0 SOAR_GHTS_REDCAM`) independently confirms the clean, non-doubled title in a real (non-mocked-assertion) execution. |
| 3 | The SOAR fallback record is counted in `telescope_api_failed` (distinct from `skipped`) with a usable degraded label attached (SYNC-06) | VERIFIED | Same test asserts `'skipped: 0'` in captured stdout; `_build_event_fields` sets `label_was_fallback=True` on this path, which `Command.handle()` (pre-existing, unmodified Phase-7 logic) routes to the `telescope_api_failed` counter, not `skipped`. The attached label is now `'4m0'` (usable) instead of the raw doubled string. |
| 4 | Every pre-existing LCO-path test still passes unchanged — LCO fallback behavior is untouched | VERIFIED | Full suite run: `python3 manage.py test solsys_code.tests.test_sync_lco_observation_calendar` → 36/36 tests pass, including `test_telescope_03_api_failure_falls_back_not_skipped` (LCO analog) asserting `event.telescope == '1m0'` unchanged. The LCO branch in `_coarse_telescope_label` (the `{'0m4','1m0','2m0','4m0'}` membership test and raw-string fallthrough) is byte-for-byte identical to the pre-fix version (diff confirms only the new SOAR early-return guard + signature/docstring changes were added). |
| 5 | The demo notebook shows the corrected SOAR fallback label with real executed output | VERIFIED | `docs/notebooks/pre_executed/sync_lco_observation_calendar_demo.ipynb` (44 cells, was 42) has a new markdown cell 38 + code cell 39 inserted strictly before `## Summary` (now cell 40) and `## Teardown` (now cell 41). Cell 39's committed `outputs` show real executed stdout: `title : [UNVERIFIED] 4m0 SOAR_GHTS_REDCAM`, `telescope : 4m0`, and a `PASS:` line. New fixture `demo-6071001` is registered in a new `demo_phase71_all_ids` Teardown block (cell 42), placed after the Phase-7 block and before the shared Target/User cleanup, with executed Teardown output confirming the run completed cleanly. Summary cell (40) text extended with new TELESCOPE-03/04/SYNC-06 "(SOAR gap fix)" rows; all pre-existing rows preserved. |

**Score:** 5/5 truths verified (0 present, behavior-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `solsys_code/management/commands/sync_lco_observation_calendar.py` | Facility-aware `_coarse_telescope_label` mapping any SOAR record to `4m0` on the fallback path | VERIFIED | Function signature is `_coarse_telescope_label(instrument_type: str, facility_name: str) -> str` (line 274); SOAR early-return at line ~301; sole call site at line 420 passes `record.facility` (the string), not the `LCOFacility` instance parameter named `facility`. |
| `solsys_code/tests/test_sync_lco_observation_calendar.py` | Test driving a placed `facility='SOAR'` record through the API-failure fallback, asserting label/title | VERIFIED | `test_telescope_03_soar_api_failure_fallback_returns_4m0_label` (line 782) present, uses `facility='SOAR'`, asserts `event.telescope == '4m0'`, title shape, doubled-string absence, and `skipped: 0`. Runs and passes individually and as part of the full 36-test suite. |
| `docs/notebooks/pre_executed/sync_lco_observation_calendar_demo.ipynb` | Demo cell exercising the corrected SOAR fallback label with executed output | VERIFIED | New markdown+code cell pair present, contains `'SOAR'`, has committed executed `outputs` showing the `4m0` corrected label and a `PASS:` confirmation line. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `sync_lco_observation_calendar.py:_build_event_fields` | `sync_lco_observation_calendar.py:_coarse_telescope_label` | sole call site passes the record's facility string into the now-facility-aware coarse label helper | WIRED | Line 420: `coarse = _coarse_telescope_label(instrument, record.facility)`. Confirmed via grep that this is the only call site in the file, and that it passes `record.facility` (string attribute) rather than the `facility: LCOFacility` parameter of `_build_event_fields` — the exact risk the plan flagged as highest-priority to verify. |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|--------------|--------|----------|
| TELESCOPE-03 | 07.1-01-PLAN.md | API failure/timeout/unmapped code falls back to coarse instrument-class label instead of skipping | SATISFIED | SOAR fallback now returns `'4m0'` (Truth 1); LCO behavior unchanged (Truth 4); both proven by passing tests. v1.3 audit's "partial (LCO-only evidence)" finding is closed. |
| TELESCOPE-04 | 07.1-01-PLAN.md | Fallback-labeled event distinguishable from verified-label event, no silent doubled/raw title | SATISFIED | Clean `[UNVERIFIED] 4m0 ...` title proven by test assertion + notebook executed output; doubled-string absence explicitly asserted (Truth 2). |
| SYNC-06 | 07.1-01-PLAN.md | Per-record telescope-API failures tracked as a distinct counter from `skipped_count`, with a usable label | SATISFIED | `skipped: 0` assertion confirms the SOAR fallback record routes to `telescope_api_failed`, not `skipped`; the now-usable `'4m0'` label is attached to that counted event (Truth 3). |

All three requirement IDs declared in the PLAN frontmatter (`TELESCOPE-03`, `TELESCOPE-04`, `SYNC-06`) match exactly the three IDs REQUIREMENTS.md maps to Phase 7, which this gap-fix phase explicitly re-closes per the v1.3 milestone audit (`.planning/v1.3-MILESTONE-AUDIT.md`). No orphaned requirements — REQUIREMENTS.md shows 14/14 v1 requirements mapped, 0 unmapped.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| — | — | None found | — | `TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER` grep across both modified Python files returned zero matches. |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Full LCO-facility-sync test suite (regression guard) | `python3 manage.py test solsys_code.tests.test_sync_lco_observation_calendar` | `Ran 36 tests ... OK` | PASS |
| New SOAR-fallback regression test runs in isolation | `python3 manage.py test solsys_code.tests.test_sync_lco_observation_calendar.TestSyncLcoObservationCalendar.test_telescope_03_soar_api_failure_fallback_returns_4m0_label` | `Ran 1 test ... OK` | PASS |
| Pre-fix RED-state check | `git show 7149fce:solsys_code/management/commands/sync_lco_observation_calendar.py` (manual trace of old 1-arg function against `'SOAR_GHTS_REDCAM'`) | `'soa'` not in `{'0m4','1m0','2m0','4m0'}` → old code returns raw string, confirming the new test's `== '4m0'` assertion would fail pre-fix | PASS (confirms TDD claim) |
| Lint/format clean on touched files | `ruff check .` / `ruff format --check .` (scoped to the two modified Python files) | `All checks passed!` / `2 files already formatted` | PASS |
| Demo notebook re-executes and shows corrected label | Inspected committed cell `outputs` (no live re-run needed; output already present and consistent with source) | `title : [UNVERIFIED] 4m0 SOAR_GHTS_REDCAM` / `telescope : 4m0` / `PASS:` line | PASS |

### Human Verification Required

None. All must-haves resolved to VERIFIED via direct code inspection, isolated test execution, full-suite regression run, and inspection of committed notebook execution output. No visual, real-time, or external-service-dependent behavior is in scope for this phase (pure string-mapping change, per the plan's own threat model).

### Gaps Summary

No gaps found. All 5 must-have truths verified, all 3 required artifacts present/substantive/wired, the single key link correctly passes `record.facility` (not the `LCOFacility` instance — the specific risk the plan called out), all 3 requirement IDs satisfied, zero debt markers, full regression suite green (36/36), and the demo notebook companion updated with real executed output per CLAUDE.md's notebook-companion convention. This closes the v1.3 milestone audit's only open gap.

---

*Verified: 2026-06-24T15:10:00Z*
*Verifier: Claude (gsd-verifier)*
