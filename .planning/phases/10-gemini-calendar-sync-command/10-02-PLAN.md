---
phase: 10-gemini-calendar-sync-command
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - docs/notebooks/pre_executed/sync_gemini_observation_calendar_demo.ipynb
autonomous: true
requirements:
  - GEM-WINDOW-01
  - GEM-WINDOW-02
  - GEM-STATUS-01
  - GEM-NOCHURN-01
  - GEM-SECURE-01

must_haves:
  truths:
    - "docs/notebooks/pre_executed/sync_gemini_observation_calendar_demo.ipynb executes end-to-end without error using synthetic GS-2026A-T-999 fixtures"
    - "The notebook demonstrates all four D-06 scenarios: explicit window, Rap: derived window, Std: derived window, and ON_HOLD + idempotent re-run"
    - "The notebook never contains a real credential — the password placeholder is '[redacted]'"
    - "The notebook patches FACILITIES['GEM']['programs'] with GS-2026A-T-999 -> {MM: 'Std: GMOS-S MOS', QQ: 'Rap: GMOS-S MOS'} and cleans up all fixtures at the end"
  artifacts:
    - path: "docs/notebooks/pre_executed/sync_gemini_observation_calendar_demo.ipynb"
      provides: "Pre-executed demo notebook for the Gemini sync command (committed with output)"
      contains: "sync_gemini_observation_calendar"
  key_links:
    - from: "sync_gemini_observation_calendar_demo.ipynb"
      to: "sync_gemini_observation_calendar command"
      via: "call_command in demo cells"
      pattern: "call_command\\('sync_gemini_observation_calendar'"
    - from: "sync_gemini_observation_calendar_demo.ipynb"
      to: "settings.FACILITIES['GEM']['programs']"
      via: "settings patch cell"
      pattern: "FACILITIES\\['GEM'\\]"
---

<objective>
Create the pre-executed demo notebook
`docs/notebooks/pre_executed/sync_gemini_observation_calendar_demo.ipynb` (CLAUDE.md mandatory
companion-notebook deliverable for the new `sync_gemini_observation_calendar` command). The notebook
sets up Django, patches a minimal `FACILITIES['GEM']['programs']` block, creates synthetic
`ObservationRecord` fixtures covering the four D-06 scenarios, runs the command, displays the
resulting `CalendarEvent`s and command summary, then tears down all fixtures. It is executed with
`jupyter nbconvert --to notebook --execute --inplace` and committed WITH output.

Purpose: Give operators a runnable, reviewed walkthrough of the Gemini sync, and satisfy the
CLAUDE.md rule that every new management command ships its paired pre-executed notebook.
Output: One pre-executed notebook committed with cell output.
</objective>

<execution_context>
@/home/tlister/git/fomo_devel/.claude/gsd-core/workflows/execute-plan.md
@/home/tlister/git/fomo_devel/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-gemini-calendar-sync-command/10-CONTEXT.md
@.planning/phases/10-gemini-calendar-sync-command/10-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create and execute the Gemini sync demo notebook</name>
  <files>docs/notebooks/pre_executed/sync_gemini_observation_calendar_demo.ipynb</files>
  <read_first>
    - docs/notebooks/pre_executed/sync_lco_observation_calendar_demo.ipynb (structure/style analog — Django setup cell, fixture-creation cell, call_command cell, teardown cell; copy the Django setup and teardown cells verbatim and adapt)
    - .planning/phases/10-gemini-calendar-sync-command/10-PATTERNS.md (the notebook pattern section — cell-by-cell layout, the FACILITIES['GEM']['programs'] patch cell, the four scenario cells, expected stdout)
    - .planning/phases/10-gemini-calendar-sync-command/10-CONTEXT.md (decisions D-05, D-06, D-07: realistic GS-2026A-T-999 IDs, the four required scenarios, the settings-patch + '[redacted]' password placeholder)
    - solsys_code/management/commands/sync_gemini_observation_calendar.py (the command this notebook demonstrates — confirm parameter key names and summary output format before writing assertions)
    - CLAUDE.md (pre_executed notebooks are committed WITH output via jupyter nbconvert --to notebook --execute --inplace; NonSiderealTargetFactory mandate; plain-English-over-jargon — write "find-or-create", not "upsert")
  </read_first>
  <action>
    Create docs/notebooks/pre_executed/sync_gemini_observation_calendar_demo.ipynb following the LCO
    demo notebook's structure. Cells:

    1. Markdown intro — describe `sync_gemini_observation_calendar` and list the four scenarios it
       demonstrates (explicit window, Rap: derived, Std: derived, ON_HOLD + idempotent re-run). Use
       plain English (find-or-create, not upsert).
    2. Django setup — copy the LCO demo's setup cell verbatim: resolve `repo_root` via
       `Path.cwd().resolve().parents[2]`, insert on sys.path, set `DJANGO_SETTINGS_MODULE=
       'src.fomo.settings'` and `DJANGO_ALLOW_ASYNC_UNSAFE='true'`, then `django.setup()`.
    3. Settings patch (D-07) — set
       `settings.FACILITIES['GEM']['programs'] = {'GS-2026A-T-999': {'MM': 'Std: GMOS-S MOS',
       'QQ': 'Rap: GMOS-S MOS'}}` (use `settings.FACILITIES.setdefault('GEM', {})` first).
    4. Fixture creation — get-or-create a demo user and a `NonSiderealTargetFactory.create(...)`
       target (NEVER SiderealTargetFactory), guarded so re-execution does not duplicate. Create the
       synthetic `ObservationRecord(facility='GEM')` rows for the four scenarios below; every params
       dict includes `'password': '[redacted]'` (D-05/D-07 — never a real key) and uses program ID
       `GS-2026A-T-999` (D-05).
    5. Scenario 1 — explicit window (GEM-WINDOW-01): a record with `windowDate`/`windowTime`/
       `windowDuration`. After running the command, display the event and show `start_time`/`end_time`
       match the parsed values.
    6. Scenario 2 — Rap: derived window (GEM-WINDOW-02): a record with no explicit window and
       `obsid=['QQ']` (maps to `'Rap: GMOS-S MOS'`); show `start_time == record.created` and
       `end_time == record.created + 24h`.
    7. Scenario 3 — Std: derived window (GEM-WINDOW-02): a record with `obsid=['MM']` (maps to
       `'Std: GMOS-S MOS'`); show window `[created + 24h, created + 7d]`.
    8. Scenario 4 — ON_HOLD + idempotent re-run (GEM-STATUS-01, GEM-NOCHURN-01): a record with
       `ready='false'`; show the event title starts with `[ON_HOLD] `; then call the command a second
       time on all records and show no new events were created and `CalendarEvent.modified` is
       unchanged (the summary line shows the records as `unchanged`).
    9. Run cell(s) — invoke `call_command('sync_gemini_observation_calendar', stdout=io.StringIO(),
       stderr=io.StringIO())` and print the captured stdout so the two per-site summary lines plus
       `Done.` are visible in committed output.
    10. Teardown — delete every `CalendarEvent` whose url starts with `GEM:`, delete the synthetic
        ObservationRecords, the demo target, and the demo user (mirror the LCO demo teardown cell).

    After writing the notebook, execute it in place so output is captured and committed:
    `jupyter nbconvert --to notebook --execute --inplace
    docs/notebooks/pre_executed/sync_gemini_observation_calendar_demo.ipynb`.
    The notebook must run cleanly from a fresh kernel (no manual state). Single quotes, plain English.
  </action>
  <verify>
    <automated>cd /home/tlister/git/fomo_devel && jupyter nbconvert --to notebook --execute --inplace docs/notebooks/pre_executed/sync_gemini_observation_calendar_demo.ipynb && python -c "import json,sys; nb=json.load(open('docs/notebooks/pre_executed/sync_gemini_observation_calendar_demo.ipynb')); src=''.join(''.join(c.get('source',[])) for c in nb['cells']); out=''.join(''.join(o.get('text',[]) if isinstance(o.get('text',[]),list) else [o.get('text','')]) for c in nb['cells'] for o in c.get('outputs',[])); assert 'sync_gemini_observation_calendar' in src; assert 'GS-2026A-T-999' in src; assert 'SiderealTargetFactory' not in src or 'NonSiderealTargetFactory' in src; assert 'password' not in out, 'password key leaked into notebook output'; print('notebook ok')"</automated>
  </verify>
  <acceptance_criteria>
    - docs/notebooks/pre_executed/sync_gemini_observation_calendar_demo.ipynb exists and re-executes with `jupyter nbconvert --to notebook --execute --inplace` exiting 0
    - Notebook source contains `call_command('sync_gemini_observation_calendar'`, `GS-2026A-T-999`, `NonSiderealTargetFactory`, and `'[redacted]'`
    - Notebook source does NOT contain `SiderealTargetFactory`
    - Notebook contains cells exercising all four D-06 scenarios (explicit window, Rap: derived, Std: derived, ON_HOLD + idempotent re-run)
    - The committed cell output shows the two per-site summary lines and `Done.`, and the substring `password` does not appear anywhere in any cell output
  </acceptance_criteria>
  <done>The notebook re-executes cleanly with committed output, covers all four scenarios, uses synthetic GS-2026A-T-999 fixtures with a redacted password placeholder, and leaks no credential into output.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 02)

- `docs/notebooks/pre_executed/sync_gemini_observation_calendar_demo.ipynb`
  - Pre-executed demo notebook (committed with cell output) demonstrating the four D-06 scenarios for `sync_gemini_observation_calendar`
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| notebook fixture params -> committed notebook output | Synthetic ObservationRecord params carry a `password` placeholder; command stdout is printed into committed cell output |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-N1 | Information Disclosure | committed notebook cell output | mitigate | Fixtures use `'password': '[redacted]'` only (D-05/D-07) — never a real key; the command strips `password` before logging (Plan 01 D-04); the verify step asserts the substring `password` is absent from all committed cell output |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | This plan installs no new packages (jupyter/nbconvert already in dev deps); no Package Legitimacy Gate required |
</threat_model>

<verification>
- `jupyter nbconvert --to notebook --execute --inplace docs/notebooks/pre_executed/sync_gemini_observation_calendar_demo.ipynb` exits 0.
- The committed notebook contains executed output for all four scenario cells and the command summary.
- `grep` of the notebook JSON output streams shows no occurrence of the literal `password`.
</verification>

<success_criteria>
- The demo notebook executes end-to-end without error using synthetic fixtures.
- All four D-06 scenarios are demonstrated with real executed output.
- No credential appears in committed output; the password placeholder is `[redacted]`.
</success_criteria>

<output>
Create `.planning/phases/10-gemini-calendar-sync-command/10-02-SUMMARY.md` when done.
</output>
