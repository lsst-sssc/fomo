---
phase: 30-v2-2-tech-debt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - solsys_code/campaign_attribution.py
  - solsys_code/tests/test_campaign_attribution.py
  - docs/runbooks/telescope_runs_calendar.rst
  - docs/notebooks/pre_executed/campaign_lifecycle_demo.ipynb
autonomous: true
requirements: [D-01, D-02, D-03, D-12]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-01/D-02: a CampaignRun whose approval_status is REJECTED is never returned by _eligible_runs_for_event or by _eligible_runs_for_record, so it is never offered as a suggested match for an orphan CalendarEvent or an orphan ObservationRecord (ROADMAP criterion 1)"
    - "D-01: an APPROVED run and a PENDING_REVIEW run in the identical position are both still offered as candidates — the exclusion is targeted, not a blanket emptying (non-vacuous control, ROADMAP criterion 1)"
    - "D-03: the exclusion is enforced once, at the two eligibility gates, so candidates_for_event, candidates_for_record, event_attribution_backlog, record_attribution_backlog, unattributable_orphan_count, _sole_high_candidate_pk, is_offered_candidate and the high-band template hint all move together rather than disagreeing (ROADMAP criterion 1)"
    - "An association already confirmed before its run was rejected is left intact: orphan_calendar_events() returns only un-attributed events, so a confirmed CalendarEventMeta.run link survives its run turning REJECTED (ROADMAP criterion 1)"
    - "D-03: is_offered_candidate() refuses a POST naming a REJECTED run's pk, because it re-derives eligibility through the changed gates rather than trusting the submitted pk"
    - "D-12: docs/runbooks/telescope_runs_calendar.rst tells staff that a rejected run is never offered as a match (ROADMAP criterion 6)"
    - "D-12: campaign_lifecycle_demo.ipynb shows a fifth, deliberately-rejected submission present as a candidate while PENDING_REVIEW and absent after rejection, in real executed output (ROADMAP criterion 6)"
  artifacts:
    - solsys_code/campaign_attribution.py
    - solsys_code/tests/test_campaign_attribution.py
    - docs/runbooks/telescope_runs_calendar.rst
    - docs/notebooks/pre_executed/campaign_lifecycle_demo.ipynb
  key_links:
    - "_eligible_runs_for_event -> candidates_for_event -> event_attribution_backlog / is_offered_candidate / templatetags/attribution_display_extras.py high-band hint"
    - "_eligible_runs_for_record -> candidates_for_record -> record_attribution_backlog / unattributable_orphan_count"
    - "CampaignRun.ApprovalStatus (solsys_code/models.py) -> _ATTRIBUTION_INELIGIBLE_APPROVAL_STATUSES -> both gates"
  prohibitions:
    - statement: "_eligible_runs_for_record must NOT additionally require the run's target FK to equal the record's target FK (standing prohibition recorded in its own docstring)"
      status: enforced
      verification: "python manage.py test solsys_code.tests.test_campaign_attribution.TestCriterion5RealCase -v2 passes — it pairs a non-sidereal run target with sidereal field-target records and would fail if target-FK equality were required"
    - statement: "No repo-wide reformat (D-05) — this plan runs no formatter over the tree"
      status: enforced
      verification: "git show --stat for this plan's commits lists only the four paths in files_modified"
---

<objective>
Close 27-REVIEW IN-02: exclude REJECTED runs from attribution eligibility at both gates
(D-01, D-02, D-03), and carry the change into the two paired artifacts the CLAUDE.md
paired-docs rule binds to `solsys_code/campaign_attribution.py` (D-12).

This is the phase's tracer slice: it runs one thin path — `CampaignRun.ApprovalStatus`
vocabulary -> eligibility gate -> scorer -> candidate list -> staff-facing docs and a real
executed notebook — end to end, proving the whole delivery path (including the paired-docs
rule) before any expansion task builds on it. The ROADMAP goal this plan serves was
corrected before planning per D-11, so it names genuinely-open work only.

Purpose: a rejected run being offered as a suggested match is the unambiguous defect the
27 code review found. Fixing it at the gate rather than at four display surfaces is what
keeps the queue, the backlog and the unattributable count from drifting apart (D-03).
Output: the two gates filtered through one named constant, a `TestApprovalStatusGate` test
class pinning both the exclusion and its non-vacuous controls, a new runbook paragraph, and
two new pre-executed notebook cells.
</objective>

<execution_context>
@/home/tlister/git/fomo_devel/.claude/gsd-core/workflows/execute-plan.md
@/home/tlister/git/fomo_devel/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-v2-2-tech-debt-cleanup/30-CONTEXT.md
@.planning/phases/30-v2-2-tech-debt-cleanup/30-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "a rejected run is never offered" — the event path only</name>
  <files>solsys_code/campaign_attribution.py, solsys_code/tests/test_campaign_attribution.py</files>
  <read_first>
    - `solsys_code/campaign_attribution.py` lines 435-560 — `orphan_calendar_events()`,
      `_eligible_runs_for_event()` (:473-486), `_eligible_runs_for_record()` (:489-508) and
      `candidates_for_event()` (:511-555). Read the docstrings, not just the bodies: they are
      where the "deliberately permissive" convention is stated, and the new hard gate has to
      be written as a deliberate contrast to it.
    - `solsys_code/models.py` lines 83-120 and 207-223 — the `ApprovalStatus` TextChoices
      vocabulary (`PENDING_REVIEW` / `APPROVED` / `REJECTED`) and 26-DECISION Criterion 1.
    - `solsys_code/tests/test_campaign_attribution.py` lines 175-275 — `TestCampaignBoundaryGate`
      and `test_cross_campaign_run_never_offered_for_event_even_at_perfect_score`, the exact
      assertion shape and the `setUpTestData` fixture-building convention to copy.
    - `.planning/phases/30-v2-2-tech-debt-cleanup/30-PATTERNS.md` — the "Docstring finding-ID
      citation convention", the "Enum-reference convention", and the excerpt of the two gates.
  </read_first>
  <behavior>
    - A `CampaignRun` in the same campaign as an orphan `CalendarEvent`, with a window that
      overlaps the event and a matching telescope/instrument (so it would otherwise score in
      the High band), is NOT in `candidates_for_event(event)` when its `approval_status` is
      `REJECTED`.
    - The same run at `approval_status=APPROVED` IS in `candidates_for_event(event)` — the
      fixture is proven capable of producing a candidate, so the exclusion assertion is not
      vacuously true.
  </behavior>
  <action>
    In `solsys_code/campaign_attribution.py`, add one module-level private constant near the
    other module-level definitions naming the approval states that disqualify a run from
    attribution eligibility. Name it `_ATTRIBUTION_INELIGIBLE_APPROVAL_STATUSES` and give it
    the value `frozenset({CampaignRun.ApprovalStatus.REJECTED})` — referenced through the
    enum class per the project's enum-reference convention, never a bare string literal. One
    constant, two call sites, per D-03's anti-drift rationale; do NOT inline the status in
    each gate.

    Give the constant a comment citing `27-REVIEW IN-02` and `D-01` by ID (the project answers
    findings in docstrings/comments by ID) and stating the D-01 boundary explicitly: only
    `REJECTED` is ineligible; `APPROVED` and `PENDING_REVIEW` both stay eligible, because an
    orphan matching a still-pending web submission is useful evidence that the submission is
    genuine.

    Apply `.exclude(approval_status__in=_ATTRIBUTION_INELIGIBLE_APPROVAL_STATUSES)` to the
    returned queryset of BOTH `_eligible_runs_for_event()` and `_eligible_runs_for_record()`
    (D-02 — the audit named only the event gate; the record gate has the identical omission).
    Keep each function's existing early-return-on-None-FK shape byte-for-byte; the exclusion
    goes on the final `CampaignRun.objects.filter(...)` return only, so an event with no
    `target_list` and a record with no `target` still short-circuit to
    `CampaignRun.objects.none()`.

    Extend both docstrings. Each must (a) cite `27-REVIEW IN-02` and `D-01`/`D-02`/`D-03` by
    ID, (b) state that the approval filter is a HARD gate applied here rather than at the
    display surfaces, and (c) say plainly that this is a deliberate departure from the
    "eligibility gates are deliberately permissive, downstream filters do the narrowing"
    convention the surrounding docstrings state — with D-03's reason: an orphan whose only
    candidate was a rejected run genuinely has no valid candidate, and one filter in one place
    cannot drift out of sync the way four call-site filters could. Leave
    `_eligible_runs_for_record()`'s existing paragraph about NOT requiring target-FK equality
    untouched and intact — it is a standing prohibition.

    Then add a new `TestApprovalStatusGate(TestCase)` class to
    `solsys_code/tests/test_campaign_attribution.py`, following the file's existing
    `setUpTestData` class-fixture convention. Its class docstring must name BOTH sides
    explicitly, in the shape `test_admin.py`'s `SourceProvenanceLockTests` uses: what is
    excluded (`REJECTED`) and what stays included (`APPROVED`, `PENDING_REVIEW`). Any `Target`
    fixture uses `tom_targets.tests.factories.NonSiderealTargetFactory` (CLAUDE.md), never
    `SiderealTargetFactory`.

    For this task add exactly two methods to that class:
    `test_rejected_run_never_offered_for_event` and
    `test_approved_run_still_offered_for_event`. Build one orphan `CalendarEvent` in a demo
    campaign whose `telescope`/`instrument` and window match a `CampaignRun` in the same
    campaign, assert via the `{c.run.pk for c in candidates_for_event(...)}` set-comprehension
    shape the file already uses, and drive the two cases off `approval_status` alone so the
    only difference between the pass and the fail is the field under test.
  </action>
  <verify>
    <automated>python manage.py test solsys_code.tests.test_campaign_attribution -v2</automated>
    <fails_when>non-zero exit, or "OK" absent from the final line of the runner output</fails_when>
    <automated>python manage.py test solsys_code.tests.test_campaign_attribution.TestApprovalStatusGate -v2 2>&1 | grep -E 'Ran [1-9][0-9]* tests?'</automated>
    <fails_when>non-zero exit from grep, which means the runner reported "Ran 0 tests" — the new test class did not exist or matched nothing, so the suite passed vacuously</fails_when>
    <automated>grep -c 'approval_status__in=_ATTRIBUTION_INELIGIBLE_APPROVAL_STATUSES' solsys_code/campaign_attribution.py</automated>
    <fails_when>prints a number other than 2 — the filter is missing from one of the two gates (D-02), or was inlined at extra call sites instead of the two gates</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c '_ATTRIBUTION_INELIGIBLE_APPROVAL_STATUSES' solsys_code/campaign_attribution.py` prints 3 (one definition, two uses).
    - `grep -c "CampaignRun.ApprovalStatus.REJECTED" solsys_code/campaign_attribution.py` prints at least 1, and `grep -c "'rejected'" solsys_code/campaign_attribution.py` prints 0 — the status is referenced through the enum, never as a bare string.
    - `grep -c '27-REVIEW IN-02' solsys_code/campaign_attribution.py` prints at least 2 (both gate docstrings cite the finding by ID).
    - `grep -c 'must NOT additionally require' solsys_code/campaign_attribution.py` prints at least 1 — `_eligible_runs_for_record`'s standing target-FK prohibition paragraph survived the docstring edit.
    - `python manage.py test solsys_code.tests.test_campaign_attribution -v2` exits 0.
    - `python manage.py test solsys_code.tests.test_campaign_attribution.TestApprovalStatusGate -v2` reports "Ran 2 tests" and exits 0.
    - `python manage.py test solsys_code.tests.test_campaign_attribution.TestCriterion5RealCase -v2` still exits 0 (the target-FK prohibition is unbroken).
  </acceptance_criteria>
  <reversibility rating="reversible">One `.exclude()` per function plus a constant; no schema change and no persisted state depends on it (carried through from 30-CONTEXT.md's D-03 rating).</reversibility>
  <done>A REJECTED run in a perfect-scoring position is absent from `candidates_for_event()`, the identical run at APPROVED is present, both gates carry the filter through one named constant, and both docstrings cite 27-REVIEW IN-02 and name the hard-gate departure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand to the record path, the PENDING_REVIEW control, and the confirmed-link survival</name>
  <files>solsys_code/tests/test_campaign_attribution.py</files>
  <read_first>
    - `solsys_code/tests/test_campaign_attribution.py` — the `TestApprovalStatusGate` class
      Task 1 created, plus `TestCriterion5RealCase` (:275-378) for how an
      `ObservationRecord` orphan fixture is built against a sidereal field target, and
      `TestOrphanQuerysets` (:456-509) for the `orphan_calendar_events()` assertion shape.
    - `solsys_code/campaign_attribution.py` lines 435-460 (`orphan_calendar_events()`) and
      764-800 (`is_offered_candidate()`) — the two functions Task 2 asserts against.
    - `solsys_code/models.py` — the `CalendarEventMeta` companion model's required fields and
      its `run` FK, needed to build a confirmed association directly in a fixture.
  </read_first>
  <behavior>
    - Test: a REJECTED run in the same campaign as an orphan `ObservationRecord` is not in
      `candidates_for_record(record)` (D-02, the record half).
    - Test: the same run at `APPROVED` is in `candidates_for_record(record)` (control).
    - Test: a run at `PENDING_REVIEW` IS offered for both an event orphan and a record orphan
      (D-01 — pending submissions deliberately stay eligible).
    - Test: `is_offered_candidate('event', event.pk, rejected_run.pk)` returns None, and
      returns a candidate for the same run at `APPROVED` (D-03 — the server-side
      re-derivation moves with the gate, so a tampered POST cannot name a rejected run).
    - Test: an event with a `CalendarEventMeta` whose `run` is already set stays out of
      `orphan_calendar_events()` and keeps its `run_id` unchanged after that run is set to
      `REJECTED` — an association confirmed before the rejection is left intact.
  </behavior>
  <action>
    Add the remaining methods to `TestApprovalStatusGate` (or a sibling `TestCase` class in
    the same module if the record fixtures make one class unwieldy — the file already splits
    by concern):

    `test_rejected_run_never_offered_for_record` and
    `test_approved_run_still_offered_for_record` — mirror Task 1's event pair against
    `candidates_for_record`. Per the standing prohibition in `_eligible_runs_for_record`'s
    docstring, build the fixture the way `TestCriterion5RealCase` does: the run's target is
    the campaign's non-sidereal target (`NonSiderealTargetFactory`) while the
    `ObservationRecord` points at a separate per-pointing field target that belongs to the
    same campaign `TargetList`. Do not make the two targets equal — a fixture that passes only
    because the FKs match would silently re-introduce the prohibited target-equality coupling.

    `test_pending_review_run_still_offered_for_event` and
    `test_pending_review_run_still_offered_for_record` — the D-01 half the ROADMAP calls out
    by name. Same fixtures, `approval_status=CampaignRun.ApprovalStatus.PENDING_REVIEW`,
    asserting the run pk IS present in the candidate set.

    `test_is_offered_candidate_refuses_a_rejected_run` — call
    `campaign_attribution.is_offered_candidate('event', event.pk, run.pk)` with the run at
    `REJECTED` and assert it returns `None`; assert it returns a non-None
    `AttributionCandidate` for the identical run at `APPROVED`. Docstring should say why this
    matters: the attribution decide view trusts this function, not the submitted pk.

    `test_confirmed_attribution_survives_its_run_being_rejected` — create the event, create a
    `CalendarEventMeta` companion row for it with `run` set to the run, then set the run's
    `approval_status` to `REJECTED` and save. Assert (a) the event is not in
    `orphan_calendar_events()`, and (b) the companion row's `run_id` is unchanged after
    `refresh_from_db()`. Docstring must state the mechanism, not just the outcome:
    `orphan_calendar_events()` returns only un-attributed events, so the eligibility gates
    never see an already-linked event and cannot unlink one.

    Every test method docstring cites the decision it pins (`D-01`, `D-02` or `D-03`) by ID,
    matching the file's existing convention of citing findings and decisions by ID.
  </action>
  <verify>
    <automated>python manage.py test solsys_code.tests.test_campaign_attribution -v2</automated>
    <fails_when>non-zero exit, or "OK" absent from the final line of the runner output</fails_when>
    <automated>python manage.py test solsys_code.tests.test_campaign_attribution solsys_code.tests.test_campaign_attribution_views solsys_code.tests.test_attribution_dismissals solsys_code.tests.test_attribution_template solsys_code.tests.test_campaign_approval -v1</automated>
    <fails_when>non-zero exit, or "OK" absent from the final line — a downstream consumer of the two gates (queue view, dismissals, template hint, approval queue) regressed on the new filter</fails_when>
  </verify>
  <acceptance_criteria>
    - `python manage.py test solsys_code.tests.test_campaign_attribution.TestApprovalStatusGate -v2` reports "Ran 8 tests" (or more if the record cases were split into a sibling class, in which case both classes together report at least 8) and exits 0.
    - `grep -c 'PENDING_REVIEW' solsys_code/tests/test_campaign_attribution.py` prints at least 2 — the D-01 "pending stays eligible" control exists for both the event and the record path.
    - `grep -c 'is_offered_candidate' solsys_code/tests/test_campaign_attribution.py` prints at least 1.
    - `grep -c 'orphan_calendar_events' solsys_code/tests/test_campaign_attribution.py` prints at least 1 inside the confirmed-link-survival test.
    - `grep -c 'SiderealTargetFactory' solsys_code/tests/test_campaign_attribution.py` equals `grep -c 'NonSiderealTargetFactory' solsys_code/tests/test_campaign_attribution.py` — every match is the non-sidereal factory (the sidereal name is a strict substring of the non-sidereal one, so equal counts prove no bare sidereal factory was introduced).
    - The five-module regression command above exits 0.
  </acceptance_criteria>
  <done>All eight behaviors above are pinned by named tests that pass; the record path, the PENDING_REVIEW control, the server-side re-derivation and the confirmed-link survival are each covered.</done>
</task>

<task type="auto">
  <name>Task 3: Paired docs — runbook attribution paragraph and the notebook's fifth, rejected submission</name>
  <files>docs/runbooks/telescope_runs_calendar.rst, docs/notebooks/pre_executed/campaign_lifecycle_demo.ipynb</files>
  <read_first>
    - `docs/runbooks/telescope_runs_calendar.rst` lines 187-215 — the attribution section and
      the bold-lead-in paragraph style to match. Lines 292-357 (the source-lock section) are
      already correct and are explicitly out of scope; do not re-edit them.
    - `docs/notebooks/pre_executed/campaign_lifecycle_demo.ipynb` cells 5-6 (the demo-scoped
      reset and its `DEMO_CAMPAIGN_NAME` / `DEMO_LEGACY_EVENT_URL_PREFIX` constants), cells
      9-10 (the four public submissions and the `submitted_runs` dict), cells 19-22 (the
      orphan event, the candidate print, and the attribution confirm), cell 24 (the four-way
      payoff, which iterates an explicit four-tuple list) and cell 26 (the public campaign
      table, whose `visible_pks` assertion is an exact set equality).
    - `solsys_code/campaign_views.py` around line 142-148 — `CampaignRunTableView` excludes
      only `PENDING_REVIEW` from the public table, so a REJECTED run IS publicly listed. This
      is why cell 26's set-equality assertion has to be widened, not left alone.
    - `.planning/phases/30-v2-2-tech-debt-cleanup/30-CONTEXT.md` — D-12 and the Specific Ideas
      note about labelling the fifth run visibly.
  </read_first>
  <action>
    **Runbook (D-12).** In the attribution section of
    `docs/runbooks/telescope_runs_calendar.rst`, immediately after the existing paragraph
    that begins `**The two worklists, and why an orphan may be absent.**`, add a new
    bold-lead-in paragraph in the same plain-English style, starting with the exact sentence
    `**A rejected run is never offered as a match.**`. The paragraph must state all four
    facts: a run a staff member rejected in the approval queue is never suggested for any
    orphan; an approved run and a still-pending submission both remain offerable (a pending
    submission being suggested is useful evidence that the submission is genuine); the filter
    is applied once, where candidates are chosen, so the queue, the campaign-list "N orphans
    awaiting attribution" banner count and the calendar modal's staff hint all agree; and an
    attribution a staff member already confirmed stays confirmed if the run is rejected
    afterwards. Do not add a new section heading — this extends an existing answer.

    **Notebook (D-12).** Insert a new markdown cell and a new code cell between the existing
    orphan-event cell (the one that creates `orphan_event` and prints its candidates) and the
    existing "Confirm the attribution" markdown cell, so the rejected run is demonstrated
    while the event is still an orphan.

    The markdown cell explains, in the notebook's existing voice, that a fifth submission
    exists ONLY to demonstrate the attribution exclusion and is deliberately not part of the
    four-run lifecycle narrative the rest of the notebook follows; that it is submitted
    through the same public form as the other four and therefore arrives as
    `pending_review`; and that the cell below shows it as a scored candidate first and absent
    second, so the exclusion is visibly about `approval_status` and nothing else.

    The code cell must, in this order:
    1. Submit a fifth run via `public_client.post(reverse('campaigns:submit'), ...)` with
       `campaign=campaign.pk`, `telescope_instrument='Y21 0.4m/SBIG-STX16803'` (matching the
       orphan event's telescope and instrument so it scores highly),
       `site_raw='Y21'`, `obs_date='2026-09-01 to 2026-09-02'` (a window that overlaps the
       orphan event but differs from the classical run's `2026-09-01 to 2026-09-03`, so the
       two rows do not collide on the campaign+telescope_instrument+window natural key), and
       the module-level `CONTACT_PERSON`/`CONTACT_EMAIL` already defined earlier in the
       notebook. Assert the 302, then look the run back up by its natural key exactly as the
       four-submission cell does. Bind it to a NEW variable named `rejected_demo_run` and do
       NOT add it to the `submitted_runs` dict — the approve-all cell and the four-way payoff
       cell both iterate over those four runs and must stay untouched.
    2. Print the candidate list for `orphan_event` while `rejected_demo_run` is still
       `pending_review`, and assert its pk IS present — the non-vacuous control that proves
       the exclusion below is about the rejection, not about the fixture.
    3. `staff_client.post(reverse('campaigns:decide', args=[rejected_demo_run.pk]),
       {'action': 'reject'})`, assert the 302, `refresh_from_db()`, and assert
       `approval_status == CampaignRun.ApprovalStatus.REJECTED`.
    4. Print the candidate list again and assert `rejected_demo_run.pk` is NOT present while
       `classical_run.pk` still IS — the exclusion is targeted, not a blanket emptying.

    Every print line for this run must carry a visible label such as
    `Rejected demo run (5th -- exists only for the attribution-exclusion demo)` so a reader
    of the executed output cannot mistake it for part of the four-run narrative.

    **Widen the public-table assertion.** The public campaign-table cell asserts exact set
    equality of the four visible run pks. Because the public table excludes only
    `PENDING_REVIEW`, the newly-rejected run is publicly listed and that assertion would now
    fail. Add `rejected_demo_run.pk` to that expected set and extend the cell's markdown to
    say why it is there: rejection removes a run from attribution candidacy, not from the
    public table. Also extend the closing Summary markdown cell, which currently says the
    notebook covers one campaign and four `CampaignRun`s, to name the fifth demo run and what
    it demonstrates.

    Finally regenerate the notebook in place with
    `jupyter nbconvert --to notebook --execute --inplace docs/notebooks/pre_executed/campaign_lifecycle_demo.ipynb`
    and commit it WITH its output — `docs/notebooks/pre_executed/` is the documented exception
    to this repo's pre-commit notebook-output-clearing convention.
  </action>
  <verify>
    <automated>jupyter nbconvert --to notebook --execute --inplace docs/notebooks/pre_executed/campaign_lifecycle_demo.ipynb</automated>
    <fails_when>non-zero exit, which for nbconvert means a cell raised — most likely an AssertionError from the widened public-table set equality or from the candidate-presence control</fails_when>
    <automated>python -c "import json; nb=json.load(open('docs/notebooks/pre_executed/campaign_lifecycle_demo.ipynb')); cells=[c for c in nb['cells'] if c['cell_type']=='code' and 'rejected_demo_run' in ''.join(c['source'])]; assert cells, 'no code cell references rejected_demo_run'; assert all(c.get('outputs') for c in cells), 'a rejected_demo_run cell carries no executed output'; print('rejected-demo code cells', len(cells), 'all with output')"</automated>
    <fails_when>non-zero exit — either no code cell references the new run variable (the cell was never added) or a cell that does has an empty outputs list (committed without executed output)</fails_when>
    <automated>grep -c 'A rejected run is never offered as a match' docs/runbooks/telescope_runs_calendar.rst</automated>
    <fails_when>prints 0 — the runbook paragraph required by ROADMAP criterion 6 is absent</fails_when>
    <automated>python -m sphinx -b html docs docs/_build/html -q 2>&1 | tail -20</automated>
    <fails_when>non-zero exit, or any line in the tail output names telescope_runs_calendar.rst (a docutils warning on the page just edited)</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'A rejected run is never offered as a match' docs/runbooks/telescope_runs_calendar.rst` prints 1.
    - `sed -n '292,357p' docs/runbooks/telescope_runs_calendar.rst` is unchanged from its pre-task content — the source-lock section was not re-edited.
    - `grep -c 'rejected_demo_run' docs/notebooks/pre_executed/campaign_lifecycle_demo.ipynb` prints at least 6 (submission, pending-candidate control, reject POST, post-reject assertion, public-table set, print labels).
    - `python -c "import json; nb=json.load(open('docs/notebooks/pre_executed/campaign_lifecycle_demo.ipynb')); s=''.join(''.join(c['source']) for c in nb['cells']); assert \"submitted_runs['\" not in s.split('rejected_demo_run')[1][:400]; print('ok')"` exits 0 — the fifth run was not added to the four-run `submitted_runs` dict.
    - `jupyter nbconvert --to notebook --execute --inplace docs/notebooks/pre_executed/campaign_lifecycle_demo.ipynb` exits 0, and `git diff --stat docs/notebooks/pre_executed/campaign_lifecycle_demo.ipynb` afterwards shows the file changed (output was regenerated, not left stale).
    - `python -m sphinx -b html docs docs/_build/html -q` exits 0.
  </acceptance_criteria>
  <done>The runbook's attribution section states that a rejected run is never offered (and that approved and pending runs still are); the notebook carries a fifth, visibly-labelled rejected submission shown as a candidate before rejection and absent after, with real executed output committed; the public-table cell and the Summary cell both account for it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public submitter -> `CampaignRun` rows | Anonymous free text (`telescope_instrument`, `contact_person`, `comments`) enters the database through `campaigns:submit` with no authentication, then reaches staff-facing surfaces |
| staff browser -> `campaigns:attribution_decide` POST | Staff-supplied `orphan_pk` / `run_pk` cross into a write that creates a permanent association |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-01 | Information Disclosure | `_eligible_runs_for_event` / `_eligible_runs_for_record` -> attribution queue and `templatetags/attribution_display_extras.py` high-band hint | low | mitigate | Task 1's `.exclude()` at both gates: a submission a staff member rejected (the disposal path for spam and bad-faith entries) stops being re-surfaced as a suggested match on every calendar-modal render |
| T-30-02 | Tampering | `campaigns:attribution_decide` POST, via `campaign_attribution.is_offered_candidate()` | medium | mitigate | The view re-derives eligibility server-side through `candidates_for_event`/`candidates_for_record`, which route through the changed gates; Task 2's `test_is_offered_candidate_refuses_a_rejected_run` pins that a crafted POST naming a rejected run's pk is refused rather than trusted |
| T-30-03 | Elevation of Privilege | attribution queue view (`StaffRequiredMixin`) | low | accept | Staff-only gating is pre-existing and untouched by this plan; the change narrows what staff are offered and adds no new route or permission |
| T-30-04 | Denial of Service | `unattributable_orphan_count()` full-scan over orphans | low | accept | Pre-existing cost, unchanged in shape by this plan; the filter can only reduce the per-orphan candidate set, never enlarge it |
| T-30-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no packages and modifies no dependency manifest — `files_modified` contains two Python source paths and two documentation paths only. `jupyter` and `sphinx` are already-installed dev dependencies invoked, not added |
</threat_model>

<verification>
1. `python manage.py test solsys_code.tests.test_campaign_attribution -v2` exits 0 with the new `TestApprovalStatusGate` class reporting a non-zero test count.
2. `python manage.py test solsys_code.tests.test_campaign_attribution solsys_code.tests.test_campaign_attribution_views solsys_code.tests.test_attribution_dismissals solsys_code.tests.test_attribution_template solsys_code.tests.test_campaign_approval -v1` exits 0 — every downstream consumer of the two gates still passes.
3. `grep -c 'approval_status__in=_ATTRIBUTION_INELIGIBLE_APPROVAL_STATUSES' solsys_code/campaign_attribution.py` prints 2 (D-02: both gates, not one).
4. `grep -c 'A rejected run is never offered as a match' docs/runbooks/telescope_runs_calendar.rst` prints 1.
5. `jupyter nbconvert --to notebook --execute --inplace docs/notebooks/pre_executed/campaign_lifecycle_demo.ipynb` exits 0 and the committed notebook's rejected-run cells carry outputs.
6. `python -m sphinx -b html docs docs/_build/html -q` exits 0.
7. `git show --stat` for this plan's commits lists only the four paths in `files_modified` — no repo-wide reformat (D-05).

Note on the whole-suite run: `solsys_code.tests.test_views.TestEphemeris` segfaults in native
ASSIST, so a blanket `python manage.py test solsys_code` is deliberately NOT used as a gate
here. The five-module command in check 2 is the regression net for this plan's blast radius.
</verification>

<success_criteria>
- ROADMAP criterion 1 holds: a REJECTED run is never offered for an orphan `CalendarEvent` or an orphan `ObservationRecord`, while APPROVED and PENDING_REVIEW runs both still are, enforced at the two eligibility gates; and an association confirmed before the rejection is left intact.
- ROADMAP criterion 6 holds for both paired artifacts: the runbook states the exclusion, and the notebook demonstrates it in real executed output.
- Every assertion above is proven by a named test or a named command, not by inspection.
</success_criteria>

## Artifacts this phase produces

Symbols and files created by THIS plan (newly created — not pre-existing, so drift
verification should not expect to find them in the current tree):

**`solsys_code/campaign_attribution.py`**
- `_ATTRIBUTION_INELIGIBLE_APPROVAL_STATUSES` — new module-level `frozenset` constant

**`solsys_code/tests/test_campaign_attribution.py`**
- `TestApprovalStatusGate` — new `TestCase` class, with:
  - `test_rejected_run_never_offered_for_event`
  - `test_approved_run_still_offered_for_event`
  - `test_rejected_run_never_offered_for_record`
  - `test_approved_run_still_offered_for_record`
  - `test_pending_review_run_still_offered_for_event`
  - `test_pending_review_run_still_offered_for_record`
  - `test_is_offered_candidate_refuses_a_rejected_run`
  - `test_confirmed_attribution_survives_its_run_being_rejected`

**`docs/runbooks/telescope_runs_calendar.rst`**
- A new bold-lead-in paragraph beginning `**A rejected run is never offered as a match.**` in the existing attribution section (no new section heading)

**`docs/notebooks/pre_executed/campaign_lifecycle_demo.ipynb`**
- One new markdown cell and one new code cell inserted between the orphan-event cell and the "Confirm the attribution" cell
- `rejected_demo_run` — new notebook-level variable (deliberately NOT a member of `submitted_runs`)

<output>
Create `.planning/phases/30-v2-2-tech-debt-cleanup/30-01-SUMMARY.md` when done.
</output>