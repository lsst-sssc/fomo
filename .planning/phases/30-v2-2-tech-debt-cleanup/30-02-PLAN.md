---
phase: 30-v2-2-tech-debt-cleanup
plan: 02
type: execute
wave: 2
depends_on: ["30-01"]
files_modified:
  - pyproject.toml
  - CLAUDE.md
  - solsys_code/campaign_reconciler.py
  - .planning/phases/26-canonical-record-spike/26-DECISION.md
autonomous: true
requirements: [D-05, D-06, D-07, D-10]

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-06: pyproject.toml's dev extra pins ruff to the same version .pre-commit-config.yaml pins, so a fresh `pip install '.[dev]'` cannot reproduce the 0.15.20 drift Phases 26, 27 and 27.1 each logged (ROADMAP criterion 3)"
    - "D-07: CLAUDE.md documents the lint and format gates as pre-commit-mediated invocations, so a developer following the documented command gets the pinned version — no bare, unpinned ruff invocation is documented anywhere in CLAUDE.md (ROADMAP criterion 3)"
    - "D-05: both gates pass under the pinned version with no file reformatted — the hooks run clean rather than rewriting the tree (ROADMAP criterion 3)"
    - "D-10: solsys_code/campaign_reconciler.py's docstrings no longer name functions that were deleted from the codebase; where the historical fact still explains a current choice, the fact is kept and the dead symbol name is replaced by a description"
    - "D-10: 26-DECISION.md's header preamble accounts for all five plans that built it (26-01 through 26-05) and no longer describes the document as in progress"
  artifacts:
    - pyproject.toml
    - CLAUDE.md
    - solsys_code/campaign_reconciler.py
    - .planning/phases/26-canonical-record-spike/26-DECISION.md
  key_links:
    - ".pre-commit-config.yaml ruff-pre-commit rev (v0.2.1) -> pyproject.toml dev extra pin -> the version a fresh dev environment installs"
    - "CLAUDE.md §Commands lint block -> `pre-commit run <hook-id>` -> the same pinned binary pre-commit already enforces at commit time"
  prohibitions:
    - statement: "No repo-wide reformat (D-05): this plan must not run a formatter or an autofix pass across the tree. The repo is already clean under the pinned version; formatting under 0.15.20 would commit a diff the pinned hook never asked for."
      status: enforced
      verification: "`pre-commit run ruff --all-files` and `pre-commit run ruff-format --all-files` both exit 0 without reporting that files were modified, and `git show --stat` for this plan's commits lists only the four paths in files_modified"
---

<objective>
Fix the root cause behind three phases (26, 27, 27.1) independently logging and deferring the
same phantom ruff drift, and clear the two cosmetic bookkeeping items that would otherwise be
re-flagged at milestone close.

The drift was never dirty code. Under the pinned pre-commit ruff (`v0.2.1`) the repo reports
0 lint errors and 89/89 files already formatted; the findings came from executors following
CLAUDE.md's documented command, which invokes whatever unpinned `ruff` the dev environment
happens to hold — 0.15.20 in this case. So the fix is two places: pin the dev dependency
(D-06) and correct the documented invocation (D-07). Explicitly NOT a cleanup pass (D-05).

Purpose: stop the same non-finding being rediscovered every phase, and stop
`/gsd-complete-milestone` reading stale bookkeeping.
Output: a pinned ruff dev dependency, a pre-commit-mediated lint/format instruction, five
repaired docstrings in `campaign_reconciler.py`, and a corrected `26-DECISION.md` header.
</objective>

<execution_context>
@/home/tlister/git/fomo_devel/.claude/gsd-core/workflows/execute-plan.md
@/home/tlister/git/fomo_devel/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-v2-2-tech-debt-cleanup/30-CONTEXT.md
@.planning/phases/30-v2-2-tech-debt-cleanup/30-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin ruff in the dev extra and route CLAUDE.md's documented gate through pre-commit</name>
  <files>pyproject.toml, CLAUDE.md</files>
  <precondition>`pre-commit` is on PATH and its hook environments are installed (`pre-commit --version` succeeds), since the verification runs the hooks rather than a bare binary.</precondition>
  <read_first>
    - `.pre-commit-config.yaml` lines 48-62 — the two `astral-sh/ruff-pre-commit` blocks. Read
      BOTH `rev:` values and BOTH hook `id:` values verbatim; the pin you write and the hook
      names you document must match these exactly, and `pre-commit run <id>` requires an exact
      id match. Note the lint hook is `types_or: [python, pyi]` while the format hook adds
      `jupyter` — the two hooks cover different file sets, so both must be documented.
    - `pyproject.toml` lines 34-44 — the `[project.optional-dependencies] dev` list and the
      per-entry trailing-comment style used by its neighbours.
    - `CLAUDE.md` lines 27-36 — the `## Commands` fenced block, specifically the two-line lint
      block under its `# Lint / format` comment.
    - `CLAUDE.md` §Project > Constraints — the "Testing" bullet, whose last sentence names the
      quality gates as bare invocations.
  </read_first>
  <action>
    In `pyproject.toml`, replace the bare `ruff` entry in the `dev` extra with an exact pin
    equal to the version `.pre-commit-config.yaml` pins for `astral-sh/ruff-pre-commit`,
    written without the leading `v` (a PEP 440 specifier, not a git tag). Keep the entry's
    position in the list and its trailing-comment style; update the comment text to say the
    pin exists to match the pre-commit hook, naming `.pre-commit-config.yaml` as the source of
    truth, and cite `D-06`. Do not pin or otherwise touch any other entry in the list.

    In `CLAUDE.md` §Commands, replace the two bare invocations in the lint block with the
    pre-commit-mediated equivalents, one line per hook, using the exact hook ids read from
    `.pre-commit-config.yaml` and the `--all-files` flag. Rewrite the block's `#` comment so it
    states the reason a reader needs: pre-commit owns the pinned ruff, so running the tool
    directly can report findings the enforced gate does not have; run it this way and the
    documented command and the enforced version are the same thing. Keep the single-quotes /
    120-column style note that already lives on that comment line.

    Then update the "Testing" bullet under §Project > Constraints so it names the same
    pre-commit-mediated gate rather than a bare invocation. This is the second place a reader
    is told how to check lint and format, and leaving a bare invocation there would leave the
    exact hazard D-07 exists to remove.

    Do NOT run a formatter or an autofix pass over the repository (D-05). The repository is
    already clean under the pinned version; a pass under the environment's newer, unpinned
    binary would commit a diff the pinned hook never asked for. Do NOT install, upgrade or
    downgrade any package: the pin is a declaration for the next fresh environment, and
    pre-commit already manages its own isolated hook environment at the pinned version, so
    nothing needs installing for the verification below to be meaningful.

    <!-- planner-discipline-allow: ruff (check|format) \. -->
  </action>
  <verify>
    <automated>grep -n 'ruff' pyproject.toml</automated>
    <fails_when>the printed dev-extra line still has no version specifier, or its specifier does not equal the .pre-commit-config.yaml rev with the leading "v" stripped</fails_when>
    <automated>test "$(grep -oP '(?<=ruff==)[0-9.]+' pyproject.toml | head -1)" = "$(grep -A2 'astral-sh/ruff-pre-commit' .pre-commit-config.yaml | grep -oP '(?<=rev: v)[0-9.]+' | head -1)" && echo PIN_MATCHES</automated>
    <fails_when>non-zero exit or "PIN_MATCHES" absent from the output — the pyproject pin and the pre-commit rev disagree, which is exactly the drift D-06 closes</fails_when>
    <automated>grep -cE 'ruff (check|format) \.' CLAUDE.md</automated>
    <fails_when>prints anything other than 0 — a bare, unpinned invocation is still documented somewhere in CLAUDE.md</fails_when>
    <automated>grep -c 'pre-commit run ruff --all-files' CLAUDE.md</automated>
    <fails_when>prints 0 — the replacement lint invocation is absent</fails_when>
    <automated>pre-commit run ruff --all-files && pre-commit run ruff-format --all-files</automated>
    <fails_when>non-zero exit from either hook, or "files were modified by this hook" appears in the output (which would mean the pinned version did want a reformat after all, contradicting the D-05 premise)</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -oP '(?<=ruff==)[0-9.]+' pyproject.toml` prints `0.2.1`, matching `.pre-commit-config.yaml`'s `rev: v0.2.1`.
    - `grep -cE 'ruff (check|format) \.' CLAUDE.md` prints 0.
    - `grep -c 'pre-commit run ruff --all-files' CLAUDE.md` prints at least 1 and `grep -c 'pre-commit run ruff-format --all-files' CLAUDE.md` prints at least 1.
    - `grep -c 'D-06' pyproject.toml` prints at least 1 — the pin cites the decision that put it there.
    - `pre-commit run ruff --all-files` exits 0 and `pre-commit run ruff-format --all-files` exits 0.
    - `git status --porcelain` after those two hook runs lists no file this plan did not intend to change — no file was reformatted to make the gates pass.
    - `git diff --name-only` for this task lists exactly `pyproject.toml` and `CLAUDE.md`.
  </acceptance_criteria>
  <reversibility rating="reversible">Documentation and a dependency specifier; no code path depends on either (carried through from 30-CONTEXT.md's D-07 rating).</reversibility>
  <done>`pyproject.toml` pins ruff to the pre-commit rev, CLAUDE.md documents both gates through pre-commit in both places it mentions them, both hooks pass under the pinned version, and no source file was reformatted.</done>
</task>

<task type="auto">
  <name>Task 2: Repair the five stale docstring references in campaign_reconciler.py</name>
  <files>solsys_code/campaign_reconciler.py</files>
  <read_first>
    - `solsys_code/campaign_reconciler.py` at lines 72-84 (`ReconcileResult`), 96-108
      (`run_night_url`), 168-178 (`event_title`), 190-200 (`_skip_reason`) and 333-345
      (`_reconcile_classical_nights`) — the five docstrings holding a reference to a function
      that no longer exists. Read each in full: some references carry a load-bearing
      historical fact and some carry none, and the two cases get different treatment.
    - `solsys_code/management/commands/reconcile_campaign_runs.py` around line 17 — the ONE
      mention that is deliberately kept, because it explains why this module exists at all
      (the retired command imported a private helper out of the views module). This file is
      out of scope and must not be edited.
    - `.planning/phases/30-v2-2-tech-debt-cleanup/30-PATTERNS.md` — the per-line notes for
      each of the five sites.
  </read_first>
  <action>
    <!-- planner-discipline-allow: _project_calendar_event -->
    <!-- planner-discipline-allow: _calendar_event_title -->

    Five docstrings in `solsys_code/campaign_reconciler.py` name `_project_calendar_event()`
    or `campaign_views._calendar_event_title()`. Both functions were deleted in Phase 29, so a
    reader who greps for either name finds nothing and cannot check the claim. Repair all five
    with prose only — no code, no signatures, no behaviour changes.

    Apply this rule to each site: if the historical fact still explains a CURRENT choice, keep
    the fact and replace the dead symbol name with a description such as "the retired
    pre-reconciler projection helper in `campaign_views`". If the reference only compares the
    present code to something a reader can no longer see, drop the comparison and state the
    present behaviour directly.

    Site by site:
    - `ReconcileResult`'s docstring: the comparison adds nothing a reader can verify. State
      directly that `skipped_reason is None` is this module's success signal, and keep the
      existing sentence about `_resolve_site()` (plan 29-04) using it to pick between its two
      success messages (D-04).
    - `run_night_url`'s docstring: the historical fact IS load-bearing — it explains why the
      per-night key is date-bearing even for a single-night run. Keep the fact (the retired
      helper used the bare key when `n_nights == 1`) and the 26-DECISION.md citation; replace
      only the dead symbol name.
    - `event_title`'s docstring: "byte-identical to" a deleted function is an unverifiable
      claim. Restate it as the requirement it really is — the title must keep the terminal
      cancelled/weathered prefix form that `calendar_display_extras`' terminal-prefix ring
      matches on.
    - `_skip_reason`'s docstring: keep the fact that the pre-reconciler "no event yet" cases
      are preserved, and keep the two additions already listed (the approval gate and the
      29-REVIEW.md WR-02 window-integrity guard); replace only the dead symbol name.
    - `_reconcile_classical_nights`'s docstring: keep the description of the ground per-night
      loop and the D-06 note about `sun_event()`'s `ValueError` propagating uncaught; replace
      only the dead symbol name.

    Do not touch `reconcile_campaign_runs.py` — its single mention is the one clear historical
    reference the codebase keeps on purpose, and it lives in a file this plan does not own.
  </action>
  <verify>
    <automated>python manage.py test solsys_code.tests.test_campaign_reconciler solsys_code.tests.test_reconcile_campaign_runs -v1</automated>
    <fails_when>non-zero exit, or "OK" absent from the final line — a prose-only docstring edit changed behaviour, which means something other than prose was edited</fails_when>
    <automated>grep -cE '_project_calendar_event|_calendar_event_title' solsys_code/campaign_reconciler.py</automated>
    <fails_when>prints anything other than 0 — at least one of the five dead symbol references survived</fails_when>
    <automated>grep -cE '_project_calendar_event' solsys_code/management/commands/reconcile_campaign_runs.py</automated>
    <fails_when>prints 0 — the one deliberately-kept historical mention was deleted too, which is out of D-10's scope</fails_when>
    <automated>pre-commit run ruff --all-files</automated>
    <fails_when>non-zero exit, or "files were modified by this hook" appears in the output — an over-long docstring line broke the 120-column limit</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -cE '_project_calendar_event|_calendar_event_title' solsys_code/campaign_reconciler.py` prints 0.
    - `grep -cE '_project_calendar_event' solsys_code/management/commands/reconcile_campaign_runs.py` prints 1 — the deliberately-kept mention is intact.
    - `git diff -U0 solsys_code/campaign_reconciler.py` shows changes only on lines inside triple-quoted docstrings — no added, removed or altered executable statement.
    - `grep -c '26-DECISION.md' solsys_code/campaign_reconciler.py` is unchanged from its pre-task value — the load-bearing citations survived the rewrite.
    - `python manage.py test solsys_code.tests.test_campaign_reconciler solsys_code.tests.test_reconcile_campaign_runs -v1` exits 0.
  </acceptance_criteria>
  <done>No docstring in `campaign_reconciler.py` names a function that no longer exists; every historical fact that explains a current choice is preserved in words a reader can act on; the reconciler's tests still pass and no executable line changed.</done>
</task>

<task type="auto">
  <name>Task 3: Correct 26-DECISION.md's header preamble</name>
  <files>.planning/phases/26-canonical-record-spike/26-DECISION.md</files>
  <read_first>
    - `.planning/phases/26-canonical-record-spike/26-DECISION.md` lines 1-20 — the header
      preamble to correct, including its per-plan sentence listing which plan contributed
      which section.
    - `.planning/ROADMAP.md` §"Phase 26: Canonical-Record Spike", the "Plans:" list — the
      authoritative one-line description of what each of the five plans contributed,
      including 26-04 (the queue-versus-classical inventory, the `campaign_gap.claimed_dates()`
      over-claim, and the three-way span / none / per-night comparison) and 26-05 (the
      queue-run projection decision and the amended Criterion 3 / Domain-correction sections).
    - `.planning/phases/26-canonical-record-spike/26-05-SUMMARY.md` — confirm which sections of
      26-DECISION.md plan 26-05 actually amended before writing the sentence that says so.
  </read_first>
  <action>
    The document's header still reads `**Status:** In progress.` and says it was built up
    across plans 26-01, 26-02 and 26-03 — it was never amended when the phase's gap-closure
    waves added 26-04 and 26-05. Phase 26 is complete (5/5 plans) and the milestone audit
    lists this as an open bookkeeping item (D-10).

    Change the `**Status:**` value from in-progress to complete, and extend the per-plan
    sentence so it names all five plans and what each contributed, in plan order. The existing
    sentences for 26-01, 26-02 and 26-03 are accurate — keep them and append the two missing
    ones rather than rewriting the paragraph: 26-04 closed the SPIKE-03 gap with the
    queue-versus-classical inventory and the three-way queue-run projection comparison, and
    26-05 recorded the queue-run projection decision and amended this document's Criterion 3
    and Domain-correction sections.

    Change the header preamble only. Do not edit any decision, criterion, recommendation or
    durable-summary section below it — the milestone audit's own words are that the content
    itself is current, and this item is cosmetic.
  </action>
  <verify>
    <automated>sed -n '1,20p' .planning/phases/26-canonical-record-spike/26-DECISION.md</automated>
    <fails_when>the printed header still contains "In progress", or its plan list stops at 26-03 without naming 26-04 and 26-05</fails_when>
    <automated>grep -c '26-04' .planning/phases/26-canonical-record-spike/26-DECISION.md</automated>
    <fails_when>prints 0 — plan 26-04 is still unaccounted for in the document</fails_when>
    <automated>git diff -U0 .planning/phases/26-canonical-record-spike/26-DECISION.md | grep -c '^@@'</automated>
    <fails_when>prints a number greater than 2 — the edit spilled past the header preamble into the decision sections, which are out of scope</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'In progress' .planning/phases/26-canonical-record-spike/26-DECISION.md` prints 0.
    - `grep -c '26-04' .planning/phases/26-canonical-record-spike/26-DECISION.md` prints at least 1 and `grep -c '26-05' .planning/phases/26-canonical-record-spike/26-DECISION.md` prints at least 1.
    - `git diff -U0 .planning/phases/26-canonical-record-spike/26-DECISION.md | grep -c '^@@'` prints 2 or fewer — the change is confined to the header preamble.
    - `git diff --name-only` for this task lists exactly `.planning/phases/26-canonical-record-spike/26-DECISION.md`.
  </acceptance_criteria>
  <done>26-DECISION.md's header reports the document as complete and names all five plans that built it; nothing below the header changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI / GitHub -> developer environment | `pyproject.toml`'s dev extra and `.pre-commit-config.yaml`'s hook repo both resolve third-party code that then executes on a developer machine and rewrites source files |
| CLAUDE.md instruction -> executing agent | Every GSD executor follows the documented command literally; an instruction naming an unpinned binary is how three phases each ran a different tool than the enforced gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-05 | Tampering | `pyproject.toml` dev extra — an unpinned dependency resolves to whatever the index serves today, including a future release that rewrites source files differently | medium | mitigate | Task 1 pins the entry to an exact version equal to the rev `.pre-commit-config.yaml` already pins, so the two enforcement paths cannot diverge and a silent upstream change cannot alter the formatter's behaviour |
| T-30-06 | Repudiation | The documented lint command vs. the enforced gate — findings could be attributed to the codebase when they came from a different tool version | medium | mitigate | Task 1 routes both documented gates through `pre-commit run`, which resolves the pinned hook environment, so a reported finding is attributable to the version the repo actually enforces |
| T-30-SC | Tampering | npm/pip/cargo installs | low | accept | No package is added and no install is run. The single dependency change narrows an existing, already-in-use entry (`ruff`) from unpinned to an exact version, and that version is corroborated in-repo by `.pre-commit-config.yaml`'s `astral-sh/ruff-pre-commit` rev — an existing source of truth, not a new upstream. Task 1's action forbids running any package install |
| T-30-07 | Information Disclosure | `campaign_reconciler.py` docstrings, `26-DECISION.md` header | low | accept | Prose-only edits to already-committed internal documentation; no new data is exposed and no new reader gains access |
</threat_model>

<verification>
1. `grep -oP '(?<=ruff==)[0-9.]+' pyproject.toml` and `grep -oP '(?<=rev: v)[0-9.]+' .pre-commit-config.yaml | head -1` print the same string.
2. `grep -cE 'ruff (check|format) \.' CLAUDE.md` prints 0; both `pre-commit run` lines are present.
3. `pre-commit run ruff --all-files` and `pre-commit run ruff-format --all-files` both exit 0 and neither reports modifying a file — ROADMAP criterion 3, including its "no file is reformatted to achieve this" clause.
4. `grep -cE '_project_calendar_event|_calendar_event_title' solsys_code/campaign_reconciler.py` prints 0, while the one deliberately-kept mention in `reconcile_campaign_runs.py` survives.
5. `python manage.py test solsys_code.tests.test_campaign_reconciler solsys_code.tests.test_reconcile_campaign_runs -v1` exits 0.
6. `grep -c 'In progress' .planning/phases/26-canonical-record-spike/26-DECISION.md` prints 0 and both 26-04 and 26-05 are named.
7. `git show --stat` for this plan's commits lists only the four paths in `files_modified`.

Paired-docs note (CLAUDE.md rule): `solsys_code/campaign_reconciler.py` is a paired module, but
Task 2 changes prose inside docstrings only — no extraction logic, no parameters, no fixture
shapes. The rule's trigger is a *behaviour* change and explicitly excludes pure refactors and
typo fixes, so `reconcile_campaign_runs_demo.ipynb` and `campaign_lifecycle_demo.ipynb` are
correctly out of scope for this plan. Task 2's `git diff -U0` acceptance criterion is what
proves the docstring-only claim rather than asserting it.
</verification>

<success_criteria>
- ROADMAP criterion 3 holds: both gates pass under the pinned version, a developer following the documented command gets that same version, and no file was reformatted to achieve it.
- The two cosmetic bookkeeping items named in `.planning/v2.2-MILESTONE-AUDIT.md` (the reconciler docstrings, the 26-DECISION.md header) are closed, so plan 30-04 can record them as such.
- Every assertion above is proven by a named command, not by inspection.
</success_criteria>

## Artifacts this phase produces

This plan creates no new code symbols. It modifies existing lines only:

**`pyproject.toml`**
- The `dev` extra's `ruff` entry gains an exact version specifier and a new trailing comment citing D-06

**`CLAUDE.md`**
- The `## Commands` lint block's two invocation lines are replaced with `pre-commit run` equivalents, and its `#` comment is rewritten
- The §Project > Constraints "Testing" bullet's quality-gate sentence is rewritten to name the same pre-commit-mediated gate

**`solsys_code/campaign_reconciler.py`**
- Five existing docstrings are rewritten in prose (no new functions, constants or parameters)

**`.planning/phases/26-canonical-record-spike/26-DECISION.md`**
- The header preamble's `**Status:**` value and per-plan sentence are corrected

<output>
Create `.planning/phases/30-v2-2-tech-debt-cleanup/30-02-SUMMARY.md` when done.
</output>