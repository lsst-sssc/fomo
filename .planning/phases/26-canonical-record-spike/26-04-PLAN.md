---
phase: 26-canonical-record-spike
plan: 04
type: execute
wave: 4
depends_on: ["26-03"]
gap_closure: true
files_modified:
  - .planning/phases/26-canonical-record-spike/26-DECISION.md
scratch_artifacts:
  # Throwaway investigation tooling — gitignored, ALL discarded in task 3 of this same plan.
  # Deliberately NOT in files_modified: none of these is committed, and this plan's real git
  # diff must contain only the single file listed above.
  # NOTE: unlike plans 26-01/26-02 this plan creates NO scratch git branch and edits NO
  # solsys_code/ file — every probe imports the shipped code unchanged. The teardown bar is
  # therefore stricter, not looser: `git diff --quiet -- solsys_code/` must pass at every
  # point in this plan, not only at the end.
  - local_settings.py                              # gitignored (.gitignore:61)
  - tmp/                                           # gitignored (.gitignore:145)
  - tmp/26_queue_inventory_probe.py
  - tmp/26_queue_projection_probe.py
  - tmp/26_calendar_render_probe.py
  - tmp/26-realdb-fingerprint-before.txt
  - tmp/26-queue-inventory.txt
  - tmp/26-queue-projection-counts.txt
  - tmp/26-calendar-render.txt
  - tmp/26-queue-span-copy.sqlite3
  - tmp/26-queue-none-copy.sqlite3
  - tmp/26-queue-pernight-copy.sqlite3
autonomous: true
requirements: [SPIKE-03]
user_setup: []

must_haves:
  truths:
    - "26-DECISION.md carries a measured queue-run projection Finding built against the real `CampaignRun` pk=1 window and its real 11 LCO events: for each candidate projection option, the in-window event count, the full url-key list, the created/updated/unchanged tally, the idempotent re-run result, and the key-stability result under a window change — the same evidence bar D-11 met for the adopt-vs-gap-fill question."
    - "The Finding records at least one concrete, code-level fact that distinguishes the options, cited by file:line against shipped source rather than argued as a preference — matching D-11's `sync_lco_observation_calendar.py:361` + `calendar_utils._update_or_unchanged()` two-writer-churn precedent."
    - "The Finding records, against the real 31 dev-DB `CampaignRun` rows, how many are queue-scheduled, classically scheduled, and space-mission — including the queue-versus-classical split of the 19 RECON-07 3I/ATLAS runs and the run type of pk=29/pk=30 — with the classification tagged as a judgment applied to real field values, not as an executed check."
    - "The Finding records what a calendar reader actually sees under each option, measured from a real `/calendar/?year=2026&month=7` render against the scratch copy, with the number explained by `solsys_code/views.py`'s all-day containment filter and corroborated against a genuinely real multi-day row."
    - "The Finding states whether D-05's 80x5=400 class-wide fan-out figure survives, separating the site-fanout half (already verified) from the per-day half the domain correction reopened."
    - "The repository is left exactly as this plan found it apart from `26-DECISION.md`: the real `src/fomo_db.sqlite3` size+mtime fingerprint is identical before and after, `git diff --quiet -- solsys_code/` passes, no `local_settings.py` or `tmp/` exists on disk, no scratch branch exists, and no scratch path appears in any git ref."
  artifacts:
    - path: ".planning/phases/26-canonical-record-spike/26-DECISION.md"
      provides: "A new `### SPIKE-03 gap closure` Findings subsection carrying the measured queue-run projection evidence, appended after the existing SPIKE-03 criterion-3 Finding and before `## Recommendation`. Every earlier Finding and the whole Recommendation section left byte-unchanged."
      contains: "### SPIKE-03 gap closure"
    - path: "tmp/26-queue-projection-counts.txt"
      provides: "Verbatim per-scenario tallies the Finding's numbers are copied from (DISCARDED in task 3)"
    - path: "tmp/26-calendar-render.txt"
      provides: "Verbatim rendered-calendar occurrence counts per scenario (DISCARDED in task 3)"
  key_links:
    - from: "local_settings.py DATABASES override"
      to: "tmp/26-queue-*-copy.sqlite3"
      via: "settings.DATABASES['default']['NAME'], guard-printed and asserted before every write command"
      pattern: "tmp/26-queue-.*-copy\\.sqlite3"
    - from: "solsys_code/calendar_utils.py insert_or_create_calendar_event()"
      to: "each scenario's minted calendar events"
      via: "imported unchanged by tmp/26_queue_projection_probe.py — no substitute write helper is defined"
      pattern: "insert_or_create_calendar_event"
    - from: "solsys_code/views.py all-day containment filter"
      to: "the measured per-scenario occurrence count in the rendered /calendar/ body"
      via: "django.test.Client() fetch of /calendar/?year=2026&month=7 against the scratch copy"
      pattern: "all_day_events"
---

<objective>
Close the one gap 26-VERIFICATION.md found: SPIKE-03's *"one canonical reconciler event-key
scheme, stable across all four pipeline stages"* claim does not hold for queue-scheduled runs.
This plan produces the **measured evidence** that closure needs — the same kind D-11 produced
for the adopt-vs-gap-fill write-strategy question — against the real dev-DB rows the phase goal
names as its anchor: `CampaignRun` pk=1 (an LCO **queue** run), its 11 real LCO calendar events,
and the 19 unprojected 3I/ATLAS runs.

Three candidate answers to "does a queue-scheduled run project onto the calendar at all, and
under what key" are built and counted against real data. Plan 26-05 turns that evidence into a
locked verdict and publishes it.

Still **investigation-only**, and more strictly than plans 26-01..03: this plan creates **no**
scratch git branch and edits **no** `solsys_code/` file. Every probe imports the shipped code
unchanged. The sole committed artifact is a new Findings subsection in `26-DECISION.md`.

Purpose: give Phase 29 a settled decision for queue runs instead of a second open question, and
give plan 26-05 real numbers to lock a verdict against rather than an abstract argument.
Output: `26-DECISION.md` with a `### SPIKE-03 gap closure` Findings subsection; a repository
provably identical to its pre-plan state apart from that one file.
</objective>

<execution_context>
@/home/tlister/git/fomo_devel/.claude/gsd-core/workflows/execute-plan.md
@/home/tlister/git/fomo_devel/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/26-canonical-record-spike/26-CONTEXT.md
@.planning/phases/26-canonical-record-spike/26-VALIDATION.md
@.planning/phases/26-canonical-record-spike/26-VERIFICATION.md
@.planning/phases/26-canonical-record-spike/26-DECISION.md
@.planning/phases/26-canonical-record-spike/26-03-SUMMARY.md
</context>

<phase_nature_note>
No-shippable-code investigation spike, in gap-closure mode. The usual "add a test for the new
behavior" framing does not apply: this plan changes no shipped `solsys_code/` behavior, so there
is nothing for a permanent unit test to assert against. Verification is **evidence-shaped** — a
command's real exit status, a verbatim printed output block, a per-scenario event count, an HTTP
status plus an occurrence count, and a before/after fingerprint of the real dev DB.

**CLAUDE.md paired-docs rule:** ROADMAP.md records "Paired docs: None" for Phase 26 —
investigation only, no module behavior changes — so no demo notebook and no `docs/runbooks/`
page is triggered. Do not regenerate any notebook. The `docs/design/` page is a phase deliverable
in its own right and is amended by plan 26-05, not here.

**Explicitly out of scope for this plan** (all four confirmed by 26-VERIFICATION.md):
SPIKE-01, SPIKE-02 and SPIKE-04 are independently verified against live source and stay ticked —
do not re-do or re-litigate them. The class-wide **site-fanout** half of SPIKE-03 ("a single
class-wide event per day, not one event per candidate site") is VERIFIED against the real
`SITE_TELESCOPE_MAP` at `solsys_code/calendar_utils.py:36-53` — do not re-measure it; this plan
only records whether the *per-day* half of D-05's 400-event figure survives. D-11's
adopt-vs-gap-fill deferral was judged acceptable and explicitly **not** a gap — leave it deferred
to Phase 29, unamended.
</phase_nature_note>

<scratch_discipline>
Four rules the executor must hold for the whole plan:

1. **Task 1 runs against the REAL `src/fomo_db.sqlite3`, read-only, with NO `local_settings.py`
   present.** Write `local_settings.py` only in task 2, only after task 1's fingerprint is
   recorded.
2. **From task 2 onward, print `settings.DATABASES['default']['NAME']` before every write-capable
   command** and stop if it does not end in the intended `tmp/26-queue-*-copy.sqlite3` filename
   (26-RESEARCH.md Pitfall 1 — the single highest-value guard in the procedure; the override is
   invisible at the call site, there is no `--database=scratch` flag).
3. **No `solsys_code/` file may be created, edited, or deleted at any point.** There is no scratch
   branch in this plan and none may be created. `git diff --quiet -- solsys_code/` must pass
   before task 1, between every task, and after task 3.
4. **No probe may import `solsys_code.views`, `solsys_code.ephem_utils`, or
   `solsys_code.campaign_views`.** The first two trigger a ~1.6 GB SPICE kernel download at module
   load (CLAUDE.md "Heavy import side effect"); the third is a views module, which CONTEXT.md's
   locked constraints forbid reconciler-shaped logic from importing. Probes import
   `solsys_code.calendar_utils`, `solsys_code.models`, `solsys_code.campaign_gap` and
   `solsys_code.solsys_code_observatory.models` only, and reproduce any needed helper's output
   format inline with a file:line citation to the original.
</scratch_discipline>

<terminology_note>
CLAUDE.md's planning-doc terminology rule applies to everything written into `26-DECISION.md`:
plain English over database jargon. Write "create or update" / "find-or-create" / "create the
record if missing, otherwise update it in place" rather than "upsert", and "model" / "data model"
rather than "shape". The word "upsert" must not appear in the file.
</terminology_note>

<tasks>

<task type="auto">
  <name>Task 1: Measure the queue-versus-classical run inventory and the existing over-claim, read-only against the real dev DB</name>
  <files>.planning/phases/26-canonical-record-spike/26-DECISION.md, tmp/26_queue_inventory_probe.py, tmp/26-queue-inventory.txt, tmp/26-realdb-fingerprint-before.txt</files>
  <read_first>
    - .planning/phases/26-canonical-record-spike/26-VERIFICATION.md (the YAML `gaps:` block in full — `truth`, `reason`, `artifacts`, and the `missing` item naming option (b) "a short closure plan that produces the same kind of measured evidence for queue-run projection that D-11 produced for the write-strategy question")
    - .planning/phases/26-canonical-record-spike/26-DECISION.md (the `### Snapshot (D-04)` Finding for the pinned figures and the `946176 1785094461` fingerprint; the `### Domain correction — queue windows are not sets of owned nights` section, all six consequences; the `#### D-11` subsection — this is the evidence bar to match)
    - solsys_code/campaign_gap.py (lines 116-210 — `claimed_dates()`; specifically the ground branch at the end of the loop that adds every date in the inclusive `[window_start, window_end]` range to `claimed`, and the `is_space_mission` / `pending_narrowing_runs` branch above it)
    - solsys_code/models.py (lines 60-160 — `CampaignRun`'s real field set: `telescope_instrument`, `site`, `site_raw`, `window_start`, `window_end`, `approval_status`, `run_status`, and the two partial unique constraints; note `contact_person`/`contact_email` exist and must never be selected)
    - solsys_code/calendar_utils.py (lines 36-53 — `SITE_TELESCOPE_MAP`, the source of the already-verified 5-site `1m0` / 2-site `2m0` counts behind D-05)
    - .planning/phases/26-canonical-record-spike/26-01-PLAN.md (task 1 — the read-only-probe discipline, the fingerprint-first step, and the "never select contact_person/contact_email" rule this task reuses verbatim)
  </read_first>
  <action>
    Work on the current phase-26 working branch. Create no git branch. Do **not** create
    `local_settings.py` in this task — every command here reads the real `src/fomo_db.sqlite3` and
    must not write to it.

    Step 1 — recreate the tamper-evidence fingerprint (plan 26-03 deleted the original along with
    `tmp/`). Create `tmp/` (already gitignored at `.gitignore:145`), run `stat -c '%s %Y'
    src/fomo_db.sqlite3`, and write the result to `tmp/26-realdb-fingerprint-before.txt`. Compare
    it against the value `946176 1785094461` recorded in `26-DECISION.md`'s Snapshot Finding. If
    it matches, say so. **If it differs, that is itself a finding**: the dev DB has moved since the
    D-04 snapshot, so record the new fingerprint and re-confirm pk=1's window dates, its 11 LCO
    event ids, and the 19-run RECON-07 count in step 2 before any later number in this plan leans
    on them.

    Step 2 — write `tmp/26_queue_inventory_probe.py`, a strictly read-only Django-shell script. It
    must never select, print or reference `CampaignRun.contact_person` or
    `CampaignRun.contact_email`, and must never call `.save()`, `.create()`, `.update()`,
    `.get_or_create()` or `.delete()`. It prints these clearly-labelled blocks:

    (A) `=== Block (A): run-type inventory ===`. For all 31 `CampaignRun` rows, print pk,
        `telescope_instrument`, `site_raw`, the resolved site's obscode (or the literal `None`),
        `approval_status`, `run_status`, `window_start`, `window_end`, and the inclusive window
        length in nights. Then assign each row exactly one of three scheduling categories and
        print the assignment plus a one-clause reason:
        `QUEUE` — the facility schedules observations into a queue across the window rather than
        awarding specific nights (LCO network rows including FTS/FTN/`LCO 1m`/`LCO 2m` and any
        `0m4`/`1m0`/`2m0` aperture-class string; Gemini rows including `GS-`/`GN-` program strings
        and GMOS; SOAR; ESO VLT rows including FORS2);
        `CLASSICAL` — the run owns specific awarded nights at one telescope (the NTT/EFOSC2 and
        Magellan-Baade/IMACS and Magellan-Clay/Lightspeed families, which D-19 already identifies
        as the 9 blank-`url` classical calendar events' telescopes);
        `SPACE` — a space mission with no ground site at all (the pk=8/12/13/21/26 Hubble, HST,
        Swift, JWST and JUICE rows D-06 names).
        Print the three category totals, and print the category of pk=1, pk=29 and pk=30
        individually and by name.
        Label this block a **judgment applied to real field values**, not an executed check: the
        `source` field that would decide this mechanically does not exist yet (it is Phase 27's
        CANON-01), so the categorisation is read off `telescope_instrument`/`site` by a human-
        auditable rule. Printing every row's inputs alongside its assigned category is what makes
        it auditable — do not print only the totals.

    (B) `=== Block (B): RECON-07 split ===`. Recompute the RECON-07 baseline the same way
        `26-DECISION.md`'s RECON-07 Finding did (approved, site-resolved, windowed `CampaignRun`
        rows on the `3I/ATLAS` campaign with no blank-`url`-classical-style calendar presence),
        print the total (expected 19, matching D-20), then print how many of those fall in each of
        block (A)'s three categories, with each row's pk and category listed. This is the figure
        that decides whether queue-run projection is a corner case or the dominant mechanism for
        the roadmap's flagship "19 invisible runs become visible" criterion.

    (C) `=== Block (C): existing over-claim in the shipped gap analysis ===`. Import
        `claimed_dates` from `solsys_code.campaign_gap` and call it read-only with pk=1's own
        campaign, pk=1's target (or `None`), and pk=1's resolved site (obscode `E10`). Print: the
        number of dates it returns as claimed that fall inside pk=1's inclusive window; the list
        of those dates; the 11 site-local nights pk=1's real LCO events actually cover (reuse the
        site-local-night derivation `26-DECISION.md`'s SPIKE-03 Finding already measured — convert
        `start_time` into the site's timezone and take the local calendar date, substituting
        `Australia/Sydney` explicitly and printing that substitution because obscode `E10`'s
        `timezone` field is blank); and the set difference. Expected: `claimed_dates()` claims all
        15 window dates while only 11 were ever scheduled. Print the actual numbers either way.
        Then quote the exact source lines from `solsys_code/campaign_gap.py` responsible (the
        ground branch's `n_days = (run.window_end - run.window_start).days + 1` loop that adds
        every date in the range) with their real line numbers, read from the file rather than
        assumed. **This is the code-level distinguishing fact this closure needs** — it shows the
        same category error the domain correction identified already exists, today, in shipped
        code one module over from the calendar.

    (D) `=== Block (D): D-05 fan-out inputs ===`. Print pk=29's inclusive window length in nights,
        pk=30's, the number of `SITE_TELESCOPE_MAP` sites carrying `1m0` and the number carrying
        `2m0`, and the three arithmetic results a reader needs to judge D-05: per-site-per-night
        (window x sites), per-day (window), and whole-window span (1). Label all of these
        **computed figures from real field values**, not executed DB checks — the same distinction
        26-VALIDATION.md's Evidence Map row 3 draws.

    Step 3 — run it with `./manage.py shell -c "exec(open('tmp/26_queue_inventory_probe.py').read())"`
    so no REPL banner pollutes the captured output, capturing full stdout to
    `tmp/26-queue-inventory.txt`. Re-run `stat -c '%s %Y' src/fomo_db.sqlite3` immediately
    afterwards and confirm it still matches `tmp/26-realdb-fingerprint-before.txt`.

    Step 4 — open `26-DECISION.md` and insert a new subsection headed
    `### SPIKE-03 gap closure — queue-run projection, measured` at the **end of `## Findings`**:
    immediately after the existing `### SPIKE-03 criterion 3 — canonical event key and the
    adopt-vs-gap-fill comparison` subsection (which ends with the `src/fomo_db.sqlite3`
    fingerprint sentence) and immediately before the `## Recommendation` heading. Do not modify a
    single character of any existing Finding, of the `### Domain correction` section, or of
    anything under `## Recommendation` — plan 26-05 owns those edits.

    Open the new subsection with one sentence stating why it exists (26-VERIFICATION.md reopened
    SPIKE-03 because the key scheme was settled for classically scheduled runs only, and this is
    the option-(b) measured closure), then write these parts from blocks (A)-(D):

    - `#### Run-type inventory (which runs this question actually affects)` — block (A)'s three
      totals, pk=1/pk=29/pk=30 named individually, and block (B)'s RECON-07 split, stated with the
      probe date in the same sentence as every number (D-04's date-pinning rule). Carry the
      "judgment applied to real field values" tag explicitly; do not present it at the same
      confidence as an executed check.
    - `#### The same category error already exists in campaign_gap.claimed_dates()` — block (C)'s
      claimed-date count versus the 11 actually-scheduled nights, the set difference, the
      file:line citation, and the plain consequence: for a queue run, the shipped coverage-gap
      analysis already treats every night of the window as claimed, so a decision to stop minting
      per-night calendar events for queue runs does not by itself make the two views agree. Record
      that correcting `claimed_dates()` is **not** this phase's work (investigation-only) and name
      the requirement that would own it (v2.3's GAPB-01) — flag, do not fix, exactly as D-16 did
      for PROJECT.md.
    - `#### D-05's 400-event figure` — block (D)'s three arithmetic results, and an explicit
      statement that the **site-fanout half** of D-05 (a single class-wide event, not one per
      candidate site) is independently verified against `SITE_TELESCOPE_MAP` and is not in
      question here, while the **per-day half** is what the domain correction reopened. Do not
      state a verdict yet — the verdict is plan 26-05's task 1 decision.

    Redaction discipline throughout: real people's names may describe a finding, but no email
    address and no name+email pairing may appear anywhere in the file.
  </action>
  <verify>
    <automated>D=.planning/phases/26-canonical-record-spike/26-DECISION.md; test -f tmp/26-queue-inventory.txt && grep -q 'Block (A)' tmp/26-queue-inventory.txt && grep -q 'Block (B)' tmp/26-queue-inventory.txt && grep -q 'Block (C)' tmp/26-queue-inventory.txt && grep -q 'Block (D)' tmp/26-queue-inventory.txt && grep -q '### SPIKE-03 gap closure' "$D" && grep -q 'claimed_dates' "$D" && grep -q 'campaign_gap.py' "$D" && grep -q 'GAPB-01' "$D" && grep -q 'RECON-07' "$D" && grep -q '## Recommendation' "$D" && ! grep -qi 'upsert' "$D" && ! grep -qE '[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}' "$D" && [ "$(stat -c '%s %Y' src/fomo_db.sqlite3)" = "$(cat tmp/26-realdb-fingerprint-before.txt)" ] && ! test -e local_settings.py && git diff --quiet -- solsys_code/</automated>
  </verify>
  <acceptance_criteria>
    - `tmp/26-realdb-fingerprint-before.txt` exists and `stat -c '%s %Y' src/fomo_db.sqlite3` still
      returns exactly its contents after the probe run — the real dev DB took zero writes.
    - `tmp/26-queue-inventory.txt` contains all four labelled blocks `(A)`, `(B)`, `(C)`, `(D)`.
    - Block (A) prints one line per `CampaignRun` row with its pk, `telescope_instrument`, resolved
      obscode, window dates, window length and assigned category, plus the three category totals
      and the named categories of pk=1, pk=29 and pk=30.
    - Block (B) prints the RECON-07 total and its per-category breakdown with pks listed.
    - Block (C) prints the count of dates `claimed_dates()` claims inside pk=1's window, the 11
      site-local nights its real LCO events cover, the set difference, and the real line numbers of
      the responsible `solsys_code/campaign_gap.py` loop read from the file.
    - No line of any probe output or of `26-DECISION.md` contains a `contact_person` or
      `contact_email` value; the email-regex negative grep passes.
    - `26-DECISION.md` gained exactly one new heading `### SPIKE-03 gap closure — queue-run
      projection, measured`, positioned after the existing SPIKE-03 criterion-3 Finding and before
      `## Recommendation`; `git diff` for the file shows no deletion or modification of any
      pre-existing line.
    - The new subsection names the "judgment applied to real field values" tag for the run-type
      inventory and the "computed figure from real field values" tag for the D-05 arithmetic.
    - `local_settings.py` does not exist and `git diff --quiet -- solsys_code/` passes.
  </acceptance_criteria>
  <done>The real dev DB has been read once, read-only and fingerprint-guarded, producing the run-type inventory, the RECON-07 split, the shipped-code over-claim finding with its file:line citation, and the D-05 arithmetic inputs — all recorded in a new `26-DECISION.md` Findings subsection with no earlier content disturbed.</done>
</task>

<task type="auto">
  <name>Task 2: Build and count the three candidate queue-run projections against pk=1's real window</name>
  <files>.planning/phases/26-canonical-record-spike/26-DECISION.md, local_settings.py, tmp/26_queue_projection_probe.py, tmp/26_calendar_render_probe.py, tmp/26-queue-projection-counts.txt, tmp/26-calendar-render.txt, tmp/26-queue-span-copy.sqlite3, tmp/26-queue-none-copy.sqlite3, tmp/26-queue-pernight-copy.sqlite3</files>
  <read_first>
    - .planning/phases/26-canonical-record-spike/26-DECISION.md (the `### SPIKE-03 criterion 3` Finding in full — the three-copy scenario harness pattern, the `RECONCILER_SCENARIO` environment-variable dispatch, the `IDEMPOTENT_RERUN <scenario> created=N updated=N` output token, the measured site-local-versus-UTC night sets, and the `Australia/Sydney` substitution for obscode `E10`'s blank timezone; plus the task-1 subsection this task appends to)
    - solsys_code/calendar_utils.py (lines 297-378 — `_update_or_unchanged()` and `insert_or_create_calendar_event()`: the caller-supplied `lookup` dict, the `created`/`updated`/`unchanged` return action, and the `update_fields=list(fields.keys()) + ['modified']` save)
    - solsys_code/campaign_views.py (lines 425-490 — `_project_calendar_event()`: the exact `event_fields` dict (`title`, `description`, `target_list`, `telescope`), the satellite branch's whole-window spanning convention `datetime.combine(window_start, 00:00, utc)` to `datetime.combine(window_end, 23:59, utc)` under the bare `CAMPAIGN:{run.pk}` key, and the ground branch's per-night `CAMPAIGN:{run.pk}:{night.isoformat()}` key — this is the shipped bare-key-versus-dated-key precedent the `RUN:` namespace mirrors; line 392-401 `_calendar_event_title()` for the title format. Read it, do NOT import it)
    - solsys_code/campaign_views.py (line 797 — the existing ownership query `Q(url=f'CAMPAIGN:{run.pk}') | Q(url__startswith=f'CAMPAIGN:{run.pk}:')`, the shipped precedent for one query covering both key forms)
    - solsys_code/views.py (lines 118-136 — the calendar month-grid build: `all_day_events` is every event whose `[start_date, end_date]` range contains the cell's date **and** whose start and end dates differ, while `events` is only same-day events; this is why a multi-day span renders in every cell it covers)
    - src/templates/tom_calendar/partials/calendar.html (lines 210-250 — the `{% for event in day.all_day_events %}` loop and whether an all-day event gets any visual treatment distinguishing it from a same-day event)
    - .planning/phases/26-canonical-record-spike/26-01-PLAN.md (the `django.test.Client()` fetch pattern with `SERVER_NAME='localhost'`, needed because `ALLOWED_HOSTS=[]` in `src/fomo/settings.py` does not include the test framework's default host outside `manage.py test`)
    - .planning/phases/26-canonical-record-spike/26-RESEARCH.md ("Common Pitfalls" 1 — the invisible `local_settings.py` database override and why the printed guard is mandatory)
  </read_first>
  <action>
    Step 1 — stand up three independent scratch copies. Copy `src/fomo_db.sqlite3` to
    `tmp/26-queue-span-copy.sqlite3`, `tmp/26-queue-none-copy.sqlite3` and
    `tmp/26-queue-pernight-copy.sqlite3`. Write `local_settings.py` (gitignored at
    `.gitignore:61`) overriding `DATABASES['default']['NAME']` to whichever copy the current
    scenario needs, repointing it between scenarios. Before **every** write-capable command, print
    `settings.DATABASES['default']['NAME']` and abort unless it ends in the intended
    `tmp/26-queue-<scenario>-copy.sqlite3` filename.

    Step 2 — write `tmp/26_queue_projection_probe.py`, dispatching on a `QUEUE_SCENARIO`
    environment variable with the three values `span`, `none` and `per-night`, mirroring the
    `RECONCILER_SCENARIO` harness `26-DECISION.md`'s SPIKE-03 Finding already describes. It imports
    `insert_or_create_calendar_event` from `solsys_code.calendar_utils` **unchanged** and defines no
    substitute write helper; it imports neither `solsys_code.views` nor `solsys_code.ephem_utils`
    nor `solsys_code.campaign_views`. It reproduces the `event_fields` dict and the title format
    from `campaign_views.py:392-401,430-436` inline, citing those lines in a comment.

    The three scenarios, all against real `CampaignRun` pk=1 (an LCO queue run, 15-night window
    2026-07-07..2026-07-21, 11 real LCO events ids 53-63):

    - `span` — create or update **one** event under the bare lookup `{'url': 'RUN:1'}`, with
      `start_time` = pk=1's `window_start` at 00:00 UTC and `end_time` = pk=1's `window_end` at
      23:59 UTC, exactly mirroring the satellite branch's spanning convention at
      `campaign_views.py:442-443`. The 11 real LCO events are not touched.
    - `none` — mint nothing at all for the queue run; its calendar presence is exactly its 11
      attributed adapter events. Still run every measurement below so the row is comparable.
    - `per-night` — the pre-correction behavior, reproduced as the rejected candidate: mint
      `RUN:1:{site-local night}` for every night in pk=1's window that carries no attributed LCO
      event (the four site-local-uncovered nights `26-DECISION.md`'s SPIKE-03 Finding already
      measured: 2026-07-08, 2026-07-13, 2026-07-15, 2026-07-21), leaving the 11 real events
      untouched. Reuse the same site-local-night derivation and the same explicit
      `Australia/Sydney` substitution for obscode `E10`'s blank timezone, printing the
      substitution rather than assuming the field is populated.

    For each scenario print, on labelled lines:
    (i) the in-window `CalendarEvent` count and the full sorted list of their `url` values;
    (ii) the pass-1 tally in the form `created=N updated=N unchanged=N`;
    (iii) the pass-2 idempotency token in the form `IDEMPOTENT_RERUN <scenario> created=N
    updated=N` after running the identical scenario a second time against the same copy;
    (iv) the `modified` timestamps of the 11 real LCO events before and after the scenario, and a
    boolean `LCO_ROWS_UNTOUCHED` — the direct check of D-09's "not mine, never touch" rule.

    Step 3 — the key-stability probe, the direct test of SPIKE-03's *"stable across all four
    pipeline stages"* claim. After the pass-2 re-run, on the same scenario copy, narrow pk=1's
    window by one night at each end (`window_start` 2026-07-08, `window_end` 2026-07-20) — a real
    staff window edit, and the closest available stand-in for a stage-3 narrowing — then re-run the
    scenario once more. Print the sorted url-key list before and after the narrowing, a boolean
    `KEY_SET_STABLE`, and the list of any key that is now orphaned (present as a row but no longer
    corresponding to any night of the run's window). The window edit lives only on the disposable
    copy; do not restore it.

    Step 4 — write `tmp/26_calendar_render_probe.py`, measuring what a reader actually sees. Using
    `django.test.Client()` with `SERVER_NAME='localhost'`, fetch `/calendar/?year=2026&month=7`
    against each scenario copy after its scenario has run. Print the HTTP status (expect 200), and
    the number of occurrences in the decoded response body of the minted event's title string, on
    a line labelled `RENDER_OCCURRENCES <scenario> title_occurrences=N`. Then, as the real-row
    corroboration of the same mechanism, print how many day cells one of the 9 real classical
    blank-`url` events occupies (a sunset-to-sunrise event crosses midnight, so its start and end
    dates differ and `solsys_code/views.py:126-132` routes it into `all_day_events` for every cell
    in its range) on a line labelled `REAL_MULTIDAY_ROW_CELLS`. Also record verbatim whether
    `src/templates/tom_calendar/partials/calendar.html`'s all-day loop applies any visual treatment
    distinguishing an all-day event from a same-day one.

    Step 5 — run all three scenarios and the render probe, capturing stdout to
    `tmp/26-queue-projection-counts.txt` and `tmp/26-calendar-render.txt`. Re-run
    `stat -c '%s %Y' src/fomo_db.sqlite3` and confirm it still matches
    `tmp/26-realdb-fingerprint-before.txt` — the real dev DB must be untouched throughout.

    Step 6 — extend the `### SPIKE-03 gap closure — queue-run projection, measured` subsection
    task 1 created, appending these parts. Change nothing task 1 already wrote and nothing under
    `## Recommendation`.

    - `#### Three-way comparison against pk=1's real window` — a table with one row per scenario
      carrying the in-window count, the pass-1 tally, the idempotency token, `LCO_ROWS_UNTOUCHED`,
      and `KEY_SET_STABLE` under the window narrowing, plus the orphaned-key list where any. Quote
      the measured tokens verbatim rather than paraphrasing them. Tag the comparison **Confirmed
      against real rows** (real pk=1 window, real 11 LCO events, three independent real copies of
      the dev DB).
    - `#### What a calendar reader actually sees` — the `RENDER_OCCURRENCES` numbers per scenario,
      the `REAL_MULTIDAY_ROW_CELLS` corroboration, and the explanation drawn from
      `solsys_code/views.py:126-132`: the month grid places any event whose start and end dates
      differ into every day cell its range covers, so a single whole-window span row is rendered
      once per covered day. State plainly what that means for the options — whether "one span row"
      and "one row per night" are or are not distinguishable to a reader, and where the difference
      actually lives (stored rows, ownership surface, and machine consumers) if not on the grid.
      Tag the containment-filter behavior **Confirmed against real rows** (the real classical
      multi-day rows exercise it) and the span row itself **Constructed-input code-path check**
      (the span is the proposal under test, not an existing row).
    - `#### The key-scheme consequence` — state, from the measurements rather than from argument,
      what a single canonical scheme covering both run types would have to look like: which key
      form each run type takes, whether one ownership query covers both (citing
      `campaign_views.py:797`'s shipped `Q(url=...) | Q(url__startswith=...)` precedent for exactly
      this bare-key-plus-prefix pair), and which form has a date component that a stage transition
      could change. Do not state a verdict — plan 26-05 task 1 locks it.

    Do not touch the `### Domain correction` section, the `## Recommendation` section, or D-11's
    deferral. Redaction discipline as in task 1.
  </action>
  <verify>
    <automated>D=.planning/phases/26-canonical-record-spike/26-DECISION.md; test -f tmp/26-queue-projection-counts.txt && test -f tmp/26-calendar-render.txt && grep -q 'IDEMPOTENT_RERUN span' tmp/26-queue-projection-counts.txt && grep -q 'IDEMPOTENT_RERUN none' tmp/26-queue-projection-counts.txt && grep -q 'IDEMPOTENT_RERUN per-night' tmp/26-queue-projection-counts.txt && grep -q 'KEY_SET_STABLE' tmp/26-queue-projection-counts.txt && grep -q 'LCO_ROWS_UNTOUCHED' tmp/26-queue-projection-counts.txt && grep -q 'RENDER_OCCURRENCES' tmp/26-calendar-render.txt && grep -q 'REAL_MULTIDAY_ROW_CELLS' tmp/26-calendar-render.txt && grep -q 'Three-way comparison' "$D" && grep -q 'What a calendar reader actually sees' "$D" && grep -q 'RUN:1' "$D" && grep -q 'all_day_events' "$D" && grep -q 'Confirmed against real rows' "$D" && grep -q 'Constructed-input code-path check' "$D" && grep -q 'deliberately left open for Phase 29' "$D" && ! grep -qi 'upsert' "$D" && ! grep -qE '[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}' "$D" && [ "$(stat -c '%s %Y' src/fomo_db.sqlite3)" = "$(cat tmp/26-realdb-fingerprint-before.txt)" ] && git diff --quiet -- solsys_code/ && [ -z "$(git branch --list 'spike/*')" ]</automated>
  </verify>
  <acceptance_criteria>
    - Three scratch copies exist under `tmp/`, and the printed
      `settings.DATABASES['default']['NAME']` guard line appears before every write-capable
      command with a path ending in the intended `tmp/26-queue-<scenario>-copy.sqlite3`.
    - `tmp/26-queue-projection-counts.txt` contains, for each of `span`, `none` and `per-night`:
      an in-window event count, the full sorted url-key list, a `created=N updated=N unchanged=N`
      tally, an `IDEMPOTENT_RERUN <scenario> created=N updated=N` token, an
      `LCO_ROWS_UNTOUCHED` boolean, and a `KEY_SET_STABLE` boolean with the before/after key lists
      across the window narrowing.
    - `tmp/26-calendar-render.txt` contains an HTTP status of 200 for each scenario, a
      `RENDER_OCCURRENCES <scenario> title_occurrences=N` line per scenario, and a
      `REAL_MULTIDAY_ROW_CELLS` line derived from a genuinely real classical calendar row.
    - `tmp/26_queue_projection_probe.py` contains no import of `solsys_code.views`,
      `solsys_code.ephem_utils` or `solsys_code.campaign_views`, and imports
      `insert_or_create_calendar_event` from `solsys_code.calendar_utils` without redefining it.
    - `26-DECISION.md`'s `### SPIKE-03 gap closure` subsection now carries the `#### Three-way
      comparison against pk=1's real window`, `#### What a calendar reader actually sees` and
      `#### The key-scheme consequence` parts, with the measured tokens quoted verbatim and both
      confidence tags used.
    - The subsection states no verdict on which option to adopt — the decision is plan 26-05's.
    - `26-DECISION.md` still contains the D-11 deferral sentence "deliberately left open for Phase
      29", unmodified.
    - `stat -c '%s %Y' src/fomo_db.sqlite3` still equals `tmp/26-realdb-fingerprint-before.txt`;
      `git diff --quiet -- solsys_code/` passes; no `spike/*` branch exists.
  </acceptance_criteria>
  <done>All three candidate queue-run projections are built and counted against pk=1's real 15-night window and its real 11 LCO events, with idempotency, key stability under a window change, ownership non-interference, and rendered-calendar occurrence counts all measured and recorded — meeting the same evidence bar D-11 set.</done>
</task>

<task type="auto">
  <name>Task 3: Record the surviving fingerprint, discard every scratch artifact, and prove the tree is pristine</name>
  <files>.planning/phases/26-canonical-record-spike/26-DECISION.md</files>
  <read_first>
    - .planning/phases/26-canonical-record-spike/26-03-PLAN.md (task 3, parts D, E and F — the discard order, the "a gitignored file is invisible to `git status`, so check for literal absence on disk" rule, the `git log --all` never-committed check, and the deliberate exclusion of repo-wide `ruff` gates whose failures are pre-existing)
    - .planning/phases/26-canonical-record-spike/deferred-items.md (the pre-existing repo-wide `ruff check .` / `ruff format --check .` drift already confirmed empty-diff against commit `77e16b5` — this plan neither fixes nor gates on it)
    - .planning/phases/26-canonical-record-spike/26-VERIFICATION.md (the Required Artifacts table — the exact teardown bar the verifier checked last time: no scratch branch anywhere, no `tmp/`, no `local_settings.py`, no scratch migration in any ref, `solsys_code/` byte-identical to `77e16b5`)
    - .planning/phases/26-canonical-record-spike/26-DECISION.md (the `### Snapshot (D-04)` Finding, where the surviving fingerprint value must be recorded before `tmp/` is deleted)
  </read_first>
  <action>
    Part A — record the surviving fingerprint **before** anything is deleted. Re-run
    `stat -c '%s %Y' src/fomo_db.sqlite3` and compare it against
    `tmp/26-realdb-fingerprint-before.txt`. They must match — this is the plan-level proof that no
    command in this plan ever wrote to the real dev DB. Then record that value verbatim inside the
    `### SPIKE-03 gap closure` subsection, in a closing sentence stating that the real dev DB's
    size+mtime fingerprint was unchanged across this plan, so the evidence survives the deletion of
    `tmp/`.

    Part B — the discard, in this order:
    1. `rm -f local_settings.py`, then confirm its literal absence with an `ls` that must error. A
       leaked override is gitignored and therefore invisible to `git status`, so checking for
       absence on disk is a separate necessary step, not implied by a clean status. A surviving
       override would silently repoint the whole project's database at a deleted scratch path.
    2. `rm -rf tmp/`, then confirm its literal absence the same way — this removes the three
       scratch SQLite copies, the three probe scripts and the three captured output files.
    3. Confirm no git branch matching `spike/*` exists (this plan created none, and plan 26-03's
       `spike/26-canonical-record-probe` was already force-deleted) and that HEAD is still the
       phase-26 working branch.
    4. Confirm `git diff --quiet -- solsys_code/` passes and that no untracked file exists under
       `solsys_code/` — this plan edited no application code at all, so any difference here is a
       failure, not a cleanup step.
    5. Confirm `git status --porcelain` shows only this plan's documentation deliverable
       (`26-DECISION.md`) and other `.planning/` files — no scratch script, no database file, no
       `solsys_code/` change, no `local_settings.py`.
    6. Confirm no scratch artifact was ever committed anywhere, by checking that
       `git log --all --oneline -- 'tmp/' 'local_settings.py'` returns nothing.

    Part C — prove the repository still works from the pristine state. Run `python -m pytest` and
    the single-module Django quick run `./manage.py test solsys_code.tests.test_calendar_template`
    (Django builds its own isolated test database, so this never touches `src/fomo_db.sqlite3`, and
    with `local_settings.py` now gone it runs against the real settings module exactly as any
    developer would). Both must pass.

    Deliberately **not** gated here: repo-wide `ruff check .` and `ruff format --check .`. Their
    failures are pre-existing drift in files this plan never touched, already logged in
    `deferred-items.md` and confirmed empty-diff against commit `77e16b5` by plan 26-01. This plan
    writes no Python that survives teardown, so there is nothing of its own for them to check.
  </action>
  <verify>
    <automated>D=.planning/phases/26-canonical-record-spike/26-DECISION.md; grep -q "$(stat -c '%s %Y' src/fomo_db.sqlite3)" "$D" && ! test -e local_settings.py && ! test -e tmp && [ -z "$(git branch --list 'spike/*')" ] && git diff --quiet -- solsys_code/ && [ -z "$(git status --porcelain solsys_code/)" ] && [ -z "$(git log --all --oneline -- 'tmp/' 'local_settings.py')" ] && [ -z "$(git status --porcelain | grep -v -e '.planning/' )" ] && python -m pytest && ./manage.py test solsys_code.tests.test_calendar_template</automated>
  </verify>
  <acceptance_criteria>
    - `26-DECISION.md` contains the current `stat -c '%s %Y' src/fomo_db.sqlite3` value verbatim,
      recorded before `tmp/` was deleted and stated as unchanged across this plan.
    - `ls local_settings.py` errors and `ls tmp` errors — both are literally absent from disk, not
      merely absent from `git status`.
    - `git branch --list 'spike/*'` returns nothing, and HEAD is the phase-26 working branch.
    - `git diff --quiet -- solsys_code/` passes and `git status --porcelain solsys_code/` is empty.
    - `git status --porcelain` lists only paths under `.planning/`.
    - `git log --all --oneline -- 'tmp/' 'local_settings.py'` returns nothing — no scratch artifact
      was ever committed to any ref.
    - `python -m pytest` and `./manage.py test solsys_code.tests.test_calendar_template` both exit
      0 from the pristine tree.
  </acceptance_criteria>
  <done>The real dev DB is provably unwritten, its fingerprint survives inside the committed decision doc, every scratch artifact is gone from disk and from every git ref, `solsys_code/` is untouched, and both test gates pass from a pristine tree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| real `src/fomo_db.sqlite3` → the three disposable scratch copies | Every write in this plan must land on a copy. The `local_settings.py` override is invisible at the call site and there is no `--database=scratch` flag, so the printed guard is the only thing separating a scenario write from the real dev DB (26-RESEARCH.md Pitfall 1) |
| real `CampaignRun` rows (bearing `contact_person`/`contact_email`) → `26-DECISION.md` | The decision doc is committed and is mirrored by plan 26-05 into a published Sphinx page, so any PII that survives into it crosses a second, wider disclosure boundary (26-RESEARCH.md Pitfall 6, Phase 18 D-01) |
| gitignored scratch files → the phase-26 branch and every later phase | `local_settings.py` and `tmp/` are invisible to `git status`; a survivor silently repoints the whole project's database at a deleted path for every future `manage.py` invocation (26-VALIDATION.md sampling step 7) |
| shipped `solsys_code/` source → this investigation-only plan | 26-VERIFICATION.md records `solsys_code/` byte-identical to `77e16b5` as a success criterion, not an accident. This plan creates no scratch branch, so there is no isolation mechanism for an application-code edit and none may be made |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-01 | Information Disclosure | Real `contact_person`/`contact_email` values reaching `26-DECISION.md` via the run-type inventory (which iterates every `CampaignRun` row) | high | mitigate | The probe never selects or prints either field; task 1's action states the field-set restriction explicitly; every task's `<automated>` verify runs an email-regex negative grep over `26-DECISION.md`. Real names may describe a finding; addresses and name+email pairings never appear. |
| T-26-02 | Tampering | A scenario write landing on the real `src/fomo_db.sqlite3` instead of a scratch copy | high | mitigate | Task 1 runs with no `local_settings.py` present at all; from task 2 the DB-path guard prints `settings.DATABASES['default']['NAME']` and aborts unless it ends in the intended scratch filename; every task's `<automated>` verify re-derives the live fingerprint and compares it to `tmp/26-realdb-fingerprint-before.txt`; task 3 copies the surviving value into the committed doc so the check stays reproducible after teardown. |
| T-26-03 | Tampering | `local_settings.py` or `tmp/` surviving teardown into the branch or into Phase 27 | high | mitigate | Task 3 part B deletes each in a fixed order and proves absence three independent ways: literal `test -e` checks on disk (gitignored files are invisible to `git status`), a filtered `git status --porcelain`, and `git log --all` over both paths proving neither was ever committed. |
| T-26-09 | Tampering | An application-code edit made "just to get a probe running" and then merged | high | mitigate | This plan creates no scratch branch, so there is no place to hide such an edit. `git diff --quiet -- solsys_code/` plus an empty `git status --porcelain solsys_code/` are asserted in all three tasks' `<automated>` verifies, not only at the end, and `git branch --list 'spike/*'` must stay empty. |
| T-26-10 | Repudiation | A run-type category presented as an executed check when it is a human-auditable judgment, so Phase 29 over-trusts it | medium | mitigate | Block (A) prints every row's inputs alongside its assigned category so the judgment is auditable, and the Finding carries the explicit "judgment applied to real field values" tag, kept distinct from the "Confirmed against real rows" tag used for the three-way scenario comparison (Phase 18 D-09 precedent). |
| T-26-SC | Tampering | npm/pip/cargo package installs | — | accept | No package-manager install occurs in this plan. 26-RESEARCH.md's Package Legitimacy Audit records "Not applicable"; CONTEXT.md locks "no new dependencies". No legitimacy checkpoint is required. |
</threat_model>

<verification>
- `26-DECISION.md` gained exactly one new Findings subsection, `### SPIKE-03 gap closure —
  queue-run projection, measured`, positioned after the existing SPIKE-03 criterion-3 Finding and
  before `## Recommendation`; no pre-existing line of the file was modified or deleted.
- The subsection carries: the run-type inventory with the RECON-07 split, the
  `campaign_gap.claimed_dates()` over-claim finding with a real file:line citation, D-05's three
  arithmetic results, the three-way scenario table with idempotency and key-stability tokens
  quoted verbatim, the rendered-calendar occurrence counts with the `views.py` containment-filter
  explanation, and the key-scheme consequence — and states no verdict.
- Both confidence tags ("Confirmed against real rows", "Constructed-input code-path check") and
  the "judgment applied to real field values" tag are used and kept distinct.
- No email address and no occurrence of "upsert" appears in `26-DECISION.md`.
- D-11's adopt-vs-gap-fill deferral text is unmodified.
- `src/fomo_db.sqlite3`'s size+mtime fingerprint is identical before and after the plan, and the
  surviving value is recorded in the committed doc.
- No `local_settings.py`, no `tmp/`, no `spike/*` branch, no `solsys_code/` diff, and no git ref
  containing either scratch path.
- `python -m pytest` and `./manage.py test solsys_code.tests.test_calendar_template` pass from the
  pristine tree.
- The committed diff for this plan contains only `26-DECISION.md` (plus this plan's own
  `.planning/` metadata).
</verification>

<success_criteria>
- The queue-run projection question has D-11-grade measured evidence behind it: every candidate
  option counted against the real pk=1 window and its real 11 LCO events, a concrete code-level
  fact with a file:line citation distinguishing them, and idempotency plus key stability measured
  rather than asserted.
- Plan 26-05 can lock a verdict from numbers rather than from argument, and Phase 29 inherits
  evidence rather than an open investigative question.
- The repository is left exactly as this plan found it apart from one documentation file —
  provably, not by assertion.
</success_criteria>

<artifacts_produced>
**Committed (survives phase close):**
- `.planning/phases/26-canonical-record-spike/26-DECISION.md` — one new Findings subsection,
  `### SPIKE-03 gap closure — queue-run projection, measured`, containing the sub-parts
  `#### Run-type inventory (which runs this question actually affects)`,
  `#### The same category error already exists in campaign_gap.claimed_dates()`,
  `#### D-05's 400-event figure`, `#### Three-way comparison against pk=1's real window`,
  `#### What a calendar reader actually sees`, and `#### The key-scheme consequence`, closing with
  the surviving real-DB fingerprint.

**Created by this plan and discarded within it (task 3 part B):**
- `local_settings.py` (gitignored database override)
- `tmp/26_queue_inventory_probe.py`, `tmp/26_queue_projection_probe.py`,
  `tmp/26_calendar_render_probe.py`
- `tmp/26-realdb-fingerprint-before.txt`, `tmp/26-queue-inventory.txt`,
  `tmp/26-queue-projection-counts.txt`, `tmp/26-calendar-render.txt`
- `tmp/26-queue-span-copy.sqlite3`, `tmp/26-queue-none-copy.sqlite3`,
  `tmp/26-queue-pernight-copy.sqlite3`, and with them the throwaway `RUN:1` and
  `RUN:1:{date}` calendar-event keys minted into those copies and the narrowed `CampaignRun` pk=1
  window used for the key-stability probe
- the whole `tmp/` directory

**No new symbols, modules, models, migrations or branches are produced by this plan.**

**Deliberately NOT touched by this plan:**
- Any `solsys_code/` file — investigation-only, and 26-VERIFICATION.md records byte-identity with
  commit `77e16b5` as a success criterion.
- `26-DECISION.md`'s `## Recommendation` section, its `### Domain correction` section, and D-11's
  deferral — all owned by plan 26-05.
- `docs/design/canonical_record_spike.rst`, `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md` —
  all owned by plan 26-05.
- Any `docs/runbooks/` page or demo notebook — ROADMAP records "Paired docs: None" for this phase.
</artifacts_produced>

<output>
Create `.planning/phases/26-canonical-record-spike/26-04-SUMMARY.md` when done.
</output>
