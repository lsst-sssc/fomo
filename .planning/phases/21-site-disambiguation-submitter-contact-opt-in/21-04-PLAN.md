---
phase: 21-site-disambiguation-submitter-contact-opt-in
plan: 04
type: execute
wave: 3
depends_on: [21-01, 21-02, 21-03]
files_modified:
  - solsys_code/campaign_views.py
  - solsys_code/solsys_code_observatory/views.py
  - solsys_code/tests/test_campaign_approval.py
autonomous: true
requirements: [SITE-02, SITE-03]
must_haves:
  truths:
    - "Approving a run whose site is already set skips resolve_site() entirely and never overwrites the existing resolution"
    - "Approving a run with site=None reads the staff-submitted site_selection (falling back to site_raw) and resolves it, never fabricating a placeholder Observatory"
    - "Staff can create a new Observatory from an unresolved row and be returned to the approval queue afterward (?obscode= prefill + ?next= redirect)"
    - "An oversized/malformed site_selection is flagged for review via the existing length guard, not processed or crashed"
  artifacts:
    - "solsys_code/campaign_views.py::CampaignRunDecisionView.post (D-06 guard + site_selection read)"
    - "solsys_code/solsys_code_observatory/views.py::CreateObservatory.get_success_url (?next=)"
    - "solsys_code/solsys_code_observatory/views.py::CreateObservatory.get_initial (?obscode=)"
  key_links:
    - "site_selection POST field (emitted by Plan 21-03's render_site input) is read only in the run.site is None branch"
    - "?next= is validated with django.utils.http.url_has_allowed_host_and_scheme before redirecting"
---

<objective>
Complete the site-disambiguation feature's decision-time behavior: fix SITE-03's clobbering bug
(D-06 — approving an already-resolved run must not re-run `resolve_site()`), wire the staff-submitted
`site_selection` value into the approve path (SITE-02), and extend `CreateObservatory` with the
`?obscode=` prefill + `?next=` redirect support (D-05/Pitfall 6) so the "Create new Observatory"
link from Plan 21-03 round-trips back to the approval queue.

Purpose: Turn the inline UI (Plan 21-03) into a working resolution flow and close a
live-reachable clobbering bug (RESEARCH Pitfall 3: the existing `except Exception` revert path leaves
`run.site` set, so a second Approve click re-resolves unconditionally today).

Output: A guarded `CampaignRunDecisionView.post()`, an extended `CreateObservatory`, and the SITE-02
resolution/create-new + SITE-03 clobber regression tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-site-disambiguation-submitter-contact-opt-in/21-CONTEXT.md
@.planning/phases/21-site-disambiguation-submitter-contact-opt-in/21-RESEARCH.md
@.planning/phases/21-site-disambiguation-submitter-contact-opt-in/21-PATTERNS.md
@.planning/phases/21-site-disambiguation-submitter-contact-opt-in/21-VALIDATION.md
@solsys_code/campaign_views.py
@solsys_code/solsys_code_observatory/views.py
@solsys_code/campaign_utils.py
@solsys_code/tests/test_campaign_approval.py
</context>

<artifacts_this_phase_produces>
Symbols this plan (21-04) creates/changes:
- `CampaignRunDecisionView.post` — the `resolve_site()` call is now guarded by `if run.site is None:`
  and reads the `site_selection` POST field (from Plan 21-03's inline input) in that branch, in
  `solsys_code/campaign_views.py`.
- `CreateObservatory.get_initial` — new override reading `?obscode=` for form prefill in
  `solsys_code/solsys_code_observatory/views.py`.
- `CreateObservatory.get_success_url` — extended to honor a validated `?next=` redirect target.
- New/extended test coverage: `TestApproval` (Pitfall 3 clobber regression), `TestApprovalSiteResolution`
  (SITE-02 typed-code resolution, create-new round-trip, no-fabrication, oversized guard).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: D-06 clobber guard + site_selection read + SITE-03/T-21-04 tests</name>
  <files>solsys_code/campaign_views.py, solsys_code/tests/test_campaign_approval.py</files>
  <read_first>
    - solsys_code/campaign_views.py (CampaignRunDecisionView.post at lines 304-414 — the current
      unconditional `resolve_site(run.site_raw, create_placeholder=False)` at 324-326, the
      calendar-projection block, and the `except Exception:` revert at 387-402 that must stay unchanged)
    - solsys_code/campaign_utils.py (resolve_site — its `create_placeholder=False` contract, its
      `_MAX_OBSCODE_LEN` length guard and blank-check, its `(None, True)` never-fabricate return)
    - solsys_code/tests/test_campaign_approval.py (TestApproval and TestApprovalSiteResolution
      classes, CampaignApprovalTestBase, and the existing `@patch` mocking of `resolve_site` /
      `requests.get` / calendar-projection helpers)
    - .planning/phases/21-site-disambiguation-submitter-contact-opt-in/21-PATTERNS.md
      (D-06 clobber-fix section — wrap resolve_site in `if run.site is None:`, read
      `request.POST.get('site_selection', '')` only in that branch; preserve the except-revert)
    - .planning/phases/21-site-disambiguation-submitter-contact-opt-in/21-RESEARCH.md
      (Pitfall 3 exact reproduction + regression-test recipe; Pitfall 4 satellite comment guidance)
  </read_first>
  <behavior>
    - Approving a run with `run.site` already set (not None) does NOT call `resolve_site()` and leaves
      `run.site`/`run.site_needs_review` unchanged (SITE-03/D-06).
    - Approving a run with `run.site is None` reads `request.POST.get('site_selection', '').strip()`,
      falls back to `run.site_raw` when blank, passes it to `resolve_site(..., create_placeholder=False)`,
      and saves the result — never fabricating a placeholder Observatory.
    - Pitfall 3 sequence: approve where site-resolution succeeds but calendar projection raises → the
      `except Exception` reverts `approval_status` to `PENDING_REVIEW` while `run.site` stays set; a
      second approve does NOT call `resolve_site()`/the underlying MPC fetch again.
    - An oversized `site_selection` is flagged (`site=None`, `site_needs_review=True`) by
      `resolve_site()`'s existing `_MAX_OBSCODE_LEN` guard — no crash, no network call (T-21-04).
  </behavior>
  <action>
    In `CampaignRunDecisionView.post`, wrap the existing `resolve_site(...)` + `run.save(...)` lines
    in `if run.site is None:`. Inside that branch, compute
    `selection = request.POST.get('site_selection', '').strip() or run.site_raw` and pass `selection`
    (not `run.site_raw` directly) to `resolve_site(selection, create_placeholder=False)`. When
    `run.site` is already set, skip `resolve_site()` entirely (the SITE-03/D-06 fix — trust the
    existing value from CSV import, tier 1/2 auto-resolution, or staff's manual UI). Leave the
    calendar-projection block and the `except Exception:` revert (lines 387-402) unchanged. Add a
    short code comment at the guard explaining that a satellite-type site_selection (250/274/289)
    still falls through to `(None, True)` via `resolve_site()`'s `to_observatory()` TypeError path
    — expected, pre-existing behavior, not a Phase 21 regression (RESEARCH Pitfall 4).

    Extend `TestApproval` with the Pitfall 3 regression: patch the calendar-projection helper
    (`insert_or_create_calendar_event` or `sun_event`) to raise a non-`ValueError` exception; approve
    a run whose `site_selection`/`site_raw` resolves; assert `approval_status` reverts to
    `PENDING_REVIEW` and `run.site` stays set; approve again and assert `resolve_site` (patched) is
    `assert_not_called()` on the second attempt and `run.site` is unchanged. Add a SITE-03 case:
    approving a run with `run.site` pre-set does not call `resolve_site`. Add a T-21-04 case: an
    oversized `site_selection` yields `site=None, site_needs_review=True` with no fabricated
    Observatory and no network call. Any `Target` fixture uses `NonSiderealTargetFactory`; use
    `cache.clear()` in setUp/tearDown.
  </action>
  <verify>
    <automated>python manage.py test solsys_code.tests.test_campaign_approval.TestApproval solsys_code.tests.test_campaign_approval.TestApprovalSiteResolution 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - Approving a run with `run.site` already set does not call `resolve_site()` and preserves the
      site (VALIDATION SITE-03 row).
    - The Pitfall 3 two-attempt regression passes: after a projection-failure revert, a second
      approve does not re-call `resolve_site`/the MPC fetch (VALIDATION SITE-03 regression row).
    - An oversized `site_selection` is flagged, not processed, with no placeholder Observatory created
      and no network call (VALIDATION T-21-04 row; RESEARCH Security Domain).
    - `run.site is None` approvals resolve `site_selection` (falling back to `site_raw`); the
      `except Exception` revert path is unchanged.
  </acceptance_criteria>
  <done>The approve path trusts an already-resolved site and only resolves `site_selection` for
  unresolved runs, closing the SITE-03 clobber bug with regression coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CreateObservatory ?obscode= prefill + ?next= redirect + SITE-02 create-new tests</name>
  <files>solsys_code/solsys_code_observatory/views.py, solsys_code/tests/test_campaign_approval.py</files>
  <read_first>
    - solsys_code/solsys_code_observatory/views.py (CreateObservatory at lines 17-59 — the current
      `get_success_url` unconditional detail redirect at 26-30, the `get_context_data` override style
      at 32-34, and `form_valid` at 36-59 which already calls `get_success_url()` at the end)
    - solsys_code/solsys_code_observatory/forms.py (CreateObservatoryForm — the `obscode` field name
      used for `get_initial` prefill)
    - .planning/phases/21-site-disambiguation-submitter-contact-opt-in/21-PATTERNS.md
      (CreateObservatory `?next=`/`?obscode=` section — the exact get_success_url + get_initial shapes
      and the `url_has_allowed_host_and_scheme` validation)
    - .planning/phases/21-site-disambiguation-submitter-contact-opt-in/21-RESEARCH.md
      (Pitfall 6 — the current unconditional detail redirect is the gap to close)
  </read_first>
  <behavior>
    - `GET /observatory/create/?obscode=G37&next=/campaigns/approval-queue/` renders the form with the
      `obscode` field pre-filled to `G37`.
    - A successful create with a valid `?next=` (same-host) redirects to that `next` URL (the approval
      queue) instead of the Observatory detail page.
    - A missing or unsafe `?next=` (off-host / bad scheme) falls back to the existing Observatory
      detail redirect (open-redirect defense).
  </behavior>
  <action>
    Add `from django.utils.http import url_has_allowed_host_and_scheme` to the imports. Add a
    `get_initial()` override that calls `super().get_initial()` and sets
    `initial['obscode'] = self.request.GET.get('obscode', '')` (mirror the existing `get_context_data`
    "call super, return dict" style). Rewrite `get_success_url()` to read
    `next_url = self.request.GET.get('next') or self.request.POST.get('next')`, and if `next_url` and
    `url_has_allowed_host_and_scheme(next_url, allowed_hosts={self.request.get_host()},
    require_https=self.request.is_secure())`, return `next_url`; otherwise return the existing
    `reverse_lazy('solsys_code_observatory:detail', kwargs={'pk': self.kwargs['pk']})`. Do not modify
    `form_valid` — it already calls `get_success_url()` at the end, so `?next=` flows through.

    Add SITE-02 tests to `TestApprovalSiteResolution` (or a sibling class) in test_campaign_approval.py:
    (1) staff-typed existing obscode via the decision endpoint resolves to an existing `Observatory`
    (site set, needs_review False) — a tier-1 hit, no fabrication; (2) an unresolvable
    `site_selection` on approve leaves `Observatory.objects.count()` unchanged (no placeholder
    fabricated, regression on `260705-l1v`'s invariant); (3) create-new round-trip: GET the create
    URL with `?obscode=`/`?next=<approval queue>` (assert the form initial carries the obscode), then
    POST a valid obscode with `MPCObscodeFetcher.query` mocked so `to_observatory()` creates the row,
    and assert the response redirects to the approval-queue `next` target. Any `Target` fixture uses
    `NonSiderealTargetFactory`.
  </action>
  <verify>
    <automated>python manage.py test solsys_code.tests.test_campaign_approval solsys_code.solsys_code_observatory 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - A GET to the create URL with `?obscode=` pre-fills the form's obscode field; a valid same-host
      `?next=` redirects there after create (VALIDATION SITE-02 create-new round-trip row).
    - An unsafe/off-host `?next=` falls back to the detail redirect (open-redirect defense, T-21-06).
    - Approving with an unresolvable `site_selection` creates no placeholder Observatory (VALIDATION
      SITE-02 no-fabrication row).
    - The existing `solsys_code_observatory` tests still pass (form_valid contract unchanged).
  </acceptance_criteria>
  <done>Staff can create a new Observatory from an unresolved row and return to the approval queue,
  with obscode prefill and a validated redirect, and no placeholder is ever fabricated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Staff `site_selection` POST → resolve_site | Staff free text routed through the existing length-guarded resolver |
| `?next=` query param → post-create redirect | Untrusted redirect target must be validated before use |
| Staff browser → StaffRequiredMixin-gated decision endpoint | site_selection only takes effect under staff auth |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-04 | Tampering / Denial of Service | `site_selection` free text in `post()` | medium | mitigate | Routed unchanged through `resolve_site()`'s existing `_MAX_OBSCODE_LEN` guard + blank-check — oversized/malformed input is flagged (`site=None, needs_review=True`), no tier attempted, no network call. Verified by a T-21-04 test. |
| T-21-06 | Tampering | `CreateObservatory.get_success_url` `?next=` | high | mitigate | `?next=` validated with `django.utils.http.url_has_allowed_host_and_scheme(allowed_hosts={request.get_host()})`; off-host/bad-scheme targets fall back to the detail redirect (open-redirect defense). |
| T-21-03 | Tampering | approve/reject decision POST | low | accept | CSRF + `StaffRequiredMixin` unchanged; `http_method_names=['post']` still blocks GET-triggered state changes. |
| T-21-SAT | (documented limitation) | satellite-type `site_selection` (250/274/289) | low | accept | Falls through to `(None, True)` via `resolve_site()`'s pre-existing `to_observatory()` TypeError path (RESEARCH Pitfall 4) — safe, no crash/fabrication; noted with a code comment, not fixed this phase. |
| T-21-SC | Tampering | npm/pip/cargo installs | n/a | accept | No packages installed this phase (RESEARCH Package Legitimacy Audit: none). No supply-chain checkpoint required. |
</threat_model>

<verification>
- `python manage.py test solsys_code.tests.test_campaign_approval solsys_code.solsys_code_observatory` passes GREEN.
- Phase gate: full `python manage.py test solsys_code` green plus `ruff check .` / `ruff format --check .` clean before `/gsd-verify-work`.
- `ruff check solsys_code/campaign_views.py solsys_code/solsys_code_observatory/views.py` clean.
</verification>

<success_criteria>
Already-resolved runs are never re-resolved on approve (SITE-03), unresolved runs resolve the
staff-submitted `site_selection` without fabricating placeholders (SITE-02), and the create-new flow
round-trips back to the approval queue with a validated redirect.
</success_criteria>

<output>
Create `.planning/phases/21-site-disambiguation-submitter-contact-opt-in/21-04-SUMMARY.md` when done
</output>
