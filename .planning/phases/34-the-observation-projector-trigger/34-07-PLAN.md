---
phase: 34-the-observation-projector-trigger
plan: 07
type: execute
wave: 6
depends_on: ["34-06"]
files_modified:
  - docs/notebooks/pre_executed/project_observation_calendar_demo.ipynb
  - solsys_code/tests/test_projector_demo_notebook.py
  - .planning/phases/34-the-observation-projector-trigger/34-UAT.md
autonomous: true
gap_closure: true
gap_ids: [G-34-3]
requirements: [PROJ-05, TRIG-03, ANNOT-03, SCHED-06]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The committed `project_observation_calendar_demo.ipynb` shows a takeover that actually took something over: the takeover-diff cell (id `05528b38`) reports a non-zero re-titled count and lists at least one before -> after title pair (G-34-3, D-19, PROJ-05)."
    - "The committed notebook's first and second sweep summary lines differ: the first carries a non-zero created+updated total for at least one facility and the second is all zeros for every facility segment (G-34-3, TRIG-03, D-08)."
    - "A scratch-routed re-execution that takes nothing over fails loudly instead of committing empty evidence: the takeover-diff cell (id `05528b38`) and the second-sweep cell (id `556d2a9f`) each raise when the notebook is routed to a copy and the first sweep did no work (G-34-3)."
    - "The notebook's own prose matches its own output: the takeover framing cell (id `8eeddc83`) quotes this run's observed re-titled pair instead of an expectation its output falsifies, and the closing table cell (id `35debc54`) carries this run's numbers in its PROJ-05 and TRIG-03 evidence entries (G-34-3, ANNOT-03)."
    - "`docs/notebooks/pre_executed/project_observation_calendar_demo.sched06-baseline.json` is byte-identical to its committed state after this plan — it stays the 2026-09-11T04:44:59Z evidence UAT Test 4 diffs against (D-20, SCHED-06)."
    - "`src/fomo_db.sqlite3` is unmodified by this plan — same size and modification time before the first command and after the last — so its stale LCO events remain available for the operator's next real `updatestatus` run as UAT Test 4's SCHED-06 evidence (D-20, SCHED-06)."
    - "The committed notebook's evidence is checkable without executing the notebook: `solsys_code/tests/test_projector_demo_notebook.py` passes against the committed artifact and fails against a copy whose takeover evidence has been emptied (G-34-3)."
    - "`docs/runbooks/telescope_runs_calendar.rst` is checked against this change and the outcome recorded either way, per CLAUDE.md's directory-scoped paired-docs rule."
  artifacts:
    - "`docs/notebooks/pre_executed/project_observation_calendar_demo.ipynb` — three code cells made self-checking, three markdown cells reconciled, re-executed against a fresh un-swept clone, committed with output"
    - "`solsys_code/tests/test_projector_demo_notebook.py` — repo-level guard over the committed notebook's evidence"
    - "`tmp/34-07-devdb-path.txt`, `tmp/34-07-devdb-stamp.txt`, `tmp/34-07-preflight-dryrun.txt`, `tmp/34-07-fresh-clone.sqlite3` — gitignored evidence and scratch files quoted in the SUMMARY, never committed"
  key_links:
    - "`FOMO_DATABASE_PATH` (src/fomo/settings.py:134) -> the notebook kernel and every management command: the only mechanism keeping this plan's writes off the developer database."
    - "The preflight dry-run over the fresh clone -> the decision to execute the notebook at all: a zero LCO `updated:` count there means the clone is already converged and executing would bake empty evidence again."
    - "The in-notebook assertions in cells `05528b38` / `556d2a9f` -> `jupyter nbconvert --execute`'s exit status: a vacuous takeover aborts the conversion, so empty evidence can never reach a commit."
    - "The SCHED-06 baseline guard in cell `250b5d0b` -> `project_observation_calendar_demo.sched06-baseline.json`: a scratch-routed run reads that file and never rewrites it."
---

<objective>
Close gap **G-34-3**: the paired demo notebook's takeover demonstration currently demonstrates
nothing, and nothing in the repository would have noticed.

Plan 34-06 re-executed `docs/notebooks/pre_executed/project_observation_calendar_demo.ipynb`
against `tmp/fomo_g34_2_copy.sqlite3` — a scratch copy that 34-06's own live-proof step had
already swept to convergence before the notebook ran (commit 73b465d produced real evidence;
the code-review re-execution in 46d8390 ran against the now-converged copy and overwrote it).
The committed output therefore shows a takeover with a zero re-titled count and an empty
before -> after list (cell `05528b38`), first and second sweep summary lines that are identical
(cell `556d2a9f`), framing prose in cell `8eeddc83` that its own output falsifies, and a closing
table (cell `35debc54`) pointing PROJ-05 and TRIG-03 evidence at cells that show nothing.

This plan re-executes the notebook against a **fresh clone taken from an un-swept state**, makes
the demonstration self-checking so it can never silently go empty again, reconciles the prose
with what the run actually produced, and adds a repo-level guard over the committed artifact.

Purpose: the notebook is the deliverable (CLAUDE.md paired-docs rule), and evidence that
quietly describes nothing is worse than no evidence — a reader cannot tell the difference.

Output: a re-executed, self-checking, committed notebook; a new test guarding it; a recorded
runbook determination; gitignored evidence under `tmp/`.

**Two hard constraints, both non-negotiable:** nothing in this plan may write to
`src/fomo_db.sqlite3` (its stale LCO events are UAT Test 4's SCHED-06 evidence and must be
repaired by the operator's next real `updatestatus` through the receiver, never by a sweep), and
`project_observation_calendar_demo.sched06-baseline.json` must stay byte-identical (D-20). Note
that `src/fomo_db.sqlite3` is **gitignored** (`.gitignore:64`), so `git status` on that path is
vacuous — this plan stamps its size and modification time instead.

**Runbook determination (CLAUDE.md directory-scoped rule), made during planning and to be
confirmed, not re-litigated, during execution:** `docs/runbooks/telescope_runs_calendar.rst`
needs **no edit**. This plan changes no behaviour an operator can observe — it changes which
database a demo notebook was executed against and adds assertions to it. 34-06 already read the
page's three affected regions (lines 47-65, 144-198, 1144-1155) and recorded that they describe
post-fix behaviour. Task 2 confirms this with grep and records it either way; it does not edit
the page.

**Note (spec-less probe, visible skip):** this phase has no SPEC.md and the orchestrator
deliberately skipped the edge/prohibition probe over the ten phase requirement IDs for this
single-gap addendum. The `must_haves` above are derived directly from the VERIFICATION gap's own
`truth`, `missing` items and constraints; the phase-wide predicates live in plans 34-01..34-06.
</objective>

<execution_context>
@/home/tlister/git/fomo_devel/.claude/gsd-core/workflows/execute-plan.md
@/home/tlister/git/fomo_devel/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/34-the-observation-projector-trigger/34-VERIFICATION.md
@.planning/phases/34-the-observation-projector-trigger/34-06-SUMMARY.md

Facts already established, so you do not need to re-derive them:

- `src/fomo/settings.py:134` resolves the database as
  `os.getenv('FOMO_DATABASE_PATH') or os.path.join(BASE_DIR, 'fomo_db.sqlite3')`. The value is
  used as given, so it **must be an absolute path** — the notebook kernel runs with its working
  directory set to `docs/notebooks/pre_executed/`, where a relative path resolves elsewhere.
- The notebook has 20 cells, 12 of them code, every code cell currently carrying output, and
  nbformat 4.5 **stable cell ids** — address cells by id, never by position. The ids this plan
  names: `7e7bd66e` (intro markdown), `7022f987` (Django setup code), `8eeddc83` (takeover
  framing markdown), `4b61a527` (dry-run preview), `65e111fa` (first sweep), `05528b38`
  (takeover diff), `556d2a9f` (second sweep), `250b5d0b` (SCHED-06 baseline),
  `6a9bd576` ("What happens next" markdown), `35debc54` (closing table).
- Cell `7022f987` already reads `FOMO_DATABASE_PATH` into `SCRATCH_DB_OVERRIDE` and branches its
  assert and its print between "the developer database itself" and "routed to a scratch copy".
  Cell `250b5d0b` re-reads the environment itself (the WR-09 pattern) rather than trusting that
  binding, and skips the baseline write when routed. Both behaviours stay exactly as they are.
- The sweep's summary line format is
  `Done. failed: 0 | LCO: created: N, updated: N, unchanged: N, unprojectable: N, site_lookups: N, site_lookup_failed: N | SOAR: ...`
  — facility segments separated by ` | `, the first segment being `Done. failed: N`.
- The developer database today holds 159 LCO records and 159 facility-url-keyed events, of which
  23 carry a `[Q]` title while their record already has a scheduled block and 10 carry `[S]`
  while their record is COMPLETED. A sweep over a fresh clone therefore has real work to do
  (34-06's first run on a fresh clone reported `updated: 33`); that staleness is what makes the
  takeover demonstrable, and it is exactly what the operator's next `updatestatus` will repair.
- `tmp/` is gitignored (`.gitignore:145`); the pre-commit hook `jupyter-nb-clear-output` excludes
  `^docs/notebooks/pre_executed`, so this notebook is committed **with** its output.
- Credentials come from `src/fomo/local_settings.py` (gitignored) via `LCO_API_KEY`; the sweep's
  one-time observed-site lookup uses them. Never print, echo, log or paste that value, and never
  paste a portal request URL carrying a token.
- Project rules: `python manage.py test ...` only (never `./manage.py`); never run
  `solsys_code.tests.test_views.TestEphemeris` (it segfaults in native ASSIST); ruff runs through
  pre-commit only.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Make the takeover demonstration self-checking, then prove it on a fresh un-swept clone</name>
  <files>docs/notebooks/pre_executed/project_observation_calendar_demo.ipynb</files>
  <read_first>
    - `docs/notebooks/pre_executed/project_observation_calendar_demo.ipynb` — read the **cell
      sources** (the outputs are large; read only the outputs of cells `05528b38` and `556d2a9f`
      to see the empty evidence you are replacing). The five cells you touch are named by id
      below.
    - `.planning/phases/34-the-observation-projector-trigger/34-06-SUMMARY.md` — the source of
      truth for the `FOMO_DATABASE_PATH` routing mechanism, the clone procedure and the
      "Accomplishments" section quoting what a fresh-clone first sweep actually reports.
    - `src/fomo/settings.py` around line 134 — the one line that makes the override work, and
      the reason the path must be absolute.
    - `solsys_code/management/commands/project_observation_calendar.py` — the summary-line
      format your new parsing code reads (`created: N, updated: N, ...` per facility segment).
  </read_first>
  <precondition>
    The developer database still holds un-projected LCO staleness — the preflight dry-run in
    step 4 must report a non-zero LCO `updated:` count over the fresh clone; and
    `src/fomo/local_settings.py` supplies a working `LCO_API_KEY` with `https://observe.lco.global`
    reachable, because the sweep's one-time observed-site lookup calls the portal. If the
    preflight reports a zero `updated:` count, halt and report (most likely the operator already
    ran `updatestatus` against the developer database, which closes SCHED-06 but removes this
    plan's evidence source) — do not manufacture staleness and do not sweep the developer database.
  </precondition>
  <reversibility rating="costly">
    The notebook execution itself is reversible through git, but an accidental sweep against
    `src/fomo_db.sqlite3` would spend UAT Test 4's SCHED-06 evidence with no way to restore the
    stale state — hence the routing, the stamp and the prohibitions below.
  </reversibility>
  <action>
    Make all five cell edits FIRST, then clone, then preflight, then execute exactly once.
    Ordering is load-bearing: executing before the assertions exist can bake vacuous evidence,
    and every execution rewrites all outputs in place.

    Edit 1 — code cell id `05528b38` (the takeover diff, headed by the D-19 three-family check).
    Keep the existing three-family assert loop and the `changed_titles` comprehension untouched.
    After the loop, re-read the routing flag from the environment in this cell rather than
    trusting cell `7022f987`'s binding, following the pattern and rationale already written into
    cell `250b5d0b`: bind `scratch_routed` to whether `os.environ.get('FOMO_DATABASE_PATH')` is a
    non-empty string. Add a second comprehension, `changed_keys`, holding every primary key whose
    full snapshot tuple (url, title, start_time, end_time, observation_record id, observation
    group id) differs between `before_families['facility-url-keyed']` and
    `after_families['facility-url-keyed']` — this catches a sweep that narrowed spans without
    changing titles, which the title-only diff would miss. Keep the existing count line and the
    existing sample loop exactly as they print today. Add one printed line reporting how many
    events changed in at least one snapshot field, next to how many of those changed title.
    Then add the guard: when `scratch_routed` is true, assert that `changed_keys` is non-empty
    and that `changed_titles` is non-empty, with a message that names the cause and both
    remedies — the clone was already swept before this run, so either re-clone from an un-swept
    developer database and re-execute, or, if the developer database itself has legitimately
    converged, run the notebook un-routed so the converged branch applies and says so. When
    `scratch_routed` is false and `changed_keys` is empty, print (do not assert) a line stating
    that this run found no takeover work, so this commit's takeover cells are historical rather
    than a demonstration, and that the divergent run lives in this notebook's git history.

    Edit 2 — code cell id `556d2a9f` (the second sweep). Keep the existing per-segment
    `created: 0` / `updated: 0` / `site_lookups: 0` assertions over
    `second_sweep_summary.split(' | ')[1:]` and the closing explanatory print exactly as they
    are. Add `import re` at the top of the cell and re-read `scratch_routed` from the environment
    the same way as in Edit 1. Compute `first_sweep_work` as the sum over every
    `created: <n>, updated: <n>` pair found in `first_sweep_summary` of the two integers, and
    print a line reporting that total so a reader sees the takeover's size without doing
    arithmetic. Then add the guard: when `scratch_routed` is true, assert `first_sweep_work` is
    greater than zero and assert that `first_sweep_summary` and `second_sweep_summary` are not
    equal, with a message naming the same cause and remedies as Edit 1 — this is the assertion
    the gap asks for, the one that makes a vacuous convergence claim abort the execution instead
    of being committed. When `scratch_routed` is false and `first_sweep_work` is zero, print a
    line stating that this run found nothing to take over, that this is the legitimate,
    already-converged case the SCHED-06 re-execution in "What happens next" produces, and that
    the convergence claim below is therefore about a database that was already converged when
    the run started.

    Edit 3 — code cell id `35debc54` (the closing requirement -> evidence table). Turn the
    PROJ-05 evidence string into an f-string that appends this run's own numbers to the existing
    text: the count of re-titled events out of the facility-url-keyed family size, read from the
    same `changed_titles` and `before_families` bindings cell `05528b38` used. Turn the TRIG-03
    evidence string into an f-string that appends this run's first-sweep created+updated total
    and the fact that the second sweep reported all zeros. Leave the other seven rows, the column
    widths and the print loop untouched. After this edit the table can never point at empty cells
    without saying so in its own text.

    Edit 4 — markdown cell id `8eeddc83` (the takeover framing). Delete only the sentence
    beginning "This particular run's first sweep is expected to report" and the clause following
    it about repairing the copy; keep the four-family description, the `--dry-run` sentence and
    the closing convergence sentence. In their place, state the rule this plan establishes: a
    re-execution routed to a scratch copy must start from a clone taken from an **un-swept**
    database, because a copy an earlier sweep already converged produces a takeover that takes
    nothing over — and the cells below now assert exactly that, so such a run aborts rather than
    committing empty evidence. Leave a clearly marked placeholder sentence for the observed
    numbers; Task 2 fills it in from the executed output, which does not exist yet.

    Edit 5 — markdown cell id `6a9bd576` ("What happens next"). Keep every existing instruction.
    Add one paragraph: any scratch-routed re-execution must clone the database fresh immediately
    before it runs and must never re-use a clone a previous execution already swept — that is
    what produced the empty takeover this section's own evidence cells once showed.

    Now run it, from the repository root of the checkout holding these edits:

    Step 1 — identify the developer database and stamp it. The main working tree is the first
    entry of `git worktree list`; the developer database is its `src/fomo_db.sqlite3`. Before
    running anything else, write that file's absolute path to `tmp/34-07-devdb-path.txt` and its
    `stat -c '%Y %s'` output to `tmp/34-07-devdb-stamp.txt` — two separate files, so the later
    check re-reads the same path without re-deriving it from git. That stamp, not `git status`,
    is this plan's proof the developer database was never written — the file is gitignored, so
    `git status` on it is always empty and proves nothing.

    Step 2 — make sure the execution checkout has a developer-database copy where cell
    `7022f987` expects one. If you are executing in the main working tree this is a no-op. If you
    are executing in a worktree, `src/fomo_db.sqlite3` there is either absent (gitignored, so not
    checked out) or a leftover from an earlier worktree; copy the main tree's file over it and
    confirm the two are byte-identical with `cmp`. Never copy in the other direction.

    Step 3 — clone fresh. Copy `src/fomo_db.sqlite3` (which step 2 guarantees matches the
    developer database) to `tmp/34-07-fresh-clone.sqlite3`. Use a new filename, not 34-06's
    `tmp/fomo_g34_2_copy.sqlite3` — that copy is already swept to convergence and is the reason
    this gap exists. Export `FOMO_DATABASE_PATH` as the absolute path of the new clone.

    Step 4 — preflight. Run `python manage.py project_observation_calendar --dry-run` with that
    `FOMO_DATABASE_PATH`, capturing stdout and stderr together into
    `tmp/34-07-preflight-dryrun.txt`. A dry run writes nothing, so it does not spoil the clone.
    Read its LCO segment: a non-zero `updated:` count means the clone came from an un-swept
    state and the notebook has something real to demonstrate. A zero count means it did not —
    halt and report per the precondition above.

    Step 5 — execute exactly once. In a subshell (so the repository root stays the working
    directory for later commands), with the working directory set to
    `docs/notebooks/pre_executed/` and `FOMO_DATABASE_PATH` exported, run
    `jupyter nbconvert --to notebook --execute --inplace project_observation_calendar_demo.ipynb`.
    Never pass `--allow-errors`: the whole point of Edits 1 and 2 is that a vacuous run aborts
    the conversion. Allow several minutes (a 600000 ms timeout, or run it in the background and
    poll): both sweeps touch the whole corpus and the one-time observed-site lookups call the
    portal.

    Step 6 — the re-run rule. If the execution fails partway, for any reason, fix the cell and
    then **delete the clone, re-clone it from `src/fomo_db.sqlite3`, and re-run step 4** before
    executing again. A second execution against a clone the first execution already swept is
    exactly the regression this plan closes.

    Step 7 — confirm before stopping: the developer database's `stat -c '%Y %s'` still equals
    `tmp/34-07-devdb-stamp.txt`; `git status --porcelain` shows the SCHED-06 baseline JSON
    unmodified and the notebook as the only changed tracked file; all 12 code cells carry
    output; cell `7022f987`'s output names the new clone. Also scan the whole notebook file for
    credential-shaped text before committing (the three literals are named in the acceptance
    criteria) — the run makes authenticated portal calls, and an accidental traceback could
    carry a request URL.

    **PROHIBITED in this task, without exception:** executing the notebook without
    `FOMO_DATABASE_PATH` set to the fresh clone; running `python manage.py
    project_observation_calendar` (with or without `--dry-run`) or `python manage.py
    updatestatus` against `src/fomo_db.sqlite3`; re-executing the notebook against an
    already-swept clone; passing `--allow-errors` to nbconvert; and editing or regenerating
    `project_observation_calendar_demo.sched06-baseline.json`. If the baseline JSON ever shows as
    modified, restore it with `git checkout --` and find out which cell wrote it before doing
    anything else.
  </action>
  <acceptance_criteria>
    - Cell `05528b38`'s committed output contains at least one line matching `^\s+before: ` and
      one matching `^\s+after : `, and its count line matches `^[1-9][0-9]* of [0-9]+ pre-existing`
      (a non-zero re-titled count).
    - Cell `556d2a9f`'s committed output has a `First sweep` line and a `Second sweep` line whose
      payloads (everything after the first colon) differ, and every ` | `-separated facility
      segment of the `Second sweep` line contains `created: 0`, `updated: 0` and `site_lookups: 0`.
    - Cell `7022f987`'s committed output contains `routed to a scratch copy` and names
      `34-07-fresh-clone.sqlite3`.
    - `tmp/34-07-preflight-dryrun.txt` exists and its LCO segment's `updated:` count is greater
      than zero.
    - `stat -c '%Y %s'` of the main working tree's `src/fomo_db.sqlite3` equals the contents of
      `tmp/34-07-devdb-stamp.txt`.
    - `git status --porcelain docs/notebooks/pre_executed/project_observation_calendar_demo.sched06-baseline.json`
      prints nothing.
    - The notebook has 12 code cells and none of them has an empty `outputs` list.
    - A case-insensitive search of the whole notebook file for `api_key`, `Authorization` and
      `token=` returns no match (it returns none today).
  </acceptance_criteria>
  <verify>
    <automated>python -c "import json,re;nb=json.load(open('docs/notebooks/pre_executed/project_observation_calendar_demo.ipynb'));o={c['id']:''.join(t for out in c.get('outputs',[]) for t in out.get('text',[])) for c in nb['cells'] if c['cell_type']=='code'};t=o['05528b38'];s=o['556d2a9f'];assert re.search(r'^\s+before: ',t,re.M),'no before/after title pair in the takeover diff cell';assert re.search(r'^[1-9][0-9]* of [0-9]+ pre-existing',t,re.M),'takeover diff cell reports a zero re-titled count';assert [l for l in s.splitlines() if l.startswith('First sweep')][0].split(':',1)[1].strip()!=[l for l in s.splitlines() if l.startswith('Second sweep')][0].split(':',1)[1].strip(),'first and second sweep summary lines are identical'"</automated>
    <fails_when>non-zero exit, or any of the three AssertionError messages appears ("no before/after title pair in the takeover diff cell", "takeover diff cell reports a zero re-titled count", "first and second sweep summary lines are identical")</fails_when>
    <automated>python -c "import json;nb=json.load(open('docs/notebooks/pre_executed/project_observation_calendar_demo.ipynb'));o={c['id']:''.join(t for out in c.get('outputs',[]) for t in out.get('text',[])) for c in nb['cells'] if c['cell_type']=='code'};line=[l for l in o['556d2a9f'].splitlines() if l.startswith('Second sweep')][0];segs=line.split(' | ')[1:];assert segs,'no facility segments in the second sweep line';assert all(all(k in seg for k in ('created: 0','updated: 0','site_lookups: 0')) for seg in segs),segs"</automated>
    <fails_when>non-zero exit, or the printed AssertionError shows a facility segment missing one of the three zero counters</fails_when>
    <automated>python -c "import re,sys;t=open('tmp/34-07-preflight-dryrun.txt').read();m=re.search(r'LCO: created: (\d+), updated: (\d+)',t);sys.exit(0 if m and int(m.group(2))>0 else 1)"</automated>
    <fails_when>non-zero exit — the preflight capture has no LCO segment, or its updated count is zero, meaning the clone was not taken from an un-swept database</fails_when>
    <automated>test "$(stat -c '%Y %s' "$(cat tmp/34-07-devdb-path.txt)")" = "$(cat tmp/34-07-devdb-stamp.txt)"</automated>
    <fails_when>non-zero exit — the developer database's modification time or size changed during this plan, or either of the two stamp files is missing</fails_when>
    <automated>test -z "$(git status --porcelain docs/notebooks/pre_executed/project_observation_calendar_demo.sched06-baseline.json)"</automated>
    <fails_when>non-zero exit — git status printed a line for the baseline JSON, meaning the committed SCHED-06 evidence was rewritten</fails_when>
    <automated>python -c "import json,sys; nb=json.load(open('docs/notebooks/pre_executed/project_observation_calendar_demo.ipynb')); code=[c for c in nb['cells'] if c['cell_type']=='code']; empty=[i for i,c in enumerate(code) if not c.get('outputs')]; print('code cells',len(code),'without output',empty); sys.exit(0 if len(code)==12 and not empty else 1)"</automated>
    <fails_when>non-zero exit, or the printed line reports a code-cell count other than 12 or a non-empty list of cells without output</fails_when>
    <automated>python -c "import re,sys;src=open('docs/notebooks/pre_executed/project_observation_calendar_demo.ipynb').read();hits=sorted(set(re.findall(r'api_key|Authorization|token=',src,re.I)));print(hits);sys.exit(1 if hits else 0)"</automated>
    <fails_when>non-zero exit, or the printed list is not empty — credential-shaped text reached the committed notebook</fails_when>
  </verify>
  <done>
    The committed notebook shows a real takeover: a non-zero re-titled count with sample
    before -> after title pairs, a first sweep line that differs from the second, and a second
    sweep that is all zeros per facility. Cells `05528b38` and `556d2a9f` now abort a
    scratch-routed run that took nothing over. The developer database's stamp is unchanged, the
    SCHED-06 baseline JSON is unmodified, and no credential-shaped text is in the notebook.
  </done>
</task>

<task type="auto">
  <name>Task 2: Reconcile the notebook's prose with what the run produced, and record the runbook determination</name>
  <files>docs/notebooks/pre_executed/project_observation_calendar_demo.ipynb, .planning/phases/34-the-observation-projector-trigger/34-UAT.md</files>
  <read_first>
    - The executed output of cells `65e111fa`, `05528b38` and `556d2a9f` from Task 1 — the exact
      numbers this task quotes. Read them from the committed notebook, not from memory.
    - `docs/notebooks/pre_executed/project_observation_calendar_demo.ipynb` cells `7e7bd66e`,
      `8eeddc83` and `6a9bd576` — the three markdown cells this task edits.
    - `docs/runbooks/telescope_runs_calendar.rst` — the regions around the `updatestatus`,
      `post_save` and `unprojectable` hits, for the determination.
    - `.planning/phases/34-the-observation-projector-trigger/34-UAT.md` — the `## Gaps` entry for
      G-34-2 and the Test 2 section, so the housekeeping edit lands in the right place and
      touches nothing else.
  </read_first>
  <reversibility rating="reversible">Markdown and planning-doc text; revertible with git.</reversibility>
  <action>
    Every edit here is markdown or planning-doc prose. **Do not re-execute the notebook in this
    task** — markdown cells carry no output, so editing them does not invalidate the executed
    evidence, and a re-execution against the now-swept clone would recreate the exact regression
    this plan closes.

    Edit 1 — markdown cell id `8eeddc83`. Replace the placeholder sentence Task 1 left with the
    numbers this commit's run actually produced, written as a claim a reader can check against
    the cells below: state the first sweep's LCO created and updated counts exactly as cell
    `65e111fa` printed them, and state the re-titled count as the "N of M" pair exactly as cell
    `05528b38` printed it (the same two integers, in the same order, so the prose and the output
    cannot drift apart). Say plainly that those events are the ones the receiver failed to narrow
    before plan 34-05's fix, repaired here on the clone by the sweep exactly as the operator's
    next `updatestatus` will repair them on the developer database through the receiver alone.
    Keep the rule sentence Task 1 added about cloning from an un-swept database.

    Edit 2 — markdown cell id `7e7bd66e` (the intro). Its closing paragraph still says this
    notebook, unlike the repo's other pre-executed notebooks, runs directly against the developer
    database and not a scratch copy. That is now only half true and it is the first thing a
    reader sees. Rewrite it to say: the notebook is written for the developer database (the
    SCHED-06 section's claim is about that database's real records over real time, which a
    discarded copy could not support), and it honours a `FOMO_DATABASE_PATH` override so a
    re-execution can be routed to a fresh clone when the developer database is holding evidence a
    sweep would spend; name which of the two this commit's run used, and point at the Django
    setup cell's own printed output as the authority.

    Edit 3 — markdown cell id `6a9bd576`. Task 1 added the re-clone rule; confirm it reads
    correctly alongside the existing un-routed re-execution instructions and that the two are not
    in tension: the un-routed SCHED-06 re-execution stays the real one, the scratch-routed run is
    only for refreshing this notebook's own narrative.

    Edit 4 — `.planning/phases/34-the-observation-projector-trigger/34-UAT.md`, housekeeping,
    **additive only**. The `## Gaps` entry for G-34-2 still reads `status: failed`, but
    `34-VERIFICATION.md` re-ran after plans 34-05 and 34-06 and recorded that G-34-2 is fixed.
    Leave `status: failed` in place — it is the record of the original UAT verdict, and the
    file's status vocabulary is not yours to extend — and add two new keys immediately after it:
    `closed_by:` naming 34-05 (the receiver coercion) plus 34-06 and 34-07 (paired docs and live
    proof), and `closed_verified:` quoting 34-VERIFICATION.md's own finding with its date. Add
    one dated bullet to the Test 2 narrative recording the same closure and pointing at
    `tmp/34-06-updatestatus.txt`'s zero-unprojectable result as the evidence. Do not touch the
    SCHED-06 baseline section, Test 4's dated re-check table, or any existing line — this edit
    adds lines and removes none.

    Edit 5 — the runbook determination. Grep `docs/runbooks/telescope_runs_calendar.rst` for
    `updatestatus`, `post_save` and `unprojectable`, read the regions the hits land in, and
    confirm the conclusion stated in this plan's objective: this plan changes no operator-visible
    behaviour, so the page needs no edit. Record that conclusion in the SUMMARY with the line
    numbers you actually read. If you find prose that does contradict current behaviour, stop and
    report it rather than editing the page here — a runbook edit is a scope change that belongs
    in its own plan.
  </action>
  <acceptance_criteria>
    - Markdown cell `8eeddc83` contains the literal "N of M" pair that appears in cell
      `05528b38`'s output count line (same two integers, same order).
    - Markdown cell `8eeddc83` contains the word `un-swept` (the cloning rule survived the
      reconciliation).
    - Markdown cell `7e7bd66e` names `FOMO_DATABASE_PATH`.
    - `34-UAT.md` still contains all three pre-existing anchors — `gap_id: G-34-2`,
      `status: failed`, and the SCHED-06 baseline timestamp `2026-09-11T04:44:59.526430+00:00` —
      and now also contains a `closed_by:` key, proving the edit added lines and removed none.
    - `grep -n -E 'updatestatus|post_save|unprojectable' docs/runbooks/telescope_runs_calendar.rst`
      prints at least one line, and the SUMMARY records the determination with those line numbers.
    - The notebook's code cells still carry the outputs Task 1 produced (no re-execution
      happened): Task 1's first verify command still passes.
  </acceptance_criteria>
  <verify>
    <automated>python -c "import json,re;nb=json.load(open('docs/notebooks/pre_executed/project_observation_calendar_demo.ipynb'));cells={c['id']:c for c in nb['cells']};out=''.join(t for o in cells['05528b38'].get('outputs',[]) for t in o.get('text',[]));m=re.search(r'^([1-9][0-9]*) of ([0-9]+) pre-existing',out,re.M);assert m,'no non-zero re-titled count in the takeover cell output';md=''.join(cells['8eeddc83']['source']);pair=m.group(1)+' of '+m.group(2);assert pair in md,'framing cell does not quote '+pair;assert 'un-swept' in md,'framing cell lost the un-swept cloning rule';assert 'FOMO_DATABASE_PATH' in ''.join(cells['7e7bd66e']['source']),'intro cell does not name the override'"</automated>
    <fails_when>non-zero exit, or one of the four AssertionError messages appears ("no non-zero re-titled count in the takeover cell output", "framing cell does not quote ...", "framing cell lost the un-swept cloning rule", "intro cell does not name the override")</fails_when>
    <automated>grep -q 'gap_id: G-34-2' .planning/phases/34-the-observation-projector-trigger/34-UAT.md && grep -q 'status: failed' .planning/phases/34-the-observation-projector-trigger/34-UAT.md && grep -q '2026-09-11T04:44:59.526430+00:00' .planning/phases/34-the-observation-projector-trigger/34-UAT.md</automated>
    <fails_when>non-zero exit — one of the three pre-existing anchors was removed or rewritten by the housekeeping edit (the G-34-2 gap entry, its original `status: failed` verdict, or the SCHED-06 baseline capture timestamp), which means the edit was not additive</fails_when>
    <automated>grep -n 'closed_by:' .planning/phases/34-the-observation-projector-trigger/34-UAT.md</automated>
    <fails_when>no lines printed / non-zero exit — the G-34-2 closure was never recorded in the tracker</fails_when>
    <automated>grep -n -E 'updatestatus|post_save|unprojectable' docs/runbooks/telescope_runs_calendar.rst | head -20</automated>
    <fails_when>no lines printed / non-zero exit — the runbook regions the determination rests on do not exist, so the determination was never actually checked</fails_when>
  </verify>
  <done>
    The notebook's framing cell quotes this run's own re-titled pair and first-sweep counts, the
    intro cell names the override and which database this commit used, the "What happens next"
    section carries the re-clone rule, and no executed output changed. `34-UAT.md` records
    G-34-2's closure additively, with the SCHED-06 baseline section and Test 4's table untouched.
    The runbook determination is recorded in the SUMMARY with the line numbers read.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Guard the committed evidence with a test that fails when it goes empty</name>
  <files>solsys_code/tests/test_projector_demo_notebook.py</files>
  <read_first>
    - `docs/notebooks/pre_executed/project_observation_calendar_demo.ipynb` — the committed
      outputs of cells `7022f987`, `05528b38`, `556d2a9f` and `250b5d0b`, which are exactly what
      this test parses. Read them after Task 1, not before.
    - `docs/notebooks/pre_executed/project_observation_calendar_demo.sched06-baseline.json` — its
      top-level keys are `captured_at`, `proposal`, `record_count`, `records`.
    - `solsys_code/tests/test_calendar_utils.py` — the repo's `SimpleTestCase` precedent (its
      `TestCoerceScheduleDatetime` class) for a database-free test class in this app.
  </read_first>
  <behavior>
    - The committed notebook names the database its run used: cell `7022f987`'s output contains
      either "the developer database itself" or "routed to a scratch copy".
    - When that output says the run was routed to a scratch copy, cell `05528b38`'s output lists
      at least one before -> after title pair and reports a non-zero re-titled count, and cell
      `556d2a9f`'s "First sweep" and "Second sweep" payloads differ.
    - Every ` | `-separated facility segment of the "Second sweep" line reports `created: 0`,
      `updated: 0` and `site_lookups: 0` — convergence, for any run.
    - Cell `250b5d0b`'s output contains the committed baseline JSON's own `captured_at` value, so
      the notebook and the SCHED-06 evidence file cannot drift apart unnoticed.
    - No credential-shaped text (`api_key`, `Authorization`, `token=`, case-insensitive) appears
      anywhere in the notebook file.
    - Pointed at a notebook whose takeover evidence has been emptied, the suite fails.
  </behavior>
  <action>
    Create `solsys_code/tests/test_projector_demo_notebook.py`: a database-free
    `django.test.SimpleTestCase` guard over the committed demo notebook, so the evidence is
    checkable on every test run rather than only when someone re-executes the notebook.

    Resolve the notebook path from this test file's own location —
    `Path(__file__).resolve().parents[2]` is the repository root, then
    `docs/notebooks/pre_executed/project_observation_calendar_demo.ipynb` — with an override read
    from the `FOMO_DEMO_NOTEBOOK_PATH` environment variable. Document that override in the module
    docstring as what it is: the hook that lets this plan prove the guard fails for the right
    reason by pointing it at a deliberately emptied copy. Resolve the baseline JSON as a sibling
    of the notebook.

    Load the notebook once in `setUpClass` and build a mapping from cell id to that cell's joined
    stream-output text (concatenate the `text` entries of each output, ignoring non-stream output
    types). Address cells by their nbformat id — `7022f987`, `05528b38`, `556d2a9f`, `250b5d0b` —
    never by position, so inserting a cell cannot silently retarget an assertion. Fail with a
    clear message if an expected id is missing.

    Write one test per behavior listed above. For the routing test, treat "routed to a scratch
    copy" appearing in cell `7022f987`'s output as the condition; the takeover assertions apply
    only in that case, because an un-routed re-execution against an already-converged developer
    database is the legitimate SCHED-06 case described in the notebook's "What happens next"
    section, and this guard must not block it. In that un-routed case, assert instead that the
    notebook says so — that cell `556d2a9f`'s output contains the converged-run statement Task 1
    added. For the re-titled count, match the count line with a regular expression requiring a
    non-zero leading integer. For the sweep-divergence check, compare the text after the first
    colon of the "First sweep" and "Second sweep" lines. For the credential check, scan the raw
    file text, not the parsed outputs, so text hiding in a cell source is caught too.

    Follow the repo's conventions: single quotes, 120-column lines, Google-style docstrings on
    module and helper (test classes and methods are exempt from D101/D102 under the per-file
    ignores), no import of `solsys_code.views` or `solsys_code.ephem_utils` (importing either
    triggers the SPICE kernel download), and no database access at all.
  </action>
  <acceptance_criteria>
    - `python manage.py test solsys_code.tests.test_projector_demo_notebook` exits zero with no
      `FAILED` line.
    - With `FOMO_DEMO_NOTEBOOK_PATH` pointed at a copy of the notebook whose cell `05528b38`
      outputs have been emptied, the same test command exits non-zero.
    - `pre-commit run ruff --all-files` and `pre-commit run ruff-format --all-files` both report
      the new file passing.
    - The test module addresses cells by nbformat id: the four ids `7022f987`, `05528b38`,
      `556d2a9f`, `250b5d0b` appear in the file.
  </acceptance_criteria>
  <verify>
    <automated>python manage.py test solsys_code.tests.test_projector_demo_notebook</automated>
    <fails_when>non-zero exit, or "FAILED" appears in the summary line, or the run reports "Ran 0 tests"</fails_when>
    <automated>python -c "import json;nb=json.load(open('docs/notebooks/pre_executed/project_observation_calendar_demo.ipynb'));[c.update(outputs=[]) for c in nb['cells'] if c.get('id')=='05528b38'];json.dump(nb,open('tmp/34-07-emptied-demo.ipynb','w'))" && ! FOMO_DEMO_NOTEBOOK_PATH=tmp/34-07-emptied-demo.ipynb python manage.py test solsys_code.tests.test_projector_demo_notebook</automated>
    <fails_when>non-zero exit — which here means the test suite PASSED against a notebook whose takeover evidence was emptied, so the guard does not guard anything</fails_when>
    <automated>grep -c "556d2a9f" solsys_code/tests/test_projector_demo_notebook.py</automated>
    <fails_when>prints 0 / non-zero exit — the second-sweep cell is not addressed by id</fails_when>
    <automated>pre-commit run ruff --all-files && pre-commit run ruff-format --all-files</automated>
    <fails_when>non-zero exit, or either hook reports "Failed"</fails_when>
  </verify>
  <done>
    `solsys_code/tests/test_projector_demo_notebook.py` passes against the committed notebook and
    fails against a copy whose takeover evidence has been emptied. It runs with no database
    access, addresses cells by nbformat id, and allows the legitimate un-routed converged run
    while requiring that run to say so in its own output. Both ruff hooks are clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fresh clone -> developer database | Every command in this plan must write to `tmp/34-07-fresh-clone.sqlite3` only; `src/fomo_db.sqlite3` holds UAT Test 4's SCHED-06 evidence, which a stray sweep destroys irrecoverably. |
| Notebook execution -> committed artifacts | A re-execution rewrites every cell output and, without the cell `250b5d0b` guard, the committed SCHED-06 baseline JSON. |
| FOMO -> LCO observation portal | The sweep's one-time observed-site lookup authenticates with `LCO_API_KEY` read from the gitignored `src/fomo/local_settings.py`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34-07-01 | Tampering | `src/fomo_db.sqlite3` (developer database, SCHED-06 evidence) | high | mitigate | Every command carries an absolute `FOMO_DATABASE_PATH` pointing at `tmp/34-07-fresh-clone.sqlite3`; cell `7022f987`'s own guard raises unless the resolved database is that override and is not the developer database; the developer database's size and modification time are stamped into `tmp/34-07-devdb-stamp.txt` before the first command and re-compared as an automated verify. `git status` is explicitly NOT used for this file — it is gitignored (`.gitignore:64`) and would report clean no matter what was written. |
| T-34-07-02 | Tampering | `project_observation_calendar_demo.sched06-baseline.json` | high | mitigate | Cell `250b5d0b`'s existing scratch-routed guard reads the file and never writes it; a `git status --porcelain` gate on that tracked path is an automated verify in Task 1; Task 3's test asserts the notebook's own output still quotes that file's `captured_at`, so a swapped baseline is caught too. |
| T-34-07-03 | Repudiation | The committed takeover evidence | high | mitigate | The preflight dry-run (`tmp/34-07-preflight-dryrun.txt`) proves the clone was un-swept BEFORE the notebook runs; the new assertions in cells `05528b38` and `556d2a9f` abort `nbconvert --execute` when a routed run takes nothing over; `--allow-errors` is prohibited; Task 3's test re-checks the committed artifact on every suite run and is itself proven to fail on emptied evidence. |
| T-34-07-04 | Information Disclosure | Notebook output, `tmp/` evidence files, SUMMARY | high | mitigate | Never print, echo or paste `LCO_API_KEY` or a portal request URL carrying a token; quote only command summary lines in the SUMMARY; an automated verify scans the whole notebook file for credential-shaped text (none present today) before it is committed. |
| T-34-07-05 | Denial of Service | LCO observation portal | low | accept | The run makes at most the same one-time observed-site lookups plan 34-06 made over the same corpus (14 in that run), bounded by the number of newly observed records on one clone. |
| T-34-07-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no packages; `jupyter`, `nbconvert` and `nbformat` are already present in the project environment. No `## Package Legitimacy Audit` is required because no package-manager install task exists. |
</threat_model>

## Artifacts this phase produces

**Files created**

- `solsys_code/tests/test_projector_demo_notebook.py` — database-free `SimpleTestCase` guard over
  the committed demo notebook's evidence. Public names: the test class (one class), a module-level
  notebook-path constant honouring `FOMO_DEMO_NOTEBOOK_PATH`, a module-level baseline-path
  constant, and a helper that maps nbformat cell id to joined stream-output text.

**Files materially changed**

- `docs/notebooks/pre_executed/project_observation_calendar_demo.ipynb`
  - cell `05528b38` — new `scratch_routed` re-read, new `changed_keys` comprehension, new
    changed-field count line, new scratch-routed assertions over `changed_keys` / `changed_titles`,
    new converged-run statement on the un-routed path.
  - cell `556d2a9f` — new `import re`, new `first_sweep_work` total parsed from the first sweep's
    per-facility `created:`/`updated:` counters, new printed total, new scratch-routed assertions
    (non-zero first-sweep work, first summary differs from second), new converged-run statement on
    the un-routed path.
  - cell `35debc54` — PROJ-05 and TRIG-03 evidence entries become f-strings carrying this run's
    re-titled pair and first-sweep created+updated total.
  - markdown cells `7e7bd66e`, `8eeddc83`, `6a9bd576` — reconciled prose (override acknowledged,
    observed numbers quoted, un-swept re-clone rule stated).
  - all 12 code cells — outputs regenerated by the fresh-clone execution.
- `.planning/phases/34-the-observation-projector-trigger/34-UAT.md` — additive `closed_by:` /
  `closed_verified:` keys on the G-34-2 gap entry and one dated Test 2 bullet.

**Gitignored evidence produced (never committed, quoted in the SUMMARY)**

- `tmp/34-07-devdb-path.txt`, `tmp/34-07-devdb-stamp.txt`, `tmp/34-07-preflight-dryrun.txt`,
  `tmp/34-07-fresh-clone.sqlite3`, `tmp/34-07-emptied-demo.ipynb`.

## Gap coverage audit (G-34-3)

| Source item (from `34-VERIFICATION.md` `gaps[0].missing`) | Covered by | Status |
|---|---|---|
| Re-execute against a scratch copy taken from an un-swept state, so cell 9 shows real before -> after title pairs and cell 10's two summary lines differ | Task 1 steps 1-7 (fresh clone, preflight gate, single execution) | COVERED |
| Add an assertion to cell 10 that the first sweep summary differs from the second, so the demonstration can never silently go empty again | Task 1 Edit 2 (in-notebook), plus Task 3 (repo-level guard over the committed artifact) | COVERED |
| Reconcile cell 5's "expected to report a non-zero `updated` count" prose with what the re-execution actually produces | Task 1 Edit 4 (removes the falsified expectation, leaves a placeholder) and Task 2 Edit 1 (fills it with the observed numbers) | COVERED |
| Do NOT satisfy this by running anything against `src/fomo_db.sqlite3` | Task 1 prohibitions + stamp verify; T-34-07-01 | COVERED |
| Cell 19's closing table pointing PROJ-05/TRIG-03 at cells that show nothing (from the gap's `issue` text) | Task 1 Edit 3 (evidence entries carry this run's numbers) | COVERED |
| CLAUDE.md paired-docs rule: notebook in `files_modified` up front; runbook determination recorded either way | `files_modified` frontmatter; Task 2 Edit 5 | COVERED |

Not in scope for this plan, and deliberately so: the `deferred:` title-prefix vocabulary (Phase
37), the two `human_verification` items (SCHED-06 over real nights, and the interleaved-save
re-run of UAT Test 2 — both need elapsed time or an operator and are tracked in `34-UAT.md`), and
G-34-2 itself, which plans 34-05 and 34-06 already closed and `34-VERIFICATION.md` re-confirmed.

<verification>
- The committed notebook's takeover cell reports a non-zero re-titled count with sample
  before -> after title pairs, and its first and second sweep summary lines differ, with the
  second all zeros per facility segment.
- `tmp/34-07-preflight-dryrun.txt` shows a non-zero LCO `updated:` count, proving the clone was
  taken from an un-swept state before the notebook ran.
- The developer database's `stat -c '%Y %s'` matches `tmp/34-07-devdb-stamp.txt`, and
  `git status --porcelain` on `project_observation_calendar_demo.sched06-baseline.json` is empty.
- `python manage.py test solsys_code.tests.test_projector_demo_notebook` passes, and the same
  command against an emptied copy of the notebook fails.
- The full project test gate (`workflow.test_command` in `.planning/config.json`) passes; never
  run `solsys_code.tests.test_views.TestEphemeris`, which segfaults in native ASSIST.
- `pre-commit run ruff --all-files` and `pre-commit run ruff-format --all-files` stay clean. The
  commit itself also runs `sphinx-build` and the legacy pytest hook; if `sphinx-build` fails on
  the re-executed notebook, fix the notebook — never bypass the hook.
</verification>

<success_criteria>
- The paired demo notebook demonstrates the one-time takeover with real executed output again,
  and says in its own prose exactly what its own output shows (CLAUDE.md paired-docs rule).
- A scratch-routed re-execution that takes nothing over now aborts instead of committing empty
  evidence, and a repo-level test catches it even if someone commits such a notebook by hand.
- `src/fomo_db.sqlite3` and `project_observation_calendar_demo.sched06-baseline.json` are both
  untouched, so UAT Test 4's SCHED-06 evidence survives this plan intact.
- The runbook determination is recorded either way, per CLAUDE.md's directory-scoped rule.
</success_criteria>

<output>
Create `.planning/phases/34-the-observation-projector-trigger/34-07-SUMMARY.md` when done.
Record in it: the fresh clone's path, the preflight dry-run's LCO segment, the notebook's own
first- and second-sweep summary lines and its re-titled count, the developer database's stamp
before and after (identical), proof the SCHED-06 baseline JSON is unmodified, the emptied-copy
run that proves the new test fails for the right reason, and the runbook determination with the
line numbers read — all credential-free.
</output>