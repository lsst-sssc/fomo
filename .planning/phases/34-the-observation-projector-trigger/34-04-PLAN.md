---
phase: 34-the-observation-projector-trigger
plan: 04
type: execute
wave: 3
depends_on: ["34-02", "34-03"]
files_modified:
  - docs/notebooks/pre_executed/project_observation_calendar_demo.ipynb
  - docs/notebooks/pre_executed/sync_lco_observation_calendar_demo.ipynb
  - docs/notebooks/pre_executed/sync_gemini_observation_calendar_demo.ipynb
  - docs/notebooks/pre_executed/campaign_lifecycle_demo.ipynb
  - docs/notebooks.rst
  - docs/runbooks/telescope_runs_calendar.rst
  - CLAUDE.md
  - .planning/todos/pending/2026-09-02-retarget-adapt-03-to-soar-and-caveat-phase-33-gemini-outcome.md
  - .planning/todos/pending/2026-06-23-extract-site-telescope-mapping-and-instrument-extraction-int.md
autonomous: true
requirements: [TRIG-03, ANNOT-03, SCHED-06]

estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The sweep command has a paired, pre-executed demo notebook committed with its output, registered in `docs/notebooks.rst` and in CLAUDE.md's notebook map (TRIG-03, CLAUDE.md paired-docs rule)."
    - "The notebook shows the first sweep taking over the legacy URL-keyed events in place: every event's url, title, start, end and companion-row links snapshotted before and after, with no migration script involved (D-19)."
    - "The notebook shows the `RUN:`-prefixed, `GEM:`-prefixed and blank-url event sets byte-identical across that takeover (D-19, PROJ-05)."
    - "The notebook reconciles the whole real corpus, not a sample: the LCO/SOAR record count is set against the facility-URL-keyed event count with the arithmetic shown, and every record without such an event is enumerated with the reason it is unprojectable (ROADMAP criterion 1, PROJ-01, D-19)."
    - "The notebook's first and second sweep summary lines are printed together and differ — the first carries non-zero `created`/`updated` and `site_lookups`, the second zeros — so an all-unchanged second run reads as convergence rather than as silence (TRIG-03, D-08, D-17)."
    - "The notebook carries a SCHED-06 baseline section: a committed snapshot of each pending `KEY2026B-004` record's status, schedule fields and event span/title, with the instruction that only TOM's own `updatestatus` is run between the baseline and the re-execution (D-20)."
    - "`34-UAT.md` records the baseline date and the dates the live nights are watched, so the post-nights re-execution is a traceable follow-up rather than a forgotten one (D-20)."
    - "`docs/notebooks/pre_executed/sync_lco_observation_calendar_demo.ipynb` is gone, not left demonstrating a command that no longer exists (ANNOT-03, D-18)."
    - "`docs/runbooks/telescope_runs_calendar.rst`'s LCO/SOAR sync section is replaced by a projector-and-sweep section describing automatic narrowing, the sweep's flags, and when an operator would run it (ANNOT-03, D-18)."
    - "The runbook tells an operator that the calendar page carries its own marker legend and what each marker means, so the vocabulary plan 34-03 put on the page is documented rather than left to be inferred (ANNOT-03, PROJ-03, D-02, D-13)."
    - "The runbook describes the pop-up's Observation series block — group name, night n of N, links back — and says it is read from the companion row at request time and never stored in the entry, matching how the page already describes the campaign block (ANNOT-03, D-04, PROJ-05)."
    - "The runbook carries a one-time title change note beside the projector narrative, so the legacy LCO/SOAR entries all changing title in the first sweep reads as expected rather than as a fault, the same way the reconciler's own one-time change is already framed (ANNOT-03, D-19)."
    - "The runbook's command cheat-sheet lists the sweep and no longer lists the retired command (D-18)."
    - "The runbook's troubleshooting section no longer describes the retired command's `[UNVERIFIED]` fallback and `telescope_api_failed` counter, and describes the sweep's `unprojectable` and `site_lookup_failed` counters instead (D-06, D-18)."
    - "The runbook's Gemini section and the Gemini demo notebook both state that the Gemini facility has no status or URL read-back, so Gemini events are submission-echo only and never narrow — and that the projector ignores Gemini records by design (ANNOT-03, D-16, D-21)."
    - "The Gemini command's own behaviour is unchanged by this plan — only its documentation gains the caveat (D-21)."
    - "`campaign_attribution.py`'s paired notebook is updated in the same phase as the `_extract_lco_site_code()` branch that plan 34-02 adds: `campaign_lifecycle_demo.ipynb` gains a cell resolving `FTN` / `FTS` / `SOAR` to their LCO site codes and scoring a site-level match, committed with executed output (CLAUDE.md paired-docs rule, D-05, D-07)."
    - "The folded todo about retargeting the SOAR/Gemini scope is marked done, and the overtaken todo about extracting the site/telescope mapping is closed with its reason (D-21, folded-todos section of 34-CONTEXT.md)."
  artifacts:
    - docs/notebooks/pre_executed/project_observation_calendar_demo.ipynb
    - docs/notebooks.rst
    - docs/runbooks/telescope_runs_calendar.rst
    - CLAUDE.md
    - .planning/phases/34-the-observation-projector-trigger/34-UAT.md
  key_links:
    - "`docs/notebooks.rst` toctree -> the new notebook path: without this the notebook exists but is invisible in the built docs."
    - "CLAUDE.md's notebook map -> the module/notebook pairing: what makes a future planner scope the notebook up front instead of discovering the rule after the fact."
    - "the notebook's baseline snapshot -> the post-nights re-execution -> SCHED-06's verdict: the only evidence that closes spike 004's PARTIAL."
  prohibitions:
    - statement: "The notebook MUST NOT print, log, or embed a portal credential, an API key, a raw portal response body, or any submitter contact detail in a committed output cell — a pre-executed notebook is committed with its output and is public forever."
      status: resolved
      verification: judgment
    - statement: "The SCHED-06 section MUST NOT run the sweep between the baseline and the re-check; the whole claim is that a record narrows with nobody running anything, and a sweep in the middle would quietly prove the opposite of what it says."
      status: resolved
      verification: judgment
    - statement: "The retired command's notebook MUST NOT be left in the tree demonstrating a command that no longer exists — a doc that cannot be executed is worse than no doc, because it reads as current."
      status: resolved
      verification: judgment
    - statement: "The Gemini caveat MUST NOT be written as a defect report about the Gemini facility; it states an operating limitation an operator needs to know, without editorialising about upstream code."
      status: resolved
      verification: judgment
---

<objective>
Ship the paired docs this phase owes and stand up SCHED-06's live-narrowing evidence: a
pre-executed demo notebook for the sweep, the runbook's projector section, the Gemini
no-read-back caveat, and the retirement of the old command's notebook.

Purpose: CLAUDE.md treats paired docs as part of the deliverable, and this phase deletes a command
whose notebook and runbook section would otherwise be left describing something that no longer
exists. It is also where SCHED-06 gets its baseline: the requirement is a verification-over-time
claim, and the only way to close spike 004's PARTIAL verdict is to snapshot the real records now
and re-execute after real nights have passed.

Output: `docs/notebooks/pre_executed/project_observation_calendar_demo.ipynb` committed with its
executed output, the old notebook deleted and both registries updated, the runbook's LCO section
replaced and its Gemini section caveated, and a `34-UAT.md` recording the dates the live nights
are watched.
</objective>

<execution_context>
@/home/tlister/git/fomo_devel/.claude/gsd-core/workflows/execute-plan.md
@/home/tlister/git/fomo_devel/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-the-observation-projector-trigger/34-CONTEXT.md
@.planning/phases/34-the-observation-projector-trigger/34-RESEARCH.md
@.planning/phases/34-the-observation-projector-trigger/34-02-SUMMARY.md
@.planning/phases/34-the-observation-projector-trigger/34-03-SUMMARY.md
@CLAUDE.md
</context>

<!-- planner-discipline-allow: sync_lco_observation_calendar -->
<!-- planner-discipline-allow: retarget-adapt-03-to-soar -->
<!-- planner-discipline-allow: extract-site-telescope-mapping -->

<tasks>

<task type="auto">
  <name>Task 1: The sweep's pre-executed demo notebook — takeover diff and the SCHED-06 baseline</name>
  <files>docs/notebooks/pre_executed/project_observation_calendar_demo.ipynb, docs/notebooks/pre_executed/sync_lco_observation_calendar_demo.ipynb, docs/notebooks/pre_executed/campaign_lifecycle_demo.ipynb, docs/notebooks.rst, CLAUDE.md, .planning/phases/34-the-observation-projector-trigger/34-UAT.md</files>
  <precondition>The developer database `src/fomo_db.sqlite3` exists with all migrations applied and carries the real LCO records this notebook snapshots, and the LCO observation portal is reachable for the sweep's one-time observed-site lookups. Assert both before running the notebook: `python manage.py migrate --check` exits 0, and a Django shell reports a non-zero `ObservationRecord.objects.filter(facility='LCO').count()`. If either fails, halt and report rather than executing the notebook against an empty database.</precondition>
  <read_first>
    - docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb (whole file — the sibling sweep's notebook: the Django-setup boilerplate cell, the `parents[2]` path handling, the markdown framing, the `call_command` dry-run-then-real-run structure, and the closing requirement-to-evidence table)
    - docs/notebooks/pre_executed/sync_lco_observation_calendar_demo.ipynb (whole file — the notebook being deleted; its before/after count structure is the model for the takeover section, and its requirement table shows what a reader expects to find)
    - .planning/spikes/004-live-narrowing-updatestatus/recheck.py (whole file — the `snapshot()` function and the baseline/recheck mode switch; the SCHED-06 section is built from this)
    - .planning/spikes/002-observation-projector/sweep.py (whole file — the namespace-isolation check and the twice-run idempotency proof)
    - docs/notebooks/README.md (whole file — why `pre_executed/` exists and what may go in it)
    - docs/notebooks.rst (whole file — the toctree entry to replace)
    - CLAUDE.md lines 118-145 (the paired-docs rule and the notebook map this task extends)
    - docs/notebooks/pre_executed/campaign_lifecycle_demo.ipynb, its attribution section only (the orphan calendar-event cell and the `campaign_attribution` import beside it — where the new site-code cell goes; the rest of the notebook is not to be edited)
    - .planning/phases/34-the-observation-projector-trigger/34-02-SUMMARY.md (the `_extract_lco_site_code()` branch as it actually shipped, so the notebook demonstrates the real function rather than the planned one)
    - .pre-commit-config.yaml lines 12-35 (the `jupyter-nb-clear-output` hook and its `^docs/notebooks/pre_executed` exclusion, and `check-added-large-files --maxkb=500`)
    - .planning/phases/34-the-observation-projector-trigger/34-CONTEXT.md decisions D-17, D-19, D-20
  </read_first>
  <reversibility rating="reversible">D-19: the first sweep re-titles and re-links the legacy URL-keyed events in the developer database. The churn is one-time and accepted; a later title-builder change plus one more sweep re-derives every event from current record state, so nothing here is a one-way door.</reversibility>
  <action>
Create `docs/notebooks/pre_executed/project_observation_calendar_demo.ipynb`, execute it against the
developer database, and commit it with its output. Delete the retired command's notebook and update
both registries.

Notebook structure, following how `reconcile_campaign_runs_demo.ipynb` is put together:

1. **Title and framing markdown.** `# Projecting Observation Records onto the Calendar`. State what
   the notebook demonstrates: `solsys_code/observation_projector.py` and
   `solsys_code/management/commands/project_observation_calendar.py` — the post-save receiver that
   draws and narrows an event with no operator command, and the sweep that is its backstop for bulk
   write paths. State that it lives in `pre_executed/` because it is database-dependent and makes
   real portal calls, so it is not run during Sphinx / CI / ReadTheDocs builds.
2. **Django setup cell.** Copy the sibling notebook's boilerplate verbatim (the `parents[2]` path
   insertion and `django.setup()` with `DJANGO_SETTINGS_MODULE='src.fomo.settings'`).
3. **The receiver, demonstrated.** Create a throwaway LCO `ObservationRecord` inside a transaction
   that is rolled back at the end, using `NonSiderealTargetFactory` for its target. Show: the event
   appearing on `save()` with a `[Q]` title and the request-window span; the same event narrowing to
   a `[S]` block span after a schedule-only save; the title turning `[O]` on a successful terminal
   status. Print the event's `(url, title, start_time, end_time)` at each step. This is the
   "no operator command" claim, shown rather than asserted.
4. **Takeover section (D-19).** Snapshot every existing `CalendarEvent` into a list of
   `(url, title, start_time, end_time, companion-row observation_record id, observation_group id)`
   tuples, partitioned into four sets by key family: facility-URL-keyed, `RUN:`-prefixed,
   `GEM:`-prefixed and blank-url. Run `call_command('project_observation_calendar', '--dry-run')`
   and show its would-change counters. Then run the real sweep. Re-snapshot and print: how many
   URL-keyed events were re-titled and re-linked, a small sample of before/after title pairs, and an
   explicit equality assertion that the `RUN:`, `GEM:` and blank-url tuple sets are identical before
   and after. Then run the sweep a second time and print both summary lines side by side. The two
   lines must differ and be shown to differ: the first carries non-zero `created`/`updated` and a
   non-zero `site_lookups` for the newly observed records, the second carries zeros for all three
   and reports every record `unchanged`. Say in the surrounding markdown that the first run's
   `updated` count includes the records whose title gained its observed-telescope token during that
   same run, so an all-unchanged second run is evidence of convergence rather than of a sweep that
   never reported anything. Do not wrap the takeover in a transaction — it is the real one-time
   takeover, and its result is what the calendar should carry from here on.
5. **Per-corpus reconciliation (ROADMAP criterion 1).** Account for the whole real corpus, not a
   sample. In one cell: count every LCO and SOAR `ObservationRecord` in the developer database
   (`ObservationRecord.objects.filter(facility__in=PROJECTED_FACILITIES).count()`), count the
   `CalendarEvent` rows whose `url` is a facility observation URL, and assert the two agree once the
   unprojectable records are subtracted — print the arithmetic, not just a boolean. Then enumerate
   **every** record with no facility-URL-keyed event as a table of
   `(observation_id, facility, status, reason)`, where the reason is the `unprojectable` class name
   the sweep logged for it (missing `parameters['start'/'end']`, unparsable dates, instrument
   extraction failure). An empty table is the strongest result; a non-empty one must be short enough
   to read in full, because "which records did not make it and why" is the claim this section
   exists to answer. Add a second small table tallying the projected events by stage marker
   (`[Q]`/`[S]`/`[O]`/`[X]`/`[C]`/`[F]`/`[?]`) so a reader can see the corpus is spread across the
   lifecycle rather than stuck in one stage. Print counts and identifiers only — no `parameters`
   dicts.
6. **SCHED-06 baseline section (D-20).** Build the spike's snapshot over the pending
   `KEY2026B-004` records: per record, `(observation_id, target name, status, scheduled_start,
   scheduled_end, event start, event end, event title)`. Print a stage tally and the full table.
   Write the same data to a JSON file beside the notebook so the re-execution can diff against it
   rather than against a rendered cell. Follow it with a markdown cell stating, in the operator's
   own terms, exactly what happens next: over the coming nights run **only**
   `python manage.py updatestatus` — nothing else, and specifically not the sweep — then re-execute
   this notebook and commit it again. State plainly that if a sweep were run in between, the
   narrowing this section claims would no longer be evidence of anything.
7. **Closing table.** A requirement-to-evidence table mapping PROJ-01, PROJ-02, PROJ-03, PROJ-05,
   TRIG-01, TRIG-02, TRIG-03, ANNOT-03 and SCHED-06 to the cell that demonstrates each — with
   SCHED-06 marked as pending the post-nights re-execution rather than claimed.

Output hygiene for a committed-with-output notebook: print only what a reader needs. Never print a
whole `record.parameters` dict (it can carry submission detail), never print a facility's headers or
settings, never print a raw portal response. Truncate any per-record table to a readable sample.
Keep the executed file under 500 KB — `check-added-large-files` fails the commit above that.

Execute with `jupyter nbconvert --to notebook --execute --inplace
docs/notebooks/pre_executed/project_observation_calendar_demo.ipynb` and commit the file with its
output. The `jupyter-nb-clear-output` hook excludes `^docs/notebooks/pre_executed`, so the output
survives the commit.

Registries:

- `docs/notebooks.rst`: replace the `Syncing the LCO Observation Calendar` toctree line with
  `Projecting Observation Records onto the Calendar
  <notebooks/pre_executed/project_observation_calendar_demo>`, keeping the surrounding entries in
  place.
- `CLAUDE.md`: in the paired-docs notebook map, replace the
  `sync_lco_observation_calendar.py -> sync_lco_observation_calendar_demo.ipynb` pairing with
  `solsys_code/observation_projector.py` and
  `solsys_code/management/commands/project_observation_calendar.py` ->
  `project_observation_calendar_demo.ipynb`. Both modules pair to the one notebook, the way the
  reconciler pairing already does. Change nothing else in that rule's text.
- `git rm docs/notebooks/pre_executed/sync_lco_observation_calendar_demo.ipynb`.

`docs/notebooks/pre_executed/campaign_lifecycle_demo.ipynb` — the paired notebook CLAUDE.md maps to
`solsys_code/campaign_attribution.py`, which plan 34-02 Task 3 changes by giving
`_extract_lco_site_code()` a new resolution branch for the three renamed telescope labels. The rule
fires on that behaviour change, so the notebook is updated in this phase rather than left to drift.
It is updated here, in the plan that already carries the developer-database precondition and the
nbconvert step, rather than in 34-02, whose tasks run entirely against the in-memory test database:

- Add one markdown cell plus one code cell in the attribution section, after the existing orphan
  calendar-event walkthrough. The code cell shows a `CalendarEvent.telescope` of `FTN`, `FTS` and
  `SOAR` each resolving to its LCO site code through `campaign_attribution`, and shows one of them
  scoring a site-level match against a run at that obscode rather than dropping to an
  aperture-only score. Print the label, the resolved site code and the match level — nothing else.
- The markdown cell says why the branch exists: the three 2m0/4m0 labels no longer begin with a
  three-letter site code after the D-07 rename, so without an explicit mapping an `FTN` or `SOAR`
  event would quietly score one level lower than before. Name it as the reason the notebook grew
  this cell, so a later reader can see the pairing was honoured rather than guessed at.
- Every pre-existing cell is left alone. The notebook exercises no 2m0 or 4m0 telescope label
  today, so the rename changes none of its existing outputs; the new cell is the only behavioural
  addition, and the rest of the file re-executes to the same results.
- Re-execute with `jupyter nbconvert --to notebook --execute --inplace` and commit with output, the
  same way the sweep's notebook is handled. The notebook already builds its own scratch copy of the
  developer database, so re-execution is safe to repeat.

`34-UAT.md`: create `.planning/phases/34-the-observation-projector-trigger/34-UAT.md` with a
SCHED-06 section recording the baseline capture date, the record count in the baseline, the
instruction that only `updatestatus` runs between now and the re-check, and a dated table to fill in
as nights pass. This is the artefact that makes the follow-up commit findable months later.
  </action>
  <verify>
    <automated>python manage.py migrate --check</automated>
    <fails_when>non-zero exit — the developer database is missing or behind on migrations, so the notebook would execute against the wrong schema</fails_when>
    <automated>test -f docs/notebooks/pre_executed/project_observation_calendar_demo.ipynb && test ! -e docs/notebooks/pre_executed/sync_lco_observation_calendar_demo.ipynb</automated>
    <fails_when>non-zero exit — either the new notebook is missing or the retired one still exists</fails_when>
    <automated>python -c "import json;nb=json.load(open('docs/notebooks/pre_executed/project_observation_calendar_demo.ipynb'));print(sum(1 for c in nb['cells'] if c['cell_type']=='code' and c.get('outputs')))"</automated>
    <fails_when>a printed count of 0 — the notebook was committed without executed output, so it is a script rather than a demonstration</fails_when>
    <automated>python -c "import os;print(os.path.getsize('docs/notebooks/pre_executed/project_observation_calendar_demo.ipynb') < 500*1024)"</automated>
    <fails_when>a printed `False` — the executed notebook exceeds the 500 KB limit `check-added-large-files` enforces, so the commit will be rejected</fails_when>
    <automated>grep -c 'sync_lco_observation_calendar' docs/notebooks.rst CLAUDE.md</automated>
    <fails_when>either printed per-file count is above 0 — a registry still points at the retired command or its deleted notebook</fails_when>
    <automated>grep -c 'project_observation_calendar_demo' docs/notebooks.rst CLAUDE.md</automated>
    <fails_when>either printed per-file count is 0 — the new notebook is not registered in the docs toctree or in CLAUDE.md's notebook map</fails_when>
    <automated>python -c "import json;nb=json.load(open('docs/notebooks/pre_executed/campaign_lifecycle_demo.ipynb'));s=''.join(''.join(c['source']) for c in nb['cells']);print(min(s.count('FTN'), s.count('SOAR')))"</automated>
    <fails_when>a printed count of 0 — the paired campaign-lifecycle notebook has no cell exercising the renamed telescope labels, so the `_extract_lco_site_code()` change shipped without its notebook</fails_when>
    <automated>python -c "import json;nb=json.load(open('docs/notebooks/pre_executed/campaign_lifecycle_demo.ipynb'));print(sum(1 for c in nb['cells'] if c['cell_type']=='code' and c.get('outputs')))"</automated>
    <fails_when>a printed count of 0 — the campaign-lifecycle notebook lost its committed output, meaning it was cleared rather than re-executed</fails_when>
    <automated>test -f .planning/phases/34-the-observation-projector-trigger/34-UAT.md && grep -c 'SCHED-06' .planning/phases/34-the-observation-projector-trigger/34-UAT.md</automated>
    <fails_when>non-zero exit, or a printed count of 0 — there is no recorded baseline date, so the live-nights follow-up has nothing to come back to</fails_when>
  </verify>
  <acceptance_criteria>
    - `docs/notebooks/pre_executed/project_observation_calendar_demo.ipynb` exists, has executed output in at least ten code cells, and is under 500 KB.
    - `docs/notebooks/pre_executed/sync_lco_observation_calendar_demo.ipynb` does not exist and is removed from git's index.
    - `docs/notebooks.rst` contains `project_observation_calendar_demo` and does not contain `sync_lco_observation_calendar`.
    - `CLAUDE.md`'s notebook map pairs `project_observation_calendar.py` with `project_observation_calendar_demo.ipynb` and no longer names the retired command.
    - The notebook contains a markdown cell instructing that only `updatestatus` is run between the baseline and the re-execution, and its text names the sweep as the thing NOT to run.
    - The notebook's takeover section prints an explicit equality result for the `RUN:`, `GEM:` and blank-url event sets before and after the sweep.
    - The notebook prints the first and second sweep summary lines together; the first shows non-zero `created`/`updated` and non-zero `site_lookups`, the second shows zero for all three.
    - The notebook's reconciliation cell prints the LCO/SOAR record count, the facility-URL-keyed event count and the arithmetic relating them, and enumerates every record with no such event together with its `unprojectable` reason (an empty table is acceptable and is printed as such).
    - The notebook prints a per-marker tally of the projected events covering the stage vocabulary.
    - No committed output cell contains an API key, an authorization header, a raw portal response body, or a submitter contact detail.
    - `docs/notebooks/pre_executed/campaign_lifecycle_demo.ipynb` has a new cell resolving the three renamed telescope labels to their LCO site codes and showing a site-level attribution match, is committed with executed output, and has no other cell edited.
    - `.planning/phases/34-the-observation-projector-trigger/34-UAT.md` exists with a SCHED-06 section recording the baseline date and record count.
    - A JSON baseline file sits beside the notebook for the re-execution to diff against.
  </acceptance_criteria>
  <done>The sweep has a paired, pre-executed notebook showing the receiver narrowing an event, the one-time takeover with the other three key namespaces provably untouched, and a committed SCHED-06 baseline with the operator instruction that closes it.</done>
</task>

<task type="auto">
  <name>Task 2: The runbook — projector and sweep replace the LCO sync section, Gemini gains its caveat</name>
  <files>docs/runbooks/telescope_runs_calendar.rst, docs/notebooks/pre_executed/sync_gemini_observation_calendar_demo.ipynb, .planning/todos/pending/2026-09-02-retarget-adapt-03-to-soar-and-caveat-phase-33-gemini-outcome.md, .planning/todos/pending/2026-06-23-extract-site-telescope-mapping-and-instrument-extraction-int.md</files>
  <read_first>
    - docs/runbooks/telescope_runs_calendar.rst lines 1-50 (the page's framing and section-heading style), lines 47-90 (the LCO/SOAR sync section being replaced, and the backfill section that references it), lines 235-258 (the Gemini section gaining the caveat), lines 912-950 (the command cheat-sheet table) and lines 950-1010 (the troubleshooting section's per-record skip-and-log subsection and its `[UNVERIFIED]` / `telescope_api_failed` paragraph)
    - docs/runbooks/telescope_runs_calendar.rst lines 784-792 (the reconciler's own "One-time title change" paragraph — the exact framing the projector takeover's note mirrors) and lines 803-860 (the "Attributed campaign run" pop-up section and its month-cell marker paragraph — the model for describing a decoration read from a link at request time rather than stored)
    - docs/notebooks/pre_executed/sync_gemini_observation_calendar_demo.ipynb (whole file — its framing markdown cells, where the caveat belongs; its code is not to be changed)
    - .planning/todos/pending/2026-09-02-retarget-adapt-03-to-soar-and-caveat-phase-33-gemini-outcome.md (whole file — the three items this phase resolves)
    - .planning/todos/pending/2026-06-23-extract-site-telescope-mapping-and-instrument-extraction-int.md (whole file — the todo whose target file this phase deleted)
    - .planning/phases/34-the-observation-projector-trigger/34-02-SUMMARY.md (the sweep's real flag set and summary-line wording, so the runbook documents what shipped rather than what was planned)
    - .planning/phases/34-the-observation-projector-trigger/34-03-SUMMARY.md (the legend entries and the modal series block as they actually shipped — their labels and wording are what the runbook must describe, not this plan's paraphrase)
    - .planning/phases/34-the-observation-projector-trigger/34-CONTEXT.md decisions D-02, D-04, D-13, D-16, D-18, D-19, D-21 and the "Folded Todos" section
  </read_first>
  <action>
Rewrite the runbook's LCO section around the projector and sweep, caveat the Gemini section and
notebook, and close the two todos this phase settles. Documentation only — no Python changes in this
task, and the Gemini command's behaviour is untouched.

`docs/runbooks/telescope_runs_calendar.rst`:

- Replace the section `How do I sync LCO/SOAR queue observations?` with
  `How do LCO/SOAR queue observations get onto the calendar?`. Lead with the answer that changed:
  they get there by themselves. Saving an `ObservationRecord` — whether FOMO submitted it, TOM's
  `updatestatus` refreshed it, or the backfill command created it — draws or updates that record's
  own calendar event, keyed on its portal URL. Describe the narrowing an operator will see: the
  submitted window while queued, the placed block once the scheduler places it, the observed block
  once it is observed, and a marked event on the original window for an expired, cancelled or
  failed record. List the compact title vocabulary as a small table of marker and meaning, and note
  that Phase 37 owns the final wording so the letters may change.
- In that same section, say where the vocabulary lives for someone already looking at the page: the
  calendar carries its own status legend row, shipped by plan 34-03, listing every marker beside its
  meaning — Queued, Scheduled, Observed, Window expired, Cancelled, Failed and Inconsistent record —
  so an operator decoding a month cell does not have to come back to this runbook for it. Add that
  the ring drawn around a month cell follows the same vocabulary: a queued entry and an
  inconsistent-record entry are ringed, a placed or observed entry is not. Take the legend's entry
  labels from what plan 34-03 actually shipped rather than from this sentence, so the page and the
  runbook cannot disagree (PROJ-03, D-02, D-13).
- Add a short subsection describing the pop-up's **Observation series** block, modelled on the
  existing "Why doesn't the calendar pop-up show an 'Attributed campaign run' block?" entry further
  down the page: for a record belonging to an observation group of two or more, clicking its calendar
  entry shows the group's name, which night of how many this entry is, and links back to the group
  and to the record itself. Frame it the way that existing section frames the campaign block — the
  numbering is read from the entry's companion row every time the page is drawn and is never written
  into the entry's title or description, so re-projecting an entry cannot erase it and adding or
  removing a night never rewrites a sibling's stored title. Note that nights are numbered by window
  start, so a cadence reads in observing order rather than in the order the entries were created,
  and that an entry with no group, or a group of one, simply shows no block (D-04, PROJ-05).
- Add a **One-time title change** paragraph for the projector takeover, adjacent to the projector
  narrative and mirroring the reconciler's own paragraph of that name: the first sweep after this
  phase updates, once, the titles and companion-row links of the legacy LCO/SOAR calendar entries an
  earlier sync had already created, because they still carry that older stopgap wording. Say plainly
  that this is expected rather than a fault, and say what survives it — entries keyed on a campaign
  run, on a Gemini submission, or on nothing at all are left exactly as they were, and every field
  the sweep rewrites is re-derived from the observation record, so a later wording change plus one
  more sweep simply re-derives them again (D-19).
- Add a following subsection `When would I run the sweep?` documenting
  `project_observation_calendar`: that it needs no arguments; that its job is the write paths a
  signal cannot see (`QuerySet.update()`, `bulk_create()`, and anything that edits rows outside
  Django), plus the one-time observed-telescope lookup for newly observed records; the optional
  `--proposal` (exact codes, comma-separated, no substring matching), `--facility` and `--dry-run`
  flags; and a console block showing a bare run and a `--dry-run`. Quote the summary line's real
  counter names and say in one sentence what each counter means, taking the wording from what
  actually shipped in plan 34-02.
- Update the backfill section's cross-reference: it currently tells the reader to run the LCO sync
  after a backfill. Replace that with the accurate statement — the backfill creates records, whose
  saves draw their own events, but because the backfill adds group membership after each record's
  save it is still worth running the sweep afterwards to pick up series links and observed-telescope
  labels.
- Cheat-sheet table: replace the `sync_lco_observation_calendar` row with a
  `project_observation_calendar` row — key flags `--proposal <A,B>`, `--facility <LCO|SOAR>`,
  `--dry-run` (all optional) — and a one-line description naming it the backstop sweep. Leave every
  other row alone.
- Troubleshooting: rewrite the per-record skip-and-log bullet for the sweep. The retired command's
  `[UNVERIFIED]` fallback label and `telescope_api_failed` counter are gone with it — a pending
  record simply carries its aperture class, which is not a failure and is never flagged as one.
  Describe instead what an operator will actually see: an `unprojectable` count for a record with no
  usable window, and a `site_lookup_failed` count for an observed record whose telescope lookup has
  not succeeded yet, which retries on the next run. In the "Observatory missing timezone" entry,
  replace the reference to the retired command with `reconcile_campaign_runs` and any other command
  that still computes a sun event — the projector computes none, so it cannot produce that error.
- Search the whole page for remaining occurrences of the retired command name and update each in
  place; leave no sentence describing a command a reader cannot run.

`docs/runbooks/telescope_runs_calendar.rst`, Gemini section (D-21): add a short caveat paragraph to
`How do I sync Gemini queue observations?` stating plainly that the Gemini facility class provides
no observation-status or observation-URL read-back, so a Gemini calendar event is an echo of what
was submitted and never narrows to a placed or observed block the way an LCO or SOAR event does;
that the automatic projector deliberately ignores Gemini records for that reason; and that this
command therefore stays as the way Gemini observations reach the calendar. State it as an operating
limitation the reader needs in order to interpret what they see, not as a complaint about upstream
code.

`docs/notebooks/pre_executed/sync_gemini_observation_calendar_demo.ipynb`: add the same caveat as a
markdown cell near the top, in the notebook's own voice. Change no code cell and do not re-execute
the notebook — its existing output stays valid because its behaviour is unchanged. Editing a
markdown cell by hand keeps the committed outputs intact; re-running it would churn the whole file
for nothing.

Todos:

- `.planning/todos/pending/2026-09-02-retarget-adapt-03-to-soar-and-caveat-phase-33-gemini-outcome.md`:
  mark it done and move it to `.planning/todos/completed/`, adding a closing note that all three of
  its items landed in Phase 34 — SOAR as the second robotic facility in the projector's scope, the
  projector ignoring Gemini records, and this caveat in the runbook and notebook.
- `.planning/todos/pending/2026-06-23-extract-site-telescope-mapping-and-instrument-extraction-int.md`:
  close it as overtaken and move it to `.planning/todos/completed/`, with a note that its target
  module was deleted in Phase 34 and the helpers it wanted extracted already live in
  `calendar_utils.py`.
  </action>
  <verify>
    <automated>grep -rc 'sync_lco_observation_calendar' docs/runbooks/telescope_runs_calendar.rst</automated>
    <fails_when>a printed count above 0 — the runbook still documents a command a reader cannot run</fails_when>
    <automated>grep -c 'project_observation_calendar' docs/runbooks/telescope_runs_calendar.rst</automated>
    <fails_when>exit 1 (no match) or a printed count below 3 — the sweep is missing from the narrative section, the console example, or the cheat-sheet table</fails_when>
    <automated>grep -ci 'legend' docs/runbooks/telescope_runs_calendar.rst</automated>
    <fails_when>exit 1 (no match) or a printed count of 0 — the runbook never tells an operator that the calendar page carries its own marker legend, so plan 34-03's on-page legend ships undocumented</fails_when>
    <automated>grep -c 'Observation series' docs/runbooks/telescope_runs_calendar.rst</automated>
    <fails_when>exit 1 (no match) or a printed count of 0 — the pop-up's series block is missing from the runbook, so a reader seeing "Night 2 of 5" on a modal has nothing explaining where it comes from</fails_when>
    <automated>python -c "src=open('docs/runbooks/telescope_runs_calendar.rst').read(); hits=[i for i in range(len(src)) if src.startswith('One-time title change', i)]; print(any('projector' in src[max(0, i - 900):i + 900] for i in hits))"</automated>
    <fails_when>a printed `False` — no one-time title change note sits beside the projector narrative, so an operator watching the legacy LCO/SOAR entries all change title in a single sweep has nothing telling them it is expected</fails_when>
    <automated>grep -ci 'never narrow\|no.*read-back\|submission-echo\|submission echo' docs/runbooks/telescope_runs_calendar.rst</automated>
    <fails_when>exit 1 (no match) or a printed count of 0 — the Gemini no-read-back caveat is absent from the runbook</fails_when>
    <automated>python -c "import json;nb=json.load(open('docs/notebooks/pre_executed/sync_gemini_observation_calendar_demo.ipynb'));t=''.join(''.join(c['source']) for c in nb['cells'] if c['cell_type']=='markdown').lower();print(('read-back' in t or 'read back' in t) and ('narrow' in t))"</automated>
    <fails_when>a printed `False` — the Gemini notebook has no markdown cell carrying the no-read-back caveat</fails_when>
    <automated>python -c "import json;a=json.load(open('docs/notebooks/pre_executed/sync_gemini_observation_calendar_demo.ipynb'));print(sum(1 for c in a['cells'] if c['cell_type']=='code' and c.get('outputs')))"</automated>
    <fails_when>a printed count of 0 — the Gemini notebook lost its committed output, meaning it was re-executed or cleared when only a markdown cell should have changed</fails_when>
    <automated>ls .planning/todos/pending/ | grep -c 'retarget-adapt-03-to-soar\|extract-site-telescope-mapping'</automated>
    <fails_when>exit 0 with a printed count of 1 or more — a todo this phase settles is still sitting in the pending directory (exit 1 with a printed 0 is the passing result)</fails_when>
    <automated>sphinx-build -M html ./docs ./_readthedocs -T -E -d ./docs/_build/doctrees -D exclude_patterns=notebooks/*,_build</automated>
    <fails_when>non-zero exit, or a line containing "WARNING: toctree contains reference to nonexisting document" — the toctree still points at the deleted notebook</fails_when>
    <automated>pre-commit run ruff --all-files && pre-commit run ruff-format --all-files</automated>
    <fails_when>non-zero exit, or a "Failed" line for either hook</fails_when>
  </verify>
  <acceptance_criteria>
    - `docs/runbooks/telescope_runs_calendar.rst` contains no occurrence of `sync_lco_observation_calendar` anywhere on the page.
    - `docs/runbooks/telescope_runs_calendar.rst` contains `project_observation_calendar` at least three times: in the narrative section, in a `code-block:: console` example, and in the cheat-sheet table.
    - The runbook contains a marker-to-meaning table covering the projector's title markers and a sentence noting the vocabulary is provisional.
    - The runbook names the calendar page's own status legend row and gives the meaning of every marker it carries, with labels matching what plan 34-03 shipped, and states which markers draw a ring around a month cell.
    - The runbook describes the pop-up's `Observation series` block — group name, which night of how many, and links back to the group and the record — and states that it is read from the companion row at request time and never written into the entry's title or description.
    - The runbook carries a `One-time title change` paragraph within the projector narrative, stating that the first sweep re-titles the legacy LCO/SOAR entries once and that campaign-keyed, Gemini-keyed and un-keyed entries are untouched.
    - The runbook's troubleshooting section names `unprojectable` and `site_lookup_failed` and no longer names `telescope_api_failed` or the `[UNVERIFIED]` label.
    - The runbook's "Observatory missing timezone" entry no longer cites the retired command.
    - The runbook's Gemini section states that the Gemini facility provides no status or URL read-back, that Gemini events never narrow, and that the projector ignores Gemini records.
    - `docs/notebooks/pre_executed/sync_gemini_observation_calendar_demo.ipynb` has a markdown cell carrying the same caveat, and its code cells still hold their previously committed output.
    - Neither `2026-09-02-retarget-adapt-03-to-soar-and-caveat-phase-33-gemini-outcome.md` nor `2026-06-23-extract-site-telescope-mapping-and-instrument-extraction-int.md` remains in `.planning/todos/pending/`; both are in `.planning/todos/completed/` with a closing note.
    - `sphinx-build` completes with no "reference to nonexisting document" warning.
  </acceptance_criteria>
  <done>A reader of the runbook learns that LCO/SOAR events keep themselves current, when to run the sweep and what its counters mean; a Gemini reader learns why their events never narrow; and the two todos this phase settles are closed with their reasons.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer database and LCO portal -> committed notebook output | a pre-executed notebook is committed with its output and published; anything printed becomes permanent public text |
| operator reading the runbook -> production commands | the runbook is what an operator follows on the real host; a wrong or stale instruction is an operational defect, not a typo |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34-19 | Information Disclosure | committed notebook output cells | high | mitigate | Task 1 forbids printing whole `parameters` dicts, facility headers or settings, and raw portal responses, and caps per-record tables to a readable sample; an acceptance criterion requires no API key, authorization header, raw response body or submitter contact detail in any committed output cell |
| T-34-20 | Repudiation | a runbook still naming a deleted command | medium | mitigate | Task 2's verify counts occurrences of the retired name on the whole page at 0 and requires the sweep to appear in three distinct places, so a partial rewrite cannot pass |
| T-34-21 | Tampering | the takeover sweep run against the developer database | medium | accept | The notebook runs the real one-time takeover against a git-ignored developer database, deliberately (D-19). The churn is one-time, every field is re-derivable from current record state by another sweep, and the notebook itself records the before state. Accepted |
| T-34-22 | Repudiation | a SCHED-06 claim with no recorded baseline | medium | mitigate | Task 1 writes a JSON baseline beside the notebook and a dated `34-UAT.md` section, and the notebook's closing table marks SCHED-06 pending rather than claimed — the requirement cannot be silently counted as met |
| T-34-23 | Information Disclosure | the Gemini notebook's existing committed output | low | mitigate | Task 2 changes a markdown cell only and forbids re-execution; a verify asserts the code cells still carry their previously committed output, so no new data is introduced into a published file |
| T-34-SC | Tampering | npm/pip/cargo installs | low | accept | This plan runs no package-manager install step; 34-RESEARCH.md `## Package Legitimacy Audit` records that the phase introduces no external package |
</threat_model>

<verification>
- `sphinx-build` over `./docs` completes with no "reference to nonexisting document" warning.
- `grep -rc 'sync_lco_observation_calendar' docs/runbooks/telescope_runs_calendar.rst docs/notebooks.rst CLAUDE.md` prints 0 for every file.
- `docs/notebooks/pre_executed/project_observation_calendar_demo.ipynb` exists with executed output, under 500 KB, and is registered in both `docs/notebooks.rst` and CLAUDE.md's notebook map.
- `.planning/phases/34-the-observation-projector-trigger/34-UAT.md` records the SCHED-06 baseline date and record count.
- `pre-commit run ruff --all-files` and `pre-commit run ruff-format --all-files` exit 0.
- Behavior: the committed notebook shows a record's event narrowing with no command run, the takeover leaving the other three key namespaces byte-identical, and a second sweep reporting everything unchanged.

## Human verification (end-of-phase)

<human-check>
Open the built calendar for a month carrying real `KEY2026B-004` nights and confirm, by eye:
the month cells read as `[Q] 2m0 3I/ATLAS`-style compact titles with the marker and telescope
visible before the truncation; expired and cancelled nights carry the terminal ring; a grouped
record's modal says which night of how many it is; and the legend explains every marker on the
page. Then, after several real nights during which only `python manage.py updatestatus` has run,
re-execute `project_observation_calendar_demo.ipynb` and confirm records moved `[Q]` to `[S]` to
`[O]` with no sweep in between — that re-execution is a follow-up commit, not a gate on Phase 35
planning.
</human-check>
</verification>

<success_criteria>
- ROADMAP criterion 5 holds in full: the retired command's runbook section, demo notebook and tests are migrated rather than duplicated, and `sync_gemini_observation_calendar` stays as submission-echo with its no-facility-read-back caveat documented.
- ROADMAP criterion 4's evidence is standing: a committed baseline plus a recorded operator instruction, so the live-narrowing claim can be closed by re-execution rather than re-derivation.
- The CLAUDE.md paired-docs rule is satisfied for this phase: the sweep has a pre-executed notebook, it is in the notebook map, the retired notebook is gone, and the affected runbook page is current.
- TRIG-03's paired-notebook clause and ANNOT-03's documentation clause each have a committed artefact.
</success_criteria>

<output>
Create `.planning/phases/34-the-observation-projector-trigger/34-04-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

Files created by THIS plan (they do not exist in the codebase before it):

- `docs/notebooks/pre_executed/project_observation_calendar_demo.ipynb` (new, committed with output)
- the JSON SCHED-06 baseline file committed beside it
- `.planning/phases/34-the-observation-projector-trigger/34-UAT.md` (new)
- `docs/runbooks/telescope_runs_calendar.rst`: the sections "How do LCO/SOAR queue observations get
  onto the calendar?" and "When would I run the sweep?" (replacing "How do I sync LCO/SOAR queue
  observations?"), the marker-legend and status-ring paragraphs, the "Observation series" pop-up
  subsection, the projector's "One-time title change" paragraph, the
  `project_observation_calendar` cheat-sheet row, and the Gemini caveat paragraph
- `CLAUDE.md`: the `project_observation_calendar_demo.ipynb` notebook-map entry

Removed by this plan: `docs/notebooks/pre_executed/sync_lco_observation_calendar_demo.ipynb`, its
`docs/notebooks.rst` toctree entry, its CLAUDE.md notebook-map entry, and the two pending todos
(moved to `.planning/todos/completed/`).

## Flagged Assumptions (spec-less probe fallback, unresolved rows)

| Requirement | Category | Probe | Assumption made | Risk if wrong |
|-------------|----------|-------|-----------------|---------------|
| SCHED-06 | unclassified | "unclassified — review manually" | SCHED-06's behaviour is taken to be fully covered by D-20: a committed baseline now, only `updatestatus` run over real nights, and a re-executed notebook showing the transitions. Phase verification passes on the baseline plus the mechanism tests in plans 34-01 and 34-02; the post-nights re-execution is a follow-up commit. | If no real `KEY2026B-004` record happens to narrow during the watched nights (a quiet stretch, weather, an expired proposal), the re-execution would show nothing and SCHED-06 would need another window rather than a code change. `34-UAT.md`'s dated table is what makes that visible instead of ambiguous. |

## No-silent-drop accounting (phase-wide)

The spec-less edge probe returned 24 applicable items across this phase's ten requirements. All 24
are accounted for:

- **19 authored into `must_haves`** — 17 as plain `truths` strings (verification: explicit) and 2 as
  structured `verification: backstop` markers. By plan: 34-01 carries the PROJ-01 adjacency /
  empty / ordering rows, the PROJ-02 adjacency / empty / ordering rows and its backstop concurrency
  row, and all five TRIG-01 and all five TRIG-02 rows; 34-02 carries the TRIG-03 idempotency row and
  its backstop concurrency row.
- **5 surfaced as flagged assumptions**, never dropped — PROJ-03, PROJ-05 and PROJ-06 in plan 34-01,
  ANNOT-03 in plan 34-02, and SCHED-06 above. All five were returned `unclassified` by the probe and
  stay `unresolved` by rule.

19 + 5 = 24.

Prohibitions: the recall pass surfaced fifteen kept must-NOT statements across the four plans, each
authored descriptor-less into that plan's `must_haves.prohibitions` block (four in 34-01, four in
34-02, three in 34-03, four in 34-04). Canon security items (injection, access control, transport
security) were recalled and dropped as canon-referral rather than minted: Django's template
auto-escaping and the management-command-only surface cover them, and they are recorded in each
plan's `<threat_model>` instead. None were auto-dismissed.