---
phase: 27-the-canonical-run-record
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - solsys_code/calendar_utils.py
  - solsys_code/management/commands/sync_lco_observation_calendar.py
  - solsys_code/management/commands/backfill_lco_observation_records.py
  - solsys_code/tests/test_sync_lco_observation_calendar.py
  - solsys_code/tests/test_calendar_utils.py
autonomous: true
requirements: [CANON-02]

must_haves:
  truths:
    - "calendar_utils.py's five cross-module-consumed helpers are public (no leading underscore) and every call site still works"
    - "A single shared helper derives a telescope_class value from primitives, callable from both a data migration and import_campaign_csv (D-20)"
    - "The stored telescope_class vocabulary is lowercase 2m0/1m0/0m4 plus SPACE, and a test proves the three aperture values are a subset of calendar_utils' aperture-class set with 4m0 named as the known exclusion (D-12/D-21)"
    - "calendar_utils-owned unit tests live in test_calendar_utils.py, not in test_sync_lco_observation_calendar.py"
  artifacts:
    - path: "solsys_code/calendar_utils.py"
      provides: "derive_telescope_class(), NO_OBSCODE_SPACE_OBSERVATORIES, five de-underscored public helpers"
      contains: "def derive_telescope_class"
    - path: "solsys_code/tests/test_calendar_utils.py"
      provides: "derive_telescope_class unit tests incl. D-12 subset assertion; the six relocated calendar_utils-owned tests"
      contains: "derive_telescope_class"
  key_links:
    - from: "solsys_code/management/commands/sync_lco_observation_calendar.py"
      to: "solsys_code/calendar_utils.py"
      via: "import of the five de-underscored helper names"
      pattern: "from solsys_code.calendar_utils import"
    - from: "solsys_code/calendar_utils.py"
      to: "solsys_code/campaign_utils.py"
      via: "function-local import of HORIZONS_OBSERVER_TO_OBSCODE inside derive_telescope_class"
      pattern: "HORIZONS_OBSERVER_TO_OBSCODE"
---

<objective>
Turn `calendar_utils.py` into the honest shared module it already is, and give Phase 27 the one
`telescope_class` derivation helper that both the backfill migration (Plan 04) and
`import_campaign_csv` (Plan 06) will call.

Purpose: D-20 requires exactly one derivation, taking primitives, homed in `calendar_utils.py`
"where the class vocabulary already lives". Without it, the new field rots the moment the
backfill lands. The folded private-helper rename (STATE.md todo
`2026-07-02-rename-calendar-utils-py-private-helpers-to-reflect-shared-m.md`) is done in the same
plan because 26-DECISION.md's "Recommended naming posture for calendar_utils.py" section asks
Phase 27 to do it while it is editing this module anyway, and because D-12/D-20 touch
`_aperture_class_from_telescope_code` regardless.

Output: five public helpers, one new `derive_telescope_class()` with its unit tests, and the
`calendar_utils`-owned tests relocated out of the LCO sync command's test module.
</objective>

<execution_context>
@/home/tlister/git/fomo_devel/.claude/gsd-core/workflows/execute-plan.md
@/home/tlister/git/fomo_devel/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-the-canonical-run-record/27-CONTEXT.md
@.planning/phases/27-the-canonical-run-record/27-RESEARCH.md
@.planning/phases/27-the-canonical-run-record/27-PATTERNS.md
@.planning/phases/27-the-canonical-run-record/27-VALIDATION.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
Every symbol below is **created by Phase 27** and does not exist in the codebase before this
phase. Source-grounding / drift checks must not flag these as unresolved references.

**Renamed model and admin class (Plan 03):** `CalendarEventMeta` (from
`CalendarEventTelescopeLabel`), `CalendarEventMetaAdmin` (from
`CalendarEventTelescopeLabelAdmin`).

**New model fields and properties (Plans 03, 04):** `CalendarEventMeta.run`;
`CampaignRun.source`; `CampaignRun.telescope_class`; `CampaignRun.is_publicly_visible`.

**New `TextChoices` enum classes (Plan 04):** `CampaignRun.Source` (members `WEB`,
`CLASSICAL_FILE`, `LCO_QUEUE`, `GEMINI_QUEUE`, `CSV_IMPORT`, `LEGACY`);
`CampaignRun.TelescopeClass` (members `TWO_M0`, `ONE_M0`, `ZERO_M4`, `SPACE`).

**New link model (Plan 04):** `CampaignRunObservation`, with fields `run`,
`observation_record`, `confirmed_by`, `confirmed_at`, and the named DB constraint
`unique_campaign_run_observation_record`.

**New admin surfaces (Plan 05):** `CalendarEventMetaInline`, `CampaignRunObservationInline`,
`CampaignRunAdmin.save_formset`.

**New `calendar_utils` symbols (this plan):** `derive_telescope_class`,
`NO_OBSCODE_SPACE_OBSERVATORIES`, and the five de-underscored helpers
`aperture_class_from_telescope_code`, `derive_telescope`, `resolve_placement_block`,
`extract_instrument`, `coarse_telescope_label`.

**New management command (Plan 02):** `solsys_code/management/commands/repair_stale_campaign_run_sites.py`.

**New migration files:** `solsys_code/migrations/0008_rename_calendareventtelescopelabel_calendareventmeta.py`,
`solsys_code/migrations/0009_calendareventmeta_run.py`,
`solsys_code/migrations/0010_campaignrun_source_telescope_class_campaignrunobservation.py`,
`solsys_code/migrations/0011_backfill_campaignrun_telescope_class.py`,
`solsys_code/solsys_code_observatory/migrations/0003_backfill_observatory_timezone.py`.

**New template override (Plan 05):** `src/templates/tom_calendar/partials/event_form.html`.

**New test modules and fixtures:** `solsys_code/tests/test_canonical_record_migration.py`,
`solsys_code/tests/test_campaign_run_observation.py`,
`solsys_code/tests/test_repair_stale_campaign_run_sites.py`, and the mock fixture constants
`_MPC_OBS_DATA_250` (HST) and `_MPC_OBS_DATA_C52` (Swift).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Drop the leading underscore on calendar_utils.py's five shared helpers</name>
  <files>solsys_code/calendar_utils.py, solsys_code/management/commands/sync_lco_observation_calendar.py, solsys_code/management/commands/backfill_lco_observation_records.py, solsys_code/tests/test_sync_lco_observation_calendar.py</files>
  <read_first>
    - `solsys_code/calendar_utils.py` — current state of all five `def` lines and their in-module call sites
    - `.planning/phases/26-canonical-record-spike/26-DECISION.md` section `### Recommended naming posture for calendar_utils.py (folded todo)` — the recommendation this task executes, and the five names it lists with their line numbers
    - `.planning/todos/pending/2026-07-02-rename-calendar-utils-py-private-helpers-to-reflect-shared-m.md` — the todo being closed
    - `solsys_code/management/commands/sync_lco_observation_calendar.py` lines 10-20 and 150-180 — the four-name import block and the four call sites
  </read_first>
  <action>
    Rename exactly these five `calendar_utils.py` functions by removing the leading underscore,
    changing nothing else about their signatures, docstrings, bodies or ordering:
    `_aperture_class_from_telescope_code` -> `aperture_class_from_telescope_code`;
    `_derive_telescope` -> `derive_telescope`; `_resolve_placement_block` ->
    `resolve_placement_block`; `_extract_instrument` -> `extract_instrument`;
    `_coarse_telescope_label` -> `coarse_telescope_label`.

    Update every consumer in the same commit, because a missed one is a loud `ImportError`:
    (a) the in-module call site of `aperture_class_from_telescope_code` inside `derive_telescope`;
    (b) `sync_lco_observation_calendar.py`'s multi-name import block and its four call sites
    (`extract_instrument`, `coarse_telescope_label`, `resolve_placement_block`,
    `derive_telescope`);
    (c) `InstrumentExtractionError`'s docstring, which names `_extract_instrument`;
    (d) `sync_lco_observation_calendar.py`'s docstring/comment references to
    `_resolve_placement_block` and `_extract_instrument`;
    (e) `backfill_lco_observation_records.py`'s docstring reference to
    `solsys_code.calendar_utils._extract_instrument`;
    (f) `test_sync_lco_observation_calendar.py`'s import block and every call site of the three
    helpers it imports (`aperture_class_from_telescope_code`, `derive_telescope`,
    `resolve_placement_block`), plus the docstrings naming them.

    Do NOT rename `insert_or_create_calendar_event`, `SITE_TELESCOPE_MAP`,
    `InstrumentExtractionError`, `_API_TIMEOUT_SECONDS`, `_SCIENCE_CONFIGURATION_TYPES`,
    `_MUSCAT_CHANNEL_SUFFIXES` or `_update_or_unchanged` — the todo names five functions and only
    those five. Module-private constants stay private.

    **D-24 constraint (negative):** do NOT extract `SITE_TELESCOPE_MAP` or the
    instrument-extraction chain into a new module. That module split was deliberately dropped
    back out of Phase 27 by D-24; the todo
    `2026-06-23-extract-site-telescope-mapping-and-instrument-extraction-int.md` stays open.
    Everything in this plan stays inside `calendar_utils.py`.

    Leave `patch('solsys_code.calendar_utils.make_request', ...)` targets in the tests unchanged —
    `make_request` is an imported third-party name, not one of the five.
  </action>
  <acceptance_criteria>
    - `grep -rn "_aperture_class_from_telescope_code\|_derive_telescope\b\|_resolve_placement_block\|_extract_instrument\|_coarse_telescope_label" --include=*.py solsys_code/ | grep -v __pycache__` returns zero lines (note: `_derive_telescope_class` is NOT one of these names and does not exist; the `\b` anchor keeps `derive_telescope` distinct)
    - `grep -c "def aperture_class_from_telescope_code\|def derive_telescope(\|def resolve_placement_block\|def extract_instrument\|def coarse_telescope_label" solsys_code/calendar_utils.py` returns 5
    - `./manage.py test solsys_code.tests.test_sync_lco_observation_calendar solsys_code.tests.test_calendar_utils solsys_code.tests.test_load_telescope_runs solsys_code.tests.test_sync_gemini_observation_calendar solsys_code.tests.test_backfill_lco_observation_records` exits 0
    - `ruff check . && ruff format --check .` exits 0
  </acceptance_criteria>
  <verify>
    <automated>./manage.py test solsys_code.tests.test_sync_lco_observation_calendar solsys_code.tests.test_calendar_utils solsys_code.tests.test_load_telescope_runs solsys_code.tests.test_sync_gemini_observation_calendar solsys_code.tests.test_backfill_lco_observation_records</automated>
  </verify>
  <done>All five helpers are public, every call site imports the new names, and the five targeted test modules pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add derive_telescope_class() and its unit tests, including D-12's subset assertion</name>
  <files>solsys_code/calendar_utils.py, solsys_code/tests/test_calendar_utils.py</files>
  <read_first>
    - `solsys_code/calendar_utils.py` lines 37-105 — `SITE_TELESCOPE_MAP` and
      `aperture_class_from_telescope_code` (post-Task-1 name), whose "primitives in, never raise,
      return None/sentinel on no-match" shape this helper copies, and whose lowercase
      `{'0m4', '1m0', '2m0', '4m0'}` set is the vocabulary D-12 cross-checks against
    - `solsys_code/campaign_utils.py` lines 33-47 — `HORIZONS_OBSERVER_TO_OBSCODE` and its
      documented both-sides-verified extension rule, which the SPACE branch reuses
    - `.planning/phases/27-the-canonical-run-record/27-CONTEXT.md` D-11, D-12, D-13, D-20, D-21 and
      the `<specifics>` bullet defining what `SPACE` means
    - `.planning/phases/27-the-canonical-run-record/27-PATTERNS.md` section
      `### solsys_code/calendar_utils.py — derive_telescope_class() (D-20)`
    - `solsys_code/tests/test_calendar_utils.py` — the existing module's `TestCase` shape and
      import style
  </read_first>
  <action>
    Add `derive_telescope_class(site_raw: str | None, telescope_instrument: str | None) -> str` to
    `calendar_utils.py`, placed immediately after `aperture_class_from_telescope_code` (D-20's
    stated placement). Primitives only — never a model instance — so a `RunPython` step can import
    it without coupling to a model that keeps changing through Phases 28-29. Never raises; returns
    `''` for "no class signal", which is the correct value for a genuine site-resolution failure
    (D-13: `site_needs_review` carries "unresolved", not `telescope_class`).

    Return values are exactly `'2m0'`, `'1m0'`, `'0m4'`, `'SPACE'` or `''`. **Per D-21 the three
    aperture values are stored lowercase**, matching `aperture_class_from_telescope_code`'s
    existing set, so D-12's subset assertion compares directly with no case-folding. `'SPACE'`
    stays uppercase (D-21: "`SPACE` is unaffected") because it has no `calendar_utils` aperture
    counterpart to match; note that in a comment so a future reader does not "normalise" it.

    Derivation order, all rules derived rather than pk-enumerated (D-13):

    1. **SPACE, tier a (derived from `site_raw`):** if `site_raw`, stripped, matches JPL Horizons
       observer notation (prefix `500@`) and is NOT a key of
       `HORIZONS_OBSERVER_TO_OBSCODE`, return `'SPACE'`. This is literally D-11's definition — a
       space observatory with a Horizons code but no MPC obscode assigned. Import
       `HORIZONS_OBSERVER_TO_OBSCODE` with a **function-local** `from solsys_code.campaign_utils
       import HORIZONS_OBSERVER_TO_OBSCODE`, and comment why: `campaign_utils` imports
       `solsys_code.models` at module scope, and a module-scope import here would drag the live
       `CampaignRun` model into `calendar_utils`' import graph, which a data migration imports.
    2. **SPACE, tier b (derived from `telescope_instrument`):** needed because the real JUICE row
       carries a blank `site_raw`, so tier a cannot see it. Add a module-level constant
       `NO_OBSCODE_SPACE_OBSERVATORIES: frozenset[str] = frozenset({'juice'})` immediately above
       the function, documented with the same extension rule
       `HORIZONS_OBSERVER_TO_OBSCODE` uses: only add a name after confirming on both sides that
       the observatory has a Horizons code and no MPC obscode. If any whitespace/punctuation-split
       token of the lowercased `telescope_instrument` is in that set, return `'SPACE'`.
       Comment that Swift (`C52`), HST (`250`) and JWST (`274`) are deliberately NOT members —
       widening `SPACE` back to "any space mission" is the premise D-11 falsified.
    3. **Aperture class (derived from `telescope_instrument`):** search the lowercased text for
       either an already-canonical token (`0m4`, `1m0`, `2m0`, `4m0`) or a metre-aperture phrase
       (`0.4m`, `1m`, `1.0m`, `2m`, `2.0m`, `4m`, `4.0m`, optional whitespace between the number
       and the `m`), and canonicalise to `'0m4'` / `'1m0'` / `'2m0'` / `'4m0'`. Return the token
       only if it is in `{'2m0', '1m0', '0m4'}`; a `4m0` match returns `''` with an inline comment
       naming D-12's deliberate exclusion of `4m0` (SOAR) from the model vocabulary. Anchor the
       number on a word boundary so `MuSCAT4`, `WFC3/UVIS` and `FORS2` cannot match, and keep the
       pattern linear — no nested quantifiers — because this text comes from a CSV cell (see this
       plan's threat model, T-27-04). Deliberately do NOT match centimetre forms; a class-wide LCO
       allocation names the network class (`LCO 1m`, `LCO 2m`), and matching `43cm` would
       mis-class a real resolved-site row.
    4. Otherwise return `''`.

    Both call sites gate on "the run has no resolved site" — the migration filters
    `site__isnull=True` (Plan 04) and the importer only calls this when `resolve_site()` returned
    `None` (Plan 06). Record that contract in the docstring so neither caller drifts: a
    site-resolved run must never carry a `telescope_class`.

    Then add tests to `solsys_code/tests/test_calendar_utils.py` in a new `TestCase` class
    (e.g. `TestDeriveTelescopeClass`) covering, at minimum, these input/output pairs — each mirrors
    a real dev-DB row shape named in D-16's table:
    `('', 'LCO 1m')` -> `'1m0'`; `('', 'LCO 2m')` -> `'2m0'`; `('', 'LCO 0.4m')` -> `'0m4'`;
    `('', 'JUICE')` -> `'SPACE'`; `('500@-28', 'JUICE')` -> `'SPACE'`;
    `('500@-170', 'JWST')` -> `''` (alias exists, so JWST has an MPC obscode);
    `('250', 'HST STIS/COS')` -> `''`; `('', 'Swift/UVOT')` -> `''`;
    `('X05', 'FOO / BAR')` -> `''`; `('', 'SOAR 4m')` -> `''` (D-12 excludes `4m0`);
    `('', 'FTS/MuSCAT4')` -> `''` (no false positive on a trailing digit);
    `(None, None)` -> `''` (never raises).

    Add the D-12 cross-check as its own test with an explanatory docstring: assert that the three
    aperture members of `CampaignRun.TelescopeClass` are a **subset** of
    `aperture_class_from_telescope_code`'s lowercase aperture set, and assert explicitly that
    `'4m0'` is in that set but NOT among the model's values, so a future reader cannot "fix" the
    discrepancy by adding `4m0` to the model. Also assert `'SPACE'` is deliberately absent from the
    aperture set because it is not an aperture class at all. `CampaignRun.TelescopeClass` does not
    exist until Plan 04, so write this test now against the literal expected value set
    `{'2m0', '1m0', '0m4'}` and leave a `# Plan 27-04 wires this to CampaignRun.TelescopeClass`
    marker; Plan 04's task 3 replaces the literal with the enum.
  </action>
  <acceptance_criteria>
    - `grep -c "def derive_telescope_class" solsys_code/calendar_utils.py` returns 1
    - `grep -c "NO_OBSCODE_SPACE_OBSERVATORIES" solsys_code/calendar_utils.py` returns at least 2 (definition plus use)
    - `grep -n "HORIZONS_OBSERVER_TO_OBSCODE" solsys_code/calendar_utils.py` shows the import inside the function body, not at module top level (i.e. the matched line is indented)
    - `./manage.py test solsys_code.tests.test_calendar_utils` exits 0 and reports at least 13 additional tests over the pre-task count
    - `./manage.py test solsys_code.tests.test_calendar_utils -v 2 2>&1 | grep -c "4m0"` is at least 1 (the D-12 exclusion test is present and named)
    - `ruff check . && ruff format --check .` exits 0
  </acceptance_criteria>
  <verify>
    <automated>./manage.py test solsys_code.tests.test_calendar_utils</automated>
  </verify>
  <done>derive_telescope_class exists in calendar_utils.py, returns only 2m0/1m0/0m4/SPACE/'' from primitives, never raises, and its tests (including the D-12 subset assertion naming 4m0) pass.</done>
</task>

<task type="auto">
  <name>Task 3: Move the calendar_utils-owned tests into test_calendar_utils.py</name>
  <files>solsys_code/tests/test_sync_lco_observation_calendar.py, solsys_code/tests/test_calendar_utils.py</files>
  <read_first>
    - `solsys_code/tests/test_sync_lco_observation_calendar.py` lines 1-30 (imports and the
      module-level `_observations_block_response` helper it defines) and lines 786-880 (the six
      tests being moved)
    - `solsys_code/tests/test_calendar_utils.py` — destination module's imports and class layout
    - `.planning/todos/pending/2026-07-02-rename-calendar-utils-py-private-helpers-to-reflect-shared-m.md`
      — the todo's second half, which this task closes
  </read_first>
  <action>
    Move exactly these six tests out of `TestSyncLcoObservationCalendar` in
    `test_sync_lco_observation_calendar.py` and into `test_calendar_utils.py`, unchanged in body
    except for whatever import adjustment the new module needs. They are the tests that exercise
    `calendar_utils` helpers directly with mocks and never invoke the management command:
    `test_telescope_01_verified_dict_covers_all_sites`,
    `test_telescope_01_aperture_class_from_telescope_code`,
    `test_telescope_01_coj_ogg_full_aperture_class_coverage`,
    `test_telescope_02_placed_record_resolves_via_api`,
    `test_sync_08_single_attempt_no_retry`,
    `test_sync_09_no_credential_or_body_leak_in_logs`.

    Group them in `test_calendar_utils.py` under one or two new `TestCase` classes named for what
    they cover (e.g. `TestTelescopeLabelResolutionHelpers` and
    `TestResolvePlacementBlockFailureModes`). Keep every existing test method name byte-identical so
    the requirement-ID prefixes (`telescope_01`, `telescope_02`, `sync_08`, `sync_09`) stay
    greppable and traceable.

    `test_telescope_02_placed_record_resolves_via_api`, `test_sync_08_single_attempt_no_retry` and
    `test_sync_09_no_credential_or_body_leak_in_logs` use the module-level
    `_observations_block_response(...)` builder defined in `test_sync_lco_observation_calendar.py`.
    That builder is still needed by many command-level tests that stay behind, so **copy** it (do
    not move it) into `test_calendar_utils.py`, or import it from the sync module — pick one and
    state which in the summary. If you copy it, add a one-line comment in both copies noting they
    are intentional duplicates of a test-fixture builder and must be kept in step.

    Carry over only the imports the moved tests actually need (`re`, `requests`, `forms`,
    `MagicMock`, `patch`, `ImproperCredentialsException`, `SITE_TELESCOPE_MAP`,
    `aperture_class_from_telescope_code`, `derive_telescope`, `resolve_placement_block`), and
    remove from `test_sync_lco_observation_calendar.py` any import that becomes unused after the
    move — `ruff check` will flag leftovers as F401.

    Do NOT move any test that calls `call_command('sync_lco_observation_calendar', ...)` — those
    are command-behaviour tests and belong where they are, even when their name shares a
    `telescope_0X`/`sync_0X` prefix with a moved one.
  </action>
  <acceptance_criteria>
    - `grep -c "def test_telescope_01_verified_dict_covers_all_sites\|def test_telescope_01_aperture_class_from_telescope_code\|def test_telescope_01_coj_ogg_full_aperture_class_coverage\|def test_telescope_02_placed_record_resolves_via_api\|def test_sync_08_single_attempt_no_retry\|def test_sync_09_no_credential_or_body_leak_in_logs" solsys_code/tests/test_calendar_utils.py` returns 6
    - The same grep against `solsys_code/tests/test_sync_lco_observation_calendar.py` returns 0
    - `./manage.py test solsys_code.tests.test_calendar_utils solsys_code.tests.test_sync_lco_observation_calendar` exits 0, and the combined test count is not lower than the pre-task combined count for the two modules
    - `ruff check . && ruff format --check .` exits 0
  </acceptance_criteria>
  <verify>
    <automated>./manage.py test solsys_code.tests.test_calendar_utils solsys_code.tests.test_sync_lco_observation_calendar</automated>
  </verify>
  <done>The six calendar_utils-owned tests run from test_calendar_utils.py, test_sync_lco_observation_calendar.py no longer defines them, and the combined test count did not drop.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSV cell -> `derive_telescope_class(telescope_instrument=...)` | Free text from an operator-supplied spreadsheet reaches a regex in this module (Plan 06 call site) |
| Data migration -> `calendar_utils` import graph | A `RunPython` step imports this module at migrate time (Plan 04 call site) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-04 | Denial of Service | `derive_telescope_class`'s aperture regex over CSV free text | mitigate | Keep the aperture pattern linear — a bounded numeric group plus optional whitespace plus `m`, no nested quantifiers and no backtracking-prone alternation over the whole string. Text length is already bounded by `CampaignRun.telescope_instrument`'s `max_length=255` |
| T-27-05 | Tampering | A hostile `telescope_instrument` cell steering `telescope_class` to a value outside the vocabulary | mitigate | The helper returns only literals from a closed set (`2m0`/`1m0`/`0m4`/`SPACE`/`''`); it never echoes input. Plan 04's field is `TextChoices`-validated at the model layer, so an out-of-vocabulary value cannot persist through a form or the admin |
| T-27-06 | Tampering | Widening `SPACE` to "any space mission", re-introducing the premise D-11 falsified | accept (documented) | `NO_OBSCODE_SPACE_OBSERVATORIES` carries the both-sides-verification extension rule in a comment, and the tests assert HST/JWST/Swift return `''`. Accepted as a documentation-and-test control, not a code control |
| T-27-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable — this plan installs no package. RESEARCH.md's Package Legitimacy Audit records "N/A — no packages installed by this phase" |
</threat_model>

<verification>
Quick run (excludes the two SPICE-heavy modules on purpose — importing
`solsys_code.ephem_utils` downloads ~1.6 GB of SPICE kernels):

`./manage.py test solsys_code.tests.test_admin solsys_code.tests.test_campaign_models solsys_code.tests.test_campaign_approval solsys_code.tests.test_calendar_template solsys_code.tests.test_calendar_utils solsys_code.tests.test_sync_lco_observation_calendar solsys_code.tests.test_load_telescope_runs solsys_code.tests.test_import_campaign_csv`

Then, once at the end of the plan: `./manage.py test solsys_code` (full suite, pays the SPICE
cost once) and `ruff check . && ruff format --check .`.
</verification>

<success_criteria>
- Zero occurrences of the five old underscore-prefixed helper names anywhere under `solsys_code/`
- `derive_telescope_class` exists in `calendar_utils.py`, takes only primitives, never raises, and
  returns only `2m0`/`1m0`/`0m4`/`SPACE`/`''`
- A test names `4m0` explicitly as the known-excluded aperture value (D-12)
- The six `calendar_utils`-owned tests live in `test_calendar_utils.py`
- Quick run green; `ruff check .` and `ruff format --check .` clean
- No new module was created for `SITE_TELESCOPE_MAP` (D-24)
</success_criteria>

<output>
Create `.planning/phases/27-the-canonical-run-record/27-01-SUMMARY.md` when done.
</output>
