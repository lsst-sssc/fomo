---
phase: 31-foundation-spikes-run-identity-unattended-invocation
plan: 05
type: execute
wave: 5
depends_on: ["31-04"]
files_modified:
  - docs/design/run_identity_and_unattended_invocation_spike.rst
  - docs/design/design.rst
autonomous: true
requirements: [SCHEMA-01, SCHEMA-02, SCHEMA-03, SCHED-07]

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A page under docs/design/ carries both this phase's verdicts - the run-identity scheme and the scheduling mechanism - so the decisions are readable outside .planning/ (roadmap Success Criterion 5)"
    - "The new page is wired into the Sphinx toctree in docs/design/design.rst, so it is reachable from the published documentation rather than orphaned"
    - "The Sphinx documentation build succeeds with the new page included"
    - "The existing test suite is unchanged, because no file under solsys_code/ or src/ was modified by any plan in this phase (roadmap Success Criterion 5)"
    - "No file under the git-excluded tmp/ directory was committed, and the disposable database copy is removed"
    - "Neither committed artifact - the decision document nor the design page - contains a credential value, a long hex secret or an email address"
  artifacts:
    - path: "docs/design/run_identity_and_unattended_invocation_spike.rst"
      provides: "Durable, reader-facing summary of both Phase 31 verdicts, with a pointer back to 31-DECISION.md for the full evidence"
      contains: "list-table"
    - path: "docs/design/design.rst"
      provides: "Sphinx toctree entry making the new spike page reachable"
      contains: "run_identity_and_unattended_invocation_spike"
  key_links:
    - from: "docs/design/design.rst"
      to: "docs/design/run_identity_and_unattended_invocation_spike.rst"
      via: "toctree entry under the Design Notes section"
      pattern: "run_identity_and_unattended_invocation_spike"
    - from: "docs/design/run_identity_and_unattended_invocation_spike.rst"
      to: ".planning/phases/31-foundation-spikes-run-identity-unattended-invocation/31-DECISION.md"
      via: "Future scope section pointing back to the full-evidence companion, with the archival-path caveat"
      pattern: "31-DECISION"
  prohibitions:
    - statement: "MUST NOT copy a live credential value out of the machine-local settings override, the process environment or a crontab line into any committed artifact; committed artifacts name credential variables, never their values"
      status: unverified
      verification: judgment
---

<objective>
Carry both of this phase's verdicts out of `.planning/` and into the published documentation, then
prove the phase left the source tree and the test suite untouched.

Purpose: roadmap Success Criterion 5 has two halves. The first is that the decisions are readable
outside `.planning/` - both prior investigation-only phases produced a `docs/design/` page beside
their decision document, and this phase follows that precedent with one page carrying two verdicts.
The second half is that the test suite is unchanged because no source behaviour changed, which for
an investigation phase is a claim to prove rather than assert.

Output: `docs/design/run_identity_and_unattended_invocation_spike.rst`, a toctree entry for it, a
green Sphinx build, a green targeted regression run, and a clean source tree.

Canon-referral note: generic secret-scanning and credential-storage canon is owned by
`/gsd-secure-phase` and is not re-minted as a bespoke prohibition here. The one prohibition this
plan carries is specific to this phase's verbatim-quoting evidence discipline meeting a real,
git-excluded credential file on the operator's own machine.
</objective>

<execution_context>
@/home/tlister/git/fomo_devel/.claude/gsd-core/workflows/execute-plan.md
@/home/tlister/git/fomo_devel/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-foundation-spikes-run-identity-unattended-invocation/31-CONTEXT.md
@.planning/phases/31-foundation-spikes-run-identity-unattended-invocation/31-PATTERNS.md
@.planning/phases/31-foundation-spikes-run-identity-unattended-invocation/31-04-SUMMARY.md
</context>

## Artifacts this phase produces

See the table in `31-01-PLAN.md` under the same heading. This plan creates
`docs/design/run_identity_and_unattended_invocation_spike.rst` (new, committed), edits
`docs/design/design.rst` (existing, committed - one toctree line added), and deletes
`tmp/31-spike-db-copy.sqlite3`.

Paired-docs note: CLAUDE.md's paired-docs rule triggers on a change to a module's behaviour, and
this phase changes none - no notebook under `docs/notebooks/pre_executed/` and no page under
`docs/runbooks/` documents behaviour this phase altered. CONTEXT.md's Phase Boundary records the
same exemption. The `docs/design/` page below is a deliverable of this phase in its own right, not
a paired-docs obligation.

<tasks>

<task type="auto">
  <name>Task 1: Write the durable docs/design page carrying both verdicts, and wire it into the toctree</name>
  <files>docs/design/run_identity_and_unattended_invocation_spike.rst, docs/design/design.rst</files>
  <read_first>
    - `.planning/phases/31-foundation-spikes-run-identity-unattended-invocation/31-DECISION.md` - the whole document, which is the source this page summarises
    - `docs/design/canonical_record_spike.rst` - the primary structural template: opening paragraph naming the investigation date and what was and was not built, a Background section listing the concrete questions settled, per-topic three-column list-tables, and a closing Future scope section
    - `docs/design/uncertain_scheduling_spike.rst` - the secondary template, for its more compact single-topic Decisions list-table shape
    - `docs/design/design.rst` - the existing Design Notes toctree this page must join
    - `.planning/phases/31-foundation-spikes-run-identity-unattended-invocation/31-PATTERNS.md` section on the durable summary, which quotes both templates' section shapes verbatim
  </read_first>
  <action>
    Create `docs/design/run_identity_and_unattended_invocation_spike.rst`, following the structural
    template quoted in 31-PATTERNS.md.

    Open with a title and an underline of matching length, then an opening paragraph in the shape
    the canonical-record page uses: what this spike settled, the date the live investigation ran,
    that it read the real dev database and probed the real host rather than reasoning from
    documentation alone, and what was deliberately not built - no schema migration, no adapter code,
    no scheduler entry point. Point to `31-DECISION.md` as the full-evidence companion, giving its
    current path and repeating the archival caveat both prior pages carry: this project's milestone
    workflow moves completed phase directories once their milestone closes, so a reader whose path
    no longer resolves should look in the archive location.

    Background section: a bulleted list of the four concrete questions this phase answers, one per
    requirement, phrased as questions a reader outside `.planning/` would recognise rather than as
    requirement identifiers. Say why they had to be settled first: every later phase in this
    milestone executes these decisions instead of making them.

    Then two Decisions sections, one per track, each a three-column list-table with Topic, Decision
    and Phase columns, matching the shape the canonical-record page uses.

    The identity table needs one row per topic: the schema shape for a non-campaign run; the
    write-time identity field and its uniqueness constraint; what each of the three ingest paths
    writes into it; and the classical path's sufficiency verdict. Each Decision cell states the
    verdict in prose a reader can act on, and each Phase cell names the phase that implements it.

    The scheduling table needs one row per topic: the invocation mechanism; overlap prevention; how
    credentials reach an unattended run; and how a missed invocation becomes visible. Where a
    finding is unconfirmed for a scope - the container image, the AWS target - say so inside the
    Decision cell rather than omitting it, using the same words the decision document used. A
    durable page that quietly drops the caveats is worse than no page.

    Where the decision document recorded a correction or a late-breaking qualification, use the
    bold-dated attribution shape the canonical-record page uses for its own domain correction:
    state the date, state who it came from, and frame it as qualifying the section below rather
    than silently rewriting an earlier finding.

    Close with a Future scope section in the shape both prior pages use: point to `31-DECISION.md`
    for the full evidence each decision rests on, name explicitly which questions are still open
    and for which phase, and restate that none of it is implemented in this spike.

    Then add one line to `docs/design/design.rst`: the new page's name, appended to the existing
    toctree under Design Notes, after the canonical-record entry so the two spike pages sit
    together. Change nothing else in that file.

    Quote no credential value, no email address, no submitter contact detail and no line lifted
    from an operator-supplied schedule file. This page is published documentation.
  </action>
  <verify>
    <automated>P=docs/design/run_identity_and_unattended_invocation_spike.rst; test -f "$P" &amp;&amp; grep -q '^Background' "$P" &amp;&amp; grep -q '^Future scope' "$P" &amp;&amp; test "$(grep -c 'list-table' "$P")" -ge 2 &amp;&amp; grep -q '31-DECISION' "$P" &amp;&amp; grep -q 'run_identity_and_unattended_invocation_spike' docs/design/design.rst</automated>
    <fails_when>non-zero exit - the page does not exist, is missing its Background or Future scope section, carries fewer than two decision tables (one per track), never points back to the full-evidence companion, or was never added to the toctree in `docs/design/design.rst`.</fails_when>
    <automated>P=docs/design/run_identity_and_unattended_invocation_spike.rst; grep -q 'load_telescope_runs' "$P" &amp;&amp; grep -q 'sync_lco_observation_calendar' "$P" &amp;&amp; grep -q 'sync_gemini_observation_calendar' "$P" &amp;&amp; grep -qiE 'flock|file lock' "$P" &amp;&amp; grep -qiE 'unconfirmed|not confirmed' "$P"</automated>
    <fails_when>non-zero exit - the page omits one of the three ingest paths, never names the locking mechanism, or carries no unconfirmed label, which is what a durable page that dropped the scope caveats looks like.</fails_when>
    <automated>test "$(git diff --numstat -- docs/design/design.rst | awk '{print $1"/"$2}')" = "1/0"</automated>
    <fails_when>non-zero exit - the added/removed line counts for `docs/design/design.rst` are not exactly one added and none removed. An empty diff yields an empty string and fails here, so a toctree entry that was never added does not pass silently; a rewritten file yields something like `47/46` and fails too.</fails_when>
  </verify>
  <acceptance_criteria>
    - `docs/design/run_identity_and_unattended_invocation_spike.rst` exists with a title, a matching underline, an opening paragraph naming the investigation date, and Background, two Decisions and Future scope sections
    - The opening paragraph states what was not built and points to `31-DECISION.md` with the archival-path caveat
    - The identity Decisions table has a row for each of: schema shape, identity field and constraint, per-ingest-path value, classical sufficiency
    - The scheduling Decisions table has a row for each of: mechanism, overlap prevention, credential handling, missed-invocation visibility
    - Every unconfirmed scope from the decision document appears in the page with the same unconfirmed wording
    - `docs/design/design.rst` gains exactly one line, the new page's toctree entry
    - The page contains no email address, no credential value and no line lifted from an operator-supplied file
  </acceptance_criteria>
  <done>Both verdicts are readable outside `.planning/` on a page wired into the published documentation, with the unconfirmed scopes carried through rather than dropped.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the phase changed no source behaviour, build the docs, and remove the disposable database copy</name>
  <files>tmp/31-spike-db-copy.sqlite3</files>
  <read_first>
    - `CLAUDE.md` - the Testing section (the Django runner is the only functioning suite) and the Commands section (use `python manage.py`, never the executable-script form)
    - `.pre-commit-config.yaml` - the Sphinx build hook, whose argument list is the invocation to mirror here
    - `.planning/phases/31-foundation-spikes-run-identity-unattended-invocation/31-VALIDATION.md` - the sampling rate and the phase gate, which set the regression bar at "unchanged", not "passing new tests"
    - `.planning/phases/31-foundation-spikes-run-identity-unattended-invocation/31-DECISION.md` - to confirm the final document is intact before the phase seals
  </read_first>
  <action>
    Close the phase by proving the three things roadmap Success Criterion 5 claims.

    First, that the documentation builds with the new page in it. Run the same Sphinx invocation the
    pre-commit hook uses - make mode, the html builder, the `docs` source directory, an output
    directory, full traceback on exception, a fresh environment rather than a saved one, the
    doctrees cache directory, and the notebook-excluding override the hook passes so the build does
    not execute every notebook. A page added to a toctree that fails to build is worse than an
    orphaned one.

    Second, that no source behaviour changed. This phase's plans list only files under `.planning/`,
    `docs/design/` and the git-excluded `tmp/` directory, so the claim is checkable directly rather
    than inferred from a test run: confirm the working tree shows no modification under
    `solsys_code/` or `src/`, and confirm no migration file was added. Then run a targeted subset of
    the existing suite as a second, independent signal - the modules covering the model whose schema
    this phase reasoned about, the three ingest paths whose identity keys it inventoried, and the
    reconciler that reads the campaign FK. Do not run the `solsys_code` suite unqualified: one test
    class in the views module segfaults in native ASSIST, and importing the ephemeris utilities
    triggers a very large kernel download. None of the modules named below imports it - verify that
    is still true before running, since a module's imports can drift.

    Third, that nothing disposable was committed. Confirm no file under `tmp/` is tracked, then
    delete the disposable database copy created in plan 31-01, which holds a full copy of the
    operator's real dev data and has no reason to persist once the probe transcripts are quoted into
    the decision document. Leave the text transcripts in place; they are git-excluded and are the
    working record behind the quoted evidence.

    Finally, run the credential-and-contact gate over both committed artifacts - the decision
    document and the new design page - as the last thing before the phase seals. Both were built by
    quoting probe output verbatim, and one of the sources of that output is a host whose
    git-excluded machine-local settings file holds a live credential.
  </action>
  <verify>
    <automated>sphinx-build -M html ./docs ./_readthedocs -T -E -d ./docs/_build/doctrees -D 'exclude_patterns=notebooks/*,_build' 2&gt;&amp;1 | tail -20 | grep -q 'build succeeded'</automated>
    <fails_when>non-zero exit - the build did not print `build succeeded` in its closing lines. A page with a broken title underline, a malformed list-table or a toctree reference to a missing document produces a warning-or-error summary instead.</fails_when>
    <automated>test -z "$(git status --porcelain -- solsys_code src)" &amp;&amp; test -z "$(git status --porcelain -- solsys_code/migrations)"</automated>
    <fails_when>non-zero exit - `git status` printed at least one modified, added or untracked path under `solsys_code/` or `src/`, meaning the investigation left a source edit or a stray migration behind.</fails_when>
    <automated>python manage.py test solsys_code.tests.test_campaign_models solsys_code.tests.test_load_telescope_runs solsys_code.tests.test_sync_lco_observation_calendar solsys_code.tests.test_sync_gemini_observation_calendar solsys_code.tests.test_campaign_reconciler solsys_code.tests.test_canonical_record_migration -v1 2&gt;&amp;1 | tail -5 | grep -qE '^OK'</automated>
    <fails_when>non-zero exit - the run did not end with an `OK` summary line. A failure or error prints `FAILED (failures=N)` or `FAILED (errors=N)` instead, and a label that matches no test prints an import error rather than `OK`, so a mistyped module name fails here rather than passing vacuously.</fails_when>
    <automated>test -z "$(git ls-files tmp/)" &amp;&amp; rm -f tmp/31-spike-db-copy.sqlite3 &amp;&amp; test ! -e tmp/31-spike-db-copy.sqlite3</automated>
    <fails_when>non-zero exit - at least one file under `tmp/` is tracked by git (a disposable probe artifact was committed), or the disposable database copy still exists after the removal.</fails_when>
    <automated>D=.planning/phases/31-foundation-spikes-run-identity-unattended-invocation/31-DECISION.md; F=$(grep -n '^## Findings' "$D" | head -1 | cut -d: -f1); R=$(grep -n '^## Recommendation' "$D" | head -1 | cut -d: -f1); LASTE=$(grep -n '^#### SCHE' "$D" | tail -1 | cut -d: -f1); FIRSTV=$(grep -nE '^### (SCHEMA|SCHED)-0' "$D" | head -1 | cut -d: -f1); test -n "$F" &amp;&amp; test -n "$R" &amp;&amp; test -n "$LASTE" &amp;&amp; test -n "$FIRSTV" &amp;&amp; test "$F" -lt "$LASTE" &amp;&amp; test "$LASTE" -lt "$R" &amp;&amp; test "$R" -lt "$FIRSTV" &amp;&amp; test "$(grep -c '^#### SCHE' "$D")" -ge 6 &amp;&amp; test "$(grep -cE '^### (SCHEMA|SCHED)-0' "$D")" -eq 4</automated>
    <fails_when>non-zero exit - a heading is missing, or the document is out of order: every evidence subsection must sit between `## Findings` and `## Recommendation`, and every verdict subsection after `## Recommendation`. An evidence subsection appended below the recommendations (which is what a later plan appending at end of file produces) puts `LASTE` after `R` and fails here. Fewer than six evidence subsections or other than exactly four verdict subsections also fails.</fails_when>
    <automated>D=.planning/phases/31-foundation-spikes-run-identity-unattended-invocation/31-DECISION.md; P=docs/design/run_identity_and_unattended_invocation_spike.rst; test -f "$D" &amp;&amp; test -f "$P" &amp;&amp; ! grep -REiq 'api[_-]?key[[:space:]]*[:=]|[0-9a-f]{32,}|[[:alnum:]._%+-]+@[[:alnum:].-]+\.[[:alpha:]]{2,}' "$D" "$P"</automated>
    <fails_when>non-zero exit - either committed artifact is missing, or one of them contains an assignment-shaped credential token, a 32-or-more-character hex string, or an email address. The two `test -f` guards mean a missing file fails rather than letting the negative search pass vacuously.</fails_when>
    <human-check>
      Read `docs/design/run_identity_and_unattended_invocation_spike.rst` as a reader who was not
      part of this phase. Both Decisions tables should be actionable without opening
      `31-DECISION.md`, and every unconfirmed scope - the container image, the AWS target, and the
      classical schedule-file question if no real file was obtained - should be visible on the page
      rather than only in the evidence document. If a verdict reads as more settled on the page than
      the evidence behind it supports, say so and the wording will be corrected before the phase
      seals.
    </human-check>
  </verify>
  <acceptance_criteria>
    - The Sphinx build prints `build succeeded`
    - `git status --porcelain -- solsys_code src` prints nothing
    - The targeted test run ends with an `OK` summary line
    - `git ls-files tmp/` prints nothing and `tmp/31-spike-db-copy.sqlite3` no longer exists
    - Neither `31-DECISION.md` nor the new design page contains an email address, a credential-shaped assignment or a long hex string
    - `31-DECISION.md` contains exactly four verdict subsections (`### SCHEMA-01`, `### SCHEMA-02`, `### SCHEMA-03`, `### SCHED-07`), all of them below `## Recommendation`
    - `31-DECISION.md` contains at least six `#### ` evidence subsections, all of them between `## Findings` and `## Recommendation`
    - `31-DECISION.md` contains both track headings, `### Schema/identity track` and `### Scheduling track`, under `## Findings`
  </acceptance_criteria>
  <done>The documentation builds with the new page included, the source tree and the existing suite are provably untouched, the disposable database copy is gone, and neither committed artifact carries a credential or a contact detail.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `31-DECISION.md` (internal evidence document) -> published Sphinx documentation | the design page is built for publication, so anything summarised onto it leaves the repository's internal planning context |
| git-excluded probe transcripts and disposable database copy -> the git index | a full copy of the operator's real dev data sits under `tmp/` until this plan removes it |
| machine-local settings override holding a live credential -> both committed artifacts | the phase's evidence discipline is verbatim quoting, and this is the last gate before that discipline's output is published |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-16 | Information Disclosure | published design page and committed decision document | high | mitigate | Task 2's final gate searches both artifacts for an assignment-shaped credential token, a 32-or-more-character hex string and an email address, with existence guards so a missing file cannot pass the search vacuously. Task 1 forbids quoting any operator-supplied schedule line, contact detail or credential value onto the page. |
| T-31-17 | Information Disclosure | `tmp/31-spike-db-copy.sqlite3`, a full copy of the real dev database | medium | mitigate | The copy is created inside the git-excluded `tmp/` directory and is deleted by task 2, whose verify command fails if it still exists. Task 2 also fails if any file under `tmp/` has become tracked. |
| T-31-18 | Tampering | an accidental source edit surviving to the phase seal | medium | mitigate | Task 2 asserts an empty `git status` for `solsys_code/` and `src/` and separately for the migrations directory, and independently runs a targeted subset of the existing suite as a second signal. |
| T-31-19 | Denial of Service | running the test suite unqualified | low | mitigate | The regression run names explicit module labels. Running the app's suite unqualified reaches a views test class that segfaults in native ASSIST and triggers a very large kernel download, which is why the action requires the named modules' imports to be re-checked before running rather than trusted. |
| T-31-SC | Tampering | package-manager installs | low | accept | This plan installs no package. It runs the documentation build and test runner already present in the development environment. |
</threat_model>

<verification>
Run from the repository root. Use `python manage.py`, never the executable-script form. Do not run
the `solsys_code` suite unqualified.

1. `sphinx-build -M html ./docs ./_readthedocs -T -E -d ./docs/_build/doctrees -D 'exclude_patterns=notebooks/*,_build'`
   prints `build succeeded`.
2. `python manage.py test solsys_code.tests.test_campaign_models solsys_code.tests.test_load_telescope_runs solsys_code.tests.test_sync_lco_observation_calendar solsys_code.tests.test_sync_gemini_observation_calendar solsys_code.tests.test_campaign_reconciler solsys_code.tests.test_canonical_record_migration -v1`
   ends with `OK`.
3. `git status --porcelain -- solsys_code src` prints nothing.
4. `git ls-files tmp/` prints nothing; `tmp/31-spike-db-copy.sqlite3` does not exist.
5. `docs/design/design.rst` gained exactly one line.
6. The credential-and-contact search over `31-DECISION.md` and the new design page finds nothing.
</verification>

<success_criteria>
- Roadmap Success Criterion 5 is met in both halves: a `docs/design/` page carries the identity
  scheme and the scheduling verdict forward for readers outside `.planning/`, and the test suite is
  unchanged because no source behaviour changed.
- The new page builds under Sphinx and is reachable from the Design Notes toctree.
- Every unconfirmed scope recorded in the decision document is visible on the published page.
- No disposable artifact was committed and the disposable database copy is removed.
</success_criteria>

<output>
Create `.planning/phases/31-foundation-spikes-run-identity-unattended-invocation/31-05-SUMMARY.md` when done
</output>
