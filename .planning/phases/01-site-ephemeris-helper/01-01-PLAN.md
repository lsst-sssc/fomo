---
phase: 01-site-ephemeris-helper
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - solsys_code/solsys_code_observatory/models.py
  - solsys_code/solsys_code_observatory/migrations/0002_observatory_timezone_seed.py
  - solsys_code/telescope_runs.py
  - solsys_code/tests/test_telescope_runs.py
autonomous: true
requirements: [SITE-01, SITE-02, SITE-03, EPHEM-01, EPHEM-02, EPHEM-03]
must_haves:
  truths:
    - "Observatory records for obscodes 268, 269, 809, E10 exist after migration with correct lat/lon/altitude and timezone"
    - "get_site('Magellan-Clay') returns an Observatory whose to_earth_location() yields an astropy EarthLocation at the Las Campanas coordinates"
    - "get_site(name).timezone resolves via zoneinfo to the correct IANA zone (America/Santiago for Magellan/NTT, Australia/Sydney for FTS)"
    - "horizon_dip(2402) returns 1.44 degrees within 0.02"
    - "sun_event(site, date, 'sun') returns two UTC astropy.time.Time values using the -(0.833 + dip) threshold"
    - "sun_event(site, date, 'dark') returns two UTC astropy.time.Time values using the -15 degree threshold (no dip)"
  artifacts:
    - path: "solsys_code/telescope_runs.py"
      provides: "SITES dict, get_site(), horizon_dip(), sun_event(), crossing-search helpers"
      contains: "SITES"
      exports: ["SITES", "get_site", "horizon_dip", "sun_event"]
    - path: "solsys_code/solsys_code_observatory/models.py"
      provides: "to_earth_location() method + timezone field"
      contains: "to_earth_location"
    - path: "solsys_code/solsys_code_observatory/migrations/0002_observatory_timezone_seed.py"
      provides: "AddField(timezone) + RunPython seed of 4 Observatory records"
      contains: "AddField"
    - path: "solsys_code/tests/test_telescope_runs.py"
      provides: "DB-dependent tests for site resolution, dip, sun/dark events"
      contains: "class"
  key_links:
    - from: "solsys_code/telescope_runs.py"
      to: "solsys_code.solsys_code_observatory.models.Observatory"
      via: "Observatory.objects.get(obscode=...)"
      pattern: "Observatory\\.objects\\.get"
    - from: "solsys_code/telescope_runs.py"
      to: "Observatory.to_earth_location"
      via: "site.to_earth_location() in sun_event"
      pattern: "to_earth_location"
---

<objective>
Deliver the Walking Skeleton for the telescope-runs site/ephemeris helper: the thinnest end-to-end slice that proves the full stack works. A real DB write (migration seeds 4 Observatory records + adds a timezone field), a real DB read (get_site resolves a telescope name to an Observatory), and a real computation path (sun_event computes dip-corrected sunset/sunrise and -15 degree dark-window crossings, validated by the dip-formula check and the June 10 2026 skycalc -18 degree anchor).

Purpose: Establish the architectural backbone (Observatory as single source of truth + astropy-only computation with no SPICE kernel import) that the validation slice (Plan 02) and any future stages build on.

Output: solsys_code/telescope_runs.py, a new migration, an extended Observatory model, and DB-dependent tests covering SITE-01/02/03 and EPHEM-01/02/03.
</objective>

<execution_context>
@/home/tlister/git/fomo_devel/.claude/gsd-core/workflows/execute-plan.md
@/home/tlister/git/fomo_devel/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-site-ephemeris-helper/01-CONTEXT.md
@.planning/phases/01-site-ephemeris-helper/01-RESEARCH.md
@.planning/phases/01-site-ephemeris-helper/01-PATTERNS.md
@.planning/phases/01-site-ephemeris-helper/SKELETON.md
</context>

<phase_goal>
**As a** FOMO operator scheduling telescope runs, **I want to** resolve a telescope name (Magellan, NTT, FTS) and a date to its observing site and accurate dip-corrected UTC sunset/sunrise and dark-window times, **so that** I can place observing nights on the calendar with sun-event boundaries trustworthy to within 2 minutes of the LCO skycalc reference.
</phase_goal>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend Observatory model with timezone field + to_earth_location(), and migrate + seed the 4 site records</name>
  <files>solsys_code/solsys_code_observatory/models.py, solsys_code/solsys_code_observatory/migrations/0002_observatory_timezone_seed.py</files>
  <read_first>
    - solsys_code/solsys_code_observatory/models.py (the file being modified; mirror to_geodetic()/to_geocentric() style at lines 104-129; note the from_parallax_constants() silent-fallback anti-pattern at lines 70-87 — do NOT replicate it)
    - solsys_code/solsys_code_observatory/migrations/0001_initial.py (existing migration structure and field definitions to mirror)
    - .planning/phases/01-site-ephemeris-helper/01-RESEARCH.md (Pattern 1 and Pattern 4: to_earth_location and the AddField+RunPython seed migration)
    - .planning/phases/01-site-ephemeris-helper/01-CONTEXT.md (D-01, D-02, D-05 — locked field, method, obscodes, and seed data)
  </read_first>
  <behavior>
    - Observatory has a timezone CharField (max_length=64, blank=True, default='', verbose_name='IANA timezone name').
    - Observatory.to_earth_location() returns astropy.coordinates.EarthLocation(lon=self.lon * u.deg, lat=self.lat * u.deg, height=self.altitude * u.m) with no silent guard — raises naturally if lon/lat/altitude are None.
    - After migrate, Observatory.objects.get(obscode='268') exists with lat=-29.0146, lon=-70.6926, altitude=2402, timezone='America/Santiago'.
    - After migrate, obscode '269' exists (Las Campanas coords, America/Santiago), '809' exists (lat=-29.2567, lon=-70.7300, altitude=2347, America/Santiago), 'E10' exists (lat=-31.2734, lon=149.0612, altitude=1149, Australia/Sydney).
    - Each of the 4 records has a distinct unique name (name field is unique=True) — Magellan-Clay and Magellan-Baade must NOT share a name.
    - Migration is reversible: unseed deletes the 4 records by obscode.
  </behavior>
  <action>
    Add `import astropy.units as u` and `from astropy.coordinates import EarthLocation` to the model imports. Add a `timezone` CharField (per D-01) following the existing `altitude` field style. Add `to_earth_location(self) -> EarthLocation` (per D-02) alongside `to_geodetic`/`to_geocentric`/`ObservatoryXYZ`, with a Google-style docstring matching `to_geodetic`, returning `EarthLocation(lon=self.lon * u.deg, lat=self.lat * u.deg, height=self.altitude * u.m)`. Do NOT add an `if self.lat and self.lon` guard — let astropy raise, avoiding the documented silent-fallback anti-pattern.

    Create migration `0002_observatory_timezone_seed.py` with `dependencies = [('solsys_code_observatory', '0001_initial')]`. Operations: (1) `migrations.AddField` for `timezone` (CharField, blank=True, default='', max_length=64, verbose_name='IANA timezone name'); (2) `migrations.RunPython(seed_observatories, unseed_observatories)`. seed_observatories uses `apps.get_model('solsys_code_observatory', 'Observatory')` and `update_or_create(obscode=..., defaults=...)` for the 4 locked records (D-05): obscode 268 name 'Magellan Clay Telescope' short_name 'Magellan-Clay' lat=-29.0146 lon=-70.6926 altitude=2402 timezone='America/Santiago'; obscode 269 name 'Magellan Baade Telescope' short_name 'Magellan-Baade' (same Las Campanas coords/tz); obscode 809 name 'ESO, La Silla' short_name 'NTT' lat=-29.2567 lon=-70.7300 altitude=2347 timezone='America/Santiago'; obscode E10 name 'Siding Spring Observatory' short_name 'FTS' lat=-31.2734 lon=149.0612 altitude=1149 timezone='Australia/Sydney'. unseed_observatories deletes `obscode__in=['268','269','809','E10']`. Use distinct unique `name` values for Clay vs Baade to avoid the IntegrityError pitfall (RESEARCH Pitfall 3).

    Do not run `makemigrations` to autogenerate — author the migration file by hand following the 0001_initial structure so the RunPython seed is included.
  </action>
  <verify>
    <automated>cd /home/tlister/git/fomo_devel && ./manage.py migrate solsys_code_observatory && ./manage.py shell -c "from solsys_code.solsys_code_observatory.models import Observatory; o=Observatory.objects.get(obscode='268'); print(o.timezone, round(o.altitude,1)); print(o.to_earth_location())"</automated>
  </verify>
  <acceptance_criteria>
    - `./manage.py migrate solsys_code_observatory` applies cleanly with no error.
    - `Observatory.objects.get(obscode='268').timezone` equals 'America/Santiago' and `.altitude` equals 2402.
    - `Observatory.objects.get(obscode='E10').timezone` equals 'Australia/Sydney'.
    - `Observatory.objects.filter(obscode__in=['268','269','809','E10']).count()` equals 4.
    - `Observatory.objects.get(obscode='268').to_earth_location()` returns an astropy EarthLocation without raising.
    - `models.py` contains the string `to_earth_location` and the string `timezone = models.CharField`.
  </acceptance_criteria>
  <done>The Observatory model exposes a timezone field and to_earth_location() method, and migrate creates the 4 site records with correct coordinates/timezones, satisfying SITE-02 (timezone data), SITE-03 (records exist), and the model side of SITE-01.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create telescope_runs.py (SITES, get_site, horizon_dip, sun_event + crossing search) and DB-dependent tests proving the end-to-end slice</name>
  <files>solsys_code/telescope_runs.py, solsys_code/tests/test_telescope_runs.py</files>
  <read_first>
    - .planning/phases/01-site-ephemeris-helper/01-RESEARCH.md (Patterns 1-3: _solar_altitude, _find_crossing, _local_noon_utc, the horizon_dip and sun_event sketches — verified against astropy 7.2.0; the Anti-Patterns and Common Pitfalls sections)
    - .planning/phases/01-site-ephemeris-helper/01-CONTEXT.md (D-03 SITES dict, D-04 get_site, D-06/D-07/D-08 sun_event signature/kinds/date semantics)
    - .planning/phases/01-site-ephemeris-helper/01-PATTERNS.md (telescope_runs.py and test_telescope_runs.py sections: imports, DB-lookup pattern, docstring style, TestCase/setUp/assertAlmostEqual pattern, error-path test)
    - solsys_code/solsys_code_observatory/tests/test_models.py (TestCase + setUp(precision) + assertAlmostEqual analog)
    - solsys_code/solsys_code_observatory/models.py (to_earth_location() and timezone added in Task 1)
  </read_first>
  <behavior>
    - SITES is a dict mapping 'Magellan-Clay'->'268', 'Magellan-Baade'->'269', 'NTT'->'809', 'FTS'->'E10' (D-03).
    - get_site('Magellan-Clay') returns the Observatory with obscode '268'; get_site('NoSuchTelescope') raises Observatory.DoesNotExist (no silent None).
    - get_site('Magellan-Clay').to_earth_location() yields an EarthLocation; get_site('FTS').timezone == 'Australia/Sydney'.
    - horizon_dip(2402) is within 0.02 of 1.44 (degrees).
    - sun_event(site, date, 'sun') returns a 2-tuple of astropy.time.Time (UTC scale): (sunset, sunrise) using threshold -(0.833 + horizon_dip(site.altitude)).
    - sun_event(site, date, 'dark') returns (dark_start, dark_end) using threshold -15.0, NO dip correction.
    - sun_event(site, date, 'badkind') raises ValueError.
    - For Las Campanas (obscode 268) date 2026-06-10, sun_event(..., 'sun')[0] (sunset) is within 2 minutes of 21:59 UTC and [1] (sunrise) within 2 minutes of 11:25 UTC (RESEARCH EPHEM-01 anchor).
  </behavior>
  <action>
    Create `solsys_code/telescope_runs.py` with module imports grouped stdlib / third-party / local per PATTERNS: `from datetime import date as date_cls, datetime, time`; `from math import sqrt`; `from zoneinfo import ZoneInfo`; `import numpy as np`; `import astropy.units as u`; `from astropy.coordinates import AltAz, get_sun`; `from astropy.time import Time`; `from solsys_code.solsys_code_observatory.models import Observatory`. Do NOT import `solsys_code.ephem_utils` (triggers ~1.6GB SPICE kernel download — RESEARCH Anti-Pattern).

    Define `SITES` (UPPER_CASE module constant) exactly per D-03. Define `get_site(name: str) -> Observatory` that looks up `SITES[name]` then `Observatory.objects.get(obscode=...)`, letting `Observatory.DoesNotExist` propagate (no silent fallback). Define `horizon_dip(altitude_m: float) -> float` returning `1.76 * sqrt(altitude_m) / 60.0` (degrees). Define private helpers `_solar_altitude(times, location)`, `_find_crossing(anchor, location, threshold_deg, search_hours=24, coarse_step_min=1.0)`, and `_local_noon_utc(local_date, tz_name)` exactly per RESEARCH Patterns 2-3 (coarse 1-min scan over +/-12h from local noon UTC, then ~10-iteration bisection to sub-second precision — always run the bisection refinement to stay inside the 2-minute tolerance, RESEARCH Pitfall 2).

    Define `sun_event(site, date, kind)` per D-06/D-07/D-08: build `location = site.to_earth_location()`, `anchor = _local_noon_utc(date, site.timezone)`. For kind 'sun': `threshold = -(0.833 + horizon_dip(site.altitude))`. For 'dark': `threshold = -15.0` (no dip). Else raise `ValueError`. Return `(crossings[0], crossings[1])` as the (setting, rising) pair of astropy.time.Time UTC objects. `date` is the local calendar date of sunset (D-08); anchor at local noon to keep both the evening sunset and following morning event inside the window (RESEARCH Pitfall 1). All functions use Google-style docstrings with Args/Returns.

    Create `solsys_code/tests/test_telescope_runs.py` as a Django `TestCase` (DB access) mirroring test_models.py: `setUp` sets `self.precision`, and run the migration-seeded records (test DB applies migrations automatically). Tests: (a) test_get_site_returns_observatory — get_site('Magellan-Clay').obscode == '268' and to_earth_location() returns an EarthLocation (SITE-01); (b) test_get_site_timezone — get_site('NTT').timezone == 'America/Santiago' and get_site('FTS').timezone == 'Australia/Sydney' (SITE-02); (c) test_get_site_unknown — assertRaises(Observatory.DoesNotExist, get_site, 'NoSuchTelescope'); (d) test_seeded_records — assert the 4 obscodes exist with expected altitude (SITE-03); (e) test_horizon_dip — assertAlmostEqual(horizon_dip(2402), 1.44, delta-style within 0.02) (EPHEM-03); (f) test_sun_event_sun — for obscode 268 on date(2026,6,10), sunset within 2 min of 21:59 UTC, sunrise within 2 min of 11:25 UTC, both results are astropy.time.Time (EPHEM-01); (g) test_sun_event_dark — kind 'dark' returns a 2-tuple of Time using -15 threshold, dark_start later (more negative sun) than the 'sun' sunset (EPHEM-02); (h) test_sun_event_bad_kind — assertRaises(ValueError). Compare Time values by converting to UTC datetime and asserting `abs((computed - reference).total_seconds()) <= 120`.
  </action>
  <verify>
    <automated>cd /home/tlister/git/fomo_devel && ./manage.py test solsys_code.tests.test_telescope_runs -v 2</automated>
  </verify>
  <acceptance_criteria>
    - `./manage.py test solsys_code.tests.test_telescope_runs` passes with all tests green (≥8 tests).
    - `grep -c 'SITES' solsys_code/telescope_runs.py` is ≥ 1 and the dict contains 'Magellan-Clay', 'Magellan-Baade', 'NTT', 'FTS'.
    - `grep -v '^#' solsys_code/telescope_runs.py | grep -c 'ephem_utils'` equals 0 (no SPICE-kernel import).
    - test_sun_event_sun proves Las Campanas 2026-06-10 sunset within 120 s of 21:59 UTC and sunrise within 120 s of 11:25 UTC.
    - test_get_site_unknown proves Observatory.DoesNotExist propagates (no silent None).
  </acceptance_criteria>
  <done>telescope_runs.py resolves sites from the Observatory model and computes dip-corrected sun events and -15 degree dark windows, with DB tests proving SITE-01, EPHEM-01, EPHEM-02, EPHEM-03 — the full vertical slice (DB read/write + real computation + real validation) works end-to-end.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
New symbols created by this phase (exclude from drift verification):
- File: `solsys_code/telescope_runs.py`
- File: `solsys_code/solsys_code_observatory/migrations/0002_observatory_timezone_seed.py`
- File: `solsys_code/tests/test_telescope_runs.py`
- Module constant: `SITES` (dict)
- Functions: `get_site(name)`, `horizon_dip(altitude_m)`, `sun_event(site, date, kind)`, `_solar_altitude(times, location)`, `_find_crossing(anchor, location, threshold_deg, ...)`, `_local_noon_utc(local_date, tz_name)`
- Migration functions: `seed_observatories(apps, schema_editor)`, `unseed_observatories(apps, schema_editor)`
- Method: `Observatory.to_earth_location()`
- Model field: `Observatory.timezone` (CharField)
</artifacts_this_phase_produces>

<verification>
- `./manage.py migrate solsys_code_observatory` applies the new migration cleanly.
- `./manage.py test solsys_code.tests.test_telescope_runs` passes.
- The 4 Observatory records (268, 269, 809, E10) exist with correct lat/lon/altitude/timezone.
- No import of `solsys_code.ephem_utils` anywhere in `telescope_runs.py`.
</verification>

<success_criteria>
- SITE-01: get_site resolves a telescope name to an Observatory and to_earth_location() yields an EarthLocation. ✓
- SITE-02: get_site(name).timezone gives the correct IANA zone for each site. ✓
- SITE-03: Observatory records for 268/269/809/E10 exist with correct coordinates. ✓
- EPHEM-01: sun_event(..., 'sun') applies the -(0.833 + dip) threshold. ✓
- EPHEM-02: sun_event(..., 'dark') applies the -15 degree threshold. ✓
- EPHEM-03: horizon_dip(2402) is 1.44 ± 0.02 degrees. ✓
</success_criteria>

<output>
Create `.planning/phases/01-site-ephemeris-helper/01-01-SUMMARY.md` when done.
</output>
