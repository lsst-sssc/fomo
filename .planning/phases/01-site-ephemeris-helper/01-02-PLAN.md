---
phase: 01-site-ephemeris-helper
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - solsys_code/tests/test_telescope_runs.py
autonomous: true
requirements: [EPHEM-04, EPHEM-05, EPHEM-06]
must_haves:
  truths:
    - "Las Campanas dip-corrected sunset/sunrise for June 1, 10, 20, 30 2026 agree with LCO skycalc to within 2 minutes"
    - "The -18 degree astronomical twilight crossings for Las Campanas on June 10 2026 are 19:16 / 06:08 local (Santiago UTC-4) within 2 minutes"
    - "America/Santiago resolves to UTC-4 in June and UTC-3 in January; Australia/Sydney resolves to UTC+10 in July and UTC+11 in January"
    - "ruff check . and ruff format --check . stay clean on the new/modified files"
  artifacts:
    - path: "solsys_code/tests/test_telescope_runs.py"
      provides: "Skycalc validation, -18 twilight cross-check, and DST resolution tests"
      contains: "2026"
  key_links:
    - from: "solsys_code/tests/test_telescope_runs.py"
      to: "solsys_code.telescope_runs.sun_event"
      via: "validation comparison against skycalc reference values"
      pattern: "sun_event"
    - from: "solsys_code/tests/test_telescope_runs.py"
      to: "zoneinfo.ZoneInfo"
      via: "DST offset assertions for Santiago/Sydney"
      pattern: "ZoneInfo"
---

<objective>
Refine the Walking Skeleton slice into the validated, production-trustworthy deliverable. Add the precision-validation tests that prove the sun_event computation matches the LCO skycalc reference to within 2 minutes across all four June 2026 sample nights, that the -18 degree astronomical twilight cross-check (June 10) reproduces skycalc's 19:16/06:08 local crossings, and that zoneinfo DST resolution is correct for both Southern-Hemisphere timezones across their DST boundaries. Then lock the quality gates (ruff).

Purpose: The skeleton (Plan 01) proves the path works; this plan proves it is accurate enough to ship — the core value of the phase (within 2 minutes of skycalc).

Output: Extended test_telescope_runs.py with validation, twilight cross-check, and DST tests, plus clean ruff gates.
</objective>

<execution_context>
@/home/tlister/git/fomo_devel/.claude/gsd-core/workflows/execute-plan.md
@/home/tlister/git/fomo_devel/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-site-ephemeris-helper/01-CONTEXT.md
@.planning/phases/01-site-ephemeris-helper/01-RESEARCH.md
@.planning/phases/01-site-ephemeris-helper/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add skycalc sunset/sunrise validation (4 June dates) and the -18 degree twilight cross-check</name>
  <files>solsys_code/tests/test_telescope_runs.py</files>
  <read_first>
    - solsys_code/tests/test_telescope_runs.py (created in Plan 01 — extend it, mirror its TestCase/setUp/assertAlmostEqual style and its UTC-datetime within-120-seconds comparison helper)
    - .planning/phases/01-site-ephemeris-helper/01-RESEARCH.md (EPHEM-04/05 rows; Open Question 1 on the LCO reference values; the verified June 10 2026 anchors: sunset 21:59 UTC / sunrise 11:25 UTC, and -18 crossings 23:16:00 UTC = 19:16 local / 10:08:00 UTC = 06:08 local)
    - .planning/phases/01-site-ephemeris-helper/01-CONTEXT.md (D-09 reference-data source: https://www.lco.cl/ephemeris-for-lco/; the -18 twilight value is used only as a cross-check, not a third 'kind')
  </read_first>
  <behavior>
    - For Las Campanas (obscode 268) on each of date(2026,6,1), (6,10), (6,20), (6,30): sun_event(site, d, 'sun') sunset and sunrise each agree with the LCO skycalc reference value to within 120 seconds.
    - The -18 degree twilight crossings for June 10 2026 (computed via _find_crossing or sun_event-style search at threshold -18.0, no dip) equal 23:16:00 UTC (= 19:16 local, Santiago UTC-4) and 10:08:00 UTC (= 06:08 local) to within 120 seconds.
    - All four sample nights produce a sunset earlier in UTC than the following sunrise, and the dark window (-15) sits strictly inside the sun-to-sun window.
  </behavior>
  <action>
    Obtain the LCO skycalc reference sunset/sunrise values for Las Campanas on June 1, 10, 20, 30 2026 (D-09). Primary source: the LCO ephemeris tool at https://www.lco.cl/?page_id=299 (linked from https://www.lco.cl/ephemeris-for-lco/). The June 10 anchor is already confirmed by RESEARCH: sunset 21:59 UTC, sunrise 11:25 UTC, and the -18 twilight crossings 23:16:00 UTC (19:16 local) / 10:08:00 UTC (06:08 local). If the LCO form cannot be reached during execution (RESEARCH Open Question 1 / Assumption A4), fall back to the validated-internal-consistency strategy: anchor on the exact June 10 -18 match and assert the other three dates' computed sunset/sunrise are self-consistent (monotonic seasonal drift toward the June solstice, each within the same 2-minute envelope as June 10). Record in the SUMMARY which path was taken and the exact reference values used.

    Extend test_telescope_runs.py with: (a) test_sunset_sunrise_validation — parametrized over the four June 2026 dates, asserting sun_event(site, d, 'sun') sunset/sunrise are within 120 seconds of the skycalc reference (store references in a module-level dict of {date: (sunset_utc, sunrise_utc)}); (b) test_twilight_18deg_crosscheck — assert the -18 degree crossings for June 10 2026 are within 120 seconds of 23:16:00 UTC and 10:08:00 UTC, and that converted to Santiago local time they read 19:16 / 06:08 (EPHEM-05). For the -18 cross-check, call the existing private `_find_crossing` with threshold=-18.0 (no dip) or a thin local helper — do NOT add a new 'twilight' kind to sun_event (D-07/Deferred: -18 is a cross-check value only). Reuse the within-120-seconds UTC-datetime comparison from Plan 01.
  </action>
  <verify>
    <automated>cd /home/tlister/git/fomo_devel && ./manage.py test solsys_code.tests.test_telescope_runs.TestSunEventValidation -v 2 2>/dev/null || ./manage.py test solsys_code.tests.test_telescope_runs -v 2</automated>
  </verify>
  <acceptance_criteria>
    - The validation tests pass: all four June 2026 sample nights' sunset and sunrise within 120 seconds of the recorded reference.
    - The -18 degree June 10 crossings test passes: 23:16:00 UTC / 10:08:00 UTC within 120 seconds, local 19:16 / 06:08.
    - `grep -c '2026' solsys_code/tests/test_telescope_runs.py` is ≥ 4 (the four sample dates present).
    - No new `kind` value added to sun_event — `grep -v '^#' solsys_code/telescope_runs.py | grep -c "'twilight'"` equals 0.
  </acceptance_criteria>
  <done>EPHEM-04 (4-date skycalc agreement within 2 min) and EPHEM-05 (-18 degree twilight cross-check) are proven by passing tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add timezone DST resolution tests and lock the ruff quality gates</name>
  <files>solsys_code/tests/test_telescope_runs.py</files>
  <read_first>
    - solsys_code/tests/test_telescope_runs.py (extend; same TestCase/style)
    - .planning/phases/01-site-ephemeris-helper/01-RESEARCH.md (EPHEM-06 row: Santiago UTC-4 June / UTC-3 January; Sydney UTC+10 July / UTC+11 January — verified numerically)
    - CLAUDE.md (quality gates: ruff check . and ruff format --check . must stay clean; single quotes, 120-char lines, Google docstrings)
  </read_first>
  <behavior>
    - ZoneInfo('America/Santiago') yields UTC offset -4 hours for a datetime in June 2026 and -3 hours for a datetime in January 2026.
    - ZoneInfo('Australia/Sydney') yields UTC offset +10 hours for a datetime in July 2026 and +11 hours for a datetime in January 2026.
    - ruff check . reports no errors on the new/modified files; ruff format --check . reports them already formatted.
  </behavior>
  <action>
    Add test_timezone_dst_resolution to test_telescope_runs.py: construct timezone-aware datetimes via `datetime(2026, 6, 15, 12, tzinfo=ZoneInfo('America/Santiago'))` etc. and assert `.utcoffset()` equals `timedelta(hours=-4)` in June and `timedelta(hours=-3)` in January for Santiago, and `timedelta(hours=10)` in July / `timedelta(hours=11)` in January for Sydney (EPHEM-06). This is a pure-zoneinfo test (no DB needed) but co-located in the existing TestCase per the project's simplest-placement convention.

    Then run the quality gates and fix any violations in the new/modified files: `ruff check .` and `ruff format .` (apply formatting) followed by `ruff format --check .`. Ensure single-quote style, 120-char lines, and Google-style docstrings on all new code. Do not reformat unrelated files.
  </action>
  <verify>
    <automated>cd /home/tlister/git/fomo_devel && ./manage.py test solsys_code.tests.test_telescope_runs -v 2 && ruff check . && ruff format --check .</automated>
  </verify>
  <acceptance_criteria>
    - test_timezone_dst_resolution passes (4 offset assertions across the two zones and two seasons).
    - `ruff check .` exits 0.
    - `ruff format --check .` exits 0.
    - The full `./manage.py test solsys_code.tests.test_telescope_runs` suite passes.
  </acceptance_criteria>
  <done>EPHEM-06 (DST resolution) is proven, and the ruff quality gates are clean — the phase deliverable is validated and conforms to project conventions.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
New symbols created by this plan (exclude from drift verification):
- Test methods in `solsys_code/tests/test_telescope_runs.py`: `test_sunset_sunrise_validation` (or `TestSunEventValidation`), `test_twilight_18deg_crosscheck`, `test_timezone_dst_resolution`
- Module-level reference-data dict in the test file mapping June 2026 dates to skycalc (sunset_utc, sunrise_utc) values
</artifacts_this_phase_produces>

<verification>
- `./manage.py test solsys_code.tests.test_telescope_runs` passes (all Plan 01 + Plan 02 tests).
- `ruff check .` and `ruff format --check .` are clean.
- The four June 2026 dates and the -18 degree June 10 crossings are validated within 2 minutes.
- Santiago/Sydney DST offsets resolve correctly across their respective boundaries.
</verification>

<success_criteria>
- EPHEM-04: Las Campanas sunset/sunrise for June 1/10/20/30 2026 within 2 minutes of LCO skycalc. ✓
- EPHEM-05: -18 degree twilight for June 10 2026 within 2 minutes of 19:16/06:08 local. ✓
- EPHEM-06: Santiago UTC-4(Jun)/UTC-3(Jan) and Sydney UTC+10(Jul)/UTC+11(Jan). ✓
- Quality gates (ruff check / ruff format --check) clean. ✓
</success_criteria>

<output>
Create `.planning/phases/01-site-ephemeris-helper/01-02-SUMMARY.md` when done.
</output>
