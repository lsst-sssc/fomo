---
phase: 35-allocation-layer-classical-cutover
plan: 07
type: execute
wave: 5
depends_on: ["35-05", "35-06"]
files_modified:
  - docs/runbooks/telescope_runs_calendar.rst
  - CLAUDE.md
  - docs/notebooks/pre_executed/load_telescope_runs_demo.ipynb
  - docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb
autonomous: true
requirements: [ALLOC-04, ALLOC-05]

estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The operator runbook's classical-ingest section describes what the command now does — creates or updates one allocation record per schedule line, with an optional proposal token and a --dry-run flag — rather than what it used to do (D-02, D-01)."
    - "The runbook carries a new cutover section stating the four steps in order: migrate, deploy, run the one-time cutover command, run the reconciler sweep once (D-15)."
    - "The runbook's 'Can I correct a run's source?' section states the new consequence: a queue source now decides that a run keeps one whole-window entry, and a LEGACY row stays per-night until a human relabels it (D-10)."
    - "The command cheat-sheet lists the new cutover command and the classical loader's new --dry-run flag, and the troubleshooting section covers a reported source_identifier collision and a reported unexplainable event."
    - "load_telescope_runs_demo.ipynb runs against a throwaway copy of the developer database and shows, with real executed output, one CampaignRun per line and the ALLOC:-keyed nights the projector drew from it."
    - "reconcile_campaign_runs_demo.ipynb carries the cutover before-and-after diff D-15 names: per-night counts before and after, zero date-bearing reconciler nights left, zero unexplained blank-url classical events beyond those the command reported, containers unchanged and every facility-url-keyed observation event byte-identical."
    - "Both notebooks are committed with their executed output present, as every notebook under docs/notebooks/pre_executed/ is."
    - "CLAUDE.md's notebook pairing map names the cutover command and points it at the reconciler demo notebook, so the paired-docs rule stays enforceable for the next change to that command."
    - "The Sphinx documentation build is clean."
  artifacts:
    - docs/runbooks/telescope_runs_calendar.rst
    - docs/notebooks/pre_executed/load_telescope_runs_demo.ipynb
    - docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb
  key_links:
    - "docs/index.rst toctree -> docs/runbooks/telescope_runs_calendar (the page an operator actually reaches)"
    - "CLAUDE.md notebook map -> cutover_classical_allocations.py -> reconcile_campaign_runs_demo.ipynb"
    - "the cutover command's real output -> the notebook's before/after diff cells"
---

<objective>
Ship the paired documentation this phase's CLAUDE.md rule makes part of the deliverable: the
operator runbook sections whose documented behaviour changed, and the two pre-executed demo
notebooks whose modules changed — both re-executed with real output, not hand-edited.

This is the last expansion slice: the same capability the tracer proved is now something an
operator can find, follow and trust without reading the source.

Purpose: ALLOC-04 and ALLOC-05 are operator-facing. A cutover whose sequencing exists only in a
planning document is not a stated sequencing.

Output: an updated runbook with a new cutover section, two re-executed notebooks, and an extended
CLAUDE.md notebook map.

Note on what this plan deliberately does NOT cover: `project_observation_calendar_demo.ipynb`'s
owed un-routed re-execution (the Phase 34 paired-docs step that also repairs 14 stale LCO events)
is Phase 34's debt, recorded in STATE.md as "Phase 35 or a quick task". Those are facility-url-keyed
observation events, outside every candidate set this phase touches. It is surfaced below as a
carried item rather than folded in.
</objective>

<execution_context>
@/home/tlister/git/fomo_devel/.claude/gsd-core/workflows/execute-plan.md
@/home/tlister/git/fomo_devel/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-allocation-layer-classical-cutover/35-CONTEXT.md
@.planning/phases/35-allocation-layer-classical-cutover/35-05-SUMMARY.md
@.planning/phases/35-allocation-layer-classical-cutover/35-06-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Runbook sections and the CLAUDE.md notebook map</name>
  <files>docs/runbooks/telescope_runs_calendar.rst, CLAUDE.md</files>
  <read_first>
    docs/runbooks/telescope_runs_calendar.rst (whole file, 1223 lines — in particular the sections headed "How do I load a classical telescope schedule?" (line 16), "How do I re-resolve campaign run sites that have gone stale?" (line 746, the one-time-command section whose style the new cutover section copies), "How do I get every campaign run onto the calendar?" (line 788), "Can I correct a run's source?" (line 569), the cheat-sheet list-table at line 1067, and "Troubleshooting" at line 1097)
    docs/index.rst line 24 (the toctree entry that makes this page reachable)
    solsys_code/management/commands/load_telescope_runs.py (as rewritten by 35-05 — the new flag, the new summary lines, the new help text)
    solsys_code/management/commands/cutover_classical_allocations.py (as written by 35-06 — its help text, its counters, its exit behaviour)
    solsys_code/management/commands/reconcile_campaign_runs.py (its new counters)
    .planning/phases/35-allocation-layer-classical-cutover/35-06-SUMMARY.md (the real before/after numbers to quote)
    CLAUDE.md (the "Paired docs are part of the deliverable" paragraph and its notebook pairing map)
  </read_first>
  <action>
Update the operator runbook so it describes the system as it now behaves, and extend the pairing map
so the rule stays enforceable for the next change.

1. "How do I load a classical telescope schedule?" — rewrite the body. The command now creates or
   updates one allocation record per schedule line and lets the allocation projector draw the
   per-night calendar; it no longer writes calendar events itself. Document, with a worked example
   line for each: the optional bracketed proposal token and what it is for (two proposals sharing a
   telescope, an instrument and a set of nights are otherwise indistinguishable); the new
   `--dry-run` flag and what its two summary lines mean; the two summary lines a real run prints
   (run-level tallies, then aggregated night-level tallies); and what a reported collision means and
   what to do about it (add the proposal token to one of the two lines, then re-import). State
   plainly that the calendar entries a schedule line produces look exactly as they did before this
   change — same titles, same spans — and that the one visible difference is that a cancelled line's
   event description now also carries the shared "Run status" line.

2. "How do I get every campaign run onto the calendar?" — update for the new dispatch and the new
   counters. A run with a queue source keeps one whole-window entry regardless of its site; a run
   with a resolved site and window and any other source gets one entry per night. Document
   `retired`, `rekeyed` and `legacy_deleted` in operator language: a night handed over to a real
   observation; a night carried across from the old key form; and one-time removal of leftovers from
   a run that now keeps a single whole-window entry.

3. "Can I correct a run's source?" — extend with the D-10 consequence. Correcting a run's source to
   a queue value now also changes the shape of its calendar entry from one-per-night to one
   whole-window entry, and the next reconcile converges on it. A `LEGACY` row stays per-night until
   a human relabels it: nothing infers a run's provenance from its site or its telescope name.

4. New section, "How do I run the one-time classical cutover?", placed immediately after the
   existing site-repair section so the two one-time commands sit together, and written in the same
   style. It must carry: the four steps in order with the exact command line for each; what the
   cutover converts and what it deliberately leaves alone; the instruction to run `--dry-run` first
   and read the unexplained list; what a non-zero exit means and what an operator does about it
   (resolve the listed events in the admin, then re-run); that the command never deletes a calendar
   event; that it is safe to re-run; and the real before/after numbers from plan 35-06's scratch-copy
   run, quoted as a worked example rather than as a promise about any particular database.

5. Cheat-sheet list-table — add a row for the cutover command and update the classical loader's row
   with its new flag.

6. Troubleshooting — add two entries: a reported identity-key collision on import, and a reported
   unexplainable event during the cutover. Each gives the symptom, the cause and the fix.

7. `CLAUDE.md` — extend the notebook pairing map with one entry:
   `solsys_code/management/commands/cutover_classical_allocations.py` ->
   `reconcile_campaign_runs_demo.ipynb`. Add it to the existing prose list in the same sentence
   style, so a future change to that command is covered by the paired-docs rule with no second edit.
   Change nothing else in CLAUDE.md.

Keep the file's existing reStructuredText conventions: the same heading underline characters, the
same `.. code-block:: console` form, and the same `.. note::` / `.. warning::` usage.
  </action>
  <acceptance_criteria>
    - `docs/runbooks/telescope_runs_calendar.rst` contains a section heading whose text includes `classical cutover`.
    - The runbook contains all four of `cutover_classical_allocations`, `--dry-run`, `rekeyed` and `legacy_deleted`.
    - `CLAUDE.md` contains the literal `cutover_classical_allocations.py` exactly once, inside the notebook pairing map.
    - `pre-commit run sphinx-build --all-files` exits 0.
    - `git diff --stat -- CLAUDE.md` shows a change of no more than 6 lines — the map entry only.
  </acceptance_criteria>
  <verify>
    <automated>python -c "src=open('docs/runbooks/telescope_runs_calendar.rst').read();toks=['classical cutover','cutover_classical_allocations','--dry-run','rekeyed','legacy_deleted'];print(sum(t in src for t in toks))"</automated>
    <fails_when>prints any value other than 5</fails_when>
    <automated>python -c "print(open('CLAUDE.md').read().count('cutover_classical_allocations.py'))"</automated>
    <fails_when>prints any value other than 1</fails_when>
    <automated>pre-commit run sphinx-build --all-files</automated>
    <fails_when>non-zero exit, or the hook line reports "Failed", or the output contains "WARNING" for the runbooks page</fails_when>
  </verify>
  <done>An operator can find the four cutover steps, the new flag, the new counters and the two new failure modes in the runbook, and the pairing map covers the new command.</done>
</task>

<task type="auto">
  <name>Task 2: Re-execute the classical ingest demo notebook against a scratch database</name>
  <precondition>`src/fomo_db.sqlite3` exists and is readable, and `jupyter nbconvert` is available on PATH — the notebook copies that database and is re-executed in place.</precondition>
  <files>docs/notebooks/pre_executed/load_telescope_runs_demo.ipynb</files>
  <read_first>
    docs/notebooks/pre_executed/load_telescope_runs_demo.ipynb (all 25 cells; note that its Django-setup cell at index 2 does NOT route to a scratch database today — it writes through to the real developer database, which is where the existing blank-url classical events came from)
    docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb cells 2 and 24 (the scratch-copy setup and teardown pattern to copy verbatim: export the path variable BEFORE `django.setup()`, and remove the copy at the end)
    solsys_code/management/commands/load_telescope_runs.py (as rewritten by 35-05)
    solsys_code/allocation_projector.py (`allocation_night_url()` — the key form the inspection cells now query)
    CLAUDE.md (the paired-docs rule and the nbconvert regeneration command)
  </read_first>
  <action>
Bring the classical-ingest demo across to the allocation path and re-execute it with real output.

1. Route it at a throwaway database. Copy the scratch-copy setup from the reconciler demo's
   Django-setup cell verbatim: copy `src/fomo_db.sqlite3` to a temporary file and export the
   database-path variable BEFORE `django.setup()`, because that call is what materialises the
   database configuration. Add the matching teardown cell at the end. This is a behaviour change for
   this notebook and it is deliberate: the command now creates `CampaignRun` rows, and a notebook
   run must not write those into the developer database.

2. Update the narrative and the code cells for the new behaviour:
   - the invocation cell keeps its captured-stdout pattern but now shows both summary lines;
   - the inspection cell after it shows the created `CampaignRun` row's fields (campaign, source,
     approval status, run status, telescope/instrument, window, sub-night fields, identity key)
     AND the `ALLOC:`-keyed calendar events the projector drew from it, so the reader sees the two
     halves of the new path;
   - the cancelled-run cell keeps its point but now shows the status mapping onto the run and the
     resulting title prefix on every night;
   - the idempotency cell asserts the run reports unchanged and every night reports unchanged;
   - the drift-tolerance cell is rewritten: its point is now that the identity key is derived from
     the line's own fields, so a drifting sun-event time cannot create a second row and cannot move
     an existing night's stored span. Keep the `unittest.mock` patch that produces the drift and
     assert the stored span did not move.
   - the partial-night cell shows the two new sub-night fields on the run and the resulting spans on
     the nights.
   - add one new cell demonstrating the proposal token: two lines identical except for their
     bracketed token produce two distinct runs, and the same two lines without the token produce one
     run plus a reported collision.
   - the `--campaign` cell shows the campaign on the run as well as on each night.
   - add a `--dry-run` cell before the first real invocation, showing that it writes nothing.
   - update the summary markdown cell to describe the allocation path.

3. Re-execute in place with
   `jupyter nbconvert --to notebook --execute --inplace docs/notebooks/pre_executed/load_telescope_runs_demo.ipynb`
   and commit the notebook WITH its output. Run it from the repository root: nbconvert sets the
   kernel's working directory to the notebook's own directory, which is exactly what the setup
   cell's `parents[2]` repository-root derivation assumes. Notebooks under
   `docs/notebooks/pre_executed/` are excluded from the output-clearing hook precisely so their
   executed output is what ships.

4. If a cell fails on a transient environment condition, re-clone the scratch copy from
   `src/fomo_db.sqlite3` and re-execute once more rather than editing an output by hand. A
   hand-edited output is not executed output and does not satisfy the paired-docs rule. If it fails
   twice for the same reason, stop and report it.
  </action>
  <acceptance_criteria>
    - `jupyter nbconvert --to notebook --execute --inplace docs/notebooks/pre_executed/load_telescope_runs_demo.ipynb` exits 0.
    - Every code cell in the notebook has at least one output entry and an `execution_count` that is not null.
    - The notebook's source contains all of `FOMO_DATABASE_PATH`, `ALLOC:`, `source_identifier` and `--dry-run`.
    - The notebook's source contains no occurrence of the retired blank-url classical lookup: `CalendarEvent.objects.filter(url='')`.
    - The real `src/fomo_db.sqlite3` is unmodified.
  </acceptance_criteria>
  <verify>
    <automated>jupyter nbconvert --to notebook --execute --inplace docs/notebooks/pre_executed/load_telescope_runs_demo.ipynb</automated>
    <fails_when>non-zero exit, or the output contains "CellExecutionError" or "Traceback"</fails_when>
    <automated>python -c "import json;nb=json.load(open('docs/notebooks/pre_executed/load_telescope_runs_demo.ipynb'));code=[c for c in nb['cells'] if c['cell_type']=='code'];print(len(code), sum(1 for c in code if c.get('outputs') and c.get('execution_count') is not None))"</automated>
    <fails_when>the two numbers differ, or the first number is 0</fails_when>
    <automated>python -c "import json;nb=json.load(open('docs/notebooks/pre_executed/load_telescope_runs_demo.ipynb'));src=''.join(''.join(c['source']) for c in nb['cells']);print(sum(t in src for t in ('FOMO_DATABASE_PATH','ALLOC:','source_identifier','--dry-run')))"</automated>
    <fails_when>prints any value other than 4</fails_when>
    <automated>git status --porcelain -- src/fomo_db.sqlite3</automated>
    <fails_when>prints any line at all</fails_when>
  </verify>
  <done>The classical-ingest demo runs against a throwaway database, shows the run and the nights it produced, demonstrates the proposal token and the collision report, and is committed with real executed output.</done>
</task>

<task type="auto">
  <name>Task 3: The cutover before-and-after diff in the reconciler demo notebook</name>
  <precondition>`src/fomo_db.sqlite3` exists and still contains the pre-cutover calendar state (blank-url classical events and date-bearing reconciler nights) — the notebook's before/after diff is measured against a copy of it, and a database already cut over has no before-state to show.</precondition>
  <files>docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb</files>
  <read_first>
    docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb (all 25 cells — the scratch-copy setup at index 2, the four-run dispatch-branch seeding at index 6, the before/after diff pattern at indices 15-16, and the teardown at index 24)
    solsys_code/management/commands/cutover_classical_allocations.py (as written by 35-06)
    solsys_code/campaign_reconciler.py (the new dispatch and the `legacy_deleted` counter)
    solsys_code/allocation_projector.py (the key form and the takeover)
    .planning/phases/35-allocation-layer-classical-cutover/35-06-SUMMARY.md (the scratch-copy before/after table this notebook reproduces as executed output)
    CLAUDE.md (the paired-docs rule)
  </read_first>
  <action>
Make this notebook the executed proof of ROADMAP Success Criterion 5.

1. Update the existing seeding cell so the four seeded runs exercise the THREE current dispatch
   branches plus the new queue-source one: a class-wide run, a satellite-sited run, a queue-sourced
   run with a resolved ground site (which now gets a container, not per-night events — this is the
   D-10 change, and the cell's markdown must say so), and a campaign-less classical-sourced run with
   a resolved ground site (which gets allocation nights).

2. Update the sweep cells for the new counters, and update the namespace-isolation cell: a real
   sweep still touches nothing outside the namespaces it owns, which now means the reconciler's
   container keys and the allocation projector's night keys, and never a facility-url-keyed
   observation event.

3. Update the attributed-night cell: the rule is no longer "an attributed night gets no reconciler
   event, and the existing one is detached" but "a night covered by a linked, placed or observed
   record has no allocation event at all". Show the retire and the restore as the spike measured
   them: link, re-project, unlink.

4. Add a new cutover section, the heart of this task. Against the same scratch copy of the real
   developer database:
   - capture the before-state: total calendar events, and counts by key family (date-bearing
     reconciler nights, bare containers, blank-url rows, facility-url rows, allocation rows),
     plus a snapshot of every facility-url-keyed event's url, title, description, start, end and
     modified timestamp;
   - run the cutover with `--dry-run` and show its output;
   - run it for real and show its output, including the unexplained list;
   - run the reconciler sweep once and show its output;
   - capture the after-state with the same queries and print a before/after table;
   - assert, in executed cells rather than in prose: zero date-bearing reconciler nights remain;
     the only blank-url rows remaining are the ones the cutover reported as unexplained; the bare
     containers are unchanged; and the facility-url snapshot is byte-identical before and after.
   Each assertion is a real `assert` in a code cell, so a future re-execution fails loudly rather
   than printing a stale claim.

5. Update the summary markdown cell and keep the existing teardown cell last.

6. Re-execute in place with
   `jupyter nbconvert --to notebook --execute --inplace docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb`,
   run from the repository root, and commit with output. On a transient failure, re-clone the
   scratch copy from `src/fomo_db.sqlite3` and re-execute once; on a second failure for the same
   reason, stop and report.
  </action>
  <acceptance_criteria>
    - `jupyter nbconvert --to notebook --execute --inplace docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb` exits 0.
    - Every code cell has at least one output entry and a non-null `execution_count`.
    - The notebook's source contains all of `cutover_classical_allocations`, `ALLOC:`, `legacy_deleted` and `assert`.
    - The notebook contains at least four `assert` statements in the cutover section (the four end-state properties).
    - The real `src/fomo_db.sqlite3` is unmodified.
    - `pre-commit run sphinx-build --all-files` exits 0 with both notebooks rendered.
  </acceptance_criteria>
  <verify>
    <automated>jupyter nbconvert --to notebook --execute --inplace docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb</automated>
    <fails_when>non-zero exit, or the output contains "CellExecutionError" or "Traceback"</fails_when>
    <automated>python -c "import json;nb=json.load(open('docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb'));code=[c for c in nb['cells'] if c['cell_type']=='code'];src=''.join(''.join(c['source']) for c in nb['cells']);print(len(code), sum(1 for c in code if c.get('outputs') and c.get('execution_count') is not None), sum(t in src for t in ('cutover_classical_allocations','ALLOC:','legacy_deleted')), src.count('assert '))"</automated>
    <fails_when>the first two numbers differ, or the third number is not 3, or the fourth number is below 4</fails_when>
    <automated>git status --porcelain -- src/fomo_db.sqlite3</automated>
    <fails_when>prints any line at all</fails_when>
    <automated>pre-commit run sphinx-build --all-files</automated>
    <fails_when>non-zero exit, or the hook line reports "Failed"</fails_when>
    <human-check>Open the rendered notebook and confirm the before/after table shows real numbers from a copy of the developer database, that the four end-state assertions executed and passed, and that the unexplained list contains only events you recognise.</human-check>
  </verify>
  <done>The reconciler demo notebook carries the executed cutover before-and-after diff, with the four Success-Criterion-5 properties asserted in code rather than claimed in prose.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
- `docs/runbooks/telescope_runs_calendar.rst` — new section "How do I run the one-time classical cutover?", new cheat-sheet row, two new troubleshooting entries
- `CLAUDE.md` — new notebook-map entry pairing `cutover_classical_allocations.py` with `reconcile_campaign_runs_demo.ipynb`
- `docs/notebooks/pre_executed/load_telescope_runs_demo.ipynb` — new scratch-database routing and teardown cells, a proposal-token cell, a dry-run cell
- `docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb` — a new cutover section with four executed end-state assertions

No production symbol is created by this plan.
</artifacts_this_phase_produces>

<flagged_assumptions>
- **Carried Phase 34 item, deliberately not folded in** — `project_observation_calendar_demo.ipynb`
  still owes an un-routed re-execution, which also repairs the 14 stale `KEY2026B-004` LCO events
  (`F-34-1`). STATE.md records it as "Phase 35 or a quick task". Those are facility-url-keyed
  observation events, outside every candidate set this phase touches, and re-executing that notebook
  un-routed writes to the real developer database — a different risk profile from this plan's
  scratch-copy runs. Recommend a quick task immediately after this phase rather than folding it in.
- **ALLOC-04 and ALLOC-05 (edge probe, unclassified, unresolved)** — carried from plans 35-05 and
  35-06; this plan documents the behaviours but does not resolve the probe rows.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| notebook execution -> the developer database | a re-executed notebook runs real commands that write real rows unless it is routed at a copy |
| published runbook -> operator action | a wrong or stale instruction causes an operator to run an irreversible command in the wrong order |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-16 | Tampering | `load_telescope_runs_demo.ipynb` writing through to the developer database | high | mitigate | The notebook is routed at a throwaway copy by exporting the database-path variable before `django.setup()`, copying the pattern the reconciler demo already uses, with a teardown cell at the end. An acceptance criterion asserts the real database file is unmodified after the run. This closes a real exposure: the notebook writes through today, which is where the existing blank-url classical events came from. |
| T-35-17 | Repudiation | stale or hand-edited notebook output | medium | mitigate | Both notebooks are regenerated with `jupyter nbconvert --to notebook --execute --inplace`, and a verify command asserts every code cell carries a non-null execution count and at least one output. Hand-editing an output is explicitly forbidden in the task body; on a transient failure the instruction is to re-clone and re-execute, not to patch. |
| T-35-05 | Tampering | runbook cutover instructions | high | mitigate | The new section states the four steps in order, tells the operator to dry-run first and read the unexplained list, and states that the command never deletes an event and is safe to re-run. The before/after numbers are quoted as a worked example from a measured run rather than as a promise. |
| T-35-08 | Information Disclosure | executed notebook output committed to the repository | low | accept | The notebooks run against a copy of the developer database whose contents are observatory codes, telescope names, campaign names and calendar entries — the same data the public calendar already renders. No credential and no contact detail is printed by any cell. |
| T-35-SC | Tampering | npm/pip/cargo installs | high | accept | No package is installed by this plan; `pyproject.toml` is unchanged. |
</threat_model>

<verification>
- `pre-commit run sphinx-build --all-files` green with both notebooks rendered.
- Both notebooks re-executed in place with output committed and every code cell carrying a non-null execution count.
- The runbook's cutover section states the four steps in order.
- `src/fomo_db.sqlite3` unmodified.
</verification>

<success_criteria>
- ROADMAP Success Criterion 5 is demonstrated, not asserted: the reconciler demo notebook carries a real before/after diff with four executed end-state assertions.
- The paired-docs obligation in CLAUDE.md is discharged inside the phase, for both changed modules and the affected runbook page, and the pairing map covers the new command for next time.
</success_criteria>

<output>
Create `.planning/phases/35-allocation-layer-classical-cutover/35-07-SUMMARY.md` when done.
</output>
