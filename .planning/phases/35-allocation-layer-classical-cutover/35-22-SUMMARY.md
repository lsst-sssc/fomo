---
phase: 35-allocation-layer-classical-cutover
plan: 22
subsystem: allocation-calendar
tags: [documentation, runbook, jupyter-notebook, paired-docs, cr-01, cr-02]

# Dependency graph
requires:
  - phase: 35-allocation-layer-classical-cutover
    provides: plan 35-20's human-confirmation decline guard (`_remint_decline_reason()`, CR-01) and plan 35-21's site-carrying mint-provenance token (CR-02) -- the two behaviour changes this plan documents
provides:
  - "docs/runbooks/telescope_runs_calendar.rst's `retired`/`detach_declined` counter documentation widened to describe both CR-01 and CR-02, plus a post-upgrade deploy note for the one-time provenance audit"
  - "docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb regenerated by re-execution with real executed cells for the CR-02 site correction and the CR-01 declined re-mint"
affects: [35-allocation-layer-classical-cutover verification/UAT -- this plan closes the round]

# Actuals (#2632)
actuals:
  tokens: 10303
  tasks: 2
  commits: 2

tech-stack:
  added: []
  patterns:
    - "Round-granularity paired-docs discharge: CLAUDE.md's paired-docs rule is satisfied once, in one pass against the final state of the tree, by the plan that runs LAST in a gap-closure round -- not once per code plan. Plans 35-20 and 35-21 both changed documented behaviour and were both forbidden from touching docs/; this plan wrote the runbook prose once (describing both changes together, consistently) and re-executed the notebook once (exercising both changes in the same run), rather than twice each."

key-files:
  created: []
  modified:
    - docs/runbooks/telescope_runs_calendar.rst
    - docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb

key-decisions:
  - "Seeded a second real ground site (obscode E10, Siding Spring Observatory, Australia/Sydney) in the notebook's new site-correction cell rather than reassigning to the notebook's existing satellite_site fixture -- reassigning to a SATELLITE_OBSTYPE site would change classical_run's dispatch branch entirely (to the whole-window container), which is a different mechanism (re-classification, already documented under legacy_deleted/detached) than the provenance-token site correction CR-02 actually fixes. E10 matches 35-21-PLAN.md's own reviewer probe 1 and TestSiteChangeRemints's fixture exactly, keeping the demo's real sun_event() boundary directly comparable to that test."
  - "The declined-re-mint cell seeds a dedicated single-night CampaignRun rather than reusing classical_run, whose three nights the cells above (observation handoff, then site correction) already exercise -- avoids compounding state across demos and mirrors TestRemintHumanConfirmationGuard's own fixture shape (a run with night_start_utc/night_end_utc set from the start, confirmed after one reconcile, then one sub-night field cleared to trigger the would-be re-mint)."
  - "Updated the pre-existing 'Six counters on ReconcileResult' and 'Summary' markdown cells (not just added two new cells) so the notebook's own prose agrees with the runbook's widened retired/detach_declined definitions -- per the Task 2 read_first instruction that the notebook's markdown and the runbook must agree on what the counters mean, and because leaving the old four-reasons/Phase-33-unchanged wording sitting next to two cells that prove otherwise would itself be a documentation-accuracy defect of the same class this round is about."

requirements-completed: [ALLOC-01, ALLOC-02, ALLOC-03, ALLOC-05]

coverage:
  - id: D1
    description: "The runbook's `retired` enumeration and `detach_declined` paragraph describe what the sweep actually does after this round: `retired` names five reasons (broadened reason 2 for a site correction, new reason 5 for the one-time provenance audit); `detach_declined` covers a declined re-mint alongside a declined attribution release, with the operator remedy stated for the declined-re-mint case (no longer the flat 'nothing to do' claim). A post-upgrade deploy note tells the operator to `--dry-run` first."
    requirement: ALLOC-01
    verification:
      - kind: other
        ref: "grep -c 'detach_declined' docs/runbooks/telescope_runs_calendar.rst == 6 (pre-edit count, widened not removed); grep -ci 'dry-run' >= 1; git status --short over solsys_code/, src/, docs/notebooks/ empty"
        status: pass
    human_judgment: false
  - id: D2
    description: "reconcile_campaign_runs_demo.ipynb's new site-correction cell exercises plan 35-21's CR-02 fix end to end: classical_run's site is corrected to a second real ground site (Siding Spring, E10), the affected night re-mints to a new primary key with boundaries equal to the corrected site's real sun_event() result, and the cell asserts the boundary actually moved -- reproducing 35-REVIEW.md's own probe 1."
    requirement: ALLOC-02
    verification:
      - kind: other
        ref: "docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb, cell 'A site correction re-mints an already-projected run's nights (plan 35-21, CR-02)' -- real executed assertions (pk changed, boundary changed, boundary == expected sun_event()), non-null execution_count"
        status: pass
    human_judgment: false
  - id: D3
    description: "reconcile_campaign_runs_demo.ipynb's new declined-re-mint cell exercises plan 35-20's CR-01 guard end to end: a night confirmed via CalendarEventMeta.confirmed_by survives a sub-night-window edit that would otherwise re-mint it -- reported as detach_declined=1, with the same primary key, boundaries and confirmation asserted to survive the whole reconcile_run() call."
    requirement: ALLOC-03
    verification:
      - kind: other
        ref: "docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb, cell 'Human confirmation declines an automated re-mint (plan 35-20, CR-01)' -- real executed assertions (detach_declined==1, retired==0, created==0, pk/boundaries/confirmed_by unchanged), non-null execution_count"
        status: pass
    human_judgment: false
  - id: D4
    description: "ALLOC-05's documentation consequence is discharged: the runbook's post-upgrade deploy note tells an operator that the first sweep after this release is a one-time provenance audit (a non-zero would_retire on unedited nights is expected, not a regression), with the underlying cutover-sequencing code itself untouched by this plan."
    requirement: ALLOC-05
    verification:
      - kind: other
        ref: "docs/runbooks/telescope_runs_calendar.rst, 'Post-upgrade deploy note' paragraph, placed with the sweep's counter documentation"
        status: pass
    human_judgment: false
  - id: D5
    description: "The round's code surface is still green behind the documentation pass: the five-module regression (232/232, matching plan 35-21's recorded count exactly), both ruff hooks, and makemigrations --check --dry-run all pass at the end of the round -- proving a documentation-only plan did not quietly ship a broken tree, and that no source file was touched (git status --short over solsys_code/ and src/ empty throughout, src/fomo_db.sqlite3 never written)."
    requirement: ALLOC-01
    verification:
      - kind: integration
        ref: "python manage.py test solsys_code.tests.test_allocation_projector solsys_code.tests.test_allocation_projector_signals solsys_code.tests.test_cutover_classical_allocations solsys_code.tests.test_load_telescope_runs solsys_code.tests.test_campaign_reconciler -- 232 tests, OK"
        status: pass
    human_judgment: false

duration: ~30min
completed: 2026-09-16
status: complete
---

# Phase 35 Plan 22: Paired Docs for the Gap-Closure Round (CR-01/CR-02) Summary

**The runbook's `retired`/`detach_declined` counter documentation and the paired `reconcile_campaign_runs_demo.ipynb` notebook now describe and prove what the sweep actually does after plan 35-20's human-confirmation decline guard and plan 35-21's site-carrying provenance token, closing gap-closure round 5.**

## Performance

- **Duration:** ~30 min
- **Started:** 2026-09-16T14:36:52Z (approx., immediately following plan 35-21)
- **Completed:** 2026-09-16T15:07:11Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments

- **Runbook widened, not relabelled** (Task 1): `docs/runbooks/telescope_runs_calendar.rst`'s `retired` enumeration now names five reasons instead of four -- reason (2) broadened to cover a `CampaignRun.site` correction alongside a sub-night window field change, and a new reason (5) documents the one-time provenance audit a pre-release-format mint-provenance token triggers on the first post-upgrade sweep (at most once per night; the same night reports `unchanged` on every sweep after). The `detach_declined` paragraph now covers two things instead of one: a companion row the sweep did not release (unchanged meaning) AND a re-mint the sweep declined to perform because the night carries a human confirmation, a real observation link, or an unverified companion row -- with the operator remedy stated explicitly for the declined-re-mint case, replacing the flat "nothing to do" claim that was only ever true for the first case. A new "Post-upgrade deploy note" paragraph tells an operator to run `--dry-run` first after upgrading and explains a non-zero `would_retire` on unedited nights as the expected one-time audit. No counter was renamed: `detach_declined`'s mention count holds at its pre-edit 6 (verified by `grep -c`). The `rekeyed` caveat, the `legacy_deleted` paragraph and the cutover section's four steps are byte-for-byte unchanged (confirmed by `git diff`).
- **Notebook regenerated with real proof for both changes** (Task 2): two new cell pairs (markdown + code) were added after the existing observation-handoff section and before the Summary cell. The **site-correction cell** corrects `classical_run`'s site to a second real ground site (Siding Spring, obscode `E10` -- the same site 35-21-PLAN.md's own reviewer probe 1 and `TestSiteChangeRemints` use), re-runs `reconcile_run()`, and asserts the affected night got a new primary key with boundaries equal to the corrected site's own `sun_event()` result (`pk=358 -> pk=365`, boundary moved from `2026-09-01T22:35:09Z..2026-09-02T10:49:50Z` to `2026-09-01T07:52:18Z..2026-09-01T20:14:41Z`, matching Siding Spring's real sunset/sunrise exactly). The **declined-re-mint cell** seeds a dedicated single-night run, confirms its allocation night via `CalendarEventMeta.confirmed_by`, then clears the run's sub-night window override (which would otherwise re-mint the night), and asserts the reconcile reports `detach_declined=1, retired=0, created=0` with the same primary key, boundaries and confirmation surviving. Both cells assert on real `ReconcileResult` counters and ORM state, not prose -- a future regression fails the notebook.
- **Notebook's own prose corrected to match** (Task 2): the "Six counters on ReconcileResult" and "Summary" markdown cells were updated so the notebook's own explanation of `retired`/`detach_declined` agrees with the runbook's widened definitions, rather than leaving stale "four reasons" / "unchanged in meaning from Phase 33" wording sitting next to two cells that now prove otherwise. Cell 2's stale "has not yet had migration 0018 applied" comment was also corrected (the scratch copy is migrated to whatever head currently is, which after plan 35-21 includes migration 0020) -- prose only, setup logic unchanged.
- **Round-closing gates all green**: the full five-module regression is 232/232 (matching plan 35-21's recorded count exactly, no lower); both `pre-commit run ruff --all-files` and `ruff-format --all-files` pass; `python manage.py makemigrations --check --dry-run` reports "No changes detected"; every code cell in the notebook carries a non-null `execution_count`; `git status --short -- src/fomo_db.sqlite3` printed nothing throughout the whole run, and `git status --short -- solsys_code/ src/` printed nothing at any point -- this plan changed exactly the two documentation files named in `files_modified`.

## Task Commits

Each task was committed atomically:

1. **Task 1: Make the runbook's counter vocabulary describe the sweep this round shipped** -- `4385742` (docs): widened the `retired` enumeration and the `detach_declined` paragraph; added the post-upgrade deploy note.
2. **Task 2: Give the paired demo notebook real executed output for both behaviour changes** -- `3ad0797` (docs): added and executed the site-correction and declined-re-mint cell pairs; corrected the stale migration-0018 comment; updated the "Six counters" and "Summary" markdown cells; recorded the round-closing gate results.

**Plan metadata:** this commit (docs: complete plan) -- see final commit below.

## Files Created/Modified

- `docs/runbooks/telescope_runs_calendar.rst` -- `retired` enumeration (five reasons), `detach_declined` paragraph (two declined outcomes + remedy), new "Post-upgrade deploy note" paragraph. `rekeyed`/`legacy_deleted`/cutover-steps prose untouched.
- `docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb` -- two new cell pairs (site correction, declined re-mint), regenerated by `jupyter nbconvert --to notebook --execute --inplace` twice (once after adding the cells, once more after `ruff-format` reformatted the embedded code, so the committed output matches the exact final source); "Six counters on ReconcileResult" and "Summary" markdown cells updated; cell 2's stale migration comment corrected.

## Decisions Made

See `key-decisions` in frontmatter. Summary: seeded a genuine second ground site (Siding Spring, `E10`) for the site-correction demo rather than reusing the notebook's existing satellite fixture, since reassigning to a satellite site would exercise dispatch-branch re-classification instead of CR-02's provenance-token fix; seeded a dedicated single-night run for the declined-re-mint demo to avoid compounding state on `classical_run`, mirroring the real unit test's own fixture shape; and updated the notebook's pre-existing explanatory markdown cells (not just added new ones) so its prose agrees with the runbook.

## Deviations from Plan

None -- plan executed exactly as written. One incidental correction worth recording (not a deviation, since no fix was needed beyond the format itself): `pre-commit run ruff-format --all-files` reformatted the embedded Python source inside the two new notebook cells (line wrapping, quote normalization) on its first run after Task 2's edits. Rather than leave the committed notebook's real output generated from a slightly different (pre-reformat) source, the notebook was re-executed a second time against the reformatted source before committing, so the on-disk output is generated from the exact source that is committed. Both `ruff --all-files` and `ruff-format --all-files` then passed cleanly on the re-verification pass, confirming idempotency.

## Issues Encountered

None.

## User Setup Required

None -- no schema change, no external service configuration required. This plan touched exactly two documentation files.

## Threat Flags

None -- this plan's threat model (in `35-22-PLAN.md`) is the authoritative register for the surface it touches (T-35-22-01..04, all `mitigate` disposition, all addressed by Tasks 1/2 as designed); no new surface outside that register was introduced.

## Next Phase Readiness

- **Gap-closure round 5 is complete.** With this plan green, all three of 35-REVIEW.md iteration 8's criticals (CR-01, CR-02, CR-03) are closed, and the round's paired-docs obligation (CLAUDE.md) is discharged for both plan 35-20's and plan 35-21's behaviour changes, in one pass against the final state of the tree, as designed.
- An operator reading the sweep's counters is now told what the sweep does (five `retired` reasons, two `detach_declined` outcomes with the remedy for the declined-re-mint case) and is warned in advance that the first post-upgrade sweep is a one-time provenance audit.
- The paired demo notebook proves both of this round's behaviour changes with real executed output and real assertions against a migrated scratch copy of the developer database -- the developer database (`src/fomo_db.sqlite3`) itself was never written.
- The phase's whole regression surface (five modules, 232 tests), both lint gates and the migration check are all green behind the documentation pass.
- WR-03's and WR-04's non-paired-docs items (35-REVIEW.md iteration 8, `<review_dispositions>`) remain deferred and open, as intended -- nothing beyond the paired-docs obligation was expanded into. IN-01 and IN-03 also remain untouched and open, per plan 35-21's own readiness note.
- No blockers. Phase 35's remaining plans (if any) or the phase verification/UAT pass can proceed against a runbook and a notebook that now agree with the shipped code.

---
*Phase: 35-allocation-layer-classical-cutover*
*Completed: 2026-09-16*

## Self-Check: PASSED
