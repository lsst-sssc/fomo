---
phase: 35-allocation-layer-classical-cutover
plan: 18
type: execute
gap_closure: true
wave: 2
depends_on: ["35-16", "35-17"]
files_modified:
  - docs/runbooks/telescope_runs_calendar.rst
  - docs/notebooks/pre_executed/load_telescope_runs_demo.ipynb
  - docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb
autonomous: true
requirements: [ALLOC-03, ALLOC-04, ALLOC-05]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "No operator-facing sentence states a parity guarantee the shipped code does not hold. This is the third round of this exact prohibition failing; the two surviving over-claims are the runbook's loader because-clause (lines 82-89) and the loader notebook's committed final print, which 35-VERIFICATION.md calls the strongest claim in the phase's operator-facing documentation and PROBE-P5 falsifies with the same command, the same site and the same typo'd timezone."
    - "The runbook's loader paragraph describes the two arms separately: the counter invariant holds on both passes, but the mechanism behind it -- a per-line preview reconcile -- exists only on the arm where a `CampaignRun` already exists. The honest sentence already present three paragraphs earlier (a first-time dry run predicts night counts from the window length) is extended to the run-level decision rather than contradicted."
    - "The loader notebook's committed output no longer asserts that the preview and the real run can never disagree. The cell demonstrates the existing-run arm and its printed conclusion is scoped to that arm, regenerated by re-execution rather than hand-patched -- so the sentence an operator reads is produced by the same run that produced the numbers above it."
    - "The runbook's two `duplicate_identity` DEFINITIONS -- the reason vocabulary the rest of the file calls 'the full reason vocabulary' (lines 933-937) and the troubleshooting Cause list (lines 1498-1500) -- state both causes, matching the label plan 35-17 writes, so D-18's reported reason means the same thing in the command's stderr and on the page the operator reads it against. An operator hitting the CR-01 branch no longer goes looking for a second schedule line that does not exist."
    - "The cutover's re-run gotcha appears in the runbook -- which is where D-15 puts the cutover's stated four-step sequence, so it is also where its caveats belong -- in the voice of the existing `import_campaign_csv` 'Re-import gotcha' note (lines 733-756), naming the fields re-applied from the schedule line and saying plainly that a post-import staff edit does not survive the next cutover run. Grounded in PROBE-P4: a matching-marker claimant whose `run_status` staff set to CANCELLED comes back as `planned` with exit 0 and `runs updated: 1`."
    - "`reconcile_campaign_runs_demo.ipynb` is regenerated against the two-cause reason label. Its committed output carries 12 occurrences of the superseded single-cause label text today, so the label change in plan 35-17 makes the notebook stale the moment it lands -- the paired-docs rule puts the regeneration in this round, not in a follow-up."
    - "Both notebooks are regenerated by re-execution: every code cell carries a non-null execution count, and the committed output is what the current code actually printed. No cell output is hand-edited."
    - "[edge probe ALLOC-03 `unclassified` -- RESOLVED, verification: explicit] The allocation/observation handoff is re-demonstrated end to end by the reconciler notebook's own re-execution, so its evidence is current for this round rather than carried on trust from the prior pass."
    - "[already covered by 35-15-PLAN.md must_haves] Every runbook sentence about the cutover's `duplicate_identity` guarantee still states the marker precondition, still names both remedies, and still uses no hedge word."
    - "`pre-commit run sphinx-build --all-files` still passes, so the corrected reStructuredText still builds and the runbook stays reachable from the toctree at `docs/index.rst:24`."
  artifacts:
    - docs/runbooks/telescope_runs_calendar.rst
    - docs/notebooks/pre_executed/load_telescope_runs_demo.ipynb
    - docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb
  key_links:
    - "`_REASON_LABELS[_DUPLICATE_IDENTITY]` (plan 35-17) -> the runbook's two reason definitions -> the reconciler notebook's committed stderr. The shared phrase `already holds the derived identity key` is the anchor all three are gated on, so a future drift in one shows up as a failing gate rather than as an operator hunting a nonexistent second line."
    - "The cutover's module-docstring gotcha (plan 35-17) -> the runbook's cutover section. Both carry the phrase `does not survive the next cutover run`, so the command and the operator page say the same thing in the same words."
    - "`load_telescope_runs.py`'s create-arm comment (plan 35-17) -> the runbook's loader paragraph -> the loader notebook's printed conclusion. Three copies of one claim; this plan narrows the two operator-facing ones to match the code."
  prohibitions:
    - "No notebook may be hand-patched to repair execution counts, output text or a conclusion -- a notebook is regenerated by re-execution or not at all."
    - "The notebook regeneration must NOT open `src/fomo_db.sqlite3` for writing -- work against a scratch copy, as plans 35-10, 35-11, 35-14 and 35-15 did."
    - "No file under `solsys_code/` may be changed by this plan. If a sentence cannot be made true by rewording, stop and report it rather than editing the code from here."
    - "No hedge (`usually`, `in normal operation`, `in most cases`) may be introduced anywhere in the runbook -- 35-VERIFICATION.md forbids it explicitly for this class of correction, and the file is gated at zero occurrences."
    - "The corrections already landed by plan 35-15 must NOT be undone: the cutover remedy paragraphs keep their marker precondition, both remedies and the `cannot prove` explanation."
    - "Dropped as canon security/compliance boilerplate rather than minted here: credential-logging, TLS-downgrade and authz-bypass prohibitions -- this plan writes documentation and re-executes two notebooks against a scratch database."
---

<objective>
Close the operator-facing half of Defect B and mirror plan 35-17's two text changes into the pages
an operator actually reads.

Four narrowings and one addition, all in documentation:

1. The runbook's loader paragraph attributes the counter invariant to a per-line preview reconcile
   the create arm never performs, and follows it with a sentence that is vacuous rather than true
   on that arm.
2. The loader notebook's committed output ends with a conclusion stating that the preview and the
   real run can never disagree -- printed from a fixture deliberately chosen to take the other arm.
   35-VERIFICATION.md is explicit that if the developer ships nothing else from this round, this is
   the minimum that must land first: shipping that sentence is worse than shipping the divergence,
   because it is the sentence that stops an operator noticing it.
3. and 4. The runbook's two `duplicate_identity` DEFINITIONS still describe only the
   group-versus-group collision, so they contradict the label plan 35-17 writes.
5. The cutover has no re-run gotcha, while `import_campaign_csv` -- the same class of behaviour --
   has a whole documented section of one.

Purpose: make every operator-facing sentence about this phase narrower than the code, not wider.

Output: five corrected or added runbook passages and two regenerated notebooks whose committed
output was produced by the code this round ships.

No `type="tracer"` task: this plan adds no code path. The slices it documents were proven by
35-05's, 35-12's and 35-16's tracers; its runnable gates are two notebook re-executions, which
drive those same paths against a real database, plus the Sphinx build.

Wave 2. Depends on 35-16 (the projector behaviour both notebooks re-execute) and 35-17 (the reason
label and the gotcha wording this plan mirrors, and the loader claim it scopes).
</objective>

<execution_context>
@/home/tlister/git/fomo_devel/.claude/gsd-core/workflows/execute-plan.md
@/home/tlister/git/fomo_devel/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md

@.planning/phases/35-allocation-layer-classical-cutover/35-VERIFICATION.md
@.planning/phases/35-allocation-layer-classical-cutover/35-REVIEW.md
@.planning/phases/35-allocation-layer-classical-cutover/35-11-SUMMARY.md
@.planning/phases/35-allocation-layer-classical-cutover/35-15-SUMMARY.md
@.planning/phases/35-allocation-layer-classical-cutover/35-16-SUMMARY.md
@.planning/phases/35-allocation-layer-classical-cutover/35-17-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Narrow the runbook's loader claim, define both duplicate_identity causes, add the cutover's re-run gotcha</name>
  <files>docs/runbooks/telescope_runs_calendar.rst</files>
  <read_first>
    - `docs/runbooks/telescope_runs_calendar.rst` lines 60-95 -- the `load_telescope_runs` summary-lines section: the honest sentence about a first-time dry run predicting night counts from the window length, and the paragraph plan 35-15 added whose because-clause this task corrects.
    - `docs/runbooks/telescope_runs_calendar.rst` lines 925-975 -- the cutover section's reason vocabulary (the `duplicate_identity` DEFINITION) and the operator-action paragraph plan 35-15 already corrected. The definition is what changes; the remedy paragraph must be left intact.
    - `docs/runbooks/telescope_runs_calendar.rst` lines 1488-1525 -- the troubleshooting Cause list (the second `duplicate_identity` definition) and the **Fix:** paragraph plan 35-15 already corrected.
    - `docs/runbooks/telescope_runs_calendar.rst` lines 733-790 -- the `import_campaign_csv` "Re-import gotcha" note, which is the voice, structure and admonition style the new cutover gotcha must match.
    - `solsys_code/management/commands/cutover_classical_allocations.py` as plan 35-17 leaves it -- the two-cause `_REASON_LABELS[_DUPLICATE_IDENTITY]` and the module docstring's re-run gotcha paragraph, so the runbook mirrors the shipped words rather than paraphrasing them.
    - `solsys_code/management/commands/load_telescope_runs.py` as plan 35-17 leaves it -- the scoped create-arm comment and the extended `--dry-run` help text.
    - `.planning/phases/35-allocation-layer-classical-cutover/35-VERIFICATION.md` gaps 3 and 4, and the `flagged_prohibitions` entry -- the three passages quoted verbatim and the `missing:` items for each.
  </read_first>
  <action>
    Five edits, all in `docs/runbooks/telescope_runs_calendar.rst`. State predicates; do not hedge.

    1. The loader summary-lines paragraph (lines 82-89). Keep the invariant itself -- the four run
       counters do sum to `lines processed` on both passes -- but replace the because-clause, which
       attributes it to a preview reconcile performed for every line. Describe the two arms
       separately: for a line whose `CampaignRun` already exists, the preview runs the same
       reconcile the real pass runs and folds its counter only after that call returns, so preview
       and real report the same decision; for a brand-new line there is no preview reconcile at
       all, so the preview reports `created` and cannot predict a reconcile failure that would make
       the real pass report `skipped`. Use the phrase `brand-new line` so the scoping is greppable.
       Then fix the neighbouring sentence, which currently says a line whose preview reconcile
       raises is reported under `skipped` alone on both passes -- scope it to the arm where a
       preview reconcile exists. Tie it back to the honest sentence three paragraphs earlier rather
       than contradicting it.

    2. The `duplicate_identity` DEFINITION in the reason vocabulary (lines 933-937). Add the second
       cause: either a second group (a second, distinct ``Source line:`` string) whose derived run
       identity key is the same as an earlier group's, because the key ignores the schedule line's
       status word; or a ``CampaignRun`` row that already holds the derived identity key whose own
       stored ``observation_details`` ``Source line:`` marker is absent or differs from the group's
       line, so the command cannot prove the run came from it. Use the phrase
       `already holds the derived identity key` verbatim.

    3. The troubleshooting Cause list's `duplicate_identity` definition (lines 1498-1500). Mirror
       the same two-cause sentence, using the same anchor phrase. Do not touch the **Fix:**
       paragraph below it -- plan 35-15 corrected it and its wording is gated.

    4. Add a cutover re-run gotcha, as a `.. note::` in the same shape as the
       `import_campaign_csv` "Re-import gotcha" note, placed in the cutover section near the
       operator-action paragraph. It must name the fields re-applied from the schedule line to a
       claimant whose stored marker matches -- ``run_status``, ``target``, ``campaign``,
       ``site``/``site_raw``, ``window_start``/``window_end``, the two sub-night fields and
       ``observation_details`` -- and say that a post-import staff edit to any of them
       `does not survive the next cutover run` (use that phrase verbatim; the command's own
       docstring carries it). Say it is expected behaviour for a file-authoritative command rather
       than a defect, cross-reference the `import_campaign_csv` gotcha above as the same class, and
       close with the interaction the remedy creates: restoring a missing marker to clear a
       ``duplicate_identity`` converts a refused claimant into a matching one whose fields will
       then be re-applied, so restore the marker deliberately. Use the heading text `Re-run
       gotcha` so it reads as the sibling of the existing note.

    5. Re-read the two remedy paragraphs plan 35-15 corrected (the operator-action paragraph around
       line 946 and the troubleshooting **Fix:** paragraph) and confirm they still branch on the
       marker, still name both remedies and still use `cannot prove`. Change nothing in them beyond
       what edit 4's cross-reference needs.

    Keep the file's reStructuredText conventions: double-backtick literals, the surrounding
    paragraphs' wrapping width, no new section label, and admonition style matching the existing
    notes.

    Change no file under `solsys_code/`.
  </action>
  <verify>
    <automated>test "$(grep -c 'per-line preview reconcile the real pass performs' docs/runbooks/telescope_runs_calendar.rst)" = "0"</automated>
    <fails_when>Exits 1 because the count is `1` -- the because-clause attributing the invariant to a preview reconcile on every line survives, which is the sentence PROBE-P5 falsifies for the create arm.</fails_when>
    <automated>test "$(grep -c 'brand-new line' docs/runbooks/telescope_runs_calendar.rst)" -ge "1"</automated>
    <fails_when>Exits 1 because the count is `0` -- the corrected paragraph does not name the arm that cannot be predicted, so the scoping is not stated where an operator reads it.</fails_when>
    <automated>test "$(grep -c 'already holds the derived identity key' docs/runbooks/telescope_runs_calendar.rst)" -ge "2"</automated>
    <fails_when>Exits 1 because the count is below `2` -- one or both `duplicate_identity` definitions still describe only the group-versus-group cause, contradicting the label the command now prints.</fails_when>
    <automated>test "$(grep -c 'does not survive the next cutover run' docs/runbooks/telescope_runs_calendar.rst)" -ge "1"</automated>
    <fails_when>Exits 1 because the count is `0` -- the cutover still has no re-run gotcha, so PROBE-P4's overwrite remains undocumented on the page the operator reads before running a one-time migration.</fails_when>
    <automated>test "$(grep -c 'Re-run gotcha' docs/runbooks/telescope_runs_calendar.rst)" -ge "1"</automated>
    <fails_when>Exits 1 because the count is `0` -- the caveat was written as loose prose rather than as the sibling note of the existing `Re-import gotcha`, so an operator scanning for gotchas will not find it.</fails_when>
    <automated>test "$(grep -ciE 'usually|in normal operation|in most cases' docs/runbooks/telescope_runs_calendar.rst)" = "0"</automated>
    <fails_when>Exits 1 because a hedge word was introduced instead of the predicate -- the same failure 35-VERIFICATION.md gated out in the previous round.</fails_when>
    <automated>test "$(grep -c 'cannot prove' docs/runbooks/telescope_runs_calendar.rst)" -ge "2"</automated>
    <fails_when>Exits 1 because the count dropped below `2` -- plan 35-15's corrected cutover remedy paragraphs were damaged while editing the definitions beside them.</fails_when>
    <automated>git status --short solsys_code</automated>
    <fails_when>Prints any line -- a file under `solsys_code/` was modified, which this plan forbids.</fails_when>
    <automated>pre-commit run sphinx-build --all-files</automated>
    <fails_when>Prints `Failed` -- the reStructuredText edits broke the docs build, most likely an unbalanced double-backtick literal or a mis-indented `.. note::` body.</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'per-line preview reconcile the real pass performs' docs/runbooks/telescope_runs_calendar.rst` prints `0` (it is `1` before this task).
    - `grep -c 'brand-new line' docs/runbooks/telescope_runs_calendar.rst` prints at least `1` (it is `0` before this task).
    - `grep -c 'already holds the derived identity key' docs/runbooks/telescope_runs_calendar.rst` prints at least `2` (it is `0` before this task).
    - `grep -c 'does not survive the next cutover run'` and `grep -c 'Re-run gotcha'` each print at least `1` (both are `0` before this task).
    - The hedge-word gate prints `0` and `grep -c 'cannot prove'` still prints at least `2`.
    - The loader paragraph describes the existing-run arm and the brand-new-line arm separately, and does not contradict the earlier window-length sentence.
    - The new note names every field re-applied from the schedule line and cross-references the `import_campaign_csv` gotcha.
    - `pre-commit run sphinx-build --all-files` prints `Passed` and `git status --short solsys_code` prints nothing.
  </acceptance_criteria>
  <done>Every sentence the runbook states about the loader's preview, the cutover's reason vocabulary and the cutover's field re-application is true of the code this round ships, in the same words the commands themselves print.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate the loader notebook with a conclusion it can actually support</name>
  <files>docs/notebooks/pre_executed/load_telescope_runs_demo.ipynb</files>
  <read_first>
    - `docs/notebooks/pre_executed/load_telescope_runs_demo.ipynb` markdown cell 17 and code cell 18 -- the parity section: the markdown's claim that the two passes must report the same four-counter tuple for every line, the cell's own comment explaining that its fixture deliberately takes the `existing is not None` arm, and the final `print` whose committed output is the claim 35-VERIFICATION.md calls the strongest in the phase's operator-facing documentation.
    - `.planning/phases/35-allocation-layer-classical-cutover/35-VERIFICATION.md` gap 3 -- the `artifacts` entry quoting the committed output verbatim and the `missing:` item instructing that the print be narrowed to the invariant the cell demonstrates and the notebook re-executed rather than hand-patched.
    - `.planning/phases/35-allocation-layer-classical-cutover/35-11-SUMMARY.md` and `35-14-SUMMARY.md` -- how this notebook's skip-path and parity sections were built and regenerated, including the scratch-database approach and the `America/Santigo` typo fixture the cell restores afterwards.
    - `solsys_code/tests/test_load_telescope_runs.py` as plan 35-17 leaves it -- the new create-arm test's docstring, so the notebook's narrowed wording and the test's wording agree.
  </read_first>
  <action>
    Edit the parity section's SOURCE (markdown cell 17 and the code cell's final `print`), then
    regenerate the whole notebook by re-execution.

    - Markdown cell 17: narrow the opening claim. It currently asserts that the preview and the
      real pass must report the same four-counter tuple for every line. Scope it to the arm the
      cell exercises: a line whose `CampaignRun` already exists, where the preview runs the same
      reconcile the real pass runs. Add one sentence stating what the cell does NOT show -- a
      brand-new line's preview does not call `reconcile_run()` at all, so it reports `created`
      where the real pass may report `skipped`, which is pinned by
      `test_dry_run_of_a_brand_new_line_cannot_predict_a_reconcile_failure` and documented in the
      runbook. Use the phrase `existing-run arm` so the scoping is greppable.
    - The code cell's final `print`: replace the conclusion with one that names the arm. Keep the
      printed tuple and the two assertions above it exactly as they are -- they are correct and
      they are what 35-14 restored. Only the sentence changes.
    - Leave the cell's existing in-cell comment (the one explaining that the deterministic
      `source_identifier` resolves to the pre-existing run so the line takes the
      `existing is not None` arm) in place; it is already honest and now matches the conclusion.
    - Do not add a cell that demonstrates the create-arm divergence by mutating the shared
      developer database further. The divergence is pinned by a test and stated in the runbook;
      this notebook's job here is to stop over-claiming, not to grow.

    Then regenerate: `jupyter nbconvert --to notebook --execute --inplace
    docs/notebooks/pre_executed/load_telescope_runs_demo.ipynb`, against a scratch copy of the
    developer database. Do not open `src/fomo_db.sqlite3` for writing. Do not hand-patch any output
    or execution count: if a cell fails, fix the cause and re-execute the whole notebook.
  </action>
  <precondition>`jupyter nbconvert` is available in the active environment -- it is a dev dependency installed by `./.setup_dev.sh` and was used by plans 35-10, 35-11, 35-14 and 35-15 for the same task; this plan does not install it.</precondition>
  <verify>
    <automated>test "$(grep -c 'the preview never disagrees with the real run' docs/notebooks/pre_executed/load_telescope_runs_demo.ipynb)" = "0"</automated>
    <fails_when>Exits 1 because the count is `2` (source plus committed output) -- the falsified conclusion survives in the notebook, which 35-VERIFICATION.md names as the minimum that must land from this round.</fails_when>
    <automated>test "$(grep -c 'existing-run arm' docs/notebooks/pre_executed/load_telescope_runs_demo.ipynb)" -ge "1"</automated>
    <fails_when>Exits 1 because the count is `0` -- the narrowed wording does not name the arm the cell demonstrates, so the claim is merely weaker rather than scoped.</fails_when>
    <automated>python3 -c "import json,sys; nb=json.load(open('docs/notebooks/pre_executed/load_telescope_runs_demo.ipynb')); c=[x for x in nb['cells'] if x['cell_type']=='code']; n=[i for i,x in enumerate(c) if x.get('execution_count') is None]; print('code cells:', len(c), 'null counts:', len(n)); sys.exit(1 if (n or len(c) < 16) else 0)"</automated>
    <fails_when>Exits 1 after printing a `null counts:` value above `0` (the notebook was hand-patched rather than executed) or a `code cells:` value below `16` (cells were dropped rather than re-executed).</fails_when>
    <automated>test "$(grep -c 'Both passes agree' docs/notebooks/pre_executed/load_telescope_runs_demo.ipynb)" -ge "1"</automated>
    <fails_when>Exits 1 because the count is `0` -- the demonstration itself was removed rather than narrowed, losing the coverage 35-14 added.</fails_when>
    <automated>git status --short src/fomo_db.sqlite3</automated>
    <fails_when>Prints a line beginning with ` M` or `M ` -- the developer database was written to, which the notebook regeneration must not do.</fails_when>
    <automated>git status --short solsys_code</automated>
    <fails_when>Prints any line -- a file under `solsys_code/` was modified, which this plan forbids.</fails_when>
    <automated>pre-commit run ruff-format --all-files</automated>
    <fails_when>Prints `Failed` with `- files were modified by this hook` -- notebook code-cell source was left unformatted after the edit.</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'the preview never disagrees with the real run' docs/notebooks/pre_executed/load_telescope_runs_demo.ipynb` prints `0` (it is `2` before this task).
    - `grep -c 'existing-run arm'` prints at least `1`.
    - Every code cell has a non-null `execution_count` and the notebook still has at least 16 code cells.
    - The cell's printed tuple and both of its assertions are unchanged; only the markdown claim and the final sentence differ.
    - The markdown cell names what the cell does not show and points at the create-arm test by name.
    - `src/fomo_db.sqlite3` is unmodified in `git status --short`, and nothing under `solsys_code/` changed.
    - `pre-commit run ruff-format --all-files` prints `Passed`.
  </acceptance_criteria>
  <done>The strongest operator-facing claim in the phase now says exactly what the cell beneath it proves, and the words were printed by a real run rather than typed into a committed output.</done>
</task>

<task type="auto">
  <name>Task 3: Regenerate the reconciler notebook against the two-cause reason label</name>
  <files>docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb</files>
  <read_first>
    - `docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb` -- the cells whose committed output carries the `duplicate_identity` reason text (12 occurrences of the superseded single-cause label today) and any markdown narrating that output.
    - `solsys_code/management/commands/cutover_classical_allocations.py` as plan 35-17 leaves it -- the two-cause label and the extended no-marker remedy string, which are the exact words the regenerated output must contain.
    - `.planning/phases/35-allocation-layer-classical-cutover/35-15-SUMMARY.md` -- how this notebook was regenerated in the previous round, including the scratch-database approach and the cell count to preserve.
    - `.planning/phases/35-allocation-layer-classical-cutover/35-VERIFICATION.md` -- the entries confirming this notebook currently has 18 code cells, 0 null execution counts and 5 occurrences of the corrected `CommandError` wording, so a regression in any of those is detectable.
  </read_first>
  <action>
    The label change in plan 35-17 makes this notebook's committed output stale: 12 occurrences of
    the superseded single-cause reason text are printed in its executed cells. Under CLAUDE.md's
    paired-docs rule that regeneration belongs in this round, not a follow-up.

    - Re-execute the notebook: `jupyter nbconvert --to notebook --execute --inplace
      docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb`, against a scratch copy of the
      developer database. Do not open `src/fomo_db.sqlite3` for writing.
    - Read every markdown cell that narrates a `duplicate_identity` outcome and check it against
      the regenerated output beneath it. Where a markdown cell paraphrases the old single-cause
      reason, update the prose to the two-cause form so no cell narrates an outcome that differs
      from the output below it. Prose edits are allowed; output edits are not.
    - If the re-execution changes the handoff cells' output at all -- plan 35-16 narrows the
      dry-run inversion guard, which this notebook's reconcile cells exercise -- keep the new
      output and check the narrating prose the same way. Do not restore the previous output.
    - Do not hand-patch any output or execution count. If a cell fails, fix the cause and
      re-execute the whole notebook.
  </action>
  <precondition>`jupyter nbconvert` is available in the active environment -- it is a dev dependency installed by `./.setup_dev.sh` and was used by plans 35-10, 35-11, 35-14 and 35-15 for the same task; this plan does not install it.</precondition>
  <verify>
    <automated>python3 -c "import json,sys; nb=json.load(open('docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb')); c=[x for x in nb['cells'] if x['cell_type']=='code']; n=[i for i,x in enumerate(c) if x.get('execution_count') is None]; print('code cells:', len(c), 'null counts:', len(n)); sys.exit(1 if (n or len(c) < 18) else 0)"</automated>
    <fails_when>Exits 1 after printing a `null counts:` value above `0` (the notebook was hand-patched rather than executed) or a `code cells:` value below `18` (cells were dropped rather than re-executed).</fails_when>
    <automated>test "$(grep -c 'already holds the derived identity key' docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb)" -ge "1"</automated>
    <fails_when>Exits 1 because the count is `0` -- the notebook's committed output does not carry the two-cause label, so it was not re-executed against the command plan 35-17 ships.</fails_when>
    <automated>test "$(grep -c 'duplicate_identity' docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb)" -ge "9"</automated>
    <fails_when>Exits 1 because the count dropped below `9` -- the `duplicate_identity` demonstration was lost in the regeneration rather than updated.</fails_when>
    <automated>git status --short src/fomo_db.sqlite3</automated>
    <fails_when>Prints a line beginning with ` M` or `M ` -- the developer database was written to, which the notebook regeneration must not do.</fails_when>
    <automated>git status --short solsys_code</automated>
    <fails_when>Prints any line -- a file under `solsys_code/` was modified, which this plan forbids.</fails_when>
    <automated>python manage.py test solsys_code.tests.test_allocation_projector solsys_code.tests.test_campaign_reconciler solsys_code.tests.test_cutover_classical_allocations solsys_code.tests.test_load_telescope_runs solsys_code.tests.test_observation_projector_signals</automated>
    <fails_when>Prints `FAILED (` -- this plan changes no code, so a failure here means a wave-1 plan regressed and this plan must not be committed on top of it.</fails_when>
    <automated>pre-commit run ruff-format --all-files</automated>
    <fails_when>Prints `Failed` with `- files were modified by this hook` -- notebook code-cell source was left unformatted.</fails_when>
  </verify>
  <acceptance_criteria>
    - Every code cell in `docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb` has a non-null `execution_count`, and the notebook still has at least 18 code cells.
    - `grep -c 'already holds the derived identity key'` prints at least `1` (it is `0` before this task).
    - `grep -c 'duplicate_identity'` prints at least `9`.
    - No markdown cell narrates an outcome that differs from the output of the code cell beneath it.
    - `src/fomo_db.sqlite3` is unmodified in `git status --short`, and nothing under `solsys_code/` changed.
    - The five-module test command prints `OK`, and `pre-commit run ruff-format --all-files` prints `Passed`.
  </acceptance_criteria>
  <done>The notebook an operator reads to understand the cutover prints the command's current two-cause reason text, produced by executing it, with every narrating sentence matching the output it sits above.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator reading the runbook -> the decision to run a one-time destructive migration and to trust a preview | The runbook is the control that tells a human whether a command is safe to repeat and whether a preview can be believed. A guarantee the code does not hold is a failed control, not a typo. |
| Committed notebook output -> operator expectation of command behaviour | A pre-executed notebook is read as evidence of what the command does. A conclusion printed from a fixture that takes one branch, stated as a property of every line, is evidence for something that was never run. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-40 | Repudiation | `load_telescope_runs_demo.ipynb` parity conclusion | high | mitigate | The committed sentence is the one that would stop an operator noticing a real preview/real divergence -- 35-VERIFICATION.md states shipping it is worse than shipping the divergence. Task 2 narrows it to the arm the cell demonstrates, names the untested arm and its pinning test, and regenerates by re-execution with a zero-occurrence gate on the falsified sentence. |
| T-35-41 | Repudiation | the runbook's loader because-clause | medium | mitigate | An invariant attributed to a mechanism that does not exist on one arm leaves an operator believing a preview can predict a failure it cannot. Task 1 describes the two arms separately and is gated on removing the false clause and naming the brand-new-line case. |
| T-35-42 | Tampering | undocumented field re-application on a matching-marker claimant | medium | mitigate | A post-import staff edit is reverted with exit 0 and no report beyond `runs updated: 1`. The behaviour is intended (see plan 35-17's threat register); the control that was missing is the operator-facing caveat. Task 1 adds it as the sibling of the existing `import_campaign_csv` gotcha and names the remedy interaction that routes an operator into it. |
| T-35-43 | Spoofing | the shared `duplicate_identity` phrase across code, runbook and notebook | low | mitigate | If the three copies drift, an operator matching a stderr line against the runbook finds a cause that is false for their case and improvises. All three are gated on the phrase `already holds the derived identity key`, and Task 3 re-executes the notebook so its committed output can only come from the shipped label. |
| T-35-44 | Information Disclosure | regenerated notebook output | low | accept | Both notebooks execute against a scratch copy of the developer database and print run pks, identity keys and schedule-line text -- the same class of content already committed in these and sibling notebooks, with no credential or secret. ASVS level 1 requires no further control for a docs artifact in a private repository. |
| T-35-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no package: no `pip install` task, no change to `pyproject.toml`, no new third-party import. `jupyter nbconvert` is an already-installed dev dependency (declared as a `<precondition>`, not installed here). The package-legitimacy gate does not fire. |
</threat_model>

<verification>
Run all of the following from the repository root before considering the plan complete:

1. `pre-commit run sphinx-build --all-files` -- expect `Passed`. This is the gate plans 35-10 and 35-15 used for the same file.
2. `python manage.py test solsys_code.tests.test_allocation_projector solsys_code.tests.test_campaign_reconciler solsys_code.tests.test_cutover_classical_allocations solsys_code.tests.test_load_telescope_runs solsys_code.tests.test_observation_projector_signals` -- expect `OK`. This plan changes no code, so a failure here means a wave-1 plan regressed and this one must not be committed on top of it.
3. `pre-commit run ruff --all-files` and `pre-commit run ruff-format --all-files` -- expect `Passed` on both (notebook code-cell source is in scope for the formatter).
4. `git status --short` -- expect no modified file beyond the three in `files_modified` and `.planning/`. In particular `src/fomo_db.sqlite3` and everything under `solsys_code/` must be clean.
</verification>

<success_criteria>
- No runbook sentence and no committed notebook output states a dry-run/real-run parity the loader's create arm does not have.
- The runbook's two `duplicate_identity` definitions state both causes, matching the label the command prints.
- The runbook carries a cutover re-run gotcha in the voice of the existing `import_campaign_csv` note, naming every re-applied field and the remedy interaction.
- Both notebooks carry no null execution count and their committed output was produced by the code this round ships.
- `pre-commit run sphinx-build --all-files`, `ruff`, `ruff-format` and the five-module test command all pass.
</success_criteria>

## Artifacts this phase produces

| Artifact | Kind | Where |
|----------|------|-------|
| Two-arm loader parity paragraph | corrected documentation prose | `docs/runbooks/telescope_runs_calendar.rst` (currently lines 82-89) |
| Two-cause `duplicate_identity` reason-vocabulary definition | corrected documentation prose | `docs/runbooks/telescope_runs_calendar.rst` (currently lines 933-937) |
| Two-cause `duplicate_identity` troubleshooting Cause entry | corrected documentation prose | `docs/runbooks/telescope_runs_calendar.rst` (currently lines 1498-1500) |
| Cutover "Re-run gotcha" note | new documentation admonition | `docs/runbooks/telescope_runs_calendar.rst`, cutover section |
| Loader notebook with an arm-scoped parity conclusion | regenerated pre-executed notebook | `docs/notebooks/pre_executed/load_telescope_runs_demo.ipynb` |
| Reconciler notebook regenerated against the two-cause label | regenerated pre-executed notebook | `docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb` |

No new module, class, test, migration, dependency or management command. No code file changed.

<output>
Create `.planning/phases/35-allocation-layer-classical-cutover/35-18-SUMMARY.md` when done.
</output>
