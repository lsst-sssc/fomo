---
phase: 35-allocation-layer-classical-cutover
plan: 08
type: execute
gap_closure: true
wave: 1
depends_on: []
files_modified:
  - solsys_code/management/commands/cutover_classical_allocations.py
  - solsys_code/tests/test_cutover_classical_allocations.py
autonomous: true
requirements: [ALLOC-04, ALLOC-05]

estimate:
  tokens: 78000
  raw_tokens: 78000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Running `cutover_classical_allocations` a SECOND time over the two-group status-only-collision fixture leaves the first group's `CampaignRun` at `run_status=PLANNED` with `observation_details` still starting `Status: allocation`, and reports the second group under `duplicate_identity` (never `key_collision`) with a non-zero exit (NF-19 case 1)."
    - "A `CampaignRun` that already holds the derived identity key -- from a prior cutover invocation or from `load_telescope_runs` -- but whose stored `Source line:` differs from the group's, causes that group to be reported under `duplicate_identity` and the command to exit non-zero on the FIRST pass, never a silent find-and-update (NF-19 case 2)."
    - "A `CampaignRun` that already holds the key and whose stored `Source line:` is the SAME line is still converted normally -- the guard does not over-reject the benign cutover-after-import ordering WR-11 documents."
    - "The `duplicate_identity` reason text names an action this command can carry out (editing the events' description `Source line:` in the Django admin) and never a schedule file this command does not read (NF-25)."
    - "`seen_keys[key]` is claimed only after the group is known to have something to write, so an unconvertible group no longer steals the identity key from a convertible sibling (IN-02)."
    - "`--dry-run` and the real pass still report identical summaries, reason breakdowns and exit status on every existing fixture (the NF-02 parity contract is preserved, not traded away)."
    - "No comment in `test_cutover_classical_allocations.py` cites an absolute line number as the location of an assertion (IN-01)."
    - "[flagged assumption -- edge probe ALLOC-01 `adjacency`, unresolved] Two groups whose derived identity keys are EXACTLY equal SEPARATE (first claimant converted, second reported) and never MERGE. This is the NF-19 blocker's core predicate; asserted by the twice-run test, not assumed."
    - "[flagged assumption -- edge probe ALLOC-01 `ordering`, unresolved] When two groups compare equal on the identity key, the winner is deterministic: within one invocation the earliest-pk group (`candidates.order_by('pk')` insertion order), and across invocations any pre-existing database claimant. Pinned by the twice-run test's assertion that the run's fields stay group A's."
    - "[flagged assumption -- edge probe ALLOC-01 `empty`, unresolved] A database claimant whose `observation_details` carries NO recoverable `Source line:` marker (`_extract_source_line()` returns None) is treated as the SAME line and allowed to proceed -- the permissive predicate 35-REVIEW.md NF-19 prescribes. Written into the guard's own comment, never silently assumed."
    - "[flagged assumption -- edge probe ALLOC-05 `unclassified`] The cutover's stated sequencing is safe to repeat in the sense its own `CommandError` claims: a re-run after an unresolved `duplicate_identity` changes no `CampaignRun` field and no `CalendarEvent`."
    - "[already covered by 35-06-PLAN.md must_haves] The command still never deletes a `CalendarEvent` on any path and still exits non-zero on any unexplained event."
  artifacts:
    - solsys_code/management/commands/cutover_classical_allocations.py
    - solsys_code/tests/test_cutover_classical_allocations.py
  key_links:
    - "`cutover_classical_allocations` group loop -> `CampaignRun.objects.filter(source_identifier=key)` -- the identity guard now reads the DATABASE, the same place `insert_or_create_campaign_run()` matches against."
    - "`_extract_source_line()` -> `CampaignRun.observation_details` -- the existing `Source line:` parser, reused against a run's stored details with no new parsing code."
    - "`seen_keys` -> the group's `transaction.atomic()` block -- the in-process claim now happens only after the group is known convertible (IN-02)."
  prohibitions:
    - "The cutover must NOT silently mutate a `CampaignRun` it did not create in this invocation. (Widened per 35-VERIFICATION.md's Prohibitions note: the Phase 35 prohibition only reached events; NF-19 showed a foreign RUN row being rewritten.)"
    - "The cutover must NOT delete a `CalendarEvent` on any path, including every failure path."
    - "An operator-facing remedy string must NOT name an artifact this command never reads (NF-25)."
    - "A `--dry-run` must NOT exit 0 over a fixture the immediately following real run rejects."
    - "Dropped as canon security/compliance boilerplate rather than minted here: credential-logging, TLS-downgrade and authz-bypass prohibitions -- this plan touches a staff-only management command with no network, no secret and no request path."
---

<objective>
Close the Phase 35 BLOCKER: `cutover_classical_allocations`'s identity-key guard is
invocation-scoped while the collision it protects against is database-scoped, so the re-run
the command's own `CommandError` prescribes silently flips a pre-existing APPROVED classical
run's `run_status` to `cancelled` and rewrites its `observation_details` -- which the next
reconciler sweep then renders as `[CANCELLED] NTT EFOSC2` on every one of that run's nights.
Also close the two findings that sit in the same twenty lines: the un-actionable
`duplicate_identity` remedy text (NF-25) and the premature `seen_keys` claim (IN-02), plus
the line-number-citing test comment (IN-01).

Purpose: this is the one finding 35-VERIFICATION.md reproduced with its own executed probe,
and the reason the phase reports `gaps_found` despite 278 green tests, a passing UAT and a
clean security audit. Every collision test in the suite is in-process; none constructs a
`CampaignRun` that already holds the identity key.

Output: a database-scoped identity guard, an actionable remedy string, and three new tests
that fail against today's code.

**Paired docs (CLAUDE.md rule), scoped up front, not as a follow-up:** this plan changes
`cutover_classical_allocations.py`'s behavior and its printed reason text. CLAUDE.md pairs
that module with `docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb`, whose
committed output prints the `duplicate_identity` reason verbatim, and the operator page
`docs/runbooks/telescope_runs_calendar.rst` documents the guarantee this plan makes true.
Both are owned by **35-10-PLAN.md Tasks 1 and 2**, which `depends_on` this plan -- they are
regenerated once, after 35-09's projector fixes also land, rather than twice. They are not
in this plan's `files_modified` because this plan does not edit them; they are named here so
no reader has to reconstruct the pairing.
</objective>

<execution_context>
@/home/tlister/git/fomo_devel/.claude/gsd-core/workflows/execute-plan.md
@/home/tlister/git/fomo_devel/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-allocation-layer-classical-cutover/35-CONTEXT.md
@.planning/phases/35-allocation-layer-classical-cutover/35-REVIEW.md
@.planning/phases/35-allocation-layer-classical-cutover/35-VERIFICATION.md
@solsys_code/management/commands/cutover_classical_allocations.py
@solsys_code/tests/test_cutover_classical_allocations.py
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Make the cutover's identity-key guard read the database, end to end</name>
  <files>solsys_code/management/commands/cutover_classical_allocations.py, solsys_code/tests/test_cutover_classical_allocations.py</files>
  <read_first>
    - `solsys_code/management/commands/cutover_classical_allocations.py` L334-345 (the `seen_keys` declaration and its NF-14 comment), L378-395 (the in-run guard), L410-414, L480-495 (the `transaction.atomic()` block whose `insert_or_create_campaign_run()` at L487 matches against the database), L192-210 (`_extract_source_line()`), L128-170 (the reason vocabulary and `_REASON_LABELS`).
    - `35-REVIEW.md` L130-221 -- finding NF-19 in full, including the reproduced probe transcripts at L149-179 and the prescribed predicate at L191-218.
    - `35-VERIFICATION.md` L98-99 and L233 -- the verifier's independent reproduction and the exact fields it observed flipping.
    - `solsys_code/tests/test_cutover_classical_allocations.py` L1002-1095 (`TestDuplicateIdentityKeyAcrossGroups`, including `_make_two_groups_same_identity_key_fixture()` and `_parse_cutover_summary()` usage) and L43-114 (`CutoverClassicalAllocationsTestBase`, `_make_legacy_event()`).
  </read_first>
  <behavior>
    - Test 1 (NF-19 case 1, the prescribed re-run): build the two-group status-only-collision fixture, invoke the command, then invoke it a SECOND time. After pass 2 the first group's `CampaignRun` still has `run_status == CampaignRun.RunStatus.PLANNED` and `observation_details` still contains `Status: allocation`; the second pass raises `CommandError`; the reported reason is `duplicate_identity`, not `key_collision`; the first group's three `ALLOC:` events keep their titles and urls.
    - Test 2 (NF-19 case 2, a database claimant on the FIRST pass): create a `CampaignRun` whose `source_identifier` is the key the three-night fixture line derives and whose `observation_details` carries a DIFFERENT `Source line:`; run the command once. It raises `CommandError` (non-zero exit), the pre-existing run's `run_status` and `observation_details` are unchanged, and the blank-url events keep `url == ''` with no `CalendarEventMeta` row.
    - Test 3 (the benign path the guard must not break): create a `CampaignRun` holding the same key whose stored `Source line:` is the SAME line as the group's. The command converts the group normally -- the run is updated, the events are re-keyed to `ALLOC:{run.pk}:{night}`, and no `duplicate_identity` is reported. This is the cutover-after-import ordering WR-11 pins.
    - Test 4 (edge probe ALLOC-01 `empty`): a database claimant whose `observation_details` has no `Source line:` marker at all is treated as the same line -- the group converts, nothing is reported.
  </behavior>
  <action>
Write the four tests FIRST, in a new class `TestDatabaseScopedIdentityGuard(CutoverClassicalAllocationsTestBase)` appended to `solsys_code/tests/test_cutover_classical_allocations.py`, and confirm they fail against today's code (tests 1, 2 and 4 will fail; test 3 passes today and is the regression guard). Reuse `TestDuplicateIdentityKeyAcrossGroups._make_two_groups_same_identity_key_fixture()` by lifting it onto the shared base class or by calling the same `_make_legacy_event()` helper -- do not copy the fixture body twice. Any `Target` fixture uses `NonSiderealTargetFactory` (CLAUDE.md).

Then fix `cutover_classical_allocations.py`. Immediately after the existing `if key in seen_keys:` branch ends (the `continue` at L394) and before the key is claimed, add a second guard that looks the key up in the database: query `CampaignRun.objects.filter(source_identifier=key).first()`, and when a row comes back, recover its own stored schedule line with the module's existing `_extract_source_line(existing_run.observation_details)`. Treat it as a collision only when that recovered line is neither `None` nor equal to this group's `source_line` -- the permissive predicate 35-REVIEW.md NF-19 prescribes, so a re-import of the same line and a run with no recoverable marker both still convert. On a collision, call `_mark_unexplained(events, _DUPLICATE_IDENTITY, ...)` with a message that keeps the substring `already claimed` (an existing assertion in `TestDuplicateIdentityKeyAcrossGroups` depends on it), additionally names `CampaignRun pk={existing_run.pk}`, and ends with the shared remedy phrase Task 2 introduces, then `continue`.

Write the guard's own comment to state three things a future reader needs: that the harm is a find-or-update against the database, so the guard has to look there too; that the claimant is recoverable from the run's own stored `Source line:` with no new parsing; and that a `None` recovered line is deliberately permissive (the ALLOC-01 `empty` edge), not an oversight.

Do not disturb the NF-02 dry/real parity contract: this check is a pure database read performed identically on both passes, placed in the same loop position on both, so `TestDryRunAndRealRunAgree` and `test_dry_run_and_real_run_agree_on_duplicate_identity` must both still pass unchanged.
  </action>
  <verify>
    <automated>python manage.py test solsys_code.tests.test_cutover_classical_allocations.TestDatabaseScopedIdentityGuard</automated>
    <fails_when>non-zero exit, `FAILED` or `ERROR` in the output, or the line `Ran 0 tests` (the new class was not collected)</fails_when>
  </verify>
  <verify>
    <automated>python manage.py test solsys_code.tests.test_cutover_classical_allocations</automated>
    <fails_when>non-zero exit, or `FAILED` / `ERROR` in the output -- in particular a regression in `TestDryRunAndRealRunAgree` or `TestSecondInvocationIsANoOp`</fails_when>
  </verify>
  <verify>
    <automated>grep -c '_extract_source_line(existing_run.observation_details)' solsys_code/management/commands/cutover_classical_allocations.py</automated>
    <fails_when>the command prints `0`, or exits 1 -- the database claimant's own stored schedule line is not being recovered</fails_when>
  </verify>
  <acceptance_criteria>
    - `TestDatabaseScopedIdentityGuard` exists with four tests and all four pass.
    - After a second invocation over the two-group fixture, the first group's run's `run_status` and `observation_details` are byte-identical to their post-pass-1 values (asserted, not inspected).
    - A pre-existing database claimant with a different `Source line:` produces a non-zero exit on the FIRST pass.
    - A pre-existing claimant with the same `Source line:`, and one with no recoverable marker, both still convert.
    - `TestDryRunAndRealRunAgree` and `TestDuplicateIdentityKeyAcrossGroups` still pass unmodified in their assertions about summary parity.
  </acceptance_criteria>
  <reversibility rating="reversible">A pure guard addition inside one management command; revertable by `git revert` with no persisted data model change.</reversibility>
  <done>Running the cutover twice over colliding groups, or once against a database that already holds the key for a different line, reports `duplicate_identity`, exits non-zero, and changes no `CampaignRun` field.</done>
</task>

<task type="auto">
  <name>Task 2: Make the duplicate_identity remedy actionable and claim the key only when the group is convertible</name>
  <files>solsys_code/management/commands/cutover_classical_allocations.py</files>
  <read_first>
    - `solsys_code/management/commands/cutover_classical_allocations.py` L387-395 (the `_DUPLICATE_IDENTITY` reason f-string and the `seen_keys` claim), L397-460 (the campaign-mismatch check, the `_CLASSICAL_RUN_STATUS` lookup, the foreign-attribution partition and the WR-07 `if not unattributed_events: continue`), L44-50 (the module docstring's `duplicate_identity` sentence), L617-623 (the `CommandError` text).
    - `35-REVIEW.md` L463-498 (NF-25 in full, including the notebook output at L482-487 showing the advice failing on lines that already carry a token) and L513-523 (IN-02 in full, including the prescribed placement).
  </read_first>
  <action>
NF-25 -- the reason string. `cutover_classical_allocations` reads no schedule file: every fact it uses is re-parsed from each stranded event's own `description`. Editing a schedule file therefore changes nothing this command will ever read, so the remedy clause at the tail of the `_DUPLICATE_IDENTITY` f-string (L391-392) is un-actionable. Replace that clause with one naming what an operator can actually do here: edit the affected group's events' description `Source line:` text in the Django admin so it carries a different bracketed proposal token from the earlier group's, then re-run. Use the exact phrase `disambiguate the two groups in the Django admin` inside that message, on a single physical source line (it is 47 characters; the file's limit is 120 columns), so the runbook, the notebook and this string all share one searchable operator vocabulary. Keep the substring `already claimed` -- an existing test asserts on it. Apply the same replacement to the database-claimant message Task 1 added.

Correct the module docstring's `duplicate_identity` sentence (L44-50) the same way: its "never silently merged into the earlier group's run" claim becomes TRUE once Task 1's guard is in place, so keep the claim but state WHY it now holds on every invocation (the guard reads the database, not just this process), and replace its schedule-file remedy with the admin remedy.

Correct the `CommandError` text at L619-622. Its "it is safe to repeat" promise is now genuinely true -- Task 1's guard is what makes it so. Keep the promise and add the one clause that makes it verifiable: a repeat pass converts nothing it has not already explained and rewrites no existing `CampaignRun`.

IN-02 -- the claim ordering. `seen_keys[key] = source_line` currently executes at L395, ahead of the campaign-mismatch check, the `_CLASSICAL_RUN_STATUS` lookup and the all-events-foreign `continue`. A group that is itself unconvertible therefore permanently claims the key and causes a convertible sibling to be reported under `duplicate_identity`, naming a line that converted nothing. Move that single assignment down to sit immediately before the group's `try:` / `with transaction.atomic():` block -- that is, after the `if not unattributed_events: continue` guard, at the point the group is known to have something to write. Leave `claimed_by_key` where it is: it is keyed on `key` and serves the per-night collision check, not the group-level claim.

Do not touch the dry/real parity ordering: the database read Task 1 added and the `seen_keys` check both still run in the same loop position on both passes.
  </action>
  <verify>
    <automated>! grep -q 'proposal token to one of the two lines' solsys_code/management/commands/cutover_classical_allocations.py</automated>
    <fails_when>exit 1 -- the schedule-file remedy phrase is still present somewhere in the command module</fails_when>
  </verify>
  <verify>
    <automated>test $(grep -cF 'disambiguate the two groups in the Django admin' solsys_code/management/commands/cutover_classical_allocations.py) -ge 2</automated>
    <fails_when>exit 1 -- fewer than two occurrences, i.e. the shared remedy phrase is missing from the in-run branch, the database-claimant branch, or both</fails_when>
  </verify>
  <verify>
    <automated>python -c "L=open('solsys_code/management/commands/cutover_classical_allocations.py').read().splitlines(); a=next(i for i,l in enumerate(L) if 'seen_keys[key] = source_line' in l); b=next(i for i,l in enumerate(L) if 'if not unattributed_events:' in l); assert a > b, f'IN-02 open: claim at {a+1} still precedes the convertibility gate at {b+1}'; print('IN-02 closed:', a+1, '>', b+1)"</automated>
    <fails_when>non-zero exit, or the output contains `IN-02 open:` -- the key is still claimed before the group is known convertible</fails_when>
  </verify>
  <verify>
    <automated>python manage.py test solsys_code.tests.test_cutover_classical_allocations</automated>
    <fails_when>non-zero exit, or `FAILED` / `ERROR` in the output</fails_when>
  </verify>
  <acceptance_criteria>
    - No occurrence of the schedule-file remedy phrase survives anywhere in `cutover_classical_allocations.py` (reason string or docstring).
    - The shared phrase `disambiguate the two groups in the Django admin` appears in both the in-run and database-claimant `duplicate_identity` messages.
    - `seen_keys[key] = source_line` executes on a later source line than the `if not unattributed_events:` gate.
    - `TestUnknownClassicalStatusGuard`, `TestCampaignMismatchGroupLeftUntouched` and `TestAllForeignAttributedGroupWritesNothing` still pass -- moving the claim did not change which groups those tests see rejected.
  </acceptance_criteria>
  <reversibility rating="reversible">Message text and one statement's position; no schema, no persisted key form change.</reversibility>
  <done>The `duplicate_identity` reason names an admin action this command can act on, the docstring and `CommandError` describe a guarantee the code now holds, and an unconvertible group no longer steals the identity key.</done>
</task>

<task type="auto">
  <name>Task 3: Drop the line-number-citing test comment and re-green the full suite</name>
  <files>solsys_code/tests/test_cutover_classical_allocations.py</files>
  <read_first>
    - `solsys_code/tests/test_cutover_classical_allocations.py` L505-531 -- the two assertions the comment refers to (`rekeyed_event.url` and `delete_legacy_pk`) and the NF-11 replacement comment itself.
    - `35-REVIEW.md` L504-511 -- finding IN-01 and its prescribed fix (cite the assertion by identifier alone).
    - Project memory `project_fomo_test_suite_gotchas.md`: always invoke `python manage.py` (never `./manage.py`), and exclude `solsys_code.tests.test_views.TestEphemeris` from full-suite runs -- it segfaults under native ASSIST.
  </read_first>
  <action>
Rewrite the NF-11 replacement comment at L527-531 so it cites each assertion by the identifier that already makes it unambiguous within this test -- `rekeyed_event.url` and `delete_legacy_pk` -- and remove both absolute line-number citations, which the next edit above them invalidates silently. Keep the comment's substance (why the vacuous `1 + 1 == 2` assertion was removed rather than replaced).

Then run the full Django suite and the lint gates to prove Tasks 1 and 2 broke nothing outside the cutover module. Use the repo's established full-suite invocation, which excludes the segfaulting `test_views` module, and the pinned pre-commit ruff gates (D-07: an unpinned `ruff` on PATH reports findings the enforced gate does not have).
  </action>
  <verify>
    <automated>! grep -nE 'asserted above at line [0-9]+' solsys_code/tests/test_cutover_classical_allocations.py</automated>
    <fails_when>exit 1 -- an absolute line-number citation still survives in the test module</fails_when>
  </verify>
  <verify>
    <automated>LABELS=$(ls solsys_code/tests/test_*.py solsys_code/solsys_code_observatory/tests/test_*.py | grep -v "tests/test_views\.py$" | sed "s|/|.|g; s|\.py$||" | tr "\n" " "); python manage.py test $LABELS</automated>
    <fails_when>non-zero exit, or `FAILED` / `ERROR` in the output</fails_when>
  </verify>
  <verify>
    <automated>pre-commit run ruff --all-files && pre-commit run ruff-format --all-files</automated>
    <fails_when>non-zero exit, or either hook reporting `Failed`</fails_when>
  </verify>
  <acceptance_criteria>
    - No comment in `test_cutover_classical_allocations.py` locates an assertion by absolute line number.
    - The full Django suite (every `solsys_code` test module except `test_views`) is green.
    - Both pinned ruff hooks pass with no findings.
  </acceptance_criteria>
  <done>IN-01 is closed and the whole suite is green with the NF-19/NF-25/IN-02 changes in place.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator CLI -> `cutover_classical_allocations` | A staff operator's invocation mutates production calendar and run data; the command is one-time and runs against the live database. |
| stored `CalendarEvent.description` -> `parse_run_line()` | Admin-editable free text crosses into a strict parser; the whole cutover's facts are recovered from it. |
| stored `CampaignRun.observation_details` -> `_extract_source_line()` | New in this plan: a pre-existing run's admin-editable details text is read to decide whether a group may claim its identity key. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-19 | Tampering | `cutover_classical_allocations` group loop (`:387-395`, `:483-487`) | high | mitigate | Task 1's database-scoped guard: a group may not claim an identity key already held by a `CampaignRun` carrying a different stored `Source line:`, so an APPROVED classical run's `run_status` and `observation_details` can no longer be silently rewritten by a group that did not create it. Pinned by `TestDatabaseScopedIdentityGuard` tests 1 and 2. |
| T-35-20 | Repudiation | cutover unexplained-reason reporting (`:388-393`, `:613-623`) | medium | mitigate | Task 1's message names the colliding `CampaignRun pk`; Task 2 replaces the un-actionable remedy so the operator record of what was refused, why, and what to do is complete and carriable out (NF-25). |
| T-35-21 | Denial of Service | `_extract_source_line()` over operator-controlled `observation_details` | low | accept | Plain prefix scan over a stored `TextField`, no regex and no backtracking; input is bounded by the field and reachable only from a staff-authenticated admin write. One extra indexed-column query per group. |
| T-35-22 | Elevation of Privilege | management-command entry point | low | accept | `BaseCommand` has no HTTP surface; reaching it already requires shell access to the deployment host. Unchanged by this plan. |

**Package legitimacy:** this plan installs no npm/pip/cargo package and adds no dependency, so
no `T-35-SC` supply-chain row and no legitimacy checkpoint applies. Every import it uses
(`CampaignRun`, `_extract_source_line`) already exists in the module.
</threat_model>

<verification>
- `python manage.py test solsys_code.tests.test_cutover_classical_allocations` -- green, including the four new `TestDatabaseScopedIdentityGuard` tests.
- Full Django suite green (`test_views` excluded per project memory).
- `pre-commit run ruff --all-files` and `pre-commit run ruff-format --all-files` clean.
- No occurrence of the schedule-file remedy phrase in `cutover_classical_allocations.py`.
- `seen_keys[key] = source_line` sits below the `if not unattributed_events:` gate.
</verification>

<success_criteria>
NF-19 (BLOCKER), NF-25, IN-02 and IN-01 are closed in code with tests that fail against the
pre-fix tree, the `--dry-run`/real parity contract is preserved, and the full suite plus both
pinned lint gates are green.
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this phase produces (35-08)

| Symbol / artifact | File | Change |
|---|---|---|
| database-claimant branch in `Command.handle()`'s group loop | `solsys_code/management/commands/cutover_classical_allocations.py` | new -- queries `CampaignRun.objects.filter(source_identifier=key)` and rejects a foreign-`Source line:` claimant under `_DUPLICATE_IDENTITY` |
| `_DUPLICATE_IDENTITY` reason message (both branches) | same | changed -- remedy now names the Django admin edit, sharing the phrase `disambiguate the two groups in the Django admin` |
| module docstring `duplicate_identity` paragraph (L44-50) | same | changed -- states why the never-merged guarantee now holds on every invocation |
| `CommandError` text (L619-622) | same | changed -- the "safe to repeat" promise is qualified with what a repeat pass does and does not rewrite |
| `seen_keys[key] = source_line` placement | same | moved -- now claimed only after the group is known convertible (IN-02) |
| `TestDatabaseScopedIdentityGuard` | `solsys_code/tests/test_cutover_classical_allocations.py` | new class, 4 tests |
| NF-11 replacement comment (L527-531) | same | changed -- cites `rekeyed_event.url` / `delete_legacy_pk` by identifier, no absolute line numbers (IN-01) |
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/35-allocation-layer-classical-cutover/35-08-SUMMARY.md` when done
</output>
