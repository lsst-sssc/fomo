---
phase: 35-allocation-layer-classical-cutover
plan: 13
type: execute
gap_closure: true
wave: 1
depends_on: []
files_modified:
  - solsys_code/allocation_projector.py
  - solsys_code/tests/test_allocation_projector.py
  - solsys_code/campaign_reconciler.py
autonomous: true
requirements: [ALLOC-01, ALLOC-02]

estimate:
  tokens: 68000
  raw_tokens: 68000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`reconcile_run(run, dry_run=True)` over a HALF-NULL sub-night run whose one set boundary is inverted against its stored counterpart raises the SAME `ValueError` the immediately following `reconcile_run(run)` raises. Grounded in 35-REVIEW.md PROBE-D: La Silla, night 2026-07-09, `night_start_utc=23:00` with `night_end_utc=None` minted first (span 2026-07-09T23:00 -> 2026-07-10T11:29:46), the start then edited to `11:30` -- after that night's 11:29:46 sunrise. Today the preview returns `ReconcileResult(created=1, retired=1, ...)` with no error and the real run raises `Computed an inverted allocation-night span for run pk=1 night=2026-07-09`."
    - "`_raise_if_set_window_inverted()` short-circuits only when BOTH `night_start_utc` and `night_end_utc` are None -- the same convention `_span_needs_remint()` uses, which is the convention the helper's docstring already claims to mirror and today does not (`or` at line 348 against `and` at line 378, confirmed by direct read in 35-VERIFICATION.md's Behavioral Spot-Checks)."
    - "On the re-mint branch a missing boundary is supplied from the stored `existing.start_time` / `existing.end_time`, so the guard completes with no `sun_event()` call and D-13 is not breached: that stored boundary was minted from the same deterministic `sun_event()` for the same site and the same night."
    - "The CREATE-path half-null case is the only shape the guard still cannot check, and both the helper docstring and the create-branch comment say so explicitly rather than claiming full parity."
    - "A half-null twin of `test_dry_run_of_a_remint_inverted_window_also_raises` exists and fails against the pre-fix helper. A set/set-only regression suite is precisely what let this defect through three consecutive iterations (NF-10 -> NF-20 -> WR-01) with 215 tests green."
    - "Every copy of the `claimed_legacy_urls` contract in the codebase states the same current meaning, including the copy attached to `_stale_dated_events()` -- the function whose `stale_dated.exclude(url__in=claimed_legacy_urls)` IS the exclusion being described."
    - "That third copy enumerates all four outcomes (takeover re-key, retirement delete, block on either branch, human-confirmed decline) and states the exclusion is LOAD-BEARING in real mode for a blocked or declined url -- matching the two already-corrected copies at `campaign_reconciler.py:746-756` and `allocation_projector.py:576-595`."
    - "[edge probe ALLOC-02 `unclassified` -- RESOLVED, verification: explicit] The site-local night boundary's failure mode is an inverted sub-night span, surfaced identically on the dry-run and the real path for a set/set pair on BOTH `_mint_fields()` caller branches AND for a half-null pair on the re-mint branch. This RE-RESOLVES 35-09's flagged assumption for the same probe, which asserted the helper matched `_span_needs_remint()`'s null-field convention when it did not, and was reproduced as WR-01."
    - statement: "A brand-new half-null run (no stored `CalendarEvent` for that night) whose span would resolve inverted is NOT detectable on a dry-run preview: the null boundary genuinely requires `sun_event()`, which D-13 forbids on a preview. The real run still raises. This is the only shape for which the dry-run/real-run parity prohibition is knowingly not upheld, and it is stated in the helper's docstring and in the create-branch comment rather than silently skipped."
      verification: backstop
    - "[already covered by 35-01-PLAN.md must_haves] `sun_event()` is still called only when a night is being created or re-minted; extending the inversion guard's reach adds no astropy call on any path."
    - "[already covered by 35-01/35-06-PLAN.md must_haves] `ALLOC:` remains the allocation projector's namespace alone and `RUN:{pk}` remains the reconciler's; nothing here changes which module writes which url."
  artifacts:
    - solsys_code/allocation_projector.py
    - solsys_code/tests/test_allocation_projector.py
    - solsys_code/campaign_reconciler.py
  key_links:
    - "`_raise_if_set_window_inverted(run, night, existing)` <- the re-mint branch's `if dry_run:` short-circuit, and `_raise_if_set_window_inverted(run, night)` <- the create branch's. One shared guard, now with the stored-boundary fallback that makes the re-mint call actually complete for a half-null run. 35-VERIFICATION.md marks this link 'WIRED, INCOMPLETE'."
    - "The helper's null-field short-circuit -> `_span_needs_remint()`'s null-field short-circuit. The two now use the same convention, so no shape can reach the branch that calls the guard without the guard looking at it."
    - "`_stale_dated_events()`'s `claimed_legacy_urls` Arg docstring -> the `.exclude(url__in=claimed_legacy_urls)` call it describes. The documentation and the code that performs the exclusion finally agree."
  prohibitions:
    - "A `--dry-run` preview must NOT hide a condition that makes the immediately following real run raise -- including the half-null sub-night shape a `1130-EoN` or `BoN-0230` classical line produces. Third iteration of this exact prohibition failing."
    - "A helper docstring must NOT claim to mirror a convention it does not implement."
    - "The inversion guard must NOT call `sun_event()` on any dry-run path (D-13)."
    - "A contract described in more than one place must NOT be corrected in some copies and left stale in the copy attached to the code that performs it."
    - "The re-mint branch must NOT rewrite an existing night's stored `start_time`/`end_time` in place; D-13's delete-and-recreate accounting (retired + created, never updated) stays as it is."
    - "Dropped as canon security/compliance boilerplate rather than minted here: credential-logging and transport-security prohibitions -- these modules make no network call by contract (TRIG-02) and handle no secret."
---

<objective>
Close WR-01 and WR-03, the two halves plan 35-09 left behind.

WR-01: the shared inversion guard 35-09 built is wired to both `_mint_fields()` caller branches
exactly as promised, but its early return fires when EITHER sub-night field is null, while
`_span_needs_remint()` -- the function whose convention the guard's own docstring claims to
mirror -- short-circuits only when BOTH are null. A half-night classical line (`1130-EoN`,
`BoN-0230`) produces exactly one set field and one null one, so it routes into the re-mint
branch and then meets a guard that declines to look. Dry run clean, real run raises. This is the
third consecutive iteration of one sentence (NF-10 -> NF-20 -> WR-01).

WR-03: NF-17 corrected two of three copies of the `claimed_legacy_urls` contract. The third sits
on `_stale_dated_events()`, the function that actually performs the exclusion, and NF-22 (closed
in the same batch) widened the set again without touching it.

Purpose: make the guard's reach match the branch that calls it, using the boundary already
stored on `existing` so no astropy call is added and D-13 is not breached; and make the reader of
the function that does the work see the same contract as the reader of its caller.

Output: a reshaped `_raise_if_set_window_inverted()` with an `existing` parameter, both call
sites updated, the half-null regression test the suite never had, the two documentation sites
narrowed to name the one shape that genuinely cannot be previewed, and the third contract copy
replaced.

Wave 1, no dependencies. No file overlap with plans 35-12 or 35-14.
</objective>

<execution_context>
@/home/tlister/git/fomo_devel/.claude/gsd-core/workflows/execute-plan.md
@/home/tlister/git/fomo_devel/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md

@.planning/phases/35-allocation-layer-classical-cutover/35-VERIFICATION.md
@.planning/phases/35-allocation-layer-classical-cutover/35-REVIEW.md
@.planning/phases/35-allocation-layer-classical-cutover/35-09-SUMMARY.md

@solsys_code/allocation_projector.py
@solsys_code/tests/test_allocation_projector.py
@solsys_code/campaign_reconciler.py
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Make the shared inversion guard reach the half-null shape its caller routes into</name>
  <files>solsys_code/allocation_projector.py, solsys_code/tests/test_allocation_projector.py</files>
  <read_first>
    - `.planning/phases/35-allocation-layer-classical-cutover/35-REVIEW.md` lines 255-328 (section WR-01) -- PROBE-D's executed reproduction and the full suggested helper body, including the `existing: CalendarEvent | None = None` parameter.
    - `.planning/phases/35-allocation-layer-classical-cutover/35-VERIFICATION.md` the `gaps:` entry whose `truth` begins "`reconcile_run(run, dry_run=True)` raises the same `ValueError`" -- its `missing:` list is this task's checklist.
    - `solsys_code/allocation_projector.py` lines 286-318 (`_raise_if_inverted()`), 321-353 (`_raise_if_set_window_inverted()`) and 356-389 (`_span_needs_remint()`) -- the two conventions side by side.
    - `solsys_code/allocation_projector.py` lines 169-240 (`_night_span_utc()`, `_time_of_day_to_datetime()`) -- the astropy-free resolvers the guard is allowed to use.
    - `solsys_code/allocation_projector.py` lines 700-760 -- both `if dry_run:` short-circuits that call the guard, in the re-mint branch and the create branch.
    - `solsys_code/tests/test_allocation_projector.py` lines 1195-1222 (`test_dry_run_of_a_remint_inverted_window_also_raises`) plus that class's `_make_run()` helper and its default site -- the fixture shape to twin.
  </read_first>
  <behavior>
    - Both fields null: the guard returns without checking (unchanged -- the expected span is entirely sun-event derived).
    - Both fields set, inverted: the guard raises on dry run and on the real run, identically (unchanged).
    - One field set and inverted against the stored counterpart, on the RE-MINT branch: the guard raises on dry run with the same message the real run raises. This is the new behaviour.
    - One field set, on the CREATE branch (no stored counterpart): the guard returns without checking, and the real run still raises. Documented limitation, not a silent skip.
  </behavior>
  <action>
    In `solsys_code/allocation_projector.py`:

    - Change `_raise_if_set_window_inverted()`'s signature to
      `(run: CampaignRun, night, existing: CalendarEvent | None = None) -> None`.
    - Change the early return so it fires only when `run.night_start_utc` and
      `run.night_end_utc` are BOTH None -- the same short-circuit `_span_needs_remint()` uses.
    - Resolve each boundary independently. A set field resolves through
      `_time_of_day_to_datetime(field, night, night_span)` with
      `night_span = _night_span_utc(run, night)`, exactly as today. A null field takes
      `existing.start_time` (for the start) or `existing.end_time` (for the end) when `existing`
      is not None, and otherwise stays None.
    - After resolution, return without raising if either boundary is still None -- that is the
      create-path half-null case -- and otherwise call `_raise_if_inverted(run, night, start, end)`.
    - Pass `existing` from the re-mint branch's `if dry_run:` short-circuit call. Leave the create
      branch's call with two arguments; `existing` is None there by construction.
    - Add no `sun_event()` call and no astropy import on any path. The stored boundary on
      `existing` was minted from the same deterministic `sun_event()` for the same site and night,
      which is what makes this astropy-free and D-13-safe.

    In `solsys_code/tests/test_allocation_projector.py`:

    - Add `test_dry_run_of_a_half_null_remint_inverted_window_also_raises` to the same class as
      `test_dry_run_of_a_remint_inverted_window_also_raises`, built from PROBE-D's fixture:
      `night = date(2026, 7, 9)`, `window_start=window_end=night`,
      `night_start_utc=time(23, 0)`, `night_end_utc=None`; call `reconcile_run(run)` to mint the
      night; assert the `ALLOC:{run.pk}:{night.isoformat()}` event exists; then set
      `run.night_start_utc = time(11, 30)` and `run.save(update_fields=['night_start_utc'])`;
      then assert BOTH `reconcile_run(run, dry_run=True)` and `reconcile_run(run)` raise
      `ValueError` and that `str(dry_ctx.exception) == str(real_ctx.exception)`, matching the
      sibling test's closing assertions.
    - Before hardcoding `time(11, 30)`, confirm the site `_make_run()` defaults to is the Chilean
      one PROBE-D used (La Silla, sunrise 2026-07-10T11:29:46 for that night). If the class's
      default site is a different observatory, pass the Chilean site explicitly rather than
      keeping a time that may not resolve inverted for another site's sunrise.
    - The docstring must state that this is the half-null twin of the set/set case, that a
      `1130-EoN` / `BoN-0230` half-night classical line produces exactly this shape through
      `_window_token_to_time()`, and that the set/set-only coverage is why WR-01 survived the
      NF-20 fix.
  </action>
  <reversibility rating="reversible">The guard only ever raises; it writes nothing and deletes nothing. Widening its reach can at most turn a previously-silent preview into an error the real run was already going to raise. No schema change, no data migration, no change to which events are written.</reversibility>
  <verify>
    <automated>python manage.py test solsys_code.tests.test_allocation_projector</automated>
    <fails_when>Prints `FAILED (failures=` -- most likely the new half-null case's `assertRaises(ValueError)` around `reconcile_run(run, dry_run=True)` reporting no exception was raised, meaning the guard still declines to look at a half-null run.</fails_when>
    <automated>test "$(grep -c 'night_start_utc is None and run.night_end_utc is None' solsys_code/allocation_projector.py)" = "2"</automated>
    <fails_when>Exits 1 because the count is not `2` -- the guard and `_span_needs_remint()` do not both use the same both-null short-circuit.</fails_when>
    <automated>grep -c 'test_dry_run_of_a_half_null_remint_inverted_window_also_raises' solsys_code/tests/test_allocation_projector.py</automated>
    <fails_when>Prints `0` -- the half-null regression was never added.</fails_when>
  </verify>
  <acceptance_criteria>
    - `python manage.py test solsys_code.tests.test_allocation_projector` prints `OK`.
    - `_raise_if_set_window_inverted` accepts three parameters, the third defaulting to `None`.
    - The re-mint branch calls it with three arguments and the create branch with two.
    - `solsys_code/allocation_projector.py` contains exactly two occurrences of the both-null short-circuit expression.
    - The new test asserts `str(dry_ctx.exception) == str(real_ctx.exception)`, not merely that both raise.
    - No `sun_event(` call appears inside `_raise_if_set_window_inverted`.
  </acceptance_criteria>
  <done>A half-night classical line whose operator edit inverts its span is refused by the dry run with the identical message the real run raises -- the parity sentence that failed three iterations running now holds for every shape that has a stored counterpart.</done>
</task>

<task type="auto">
  <name>Task 2: Narrow the two parity claims to name the one shape that cannot be previewed</name>
  <files>solsys_code/allocation_projector.py</files>
  <read_first>
    - `solsys_code/allocation_projector.py` lines 321-353 -- the helper docstring paragraph justifying the early return, and its `Args:` block.
    - `solsys_code/allocation_projector.py` lines 739-758 -- the create-branch comment whose closing clause asserts the two passes cannot drift apart.
    - `.planning/phases/35-allocation-layer-classical-cutover/35-REVIEW.md` lines 316-328 -- the fix block's inline comment and its final paragraph on what the docstrings must say.
    - `.planning/phases/35-allocation-layer-classical-cutover/35-VERIFICATION.md` gap 3's fourth `missing:` item, and its second `artifacts` entry naming the create-branch comment.
  </read_first>
  <action>
    - Helper docstring: the paragraph that today says the helper returns immediately when either
      sub-night field is null, and calls that the same convention `_span_needs_remint()` uses.
      Replace it with what Task 1 actually implements: the helper returns immediately only when
      both fields are null; for a half-null run on the re-mint path the missing boundary is read
      from the stored `existing` event, which was minted from the same deterministic `sun_event()`
      for the same site and night, so no astropy call is added on any path; the one shape that
      stays unchecked is a half-null run on the CREATE path, which has no stored counterpart and
      would need the `sun_event()` call D-13 forbids on a preview. Add the new `existing` entry to
      the `Args:` block, stating that it is the already-identified allocation `CalendarEvent` when
      the caller is the re-mint branch and None on the create path.
    - Create-branch comment: its closing clause today asserts the two passes cannot drift apart on
      this check, with no null-field qualifier. Qualify it: they cannot drift apart for a set/set
      pair on either branch, nor for a half-null pair on the re-mint branch; a half-null pair on
      this create branch is the single remaining shape whose inversion only the real run can
      detect, and it is documented here rather than silently skipped. Cite WR-01 and 35-REVIEW.md
      alongside the existing NF-10/NF-20 citations.
    - Keep both passages within the project's 120-column limit and single-quote style so the
      pinned `ruff-format` hook does not reflow them.
  </action>
  <verify>
    <automated>test "$(grep -c 'half-null' solsys_code/allocation_projector.py)" -ge "2"</automated>
    <fails_when>Exits 1 because fewer than two lines name the half-null shape -- one of the helper docstring or the create-branch comment was left claiming full parity.</fails_when>
    <automated>pre-commit run ruff-format --files solsys_code/allocation_projector.py</automated>
    <fails_when>Prints `Failed` with `- files were modified by this hook`, meaning the rewritten prose broke the pinned formatter's line-length or quote rules.</fails_when>
  </verify>
  <acceptance_criteria>
    - `_raise_if_set_window_inverted`'s docstring documents the `existing` parameter in its `Args:` block.
    - `_raise_if_set_window_inverted`'s docstring states that the create-path half-null case is the shape that remains unchecked, and names D-13 as the reason.
    - The create-branch comment's parity claim is qualified by branch and by null-field shape rather than stated unconditionally, and cites `WR-01`.
    - `pre-commit run ruff-format --files solsys_code/allocation_projector.py` prints `Passed`.
  </acceptance_criteria>
  <done>The module's own documentation states exactly what the guard checks and exactly what it does not -- which is the thing that failed at NF-20, where the code and its docstring disagreed about a boolean operator.</done>
</task>

<task type="auto">
  <name>Task 3: Replace the third copy of the claimed_legacy_urls contract</name>
  <files>solsys_code/campaign_reconciler.py</files>
  <read_first>
    - `.planning/phases/35-allocation-layer-classical-cutover/35-REVIEW.md` lines 384-423 (section WR-03) -- the stale text quoted in full and the exact replacement wording.
    - `solsys_code/campaign_reconciler.py` lines 588-622 -- `_stale_dated_events()`'s `Args:` block and the `.exclude(url__in=claimed_legacy_urls)` call it describes.
    - `solsys_code/campaign_reconciler.py` lines 740-760 -- the first corrected sibling copy, for wording to match.
    - `solsys_code/allocation_projector.py` lines 576-595 -- the second corrected sibling copy.
  </read_first>
  <action>
    Replace the `claimed_legacy_urls:` entry in `_stale_dated_events()`'s `Args:` block with
    wording that matches the two already-corrected copies. It must state: the set holds every
    legacy `RUN:{pk}:{date}` url the allocation projector's own per-night loop already decided the
    fate of at all -- a takeover re-key, a retirement delete, a block on either branch, or a
    human-confirmed decline; excluded here so one decision is never reported twice; a no-op in
    real mode for a re-keyed or deleted url, which has already left the `RUN:` namespace, but
    load-bearing in real mode for a blocked or declined one, which by definition is not written
    and is still sitting in that namespace; empty for a container-dispatched run, which never
    takes over a legacy night at all. Cite NF-09, NF-17, NF-22 and WR-03.

    Two things the current text gets wrong and the replacement must fix: the parenthetical
    enumeration lists two of the four outcomes, and the closing clause frames the exclusion as
    preview-only bookkeeping, which is the exact claim NF-17 was filed to correct.

    Change no code in this task -- the `.exclude()` call, the `writable_dated` filter and the
    `foreign` count are all already correct.
  </action>
  <verify>
    <automated>test "$(grep -v '^ *#' solsys_code/campaign_reconciler.py | grep -c 'double-counts the SAME url')" = "0"</automated>
    <fails_when>Exits 1 because the inner count is not `0` -- the superseded preview-only rationale (the clause that spans lines 602-603 today) still appears in the module.</fails_when>
    <automated>python manage.py test solsys_code.tests.test_campaign_reconciler</automated>
    <fails_when>Prints `FAILED (` and exits non-zero -- a code line was changed when only the docstring should have been.</fails_when>
    <automated>test "$(grep -ci 'load-bearing' solsys_code/campaign_reconciler.py)" -ge "2"</automated>
    <fails_when>Exits 1 because fewer than two lines in this module say the exclusion is load-bearing (only the already-corrected copy at line 752 matches) -- the third copy was not brought into line with its sibling.</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -v '^ *#' solsys_code/campaign_reconciler.py | grep -c 'double-counts the SAME url'` prints `0`.
    - `_stale_dated_events()`'s `claimed_legacy_urls` description names all four outcomes: a takeover re-key, a retirement delete, a block, and a human-confirmed decline.
    - It states the exclusion is load-bearing in real mode for a blocked or declined url.
    - `git diff --stat solsys_code/campaign_reconciler.py` shows changes confined to the docstring line range; no executable line changed.
    - `python manage.py test solsys_code.tests.test_campaign_reconciler` prints `OK`.
  </acceptance_criteria>
  <done>A reader of the function that performs the `claimed_legacy_urls` exclusion sees the same contract as a reader of its caller and of the projector copy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Django admin staff user -> `CampaignRun.night_start_utc` / `night_end_utc` | Operator-editable sub-night window fields. The half-night edit PROBE-D reproduced (`1130-EoN`) crosses here and reaches the projector's minting path. |
| `Observatory.timezone` (unvalidated `CharField`) -> `ZoneInfo()` in `_night_span_utc()` | A hand-typed value reaches a stdlib lookup that raises on a typo. Already handled per-line by NF-21 in the loader; unchanged by this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-22 | Denial of Service | `project_allocation()` re-mint branch, real pass | medium | mitigate | An operator sub-night edit that inverts a half-null span passes the preview and then aborts the real sweep with an uncaught `ValueError`, so a routine edit can stall the batch with no warning. Task 1 surfaces the same error on the preview, which is where the operator is looking and where nothing has been written yet. |
| T-35-23 | Tampering | stored `CalendarEvent.start_time` / `end_time` on an existing allocation night | low | accept | The guard reads `existing.start_time`/`end_time` and never writes them; D-13's delete-and-recreate accounting is untouched. Reading a stored boundary the projector itself minted introduces no new write path and no new trust boundary. |
| T-35-24 | Repudiation | `_stale_dated_events()` documentation | low | accept | A stale docstring misleads a maintainer but changes no runtime behaviour and produces no audit gap. Task 3 corrects it; no further control is warranted at ASVS level 1. |
| T-35-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no package: no `pip install` task, no change to `pyproject.toml`, no new third-party import. The package-legitimacy gate does not fire. |
</threat_model>

<verification>
Run all of the following from the repository root before considering the plan complete:

1. `python manage.py test solsys_code.tests.test_allocation_projector solsys_code.tests.test_campaign_reconciler solsys_code.tests.test_cutover_classical_allocations solsys_code.tests.test_load_telescope_runs solsys_code.tests.test_observation_projector_signals` -- the 215-test regression suite the iteration-5 code review and the verifier both ran clean. Expect `OK` and a count one higher than before (Task 1 adds one test; Tasks 2 and 3 add none).
2. `pre-commit run ruff --all-files` -- expect `Passed`.
3. `pre-commit run ruff-format --all-files` -- expect `Passed`.
4. `git status --short` -- expect no unexpected modified file beyond the three in `files_modified` and `.planning/`.
</verification>

<success_criteria>
- Re-running 35-REVIEW.md's PROBE-D fixture (Task 1's regression is that fixture) makes the dry run raise the same `ValueError` the real run raises, instead of returning `ReconcileResult(created=1, retired=1, ...)` with no error.
- `solsys_code/allocation_projector.py` uses the same both-null short-circuit in the guard and in `_span_needs_remint()`.
- Both parity claims in the projector name the create-path half-null case as the one unpreviewable shape.
- All three copies of the `claimed_legacy_urls` contract state the same four outcomes and the same real-mode role.
- Both ruff gates stay clean (D-07).
</success_criteria>

## Artifacts this phase produces

| Artifact | Kind | Where |
|----------|------|-------|
| `_raise_if_set_window_inverted(run, night, existing=None)` | changed signature -- new third parameter on an existing private helper | `solsys_code/allocation_projector.py` |
| `test_dry_run_of_a_half_null_remint_inverted_window_also_raises` | new test method, in the class holding `test_dry_run_of_a_remint_inverted_window_also_raises` | `solsys_code/tests/test_allocation_projector.py` |
| Narrowed helper docstring + create-branch parity comment | documentation | `solsys_code/allocation_projector.py` |
| Corrected `claimed_legacy_urls` Arg description | documentation | `solsys_code/campaign_reconciler.py`, `_stale_dated_events()` |

No new module, class, migration, dependency or management command. No schema change. No new
public function -- the only signature change is on an existing private helper with a defaulted
third parameter, so no call site outside this module is affected.

<output>
Create `.planning/phases/35-allocation-layer-classical-cutover/35-13-SUMMARY.md` when done.
</output>
