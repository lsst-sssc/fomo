---
phase: 35-allocation-layer-classical-cutover
plan: 22
type: execute
wave: 3
depends_on: ["35-21"]
gap_closure: true
autonomous: true
requirements: [ALLOC-01, ALLOC-02, ALLOC-03, ALLOC-05]
files_modified:
  - docs/runbooks/telescope_runs_calendar.rst
  - docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The operator runbook's `retired` and `detach_declined` paragraphs describe what the sweep now actually does: `retired` gains the provenance-audit reason and its site-correction case, and `detach_declined` covers a re-mint the sweep declined as well as an attribution it declined to release -- including the operator remedy, since the current text's flat claim that there is nothing for an operator to do is no longer true for a declined re-mint (plan 35-20's consequence, discharged here)."
    - "`reconcile_campaign_runs_demo.ipynb` carries executed output for both of this round's behaviour changes -- a site correction re-minting an already-projected run's nights (plan 35-21's CR-02), and a re-mint declined on a human-confirmed night (plan 35-20's CR-01) -- regenerated by re-execution, never hand-edited (CLAUDE.md paired-docs rule; the map entry `allocation_projector.py -> reconcile_campaign_runs_demo.ipynb` was added by commit `11b14da`)."
    - "This plan is what makes the round complete. Plans 35-20 and 35-21 both change documented behaviour and both are forbidden from touching `docs/`; CLAUDE.md's paired-docs rule is discharged at ROUND granularity, in one pass against the final state of the tree, so the runbook paragraph is written once and the notebook is re-executed once rather than three times. A green 35-20 and a green 35-21 with this plan outstanding leave the operator documentation describing a sweep that no longer exists -- that is the residual this plan clears, and it is the reason the round is not reported complete before it runs."
    - "The developer database is never written. The notebook copies `src/fomo_db.sqlite3` to a scratch directory and migrates the copy; `git status --short -- src/fomo_db.sqlite3` prints nothing after the whole run."
    - "The phase's code surface is still green behind the documentation: the five-module regression, both ruff hooks and `makemigrations --check --dry-run` all pass at the END of the round, not merely at the end of each code plan -- so a documentation pass cannot be the step that quietly ships a broken tree."
    - "[flagged assumption -- edge probe ALLOC-05 `unclassified`] ALLOC-05's cutover sequencing is untouched in code by this whole round. The assumption stated rather than hidden: the first sweep after THIS release behaves like a one-time audit for every night carrying a pre-release provenance token, exactly as it already did for a night carrying no token, and that is now written into the runbook as a deploy note so an operator seeing a non-zero `would_retire` on nights they did not edit knows what it is. The cutover command itself, its four documented steps and its own tests are not modified."
  artifacts:
    - "docs/runbooks/telescope_runs_calendar.rst -- corrected `retired` and `detach_declined` paragraphs and a post-upgrade deploy note"
    - "docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb -- new executed cells for the site-correction re-mint and the declined re-mint, whole notebook regenerated by re-execution"
  key_links:
    - "the sweep's `retired` / `detach_declined` counters -> `docs/runbooks/telescope_runs_calendar.rst:1078-1140` -> the operator reading a post-upgrade sweep summary. A counter whose printed meaning is stale is the same silent-wrong-information defect class in a different medium."
    - "plan 35-20's `_remint_decline_reason()` decline -> the `detach_declined` counter -> the runbook paragraph that now states the operator's remedy. Without the last arrow the guard is a silent refusal, which is the defect it was built to fix wearing the fix's clothes."
    - "plan 35-21's version-prefix read predicate -> every pre-release-token night resolving once on the first post-upgrade sweep -> the runbook deploy note that tells the operator to `--dry-run` first. The one-time audit is expected behaviour only if it is written down somewhere before it happens."
  prohibitions:
    - statement: "Notebook JSON must NOT be hand-edited. `reconcile_campaign_runs_demo.ipynb` is regenerated by `jupyter nbconvert --to notebook --execute --inplace` only -- hand-patching output is precisely what 35-SECURITY.md's `T-35-17` forbids, and it was caught doing exactly that once before."
      status: flagged-unverified
      verification: backstop
    - statement: "No management command may be run against `src/fomo_db.sqlite3`. The notebook copies it to a scratch file and migrates the copy; the developer database stays read-only for the whole run."
      status: flagged-unverified
      verification: backstop
    - statement: "No source file may be modified. This plan is two documentation files; plans 35-20 and 35-21 own every code change in this round, and re-opening either from a docs plan is how rounds 2 and 3 acquired their regressions."
      status: flagged-unverified
      verification: backstop
---

<objective>
Discharge this gap-closure round's paired-docs obligation for BOTH of its behaviour changes, in one
pass against the final state of the tree.

Plan 35-20 (CR-01/CR-03) gives the re-mint branch a human-confirmation and staff-state guard, so a
night a person has confirmed is now DECLINED rather than destroyed and counted under
`detach_declined`. Plan 35-21 (CR-02) makes the provenance token carry the run's site, so a site
correction on an already-projected run re-mints instead of reading as "nothing changed" forever,
and makes every pre-release token read as unrecorded, so the first sweep after this release is a
one-time provenance audit across existing nights.

Both changes alter what `docs/runbooks/telescope_runs_calendar.rst` currently promises. Its
`retired` enumeration does not mention a site correction or a provenance audit; its
`detach_declined` paragraph defines the counter as attribution releases only and states flatly that
there is nothing for an operator to do about it -- which is no longer true for a declined re-mint,
where the night keeps a boundary the run no longer declares. And CLAUDE.md maps
`solsys_code/allocation_projector.py` to `docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb`
(line 135, commit `11b14da`), which carries no executed output for either change.

Purpose: a counter whose printed meaning is stale is the same wrong-information defect this round's
three criticals are about, in a different medium. An operator seeing `retired: 37` on the first
post-upgrade sweep should not have to hunt for a window shrink that never happened.

Output: three corrected runbook passages (the `retired` reasons, the `detach_declined` definition
and remedy, a post-upgrade deploy note) and a demo notebook with executed cells for both behaviour
changes, regenerated by re-execution.

**Scope is the paired docs only.** CR-01 and CR-03 are plan 35-20, wave 1; CR-02 is plan 35-21,
wave 2. No source file is touched here (prohibition 1). WR-01..WR-04 and IN-01/IN-03 are
user-deferred; the full deferral ledger for the round is `<review_dispositions>` in
`35-20-PLAN.md`, and nothing is dropped.

**This plan is what completes the round.** Until it lands, the operator runbook and the committed
notebook describe a sweep that no longer exists.

**Run test modules by explicit dotted path, never bare `python manage.py test`.** A bare run
collects `solsys_code/tests/test_views.py`, which imports `ephem_utils` and pays a ~1.6 GB SPICE
kernel download (CLAUDE.md "Heavy import side effect"), and `TestEphemeris` segfaults in native
ASSIST. Use `python manage.py`, never `./manage.py`.
</objective>

<execution_context>
@/home/tlister/git/fomo_devel/.claude/gsd-core/workflows/execute-plan.md
@/home/tlister/git/fomo_devel/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-allocation-layer-classical-cutover/35-CONTEXT.md
@.planning/phases/35-allocation-layer-classical-cutover/35-REVIEW.md
@.planning/phases/35-allocation-layer-classical-cutover/35-20-PLAN.md
@.planning/phases/35-allocation-layer-classical-cutover/35-21-PLAN.md
@docs/runbooks/telescope_runs_calendar.rst
</context>

<why_this_plan_runs_last>
This plan is wave 3 and `depends_on: ["35-21"]`, which transitively puts it after 35-20 as well.
That ordering is deliberate and is the whole reason the paired docs are a separate plan rather than
a task inside either code plan:

- **The prose must describe one final state, not two intermediate ones.** If 35-20 wrote the
  `detach_declined` paragraph, 35-21 would have to rewrite the `retired` enumeration it sits
  beside. Writing both passages once, after both behaviour changes are in the tree, is the only
  way the page is internally consistent on the first attempt.
- **The notebook must be re-executed once, not twice.** `jupyter nbconvert --execute` regenerates
  every cell's output. Re-executing it after 35-20 and again after 35-21 produces two commits of
  churn over the same file and two chances for a convergence assertion to disagree with itself.
- **The re-execution needs both changes present.** The declined-re-mint cell exercises 35-20's
  guard; the site-correction cell exercises 35-21's token. Neither can produce real output before
  its code lands.

The cost of the ordering, stated rather than hidden: between 35-20 committing and this plan
committing, the repository carries documentation that is stale with respect to its own code. That
window is the residual, it is bounded by this round, and it is why the round is not reported
complete before this plan executes.
</why_this_plan_runs_last>

<review_dispositions>
## Review Dispositions

The complete ledger for this gap-closure round -- every CR, WR and IN finding of 35-REVIEW.md
iteration 8, plus the iteration-7 `user_deferred:` items and the already-closed
`35-VERIFICATION.md` gap -- is in `35-20-PLAN.md`'s `<review_dispositions>` section. It is not
duplicated here so the two cannot drift apart.

One entry bears directly on this plan and is restated for the executor:

- **WR-03 stays deferred as a finding; its paired-docs obligation does not.** CLAUDE.md's
  paired-docs rule is binding regardless of WR-03's deferral, so this plan updates the runbook
  paragraphs whose documented behaviour THIS round changes and re-executes the demo notebook. If
  WR-03's "four reasons -> five" sentence is corrected as a side effect, that is fine; WR-03's
  other items -- notably the `rekeyed` paragraph's own caveat and the `legacy_deleted` paragraph --
  are explicitly not touched. WR-04's deploy-note half is partly served by this plan's deploy note,
  which the round owes anyway; the rest of WR-04 stays deferred.
</review_dispositions>

<prohibitions>
1. Do NOT modify any file under `solsys_code/`, `src/` or `prisma`-equivalent source trees. This
   plan is exactly two documentation files. Plans 35-20 and 35-21 own every code change in this
   round; re-opening either from a docs plan is how rounds 2 and 3 acquired their regressions. If a
   test or an assertion genuinely fails because the documentation work exposed a real code defect,
   STOP and report it as a Rule 1 deviation rather than fixing it here.
2. Do NOT hand-edit notebook JSON. `reconcile_campaign_runs_demo.ipynb` is regenerated by
   `jupyter nbconvert --to notebook --execute --inplace` only -- hand-patching output is precisely
   what 35-SECURITY.md's `T-35-17` forbids, and it was caught doing exactly that once before.
3. Do NOT run any management command against `src/fomo_db.sqlite3`. The notebook copies it to a
   scratch file and migrates the copy; the developer database stays read-only for the whole run.
4. Do NOT touch the `rekeyed` paragraph's own caveat, the `legacy_deleted` paragraph, or the
   cutover section's four documented steps. Those are WR-03's and WR-04's other items, which stay
   deferred (`<review_dispositions>`).
5. Do NOT add, rename or remove a `ReconcileResult` counter name in the runbook's vocabulary, and
   do NOT change any documented event `title` or `description` text. Both management commands'
   summary lines and both committed notebooks quote the current vocabulary; the paragraphs are
   WIDENED in meaning, never re-labelled.
6. Do NOT weaken a pre-existing notebook assertion into a print. If a convergence or before/after
   assertion legitimately changed its numbers because of this round's behaviour, fix that cell's
   EXPECTATION and record it in the SUMMARY as a documented deviation.
7. Runbook and planning-doc prose: plain English, per CLAUDE.md's Conventions section. Write
   "create or update" or "find-or-create" rather than the database-jargon verb CLAUDE.md names, and
   "model" / "data model" / "case" / "companion-row state" rather than the abstraction noun it
   names. Follow that rule as written there; do not reproduce either forbidden term here.
</prohibitions>

<tasks>

<task type="tracer">
  <name>Task 1: Make the runbook's counter vocabulary describe the sweep this round shipped</name>
  <files>docs/runbooks/telescope_runs_calendar.rst</files>
  <read_first>
    - `CLAUDE.md` -- the "Paired docs are part of the deliverable" rule in full: the directory-scoped runbook clause (any page under `docs/runbooks/` whose documented behaviour a change affects), the notebook map at line 135, and the breach history, which names the planner, the executor and the verifier as bound by it.
    - `.planning/phases/35-allocation-layer-classical-cutover/35-20-PLAN.md` -- the guard's behaviour and its `<design_rationale>` point 3, so the `detach_declined` paragraph describes the real rule and the real remedy rather than a guess. That plan ran in wave 1, so the behaviour is already in the tree; read the shipped `_remint_decline_reason()` if the prose and the code appear to disagree.
    - `.planning/phases/35-allocation-layer-classical-cutover/35-21-PLAN.md` -- the version-prefix read predicate and the one-time provenance audit it causes on the first post-upgrade sweep. That is what the deploy note describes.
    - `docs/runbooks/telescope_runs_calendar.rst:1068-1098` -- the counter list, the sample summary lines, the `retired` four-reason enumeration and the `rekeyed` paragraph.
    - `docs/runbooks/telescope_runs_calendar.rst:1119-1145` -- the `detached` and `detach_declined` paragraphs, including the sentence "There is nothing for an operator to do about a non-zero ``detach_declined``", which is no longer true for a declined re-mint.
  </read_first>
  <action>
Make three prose corrections in `docs/runbooks/telescope_runs_calendar.rst`, in plain English
(prohibition 7), and nothing else:

  1. The `retired` enumeration (currently "any of four reasons"). Broaden reason (2) so it covers a
     SITE correction as well as a sub-night window field change -- both change what the night's
     boundaries should be, and both now re-mint. Add the provenance-audit reason: a night minted
     before this release, or carried across by the re-key path, whose recorded mint inputs are
     absent or were recorded in a pre-release format, is resolved once against the computed sun
     event and re-minted when the stored boundary disagrees by more than one minute -- at most once
     per night, with the same night reporting `unchanged` on every sweep after it. Update the count
     word in the opening sentence to match the list you end up with.
  2. The `detach_declined` paragraph. It currently defines the counter as companion rows the sweep
     did not RELEASE. Widen it to cover the second thing it now counts: a re-mint the sweep
     declined to perform because the night carries a human confirmation or a real observation link,
     which a delete-and-re-create would destroy. Replace the flat claim that there is nothing for
     an operator to do -- that is still true for a declined attribution release, and no longer true
     for a declined re-mint, where the night keeps a boundary the run no longer declares. State the
     remedy in one sentence: clear the confirmation or the link on that night's companion row in
     the admin and re-run the sweep, or leave it and accept the stored boundary.
  3. A short post-upgrade deploy note, placed with the sweep's counter documentation. After
     upgrading, run `reconcile_campaign_runs --dry-run` first: a non-zero `would_retire` on nights
     nobody edited is the one-time provenance audit, because every night minted before this release
     carries absent or pre-release mint inputs and is re-resolved once. Say that the same nights
     report `unchanged` on every sweep afterwards.

Leave the `rekeyed` paragraph's own caveat, the `legacy_deleted` paragraph and the cutover
section's four steps exactly as they are (prohibition 4). Do not re-label any counter
(prohibition 5) -- the paragraphs gain meaning, not new names.
  </action>
  <verify>
    <automated>cd /home/tlister/git/fomo_devel && grep -c 'detach_declined' docs/runbooks/telescope_runs_calendar.rst</automated>
    <fails_when>prints a number smaller than the pre-edit count of 6, or exits non-zero (grep -c exits 1 when the count is zero) -- the `detach_declined` documentation was removed or re-labelled rather than widened.</fails_when>
    <automated>cd /home/tlister/git/fomo_devel && grep -ci 'dry-run' docs/runbooks/telescope_runs_calendar.rst</automated>
    <fails_when>prints `0`, or exits non-zero -- the post-upgrade deploy note tells the operator to preview first, so the page must still name the preview flag.</fails_when>
    <automated>cd /home/tlister/git/fomo_devel && git status --short -- solsys_code/ src/ docs/notebooks/</automated>
    <fails_when>the command prints any line at all -- this task is one documentation file; a source change or a notebook change here violates prohibitions 1 and 2, and a clean run prints nothing.</fails_when>
  </verify>
  <acceptance_criteria>
    - The `retired` enumeration names the provenance-audit reason and its reason (2) covers a site correction as well as a sub-night window change; the count word in the opening sentence matches the list.
    - The `detach_declined` paragraph covers a declined re-mint as well as a declined attribution release, and states the operator remedy instead of the flat "nothing to do" claim.
    - A post-upgrade deploy note tells the operator to preview with `--dry-run` first and explains a non-zero `would_retire` on nights nobody edited as the one-time provenance audit.
    - The `rekeyed` caveat, the `legacy_deleted` paragraph and the cutover section's four steps are byte-for-byte unchanged.
    - No counter was re-labelled; `grep -c 'detach_declined'` is at least its pre-edit count of 6.
    - `git status --short` over `solsys_code/`, `src/` and `docs/notebooks/` prints nothing.
  </acceptance_criteria>
  <done>An operator reading the sweep's counter documentation is told what this round's sweep actually does -- why a night they did not edit was retired, what a declined re-mint is, and what to do about one -- travelling end to end from the shipped guard and the shipped token to the page they read.</done>
  <reversibility rating="reversible">Prose-only edits to one reStructuredText page; revertable with a single `git checkout` of the file, with no schema, data or behaviour consequence.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Give the paired demo notebook real executed output for both behaviour changes</name>
  <files>docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb</files>
  <precondition>`jupyter` is on PATH (it resolves to the project venv's copy) and `src/fomo_db.sqlite3` exists -- the notebook's setup cell copies that file to a scratch directory, migrates the copy to head and refuses to run if the file is missing. Assert both before starting; halt and report if either is unmet rather than hand-editing the notebook.</precondition>
  <read_first>
    - `docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb` -- cell 2 (the scratch-database setup, which already migrates the copy to head), the sweep cells around the "The real sweep" and "Idempotency" headings, and the teardown cell at the end. New cells go before the teardown.
    - `.planning/phases/35-allocation-layer-classical-cutover/35-18-SUMMARY.md` -- the last round that regenerated this notebook, for the re-execution command and the convergence-assert gotchas it hit.
    - `.planning/phases/35-allocation-layer-classical-cutover/35-20-PLAN.md` -- the declined-re-mint behaviour the new decline cell exercises, including which companion-row fields decline and what counters a decline produces.
    - `.planning/phases/35-allocation-layer-classical-cutover/35-21-PLAN.md` -- the site-carrying token the new site-correction cell exercises, and the `max_length=64` widening the scratch copy's migration to head must include.
    - Your own Task 1 runbook text, so the notebook's markdown cells and the runbook agree on what the counters mean.
  </read_first>
  <action>
Add executed coverage for both of this round's behaviour changes, then regenerate the whole
notebook by re-execution. Place the new cells after the existing sweep/idempotency section and
before the scratch-database teardown cell:

  - A markdown cell plus a code cell for the site correction (plan 35-21's CR-02): take one of the
    notebook's already-projected per-night runs, print its current night boundaries, reassign its
    `site` to the other seeded site, save, run `reconcile_run()` again, and print the counters and
    the new boundaries alongside the new site's own `sun_event()` values so the reader can see they
    match. Assert the boundaries actually moved, so a future regression fails the notebook rather
    than printing a wrong number quietly.
  - A markdown cell plus a code cell for the declined re-mint (plan 35-20's CR-01): confirm one
    allocation night by setting `confirmed_by` on its companion row, change the run's sub-night
    window so the night would otherwise re-mint, run the sweep, and print the counters plus the
    night's unchanged primary key and boundaries. Assert the primary key and `confirmed_by`
    survived.
  - While in cell 2, correct the one stale comment that says the scratch copy "has not yet had
    migration 0018 applied" -- it is behind head generally, and the `migrate` call that follows
    brings it to whatever head is, which after plan 35-21 includes migration 0020. Prose only; do
    not change the setup logic.

Regenerate with `jupyter nbconvert --to notebook --execute --inplace
docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb`, run from the repository root
(nbconvert sets the kernel working directory to the notebook's own directory, which is what cell
2's `parents[2]` path resolution requires). Never hand-edit the JSON (prohibition 2). If a
pre-existing convergence or before/after assertion in an earlier cell fails because this round's
behaviour legitimately changed its numbers, fix that cell's expectation and say so in the SUMMARY
as a documented deviation -- do not weaken the assertion into a print (prohibition 6).

Then run the round's closing gates and record every result in the SUMMARY: the phase's five-module
regression, both lint hooks and the migration check. Every test module is named by explicit dotted
path; a bare `python manage.py test` collects `test_views.TestEphemeris`, which segfaults in native
ASSIST and pays the SPICE download. These gates run here because this is the last plan of the
round -- a documentation pass must not be the step that quietly ships a broken tree.
  </action>
  <verify>
    <automated>cd /home/tlister/git/fomo_devel && python -c "import json,sys; nb=json.load(open('docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb')); bad=[i for i,c in enumerate(nb['cells']) if c['cell_type']=='code' and c.get('execution_count') is None]; print('cells_without_execution_count:', bad); sys.exit(1 if bad else 0)"</automated>
    <fails_when>non-zero exit, or `cells_without_execution_count:` printing a non-empty list -- a null execution count means a cell was hand-edited or the re-execution did not complete, which is exactly what 35-SECURITY.md's T-35-17 forbids.</fails_when>
    <automated>cd /home/tlister/git/fomo_devel && git status --short -- src/fomo_db.sqlite3</automated>
    <fails_when>the command prints any line -- the developer database was written to, which prohibition 3 forbids; the notebook must operate on its scratch copy only.</fails_when>
    <automated>cd /home/tlister/git/fomo_devel && git status --short -- solsys_code/ src/</automated>
    <fails_when>the command prints any line at all -- this plan is two documentation files, so a source change violates prohibition 1 and a clean run prints nothing.</fails_when>
    <automated>cd /home/tlister/git/fomo_devel && python manage.py test solsys_code.tests.test_allocation_projector solsys_code.tests.test_allocation_projector_signals solsys_code.tests.test_cutover_classical_allocations solsys_code.tests.test_load_telescope_runs solsys_code.tests.test_campaign_reconciler -v 1</automated>
    <fails_when>non-zero exit, or "FAILED" / "ERROR" in the last summary line, or a reported total below the count plan 35-21 recorded in its own SUMMARY -- anything lower means a test was deleted rather than added to.</fails_when>
    <automated>cd /home/tlister/git/fomo_devel && pre-commit run ruff --all-files && pre-commit run ruff-format --all-files</automated>
    <fails_when>either hook prints "Failed" -- D-07's lint gate. Run it through pre-commit, which pins ruff 0.2.1; an unpinned `ruff` on PATH can report findings the enforced gate does not have.</fails_when>
    <automated>cd /home/tlister/git/fomo_devel && python manage.py makemigrations --check --dry-run</automated>
    <fails_when>non-zero exit, or the output naming a model change with no migration -- plan 35-21's migration 0020 does not cover its `max_length` change, or another model was touched.</fails_when>
  </verify>
  <acceptance_criteria>
    - `reconcile_campaign_runs_demo.ipynb` has a new site-correction cell and a new declined-re-mint cell, both with real executed output and real assertions, and every code cell in the file carries a non-null execution count.
    - Cell 2's stale migration comment is corrected, and its setup logic is unchanged.
    - `git status --short -- src/fomo_db.sqlite3` prints nothing, and `git status --short` over `solsys_code/` and `src/` prints nothing.
    - The full five-module run is green, with a total no lower than the count plan 35-21 recorded.
    - Both ruff hooks pass and `makemigrations --check --dry-run` reports no drift.
    - Any pre-existing assertion whose numbers legitimately changed had its EXPECTATION corrected, never weakened into a print, and is recorded in the SUMMARY as a documented deviation.
  </acceptance_criteria>
  <done>The demo notebook shows both of this round's behaviour changes running with real output against a real migrated database -- the paired-docs obligation discharged in the same round as the code, not as a follow-up -- with the phase's whole test surface, both lint gates and the migration check green behind it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| The sweep's printed counters -> the operator reading them | `reconcile_campaign_runs` prints a counter summary an operator acts on. The runbook is the only place those counter names are defined. A definition that no longer matches the code is wrong information delivered with the authority of documentation. |
| Committed notebook output -> a reader trusting it as evidence | `docs/notebooks/pre_executed/` copies are committed WITH output (the one place in this repository where pre-commit does not strip it), so their printed numbers read as a receipt of a real run. Hand-written output would be an unfalsifiable claim wearing that receipt's clothes. |
| The notebook process -> `src/fomo_db.sqlite3` | The notebook exercises real management-command paths. The developer database is the operator's own working data and is not a fixture; a write to it is an unrequested mutation outside this plan's `files_modified`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-22-01 | Repudiation | operator-facing documentation of the sweep's counters (`docs/runbooks/telescope_runs_calendar.rst:1068-1145`) | medium | mitigate | Carries `T-35-21-05` forward to the plan that actually owns the file. A counter whose printed meaning is stale is the same wrong-information defect as this round's three criticals in a different medium: an operator seeing `retired: 37` on the first post-upgrade sweep would hunt for a window shrink that never happened, and one reading the current `detach_declined` paragraph would be told there is nothing to do about a night that is in fact holding a boundary its run no longer declares. Task 1 corrects the `retired` enumeration, widens the `detach_declined` definition and states the remedy, and adds the post-upgrade deploy note. |
| T-35-22-02 | Tampering | committed notebook output (`docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb`) | medium | mitigate | Carries `T-35-17` (35-SECURITY.md) forward. Pre-executed notebooks are committed with output, so hand-edited output would be a fabricated receipt for a run that never happened -- and this repository was caught doing exactly that once before. Task 2 regenerates the whole file with `jupyter nbconvert --to notebook --execute --inplace` (prohibition 2) and gates it: every code cell must carry a non-null `execution_count`, which a hand-edited or partially-executed notebook cannot satisfy. The new cells also carry real assertions rather than prints, so a future regression fails the notebook instead of printing a wrong number quietly. |
| T-35-22-03 | Tampering | the developer database `src/fomo_db.sqlite3` | medium | mitigate | The notebook runs real command paths; without the scratch-copy pattern it would mutate the operator's own working data as a side effect of building documentation. Cell 2 copies the file to a scratch directory and migrates the copy (prohibition 3), and Task 2 gates it with `git status --short -- src/fomo_db.sqlite3` printing nothing. Note the copy must reach head, which after plan 35-21 includes migration 0020 -- the stale "migration 0018" comment Task 2 corrects is what would mislead the next reader about that. |
| T-35-22-04 | Tampering | scope creep from a documentation plan into source | low | mitigate | A docs plan that "just fixes" a failing test would silently re-open plan 35-20's guard or plan 35-21's token at the end of a round, which is how rounds 2 and 3 acquired their regressions. Prohibition 1 forbids any source edit and requires a Rule 1 deviation report instead; both tasks gate it with `git status --short` over `solsys_code/` and `src/` printing nothing. |

ASVS level 1 scope note (block on `high`): this plan has no `high` threat -- it adds no endpoint,
no request handler, no input parsing, no authentication surface and no code path at all. It
installs no package: `pyproject.toml` is untouched and there is no `pip`/`npm`/`cargo` install
task, so `T-35-SC`'s package-legitimacy gate carries forward from 35-SECURITY.md unchanged and no
legitimacy checkpoint applies. The threats here are documentation-accuracy and evidence-integrity
defects, so ASVS L1's authentication, session and injection chapters have no applicable surface.
</threat_model>

<verification>
- The runbook's `retired` enumeration names the provenance-audit reason and covers a site
  correction; its `detach_declined` paragraph covers a declined re-mint and states the operator
  remedy; the post-upgrade deploy note tells the operator to preview with `--dry-run` first.
- `grep -c 'detach_declined' docs/runbooks/telescope_runs_calendar.rst` is at least its pre-edit
  count of 6 -- the paragraph was widened, not removed or re-labelled.
- The `rekeyed` caveat, the `legacy_deleted` paragraph and the cutover section's four steps are
  unchanged (WR-03 and WR-04 stay deferred).
- `reconcile_campaign_runs_demo.ipynb` carries executed output for both behaviour changes -- a site
  correction re-minting an already-projected run's nights, and a re-mint declined on a
  human-confirmed night -- and every code cell has a non-null execution count.
- `git status --short -- src/fomo_db.sqlite3` prints nothing, and `git status --short` over
  `solsys_code/` and `src/` prints nothing: this plan changed exactly two documentation files.
- `python manage.py test solsys_code.tests.test_allocation_projector
  solsys_code.tests.test_allocation_projector_signals solsys_code.tests.test_cutover_classical_allocations
  solsys_code.tests.test_load_telescope_runs solsys_code.tests.test_campaign_reconciler` is green,
  with a total no lower than the one plan 35-21 recorded.
- `pre-commit run ruff --all-files` and `pre-commit run ruff-format --all-files` both pass (D-07).
- `python manage.py makemigrations --check --dry-run` reports no drift.
- **Stated residual, not a gap:** under `--dry-run` the unrecorded-provenance resolution repeats on
  every invocation rather than once ever, because a preview may not record what it proves. That is
  35-REVIEW.md WR-01, user-deferred. The deploy note therefore describes the one-time audit as a
  REAL-mode property and does not claim the preview is free.
- **Round completion:** this is the last plan of gap-closure round 5. With it green, all three of
  35-REVIEW.md iteration 8's criticals are closed and the paired-docs obligation for the round is
  discharged. Before it, the round is incomplete regardless of 35-20 and 35-21 being green.
- **Edge-probe accounting for this plan (no silent drops):** 1 of the round's 7 probe items is
  authored here -- ALLOC-05 `unclassified`, as a flagged assumption, because its statement is a
  documentation consequence and travels with the runbook. Of the other 6, two are authored in plan
  35-20 and four in plan 35-21. 1 + 2 + 4 = 7 applicable, 0 dropped.
</verification>

<success_criteria>
- CLAUDE.md's paired-docs rule is satisfied for the whole round: the page under `docs/runbooks/`
  whose documented behaviour changed is updated, and `reconcile_campaign_runs_demo.ipynb` is
  regenerated by re-execution and committed with output.
- An operator reading the sweep's counters is told what the sweep now does and what to do about a
  declined re-mint, rather than being told there is nothing to do.
- An operator upgrading is told in advance that the first sweep is a one-time provenance audit, so
  a non-zero `would_retire` on nights nobody edited is expected rather than alarming.
- The notebook's new cells prove both behaviour changes with real executed output and real
  assertions, not prose.
- Nothing outside this plan's two `files_modified` paths changed, and plans 35-20's guard and
  35-21's token are intact -- proven by the five-module regression, both ruff hooks and the
  migration check all passing at the end of the round.
- WR-03's and WR-04's non-paired-docs items are still deferred and still open; nothing was expanded
  into.
</success_criteria>

<artifacts>
## Artifacts this phase produces

| Artifact | Kind | Produced by |
|----------|------|-------------|
| `docs/runbooks/telescope_runs_calendar.rst` -- corrected `retired` and `detach_declined` paragraphs, new post-upgrade deploy note | modified docs | Task 1 |
| `docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb` -- new site-correction and declined-re-mint cells, whole notebook regenerated by re-execution | modified paired notebook | Task 2 |
| `.planning/phases/35-allocation-layer-classical-cutover/35-22-SUMMARY.md` | planning artifact | Task 2 |

**Paired-docs coverage for the whole round.** CLAUDE.md maps
`solsys_code/allocation_projector.py` -> `reconcile_campaign_runs_demo.ipynb` (line 135, commit
`11b14da`), and the runbook clause is directory-scoped to any page under `docs/runbooks/` whose
documented behaviour a change affects. Plan 35-20's CR-01/CR-03 changes and plan 35-21's CR-02
change both affect `docs/runbooks/telescope_runs_calendar.rst` and both affect the reconciler demo
notebook, so THIS plan covers both in one pass against the final state of the tree -- which is why
neither code plan carries a `docs/` path in `files_modified`, and why the round is not complete
until this plan executes. `solsys_code/models.py` has no paired notebook of its own in the map.
</artifacts>

<output>
Create `.planning/phases/35-allocation-layer-classical-cutover/35-22-SUMMARY.md` when done.

Record in it: the runbook passages before and after (the `retired` count word and its new reason,
the `detach_declined` definition and remedy, the deploy note); the counters the notebook's two new
cells printed and the boundary values the site-correction cell compared against `sun_event()`; the
`nbconvert` command and its result; the execution-count gate result; the full five-module test
count; both ruff results; the `makemigrations --check` result; and the `git status --short` results
proving `src/fomo_db.sqlite3`, `solsys_code/` and `src/` were untouched.

Record any Rule 1 deviation in full -- in particular, a pre-existing notebook assertion whose
expectation had to be corrected because this round's behaviour legitimately changed its numbers:
name the cell, the old and new values, and why the change is correct rather than a regression.

State explicitly that this plan closes gap-closure round 5: with it green, all three of
35-REVIEW.md iteration 8's criticals (CR-01, CR-02, CR-03) are closed and the round's paired-docs
obligation is discharged.
</output>
