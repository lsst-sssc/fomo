---
phase: 35-allocation-layer-classical-cutover
plan: 15
type: execute
gap_closure: true
wave: 2
depends_on: ["35-12", "35-13", "35-14"]
files_modified:
  - docs/runbooks/telescope_runs_calendar.rst
  - docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb
autonomous: true
requirements: [ALLOC-01, ALLOC-02, ALLOC-03, ALLOC-04, ALLOC-05]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Every operator-facing sentence in `docs/runbooks/telescope_runs_calendar.rst` about the cutover's `duplicate_identity` guarantee states the marker precondition: the guarantee holds when the claimant's stored `Source line:` is recoverable AND matches, and a claimant with no recoverable marker is REPORTED rather than converted. Grounded in 35-VERIFICATION.md gap 2, which cites lines 938-949, 1497 and 1503 as currently false on the CR-01 path."
    - "No runbook passage softens this into 'usually' or 'in normal operation'. The predicate is stated, because the operator is the person who can restore the marker -- a hedge would leave them without the action."
    - "The runbook's `duplicate_identity` remedy covers BOTH branches the code now reports: disambiguate the two lines (a differing stored marker), or restore/correct the claimant's `observation_details` `Source line:` text in the Django admin (no recoverable marker) -- matching the two reason strings plan 35-12 writes, so the shared-operator-phrase key link NF-25 established still holds across code, runbook and notebook."
    - "The runbook's `load_telescope_runs` summary section states the dry/real parity invariant plan 35-14 restores: a preview's `created + updated + unchanged + skipped` equals `lines processed`, and a line whose preview reconcile fails is reported as `skipped` alone on both passes."
    - "`docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb` is regenerated by re-execution and never by a hand patch: every code cell carries a non-null execution count, and its committed output carries the corrected `CommandError` text plan 35-12 writes. 35-VERIFICATION.md flagged this notebook's committed output as printing the module's old unconditional clause verbatim."
    - "The regenerated notebook re-executes its allocation-and-observation handoff cells end to end against a scratch database copy, with real output -- so ALLOC-03's demonstrated surface is re-confirmed for this round rather than carried on trust from the prior pass."
    - "[edge probe ALLOC-03 `unclassified` -- RESOLVED, verification: explicit] Linking an `ObservationRecord` to an allocation night still retires only that night and leaves the observation's own event untouched; unlinking restores it. No code path in this gap-closure round touches the handoff, and the regenerated notebook's executed output re-demonstrates it."
    - "`pre-commit run sphinx-build --all-files` still passes: the runbook stays toctree-wired at `docs/index.rst:24` and builds clean after the edits."
  artifacts:
    - docs/runbooks/telescope_runs_calendar.rst
    - docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb
  key_links:
    - "The `duplicate_identity` reason strings in `cutover_classical_allocations.py` -> the runbook's remedy passages -> the notebook's committed cell output. One shared operator phrase across all three, which is the NF-25 key link 35-VERIFICATION.md records as WIRED; this plan keeps it wired after 35-12 changes the source text."
    - "`docs/index.rst:24` toctree -> `runbooks/telescope_runs_calendar` -- the page stays operator-reachable and buildable."
    - "`cutover_classical_allocations.py`'s `CommandError` text -> `reconcile_campaign_runs_demo.ipynb`'s committed output. The notebook prints the string verbatim, so it is stale the moment the string changes; this plan is where that dependency is discharged."
  prohibitions:
    - "An operator-facing runbook sentence must NOT state a guarantee the shipped code does not hold. (Carried forward from 35-10, where it was minted correctly and then violated by a replacement sentence that was narrower but still false.)"
    - "A runbook guarantee must NOT be repaired by hedging ('usually', 'in normal operation', 'in most cases') instead of stating the predicate."
    - "A paired notebook must NOT be hand-patched to repair execution counts or output; it is regenerated by re-execution or not at all."
    - "The notebook regeneration must NOT open `src/fomo_db.sqlite3` for writing -- work against a scratch copy, as plans 35-10 and 35-11 did."
    - "This plan must NOT change any file under `solsys_code/`. If a runbook sentence cannot be made true by rewording, the code plan it depends on is wrong and must be revisited rather than papered over here."
    - "Dropped as canon security/compliance boilerplate rather than minted here: credential-logging, TLS-downgrade and authz-bypass prohibitions -- this plan touches documentation and a pre-executed notebook only, with no network call, no secret and no request path."
---

<objective>
Converge the operator-facing surface on the code that waves 1 of this round ship.

Gap 2 of 35-VERIFICATION.md: the prior gap-closure round replaced one false runbook sentence
with a narrower one that is STILL false on the CR-01 path -- the documentation was updated to
match the intended fix rather than the shipped fix. Three passages promise the cutover's
no-merge guarantee unconditionally, and the very same passage instructs the operator to perform
the Django-admin edit that disarms it.

Gap 1's last `missing:` item: `reconcile_campaign_runs_demo.ipynb`'s committed output prints
`cutover_classical_allocations.py`'s `CommandError` text verbatim, including the clause plan
35-12 corrects, so the notebook is stale the moment that plan lands.

Purpose: state the predicate, not a hedge. The operator is the person who can restore the
`Source line:` marker, so telling them the guarantee "usually" holds would leave them without
the action; telling them it holds when the marker is recoverable and matches gives them one.

Output: three corrected cutover passages plus one new loader-parity sentence in the runbook, and
a re-executed `reconcile_campaign_runs_demo.ipynb` carrying the corrected text in real output.

No `type="tracer"` task: this plan adds no code path, so there is no end-to-end slice to prove.
The slice it documents was proven by 35-12 Task 1's tracer. This plan's runnable gate is the
notebook re-execution, which drives that same path against a real database.

Wave 2. Depends on 35-12 (the `CommandError` and reason-string text this documents), 35-13 (the
projector behaviour the same notebook re-executes) and 35-14 (the loader invariant the new
runbook sentence states).
</objective>

<execution_context>
@/home/tlister/git/fomo_devel/.claude/gsd-core/workflows/execute-plan.md
@/home/tlister/git/fomo_devel/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md

@.planning/phases/35-allocation-layer-classical-cutover/35-VERIFICATION.md
@.planning/phases/35-allocation-layer-classical-cutover/35-REVIEW.md
@.planning/phases/35-allocation-layer-classical-cutover/35-10-SUMMARY.md
@.planning/phases/35-allocation-layer-classical-cutover/35-11-SUMMARY.md
@.planning/phases/35-allocation-layer-classical-cutover/35-12-SUMMARY.md
@.planning/phases/35-allocation-layer-classical-cutover/35-14-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make every runbook guarantee true of the shipped code</name>
  <files>docs/runbooks/telescope_runs_calendar.rst</files>
  <read_first>
    - `.planning/phases/35-allocation-layer-classical-cutover/35-VERIFICATION.md` the `gaps:` entry whose `truth` begins "The operator runbook's cutover section states only guarantees the shipped code holds" -- both `artifacts` entries quote the offending text and both `missing:` items state what must replace it, including the explicit instruction not to hedge.
    - `.planning/phases/35-allocation-layer-classical-cutover/35-12-SUMMARY.md` -- the exact wording of the two `duplicate_identity` reason strings and of the corrected `CommandError`, so the runbook mirrors the phrase an operator will actually see.
    - `docs/runbooks/telescope_runs_calendar.rst` lines 930-975 -- the `duplicate_identity` operator-action paragraph and the "safe to repeat" paragraph that follows it.
    - `docs/runbooks/telescope_runs_calendar.rst` lines 1488-1505 -- the troubleshooting **Fix:** paragraph carrying the same claim twice.
    - `docs/runbooks/telescope_runs_calendar.rst` lines 60-82 -- the `load_telescope_runs` summary-lines section, where the dry/real parity sentence belongs.
  </read_first>
  <action>
    Four edits, all in `docs/runbooks/telescope_runs_calendar.rst`. State predicates; do not hedge.

    1. The `duplicate_identity` operator-action paragraph (lines 938-949). It currently promises
       the second group is never merged into the first group's run on the first invocation and on
       every re-run, and closes by promising the first group's run and events are converted
       regardless, however many times the command repeats. Rewrite both halves to state the
       marker precondition: the database check catches a claimant left by an earlier cutover pass
       or by a `load_telescope_runs` import WHEN that claimant's stored ``Source line:`` is
       recoverable and matches the group's line; when the claimant's marker is absent or differs,
       the group is REPORTED rather than converted, because ``observation_details`` is editable
       from the Django admin and the command cannot prove such a run came from the line in hand.
       Note that this is reachable through the very admin edit this paragraph asks the operator to
       perform.

    2. The remedy in that same paragraph must now cover both branches, matching the two reason
       strings 35-12 writes: give the later group's events a different bracketed proposal token
       (a differing marker), OR restore or correct the claimant run's ``observation_details``
       ``Source line:`` text in the Django admin so it matches (no recoverable marker). Then
       re-run.

    3. The troubleshooting **Fix:** paragraph (lines 1494-1503). Its parenthetical at line 1497
       promises the earlier group's run and events are converted regardless, and its closing
       clause at line 1503 promises a repeat pass modifies no pre-existing ``CampaignRun``.
       Replace both with the same predicate the paragraph above now states, and mirror 35-12's
       corrected ``CommandError`` wording so an operator reading the error and the runbook sees
       one phrase, not two.

    4. The `load_telescope_runs` summary-lines section (lines 62-81). Add one sentence stating the
       invariant plan 35-14 restores: a ``--dry-run`` reports the same outcome for each line the
       real pass reports, so ``created + updated + unchanged + skipped`` equals ``lines processed``
       on both, and a line whose preview fails is reported under ``skipped`` alone rather than
       counted twice.

    Wording anchors, so the two corrected cutover paragraphs stay recognisably one voice and the
    edits are checkable: each of them must use the phrase `cannot prove` when explaining why an
    unproven claimant is refused, and each must name the field by its model attribute name,
    ``observation_details``, rather than only describing it as "the description". Neither phrase
    appears anywhere in the file today.

    Keep the file's existing reStructuredText conventions: double-backtick literals, the current
    line-wrapping width of the surrounding paragraphs, and no new section or label.

    Change no file under `solsys_code/`. This plan documents behaviour that waves 1 already
    shipped; if a sentence cannot be made true by rewording, stop and report it rather than
    editing the code from here.
  </action>
  <verify>
    <automated>test "$(grep -v '^\.\. ' docs/runbooks/telescope_runs_calendar.rst | grep -c 'left untouched either way')" = "0"</automated>
    <fails_when>Exits 1 because the inner count is not `0` -- one of the two unconditional conversion promises survives in the prose.</fails_when>
    <automated>test "$(grep -v '^\.\. ' docs/runbooks/telescope_runs_calendar.rst | grep -c 'rewrites no existing')" = "0"</automated>
    <fails_when>Exits 1 because the inner count is not `0` -- the troubleshooting section still promises the unconditional no-rewrite property.</fails_when>
    <automated>test "$(grep -ciE 'usually|in normal operation|in most cases' docs/runbooks/telescope_runs_calendar.rst)" = "0"</automated>
    <fails_when>Exits 1 because a hedge word was introduced instead of the predicate, which 35-VERIFICATION.md's second `missing:` item forbids explicitly.</fails_when>
    <automated>test "$(grep -c 'cannot prove' docs/runbooks/telescope_runs_calendar.rst)" -ge "2"</automated>
    <fails_when>Exits 1 because the count is below 2 (it is `0` before this task) -- one or both corrected cutover paragraphs fail to explain why an unproven claimant is refused.</fails_when>
    <automated>test "$(grep -c 'observation_details' docs/runbooks/telescope_runs_calendar.rst)" -ge "2"</automated>
    <fails_when>Exits 1 because the count is below 2 (it is `0` before this task) -- the two-branch remedy does not name the field the operator must restore.</fails_when>
    <automated>git status --short solsys_code</automated>
    <fails_when>Prints any line -- a file under `solsys_code/` was modified, which this plan forbids.</fails_when>
    <automated>pre-commit run sphinx-build --all-files</automated>
    <fails_when>Prints `Failed` -- the reStructuredText edits broke the docs build, e.g. an unbalanced literal or a broken toctree reference.</fails_when>
  </verify>
  <acceptance_criteria>
    - Both negative greps above print `0`, and the hedge-word gate prints `0`.
    - The `duplicate_identity` operator-action paragraph names the `Source line:` marker as the precondition of the no-merge guarantee, and uses the phrase `cannot prove`.
    - That paragraph's remedy names both branches: disambiguating the two lines, and restoring or correcting the claimant run's `observation_details` marker in the Django admin.
    - `grep -c 'cannot prove' docs/runbooks/telescope_runs_calendar.rst` prints at least `2`, and `grep -c 'observation_details' docs/runbooks/telescope_runs_calendar.rst` prints at least `2` (both are `0` before this task).
    - The troubleshooting **Fix:** paragraph states the same predicate as the operator-action paragraph, with no unconditional clause left.
    - The `load_telescope_runs` summary-lines section contains a sentence stating that the four run-level counters sum to `lines processed` on both passes.
    - `pre-commit run sphinx-build --all-files` prints `Passed`.
    - `git status --short solsys_code` prints nothing.
  </acceptance_criteria>
  <done>An operator following this runbook is told exactly when the cutover's no-merge guarantee holds and exactly what to restore when it does not -- and the loader section states the preview invariant the operator relies on before writing.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate the reconciler demo notebook against the corrected command output</name>
  <files>docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb</files>
  <read_first>
    - `CLAUDE.md` the "Paired docs are part of the deliverable" paragraph -- the module-to-notebook map (this notebook is paired with `campaign_reconciler.py`, `reconcile_campaign_runs.py`, `allocation_projector.py` AND `cutover_classical_allocations.py`, all four of which waves 1 touched), the regeneration command, and the breach history.
    - `.planning/phases/35-allocation-layer-classical-cutover/35-VERIFICATION.md` -- the Required Artifacts row for this notebook, whose **Caveat** states that its committed output prints the command's `CommandError` text verbatim and must be regenerated after the CR-01 fix; and gap 1's final `missing:` item saying the same.
    - `.planning/phases/35-allocation-layer-classical-cutover/35-10-SUMMARY.md` -- how this notebook was last regenerated (18/18 code cells, the second-invocation cell, and the `ruff-format` re-stage it required).
    - `.planning/phases/35-allocation-layer-classical-cutover/35-12-SUMMARY.md` -- the corrected `CommandError` and `duplicate_identity` reason text the regenerated output must now contain.
    - `docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb` -- the existing cells, in particular the second-invocation cell 35-10 added and the cells whose output quotes the `CommandError`.
  </read_first>
  <action>
    - Regenerate the WHOLE notebook with
      `jupyter nbconvert --to notebook --execute --inplace docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb`,
      against a scratch copy of the developer database. Do not open `src/fomo_db.sqlite3` for
      writing. Do not hand-edit execution counts or cell output under any circumstances -- a
      hand patch is how T-35-17 arose in the first place.
    - Before regenerating, read the cells that invoke `cutover_classical_allocations` and confirm
      each still demonstrates what its surrounding markdown claims now that 35-12 has landed. A
      cell whose fixture previously produced a silent conversion may now produce a
      `duplicate_identity` report; where that happens, update the cell's markdown prose so the
      narration matches the new output rather than leaving a caption that contradicts the cell
      below it.
    - If 35-12's inversion changes the demo's before/after event counts, update any markdown that
      states those counts. Do not adjust the fixture to preserve an old number -- the notebook
      exists to show what the command actually does.
    - If any `Target` fixture is needed, use `tom_targets.tests.factories.NonSiderealTargetFactory`
      -- FOMO is exclusively for Solar System / non-sidereal targets (CLAUDE.md).
    - Expect the pinned `ruff-format` pre-commit hook to reformat notebook code-cell source on the
      commit attempt (plan 35-10 hit exactly this); re-stage and re-commit, no functional change.
  </action>
  <precondition>`jupyter nbconvert` is available in the active environment -- it is a dev dependency installed by `./.setup_dev.sh` and was used by plans 35-10 and 35-11 for the same task; this plan does not install it.</precondition>
  <verify>
    <automated>python3 -c "import json,sys; nb=json.load(open('docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb')); c=[x for x in nb['cells'] if x['cell_type']=='code']; n=[i for i,x in enumerate(c) if x.get('execution_count') is None]; print('code cells:', len(c), 'null counts:', len(n)); sys.exit(1 if (n or len(c) < 18) else 0)"</automated>
    <fails_when>Exits 1 after printing a `null counts:` value above `0` (the notebook was hand-patched rather than executed) or a `code cells:` value below `18` (cells were dropped rather than re-executed).</fails_when>
    <automated>test "$(grep -c 'rewrites no existing CampaignRun' docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb)" = "0"</automated>
    <fails_when>Exits 1 because the inner count is not `0` -- the notebook's committed output still carries the superseded unconditional clause, meaning it was not re-executed against 35-12's corrected command.</fails_when>
    <automated>grep -c 'Source line:' docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb</automated>
    <fails_when>Prints `0` -- the regenerated output contains no `Source line:` marker text at all, so the corrected operator phrase is not demonstrated anywhere in the notebook.</fails_when>
    <automated>git status --short src/fomo_db.sqlite3</automated>
    <fails_when>Prints a line beginning with ` M` or `M ` -- the developer database was written to, which the notebook regeneration must not do.</fails_when>
    <automated>pre-commit run ruff-format --all-files</automated>
    <fails_when>Prints `Failed` with `- files were modified by this hook` on a run after the final commit, meaning notebook code-cell source was left unformatted.</fails_when>
  </verify>
  <acceptance_criteria>
    - Every code cell in `docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb` has a non-null `execution_count`, and the notebook has at least 18 code cells.
    - `grep -c 'rewrites no existing CampaignRun' docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb` prints `0`.
    - The notebook's committed output contains the corrected `CommandError` sentence plan 35-12 writes.
    - No markdown cell narrates an outcome that differs from the output of the code cell beneath it.
    - `src/fomo_db.sqlite3` is unmodified in `git status --short`.
    - `pre-commit run ruff-format --all-files` prints `Passed`.
  </acceptance_criteria>
  <done>The notebook an operator reads to understand the cutover shows the command's real, current behaviour and its real, current error text -- with every cell executed, not edited.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator reading the runbook -> the decision to run a destructive one-time migration | The runbook is the control that tells a human whether a command is safe to repeat. A false guarantee here is a failed control, not a typo -- it is what drove the operator into CR-01's reproduced merge. |
| Committed notebook output -> operator expectation of command behaviour | The pre-executed notebook is read as evidence of what the command does. Stale output misrepresents shipped behaviour. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-28 | Repudiation | `docs/runbooks/telescope_runs_calendar.rst` cutover section | medium | mitigate | An operator-facing guarantee that the shipped code does not hold removes the human control over a destructive migration and leaves no record that the promise was conditional. Task 1 states the marker precondition in all three passages and names the restoring action, with hedge words gated out so the predicate cannot be softened away. |
| T-35-29 | Spoofing | the shared `duplicate_identity` operator phrase across code, runbook and notebook | low | mitigate | If the three copies drift, an operator matching an error message against the runbook finds no guidance and may improvise a destructive remedy. Both tasks mirror 35-12's shipped strings, and the `Source line:` presence gates keep the phrase aligned in all three places. |
| T-35-30 | Tampering | `docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb` | low | mitigate | A hand-patched notebook can assert behaviour that was never executed. Task 2 forbids hand edits and gates on non-null execution counts across every code cell, so committed output can only come from a real run. |
| T-35-31 | Information Disclosure | regenerated notebook output | low | accept | The notebook executes against a scratch copy of the developer database and prints run pks, identity keys and schedule-line text. That is the same class of content already committed in this and two sibling notebooks, contains no credential or secret, and ASVS level 1 requires no further control for a docs artifact in a private repository. |
| T-35-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no package: no `pip install` task, no change to `pyproject.toml`, no new third-party import. `jupyter nbconvert` is an already-installed dev dependency (declared as a `<precondition>`, not installed here). The package-legitimacy gate does not fire. |
</threat_model>

<verification>
Run all of the following from the repository root before considering the plan complete:

1. `pre-commit run sphinx-build --all-files` -- expect `Passed`. This is the gate plan 35-10 used for the same file.
2. `python manage.py test solsys_code.tests.test_allocation_projector solsys_code.tests.test_campaign_reconciler solsys_code.tests.test_cutover_classical_allocations solsys_code.tests.test_load_telescope_runs solsys_code.tests.test_observation_projector_signals` -- expect `OK`. This plan changes no code, so a failure here means a wave-1 plan regressed and this one must not be committed on top of it.
3. `pre-commit run ruff --all-files` and `pre-commit run ruff-format --all-files` -- expect `Passed` on both (notebook code-cell source is in scope for the formatter).
4. `git status --short` -- expect no modified file beyond the two in `files_modified` and `.planning/`. In particular `src/fomo_db.sqlite3` and everything under `solsys_code/` must be clean.
</verification>

<success_criteria>
- No runbook sentence states the cutover's no-merge or no-rewrite guarantee without naming the `Source line:` marker precondition, and no hedge word was used in place of the predicate.
- The runbook's `duplicate_identity` remedy names both branches the code now reports.
- The runbook's loader section states the dry/real parity invariant plan 35-14 restores.
- `reconcile_campaign_runs_demo.ipynb` carries no null execution count and no superseded `CommandError` clause in its committed output.
- `pre-commit run sphinx-build --all-files`, `ruff` and `ruff-format` all pass.
</success_criteria>

## Artifacts this phase produces

| Artifact | Kind | Where |
|----------|------|-------|
| Corrected `duplicate_identity` operator-action paragraph | documentation prose | `docs/runbooks/telescope_runs_calendar.rst`, cutover section (currently lines 938-949) |
| Two-branch `duplicate_identity` remedy | documentation prose | `docs/runbooks/telescope_runs_calendar.rst`, same paragraph |
| Corrected troubleshooting **Fix:** paragraph | documentation prose | `docs/runbooks/telescope_runs_calendar.rst` (currently lines 1494-1503) |
| Dry/real parity sentence for the loader summary lines | new documentation sentence | `docs/runbooks/telescope_runs_calendar.rst` (currently lines 62-81) |
| Re-executed notebook carrying the corrected command output | regenerated pre-executed notebook | `docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb` |

No new module, class, test, migration, dependency or management command. No code file changed.

<output>
Create `.planning/phases/35-allocation-layer-classical-cutover/35-15-SUMMARY.md` when done.
</output>
