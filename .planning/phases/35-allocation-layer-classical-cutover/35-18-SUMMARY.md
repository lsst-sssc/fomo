---
phase: 35-allocation-layer-classical-cutover
plan: 18
subsystem: docs
tags: [sphinx-runbook, jupyter-notebook, gap-closure, dry-run-parity, documentation]

# Dependency graph
requires:
  - phase: 35-allocation-layer-classical-cutover
    provides: "35-16's revert of the half-null dry-run fallback (the projector behaviour both notebooks re-execute)"
  - phase: 35-allocation-layer-classical-cutover
    provides: "35-17's narrowed loader create-arm claim, two-cause duplicate_identity reason label, and cutover re-run gotcha docstring -- the exact wording this plan mirrors into the runbook and regenerates the notebooks against"
provides:
  - "Runbook loader paragraph scoped to the existing-run arm vs. the brand-new-line arm, with the false because-clause removed"
  - "Runbook's two duplicate_identity DEFINITIONS (reason vocabulary + troubleshooting Cause list) stating both causes behind the shared 'already holds the derived identity key' anchor"
  - "Runbook cutover 'Re-run gotcha' note naming every field re-applied to a matching-marker claimant and the remedy interaction it creates"
  - "load_telescope_runs_demo.ipynb regenerated with its parity conclusion scoped to the existing-run arm it actually demonstrates"
  - "reconcile_campaign_runs_demo.ipynb regenerated against the two-cause duplicate_identity reason label"
affects: []

# Actuals (#2632)
actuals:
  tokens: 11848
  tasks: 3
  commits: 3
  plan_head_before: 3f10ea63f2ba50893f97d50012306ccec03038ba

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "State the predicate per-arm rather than hedge: when a guarantee holds on one code path and not a sibling, narrow the sentence to name the arm, don't soften it with a hedge word or generalize past what the untested arm can prove."

key-files:
  created: []
  modified:
    - docs/runbooks/telescope_runs_calendar.rst
    - docs/notebooks/pre_executed/load_telescope_runs_demo.ipynb
    - docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb

key-decisions:
  - "Kept the cutover 'Re-run gotcha' note's phrase 'does not survive the next cutover run' on a single physical source line (splitting the sentence rather than letting it wrap), matching plan 35-17's own decision for the module docstring -- the plan's own verify gate is a single-line grep and reStructuredText paragraphs are rewrapped by editors but not by any commit-time formatter, so a line-wrapped phrase would silently fail the gate."
  - "Did not add a new notebook cell demonstrating the database-claimant (no-marker) duplicate_identity cause in reconcile_campaign_runs_demo.ipynb, even though 35-VERIFICATION.md's advisory IN-01 flags that the notebook's demo only ever exercises the group-vs-group cause. Task 3's action explicitly scopes this round to re-execution plus prose-consistency checking, not new demonstrations; IN-01 was logged in 35-17-SUMMARY.md as deferred to 'any further doc round,' and this is confirmed as the last one this round covers."

requirements-completed: [ALLOC-03, ALLOC-04, ALLOC-05]

coverage:
  - id: D1
    description: "The runbook's loader because-clause is scoped to the existing-run arm; the brand-new-line arm is named as unable to predict a reconcile failure, without contradicting the earlier window-length sentence."
    requirement: ALLOC-04
    verification:
      - kind: other
        ref: "grep -c 'per-line preview reconcile the real pass performs' docs/runbooks/telescope_runs_calendar.rst == 0; grep -c 'brand-new line' >= 1"
        status: pass
    human_judgment: false
  - id: D2
    description: "Both duplicate_identity definitions in the runbook (reason vocabulary L933-937, troubleshooting Cause list L1498-1500) state both causes behind the shared 'already holds the derived identity key' anchor."
    requirement: ALLOC-05
    verification:
      - kind: other
        ref: "grep -c 'already holds the derived identity key' docs/runbooks/telescope_runs_calendar.rst >= 2 (actual: 2)"
        status: pass
    human_judgment: false
  - id: D3
    description: "The runbook carries a cutover 'Re-run gotcha' note naming every re-applied field and the phrase 'does not survive the next cutover run', without disturbing plan 35-15's corrected remedy paragraphs."
    requirement: ALLOC-05
    verification:
      - kind: other
        ref: "grep -c 'does not survive the next cutover run' == 1; grep -c 'Re-run gotcha' >= 1; grep -c 'cannot prove' >= 2 (actual: 3); hedge-word grep == 0"
        status: pass
    human_judgment: false
  - id: D4
    description: "load_telescope_runs_demo.ipynb's parity section is narrowed to the existing-run arm and regenerated by re-execution; the falsified 'the preview never disagrees with the real run' sentence is gone."
    requirement: ALLOC-04
    verification:
      - kind: integration
        ref: "jupyter nbconvert --to notebook --execute --inplace load_telescope_runs_demo.ipynb; grep -c 'the preview never disagrees with the real run' == 0; grep -c 'existing-run arm' >= 1 (actual: 4); 16/16 code cells, 0 null execution counts"
        status: pass
    human_judgment: false
  - id: D5
    description: "reconcile_campaign_runs_demo.ipynb is regenerated by re-execution so its committed output carries plan 35-17's two-cause duplicate_identity label instead of the superseded single-cause text."
    requirement: ALLOC-05
    verification:
      - kind: integration
        ref: "jupyter nbconvert --to notebook --execute --inplace reconcile_campaign_runs_demo.ipynb; grep -c 'already holds the derived identity key' >= 1 (actual: 12); 18/18 code cells, 0 null execution counts"
        status: pass
    human_judgment: false
  - id: D6
    description: "pre-commit run sphinx-build/ruff/ruff-format all pass; the six-module regression surface (237 tests) passes; working tree clean beyond the three files_modified paths and .planning/."
    requirement: ALLOC-05
    verification:
      - kind: integration
        ref: "python manage.py test solsys_code.tests.{test_allocation_projector,test_allocation_projector_signals,test_campaign_reconciler,test_cutover_classical_allocations,test_load_telescope_runs,test_observation_projector_signals} -> Ran 237 tests, OK; pre-commit run sphinx-build/ruff/ruff-format --all-files -> all Passed"
        status: pass
    human_judgment: false

duration: ~35min
completed: 2026-09-15
status: complete
---

# Phase 35 Plan 18: Narrow the Runbook, Regenerate Both Notebooks Against the Third-Round Fixes Summary

**Closed the operator-facing half of Defect B: the runbook's loader paragraph, both `duplicate_identity` definitions, and a new cutover "Re-run gotcha" note now say exactly what the shipped code proves, and both `load_telescope_runs_demo.ipynb`/`reconcile_campaign_runs_demo.ipynb` were regenerated by re-execution so their committed output matches.**

## Performance

- **Duration:** ~35 min
- **Completed:** 2026-09-15
- **Tasks:** 3 (each independently committed)
- **Files modified:** 3

## Accomplishments

- **Task 1 (runbook, five edits in one file):** Rewrote the loader's dry/real
  counter-parity paragraph to describe the existing-run arm (preview runs the
  same reconcile the real pass runs, folds its counter only after that call
  returns) and the brand-new-line arm (no preview reconcile exists at all, so
  the preview cannot predict a reconcile failure) separately, removing the
  false because-clause that attributed the invariant to a preview reconcile
  performed for every line. Added the second `duplicate_identity` cause (a
  `CampaignRun` that already holds the derived identity key with an absent or
  differing marker) to both the reason-vocabulary definition (L933-937) and
  the troubleshooting Cause list (L1498-1500), behind the shared
  `already holds the derived identity key` anchor phrase plan 35-17's
  `_REASON_LABELS[_DUPLICATE_IDENTITY]` uses. Added a cutover "Re-run gotcha"
  `.. note::`, in the voice of the existing `import_campaign_csv` "Re-import
  gotcha" note, naming every field (`run_status`, `target`, `campaign`,
  `site`/`site_raw`, `window_start`/`window_end`, the two sub-night fields,
  `observation_details`) re-applied to a matching-marker claimant on every
  invocation, using the verbatim phrase `does not survive the next cutover
  run`, and closing the loop the no-marker remedy opens (restoring a marker
  converts a refused claimant into a matching one). Left plan 35-15's
  corrected remedy paragraphs untouched beyond what the cross-reference
  needed -- `cannot prove` still appears at least twice.
- **Task 2 (loader notebook, PROBE-P5):** Narrowed markdown cell 17's opening
  claim to the existing-run arm (using the phrase `existing-run arm`), added
  a sentence naming what the cell does NOT show (a brand-new line's preview
  calls no `reconcile_run()` at all) and citing
  `test_dry_run_of_a_brand_new_line_cannot_predict_a_reconcile_failure` by
  name. Replaced code cell 18's final `print` conclusion (kept the printed
  tuple and both assertions unchanged) so it no longer states "the preview
  never disagrees with the real run" -- the strongest, now-falsified claim
  35-VERIFICATION.md named as the minimum that must land this round.
  Regenerated by `jupyter nbconvert --to notebook --execute --inplace`
  against the notebook's own scratch database copy (16/16 code cells,
  0 null execution counts).
- **Task 3 (reconciler notebook):** Re-executed
  `reconcile_campaign_runs_demo.ipynb` against its own scratch database copy
  so its committed `duplicate_identity` output carries plan 35-17's
  two-cause reason label (12 occurrences of `already holds the derived
  identity key`) instead of the superseded single-cause text (12
  occurrences before this task). Checked both markdown cells narrating a
  `duplicate_identity` outcome against the regenerated output beneath them --
  both describe only the group-vs-group collision the cells actually
  demonstrate, which the two-cause label still states accurately, so no
  prose edit was needed. 18/18 code cells re-executed, 0 null execution
  counts.
- All three verify commands (`sphinx-build`, `ruff`, `ruff-format`) and the
  six-module regression surface (237 tests) pass; `git status --short` shows
  no file changed beyond the three `files_modified` paths plus pre-existing
  `.planning/` and unrelated untracked scratch artifacts from earlier in the
  session. `src/fomo_db.sqlite3` and everything under `solsys_code/` are
  clean.

## Task Commits

1. **Task 1: Narrow the runbook's loader claim, define both duplicate_identity causes, add the cutover's re-run gotcha** - `a313392` (docs)
2. **Task 2: Regenerate the loader notebook with a conclusion it can actually support** - `15000cc` (docs)
3. **Task 3: Regenerate the reconciler notebook against the two-cause reason label** - `aa6dbb0` (docs)

## Files Created/Modified

- `docs/runbooks/telescope_runs_calendar.rst` -- loader paragraph scoped to
  the two arms; two `duplicate_identity` definitions extended with the
  second cause; new cutover "Re-run gotcha" note.
- `docs/notebooks/pre_executed/load_telescope_runs_demo.ipynb` -- parity
  section narrowed to the existing-run arm and regenerated by re-execution.
- `docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb` --
  regenerated by re-execution against the two-cause reason label; no source
  edits (prose already accurate against the demonstrated scenario).

## Decisions Made

- Kept the cutover note's "does not survive the next cutover run" phrase on
  a single physical source line rather than letting the paragraph wrap it
  across two -- mirroring plan 35-17's own decision for the module
  docstring, since the plan's verify gate is a single-line grep.
- Did not add a new notebook cell demonstrating the database-claimant
  (no-marker) `duplicate_identity` cause, per Task 3's action text scoping
  this round to re-execution plus prose-consistency checking. 35-VERIFICATION.md's
  advisory IN-01 (the notebook only ever exercises the group-vs-group cause)
  remains an open advisory, not a must-have for this plan.

## Deviations from Plan

None -- plan executed exactly as written. All five runbook edits, both
notebook regenerations, and every verify gate matched the plan's `<action>`
and `<verify>` sections; the one wrapping fix noted above was caught by the
plan's own gate before commit, not a post-hoc correction.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Verification

All four plan-level verification commands were run from the repository root and passed:

1. `pre-commit run sphinx-build --all-files` -> **Passed**.
2. `python manage.py test solsys_code.tests.test_allocation_projector solsys_code.tests.test_allocation_projector_signals solsys_code.tests.test_campaign_reconciler solsys_code.tests.test_cutover_classical_allocations solsys_code.tests.test_load_telescope_runs solsys_code.tests.test_observation_projector_signals` -> **Ran 237 tests, OK**.
3. `pre-commit run ruff --all-files` -> **Passed**. `pre-commit run ruff-format --all-files` -> **Passed**.
4. `git status --short` -> only `.planning/config.json`, `.planning/state.json` (pre-existing at session start) and pre-existing untracked scratch artifacts (`.gsd/`, `.planning/milestone.lock`, phase-33/34 review-diagnostics scratch files, `reqgroup_2682493.json`) beyond this plan's three `files_modified` paths; `src/fomo_db.sqlite3` and `solsys_code/` are clean.

Grep gates (all passed):
- `grep -c 'per-line preview reconcile the real pass performs' docs/runbooks/telescope_runs_calendar.rst` -> `0`.
- `grep -c 'brand-new line' docs/runbooks/telescope_runs_calendar.rst` -> `1`.
- `grep -c 'already holds the derived identity key' docs/runbooks/telescope_runs_calendar.rst` -> `2`.
- `grep -c 'does not survive the next cutover run' docs/runbooks/telescope_runs_calendar.rst` -> `1`.
- `grep -c 'Re-run gotcha' docs/runbooks/telescope_runs_calendar.rst` -> `1`.
- `grep -ciE 'usually|in normal operation|in most cases' docs/runbooks/telescope_runs_calendar.rst` -> `0`.
- `grep -c 'cannot prove' docs/runbooks/telescope_runs_calendar.rst` -> `3`.
- `grep -c 'the preview never disagrees with the real run' docs/notebooks/pre_executed/load_telescope_runs_demo.ipynb` -> `0`.
- `grep -c 'existing-run arm' docs/notebooks/pre_executed/load_telescope_runs_demo.ipynb` -> `4`.
- Loader notebook: 16 code cells, 0 null execution counts.
- `grep -c 'already holds the derived identity key' docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb` -> `12`.
- `grep -c 'duplicate_identity' docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb` -> `9`.
- Reconciler notebook: 18 code cells, 0 null execution counts.

## Next Phase Readiness

This is the last plan of Phase 35's third gap-closure round, and per
35-VERIFICATION.md's recommendation, intended to be the phase's final round.
All five findings from the third verification pass (Defect A's half-null
fallback, Defect B's create-arm preview and the two runbook over-claims, and
the `duplicate_identity` vocabulary gap) are now closed across plans 35-16,
35-17 and this plan. `ALLOC-01` through `ALLOC-05` are complete; confirmed
via `requirements mark-complete` below (already marked from earlier rounds,
this is a final confirmation). No further plan in this phase checks this.

---
*Phase: 35-allocation-layer-classical-cutover*
*Completed: 2026-09-15*
