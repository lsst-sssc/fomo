---
phase: 35-allocation-layer-classical-cutover
plan: 12
type: execute
gap_closure: true
wave: 1
depends_on: []
files_modified:
  - solsys_code/management/commands/cutover_classical_allocations.py
  - solsys_code/tests/test_cutover_classical_allocations.py
autonomous: true
requirements: [ALLOC-04, ALLOC-05]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A database claimant of the derived identity key whose stored `observation_details` yields NO recoverable `Source line:` marker is reported under `duplicate_identity` and the command exits non-zero -- never a find-and-update. Grounded in 35-REVIEW.md PROBE-A, which drove exactly this fixture to `Done. candidates: 3, groups: 1, runs created: 0, updated: 1, unchanged: 0, events re-keyed: 3, unexplained: 0` with exit 0 and empty stderr."
    - "After a refused pass over PROBE-A's fixture the claimant's `run_status`, `observation_details` and `target` are byte-identical to their pre-command values, asserted field by field rather than inferred from the absence of a string in stdout. PROBE-A flipped `run_status` planned -> cancelled, replaced the staff note `'Rescheduled per PI request; see ticket OPS-4412.'` and set `target` to None."
    - "The `duplicate_identity` reason text branches on whether a marker was recovered: a claimant WITH a differing stored marker keeps the existing 'for a different Source line' wording; a claimant with no recoverable marker gets its own wording naming the `observation_details` `Source line:` text to restore in the Django admin. Both name an action this command can carry out (the NF-25 contract)."
    - "The ALLOC-01 `empty` case is asserted as a REFUSAL, replacing -- not sitting beside -- the case that asserted conversion. That case passed while pinning the blocker: the verifier ran `TestDatabaseScopedIdentityGuard` and got `Ran 4 tests ... OK`, so a green suite was not evidence."
    - "A `CampaignRun` whose stored `Source line:` MATCHES the group's still converts normally: the benign cutover-after-import ordering WR-11 pins is not over-rejected, asserted by `test_pre_existing_claimant_with_same_source_line_still_converts`, which is left unchanged."
    - "No guarantee statement inside `cutover_classical_allocations.py` asserts the no-rewrite property without naming the marker precondition -- neither the module docstring's `duplicate_identity` paragraph nor the `CommandError` raised when unexplained events remain."
    - "[edge probe ALLOC-01 `empty` -- RESOLVED, verification: explicit] A database claimant whose `observation_details` carries no recoverable `Source line:` marker (`_extract_source_line()` returns None) is REFUSED, not converted. This RE-RESOLVES 35-08's flagged assumption for the same probe, which specified the permissive default, was implemented faithfully, and was then reproduced end-to-end as CR-01."
    - "[edge probe ALLOC-01 `adjacency` -- RESOLVED, verification: explicit] Two groups whose derived identity keys are EXACTLY equal separate (the first claimant is converted, the second reported under `duplicate_identity`) and never merge -- now true for a database claimant as well as an in-process one, which was the half CR-01 left open."
    - "[edge probe ALLOC-01 `ordering` -- RESOLVED, verification: explicit] When two groups compare equal on the identity key the winner is deterministic: within one invocation the earliest-pk group (`candidates.order_by('pk')` insertion order); across invocations a pre-existing database claimant, and ONLY when that claimant's stored marker matches -- a claimant whose marker is absent or differs wins nothing, because nothing is converted for that key at all."
    - "[edge probe ALLOC-05 `unclassified` -- RESOLVED, verification: explicit] The cutover's stated sequencing is safe to repeat in the sense its own `CommandError` now claims: a re-run after an unresolved `duplicate_identity` changes no `CampaignRun` field and no `CalendarEvent`, and the `CommandError` text states the marker precondition instead of asserting the property unconditionally."
    - "[already covered by 35-06-PLAN.md must_haves] The command still never deletes a `CalendarEvent` on any path and still exits non-zero whenever an unexplained event remains."
  artifacts:
    - solsys_code/management/commands/cutover_classical_allocations.py
    - solsys_code/tests/test_cutover_classical_allocations.py
  key_links:
    - "`_extract_source_line()`'s result -> the guard's write authorisation. `None` moves to the REFUSING side, so a parse that found nothing never authorises a write. 35-VERIFICATION.md's Key Link table marks this seam 'WIRED, WRONG TRUST BOUNDARY'."
    - "`CampaignRunAdmin`'s editable `observation_details` (`solsys_code/admin.py:165`) -> the cutover's identity decision. 35-VERIFICATION.md's Data-Flow Trace marks this 'FLOWING FROM AN UNTRUSTED SOURCE'; after this plan the field can only DENY a conversion, never grant one."
    - "`TestDatabaseScopedIdentityGuard`'s four-case partition (merge / differing line / same line / no marker) -> the guard's two branches. The partition is correct and is kept; only the fourth case's expected outcome inverts, plus one added destructive-case regression."
  prohibitions:
    - "The cutover must NOT derive write authority from the ABSENCE of a `Source line:` marker in `CampaignRun.observation_details` -- a field `solsys_code/admin.py:165` leaves editable, `import_campaign_csv.py:321` writes from an arbitrary CSV column and `campaign_forms.py:65` exposes as a free-text Textarea."
    - "The cutover must NOT silently mutate a `CampaignRun` it did not create in this invocation. (Carried forward from 35-08, where it was minted correctly and then reproduced as violated.)"
    - "A guarantee statement inside this module must NOT assert the no-rewrite property without naming the marker precondition it depends on."
    - "The cutover must NOT delete a `CalendarEvent` on any path, including every failure path."
    - "A new test must NOT be added BESIDE the ALLOC-01 `empty` case that asserts conversion -- that case must be replaced. Leaving it green would keep the blocker pinned as correct behaviour, which is how 215 passing tests coexisted with a live BLOCKER."
    - "A `--dry-run` must NOT exit 0 over a fixture the immediately following real run rejects."
    - "Dropped as canon security/compliance boilerplate rather than minted here: credential-logging, TLS-downgrade, rate-limiting and authz-bypass prohibitions -- this plan touches a staff-only management command with no network call, no secret and no request path."
---

<objective>
Close CR-01, the Phase 35 BLOCKER: `cutover_classical_allocations`'s database-scoped identity
guard resolves who a claimant is by re-parsing that claimant's own `observation_details`, and
puts "no recoverable `Source line:` marker" on the PERMISSIVE side of the predicate. Because
`observation_details` is freely editable from the Django admin (and written by
`import_campaign_csv.py:321` and `campaign_forms.py:65`), an ordinary staff edit disarms the
guard and a one-time destructive migration silently overwrites every dispatch-deciding field on
an APPROVED run -- with exit 0, `unexplained: 0` and empty stderr.

Purpose: a migration that cannot prove it owns a row must refuse it, not write to it. This is
also the round where the FLAWED ASSUMPTION is corrected, not only the code: 35-08's plan
frontmatter carried the permissive predicate as a flagged, unresolved ALLOC-01 `empty`
assumption, the executor implemented it faithfully, and three must-have truths are literally
VERIFIED while specifying the defect. The must_haves above re-resolve that probe with the
reproduction as evidence.

Output: an inverted guard predicate with a branching, actionable reason string; the wrongly
asserting regression test replaced by its inverse; the destructive-case regression the suite
never had; and the two in-module guarantee statements corrected to name the marker precondition.

Wave 1, no dependencies. Plan 35-15 depends on this one: it narrows the operator runbook and
regenerates `reconcile_campaign_runs_demo.ipynb`, whose committed output prints this module's
`CommandError` text verbatim.
</objective>

<execution_context>
@/home/tlister/git/fomo_devel/.claude/gsd-core/workflows/execute-plan.md
@/home/tlister/git/fomo_devel/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md

@.planning/phases/35-allocation-layer-classical-cutover/35-VERIFICATION.md
@.planning/phases/35-allocation-layer-classical-cutover/35-REVIEW.md
@.planning/phases/35-allocation-layer-classical-cutover/35-08-SUMMARY.md

@solsys_code/management/commands/cutover_classical_allocations.py
@solsys_code/tests/test_cutover_classical_allocations.py
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Invert the identity guard's default and replace the test that pinned the blocker</name>
  <files>solsys_code/management/commands/cutover_classical_allocations.py, solsys_code/tests/test_cutover_classical_allocations.py</files>
  <read_first>
    - `.planning/phases/35-allocation-layer-classical-cutover/35-REVIEW.md` lines 149-249 (section CR-01) -- the executed PROBE-A reproduction and the full suggested predicate and reason body.
    - `.planning/phases/35-allocation-layer-classical-cutover/35-VERIFICATION.md` the `gaps:` entry whose `truth` begins "The cutover never silently mutates a `CampaignRun` it did not create in this invocation" -- its `missing:` list is this task's checklist.
    - `solsys_code/management/commands/cutover_classical_allocations.py` lines 383-430 -- the in-process `seen_keys` guard directly above, whose reason-text prefix shape the new branch must match, then the database-scoped guard itself.
    - `solsys_code/tests/test_cutover_classical_allocations.py` lines 1103-1235 -- `TestDatabaseScopedIdentityGuard`, its class docstring, `_make_pre_existing_claimant()` and all four current cases.
    - `solsys_code/admin.py` lines 160-175 -- `CampaignRunAdmin.readonly_fields` and `get_readonly_fields()`, the premise that makes `observation_details` untrusted input.
  </read_first>
  <behavior>
    - A claimant whose stored marker DIFFERS from the group's line: reported under `duplicate_identity`, non-zero exit, no field written. (Already true; must stay true.)
    - A claimant whose stored marker MATCHES the group's line: converted normally, events re-keyed to `ALLOC:{pk}:{night}`. (Already true; must stay true.)
    - A claimant with NO recoverable marker: reported under `duplicate_identity`, non-zero exit, no field written. (Currently converts -- this is the inversion.)
    - No claimant at all: converted normally. (Already true; must stay true.)
  </behavior>
  <action>
    In `solsys_code/management/commands/cutover_classical_allocations.py`, inside the
    database-scoped guard (the block that binds `existing_run` from
    `CampaignRun.objects.filter(source_identifier=key).first()` and then binds
    `existing_source_line` from `_extract_source_line(existing_run.observation_details)`):

    - Replace the membership test against the two-element tuple with a direct inequality
      between `existing_source_line` and `source_line`, so a parse result of `None` takes the
      refusing branch. This is the whole of the CR-01 fix in the predicate.
    - Build the reason string with a branch on `existing_source_line is not None`. When a
      marker WAS recovered, keep today's wording naming the claimant pk, the key, and that it
      is claimed for a different Source line. When no marker was recovered, state that the run
      holds the key with no recoverable `'Source line:'` marker in its `observation_details`,
      so this command cannot prove the run came from this schedule line, and direct the
      operator to restore or correct that run's `observation_details` `'Source line:'` text in
      the Django admin so it matches, or to disambiguate the two lines, then re-run. Keep the
      `f'{_REASON_LABELS[_DUPLICATE_IDENTITY]}: CampaignRun pk={existing_run.pk} already
      claimed {key!r} ...'` prefix shape that both existing `_DUPLICATE_IDENTITY` sites use, so
      the shared operator phrase the runbook mirrors (the NF-25 key link) still matches.
    - Rewrite the rationale comment immediately above the guard. It currently argues that a row
      with nothing to disagree with should be treated as the same line. It must instead state:
      such a row is precisely the row this one-time destructive migration cannot prove it owns;
      `observation_details` is writable from the Django admin (`solsys_code/admin.py:165`), from
      `import_campaign_csv.py:321` and from `campaign_forms.py:65`, so its absence is not
      evidence of agreement. Cite CR-01 and 35-REVIEW.md alongside the existing NF-14/NF-19
      citations.

    In `solsys_code/tests/test_cutover_classical_allocations.py`:

    - REPLACE the fourth case of `TestDatabaseScopedIdentityGuard` (the one whose name ends
      `_still_converts` and whose docstring calls the permissive predicate correct, currently at
      lines 1221-1235) with `test_pre_existing_claimant_with_no_recoverable_source_line_is_refused`.
      Keep its fixture exactly -- `self._make_pre_existing_claimant(observation_details='')` plus
      `self._make_three_night_group()` -- and invert the assertions to the shape
      `test_pre_existing_claimant_with_different_source_line_rejects_on_first_pass` (lines
      1177-1202) already uses: `call_command` raises `CommandError`; stdout contains
      `'unexplained (duplicate_identity): 3'`; each event still has `url == ''` and no
      `CalendarEventMeta` row exists for it. Do not leave the old method in the file.
    - Update the `TestDatabaseScopedIdentityGuard` class docstring (lines 1103-1114). Its closing
      clause currently describes the fourth case as converting under the permissive predicate.
      State instead that no recoverable marker must also REFUSE, because `observation_details`
      is operator-writable and absence is not agreement, and cite CR-01.
    - Do NOT touch `test_pre_existing_claimant_with_same_source_line_still_converts` (line 1204).
      That benign path must keep passing; over-rejecting it would break the cutover-after-import
      ordering WR-11 pins.

    <!-- planner-discipline-allow: test_pre_existing_claimant_with_no_recoverable_source_line_still_converts -->
  </action>
  <reversibility rating="reversible">The change only makes the command refuse more: it deletes nothing, writes nothing new, and carries no schema or data migration. An operator who hits the new refusal restores the `Source line:` marker in the Django admin and re-runs. Verified against the shipped write paths: `load_telescope_runs.py` and this command both compose `observation_details` as `f'Status: {status}\nSource line: {line}'`, so every run the pipeline itself created still carries a recoverable marker and still converts.</reversibility>
  <verify>
    <automated>python manage.py test solsys_code.tests.test_cutover_classical_allocations.TestDatabaseScopedIdentityGuard</automated>
    <fails_when>The runner prints `FAILED (failures=` or `FAILED (errors=` and exits non-zero, or the output does not contain `Ran 4 tests` because the fourth case was added beside the old one instead of replacing it.</fails_when>
    <automated>test "$(grep -v '^ *#' solsys_code/management/commands/cutover_classical_allocations.py | grep -c 'not in (None, source_line)')" = "0"</automated>
    <fails_when>Exits 1 because the count printed by the inner grep is 1 or more -- the permissive membership test still exists in the executable body of the module.</fails_when>
    <automated>grep -c 'test_pre_existing_claimant_with_no_recoverable_source_line_is_refused' solsys_code/tests/test_cutover_classical_allocations.py</automated>
    <fails_when>Prints `0` -- the inverted case was never written under its new name.</fails_when>
  </verify>
  <acceptance_criteria>
    - `python manage.py test solsys_code.tests.test_cutover_classical_allocations.TestDatabaseScopedIdentityGuard` prints `Ran 4 tests` and `OK`.
    - The guard's predicate is a direct inequality against `source_line`; `grep -v '^ *#' solsys_code/management/commands/cutover_classical_allocations.py | grep -c 'not in (None, source_line)'` prints `0`.
    - `solsys_code/tests/test_cutover_classical_allocations.py` contains `test_pre_existing_claimant_with_no_recoverable_source_line_is_refused` and does not contain a `TestDatabaseScopedIdentityGuard` case that asserts a no-marker claimant converts.
    - The no-marker reason string contains the literal `Source line:` and the words `Django admin`, and names the claimant's pk.
    - `test_pre_existing_claimant_with_same_source_line_still_converts` is unmodified and passes.
  </acceptance_criteria>
  <done>A database claimant the command cannot prove it owns is reported and the command exits non-zero; the only regression test that asserted otherwise no longer exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the destructive-case regression the suite never had</name>
  <files>solsys_code/tests/test_cutover_classical_allocations.py</files>
  <read_first>
    - `.planning/phases/35-allocation-layer-classical-cutover/35-REVIEW.md` lines 184-207 -- the PROBE-A block: the exact fixture and the exact fields that changed under it.
    - `.planning/phases/35-allocation-layer-classical-cutover/35-VERIFICATION.md` gap 1's third `missing:` item -- the required assertion shape, stated verbatim there.
    - `solsys_code/tests/test_cutover_classical_allocations.py` lines 101-118 (`_make_three_night_group()`, `_ALLOCATION_LINE`, `_CANCELLED_LINE`) and lines 1116-1134 (`_make_pre_existing_claimant()`).
    - `solsys_code/management/commands/cutover_classical_allocations.py` lines 447-466 -- the `fields` dict a merge writes, so the new test asserts on the fields that would actually change.
  </read_first>
  <behavior>
    - Given a claimant holding the derived key with `run_status=PLANNED`, a real `Target`, and an `observation_details` value that is a staff note with no marker;
    - and a group whose schedule line is the `cancelled` counterpart of the claimant's line (same telescope, instrument and window, so `_source_identifier()` resolves the same key because it ignores the parsed status word);
    - the command exits non-zero, re-keys zero events, and leaves `run_status`, `observation_details` and `target_id` exactly as they were.
  </behavior>
  <action>
    Add `test_no_marker_claimant_keeps_run_status_details_and_target_byte_identical` to
    `TestDatabaseScopedIdentityGuard`, reproducing 35-REVIEW.md's PROBE-A:

    - Build the claimant with
      `self._make_pre_existing_claimant(observation_details='Rescheduled per PI request; see ticket OPS-4412.')`.
      Use a realistic staff note, not the empty string the Task 1 case uses: PROBE-A showed the
      empty-string fixture makes every overwritten field invisible to the assertions, which is
      exactly why the suite green-lit the blocker.
    - Give the claimant a real target so the merge's `'target': None` write would be observable:
      `existing_run.target = NonSiderealTargetFactory()` then
      `existing_run.save(update_fields=['target'])`. Use `NonSiderealTargetFactory` (already
      imported at line 24) and never the sidereal factory -- FOMO is exclusively for Solar
      System / non-sidereal targets, so a sidereal fixture misrepresents what the code
      handles (CLAUDE.md).
    - Capture `existing_run.run_status`, `existing_run.observation_details` and
      `existing_run.target_id` into locals BEFORE the command runs.
    - Build the colliding group with `self._make_three_night_group(source_line=self._CANCELLED_LINE)`.
      `_CLASSICAL_RUN_STATUS` maps that line's status word to CANCELLED, so a merge would flip
      `run_status` -- the harm PROBE-A reproduced.
    - Assert `call_command('cutover_classical_allocations', stdout=out, stderr=StringIO())` raises
      `CommandError`.
    - After `existing_run.refresh_from_db()`, assert each of the three captured values with
      `assertEqual`, and assert `out.getvalue()` contains `'events re-keyed: 0'`.
    - Assert each of the three events still has `url == ''` and no `CalendarEventMeta` row, so
      nothing was re-keyed onto the claimant.
    - The docstring must state that this is the assertion shape the pre-CR-01 suite lacked, and
      that no test asserted `run_status`/`observation_details`/`target` were unchanged after a
      refused pass.
  </action>
  <verify>
    <automated>python manage.py test solsys_code.tests.test_cutover_classical_allocations.TestDatabaseScopedIdentityGuard</automated>
    <fails_when>The output omits `Ran 5 tests` (the new case was not added), or prints `FAILED (failures=` because the refreshed `run_status` came back as `cancelled` rather than `planned` -- meaning the guard still permits the merge.</fails_when>
    <automated>test "$(grep -c 'NonSiderealTargetFactory' solsys_code/tests/test_cutover_classical_allocations.py)" -ge "4"</automated>
    <fails_when>Exits 1 because the count is still `3` (its value before this task) -- the new test built its `Target` some other way, and CLAUDE.md requires the non-sidereal factory for every `Target` fixture in this project.</fails_when>
  </verify>
  <acceptance_criteria>
    - `python manage.py test solsys_code.tests.test_cutover_classical_allocations.TestDatabaseScopedIdentityGuard` prints `Ran 5 tests` and `OK`.
    - The new method asserts all three of `run_status`, `observation_details` and `target_id` with `assertEqual` against values captured before `call_command`.
    - The claimant's `target` is created with `NonSiderealTargetFactory`.
    - The command's stdout under this fixture contains `events re-keyed: 0`.
    - The test's `observation_details` fixture is a non-empty staff note containing no `Source line:` substring.
  </acceptance_criteria>
  <done>The suite now fails loudly if the no-marker merge ever returns: a single test asserts both the non-zero exit and that every field PROBE-A destroyed is untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Correct the two in-module guarantee statements and drop the shadowed re-query</name>
  <files>solsys_code/management/commands/cutover_classical_allocations.py</files>
  <read_first>
    - `solsys_code/management/commands/cutover_classical_allocations.py` lines 36-70 -- the module docstring's `duplicate_identity` paragraph, and lines 645-666 -- the summary writes and the `CommandError`.
    - `.planning/phases/35-allocation-layer-classical-cutover/35-VERIFICATION.md` gap 1's fourth `artifacts` entry (the one naming the module docstring and the `CommandError` text) and its fourth `missing:` item.
    - `.planning/phases/35-allocation-layer-classical-cutover/35-VERIFICATION.md` the advisory entry `IN-01`, which names the redundant `existing_run` re-query and recommends folding its fix into the CR-01 change.
    - `solsys_code/management/commands/cutover_classical_allocations.py` lines 414 and 521-528 -- the two `existing_run` bindings IN-01 describes.
  </read_first>
  <action>
    - Module docstring, the `duplicate_identity` sentence that currently asserts the guarantee
      applies to every invocation and not only the first because the check reads the database:
      rewrite it to state the precondition. The database check catches a claimant left by an
      earlier cutover pass or by `load_telescope_runs` WHEN that claimant's stored `Source line:`
      is recoverable and matches; a claimant whose marker is absent or differs is reported under
      `duplicate_identity` instead of converted, because `observation_details` is writable from
      the Django admin and this command cannot prove such a row came from the line in hand. Keep
      the existing NF-14/NF-19 citations and add CR-01.
    - The `CommandError` raised when unexplained events remain: its closing clause currently
      promises a repeat pass modifies no pre-existing `CampaignRun`. Replace that clause with the
      true statement -- a repeat pass converts nothing it has not already explained, and updates
      an existing `CampaignRun` only when that run's stored `Source line:` matches the line being
      converted; anything else is reported. Keep it one self-contained sentence: this string is
      printed verbatim into `docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb`'s
      committed cell output, which plan 35-15 regenerates against it.
    - IN-01 (advisory, named by the verifier as worth folding in here because it sits in the same
      predicate's surroundings): delete the redundant
      `CampaignRun.objects.filter(source_identifier=key).first()` re-query inside the `if dry_run:`
      arm of the group transaction and use the `existing_run` already bound by the guard. Nothing
      between the two bindings creates a run holding that key, so the reads are equivalent;
      removing the second removes a shadowed name and one query per group.
  </action>
  <verify>
    <automated>test "$(grep -v '^ *#' solsys_code/management/commands/cutover_classical_allocations.py | grep -c 'holds on every invocation')" = "0"</automated>
    <fails_when>Exits 1 because the inner count is not `0` -- the module docstring still asserts the guarantee unconditionally.</fails_when>
    <automated>test "$(grep -v '^ *#' solsys_code/management/commands/cutover_classical_allocations.py | grep -c 'rewrites no existing CampaignRun')" = "0"</automated>
    <fails_when>Exits 1 because the inner count is not `0` -- the `CommandError` still promises the unconditional property.</fails_when>
    <automated>test "$(grep -c 'CampaignRun.objects.filter(source_identifier=key).first()' solsys_code/management/commands/cutover_classical_allocations.py)" = "1"</automated>
    <fails_when>Exits 1 because the count is `2` (the shadowed re-query survives) or `0` (the guard's own lookup was deleted by mistake).</fails_when>
    <automated>python manage.py test solsys_code.tests.test_cutover_classical_allocations</automated>
    <fails_when>Exits non-zero with `FAILED (errors=` -- for example a `NameError` or `UnboundLocalError` from the removed re-query -- or `FAILED (failures=` from `TestDryRunAndRealRunAgree`, meaning the dry-run and real passes no longer report identical counts.</fails_when>
  </verify>
  <acceptance_criteria>
    - Both negative greps above print `0`.
    - The module docstring's `duplicate_identity` paragraph contains the literal `Source line:` and names it as a precondition of the no-rewrite property, and cites `CR-01`.
    - The `CommandError` text names the same precondition and remains a single sentence with no embedded newline.
    - Exactly one `CampaignRun.objects.filter(source_identifier=key).first()` call remains in the module.
    - `python manage.py test solsys_code.tests.test_cutover_classical_allocations` prints `OK`.
  </acceptance_criteria>
  <done>Every guarantee this module states about itself is true of the code it ships with, including the sentence its own demo notebook prints.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Django admin staff user -> `CampaignRun.observation_details` | `CampaignRunAdmin` (`solsys_code/admin.py:165`) sets `readonly_fields = ['approval_status']` and `get_readonly_fields()` withholds only `source`, so a staff member can rewrite this free-text field on any run, classical ones included. This command's own remedy text sends operators here. |
| `import_campaign_csv` CSV file -> `CampaignRun.observation_details` | `import_campaign_csv.py:321` writes the field from an arbitrary `Observation Details` column of an operator-supplied file. |
| Campaign submission form -> `CampaignRun.observation_details` | `campaign_forms.py:65` exposes the field as a `forms.CharField(widget=forms.Textarea)`. |
| `observation_details` -> the cutover's identity/authorisation decision | The guard re-parses the field to decide whether it may perform a full find-and-update on an APPROVED run. This is the boundary CR-01 crosses in the wrong direction. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-18 | Elevation of Privilege | `cutover_classical_allocations.py` database-scoped identity guard | high | mitigate | Absence of a `Source line:` marker in an admin/CSV/form-writable `TextField` currently authorises a full find-and-update of an APPROVED `CampaignRun`. Task 1 inverts the predicate to a direct inequality so the command refuses whatever it cannot prove it owns; after this plan no code path derives write authority from the field's absence. |
| T-35-19 | Tampering | same guard, plus the `fields` dict it gates | high | mitigate | Editing `observation_details` re-points identity resolution at a different schedule line, overwriting `run_status`, `campaign`, `window_start`/`window_end`, `site`, `site_raw`, `target` and the note itself, then re-keying that line's events onto the run. Mitigated by the same inversion plus Task 2's byte-identical regression, which asserts a refused pass writes nothing. |
| T-35-20 | Repudiation | the summary writes and the `CommandError` | medium | mitigate | The destructive write completes with exit 0, `unexplained: 0` and empty stderr, leaving no operator-visible record that a run was overwritten. Task 1's no-marker reason string names the claimant pk and the exact admin field to restore, and the non-zero exit makes the refusal auditable; Task 3 stops the `CommandError` asserting a property the code does not hold. |
| T-35-21 | Information Disclosure | the reason string printed to stdout/stderr | low | accept | The string echoes the claimant's pk and the derived identity key into the output of a staff-only, locally-run management command. No secret, credential or personal datum is added beyond what the existing summary already prints, and ASVS level 1 requires no further control for a local CLI with no request path. |
| T-35-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no package: no `pip install` task, no change to `pyproject.toml`, no new import of a third-party module. The package-legitimacy gate does not fire and no `[ASSUMED]`/`[SUS]` package checkpoint is required. |
</threat_model>

<verification>
Run all of the following from the repository root before considering the plan complete:

1. `python manage.py test solsys_code.tests.test_allocation_projector solsys_code.tests.test_campaign_reconciler solsys_code.tests.test_cutover_classical_allocations solsys_code.tests.test_load_telescope_runs solsys_code.tests.test_observation_projector_signals` -- the 215-test regression suite the iteration-5 code review and the verifier both ran clean. Expect `OK` and a count no lower than 216 (215 plus Task 2's addition; Task 1 replaces rather than adds).
2. `pre-commit run ruff --all-files` -- expect `Passed`.
3. `pre-commit run ruff-format --all-files` -- expect `Passed`.
4. `git status --short` -- expect no unexpected modified file beyond the two in `files_modified` and `.planning/`.
</verification>

<success_criteria>
- The ALLOC-01 `empty` case asserts refusal, and `TestDatabaseScopedIdentityGuard` reports `Ran 5 tests ... OK`.
- Re-running 35-REVIEW.md's PROBE-A fixture (Task 2's regression is that fixture) produces a non-zero exit and `events re-keyed: 0` instead of `updated: 1, events re-keyed: 3` with exit 0.
- No statement in `cutover_classical_allocations.py` asserts the no-rewrite guarantee unconditionally.
- Both ruff gates stay clean (D-07).
</success_criteria>

## Artifacts this phase produces

| Artifact | Kind | Where |
|----------|------|-------|
| `test_pre_existing_claimant_with_no_recoverable_source_line_is_refused` | test method (replaces the case that asserted conversion) | `solsys_code/tests/test_cutover_classical_allocations.py`, class `TestDatabaseScopedIdentityGuard` |
| `test_no_marker_claimant_keeps_run_status_details_and_target_byte_identical` | new test method | `solsys_code/tests/test_cutover_classical_allocations.py`, class `TestDatabaseScopedIdentityGuard` |
| Branching `duplicate_identity` reason string (marker-recovered vs no-marker) | new operator-facing message text | `solsys_code/management/commands/cutover_classical_allocations.py`, database-scoped guard |
| Corrected module docstring `duplicate_identity` paragraph | documentation | `solsys_code/management/commands/cutover_classical_allocations.py` module docstring |
| Corrected `CommandError` closing clause | operator-facing message text, quoted verbatim by `reconcile_campaign_runs_demo.ipynb` | `solsys_code/management/commands/cutover_classical_allocations.py` |

No new module, class, migration, dependency or management command. No schema change.

<output>
Create `.planning/phases/35-allocation-layer-classical-cutover/35-12-SUMMARY.md` when done.
</output>
