---
phase: 35-allocation-layer-classical-cutover
plan: 25
subsystem: allocation-calendar
tags: [documentation, runbook, jupyter-notebook, paired-docs, remint_declined, cr-04, cr-05, wr-06, wr-05, wr-08]

# Dependency graph
requires:
  - phase: 35 (plans 35-23, 35-24)
    provides: "the remint_declined counter split, the CR-04 label-refresh fall-through, the CR-05 retirement-branch guard (35-23); the v3 site-position-fingerprint token, the WR-05 dark-window refresh, the WR-07 qualified cost bound, and the WR-08 is_verified documentation (35-24) -- the four behaviour changes this plan documents"
provides:
  - "docs/runbooks/telescope_runs_calendar.rst's counter section rewritten across seven passages to describe all four of this round's behaviour changes, in one pass against the final tree"
  - "docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb regenerated by re-execution with real executed cells for all four behaviour changes"
affects: []

# Actuals (#2632)
actuals:
  tokens: 19966
  tasks: 2
  commits: 2
plan_head_before: 6f65b23109419f00a3629982c851d0f8d302b910

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Round-granularity paired-docs discharge, repeated from plan 35-22: the paired-docs obligation for a multi-plan gap-closure round is satisfied ONCE, in the plan that runs last, against the final state of the tree -- not once per code plan. Plans 35-23 and 35-24 were both forbidden from touching docs/; this plan wrote every runbook passage once (describing all four behaviour changes together, consistently) and re-executed the notebook once (exercising all four in the same run)."

key-files:
  created: []
  modified:
    - docs/runbooks/telescope_runs_calendar.rst
    - docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb

key-decisions:
  - "Extended the existing declined-re-mint notebook cell (rather than adding a fully separate cell pair) to cover CR-04's title-refresh property in the same edit that clears the sub-night window override -- the existing cell already demonstrated CR-01's boundary/confirmation preservation; adding run_status=CANCELLED to the same save() call demonstrates CR-04's labelling-refresh guarantee on the identical night, matching TestDeclinedRemintStillUpdatesLabels's own fixture shape, without duplicating state setup."
  - "The pre-existing site-correction demo (CR-02, added in plan 35-22) was checked against WR-05's finding rather than assumed stale: its fixture (classical_run) has no night_start_utc/night_end_utc set at all, so it is the null/null shape whose behaviour the demo's prose already correctly describes. Left unedited -- the fully-set shape WR-05 found broken is instead covered by this plan's new escalated-decision demo, which seeds its own dedicated single-night run with a resolved site to avoid conflating the two mechanisms."
  - "The escalated-decision demo (plan 35-24) seeds a dedicated Observatory (obscode X32) and a dedicated single-night CampaignRun, rather than reusing ground_site (shared by classical_run/queue_run/declined_run/confirmed_retire_run) -- editing a shared site's position in place would re-mint every other demo's already-minted nights too, making the before/after assertions ambiguous about which correction caused which change."
  - "The CR-05 retirement-guard demo also seeds a dedicated single-night run (confirmed_retire_run) rather than reusing declined_run from the CR-04 demo -- avoids compounding state across demos and mirrors TestRetirePathAllocationEventGuard's own fixture shape, matching plan 35-22's stated convention for this notebook."

requirements-completed: [ALLOC-01, ALLOC-02, ALLOC-03, ALLOC-04, ALLOC-05]

coverage:
  - id: D1
    description: "The runbook's counter section describes what the sweep does after this round across seven passages: remint_declined added to the counter-list sentence and both example summary lines; retired reason (2) rewritten into three distinct site-correction cases (empty/half-set re-mints, fully-set same-timezone only refreshes the dark-window line, fully-set cross-timezone fails the reconcile with a stated remedy); retired reason (5) extended to this release's repeat of the provenance audit and the new site-position input; a new paragraph on correcting a site's own definition; detach_declined narrowed to confirmation-only causes plus the new CR-05 declined-retirement case with its duplicate-entry consequence and remedy; a new remint_declined paragraph naming all three causes, the updated/unchanged counter pairing, the repeating-warning note, and the is_verified field's exact shipped label; and the post-upgrade deploy note extended for this release."
    requirement: ALLOC-01
    verification:
      - kind: other
        ref: "grep -c 'remint_declined' docs/runbooks/telescope_runs_calendar.rst == 6 (>= 5 required); grep -c 'left attributed -- a person confirmed' solsys_code/management/commands/reconcile_campaign_runs.py == 1 (command wording untouched, prohibition 1 of plan 35-23 held); git status --short over solsys_code/, src/, docs/notebooks/ empty at the end of Task 1"
        status: pass
    human_judgment: false
  - id: D2
    description: "reconcile_campaign_runs_demo.ipynb's declined-re-mint cell now demonstrates CR-01 (boundary/pk/confirmation preserved) AND CR-04 (the [CANCELLED] title marker still lands, reported alongside remint_declined and updated==1 on the same ReconcileResult) in one extended cell -- a declined night's labelling keeps refreshing even though its boundary rewrite is declined."
    requirement: ALLOC-01
    verification:
      - kind: other
        ref: "docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb, extended cell after 'A declined re-mint declines only the boundary rewrite, and survives its own retirement' -- real executed assertions (remint_declined==1, detach_declined==0, updated==1, retired==0, created==0, pk/boundaries unchanged, title changed and starts with [CANCELLED]), non-null execution_count"
        status: pass
    human_judgment: false
  - id: D3
    description: "reconcile_campaign_runs_demo.ipynb's new confirmed-retirement cell exercises plan 35-23's CR-05 guard end to end: a human-confirmed allocation night survives the deletion an otherwise-retiring ObservationRecord link would trigger, firing from the CampaignRunObservation post_save receiver alone with no explicit reconcile_run() call, asserting retired==0/detach_declined==1 and the event/companion-row/confirmation all still existing afterward."
    requirement: ALLOC-03
    verification:
      - kind: other
        ref: "docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb, cell 'A confirmed allocation night survives its own retirement (plan 35-23, CR-05)' -- real executed assertions, non-null execution_count"
        status: pass
    human_judgment: false
  - id: D4
    description: "reconcile_campaign_runs_demo.ipynb's new command-level cell proves WR-06's split with a genuine summary line: the real reconcile_campaign_runs management command (not reconcile_run() called directly) is run against the whole scratch database, and the cell asserts the printed remint_declined token and the exact per-run stderr message an operator would read."
    requirement: ALLOC-05
    verification:
      - kind: other
        ref: "docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb, cell 'The remint_declined counter in a real summary line (plan 35-23, WR-06)' -- real executed call_command() output, asserted against"
        status: pass
    human_judgment: false
  - id: D5
    description: "reconcile_campaign_runs_demo.ipynb's new escalated-decision cell reproduces the round-5 verifier's own probe: an in-place Observatory position/timezone correction, with the dedicated run's site never reassigned, re-mints the already-projected night to the corrected position's real sun_event() boundary (retired==1, created==1, new primary key) -- the exact fix plan 35-24 shipped."
    requirement: ALLOC-01
    verification:
      - kind: other
        ref: "docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb, cell 'An in-place site-definition correction re-mints (plan 35-24, the escalated decision)' -- real executed assertions including an idempotent-reconcile control before the correction, non-null execution_count"
        status: pass
    human_judgment: false
  - id: D6
    description: "The round's code surface is still green behind the documentation pass: the seven-module regression (324 tests, matching plan 35-24's recorded count exactly, no lower), both ruff hooks, and makemigrations --check --dry-run all pass at the END of the round -- and no source file or the developer database was ever touched (git status --short over solsys_code/ and src/ empty throughout)."
    requirement: ALLOC-01
    verification:
      - kind: integration
        ref: "python manage.py test solsys_code.tests.test_allocation_projector solsys_code.tests.test_allocation_projector_signals solsys_code.tests.test_campaign_reconciler solsys_code.tests.test_reconcile_campaign_runs solsys_code.tests.test_cutover_classical_allocations solsys_code.tests.test_load_telescope_runs solsys_code.tests.test_campaign_views -- 324 tests, OK"
        status: pass
    human_judgment: false

# Metrics
duration: ~90min
completed: 2026-09-16
status: complete
---

# Phase 35 Plan 25: Paired Docs for the Round (CR-04/CR-05/WR-05/WR-06/WR-08 and the Escalated Decision) Summary

**The runbook's counter section (seven passages) and the paired `reconcile_campaign_runs_demo.ipynb` notebook (four new demos) now describe and prove, in real executed output, every behaviour change plans 35-23 and 35-24 shipped this round -- the `remint_declined` counter split, the declined-re-mint label refresh, the confirmed-retirement guard, the dark-window-line refresh, and the in-place site-definition correction.**

## Performance

- **Duration:** ~90 min
- **Started:** 2026-09-16 (immediately following plan 35-24)
- **Completed:** 2026-09-16
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments

- **Runbook counter section rewritten across seven passages** (Task 1): `docs/runbooks/telescope_runs_calendar.rst`'s counter-list sentence and both example summary lines now carry `remint_declined`. `retired` reason (2) no longer states the unqualified "a site correction re-mints" claim WR-05 showed false for a fully-set run -- it now names three distinct cases (empty/half-set window re-mints as before; a fully-set same-timezone correction only refreshes the event's dark-window line, nothing retired; a fully-set cross-timezone correction fails the whole run's reconcile, with the remedy being to correct the sub-night window together with the site). `retired` reason (5) is extended to state that this release repeats the one-time provenance audit for every night still carrying the `v2`-format token, now also checking the site's own stored position and timezone. A new paragraph states that correcting a site's own DEFINITION (not a run's `site` field) now re-mints every night already projected there, and states plainly what happened before this release: nothing, silently, wrong by up to fifteen hours. `detach_declined` is narrowed to confirmation-only causes and gains the CR-05 declined-retirement case, stated with its duplicate-entry consequence (both the confirmed allocation night and the linked observation's own entry visible until the confirmation is cleared) and its remedy. A new `remint_declined` paragraph names all three causes (confirmation, observation link, unverified companion row), states the deliberate `updated`/`unchanged` counter pairing and why, states the warning repeats until resolved, and quotes the `is_verified` field's exact shipped `verbose_name` from plan 35-24's SUMMARY verbatim. The deploy note is extended to cover this release's repeat audit and how to read a non-zero `remint_declined` on a post-upgrade `--dry-run`.
- **Notebook regenerated with real proof for all four behaviour changes** (Task 2): the existing declined-re-mint cell (plan 35-20's CR-01 demo) is extended to also flip `run_status` to `CANCELLED` in the same edit that clears the sub-night window override, proving CR-04's fall-through (the `[CANCELLED]` marker still lands on the title, reported as `updated: 1` alongside `remint_declined: 1` on the same `ReconcileResult`, with the primary key, both boundaries and the confirmation stamp all unchanged). A new dedicated-run cell demonstrates CR-05: linking a placed-block `ObservationRecord` to a confirmed allocation night no longer deletes it -- the guard fires from the `CampaignRunObservation` `post_save` receiver alone, with `retired==0` and `detach_declined==1`. A new command-level cell runs the actual `reconcile_campaign_runs` management command (not `reconcile_run()` called directly) and asserts the real `remint_declined: N` token in its summary line plus the exact per-run stderr message an operator would read (WR-06). A new dedicated-site cell reproduces the round-5 verifier's own probe: an in-place `Observatory` position correction, with the demo run's `site` never reassigned, re-mints the already-projected night to the corrected position's real `sun_event()` boundary (`retired==1, created==1`, new primary key) -- with an idempotent-reconcile control run immediately before the correction, proving the re-mint is attributable to the correction alone.
- **Stale prose corrected, not silently left** (Task 2): the "Six counters on ReconcileResult" markdown cell, which still described `detach_declined` as covering two meanings pre-split, was rewritten to describe the current `detach_declined`/`remint_declined`/`is_verified` picture, and the Summary cell was rewritten to describe all four new demos. The pre-existing site-correction demo (CR-02, plan 35-22) was checked against WR-05's finding rather than assumed correct or assumed stale: its fixture (`classical_run`) has no `night_start_utc`/`night_end_utc` set at all -- the null/null shape whose behaviour its existing prose already correctly describes -- so it was left unedited; the fully-set shape WR-05 actually found broken is what this plan's new escalated-decision demo covers, using its own dedicated run.
- **The round's second pairing re-checked mechanically, not assumed**: `campaign_lifecycle_demo.ipynb`'s three grounds for being unaffected by this round (no `django.contrib.messages`/`ReconcileResult` counter reads, no fixture producing a declined re-mint) were re-checked per Task 2's own verify block -- `grep -c 'remint_declined' docs/notebooks/pre_executed/campaign_lifecycle_demo.ipynb` returns `0`, and `git status --short` over that file is empty. All three grounds hold; the notebook was left untouched, as `35-23-PLAN.md`'s `<artifacts>` block predicted.
- **Round-closing gates all green**: the full seven-module regression is 324/324, matching plan 35-24's recorded count exactly (no lower); both `pre-commit run ruff --all-files` and `ruff-format --all-files` pass (the notebook needed one re-execution after `ruff-format` reformatted its embedded Python source on the first pass -- matching plan 35-22's precedent -- and a second ruff/ruff-format pass then confirmed idempotency); `python manage.py makemigrations --check --dry-run` reports "No changes detected"; `solsys_code.tests.test_projector_demo_notebook` passes unchanged (it asserts on the *other* pre-executed notebook, `project_observation_calendar_demo.ipynb`, and was unaffected by this plan); every code cell in the regenerated notebook carries a non-null `execution_count`; `git status --short -- solsys_code/ src/` printed nothing at any point in this plan's run -- this plan changed exactly the two documentation files named in `files_modified`.

## Task Commits

Each task was committed atomically:

1. **Task 1: Correct the operator runbook against the round's final behaviour, end to end from counter to remedy** -- `b61f9da` (docs): seven runbook passages rewritten.
2. **Task 2: Regenerate the demo notebook with real executed output for all four behaviour changes** -- `6f2f29d` (docs): four new/extended demo cell-pairs, two stale prose cells corrected, notebook re-executed twice (once for the new cells, once after `ruff-format` reformatted the embedded source).

**Plan metadata:** this commit (docs: complete plan) -- see final commit below.

## Files Created/Modified

- `docs/runbooks/telescope_runs_calendar.rst` -- counter-list sentence and both example summary lines (`remint_declined` added); `retired` reasons (2) and (5) rewritten/extended; a new site-definition-correction paragraph; `detach_declined` narrowed with the CR-05 case added; a new `remint_declined` paragraph including the `is_verified` sentence; the post-upgrade deploy note extended. The `rekeyed` paragraph, `legacy_deleted`, `skipped_nights`/`detached` (beyond the counter-list line) and every quoted `detach_declined` message wording are byte-for-byte unchanged (confirmed by `git diff` against `HEAD` for the `rekeyed` paragraph specifically -- see Deviations below for the one verify-script mismatch this uncovered).
- `docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb` -- the "Six counters" and "Summary" markdown cells rewritten; the existing declined-re-mint cell extended for CR-04; three new demo cell-pairs added (CR-05, WR-06, the escalated decision); regenerated by `jupyter nbconvert --to notebook --execute --inplace` twice (once after adding the cells, once more after `ruff-format` reformatted the embedded code, so the committed output matches the exact final source, matching plan 35-22's precedent).

## Decisions Made

See `key-decisions` in frontmatter. Summary: extended the existing declined-re-mint cell rather than duplicating it, since CR-01 and CR-04 are two properties of the same fixture shape; verified the pre-existing site-correction demo's fixture is null/null (its prose is correct, left unedited) rather than assuming WR-05's finding applied to it; seeded dedicated `Observatory`/`CampaignRun` rows for both the CR-05 and escalated-decision demos to avoid conflating multiple corrections' before/after state.

## Deviations from Plan

### Not fixed -- documented plan verify-script mismatch, not a code defect

**1. [Plan verify-script mismatch] Task 1's second `<automated>` check (`grep -n 'rekeyed. counts a night carried across' docs/runbooks/telescope_runs_calendar.rst`) prints nothing and exits non-zero, even though the `rekeyed` paragraph is byte-identical to `HEAD` (prohibition 4 held)**
- **Found during:** re-running Task 1's own verify block after committing.
- **Issue:** the runbook's actual text reads ` ``rekeyed`` counts a night carried across...` -- TWO backtick characters (RST inline-literal markup) between "rekeyed" and " counts". The plan's grep pattern uses `rekeyed. counts` -- a single-character wildcard (`.`) -- which can only match ONE character there, not two. Confirmed with `git show HEAD:docs/runbooks/telescope_runs_calendar.rst | grep -n 'rekeyed. counts a night carried across'` (exit 1, no match) that this same pattern already failed to match the file BEFORE this plan touched it at all -- the regex has a pre-existing off-by-one bug unrelated to any edit in this plan.
- **Verification that the substantive property still holds:** `diff <(git show HEAD:docs/runbooks/telescope_runs_calendar.rst | sed -n '1104,1108p') <(sed -n '1140,1144p' docs/runbooks/telescope_runs_calendar.rst)` returns no differences -- the `rekeyed` paragraph (including its opening sentence) is byte-for-byte unchanged by this plan, which is the actual thing prohibition 4 and this check exist to guard.
- **Not fixed because:** the mismatch is in the plan's own verify SCRIPT, not in the runbook text or this plan's edits -- the same class of finding as plan 35-23's own documented verify-script mismatch (deviation 2 in `35-23-SUMMARY.md`). Editing the `rekeyed` paragraph to make a wildcard-count-off regex match would violate prohibition 4 itself; the correct fix is a future regex correction to the PLAN, not a runbook edit.
- **Recommendation:** flag for `/gsd-verify-work` and the plan-checker as a stale single-char-wildcard regex in this plan's own `<verify>` block (needs `rekeyed.. counts` or an escaped double-backtick), not as unfinished work.

---

**Total deviations:** 0 auto-fixed, 1 documented plan/verify-script mismatch (no code or documentation change, analysis only).
**Impact on plan:** No scope creep, no code change. The one mismatch has zero functional impact -- the guarded property (the `rekeyed` paragraph is present and unedited) is independently confirmed true by direct diff against `HEAD`.

## Issues Encountered

None beyond the deviation above.

## `35-21-PLAN.md:24`'s superseded historical claim (WR-05's third disagreeing claim)

WR-05 (`35-REVIEW.md`) named three claims that disagreed with the code's actual behaviour for a fully-set sub-night window run corrected to a different site. Two are corrected in the tree: the runbook's `retired` reason (2), rewritten by this plan's Task 1 into three distinct cases; and `_span_needs_remint()`'s own step-2 docstring, corrected by plan 35-24. The third is `35-21-PLAN.md:24`'s own stated truth: *"a site correction re-mints every one of its nights"* -- unqualified, and false for the fully-set-window shape WR-05 found.

**`35-21-PLAN.md:24` is deliberately left unedited.** A completed plan is a historical record of what was planned and believed true at the time it ran, not a live statement of current system behaviour -- rewriting it after the fact would erase the trail that let WR-05 be found and fixed in the first place. Its correct current form lives in two places instead: this plan's Task 1 rewrite of `retired` reason (2) in `docs/runbooks/telescope_runs_calendar.rst`, and plan 35-24's rewrite of `_span_needs_remint()`'s step-2 docstring in `solsys_code/allocation_projector.py`. Anyone reading `35-21-PLAN.md:24` today should read it alongside those two, not in place of them.

## `campaign_lifecycle_demo.ipynb` pairing check

Re-checked mechanically per Task 2's own verify block, not assumed from `35-23-PLAN.md`'s `<artifacts>` argument: `grep -c 'remint_declined' docs/notebooks/pre_executed/campaign_lifecycle_demo.ipynb` returns `0`; `git status --short -- docs/notebooks/pre_executed/campaign_lifecycle_demo.ipynb` prints nothing. All three grounds `35-23-PLAN.md` gives (no `django.contrib.messages`/`ReconcileResult` counter reads in that notebook, `owned_events` the only name it imports from `campaign_reconciler`, no fixture in it can produce a declined re-mint) hold. The notebook was left untouched; no Rule 1 deviation was needed.

## Round Completion Ledger

**The round IS complete at the end of this plan.** Every entry `35-23-PLAN.md`'s `<review_dispositions>` ledger marks "Closed this round" is closed, where stated:

| Entry | Closed by |
|---|---|
| CR-04 (declined re-mint also declines the non-destructive refresh) | Plan 35-23, Task 2 |
| CR-05 (retirement branch's own delete unguarded) | Plan 35-23, Task 3 |
| WR-06 (`detach_declined`'s two messages claim one of two causes) | Plan 35-23, Task 1 (counter split) |
| IN-05 (`active_urls.add(url)` no-op under a false comment) | Plan 35-23, Task 2 |
| 35-VERIFICATION.md HVR #1 / 35-UAT.md test 4 (in-place `Observatory` correction leaves nights stale) | Plan 35-24, Tasks 1 and 3 |
| WR-05 ("a site correction re-mints" false for a set/set run) | Plan 35-24, Task 2 |
| WR-07 (declined night re-computes `sun_event()` forever) | Plan 35-24, Task 3, as a stated and bounded acceptance |
| WR-08 (`is_verified` documented as vestigial) | Plan 35-24, Tasks 1 and 3 |
| The round's paired docs (CLAUDE.md rule) | **This plan (35-25), Tasks 1 and 2** |

Every entry the same ledger marks "Deferred" is confirmed still deferred and untouched by this plan: WR-01 (`--dry-run` repeats `sun_event()`), WR-02 (a preview can raise `ValueError`), WR-03's residue (the `rekeyed` paragraph's stability promise -- prohibition 4 held, confirmed byte-identical above), WR-04(a)/(c) (the tolerance-constant premise and the round-hour fixture convention), IN-01 (`null=True`+`blank=True` third state), IN-03 (the class docstring's materialisation note), IN-06 (two decline arms unreachable outside a test), and iteration 7's WR-01/WR-02/WR-03. None of these were touched by this plan.

## User Setup Required

None -- no external service configuration required. This plan touched exactly the two documentation files named in `files_modified`.

## Threat Flags

None -- this plan's threat model (in `35-25-PLAN.md`) is the authoritative register for the surface it touches (T-35-25-01..05, all dispositioned, all addressed by Tasks 1/2 as designed); no new surface outside that register was introduced.

## Next Phase Readiness

- **Gap-closure round 6 is complete.** With this plan green, all of this round's criticals and warnings are closed (or, for WR-07, closed as a stated and bounded acceptance), and the round's paired-docs obligation (CLAUDE.md) is discharged for all four behaviour changes plans 35-23 and 35-24 shipped, in one pass against the final state of the tree.
- An operator reading the sweep's counters can now explain every number in a post-upgrade summary line from the runbook alone, including `remint_declined`, and find the remedy for each one that needs an action.
- The two surfaces that describe `is_verified` -- the admin field's `verbose_name`/`help_text` (plan 35-24) and the runbook's `remint_declined` paragraph (this plan) -- say the same thing in the same words, verified by quoting plan 35-24's SUMMARY verbatim rather than paraphrasing.
- The paired demo notebook proves all four of this round's behaviour changes with real executed output and real assertions against a migrated scratch copy of the developer database -- the developer database (`src/fomo_db.sqlite3`) itself was never written.
- The phase's whole regression surface (seven modules, 324 tests), both lint gates, and the migration check are all green behind the documentation pass.
- ALLOC-01 through ALLOC-05 are the requirement IDs shared across plans 35-23, 35-24 and this plan; all five are now ready per the shared-ID gate (#2388) since every declaring plan has a `*-SUMMARY.md`.
- No blockers. Phase 35 verification/UAT can proceed against a runbook and a notebook that now agree with the shipped code for this round's full scope.

---
*Phase: 35-allocation-layer-classical-cutover*
*Completed: 2026-09-16*

## Self-Check: PASSED

- Both key files (`docs/runbooks/telescope_runs_calendar.rst`, `docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb`) confirmed present on disk.
- Both task commit hashes (`b61f9da`, `6f2f29d`) confirmed in `git log`.
- Re-ran every plan-level `<acceptance_criteria>` and `<verification>` item: all pass except one plan-owned verify-script regex bug (documented above as a deviation, with the guarded property independently confirmed true by direct diff).
- Seven-module regression: 324 tests, OK (matching plan 35-24's recorded count).
- Both ruff hooks: Passed (confirmed idempotent on a second pass after the notebook's one re-execution). `makemigrations --check --dry-run`: no drift. `git status --short` over `solsys_code/` and `src/`: empty.
