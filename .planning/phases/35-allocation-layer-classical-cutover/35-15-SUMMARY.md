---
phase: 35-allocation-layer-classical-cutover
plan: 15
subsystem: docs
tags: [sphinx, runbook, jupyter-notebook, gap-closure, operator-documentation]

# Dependency graph
requires:
  - phase: 35-allocation-layer-classical-cutover
    provides: "35-12's CR-01 predicate inversion and corrected CommandError/reason-string wording; 35-13's half-null dry-run parity fix; 35-14's loader dry/real counter-parity fix"
provides:
  - "docs/runbooks/telescope_runs_calendar.rst's duplicate_identity operator-action paragraph and troubleshooting Fix paragraph now state the Source line: marker precondition instead of an unconditional no-merge/no-rewrite guarantee, matching the shipped CR-01 code"
  - "the runbook's load_telescope_runs summary-lines section states the dry/real counter parity invariant (created + updated + unchanged + skipped == lines processed on both passes)"
  - "reconcile_campaign_runs_demo.ipynb regenerated by re-execution, carrying 35-12's corrected CommandError text throughout its committed output"
affects: []

# Actuals (#2632)
actuals:
  tokens: 8952
  tasks: 2
  commits: 2

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Operator-facing guarantee text must state its precondition, not the code's intent -- a runbook sentence is only as true as the shipped predicate it describes, and 35-VERIFICATION.md's gap 2 was caused by narrowing a false guarantee into a narrower one that was still false, not by wording the fix incorrectly."

key-files:
  created: []
  modified:
    - docs/runbooks/telescope_runs_calendar.rst
    - docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb

key-decisions:
  - "No fixture in reconcile_campaign_runs_demo.ipynb needed updating beyond re-execution: the notebook's existing duplicate_identity demo (cells 8-11) already uses two groups with genuinely differing (not absent) Source line: markers, which exercises the database-scoped guard's 'differing marker' branch -- unchanged by CR-01 -- not the 'no recoverable marker' branch CR-01 inverted. The pre-existing pk=334 unexplainable row is a no_source_line case (the candidate event's own description has no marker), a different code path from CR-01's claimant-observation_details case. Re-execution alone was sufficient to pick up 35-12's corrected CommandError text everywhere it prints."

requirements-completed: [ALLOC-01, ALLOC-02, ALLOC-03, ALLOC-04, ALLOC-05]

coverage:
  - id: D1
    description: "The duplicate_identity operator-action paragraph and the troubleshooting Fix paragraph in docs/runbooks/telescope_runs_calendar.rst state the marker precondition -- the no-merge guarantee holds only when the claimant's stored Source line: is recoverable and matches; a claimant with no recoverable marker is reported under duplicate_identity, never converted -- with both branches of the remedy named and no hedge words."
    requirement: ALLOC-05
    verification:
      - kind: other
        ref: "grep -v '^\\.\\. ' docs/runbooks/telescope_runs_calendar.rst | grep -c 'left untouched either way' == 0; grep -v '^\\.\\. ' ... | grep -c 'rewrites no existing' == 0; grep -ciE 'usually|in normal operation|in most cases' == 0; grep -c 'cannot prove' == 2 (>=2); grep -c 'observation_details' == 3 (>=2)"
        status: pass
    human_judgment: false
  - id: D2
    description: "The load_telescope_runs summary-lines section states the dry/real counter parity invariant (created + updated + unchanged + skipped == lines processed on both the --dry-run preview and the real pass; a line whose preview reconcile raises is reported under skipped alone on both passes)."
    requirement: ALLOC-04
    verification:
      - kind: other
        ref: "docs/runbooks/telescope_runs_calendar.rst new paragraph after the --dry-run code-block in the loader section; pre-commit run sphinx-build --all-files -> Passed"
        status: pass
    human_judgment: false
  - id: D3
    description: "docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb is regenerated by re-execution (never a hand patch): every code cell carries a non-null execution count, and the committed output carries 35-12's corrected CommandError text throughout, with no superseded 'rewrites no existing CampaignRun' clause anywhere."
    requirement: ALLOC-05
    verification:
      - kind: other
        ref: "python3 JSON scan: code cells: 18, null counts: 0; grep -c 'rewrites no existing CampaignRun' == 0; grep -c 'Source line:' == 27; git status --short src/fomo_db.sqlite3 empty; pre-commit run ruff-format --all-files -> Passed"
        status: pass
    human_judgment: false
  - id: D4
    description: "The regenerated notebook re-executes its allocation-and-observation handoff cells end to end against the scratch database copy, re-confirming ALLOC-03's demonstrated surface (linking retires only the linked night, unlinking restores it) with real output for this gap-closure round."
    requirement: ALLOC-03
    verification:
      - kind: other
        ref: "jupyter nbconvert --to notebook --execute --inplace ran cells 17-36 (observation-handoff and dispatch-branch sections) with no errors; execution counts non-null across all 18 code cells"
        status: pass
    human_judgment: false
  - id: D5
    description: "Full 5-module regression suite and both pinned ruff/ruff-format gates stay green; no file under solsys_code/ was touched by this plan; docs build clean."
    requirement: ALLOC-05
    verification:
      - kind: integration
        ref: "python manage.py test solsys_code.tests.test_allocation_projector solsys_code.tests.test_campaign_reconciler solsys_code.tests.test_cutover_classical_allocations solsys_code.tests.test_load_telescope_runs solsys_code.tests.test_observation_projector_signals -> Ran 219 tests, OK; pre-commit run ruff --all-files; pre-commit run ruff-format --all-files; pre-commit run sphinx-build --all-files; git status --short solsys_code empty"
        status: pass
    human_judgment: false

duration: ~35min
completed: 2026-09-15
status: complete
---

# Phase 35 Plan 15: Converge the Runbook and Reconciler Notebook on the CR-01 Fix Summary

**Rewrote the operator runbook's `duplicate_identity` guarantee to state its `Source line:` marker precondition (closing 35-VERIFICATION.md gap 2, the second consecutive false guarantee), added the loader's dry/real counter-parity sentence, and regenerated `reconcile_campaign_runs_demo.ipynb` by re-execution so its committed output carries plan 35-12's corrected `CommandError` text instead of the superseded unconditional clause.**

## Performance

- **Duration:** ~35 min
- **Completed:** 2026-09-15T17:35:00Z
- **Tasks:** 2 (each independently committed)
- **Files modified:** 2

## Accomplishments

- **Closed 35-VERIFICATION.md gap 2** (the runbook accuracy gap): the `duplicate_identity` operator-action paragraph (`docs/runbooks/telescope_runs_calendar.rst`, formerly lines 938-949) and the troubleshooting **Fix:** paragraph (formerly lines 1494-1503) both promised the cutover's no-merge/no-rewrite guarantee unconditionally -- false on the CR-01 path plan 35-12 fixed. Both now state the marker precondition: the database check catches a claimant left by an earlier cutover pass or a `load_telescope_runs` import WHEN that claimant's stored `Source line:` is recoverable and matches; when the marker is absent or differs, the group is REPORTED under `duplicate_identity`, never converted, because `observation_details` is editable from the Django admin, `import_campaign_csv.py` and the campaign submission form, and this command cannot prove such a row came from the line in hand. Both paragraphs now name this as reachable through the very admin edit the runbook itself instructs the operator to perform. No hedge words (`usually`, `in normal operation`, `in most cases`) were introduced anywhere -- the negative grep gates for both the superseded phrases and the hedge vocabulary print `0`.
- The remedy in both passages now covers both branches, matching the two reason strings plan 35-12 writes: give the later group's events a different bracketed proposal token (differing marker), OR restore or correct the claimant run's `observation_details` `Source line:` text in the Django admin (no recoverable marker) -- then re-run in either case. `grep -c 'cannot prove'` prints `2` and `grep -c 'observation_details'` prints `3` (both were `0` before this task), satisfying the plan's wording-anchor gates.
- Added one new sentence to the `load_telescope_runs` summary-lines section stating the dry/real counter parity invariant plan 35-14 restored: `created` + `updated` + `unchanged` + `skipped` equals `lines processed` on both the `--dry-run` preview and the real pass, and a line whose preview reconcile raises is reported under `skipped` alone on both passes, never counted twice.
- Regenerated `docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb` via `jupyter nbconvert --to notebook --execute --inplace` against the scratch copy of the developer database the notebook's own setup cell already routes to -- `src/fomo_db.sqlite3` untouched, confirmed by `git status --short`. No cell source was hand-edited. All 18 code cells re-executed with fresh, non-null execution counts. The committed output now carries plan 35-12's corrected `CommandError` sentence ("...updates an existing `CampaignRun` only when that run's stored `Source line:` matches the line being converted, reporting anything else instead (NF-19/CR-01, 35-REVIEW.md)") wherever the command raises, replacing the superseded "rewrites no existing CampaignRun" clause everywhere it previously appeared. Cell 11's second-invocation demo directly exercises the CR-01 database-scoped predicate on the differing-marker branch: a claimant with a different `Source line:` is refused under `duplicate_identity`, and `run_status`, `observation_details` and the `ALLOC:` event titles are asserted byte-identical after the refused pass.
- Confirmed no notebook fixture needed narration changes: the pre-existing NF-14 duplicate-identity demo (cells 8-11) constructs two groups whose `Source line:` text genuinely differs (an `allocation` line vs. a `cancelled` line), which is the guard's "differing marker" branch -- unaffected by CR-01. The one no-marker candidate in this database (`pk=334`, a junk `tmp` row) is reported under the unrelated `no_source_line` reason, because that candidate event's own description lacks a marker entirely, a different code path from CR-01's claimant-`observation_details` case. No fixture in this notebook relied on the permissive default CR-01 removed, so re-execution alone was sufficient.

## Task Commits

1. **Task 1: Make every runbook guarantee true of the shipped code** - `25f41de` (docs)
2. **Task 2: Regenerate the reconciler demo notebook against the corrected command output** - `e8015e3` (docs)

**Plan metadata:** pending (this commit)

## Files Created/Modified

- `docs/runbooks/telescope_runs_calendar.rst` - corrected the `duplicate_identity` operator-action paragraph and the troubleshooting **Fix:** paragraph to state the `Source line:` marker precondition instead of an unconditional guarantee, with both remedy branches named; added the loader's dry/real counter-parity sentence.
- `docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb` - whole notebook regenerated by execution (18/18 code cells, 0 null execution counts); committed output now carries 35-12's corrected `CommandError` text throughout.

## Decisions Made

- No fixture changes were needed in the notebook beyond re-execution (see key-decisions in frontmatter for the full reasoning): the existing demo cells already exercise the "differing marker" branch of the CR-01 guard, not the "no recoverable marker" branch CR-01 inverted, so their surrounding markdown narration remained accurate against the regenerated output.

## Deviations from Plan

None - plan executed exactly as written. Both tasks' `<verify>` and `<acceptance_criteria>` gates pass against the final committed tree with no auto-fixes required.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Verification (plan-level, all 4 steps run from repo root)

1. `pre-commit run sphinx-build --all-files` -> **Passed**.
2. `python manage.py test solsys_code.tests.test_allocation_projector solsys_code.tests.test_campaign_reconciler solsys_code.tests.test_cutover_classical_allocations solsys_code.tests.test_load_telescope_runs solsys_code.tests.test_observation_projector_signals` -> **Ran 219 tests, OK** (no code file changed by this plan, so this confirms no wave-1/wave-2 regression rode along).
3. `pre-commit run ruff --all-files` -> **Passed**. `pre-commit run ruff-format --all-files` -> **Passed**.
4. `git status --short` -> only this plan's two `files_modified` paths and pre-existing `.planning/`/untracked scratch artifacts modified; `src/fomo_db.sqlite3` and everything under `solsys_code/` clean.

## Next Phase Readiness

- This is the last plan in Phase 35's second gap-closure round. All five gaps from `35-VERIFICATION.md` (CR-01, the runbook accuracy gap, WR-01, WR-02, WR-03) are now closed across plans 35-12 through 35-15.
- `REQUIREMENTS.md` ALLOC-01 through ALLOC-05 marked complete via `requirements mark-complete` (this plan's own responsibility, since no further plan in this phase will do so per the dispatch instructions).
- Phase 35's three documentation advisory items from `35-VERIFICATION.md` (IN-01/02/03, WR-04) remain open as INFO/advisory-severity, out of this plan's scope (this plan forbids touching `solsys_code/`).

---
*Phase: 35-allocation-layer-classical-cutover*
*Completed: 2026-09-15*

## Self-Check: PASSED
- FOUND: docs/runbooks/telescope_runs_calendar.rst
- FOUND: docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb
- FOUND: .planning/phases/35-allocation-layer-classical-cutover/35-15-SUMMARY.md
- FOUND: commit 25f41de
- FOUND: commit e8015e3
