---
phase: 35-allocation-layer-classical-cutover
plan: 10
type: execute
gap_closure: true
wave: 2
depends_on: [35-08, 35-09]
files_modified:
  - docs/runbooks/telescope_runs_calendar.rst
  - docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb
  - CLAUDE.md
autonomous: true
requirements: [ALLOC-01, ALLOC-05]

estimate:
  tokens: 88000
  raw_tokens: 88000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The runbook's `duplicate_identity` operator-action passage states a guarantee the shipped code holds: the second group is never merged into the first group's run, on the first invocation and on every re-run, because the guard reads the database (35-08)."
    - "All three runbook passages that repeat the `duplicate_identity` remedy name the Django admin edit this command can act on, never a schedule file `cutover_classical_allocations` does not read (NF-25)."
    - "The `load_telescope_runs` collision section's own schedule-file remedy SURVIVES unchanged -- it is correct for the command that actually reads a file, and over-correcting it would break a true instruction."
    - "The runbook's per-line skip-and-log bullet for `load_telescope_runs` names the malformed-timezone case and the exception class that produces it, so the stated invariant matches the shipped handler (NF-21)."
    - "`docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb` is committed with every code cell carrying a non-null execution count and real output -- regenerated with `jupyter nbconvert --to notebook --execute --inplace`, never hand-patched (T-35-17)."
    - "The reconciler demo's cutover section shows the corrected `duplicate_identity` reason text in executed output, and demonstrates a SECOND invocation leaving the first group's `CampaignRun` fields unchanged -- the behaviour NF-19 falsified, now shown working on real data."
    - "`CLAUDE.md`'s paired-docs notebook map names `solsys_code/allocation_projector.py`, closing the enforcement hole that let this phase's central new module ship with no mapped demo notebook."
    - "[flagged assumption -- edge probe ALLOC-05 `unclassified`] The cutover's stated four-step sequencing is re-runnable: the runbook's 'it is safe to repeat' promise is now backed by 35-08's guard, and the notebook's second-invocation cell is the executed evidence rather than an assertion in prose."
    - "[already covered by 35-06/35-07-PLAN.md must_haves] The runbook's four ordered cutover steps, the `docs/index.rst:24` toctree wiring and the notebook's four SC-5 end-state assertion cells are unchanged in structure; this plan corrects statements, it does not restructure the page."
  artifacts:
    - docs/runbooks/telescope_runs_calendar.rst
    - docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb
    - CLAUDE.md
  key_links:
    - "`cutover_classical_allocations`'s `_DUPLICATE_IDENTITY` message -> the runbook's three remedy passages -> the notebook's executed output: one shared operator phrase, `disambiguate the two groups in the Django admin`, in all three places."
    - "`docs/index.rst:24` toctree -> `runbooks/telescope_runs_calendar` -- the corrected page stays operator-reachable."
    - "`CLAUDE.md` notebook map -> `reconcile_campaign_runs_demo.ipynb` -- now covering `allocation_projector.py` as well as `campaign_reconciler.py` and `cutover_classical_allocations.py`."
  prohibitions:
    - "A paired notebook must NOT be hand-patched to repair execution counts or output; it is regenerated with `jupyter nbconvert --to notebook --execute --inplace` and committed with output."
    - "An operator-facing runbook sentence must NOT state a guarantee the shipped code does not hold."
    - "A remedy correct for `load_telescope_runs` must NOT be deleted from the `load_telescope_runs` section while correcting the cutover's copy of it."
    - "Dropped as canon security/compliance boilerplate rather than minted here: secret-in-docs and PII-in-output prohibitions -- the runbook and this notebook print calendar titles, run pks and site codes only, all already staff-visible."
---

<objective>
Make the operator-facing documentation for the cutover true again, and discharge the
CLAUDE.md paired-doc obligation that 35-08 and 35-09 create.

Three runbook passages currently state guarantees the shipped code does not hold or name a
remedy this command cannot carry out; the per-line skip-and-log bullet is inaccurate for a
malformed site timezone; and the reconciler demo notebook carries an uncommitted hand-edit
that nulls the execution counts on two of its seventeen code cells, which would break 35-07
truth 8 if committed as-is.

Purpose: 35-VERIFICATION.md failed 35-07 truth 1 on exactly these passages, and CLAUDE.md
names the verifier as the enforcer of the paired-docs rule ("treat a missing or stale update
as a must-have gap, not a nice-to-have"). This is the fifth logged instance of that rule
being missed in this repo; the CLAUDE.md map extension in Task 3 closes the specific hole
that let `allocation_projector.py` -- the phase's central new module -- ship with no mapped
notebook at all.

Output: a corrected runbook, a regenerated reconciler demo notebook whose executed output
shows both the new remedy text and a safe second invocation, and one new line in CLAUDE.md's
notebook map.
</objective>

<execution_context>
@/home/tlister/git/fomo_devel/.claude/gsd-core/workflows/execute-plan.md
@/home/tlister/git/fomo_devel/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-allocation-layer-classical-cutover/35-CONTEXT.md
@.planning/phases/35-allocation-layer-classical-cutover/35-REVIEW.md
@.planning/phases/35-allocation-layer-classical-cutover/35-VERIFICATION.md
@.planning/phases/35-allocation-layer-classical-cutover/35-08-SUMMARY.md
@.planning/phases/35-allocation-layer-classical-cutover/35-09-SUMMARY.md
@docs/runbooks/telescope_runs_calendar.rst
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct the three duplicate_identity passages and the per-line skip-and-log bullet</name>
  <files>docs/runbooks/telescope_runs_calendar.rst</files>
  <read_first>
    - `docs/runbooks/telescope_runs_calendar.rst` L925-970 -- the cutover section's reason vocabulary, the `duplicate_identity` operator-action sentence at L938-943 ("the SECOND group is never merged... left untouched either way"), the `--dry-run` parity paragraph at L954-961 and the "safe to repeat" paragraph at L963-970.
    - Same file L1380-1394 -- the "Per-line / per-record skip-and-log behaviour" heading, the shared invariant sentence at L1383-1386, and the `load_telescope_runs` bullet at L1388-1394 naming only `ValueError`/`Observatory.DoesNotExist`.
    - Same file L1425-1447 -- the `load_telescope_runs` collision troubleshooting section. Its own Fix at L1431-1434 is CORRECT and must survive; the cutover paragraph at L1436-1447, which ends by claiming "the remedy is the same", is the second NF-25 passage.
    - Same file L1449-1487 -- "A reported unexplainable event during the classical cutover", whose Fix paragraph at L1475-1487 carries the third NF-25 remedy and the re-run promise.
    - `35-VERIFICATION.md` L71-81 -- the two falsified guarantees and the four `missing` items for this gap.
    - `35-REVIEW.md` L463-498 (NF-25) and L288-348 (NF-21, whose runbook consequence is the skip-and-log bullet).
    - `35-08-SUMMARY.md` and `35-09-SUMMARY.md` -- the exact message strings and exception class the shipped code now emits, so the runbook quotes reality rather than the plan's intent.
  </read_first>
  <precondition>35-08 and 35-09 are complete and their fixes are on the working branch: the runbook must describe shipped behaviour, so writing it before the code lands re-creates the exact drift this plan exists to close. Halt if either SUMMARY is absent.</precondition>
  <action>
Passage 1 -- the cutover section's operator-action sentence (L938-943). Keep the "the SECOND group is never merged into the first group's run" claim: 35-08's database-scoped guard is what makes it true, and it is now true on the first invocation and on every re-run, not just within one process. Add the one clause that makes it checkable -- the guard reads the database, so a claimant created by an earlier cutover pass or by `load_telescope_runs` is caught too. Replace the schedule-file remedy with the action this command can act on, using the exact phrase `disambiguate the two groups in the Django admin` and naming what to edit: the affected group's events' description `Source line:` text, so it carries a different bracketed proposal token from the earlier group's. Keep the closing statement that the first group's run and events are converted and left untouched, and say explicitly that this now holds across repeated invocations.

Passage 2 -- the cutover paragraph inside the `load_telescope_runs` collision section (L1436-1447). Its final clause currently asserts that the remedy is the same for both commands. It is not: `load_telescope_runs` reads a file the operator can edit, and `cutover_classical_allocations` reads none. Replace that clause with the contrast -- same collision, different remedy -- and use the same shared phrase. Leave the section's own Fix at L1431-1434 exactly as it stands: editing the schedule file and re-importing is the correct instruction for the command that actually reads a file.

Passage 3 -- the cutover troubleshooting Fix paragraph (L1475-1487). Replace its `duplicate_identity` clause the same way, with the same shared phrase. Its "safe to re-run" closing sentence is now genuinely true; keep it and add what 35-08's guard makes verifiable -- a repeat pass rewrites no existing `CampaignRun`.

The skip-and-log bullet (L1388-1394). The shared invariant sentence at L1383-1386 stays as written; 35-09 makes it true again. Extend the `load_telescope_runs` bullet so its list of caught conditions matches the shipped handlers: an unparseable schedule line or an unresolvable telescope (`ValueError` / `Observatory.DoesNotExist`), an unrecognised classical status word, and -- new -- an `Observatory` whose `timezone` is a malformed IANA name, which raises `ZoneInfoNotFoundError` (a `KeyError` subclass, not a `ValueError`, which is why it needs its own clause). Name `ZoneInfoNotFoundError` in the prose. Update the worked stderr example so it shows the message 35-09 actually emits -- copy it from `35-09-SUMMARY.md` or from a real run, do not invent it -- and keep the `Done. ... skipped: 1` summary line beneath it.

Do not restructure the page: no heading changes, no section moves, no toctree edit. `docs/index.rst:24` keeps wiring this page in unchanged.
  </action>
  <verify>
    <automated>test $(grep -cF 'disambiguate the two groups in the Django admin' docs/runbooks/telescope_runs_calendar.rst) -ge 3</automated>
    <fails_when>exit 1 -- fewer than three occurrences, i.e. at least one of the three `duplicate_identity` remedy passages was left naming a schedule file</fails_when>
  </verify>
  <verify>
    <automated>grep -cF 'bracketed proposal token to one (or both) of the two lines' docs/runbooks/telescope_runs_calendar.rst</automated>
    <fails_when>the command prints `0`, or exits 1 -- the `load_telescope_runs` section's own correct schedule-file remedy was deleted by over-correction</fails_when>
  </verify>
  <verify>
    <automated>grep -cF 'ZoneInfoNotFoundError' docs/runbooks/telescope_runs_calendar.rst</automated>
    <fails_when>the command prints `0`, or exits 1 -- the skip-and-log bullet does not name the exception class 35-09 handles</fails_when>
  </verify>
  <verify>
    <automated>pre-commit run sphinx-build --all-files</automated>
    <fails_when>non-zero exit, the hook reporting `Failed`, or any `WARNING`/`ERROR` line naming `telescope_runs_calendar.rst`</fails_when>
  </verify>
  <acceptance_criteria>
    - All three `duplicate_identity` remedy passages (cutover section, `load_telescope_runs` collision section's cutover paragraph, cutover troubleshooting Fix) name the Django admin edit and share one phrase.
    - The `load_telescope_runs` collision section's own schedule-file Fix survives verbatim.
    - The skip-and-log bullet names `ZoneInfoNotFoundError` and carries a stderr example copied from a real run, not invented.
    - The Sphinx build is clean for this page.
    - No heading, section order or toctree entry changed.
  </acceptance_criteria>
  <reversibility rating="reversible">Prose edits to one committed `.rst` page.</reversibility>
  <done>Every operator-facing sentence about the cutover's collision handling and the loader's skip-and-log behaviour describes what the shipped commands do.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate the reconciler demo notebook with the corrected cutover output and a second-invocation cell</name>
  <files>docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb</files>
  <read_first>
    - `docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb` -- the "Django setup" cell's scratch-copy routing, the cutover before/after section (markdown cells 3, 5, 8, 10, 13 and their code cells), the "A duplicate run-identity key across two groups" cell whose committed output prints the old remedy text verbatim, the four SC-5 end-state assertion cells, and the "Scratch database teardown" cell.
    - `35-VERIFICATION.md` L197 and L288 -- T-35-17: the committed copy at HEAD is clean (17/17 code cells, execution counts 1..17); the WORKING TREE carries an uncommitted hand-edit that nulls the counts on code cells 0 and 15. The remedy is re-execution, never a hand patch.
    - `35-07-SUMMARY.md` -- the deliberate cell ordering decision: the cutover section runs BEFORE the fixture/dispatch demo so its before-state reflects the pristine developer database and reproduces 35-06's real numbers (241/56/16/10/0/45 to 233/0/16/1/57/48). Preserve that ordering.
    - `35-08-SUMMARY.md` -- the exact new `duplicate_identity` message and the second-invocation behaviour to demonstrate.
  </read_first>
  <precondition>`jupyter nbconvert` is runnable in the active environment and the developer database `src/fomo_db.sqlite3` exists -- the notebook clones it into a scratch copy and tears the copy down at the end. Halt if either is missing rather than hand-editing the notebook.</precondition>
  <action>
First, discard the uncommitted working-tree hand-edit to this notebook so the starting point is the clean HEAD copy (T-35-17): the nulled execution counts on code cells 0 and 15 must not survive into this plan's commit, and they must be repaired by re-execution, not by editing JSON.

Add one new code cell to the cutover section, immediately after the existing duplicate-run-identity cell, that demonstrates the behaviour NF-19 falsified: invoke `cutover_classical_allocations` a SECOND time over the same two-group fixture, capture the raised `CommandError`, and print the first group's `CampaignRun.run_status` and the first line of its `observation_details` before and after that second pass, plus the titles of its `ALLOC:` events. Assert in the cell that the run's `run_status` and `observation_details` are unchanged by the second pass. Give it a short markdown cell above it explaining, in the page's existing voice, that the re-run the command's own error message prescribes is now safe because the identity guard reads the database.

Then regenerate the whole notebook in place with `jupyter nbconvert --to notebook --execute --inplace`, from `docs/notebooks/pre_executed/` (the setup cell resolves the repo root from the kernel's working directory via `Path.cwd().parents[2]` and raises if that is wrong). Commit it with its output: `pre_executed/` is excluded from the repo's `jupyter-nb-clear-output` pre-commit hook, so output is committed here and nowhere else.

Read the executed before/after numbers rather than assuming them. The developer database has changed since 35-07 (the Phase 34 F-34-1 sweep on 2026-09-14 rewrote fourteen facility-url-keyed events' titles and spans), so the absolute counts this run prints may differ from 35-07's. The four SC-5 end-state assertions are about properties -- zero `RUN:{pk}:{date}` left, containers unchanged, no orphan, facility-url events byte-identical -- not about absolute totals, so they must still pass. If any assertion cell raises, STOP and report it in the SUMMARY as a new finding; do not relax the assertion and do not hand-patch the output. If the printed totals differ from 35-07's, record both sets of numbers in the SUMMARY so the next verifier can tell a legitimate database drift from a regression.
  </action>
  <verify>
    <automated>python -c "import json; nb=json.load(open('docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb')); c=[x for x in nb['cells'] if x['cell_type']=='code']; bad=[i for i,x in enumerate(c) if not x.get('execution_count')]; assert not bad, f'T-35-17 open: code cells {bad} carry a null execution_count'; print(len(c), 'code cells, all executed')"</automated>
    <fails_when>non-zero exit, or the output containing `T-35-17 open:` -- at least one code cell was committed without a real execution count</fails_when>
  </verify>
  <verify>
    <automated>grep -cF 'disambiguate the two groups in the Django admin' docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb</automated>
    <fails_when>the command prints `0`, or exits 1 -- the executed output still shows the old, un-actionable remedy text, meaning the notebook was not re-executed against the fixed command</fails_when>
  </verify>
  <verify>
    <automated>git status --porcelain docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb</automated>
    <fails_when>empty output -- the notebook was not regenerated at all (it should show as modified before the plan's own commit)</fails_when>
  </verify>
  <verify>
    <human-check>Read the regenerated cutover section end to end. The before/after totals are real figures from this run against a scratch copy of the developer database; the four end-state assertion cells executed without raising; the new second-invocation cell shows the first group's run unchanged; and the remaining unexplained row is one you recognise as pre-existing junk (35-UAT.md test 1 recorded pk=334, title `tmp`) rather than a real observing night the cutover failed to convert.</human-check>
  </verify>
  <acceptance_criteria>
    - Every code cell in the committed notebook carries a non-null execution count (T-35-17 closed by re-execution, not by hand).
    - The executed output contains the corrected `duplicate_identity` remedy phrase.
    - A new second-invocation cell exists, asserts the first group's `run_status` and `observation_details` are unchanged, and its assertion passed during execution.
    - The four SC-5 end-state assertion cells executed without raising.
    - The cutover section still runs before the fixture/dispatch demo.
  </acceptance_criteria>
  <reversibility rating="reversible">The notebook is regenerated from committed source against a scratch copy of the developer database, torn down at the end; the developer database itself is not written.</reversibility>
  <done>The paired notebook for `cutover_classical_allocations.py` and `campaign_reconciler.py` is committed with real executed output that shows the fixed command's behaviour, including a safe second invocation.</done>
</task>

<task type="auto">
  <name>Task 3: Map allocation_projector.py into CLAUDE.md's paired-docs notebook list</name>
  <files>CLAUDE.md</files>
  <read_first>
    - `CLAUDE.md` -- the "Paired docs are part of the deliverable" bullet and its notebook map, specifically the entries pairing `solsys_code/campaign_reconciler.py` and `solsys_code/management/commands/reconcile_campaign_runs.py` with `reconcile_campaign_runs_demo.ipynb`, and `cutover_classical_allocations.py` with the same notebook. Note the map's own closing instruction: "Extend this map when a new module gets its own demo notebook."
    - `35-REVIEW.md` L426-461 -- NF-24, and its note that this is the fourth logged instance of the rule being missed.
    - `35-VERIFICATION.md` L280 -- the verifier's count of the same breach as the fifth instance.
  </read_first>
  <action>
`solsys_code/allocation_projector.py` is the central new module Phase 35 introduced, and it is demonstrated inside `reconcile_campaign_runs_demo.ipynb` (the dispatch branches, the `ReconcileResult` counters and the observation handoff sections) -- but it appears nowhere in CLAUDE.md's notebook map, so a future change to it triggers no paired-doc obligation at all. That is the enforcement hole behind NF-20 and NF-22 shipping with no notebook consequence.

Add `solsys_code/allocation_projector.py` to the existing entry that already pairs `campaign_reconciler.py` and `reconcile_campaign_runs.py` with `reconcile_campaign_runs_demo.ipynb`, in the map's established `module -> notebook` phrasing. One entry, no restructuring of the bullet and no change to its trigger wording, its breach history or its list of enforcing agents.

Also append the current breach to the bullet's breach-history list, in the same form the existing entries use, naming Phase 35 and the two notebooks NF-24 caught (`load_telescope_runs_demo.ipynb` and `project_observation_calendar_demo.ipynb`) -- the history is what makes the rule's cost visible, and leaving the fifth instance off it repeats the omission the rule is about.
  </action>
  <verify>
    <automated>grep -cF 'solsys_code/allocation_projector.py' CLAUDE.md</automated>
    <fails_when>the command prints `0`, or exits 1 -- the module is still absent from the paired-docs map</fails_when>
  </verify>
  <verify>
    <automated>grep -cF '260620-v9x' CLAUDE.md</automated>
    <fails_when>the command prints `0`, or exits 1 -- the existing breach-history list was replaced rather than appended to</fails_when>
  </verify>
  <acceptance_criteria>
    - `solsys_code/allocation_projector.py` is named in the notebook map, paired with `reconcile_campaign_runs_demo.ipynb`.
    - The existing breach-history entries (`260619-f7u`, `260620-v9x`, `260726-kdp`) are all still present, with the Phase 35 instance appended.
    - No other part of the paired-docs bullet changed -- trigger wording, notebook list for other modules, and the list of enforcing agents are untouched.
  </acceptance_criteria>
  <reversibility rating="reversible">One map entry and one history line in a project instruction file.</reversibility>
  <done>A future behavioural change to `allocation_projector.py` now carries a mapped paired-doc obligation, and the fifth breach is on the record.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator reading the runbook -> production commands | The page is the operator's only written instruction for a one-time, destructive-looking data migration; a false sentence here is acted on against the live calendar. |
| developer database -> notebook scratch copy | The notebook clones `src/fomo_db.sqlite3` and runs a real cutover plus a real reconciler sweep against the copy; a routing mistake would run them against the developer database itself. |
| committed notebook output -> reviewers and the verifier | Executed output is treated as evidence; hand-edited output is evidence of nothing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-27 | Tampering | `docs/runbooks/telescope_runs_calendar.rst` L938-943, L1475-1487 | high | mitigate | Task 1 replaces the two operator-facing guarantees that drove an operator into NF-19's silent merge, and replaces the remedy that could not be carried out; the statements now match shipped behaviour proven by 35-08's tests. |
| T-35-28 | Repudiation | committed notebook execution counts (T-35-17) | medium | mitigate | Task 2 discards the working-tree hand-edit and regenerates with `jupyter nbconvert --execute --inplace`; the automated gate rejects any code cell committed with a null execution count, so output cannot be asserted without having been produced. |
| T-35-29 | Tampering | notebook database routing | medium | mitigate | The notebook's own setup cell clones the developer database into a scratch copy and tears it down; the plan forbids hand-relaxing any assertion and requires a halt-and-report if an end-state assertion raises. |
| T-35-30 | Information Disclosure | regenerated notebook output | low | accept | Output is calendar titles, run primary keys, site codes and event counts from a developer database of public observing schedules -- no credential, no user PII, and the same content already committed at HEAD. |

**Package legitimacy:** this plan installs no npm/pip/cargo package. `jupyter nbconvert` is an
existing development dependency already wired into `.pre-commit-config.yaml`; no `T-35-SC`
supply-chain row and no legitimacy checkpoint applies.
</threat_model>

<verification>
- `pre-commit run sphinx-build --all-files` clean.
- The shared remedy phrase appears at least three times in the runbook and at least once in the regenerated notebook.
- The `load_telescope_runs` section's own schedule-file remedy survives.
- `ZoneInfoNotFoundError` appears in the runbook.
- Every code cell in `reconcile_campaign_runs_demo.ipynb` carries a non-null execution count.
- `solsys_code/allocation_projector.py` appears in CLAUDE.md's notebook map, with the existing breach history intact.
</verification>

<success_criteria>
35-07 truth 1 holds: every runbook statement about the cutover's collision handling and the
loader's skip-and-log behaviour describes shipped code. T-35-17 is closed by re-execution.
The paired notebook for the cutover command carries executed evidence of the fixed
behaviour, and CLAUDE.md's map no longer has a hole where this phase's central module sits.
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this phase produces (35-10)

| Symbol / artifact | File | Change |
|---|---|---|
| `duplicate_identity` operator-action passage | `docs/runbooks/telescope_runs_calendar.rst` (cutover section) | changed -- guarantee restated as database-scoped, remedy becomes the admin edit |
| cutover paragraph in the `load_telescope_runs` collision section | same | changed -- "the remedy is the same" replaced by the same-collision/different-remedy contrast |
| cutover troubleshooting Fix paragraph | same | changed -- `duplicate_identity` clause and re-run promise corrected |
| `load_telescope_runs` per-line skip-and-log bullet | same | changed -- names `ZoneInfoNotFoundError` and carries a real stderr example |
| second-invocation demonstration cell (+ its markdown cell) | `docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb` | new -- asserts the first group's run is unchanged by a repeat pass |
| whole notebook execution state | same | regenerated -- every code cell re-executed, T-35-17 closed |
| paired-docs notebook map entry for `allocation_projector.py` | `CLAUDE.md` | new |
| paired-docs breach-history entry for Phase 35 | same | new |
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/35-allocation-layer-classical-cutover/35-10-SUMMARY.md` when done
</output>
