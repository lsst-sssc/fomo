---
phase: 35-allocation-layer-classical-cutover
plan: 11
type: execute
gap_closure: true
wave: 2
depends_on: [35-09]
files_modified:
  - docs/notebooks/pre_executed/load_telescope_runs_demo.ipynb
  - docs/notebooks/pre_executed/project_observation_calendar_demo.ipynb
  - docs/notebooks/pre_executed/project_observation_calendar_demo.sched06-baseline.json
autonomous: true
requirements: [ALLOC-03, ALLOC-04]

estimate:
  tokens: 82000
  raw_tokens: 82000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`load_telescope_runs_demo.ipynb` is regenerated against the post-35-09 command and committed with real executed output, so its `stderr (skipped lines)` cells show the command's current surface rather than the pre-`d57b461` one (NF-24, half 1)."
    - "The loader demo exercises the per-line skip paths with executed output: an unrecognised classical status word, and an `Observatory` whose `timezone` is a malformed IANA name -- the case 35-09 makes skippable -- with the following schedule line still processed and `skipped` counted."
    - "`project_observation_calendar_demo.ipynb` is regenerated and committed with real executed output, and carries a cell showing that campaign attribution lands on the save that FIRST creates the record's own event, not on some later save (NF-24, half 2 -- the D-11 behaviour the demo exists to show)."
    - "That cell's writes do not persist: it runs inside a transaction that is rolled back, matching the notebook's existing receiver-demo cell, because this notebook runs against the real developer database."
    - "Regenerating the projector demo un-routed also discharges the SCHED-06 paired-docs step still owed from `34-UAT.md` -- the committed `sched06-baseline.json` is rewritten with a fresh `captured_at` and the current pending `KEY2026B-004` records."
    - "Every code cell in both notebooks carries a non-null execution count -- regenerated with `jupyter nbconvert --to notebook --execute --inplace`, never hand-patched."
    - "[flagged assumption -- edge probe ALLOC-03 `unclassified`] The handoff's observable edge is WHICH save the attribution lands on. The pinned contract is the creating save: one save that both makes the record projectable and places its block must create the facility-url event AND adopt it into the linked run, with no second save and no sweep. This is asserted in `test_placing_the_block_attributes_the_records_own_event_on_the_creating_save` and now shown in executed output."
    - "[flagged assumption -- edge probe ALLOC-04 `unclassified`] The developer database has already converged under the Phase 34 F-34-1 sweep (2026-09-14), so the projector demo's takeover cells now take the notebook's own un-routed already-converged branch and print its converged message rather than raising. That branch exists and is deliberate; an assertion failure there would be a new finding, not an expected outcome."
    - "[already covered by 35-05/35-07-PLAN.md must_haves] The loader demo's idempotency, sub-night window, bracketed-proposal-token and `--campaign` sections are unchanged in structure; this plan adds a skip-path section and re-executes, it does not restructure the notebook."
  artifacts:
    - docs/notebooks/pre_executed/load_telescope_runs_demo.ipynb
    - docs/notebooks/pre_executed/project_observation_calendar_demo.ipynb
    - docs/notebooks/pre_executed/project_observation_calendar_demo.sched06-baseline.json
  key_links:
    - "`load_telescope_runs.py`'s per-line handlers -> the loader demo's `stderr (skipped lines)` cells -- the notebook prints the exact surface 35-09 changed."
    - "`observation_projector.receiver_on_record_save()` -> `project_record()` then the D-11 linked-run re-project -> `CalendarEventMeta.run` -- the ordering NF-04 fixed, now demonstrated."
    - "`project_observation_calendar_demo.sched06-baseline.json` -> the notebook's SCHED-06 section -- the committed baseline a later re-check diffs against."
  prohibitions:
    - "A paired notebook must NOT be hand-patched to repair execution counts or output; it is regenerated with `jupyter nbconvert --to notebook --execute --inplace`."
    - "A demonstration cell running against the real developer database must NOT leave persisted rows behind."
    - "A `Target` fixture must NOT use `SiderealTargetFactory`; FOMO is exclusively non-sidereal (CLAUDE.md)."
    - "An assertion cell must NOT be relaxed to make a re-execution pass."
    - "Dropped as canon security/compliance boilerplate rather than minted here: credential-in-output and PII-in-notebook prohibitions -- these notebooks print observation ids, target names, calendar titles and event counts from a developer database of public observing schedules."
---

<objective>
Close the second gap 35-VERIFICATION.md failed 35-07 truth 5 on: CLAUDE.md's paired-docs
rule was breached twice by this phase's own fix cycle, and CLAUDE.md names the verifier as
the enforcer.

`load_telescope_runs_demo.ipynb` was last regenerated at `7d7b9d4`, before commit `d57b461`
changed the command's per-line stderr surface, its transaction boundary and which exception
classes are reported per-line versus aborting the batch -- and the notebook's cells print
`stderr (skipped lines)` verbatim, i.e. exactly the surface that changed.
`project_observation_calendar_demo.ipynb` was last touched in Phase 34, before commit
`30112a0` reordered two signal-triggered writes so campaign attribution lands on the
creating save -- the precise behaviour the D-11 trigger demo exists to show.

Purpose: this is the fifth logged instance of this rule being missed in this repo. Both
notebooks need a cell exercising the changed behaviour and a real re-execution, not a
touch-up.

Output: two regenerated, committed-with-output notebooks, each carrying a new cell that
exercises the behaviour its paired module changed, plus the rewritten SCHED-06 baseline the
un-routed projector re-execution produces -- which also discharges the paired-docs step
`34-UAT.md` has been owed since Phase 34.
</objective>

<execution_context>
@/home/tlister/git/fomo_devel/.claude/gsd-core/workflows/execute-plan.md
@/home/tlister/git/fomo_devel/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-allocation-layer-classical-cutover/35-CONTEXT.md
@.planning/phases/35-allocation-layer-classical-cutover/35-REVIEW.md
@.planning/phases/35-allocation-layer-classical-cutover/35-VERIFICATION.md
@.planning/phases/35-allocation-layer-classical-cutover/35-09-SUMMARY.md
@solsys_code/tests/test_observation_projector_signals.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the per-line skip-path section to the loader demo and regenerate it</name>
  <files>docs/notebooks/pre_executed/load_telescope_runs_demo.ipynb</files>
  <read_first>
    - `docs/notebooks/pre_executed/load_telescope_runs_demo.ipynb` -- the "Django setup" cell's scratch-copy routing and teardown, the "Seed Observatory records" cell, the "Write a sample schedule file" cell, the `--dry-run` preview cell and the invoke cell (both of which print `stderr (skipped lines)`), and the "Scratch database teardown" cell at the end.
    - `solsys_code/management/commands/load_telescope_runs.py` after 35-09 -- the three per-line handlers: the narrow `KeyError` catch for `_CLASSICAL_RUN_STATUS[parsed.status]`, the new `except ZoneInfoNotFoundError` clause, and the `(ValueError, Observatory.DoesNotExist)` clause -- so the demo shows the real messages rather than paraphrases.
    - `35-09-SUMMARY.md` -- the exact stderr text the new handler emits.
    - `35-REVIEW.md` L426-461 -- NF-24, including its instruction to exercise "the new unknown-status/invalid-timezone skip path and its stderr text".
    - `35-VERIFICATION.md` L86-93 -- the gap's `artifacts` and `missing` entries for this notebook.
  </read_first>
  <precondition>35-09 is complete and its `except ZoneInfoNotFoundError` clause is on the working branch -- a demo written against the pre-fix command would abort mid-cell with an uncaught exception instead of demonstrating a skip. Halt if `35-09-SUMMARY.md` is absent.</precondition>
  <action>
Add one new section to the notebook -- a markdown cell plus one or two code cells -- placed after the existing invoke/inspect section and before the idempotency section, so the skip paths are shown against a database that already holds the good runs.

The section writes a small three-line schedule file into the notebook's existing scratch area and runs `load_telescope_runs` over it, then prints stdout and the full `stderr (skipped lines)` block. Line 1 carries an unrecognised classical status word. Line 2 names a telescope whose seeded `Observatory` has a deliberately malformed IANA `timezone` (use the typo the review reproduced, `America/Santigo`, set on a throwaway `Observatory` the cell creates and then restores or removes, so the notebook's other sections are unaffected). Line 3 is a valid line for a correctly configured site.

The cell must make three things visible in executed output and assert all three: the command completed and printed both summary lines rather than raising; the summary reports `skipped: 2`; and line 3's `CampaignRun` exists, proving the loop continued past both bad lines. Also assert that neither bad line left a `CampaignRun` row behind -- the `transaction.atomic()` boundary rolls the partial write back before the handler reports the skip, and that is the half of NF-08 the notebook never showed.

Write the markdown cell in the notebook's existing voice: name the invariant (one bad row never aborts the whole run), name the two exception classes and why a malformed timezone needs its own clause (`ZoneInfoNotFoundError` subclasses `KeyError`, not `ValueError`), and point at the runbook's per-line skip-and-log section.

Any `Target` fixture uses `NonSiderealTargetFactory` (CLAUDE.md). Then regenerate the whole notebook in place with `jupyter nbconvert --to notebook --execute --inplace`, run from `docs/notebooks/pre_executed/`, and commit it with output -- `pre_executed/` is excluded from the repo's `jupyter-nb-clear-output` pre-commit hook. Do not hand-edit any cell's output or execution count. If an existing assertion cell raises during re-execution, STOP and report it in the SUMMARY as a new finding rather than relaxing it.
  </action>
  <verify>
    <automated>python -c "import json; nb=json.load(open('docs/notebooks/pre_executed/load_telescope_runs_demo.ipynb')); c=[x for x in nb['cells'] if x['cell_type']=='code']; bad=[i for i,x in enumerate(c) if not x.get('execution_count')]; assert not bad, f'code cells {bad} carry a null execution_count'; print(len(c), 'code cells, all executed')"</automated>
    <fails_when>non-zero exit, or the output naming any cell index with a null execution count</fails_when>
  </verify>
  <verify>
    <automated>grep -cF 'ZoneInfoNotFoundError' docs/notebooks/pre_executed/load_telescope_runs_demo.ipynb</automated>
    <fails_when>the command prints `0`, or exits 1 -- the new skip-path section is absent or does not name the exception class it demonstrates</fails_when>
  </verify>
  <verify>
    <automated>grep -cF 'skipped: 2' docs/notebooks/pre_executed/load_telescope_runs_demo.ipynb</automated>
    <fails_when>the command prints `0`, or exits 1 -- the executed summary line does not show both bad lines counted as skipped, meaning the section did not run against the fixed command</fails_when>
  </verify>
  <verify>
    <automated>python manage.py test solsys_code.tests.test_load_telescope_runs</automated>
    <fails_when>non-zero exit, or `FAILED` / `ERROR` in the output -- the notebook work must not have required a source change that broke the suite</fails_when>
  </verify>
  <acceptance_criteria>
    - A new skip-path section exists with executed output showing both bad lines reported on stderr and the third line's run created.
    - The summary line in that cell's output reads `skipped: 2`.
    - Neither bad line left a `CampaignRun` row (asserted in the cell).
    - Every code cell carries a non-null execution count.
    - The notebook's existing idempotency, sub-night, proposal-token and `--campaign` sections still execute without raising.
  </acceptance_criteria>
  <reversibility rating="reversible">The notebook runs against a scratch copy of the developer database and tears it down; nothing outside the notebook file is written.</reversibility>
  <done>The paired notebook for `load_telescope_runs.py` shows the command's current stderr surface with real executed output, including the malformed-timezone skip 35-09 makes possible.</done>
</task>

<task type="auto">
  <name>Task 2: Show attribution landing on the creating save in the projector demo, and regenerate it un-routed</name>
  <files>docs/notebooks/pre_executed/project_observation_calendar_demo.ipynb, docs/notebooks/pre_executed/project_observation_calendar_demo.sched06-baseline.json</files>
  <read_first>
    - `docs/notebooks/pre_executed/project_observation_calendar_demo.ipynb` -- the "Django setup" cell's `SCRATCH_DB_OVERRIDE` handling and its un-routed assertion that the resolved database IS `src/fomo_db.sqlite3`; the "The receiver, demonstrated -- no operator command" cell, whose `_RollbackDemo` transaction pattern the new cell must copy; the takeover cells and their `scratch_routed` branches (both have an `elif` converged branch that prints rather than asserts when run un-routed); and the SCHED-06 baseline cell, which WRITES `sched06-baseline.json` only when un-routed.
    - `solsys_code/observation_projector.py` L595-665 -- `receiver_on_record_save()`: `project_record()` now runs first, guarded by `PROJECTED_FACILITIES`, then the D-11 linked-run re-project loop, with the F-34-1 lookup guard.
    - `solsys_code/tests/test_observation_projector_signals.py` L530-565 -- `test_placing_the_block_attributes_the_records_own_event_on_the_creating_save`, the exact adversarial fixture to mirror: create the record unprojectable (no `instrument_type`), link it to the run, assert no event exists yet, then supply the instrument AND the placed block in ONE save and assert the event exists and its `CalendarEventMeta.run_id` is the run's pk.
    - `35-REVIEW.md` L426-461 -- NF-24's instruction for this notebook; L76 -- NF-04's closure note and the `project_record()`-first reorder.
    - `.planning/STATE.md` "Operator Next Steps" -- the still-owed instruction to re-execute this notebook un-routed and commit it with the rewritten baseline (the SCHED-06 paired-docs step from `34-UAT.md`), and the note that the F-34-1 sweep already ran on the developer database on 2026-09-14.
  </read_first>
  <precondition>The real developer database `src/fomo_db.sqlite3` exists and `FOMO_DATABASE_PATH` is UNSET in the execution environment -- this notebook's un-routed branch asserts that the resolved database is the developer database itself, and only the un-routed branch writes the SCHED-06 baseline. Halt rather than routing to a scratch copy, which would read the committed baseline instead of rewriting it and would leave the owed step open.</precondition>
  <action>
Add one new section -- a markdown cell plus one code cell -- to the D-11 trigger area of the notebook, immediately after the existing receiver-demo cell so it reuses that cell's framing.

The code cell mirrors `test_placing_the_block_attributes_the_records_own_event_on_the_creating_save`: inside a `transaction.atomic()` block that raises the notebook's existing `_RollbackDemo` at the end so nothing persists, create a throwaway `CampaignRun` with a resolved site and a short window, create a throwaway LCO `ObservationRecord` with a request window but NO `instrument_type` (so it is unprojectable and no event exists yet), link it with `CampaignRunObservation`, print and assert that no facility-url event exists at that point, then in ONE save set both the `instrument_type` and the `scheduled_start`/`scheduled_end` block. After that single save, print and assert three things: the facility-url `CalendarEvent` now exists; its `CalendarEventMeta.run_id` equals the run's pk (attributed on this very save, not a later one); and the run's allocation night covering that block has been retired. Use `NonSiderealTargetFactory` for the target and `get_user_model().objects.create(...)` for the throwaway user, exactly as the existing receiver-demo cell does.

Write the markdown cell to say what changed and why it is observable: `receiver_on_record_save()` used to run the D-11 linked-run re-project BEFORE `project_record()`, so on the save that first created the record's own event there was no event yet for the allocation projector to adopt, and attribution slipped to a later save. The order is now `project_record()` first, then the re-project loop -- which is the behaviour this demo exists to show.

Then regenerate the notebook un-routed: ensure `FOMO_DATABASE_PATH` is unset, run `jupyter nbconvert --to notebook --execute --inplace` from `docs/notebooks/pre_executed/`, and commit both the notebook and the rewritten `project_observation_calendar_demo.sched06-baseline.json`.

Expect the already-converged path and do not treat it as a failure. The Phase 34 F-34-1 sweep ran against the developer database on 2026-09-14, so the takeover cells will find nothing to take over. Un-routed, both takeover cells take their `elif` branch and PRINT the converged message rather than asserting -- that branch exists for exactly this case. If instead an assertion raises, STOP and report it in the SUMMARY as a new finding; do not relax an assertion, do not route to a scratch copy to make it pass, and do not hand-patch output. Record in the SUMMARY the first-sweep line the run actually produced and the new baseline's `captured_at` and record count, so the next SCHED-06 re-check knows what it is diffing against.
  </action>
  <verify>
    <automated>python -c "import json; nb=json.load(open('docs/notebooks/pre_executed/project_observation_calendar_demo.ipynb')); c=[x for x in nb['cells'] if x['cell_type']=='code']; bad=[i for i,x in enumerate(c) if not x.get('execution_count')]; assert not bad, f'code cells {bad} carry a null execution_count'; print(len(c), 'code cells, all executed')"</automated>
    <fails_when>non-zero exit, or the output naming any cell index with a null execution count</fails_when>
  </verify>
  <verify>
    <automated>grep -cF 'attributed immediately' docs/notebooks/pre_executed/project_observation_calendar_demo.ipynb</automated>
    <fails_when>the command prints `0`, or exits 1 -- the new creating-save attribution cell is absent (use this exact phrase in the cell's printed line or assertion message)</fails_when>
  </verify>
  <verify>
    <automated>python -c "import json,sys; d=json.load(open('docs/notebooks/pre_executed/project_observation_calendar_demo.sched06-baseline.json')); print(d['captured_at'], d['proposal'], d['record_count']); assert d['captured_at'] > '2026-09-14', 'baseline was not rewritten by an un-routed run'"</automated>
    <fails_when>non-zero exit, or the message `baseline was not rewritten by an un-routed run` -- the notebook was executed routed to a scratch copy, which reads the committed baseline instead of rewriting it</fails_when>
  </verify>
  <verify>
    <automated>python manage.py test solsys_code.tests.test_observation_projector_signals</automated>
    <fails_when>non-zero exit, or `FAILED` / `ERROR` in the output</fails_when>
  </verify>
  <verify>
    <human-check>Read the regenerated projector demo's takeover section and its SCHED-06 baseline section. The takeover cells print the already-converged message (the developer database was swept on 2026-09-14) rather than showing fresh takeover work, and that reads to you as the expected state rather than a lost demonstration. The new SCHED-06 baseline's pending `KEY2026B-004` records and stage tally are ones you recognise as this database's current real state.</human-check>
  </verify>
  <acceptance_criteria>
    - A new markdown + code cell pair demonstrates attribution landing on the creating save, with all three assertions passing during execution.
    - The demonstration persists nothing: its transaction is rolled back via the notebook's existing `_RollbackDemo` pattern, and no throwaway run, record, user, target or event survives the cell.
    - Every code cell carries a non-null execution count.
    - `sched06-baseline.json` carries a `captured_at` later than 2026-09-14, proving the run was un-routed.
    - The takeover cells took the un-routed converged branch and printed, rather than raising.
  </acceptance_criteria>
  <reversibility rating="costly">Re-executing un-routed writes to the REAL developer database (a real `project_observation_calendar` sweep) and replaces the committed `sched06-baseline.json` snapshot and its `captured_at`. The JSON is recoverable from git; the database write is not. This is the step `34-UAT.md` has been owed since Phase 34 and `.planning/STATE.md` explicitly directs, and the sweep is near-idempotent on an already-converged database -- so it is flagged, not gated.</reversibility>
  <done>The paired notebook for `observation_projector.py` shows, with real executed output, that campaign attribution lands on the save that first creates the record's own event -- and the SCHED-06 baseline step owed since Phase 34 is discharged in the same run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| notebook execution -> the REAL developer database | Task 2 runs un-routed by design: a real `project_observation_calendar` sweep writes to `src/fomo_db.sqlite3`, and the demonstration cell creates rows inside a transaction it must roll back. |
| notebook execution -> a scratch copy | Task 1 clones the developer database and tears the copy down; a routing mistake would run a real import against the developer database. |
| committed notebook output and `sched06-baseline.json` -> the verifier | Both are treated as evidence of a run that happened; hand-edited content is evidence of nothing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-31 | Tampering | Task 2's demonstration cell against the real developer database | high | mitigate | The cell copies the notebook's existing `_RollbackDemo` pattern -- every write happens inside a `transaction.atomic()` block deliberately aborted at the end -- and the acceptance criteria require that no throwaway run, record, user, target or event survives. |
| T-35-32 | Repudiation | committed execution counts and `sched06-baseline.json` | medium | mitigate | Automated gates reject any code cell committed with a null execution count and reject a baseline whose `captured_at` predates the run, so neither notebook's evidence can be asserted without having been produced by a real execution. |
| T-35-33 | Tampering | Task 1's throwaway malformed-timezone `Observatory` | medium | mitigate | The bad `timezone` is set on a throwaway `Observatory` the cell creates, inside a scratch copy of the developer database that the notebook's own teardown cell removes -- the demo never leaves a broken site behind for a later section or a later run. |
| T-35-34 | Information Disclosure | regenerated notebook output | low | accept | Output is observation ids, minor-body target names, calendar titles, site codes and counts from a developer database of public observing schedules -- no credential, no user PII, and the same classes of content already committed. |

**Package legitimacy:** this plan installs no npm/pip/cargo package. `jupyter nbconvert` is an
existing development dependency already wired into `.pre-commit-config.yaml`; no `T-35-SC`
supply-chain row and no legitimacy checkpoint applies.
</threat_model>

<verification>
- Every code cell in both notebooks carries a non-null execution count.
- `ZoneInfoNotFoundError` and the `skipped: 2` summary line appear in the loader demo's committed output.
- The phrase `attributed immediately` appears in the projector demo's committed content.
- `sched06-baseline.json` carries a `captured_at` later than 2026-09-14.
- `python manage.py test solsys_code.tests.test_load_telescope_runs solsys_code.tests.test_observation_projector_signals` green.
</verification>

<success_criteria>
Both paired notebooks NF-24 named are regenerated, committed with real executed output, and
each carries a cell exercising the behaviour its paired module changed. The SCHED-06
paired-docs step owed since `34-UAT.md` is discharged in the same run.
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this phase produces (35-11)

| Symbol / artifact | File | Change |
|---|---|---|
| per-line skip-path section (markdown + code cells) | `docs/notebooks/pre_executed/load_telescope_runs_demo.ipynb` | new -- unknown status word and malformed IANA timezone, with the following line still processed |
| whole notebook execution state | same | regenerated -- every code cell re-executed against the post-35-09 command |
| creating-save attribution section (markdown + code cell) | `docs/notebooks/pre_executed/project_observation_calendar_demo.ipynb` | new -- one save creates the event AND adopts it into the linked run; rolled back |
| whole notebook execution state | same | regenerated un-routed against the real developer database |
| `captured_at`, `record_count`, `records` | `docs/notebooks/pre_executed/project_observation_calendar_demo.sched06-baseline.json` | rewritten by the un-routed run (the SCHED-06 step owed from `34-UAT.md`) |
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/35-allocation-layer-classical-cutover/35-11-SUMMARY.md` when done
</output>
