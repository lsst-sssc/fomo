---
phase: 35-allocation-layer-classical-cutover
plan: 25
type: execute
wave: 3
depends_on: ["35-24"]
gap_closure: true
autonomous: true
requirements: [ALLOC-01, ALLOC-02, ALLOC-03, ALLOC-04, ALLOC-05]
files_modified:
  - docs/runbooks/telescope_runs_calendar.rst
  - docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The operator runbook prints the counter vocabulary the sweep actually uses. `remint_declined` is a new counter this round created, and today the runbook's counter list, both example summary lines and the `detach_declined` paragraph all describe a single overloaded counter that no longer exists in that form."
    - "The runbook's `retired` reason (2) says what is true rather than what plan 35-21 hoped. It currently promises that a correction to the run's `site` deletes and re-creates the night at the corrected site's real sunset/sunrise, full stop; 35-REVIEW.md iteration 9's WR-05 showed that is false for a run with both sub-night fields set, where the boundaries are correctly pinned and only the dark-window line changes, and that a cross-timezone correction of such a run has a different outcome again."
    - "`retired` reason (5) covers the second one-time audit this round creates. Plan 35-24's version bump makes every token already in the database read as unrecorded, so the first sweep after this release resolves each existing night once -- exactly as the previous release's bump did -- and an operator seeing a non-zero `would_retire` on nights nobody edited must be able to find out why before it happens, not after."
    - "The runbook states what a correction to a SITE DEFINITION does, not only what a correction to a run's site assignment does. Editing an `Observatory` row's position or timezone in the admin now re-mints every night already projected at that site; before this round it silently left them stale by up to fifteen hours (35-VERIFICATION.md \"Human Verification Required\" #1)."
    - "`is_verified`'s second meaning is written where an operator reads about the counter it drives: setting it False permanently vetoes automated correction of a night's boundaries and is reported under `remint_declined`, and it does NOT prevent that night being retired when a linked observation places a block on it (35-REVIEW.md iteration 9, WR-08, and plan 35-23's CR-05 decision)."
    - "The declined-retirement consequence is documented with its remedy: a night a person has confirmed keeps its allocation event when the observation links, so the calendar shows both that night and the observation's own entry until the confirmation is cleared. That narrows ALLOC-03's promise and an operator must not have to discover it from a duplicate row."
    - "`reconcile_campaign_runs_demo.ipynb` carries real executed output for this round's four behaviour changes -- a declined night still receiving its label refresh (CR-04), a confirmed night surviving its retirement (CR-05), the `remint_declined` counter in a real summary line (WR-06), and an in-place `Observatory` correction re-minting (the escalated decision) -- regenerated by re-execution, never hand-edited (CLAUDE.md paired-docs rule; 35-SECURITY.md `T-35-17`)."
    - "This plan is what makes the round complete. Plans 35-23 and 35-24 both change documented behaviour and both are forbidden from touching `docs/`; CLAUDE.md's paired-docs rule is discharged at ROUND granularity, in one pass against the final state of the tree, so the runbook passages are written once and the notebook is re-executed once rather than three times. A green 35-23 and a green 35-24 with this plan outstanding leave the operator documentation describing a sweep that no longer exists."
    - "The developer database is never written. The notebook copies `src/fomo_db.sqlite3` to a scratch directory and migrates the copy; `git status --short -- src/fomo_db.sqlite3` prints nothing after the whole run."
    - "The phase's code surface is still green behind the documentation: the seven-module regression, both ruff hooks and `makemigrations --check --dry-run` all pass at the END of the round, not merely at the end of each code plan -- so a documentation pass cannot be the step that quietly ships a broken tree."
    - "[flagged assumption -- edge probe ALLOC-05 `unclassified`] ALLOC-05's cutover sequencing is untouched in code by this whole round. The assumption stated rather than hidden: the first sweep after THIS release behaves like a one-time audit for every night carrying a `v2` provenance token, exactly as the previous release's sweep did for nights carrying no token or a pre-release one, and that is now written into the runbook's deploy note so an operator seeing a non-zero `would_retire` on nights they did not edit knows what it is. The cutover command itself, its four documented steps and its own tests are not modified."
  artifacts:
    - "docs/runbooks/telescope_runs_calendar.rst -- the counter list and both example summary lines carrying `remint_declined`; a corrected `retired` reason (2); an extended `retired` reason (5); a narrowed `detach_declined` paragraph; a new `remint_declined` paragraph with its three causes, its counter pairing and its remedy; a site-definition-correction paragraph; an `is_verified` sentence; and an extended post-upgrade deploy note"
    - "docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb -- new executed cells for the four behaviour changes, whole notebook regenerated by re-execution"
  key_links:
    - "the sweep's `retired` / `detach_declined` / `remint_declined` counters -> `docs/runbooks/telescope_runs_calendar.rst` -> the operator reading a post-upgrade sweep summary. A counter whose printed meaning is stale is the same silent-wrong-information defect class this round's two criticals are about, in a different medium."
    - "plan 35-23's CR-05 retirement guard -> a confirmed night surviving beside the observation's own event -> the runbook paragraph that states the duplicate and its remedy. Without the last arrow the guard produces a calendar an operator cannot explain."
    - "plan 35-24's `v3` version bump -> every existing night resolving once on the first post-upgrade sweep -> the deploy note that tells the operator to `--dry-run` first. A one-time audit is expected behaviour only if it is written down before it happens."
    - "plan 35-24's `is_verified` `help_text` -> the runbook's `remint_declined` section. The two surfaces must say the same thing in the same words, or a staff member reading one and an operator reading the other disagree about what the checkbox does."
  prohibitions:
    - statement: "Notebook JSON must NOT be hand-edited. `reconcile_campaign_runs_demo.ipynb` is regenerated by `jupyter nbconvert --to notebook --execute --inplace` only -- hand-patching output is precisely what 35-SECURITY.md's `T-35-17` forbids, and it was caught doing exactly that once before."
      status: flagged-unverified
      verification: backstop
    - statement: "No management command may be run against `src/fomo_db.sqlite3`. The notebook copies it to a scratch file and migrates the copy; the developer database stays read-only for the whole run."
      status: flagged-unverified
      verification: backstop
    - statement: "No source file may be modified. This plan is two documentation files; plans 35-23 and 35-24 own every code change in this round, and re-opening either from a docs plan is how rounds 2 and 3 acquired their regressions."
      status: flagged-unverified
      verification: backstop
    - statement: "The runbook's `rekeyed` paragraph must NOT be edited. Its stability-promise defect is 35-REVIEW.md WR-03's residue, which the user deferred for this round; correcting it here would quietly expand a deferred finding into scope."
      status: flagged-unverified
      verification: backstop
---

<objective>
Discharge this gap-closure round's paired-docs obligation for all four of its behaviour changes, in
one pass against the final state of the tree.

Plan 35-23 split `detach_declined` into two counters, made a declined re-mint fall through to the
ordinary label refresh (so a declined night now reports its decline AND an `updated`/`unchanged`), and
gave the retirement branch a human-confirmation guard (so a confirmed night survives beside the
observation's own entry). Plan 35-24 made the provenance token carry the site's position, so an
in-place `Observatory` correction re-mints instead of leaving nights up to fifteen hours stale, and
bumped the token version so the first sweep after this release is a second one-time provenance audit.

All four change what `docs/runbooks/telescope_runs_calendar.rst` currently promises. Its counter list
and both example summary lines predate `remint_declined`; its `retired` reason (2) states as fact the
site-correction claim 35-REVIEW.md iteration 9's WR-05 showed to be false for a fully-set run; its
`detach_declined` paragraph describes two meanings, one of which has moved to its own counter; and
nothing in it mentions that correcting a site DEFINITION -- as opposed to a run's site assignment --
does anything at all. CLAUDE.md maps `solsys_code/allocation_projector.py` and
`solsys_code/management/commands/reconcile_campaign_runs.py` to
`docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb`, which carries no executed output for
any of the four.

Purpose: a counter whose printed meaning is stale is the same wrong-information defect this round's
criticals are about, in a different medium. An operator seeing `retired: 37` on the first
post-upgrade sweep should not have to hunt for a window shrink that never happened, and an operator
seeing a duplicate night should not have to reverse-engineer a confirmation guard.

Output: seven corrected or new runbook passages and a demo notebook with executed cells for all four
behaviour changes, regenerated by re-execution.

**Scope is the paired docs only.** CR-04, CR-05, WR-06 and IN-05 are plan 35-23, wave 1; the
escalated decision, WR-05, WR-07 and WR-08 are plan 35-24, wave 2. No source file is touched here
(prohibition 1). WR-01, WR-02, WR-03's residue, WR-04(a)/(c), IN-01, IN-03 and IN-06 are
user-deferred; the full ledger for the round is `<review_dispositions>` in `35-23-PLAN.md`, and
nothing is dropped.

**This plan is what completes the round.** Until it lands, the operator runbook and the committed
notebook describe a sweep that no longer exists.

**Run test modules by explicit dotted path, never bare `python manage.py test`.** A bare run collects
`solsys_code/tests/test_views.py`, which imports `ephem_utils` and pays a ~1.6 GB SPICE kernel
download (CLAUDE.md "Heavy import side effect"), and `TestEphemeris` segfaults in native ASSIST. Use
`python manage.py`, never `./manage.py`.
</objective>

<execution_context>
@/home/tlister/git/fomo_devel/.claude/gsd-core/workflows/execute-plan.md
@/home/tlister/git/fomo_devel/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-allocation-layer-classical-cutover/35-CONTEXT.md
@.planning/phases/35-allocation-layer-classical-cutover/35-REVIEW.md
@.planning/phases/35-allocation-layer-classical-cutover/35-VERIFICATION.md
@.planning/phases/35-allocation-layer-classical-cutover/35-23-PLAN.md
@.planning/phases/35-allocation-layer-classical-cutover/35-24-PLAN.md
@.planning/phases/35-allocation-layer-classical-cutover/35-22-PLAN.md
@docs/runbooks/telescope_runs_calendar.rst
</context>

<why_this_plan_runs_last>
This plan is wave 3 and `depends_on: ["35-24"]`, which transitively puts it after 35-23 as well. That
ordering is deliberate and is the whole reason the paired docs are a separate plan rather than a task
inside either code plan:

- **The prose must describe one final state, not two intermediate ones.** If 35-23 wrote the
  `detach_declined` and `remint_declined` paragraphs, 35-24 would have to rewrite the `retired`
  enumeration sitting beside them. Writing every passage once, after both behaviour changes are in
  the tree, is the only way the page is internally consistent on the first attempt.
- **The notebook must be re-executed once, not twice.** `jupyter nbconvert --execute` regenerates
  every cell's output. Re-executing after 35-23 and again after 35-24 produces two commits of churn
  over the same file and two chances for a convergence assertion to disagree with itself.
- **The re-execution needs both changes present.** The declined-label-refresh and confirmed-retirement
  cells exercise 35-23's guards; the site-definition-correction cell exercises 35-24's fingerprint.
  Neither can produce real output before its code lands.

The cost of the ordering, stated rather than hidden: between 35-23 committing and this plan
committing, the repository contains a runbook that describes a counter vocabulary the code no longer
prints. That window is one round, inside one branch, and it is the reason this plan is listed as the
round's completion condition in both code plans' SUMMARY instructions rather than as optional polish.
</why_this_plan_runs_last>

<prohibitions>
1. Do NOT modify any file under `solsys_code/`, `src/` or any migration. This plan is two
   documentation files. If a documentation claim cannot be made true without a code change, record it
   as a gap in the SUMMARY and state it as a residual -- do not reach back into a completed plan.
2. Do NOT hand-edit the notebook's JSON. Cells are authored as source and the file is regenerated by
   `jupyter nbconvert --to notebook --execute --inplace`; committed output must be real executed
   output (35-SECURITY.md `T-35-17`).
3. Do NOT run any command against `src/fomo_db.sqlite3`. The notebook copies it to a scratch path and
   migrates the copy, exactly as the existing cells already do; the developer database stays
   read-only.
4. Do NOT edit the runbook's `rekeyed` paragraph. Its stability-promise defect is WR-03's residue,
   user-deferred for this round.
5. Do NOT edit the runbook's `legacy_deleted`, `skipped_nights` or `detached` paragraphs beyond what
   the counter-list line and the two example summary lines require. Their content is unchanged by
   this round.
6. Do NOT change the existing `detach_declined` message wording quoted anywhere in the runbook -- plan
   35-23 deliberately left both printed messages byte-identical, and the runbook must quote what the
   code prints.
7. Any `Target` fixture in a notebook cell must use
   `tom_targets.tests.factories.NonSiderealTargetFactory`, never `SiderealTargetFactory` (CLAUDE.md).
8. Planning-doc and prose conventions: plain English, per CLAUDE.md's Conventions section. Write
   "create or update" or "find-or-create" rather than the database-jargon verb CLAUDE.md names, and
   "model" / "data model" / "case" rather than the abstraction noun it names. Follow that rule as
   written there; do not reproduce either forbidden term here.
</prohibitions>

<tasks>
<task type="tracer">
  <name>Task 1: Correct the operator runbook against the round's final behaviour, end to end from counter to remedy</name>
  <files>docs/runbooks/telescope_runs_calendar.rst</files>
  <read_first>
    - `docs/runbooks/telescope_runs_calendar.rst:1060-1185` -- the whole counter section: the counter-list sentence, both example summary lines, the five-reason `retired` enumeration, the `rekeyed` paragraph (do NOT edit -- prohibition 4), the `legacy_deleted` and `skipped_nights`/`detached` paragraphs, the `detach_declined` paragraph and its remedy sentences, and the post-upgrade deploy note.
    - `docs/runbooks/telescope_runs_calendar.rst:685-700` -- the earlier cross-reference that already points forward to `detach_declined`, which must still land on a true statement after the counter splits.
    - `docs/runbooks/telescope_runs_calendar.rst` -- the section headed for correcting a run's source, referenced by `retired` reason (2) as "see 'Can I correct a run's source?' above". Read it so the new site-DEFINITION paragraph sits beside it coherently rather than duplicating it.
    - `.planning/phases/35-allocation-layer-classical-cutover/35-23-SUMMARY.md` -- the exact new `remint_declined` message strings for the command and the view, and the recorded decision about which companion-row facts veto a retirement.
    - `.planning/phases/35-allocation-layer-classical-cutover/35-24-SUMMARY.md` -- the exact `is_verified` `verbose_name` and `help_text` strings as shipped, and the observed cross-timezone set/set outcome.
    - `solsys_code/management/commands/reconcile_campaign_runs.py` -- both summary lines as they now print, so the runbook's two example lines quote real output rather than an approximation.
    - `.planning/phases/35-allocation-layer-classical-cutover/35-REVIEW.md` -- `### WR-05` (the three outcomes for a set/set run), `### WR-06` (why one counter could not carry two meanings), `### WR-08` (what the `is_verified` section must say) and `### WR-03` (so the `rekeyed` paragraph's deferred residue is recognisable and left alone).
    - `.planning/phases/35-allocation-layer-classical-cutover/35-22-PLAN.md` -- the previous round's docs plan, for the passage-by-passage structure and the tone the runbook's counter section already uses.
  </read_first>
  <action>
Edit `docs/runbooks/telescope_runs_calendar.rst`'s counter section. Seven passages, each named for
what it must end up saying. Quote real output and real field text throughout -- take the summary-line
examples from the command source and the `is_verified` wording from plan 35-24's SUMMARY, never from
memory.

  1. **The counter-list sentence.** Add `remint_declined` to the enumeration, immediately after
     `detach_declined`, so the list matches the order both summary lines print.

  2. **Both example summary lines.** Add a `remint_declined: 0` token to each, positioned exactly
     where the command prints it. These are quoted console output; they must be copyable and correct.

  3. **`retired` reason (2).** It currently claims that a correction to the run's `site` deletes and
     re-creates the night at the corrected site's real sunset/sunrise, without qualification. Replace
     that clause with the three cases the code actually has, named plainly: a run whose sub-night
     window fields are empty or half-set has its nights re-minted at the corrected site's real sun
     events; a run with BOTH fields set has its boundaries pinned by those fields, so a same-timezone
     correction changes only the dark-window line in the entry's description (refreshed automatically
     on the next sweep) and nothing is retired; and a correction that moves such a run to a site in a
     different timezone has the outcome plan 35-24's SUMMARY recorded, whose remedy is to correct the
     sub-night window fields together with the site, because a window pinned to one site's night is
     not portable to another timezone. Keep the existing pointer to the "correct a run's source"
     section.

  4. **`retired` reason (5).** It describes the provenance audit for nights minted before the
     previous release. Extend it to say that this release repeats the audit for every night carrying
     the PREVIOUS release's token format, for the same reason and with the same once-per-night bound,
     and add the one new input that audit now checks: the site's own stored position and timezone.
     Keep the existing "at most once per night ... reports `unchanged` on every sweep after it"
     sentence, which is still true.

  5. **A new paragraph on correcting a site DEFINITION.** Place it immediately after the `retired`
     enumeration. State: editing an `Observatory` row's latitude, longitude, altitude or timezone in
     the Django admin -- without touching any run's `site` -- now re-mints every allocation night
     already projected at that site, on the next sweep, counted under `retired` and `created`. State
     what it did before this release, because an operator may be looking at a calendar built by the
     old behaviour: such an edit was invisible, and nights kept boundaries computed at the old
     position, wrong by up to fifteen hours, reported as `unchanged` forever. State the one case that
     does not re-mint and why it is deliberate: a correction too small to move the computed sunset or
     sunrise by more than a minute is recognised as correct and reported `unchanged`, so a small
     position fix does not churn the calendar.

  6. **The `detach_declined` paragraph, narrowed; a new `remint_declined` paragraph beside it.**
     `detach_declined` now counts only declines whose cause is a human confirmation: a companion row
     the sweep did not release, and -- new this round -- an allocation night the sweep did not delete
     when a linked observation would otherwise have retired it. Write that second cause out with its
     consequence and its remedy, because it is the one an operator will actually meet: the calendar
     shows both the confirmed allocation night and the linked observation's own entry until someone
     clears the confirmation on that night's companion row and re-runs the sweep. Keep the existing
     "nothing for an operator to do" sentence attached ONLY to the attribution-release case, where it
     is still true.
     Then add a `remint_declined` paragraph: it counts a night whose boundaries would have changed but
     which the sweep declined to delete and re-create, for any of three causes -- a human confirmation
     on the companion row, a real `ObservationRecord`/`ObservationGroup` link, or an unverified
     companion row. State the counter pairing explicitly, because it looks like a contradiction
     otherwise: a declined night is ALSO counted under `updated` or `unchanged`, deliberately, because
     its title, description and campaign are still refreshed on the same sweep -- only its start and
     end times are left alone. State that the warning repeats on every sweep until the night is
     resolved, and that this is the standing report rather than a fault. State the remedy: clear the
     confirmation or the link, or uncheck the unverified state, on that night's companion row in the
     Django admin and re-run the sweep; or leave it and accept the stored boundary.

  7. **An `is_verified` sentence, and the deploy note.** Inside the new `remint_declined` paragraph,
     say what "an unverified companion row" means and where it comes from: the `is_verified`
     checkbox on `CalendarEventMeta` in the Django admin, whose label reads as being about telescope
     labels -- quote it from plan 35-24's SUMMARY -- and which, when unchecked, permanently prevents
     automated correction of that night's boundaries while NOT preventing its retirement when a
     linked observation places a block on it. Then extend the existing post-upgrade deploy note so it
     covers this release too: run `reconcile_campaign_runs --dry-run` first, read a non-zero
     `would_retire` on nights nobody edited as the one-time audit rather than a regression, and read a
     non-zero `remint_declined` as nights carrying a confirmation, a link or an unverified row that
     the sweep will not correct on its own.

Leave the `rekeyed` paragraph untouched (prohibition 4). Leave `legacy_deleted`, `skipped_nights` and
`detached` untouched beyond the counter-list line and the two example summary lines (prohibition 5).
Leave every quoted `detach_declined` message wording byte-identical (prohibition 6).

WR-05 named THREE claims that disagree with the code, and all three must end this round accounted
for. Two are corrected: the runbook's `retired` reason (2) by passage 3 above, and
`_span_needs_remint()`'s step-2 docstring by plan 35-24. The third is `35-21-PLAN.md:24`'s truth that
correcting an already-projected run's site "re-mints every one of its nights". Do NOT edit
`35-21-PLAN.md` -- a completed plan is a historical record of what was planned, not a statement of
what is currently true, and rewriting one erases the trail. Record it in the SUMMARY instead, as a
knowingly superseded historical claim, naming the file and line and naming the two passages that
supersede it.
  </action>
  <verify>
    <automated>cd /home/tlister/git/fomo_devel && grep -c 'remint_declined' docs/runbooks/telescope_runs_calendar.rst</automated>
    <fails_when>prints a number below `5`, or exits non-zero (grep -c exits 1 when the count is zero) -- the new counter must appear in the counter-list sentence, both example summary lines, its own paragraph heading phrase and the deploy note, so fewer than five occurrences means a passage was skipped.</fails_when>
    <automated>cd /home/tlister/git/fomo_devel && grep -n 'rekeyed. counts a night carried across' docs/runbooks/telescope_runs_calendar.rst</automated>
    <fails_when>prints nothing, or exits non-zero -- the deferred `rekeyed` paragraph's opening sentence must still be present and unedited (prohibition 4); its absence means WR-03's residue was silently pulled into scope.</fails_when>
    <automated>cd /home/tlister/git/fomo_devel && grep -c 'left attributed -- a person confirmed' docs/runbooks/telescope_runs_calendar.rst; grep -rc 'left attributed -- a person confirmed' solsys_code/management/commands/reconcile_campaign_runs.py</automated>
    <fails_when>the second count is `0`, or exits non-zero -- the command must still print that exact wording (plan 35-23 prohibition 1), so a zero there means a completed plan was reopened from this docs plan, which prohibition 1 forbids.</fails_when>
    <automated>cd /home/tlister/git/fomo_devel && git status --short -- solsys_code/ src/ docs/notebooks/</automated>
    <fails_when>the command prints any line at all -- Task 1 is one documentation file, so a clean run at this point prints nothing (prohibition 1; the notebook is Task 2's).</fails_when>
  </verify>
  <acceptance_criteria>
    - `remint_declined` appears in the counter-list sentence, in both example summary lines in the position the command prints it, in its own paragraph and in the deploy note.
    - `retired` reason (2) names three distinct site-correction cases (empty or half-set window, fully-set window same timezone, fully-set window different timezone) with the remedy for the third, and no longer states the unqualified re-mint claim.
    - `retired` reason (5) covers this release's repeat of the audit and names the site's stored position and timezone as a checked input.
    - A paragraph on correcting a site DEFINITION exists, states the new behaviour, states what it did before this release, and states the below-one-minute case as deliberate.
    - The `detach_declined` paragraph describes only confirmation-caused declines and includes the declined-retirement case with its duplicate-entry consequence and its remedy; the "nothing for an operator to do" sentence is attached only to the attribution-release case.
    - The `remint_declined` paragraph names all three causes, states the counter pairing with `updated`/`unchanged` and why, states that the warning repeats deliberately, and gives the remedy.
    - The `is_verified` sentence quotes the shipped field label and states both what unchecking does and what it does not do.
    - The `rekeyed` paragraph's opening sentence is present and unedited; the command still prints the unchanged `detach_declined` wording the runbook quotes.
    - `git status --short` over `solsys_code/`, `src/` and `docs/notebooks/` prints nothing at the end of this task.
  </acceptance_criteria>
  <done>An operator reading the counter section can, from the page alone, explain every number in a post-upgrade summary line, find the remedy for each one that needs an action, and know what correcting a site definition will do before they do it.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate the demo notebook with real executed output for all four behaviour changes</name>
  <files>docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb</files>
  <read_first>
    - `docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb` -- the whole notebook, but especially: the scratch-database setup cells (the copy of `src/fomo_db.sqlite3` and the migrate step), the existing site-correction demo added last round, the existing declined-re-mint demo and its assertion cell, and the prose cell that currently states `detach_declined` counts two things. That prose cell is now wrong and must be rewritten, not appended to.
    - `docs/runbooks/telescope_runs_calendar.rst` -- your own Task 1 output, so the notebook's prose and the runbook agree word for word on the three causes, the counter pairing and the remedies.
    - `.planning/phases/35-allocation-layer-classical-cutover/35-23-SUMMARY.md` and `35-24-SUMMARY.md` -- the shipped message strings, the shipped field label, and any Rule 1 deviation whose behaviour a cell would otherwise assert wrongly.
    - `.planning/phases/35-allocation-layer-classical-cutover/35-22-SUMMARY.md` -- how the previous round executed and committed this notebook, including the exact `nbconvert` invocation and anything it recorded about execution time or flaky cells.
    - `solsys_code/tests/test_projector_demo_notebook.py` -- what this test module asserts about committed notebooks, so a regenerated file does not break it.
    - `CLAUDE.md` -- the paired-docs rule and the `NonSiderealTargetFactory` rule, both of which bind the cells you add.
  </read_first>
  <action>
Add cells to `docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb` demonstrating this
round's four behaviour changes against the scratch copy of the developer database the notebook already
builds (prohibition 3 -- never the real file). Each demo is a short prose cell stating what is being
shown and why it matters, a code cell producing real output, and an assertion cell that raises if the
behaviour regresses, matching the style the notebook already uses. Four demos:

  1. **A declined re-mint still refreshes the night's labelling (plan 35-23, CR-04).** Take a night,
     confirm its companion row, change a boundary-affecting field, change something that alters the
     title, reconcile, and print the resulting counters together with the stored title and the
     unchanged start and end times. Assert the title changed, the boundaries did not, and the counters
     show the decline alongside an `updated`.
  2. **A confirmed night survives its own retirement (plan 35-23, CR-05).** Confirm an allocation
     night's companion row, link an observation record with a placed block on that night, and print
     the counters plus proof that the event, the companion row and the confirmation stamp all still
     exist. Assert `retired == 0` and the decline counted. The prose cell must state the consequence
     an operator sees -- two entries on that night -- and the remedy, in the same words as the
     runbook.
  3. **The `remint_declined` counter in a real summary line (plan 35-23, WR-06).** Run the management
     command so the committed output carries a genuine summary line containing the new counter, and
     show the per-run stderr line beside it.
  4. **An in-place site-definition correction re-mints (plan 35-24, the escalated decision).** Edit an
     `Observatory` row's latitude, longitude, altitude and timezone in place, WITHOUT reassigning any
     run's site, reconcile, and print the before and after boundaries with the counters. Assert the
     boundaries moved and the counters show `retired` and `created`. The prose cell states what this
     did before the release -- silently nothing, with an error of up to fifteen hours -- because that
     is the fact the demo exists to make visible.

Rewrite the existing prose cell that states `detach_declined` counts two things. After the split it
counts one class of thing (declines caused by a human confirmation, now including a declined
retirement), and the second meaning has its own counter. Do not append a correction below the stale
sentence; replace the sentence.

Check every other prose cell and every assertion cell that names a counter or quotes a summary line
against the round's final behaviour, and correct any that has gone stale. The existing site-correction
demo added last round is the likeliest: confirm whether its fixture is a null/null run (in which case
it still behaves as its prose says) or a fully-set one (in which case its prose is now wrong), and
correct it if needed.

Regenerate the whole notebook with `jupyter nbconvert --to notebook --execute --inplace
docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb` and commit the file WITH its executed
output. Never hand-edit the JSON (prohibition 2). If a cell raises during execution, fix the cell's
source and re-execute the whole notebook -- do not patch the stored output.

Then discharge the round's SECOND pairing explicitly rather than by silence. `campaign_views.py` and
`campaign_reconciler.py` are both mapped, through CLAUDE.md's collective v2.2-campaign-surfaces
entry, to `docs/notebooks/pre_executed/campaign_lifecycle_demo.ipynb`. Re-check the three grounds the
`<artifacts>` block gives for that notebook being unaffected -- no cell reads
`django.contrib.messages` or any `ReconcileResult` counter, no fixture can produce a declined
re-mint, and `owned_events()` is the only name it imports from `campaign_reconciler` -- using the
counter grep in this task's verify block plus a read of the cells around the approval and
`resolve_site` posts. If all three hold, leave the notebook untouched and record the check and its
result in the SUMMARY. If any fails, add the notebook to this plan's `files_modified`, add cells
exercising whatever it turns out to demonstrate, re-execute it with the same `nbconvert` invocation
and record the deviation in the SUMMARY (Rule 1).
  </action>
  <verify>
    <automated>cd /home/tlister/git/fomo_devel && jupyter nbconvert --to notebook --execute --inplace docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb</automated>
    <fails_when>non-zero exit, or `CellExecutionError` / a traceback naming a cell index in the output -- any cell raising means the committed output would not be real executed output, which is what 35-SECURITY.md `T-35-17` forbids.</fails_when>
    <automated>cd /home/tlister/git/fomo_devel && python -c "import json,sys; nb=json.load(open('docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb')); c=[x for x in nb['cells'] if x['cell_type']=='code']; print('code_cells', len(c)); print('unexecuted', sum(1 for x in c if x.get('execution_count') is None)); print('with_output', sum(1 for x in c if x.get('outputs')))"</automated>
    <fails_when>`unexecuted` prints anything other than `0`, or `with_output` prints `0` -- an unexecuted code cell or a notebook with no stored output at all means the committed file is not the re-executed artefact the paired-docs rule requires.</fails_when>
    <automated>cd /home/tlister/git/fomo_devel && grep -c 'remint_declined' docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb</automated>
    <fails_when>prints `0`, or exits non-zero (grep -c exits 1 when the count is zero) -- the new counter must appear in at least the summary-line output and the assertion cell, so a zero means the demos were added without exercising the counter the round created.</fails_when>
    <automated>cd /home/tlister/git/fomo_devel && python manage.py test solsys_code.tests.test_projector_demo_notebook -v 2</automated>
    <fails_when>non-zero exit, or "FAILED" / "ERROR" in the last summary line -- the regenerated notebook broke an assertion this module makes about committed notebooks.</fails_when>
    <automated>cd /home/tlister/git/fomo_devel && python manage.py test solsys_code.tests.test_allocation_projector solsys_code.tests.test_allocation_projector_signals solsys_code.tests.test_campaign_reconciler solsys_code.tests.test_reconcile_campaign_runs solsys_code.tests.test_cutover_classical_allocations solsys_code.tests.test_load_telescope_runs solsys_code.tests.test_campaign_views -v 1</automated>
    <fails_when>non-zero exit, or "FAILED" / "ERROR" in the last summary line, or a reported total lower than the count plan 35-24 recorded in its SUMMARY -- this is the round's final green check, so a lower total means something was lost between wave 2 and wave 3. Every module is named by dotted path; a bare `python manage.py test` collects `test_views.TestEphemeris`, which segfaults in native ASSIST and pays the SPICE download.</fails_when>
    <automated>cd /home/tlister/git/fomo_devel && pre-commit run ruff --all-files && pre-commit run ruff-format --all-files</automated>
    <fails_when>either hook prints "Failed" -- D-07's lint gate, re-run at the end of the round. Run it through pre-commit, which pins ruff 0.2.1; an unpinned `ruff` on PATH can report findings the enforced gate does not have.</fails_when>
    <automated>cd /home/tlister/git/fomo_devel && python manage.py makemigrations --check --dry-run</automated>
    <fails_when>non-zero exit, or output naming a model change with no migration -- the round's migration must still cover every model change at the end, not only at the end of wave 2.</fails_when>
    <automated>cd /home/tlister/git/fomo_devel && grep -c 'remint_declined' docs/notebooks/pre_executed/campaign_lifecycle_demo.ipynb; git status --short -- docs/notebooks/pre_executed/campaign_lifecycle_demo.ipynb</automated>
    <fails_when>the printed count is anything other than `0`, or a `git status` line names the notebook. A non-zero count means `campaign_lifecycle_demo.ipynb` does reference this round's new counter after all, so the reason the `<artifacts>` block gives for leaving it out of every plan's `files_modified` is false -- add it to this plan's `files_modified`, re-execute it with the same `nbconvert` invocation Task 2 already uses, and record the deviation in the SUMMARY (Rule 1). A `git status` line beside a `0` count means the notebook was modified with nothing in this round to justify it.</fails_when>
    <automated>cd /home/tlister/git/fomo_devel && git status --short -- solsys_code/ src/</automated>
    <fails_when>the command prints any line at all -- notably `src/fomo_db.sqlite3`, which means a notebook cell wrote the developer database instead of the scratch copy (prohibition 3), or any `solsys_code/` path, which means a source file was modified from a docs plan (prohibition 1).</fails_when>
  </verify>
  <acceptance_criteria>
    - Four new demos exist, each with a prose cell, a code cell with real executed output and an assertion cell: the declined night's label refresh, the confirmed night surviving retirement, the `remint_declined` counter in a real summary line, and the in-place site-definition correction.
    - The stale prose cell claiming `detach_declined` counts two things is replaced, not appended to, and every other prose or assertion cell naming a counter agrees with the round's final behaviour.
    - The notebook's prose uses the same three causes, counter pairing and remedies as the runbook, in the same words.
    - Every code cell has a non-null `execution_count` and the notebook carries stored output.
    - `test_projector_demo_notebook` passes.
    - The seven-module regression run is green with a total no lower than plan 35-24's recorded count; both ruff hooks pass; `makemigrations --check --dry-run` reports no drift.
    - The round's second pairing is discharged explicitly: `campaign_lifecycle_demo.ipynb`'s three grounds for being unaffected are re-checked, the result is recorded in the SUMMARY, and the notebook is either left untouched (all three hold) or added, re-executed and recorded as a Rule 1 deviation.
    - `git status --short` over `solsys_code/` and `src/` prints nothing -- in particular `src/fomo_db.sqlite3` is untouched.
  </acceptance_criteria>
  <done>The committed notebook shows an operator, in real executed output, each of the four things this round changed -- including the one the verifier could only reproduce by hand -- and the round's whole code surface is green behind it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator -> the runbook's counter section | The only place a person learns what a sweep's numbers mean and what, if anything, they must do about them. A stale sentence here is indistinguishable from a correct one at the moment it is read. |
| Notebook execution -> `src/fomo_db.sqlite3` | The notebook runs real management commands. If a cell resolves to the developer database rather than the scratch copy, a documentation build mutates real calendar data. |
| Committed notebook output -> a reader's belief about the code | Stored output is read as evidence. Hand-patched output is a claim with no experiment behind it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-25-01 | Repudiation (operator-facing documentation of the sweep's counters) | `docs/runbooks/telescope_runs_calendar.rst:1060-1185` | medium | mitigate | A counter whose printed meaning is stale is the same wrong-information defect as this round's criticals in a different medium: an operator seeing `retired: 37` on the first post-upgrade sweep would hunt for a window shrink that never happened, and one seeing a duplicate night would have no way to learn that a confirmation guard produced it. Task 1 corrects seven passages and Task 2 shows each of the four changes in real executed output. |
| T-35-25-02 | Tampering (a documentation build mutating production-shaped data) | notebook cells running management commands | high | mitigate | The notebook executes real sweeps. Prohibition 3 and the notebook's existing scratch-copy setup keep every command on a copy of `src/fomo_db.sqlite3`, and the final `git status --short -- solsys_code/ src/` check proves the developer database is byte-unchanged after the whole run. A cell that resolved to the real file would show up there immediately. |
| T-35-25-03 | Tampering (false evidence in a committed artefact) | `reconcile_campaign_runs_demo.ipynb` stored output | medium | mitigate | 35-SECURITY.md `T-35-17`: hand-patched notebook output is a claim with no experiment behind it, and this repository has been caught doing exactly that once. Prohibition 2 forbids editing the JSON; the regeneration is by `nbconvert --execute --inplace`, and the execution-count check in Task 2's verify block fails on any cell that did not actually run. |
| T-35-25-04 | Information Disclosure | executed cells printing rows from a copy of the developer database | low | accept | The notebook prints campaign run and calendar rows from a copy of the developer database, as its existing committed cells already do. Accepted: the data is observing-schedule metadata already visible in the committed notebook and in the repository's other pre-executed demos, contains no credential, and the file is committed to the same repository the database copy came from. No new class of value is printed by this round's four demos. |
| T-35-25-05 | Repudiation | the runbook and the `is_verified` field label disagreeing | low | mitigate | WR-08's underlying risk is a staff member reading a checkbox label and an operator reading the runbook forming different beliefs about the same control. Task 1 quotes the shipped label text from plan 35-24's SUMMARY rather than paraphrasing it, so the two surfaces cannot drift at the moment of writing. |

ASVS level 1 scope note (block on `high`): the one `high` threat is dispositioned `mitigate` with a
named prohibition and a verify command that fails on breach, so nothing blocks. This plan installs no
package -- `pyproject.toml` is untouched and there is no `pip`/`npm`/`cargo` install task -- so
`T-35-SC`'s package-legitimacy gate carries forward from 35-SECURITY.md unchanged and no legitimacy
checkpoint applies. No source, model or migration changes (prohibition 1), so there is no schema,
authentication, session or injection-class surface in this plan at all; its risks are
documentation-accuracy and build-side data-integrity risks, which is what the register above covers.
</threat_model>

<verification>
- The runbook's counter-list sentence and both example summary lines carry `remint_declined` in the
  position the command prints it.
- `retired` reason (2) names three distinct site-correction cases with the remedy for the
  cross-timezone one; reason (5) covers this release's repeat of the audit and the site's stored
  position as a checked input.
- A site-definition-correction paragraph exists, stating the new behaviour, the pre-release behaviour
  and the deliberate below-one-minute case.
- `detach_declined` describes only confirmation-caused declines, including the declined retirement
  with its duplicate-entry consequence and remedy; `remint_declined` has its own paragraph naming
  three causes, the `updated`/`unchanged` pairing, the repeating warning and the remedy; the
  `is_verified` sentence quotes the shipped label.
- The `rekeyed` paragraph is present and unedited (WR-03's residue stays deferred), and the command
  still prints the `detach_declined` wording the runbook quotes.
- The notebook carries four new demos with real executed output and assertion cells, every code cell
  has a non-null `execution_count`, and the stale `detach_declined` prose cell is replaced.
- `test_projector_demo_notebook` passes.
- The seven-module regression is green at the END of the round with a total no lower than plan
  35-24's recorded count; both ruff hooks pass (D-07); `makemigrations --check --dry-run` reports no
  drift.
- `git status --short -- solsys_code/ src/` prints nothing after the whole run, proving no source
  file was modified and the developer database was never written.
- **Stated residuals, not gaps:** (a) WR-03's `rekeyed` stability promise is still inaccurate one
  sweep after a re-key -- deliberately untouched, user-deferred; (b) WR-01's dry-run cost claim is
  not restated in the runbook and is not fixed; (c) the runbook's three-case `retired` reason (2)
  describes the behaviour observed in the tree by plan 35-24's cross-timezone test, so if that test
  recorded a Rule 1 deviation the runbook follows the tree, not the review's hand-trace; (d)
  `35-21-PLAN.md:24`'s "re-mints every one of its nights" truth stays as written -- WR-05's third
  disagreeing claim, left unedited deliberately because a completed plan is a historical record, and
  recorded in this plan's SUMMARY as knowingly superseded rather than silently carried.
- **Edge-probe accounting for this plan (no silent drops):** 1 of the round's 7 probe items is
  authored here -- ALLOC-05 `unclassified`, as a flagged assumption, because its statement is a
  documentation consequence and travels with the deploy note. Of the other 6, two are authored in
  plan 35-23 (ALLOC-01 `ordering`, ALLOC-03 `unclassified`) and four in plan 35-24 (ALLOC-01 `empty`,
  ALLOC-01 `adjacency`, ALLOC-02 `unclassified`, ALLOC-04 `unclassified`). 1 + 2 + 4 = 7 applicable,
  0 dropped.
</verification>

<success_criteria>
- The round's paired-docs obligation is discharged for all four behaviour changes, in one pass
  against the final tree, as CLAUDE.md's rule requires.
- An operator can explain every number in a post-upgrade summary line from the runbook alone, and
  find the remedy for each one that needs an action.
- The two surfaces that describe `is_verified` -- the admin field label and the runbook -- say the
  same thing in the same words.
- ALLOC-03's narrowed promise (a confirmed night keeps its allocation event when the observation
  links) is documented with its consequence and remedy rather than left for an operator to discover
  from a duplicate row.
- ALLOC-05's cutover sequencing is unchanged in code and its post-upgrade consequence for this
  release is written into the deploy note before the sweep runs, not after.
- The notebook's committed output is real executed output, and the developer database is byte-
  unchanged.
- The round is complete at the end of this plan: nothing in `35-23-PLAN.md`'s
  `<review_dispositions>` ledger marked "Closed this round" is outstanding, and every deferred entry
  is still deferred and untouched.
</success_criteria>

<artifacts>
## Artifacts this phase produces

| Artifact | Kind | Produced by |
|----------|------|-------------|
| `docs/runbooks/telescope_runs_calendar.rst` -- `remint_declined` in the counter list and both example summary lines | modified passage | Task 1 |
| `docs/runbooks/telescope_runs_calendar.rst` -- `retired` reason (2) rewritten into three site-correction cases | modified passage | Task 1 |
| `docs/runbooks/telescope_runs_calendar.rst` -- `retired` reason (5) extended to this release's audit and the site position input | modified passage | Task 1 |
| `docs/runbooks/telescope_runs_calendar.rst` -- a new paragraph on correcting a site definition | new passage | Task 1 |
| `docs/runbooks/telescope_runs_calendar.rst` -- `detach_declined` narrowed, with the declined-retirement case and its remedy | modified passage | Task 1 |
| `docs/runbooks/telescope_runs_calendar.rst` -- a new `remint_declined` paragraph with three causes, the counter pairing, the repeating warning and the remedy | new passage | Task 1 |
| `docs/runbooks/telescope_runs_calendar.rst` -- the `is_verified` sentence and the extended post-upgrade deploy note | modified passage | Task 1 |
| `docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb` -- four new executed demos (declined label refresh, confirmed night surviving retirement, `remint_declined` in a real summary line, in-place site-definition correction) | new cells, whole notebook regenerated by re-execution | Task 2 |
| `docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb` -- the stale `detach_declined` prose cell replaced | modified cell | Task 2 |
| `.planning/phases/35-allocation-layer-classical-cutover/35-25-SUMMARY.md` | planning artifact | Task 2 |

**Paired-docs coverage for the whole round -- discharged here.** CLAUDE.md maps
`solsys_code/allocation_projector.py` and
`solsys_code/management/commands/reconcile_campaign_runs.py` ->
`docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb` (line 135, commit `11b14da`), and
the runbook clause is directory-scoped to any page under `docs/runbooks/` whose documented behaviour a
change affects -- here `docs/runbooks/telescope_runs_calendar.rst`. Plans 35-23 and 35-24 changed both
modules' behaviour and are forbidden from touching `docs/`, so this plan covers both in one pass.
Of the three remaining modules the round touches, only `solsys_code/models.py` has no paired notebook
of its own in the map -- checked against CLAUDE.md's map rather than assumed -- so its
operator-visible change, the `is_verified` label and help text, reaches a reader through Task 1's
runbook passage. The other two ARE mapped, and their pairings are named here rather than denied:
`solsys_code/campaign_reconciler.py` maps directly to
`docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb` -- the notebook Task 2 re-executes,
so that pairing is discharged by this plan -- and also, as one of the four v2.2 campaign surfaces
(`campaign_views.py`, `campaign_forms.py`, `campaign_attribution.py`, `campaign_reconciler.py`), to
`docs/notebooks/pre_executed/campaign_lifecycle_demo.ipynb`. `solsys_code/campaign_views.py` is in
the map through that collective v2.2 entry, and through it alone.

**`campaign_lifecycle_demo.ipynb` is deliberately absent from this plan's `files_modified`, on
grounds this plan's executor re-checks rather than inherits.** That notebook documents the v2.2
campaign lifecycle -- public submission, four staff approvals, one `resolve_site` action, one
confirmed attribution, the campaign decoration surviving a from-scratch event rewrite, and
`unlink_event_from_run()` clearing only that decoration -- and its own closing cell points at
`reconcile_campaign_runs_demo.ipynb` "for the reconciler command itself (`--dry-run` counters,
idempotency, sweeps and backfills) -- deliberately not duplicated here". The round's only
`campaign_views.py` change is a fourth entry in `_message_reconcile_side_effects()`'s family of
reconcile-COUNTER messages, guarded by `if result.remint_declined:`; no cell in that notebook reads,
renders or asserts on `django.contrib.messages` or on any `ReconcileResult` counter, and no fixture
in it can produce a declined re-mint (each run is projected once at approval, the only confirmed row
is the orphan event outside any run's family namespace, and `is_verified` is declared
`default=True`), so the message never fires and re-execution would reproduce the existing output
exactly. The full three-ground argument is in `35-23-PLAN.md`'s `<artifacts>` block; Task 2's verify
block re-checks it mechanically, and if the check fails the notebook is re-executed here rather than
left stale. No other module in the pairing map is touched by this round, so no other notebook is
affected.
</artifacts>

<output>
Create `.planning/phases/35-allocation-layer-classical-cutover/35-25-SUMMARY.md` when done.

Record in it: the seven runbook passages as edited, quoting the new `remint_declined` paragraph in
full so the SUMMARY carries the operator-facing text; the four notebook demos with their real
executed counter output; the `nbconvert` invocation and its exit status; the code-cell and
execution-count check results; the seven-module test count; both ruff results; the
`makemigrations --check` result; and the final `git status --short -- solsys_code/ src/` output
proving no source file was modified and the developer database was never written.

Record whether the previously committed site-correction demo's fixture turned out to be a null/null
or a fully-set run, and what that meant for its prose -- that is the exact shape WR-05 showed the
whole test suite and the notebook had missed.

Record `35-21-PLAN.md:24`'s "re-mints every one of its nights" truth as a knowingly superseded
historical claim -- WR-05's third disagreeing claim, the one neither this plan nor 35-24 corrects in
place. Name the file and line, state that it is left unedited deliberately because a completed plan
records what was planned rather than what is currently true, and name its correct current form: the
three-case `retired` reason (2) written here and `_span_needs_remint()`'s step-2 docstring written by
plan 35-24.

Record the `campaign_lifecycle_demo.ipynb` pairing check: the counter grep's output, whether all
three grounds held, and therefore whether the notebook was left untouched or re-executed.

Record any Rule 1 deviation in full, including any documentation claim that could not be made true
without a code change; state such a claim as a residual rather than reaching back into a completed
plan (prohibition 1).

State in the SUMMARY that the round IS complete at the end of this plan: name each entry in
`35-23-PLAN.md`'s `<review_dispositions>` ledger marked "Closed this round" and where it closed, and
name each deferred entry and confirm it is still deferred and untouched.
</output>
