---
phase: 35-allocation-layer-classical-cutover
plan: 17
type: execute
gap_closure: true
wave: 1
depends_on: []
files_modified:
  - solsys_code/management/commands/load_telescope_runs.py
  - solsys_code/tests/test_load_telescope_runs.py
  - solsys_code/management/commands/cutover_classical_allocations.py
  - solsys_code/tests/test_cutover_classical_allocations.py
autonomous: true
requirements: [ALLOC-04, ALLOC-05]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "No in-code sentence claims a dry-run/real-run parity the loader does not have. The `existing is None` (create) arm predicts `night_created += len(nights)` and never calls `reconcile_run()` at all, so a brand-new line whose real reconcile raises previews as `created: 1` while the real pass reports `skipped: 1`. Grounded in 35-VERIFICATION.md PROBE-P5, executed: `dry : Done (dry run). lines processed: 1, created: 1, updated: 0, unchanged: 0, skipped: 0` against `real: Done. lines processed: 1, created: 0, updated: 0, unchanged: 0, skipped: 1`, with 0 `CampaignRun` rows afterwards."
    - "The loader's stated parity is scoped to the arm that actually has it -- the `existing is not None` update/re-mint arm, where the counter fold happens after the preview reconcile returns (the fix 35-14 landed and the third verification pass confirmed). The create arm's inability to predict a reconcile failure is stated, not implied. Per D-02 the command's sole write path remains `write_and_reconcile_campaign_run()` and it still reports the run-level `created/updated/unchanged/skipped` counters alongside the reconcile summary; only what the PREVIEW can predict is narrowed."
    - "`TestMalformedTimezoneSkipsOneLine` pins the create arm's divergence as the KNOWN contract, using the no-existing-`CampaignRun` fixture its own `setUpTestData` already seeds and which no test currently runs `--dry-run` over. Whichever way the contract is later changed, a test has to change with it."
    - "D-18's posture is unchanged and better served: an event the command cannot explain is still left untouched and reported with its reason, and the reason it prints now names the cause that is actually true for the branch that printed it. The cutover's `duplicate_identity` reason label states BOTH causes the command can report it for. CR-01 gave the reason a second, structurally different cause -- a pre-existing `CampaignRun` claimant whose stored marker is absent or differs -- and today the single-cause label is prefixed verbatim onto that branch's stderr, producing a self-contradictory line that sends an operator looking for a second schedule line that does not exist."
    - "The cutover documents its own re-run gotcha: a claimant whose stored `Source line:` marker MATCHES is find-and-updated, so `run_status`, `target`, `campaign`, `window_start`/`window_end`, the sub-night fields and `observation_details` are all re-applied from the schedule line on every invocation, and a post-import staff edit to those fields does not survive the next cutover run. Reproduced as PROBE-P4: a claimant with a matching marker and `run_status` set to CANCELLED by staff comes back as `planned` with exit 0 and `runs updated: 1`."
    - "The no-marker remedy text names that consequence in the same sentence that sends the operator to the Django admin. The remedy instructs restoring the marker, which converts a refused (safe) claimant into a matching (re-applied) one -- the documentation's own worked remedy is the trigger, and it must say so."
    - "PROBE-P4's outcome is pinned by a test as the INTENDED, file-authoritative behaviour. This is distinct from the round-2 destructive-case test, which pins the no-marker/mismatched-marker case as a REFUSAL; this one pins the matching-marker case as a conversion that overwrites."
    - "The matching-marker overwrite is NOT narrowed in code. The third verification pass judged it non-blocking and intentional -- it leaves no duplicate and no orphan, it changes run fields rather than calendar topology, and it is the project's already-accepted file-authoritative semantics (`import_campaign_csv`'s documented re-import gotcha). A new refusal path in a command with two consecutive rounds of refusal-path regressions is explicitly out of scope."
    - "[edge probe ALLOC-04 `unclassified` -- RESOLVED, verification: explicit] The loader's preview and its real pass either report the same decision or the preview's inability to predict is stated in the code and pinned by a test. No sentence is left asserting agreement the create arm cannot deliver."
    - "[edge probe ALLOC-05 `unclassified` -- RESOLVED, verification: explicit] Every reason the cutover prints names a cause that is true for the branch that printed it."
    - "[already covered by 35-12-PLAN.md must_haves] The identity guard still refuses a claimant whose marker is absent or differs, still exits non-zero, and still leaves the refused run byte-identical."
  artifacts:
    - solsys_code/management/commands/load_telescope_runs.py
    - solsys_code/tests/test_load_telescope_runs.py
    - solsys_code/management/commands/cutover_classical_allocations.py
    - solsys_code/tests/test_cutover_classical_allocations.py
  key_links:
    - "The loader's create arm (`existing is None`) -> the run-level `created` counter. The link is a literal window length, not a reconcile; the code now says so where the counter is folded, so the next reader does not infer a preview that is not there."
    - "`_REASON_LABELS[_DUPLICATE_IDENTITY]` -> every `duplicate_identity` stderr line -> the runbook's reason vocabulary (plan 35-18). The label is the shared operator phrase NF-25 established; both causes must appear in it before the runbook can mirror it."
    - "The no-marker remedy string -> the operator's admin edit -> the matching-marker overwrite. This is the loop the third verification pass flagged for a human decision; the caveat closes it with a sentence rather than a refusal path."
  prohibitions:
    - "The loader's create arm must NOT gain a preview write path -- no transient `CampaignRun` row, no `transaction.set_rollback(True)` probe, no speculative reconcile. 35-VERIFICATION.md rejects this explicitly as the only proposal on the table that adds a new write path to a command with a four-round regression history."
    - "The cutover's matching-marker branch must NOT gain a new refusal path, a `_RUN_DRIFT` reason code or a field-comparison guard. The behaviour is documented, not changed."
    - "The narrowed claims must NOT be softened into hedges (`usually`, `in normal operation`, `in most cases`). State which arm has the property and which does not."
    - "No existing `duplicate_identity` test may be weakened to accommodate the new label -- the reason KEY (`duplicate_identity`) and the summary counts are unchanged; only the human-readable label text grows."
    - "Dropped as canon security/compliance boilerplate rather than minted here: credential-logging, TLS-downgrade and authz-bypass prohibitions -- both files are staff-only management commands with no request path and no secret."
---

<objective>
Close the code half of Defect B and of the two vocabulary/caveat findings.

Defect B: `load_telescope_runs --dry-run`'s create arm predicts `night_created += len(nights)` and
never calls `reconcile_run()`, so nothing in that arm can fail -- while the real branch runs
`write_and_reconcile_campaign_run()` for the same line, whose reconcile can raise and reports the
line under `skipped`. The second gap-closure round fixed the sibling arm and then wrote a sentence
about both. The fix here is subtractive: narrow the claim to the arm that has it, and pin the
create arm's divergence as the stated contract.

Finding WR-04: `_REASON_LABELS[_DUPLICATE_IDENTITY]` describes only the original group-versus-group
collision, and is printed verbatim above the claimant-marker stderr lines CR-01 introduced.

Finding WR-02: the matching-marker branch re-applies the schedule line's fields over a post-import
staff edit. The third verification pass judged this intended and non-blocking -- it is the same
file-authoritative behaviour `import_campaign_csv` has and the runbook already documents at length
-- but the cutover has no equivalent caveat, no test pinning the intended outcome, and a remedy
paragraph that routes an operator straight into it. This plan adds the caveat and the test, and
changes no behaviour.

Purpose: stop over-claiming and start stating. Every sentence this plan writes is narrower than the
one it replaces.

Output: two narrowed in-code claims, two new tests, a two-cause reason label and a re-run gotcha
paragraph -- all in code, so plan 35-18 can mirror them into the runbook and regenerate the two
notebooks that quote them.

No `type="tracer"` task: this plan adds no code path. The two slices it documents were proven by
35-05's and 35-12's tracers; its runnable gates are two tests that drive the real commands through
`call_command()` against a real database.

Wave 1. No dependencies: the four files are owned by this plan alone in this round.
</objective>

<execution_context>
@/home/tlister/git/fomo_devel/.claude/gsd-core/workflows/execute-plan.md
@/home/tlister/git/fomo_devel/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md

@.planning/phases/35-allocation-layer-classical-cutover/35-VERIFICATION.md
@.planning/phases/35-allocation-layer-classical-cutover/35-REVIEW.md
@.planning/phases/35-allocation-layer-classical-cutover/35-12-SUMMARY.md
@.planning/phases/35-allocation-layer-classical-cutover/35-14-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Say what the loader's preview can and cannot predict, and pin it</name>
  <files>solsys_code/management/commands/load_telescope_runs.py, solsys_code/tests/test_load_telescope_runs.py</files>
  <read_first>
    - `solsys_code/management/commands/load_telescope_runs.py` lines 294-347 -- the whole dry-run/real branch pair: the `existing is not None` preview reconcile, the `existing is None` window-length prediction at lines 307-310, the counter-fold comment at lines 312-326, and the real branch below it.
    - `solsys_code/management/commands/load_telescope_runs.py` lines 165-180 -- the `--dry-run` flag's help text, which already tells the truth about NIGHT counts for a brand-new run and must now tell it about the run-level decision too.
    - `solsys_code/tests/test_load_telescope_runs.py` lines 578-700 -- `TestMalformedTimezoneSkipsOneLine` in full: its class docstring, the `setUpTestData` that seeds NTT with the `America/Santigo` typo and no `CampaignRun`, `_write_schedule_file()`, and `test_dry_run_and_real_run_report_the_same_counters_for_a_skipped_line` (which repairs the timezone first precisely so it can take the OTHER arm).
    - `solsys_code/tests/test_load_telescope_runs.py` -- the `_parse_run_summary()` module helper the existing parity test uses to turn a summary line into a five-tuple.
    - `.planning/phases/35-allocation-layer-classical-cutover/35-VERIFICATION.md` gap 2 -- PROBE-P5's executed output verbatim and both `missing:` items, the second of which is the one this task implements.
    - `.planning/phases/35-allocation-layer-classical-cutover/35-REVIEW.md` lines 321-383 (WR-03) -- the full reproduction and the explicit alternative this task takes (narrow the claim) rather than the one it must not (a transient rolled-back row).
  </read_first>
  <action>
    In `solsys_code/management/commands/load_telescope_runs.py` -- comments and help text only, no
    behaviour change:

    - At the create arm (the `else:` branch that folds a window length into `night_created`),
      extend the comment so it states the consequence, not just the mechanism: no preview reconcile
      runs on this arm at all, so nothing here can fail, while the real branch runs the same
      reconcile through `write_and_reconcile_campaign_run()` and reports the line under `skipped`
      when it raises. Name `PROBE-P5` and quote its two summary lines (`created: 1` on the preview
      against `skipped: 1` for real, with zero rows written) so the next reader has the
      reproduction rather than a claim. State that the deliberate choice is to leave the preview
      unable to predict rather than to add a speculative write path to this command, citing
      35-VERIFICATION.md's rejection of the transient-row alternative.
    - At the counter-fold comment (the block explaining why the fold sits below the preview
      reconcile), add one sentence scoping it: that ordering restores the preview/real agreement on
      the `existing is not None` arm, which is the arm that has a preview reconcile to order
      against. Do not let the comment read as a statement about every line.
    - In the `--dry-run` help text, extend the brand-new-run sentence from night counts to the
      run-level decision: for a line that would create a brand-new run, the preview reports
      `created` without previewing the reconcile, so a line the real pass will drop under `skipped`
      still previews as `created`.

    Use the phrase `create arm` in at least one of these passages so the scoping is greppable.
    State the predicate -- which arm has the property and which does not -- and do not soften it
    into a hedge; the file is gated at zero occurrences of the hedge words 35-VERIFICATION.md
    forbids, so a hedge written here fails the task's own verify.

    In `solsys_code/tests/test_load_telescope_runs.py`, add one test to
    `TestMalformedTimezoneSkipsOneLine` -- name it
    `test_dry_run_of_a_brand_new_line_cannot_predict_a_reconcile_failure`:

    - Use the class fixture AS SEEDED (NTT already carries the `America/Santigo` typo and no
      `CampaignRun` exists) -- this is the no-existing-run state PROBE-P5 uses and that the sibling
      parity test deliberately repairs before it runs. Do not repair the timezone here.
    - Write a one-line schedule file with `NTT EFOSC2 allocation 9-13 July` via
      `_write_schedule_file()`, run `--dry-run`, then run the real pass, capturing both stdouts.
    - Parse both summaries with `_parse_run_summary()` and assert the DIVERGENCE as the contract:
      the preview reports the line under `created` and the real pass reports it under `skipped`.
      Assert the exact five-tuples PROBE-P5 records rather than only that they differ.
    - Assert `CampaignRun.objects.count()` is 0 afterwards, so the test also pins that the real
      pass wrote nothing (the divergence is in the report, not in the data).
    - Docstring: state that this pins a KNOWN, DELIBERATE limitation, not a passing parity
      contract; that the create arm has no preview reconcile to fail; that the round-2 counter-fold
      fix covers the `existing is not None` arm only; and that the alternative -- previewing
      against a transient rolled-back row -- was rejected because it adds a new write path to a
      command with a four-round regression history. Name PROBE-P5.
    - If the exact tuples do not match when the test is first run, re-read PROBE-P5 in
      35-VERIFICATION.md and report the difference rather than relaxing the assertion into an
      inequality.
  </action>
  <verify>
    <automated>python manage.py test solsys_code.tests.test_load_telescope_runs.TestMalformedTimezoneSkipsOneLine</automated>
    <fails_when>Prints `FAILED (failures=` naming the new test -- most likely its exact-tuple assertion, meaning the create arm's preview no longer reports `created: 1` for this fixture; re-read PROBE-P5 before adjusting the assertion.</fails_when>
    <automated>python manage.py test solsys_code.tests.test_load_telescope_runs</automated>
    <fails_when>Prints `FAILED (` -- a comment-only edit in the command broke a code line (the counter-fold comment sits inside the dry-run branch, so a mis-placed edit can swallow a statement), or the new test asserted a summary substring the command does not print.</fails_when>
    <automated>test "$(grep -c 'PROBE-P5' solsys_code/management/commands/load_telescope_runs.py)" -ge "1"</automated>
    <fails_when>Exits 1 because the count is `0` -- the create-arm comment states a limitation without citing the executed reproduction behind it.</fails_when>
    <automated>test "$(grep -c 'create arm' solsys_code/management/commands/load_telescope_runs.py)" -ge "1"</automated>
    <fails_when>Exits 1 because the count is `0` -- no passage names the arm the limitation belongs to, so the scoping is not greppable and the next reader inherits the same ambiguity.</fails_when>
    <automated>test "$(grep -ciE 'usually|in normal operation|in most cases' solsys_code/management/commands/load_telescope_runs.py)" = "0"</automated>
    <fails_when>Exits 1 because a hedge word was written in place of the predicate, which 35-VERIFICATION.md forbids for exactly this class of correction.</fails_when>
    <automated>test "$(grep -c 'transaction.set_rollback' solsys_code/management/commands/load_telescope_runs.py)" = "0"</automated>
    <fails_when>Exits 1 because a transient rolled-back preview row was added -- the one fix 35-VERIFICATION.md explicitly rules out for this command.</fails_when>
    <automated>git status --short solsys_code/allocation_projector.py</automated>
    <fails_when>Prints any line -- this plan must leave plan 35-16's file untouched; the two plans run in the same wave and must not both edit the projector.</fails_when>
  </verify>
  <acceptance_criteria>
    - `python manage.py test solsys_code.tests.test_load_telescope_runs` prints `OK`.
    - `test_dry_run_of_a_brand_new_line_cannot_predict_a_reconcile_failure` exists in `TestMalformedTimezoneSkipsOneLine`, runs `--dry-run` over the no-existing-run fixture, and asserts the preview reports `created` where the real pass reports `skipped`.
    - That test also asserts `CampaignRun.objects.count()` is `0` after both passes.
    - `grep -c 'PROBE-P5' solsys_code/management/commands/load_telescope_runs.py` prints at least `1` (it is `0` before this task).
    - `grep -c 'create arm' solsys_code/management/commands/load_telescope_runs.py` prints at least `1` (it is `0` before this task).
    - No hedge word (`usually`, `in normal operation`, `in most cases`) appears in the file.
    - No new `CampaignRun` write, `transaction.atomic()` block or reconcile call was added to the dry-run branch.
  </acceptance_criteria>
  <done>The loader's own code says which arm's preview agrees with the real run and which cannot, with the executed reproduction named beside it, and a test fails the moment either statement stops being true.</done>
</task>

<task type="auto">
  <name>Task 2: Two causes in the reason label, and the cutover's missing re-run gotcha</name>
  <files>solsys_code/management/commands/cutover_classical_allocations.py, solsys_code/tests/test_cutover_classical_allocations.py</files>
  <read_first>
    - `solsys_code/management/commands/cutover_classical_allocations.py` lines 170-183 -- `_REASON_LABELS` in full, so the new entry matches its siblings' length and voice (each is a short noun phrase prefixed onto a per-event stderr line).
    - `solsys_code/management/commands/cutover_classical_allocations.py` lines 415-490 -- the identity guard's two-branch reason strings and the `fields` dict that is written for a matching claimant; the dict is the exact list of fields the caveat must name.
    - `solsys_code/management/commands/cutover_classical_allocations.py` lines 1-70 -- the module docstring, in particular the paragraph enumerating the unexplained reasons and its closing remedy sentences, which is where the gotcha paragraph belongs.
    - `docs/runbooks/telescope_runs_calendar.rst` lines 733-756 -- the `import_campaign_csv` "Re-import gotcha" note. This is the voice to match: it names the fields, says the overwrite is expected behaviour for this class of command rather than a bug, and closes with an instruction to act deliberately.
    - `solsys_code/tests/test_cutover_classical_allocations.py` lines 1108-1300 -- `TestDatabaseScopedIdentityGuard`: its `_make_pre_existing_claimant()` helper, `_THREE_NIGHT_LINE`, `test_pre_existing_claimant_with_same_source_line_still_converts` (the benign matching-marker path this task extends) and `test_no_marker_claimant_keeps_run_status_details_and_target_byte_identical` (the refusal-case regression this task must NOT duplicate).
    - `.planning/phases/35-allocation-layer-classical-cutover/35-VERIFICATION.md` `flagged_prohibitions` -- PROBE-P4's executed output and the `verifier_judgment` paragraph stating on three grounds why this is a caveat plus a test, not a code change.
    - `.planning/phases/35-allocation-layer-classical-cutover/35-REVIEW.md` lines 384-431 (WR-04) -- the exact two-cause sentence to mirror.
  </read_first>
  <action>
    In `solsys_code/management/commands/cutover_classical_allocations.py`:

    1. Rewrite `_REASON_LABELS[_DUPLICATE_IDENTITY]` to name both causes: either a second Source
       line resolving to the same run identity key as an earlier group, OR a `CampaignRun` row that
       already holds the derived identity key and cannot be proved to have come from this line.
       Use the phrase `already holds the derived identity key` verbatim so the three definitions
       (this one plus the runbook's two, which plan 35-18 writes) share one greppable anchor.
       Keep it a single short noun phrase -- it is prefixed onto every per-event stderr line, so a
       paragraph here makes the output unreadable. Let `ruff-format` handle the wrapping (implicit
       string concatenation across two lines is fine); do not exceed 120 columns.

    2. Add a re-run gotcha paragraph to the module docstring, immediately after the paragraph that
       explains the `duplicate_identity` refusal. It must state: a claimant whose stored
       `Source line:` marker MATCHES is find-and-updated, so `source`, `approval_status`,
       `run_status`, `target`, `campaign`, `site`/`site_raw`, `window_start`/`window_end`, the two
       sub-night fields and `observation_details` are all re-applied from the schedule line on
       every invocation; a post-import staff edit to any of them does not survive the next cutover
       run. Say that this is the same file-authoritative behaviour `load_telescope_runs` has on
       every re-import and `import_campaign_csv` has on every re-import (documented in the
       runbook's "Re-import gotcha" section), and that it is expected behaviour for this class of
       command rather than a defect -- the command CAN prove this row came from this line, which is
       exactly the distinction the identity guard establishes. Close with the interaction: the
       no-marker remedy tells an operator to restore the marker, which converts a refused claimant
       into a matching one that will then be re-applied, so restore the marker deliberately.
       Use the phrase `does not survive the next cutover run` verbatim -- plan 35-18 mirrors it
       into the runbook and both are gated on it.

    3. Extend the no-marker branch's `reason` string with that consequence, in the same sentence
       that sends the operator to the Django admin: after `then re-run`, add a clause stating that
       a run whose marker matches has the schedule line's fields re-applied to it. Use the phrase
       `re-applied from the schedule line` verbatim. Keep the existing wording ahead of it intact
       -- `CampaignRun pk=`, the key repr, `Django admin` and `then re-run` are all asserted by
       existing tests and mirrored in the runbook.

    Change no predicate, no branch and no write. The guard's behaviour is unchanged by this task.

    In `solsys_code/tests/test_cutover_classical_allocations.py`, add one test to
    `TestDatabaseScopedIdentityGuard` -- name it
    `test_matching_marker_claimant_has_its_staff_edited_fields_reapplied_from_the_line`:

    - Build the claimant with `_make_pre_existing_claimant(observation_details=f'Status:
      allocation\nSource line: {_THREE_NIGHT_LINE}')` -- the MATCHING marker, exactly as PROBE-P4
      did, and exactly as `test_pre_existing_claimant_with_same_source_line_still_converts` does.
    - Apply the staff edit PROBE-P4 applies: set `run_status` to `CampaignRun.RunStatus.CANCELLED`
      and save it.
    - Build the three-night group with `_make_three_night_group()` and run the command with stdout
      and stderr captured. Assert it does NOT raise `CommandError` -- exit 0 is part of the pinned
      outcome.
    - Assert the summary reports `runs created: 0, updated: 1` and `unexplained: 0` (match against
      the substrings the summary line actually prints; read the summary construction in the command
      before hardcoding), and that after `refresh_from_db()` the claimant's `run_status` is back to
      the schedule line's value.
    - Assert the three events were re-keyed onto `ALLOC:{run.pk}:` -- the conversion is the
      behaviour being preserved, not merely tolerated.
    - Docstring: this pins PROBE-P4's outcome as INTENDED. Name the three grounds from
      35-VERIFICATION.md's `verifier_judgment`: it breaches no ROADMAP success criterion (no
      duplicate, no orphan, nothing deleted); the literal no-silent-mutation prohibition is in
      tension with 35-12 truth 5, which REQUIRES a matching-marker claimant to convert, and the
      prohibition's operative meaning as CR-01 resolved it is "must not mutate a run it cannot
      PROVE it owns"; and it is the project's already-accepted file-authoritative semantics.
      State explicitly that this is the MATCHING-marker case and that the no-marker and
      mismatched-marker cases are pinned as refusals by the two sibling tests -- so a future reader
      cannot mistake one for a contradiction of the other.
  </action>
  <reversibility rating="reversible">Label text, docstring prose, one appended clause on an existing reason string and one new test. No predicate, no branch, no schema, no write path. Reverting is a text edit.</reversibility>
  <verify>
    <automated>python manage.py test solsys_code.tests.test_cutover_classical_allocations</automated>
    <fails_when>Prints `FAILED (` -- most likely the new test's summary-substring assertion, meaning the summary line's wording differs from what was hardcoded; read the summary construction in the command rather than loosening the assertion.</fails_when>
    <automated>python manage.py test solsys_code.tests.test_cutover_classical_allocations.TestDatabaseScopedIdentityGuard</automated>
    <fails_when>Prints `Ran 5 tests` instead of `Ran 6 tests` -- the new PROBE-P4 test was not added to this class.</fails_when>
    <automated>test "$(grep -c 'already holds the derived identity key' solsys_code/management/commands/cutover_classical_allocations.py)" -ge "1"</automated>
    <fails_when>Exits 1 because the count is `0` -- the reason label still names only the group-versus-group cause, so the claimant-marker stderr lines still assert a cause that is false for them.</fails_when>
    <automated>test "$(grep -c 'does not survive the next cutover run' solsys_code/management/commands/cutover_classical_allocations.py)" -ge "1"</automated>
    <fails_when>Exits 1 because the count is `0` -- the module docstring carries no re-run gotcha, so the overwrite remains an undocumented residual risk.</fails_when>
    <automated>test "$(grep -c 're-applied from the schedule line' solsys_code/management/commands/cutover_classical_allocations.py)" -ge "1"</automated>
    <fails_when>Exits 1 because the count is `0` -- the no-marker remedy still sends an operator to the admin without naming what restoring the marker causes on the next run.</fails_when>
    <automated>test "$(grep -c '_RUN_DRIFT' solsys_code/management/commands/cutover_classical_allocations.py)" = "0"</automated>
    <fails_when>Exits 1 because a new refusal reason was introduced -- the reviewer's narrowing fix, which the third verification pass overruled in favour of documentation.</fails_when>
    <automated>test "$(grep -c 'existing_source_line != source_line' solsys_code/management/commands/cutover_classical_allocations.py)" = "1"</automated>
    <fails_when>Exits 1 because the count is not `1` -- CR-01's guard predicate was altered or duplicated by a task that must only change text.</fails_when>
    <automated>pre-commit run ruff --files solsys_code/management/commands/cutover_classical_allocations.py solsys_code/tests/test_cutover_classical_allocations.py solsys_code/management/commands/load_telescope_runs.py solsys_code/tests/test_load_telescope_runs.py</automated>
    <fails_when>Prints `Failed` -- a rewritten string exceeds 120 columns or uses double quotes where the pinned ruff config wants single.</fails_when>
    <automated>pre-commit run ruff-format --files solsys_code/management/commands/cutover_classical_allocations.py solsys_code/tests/test_cutover_classical_allocations.py solsys_code/management/commands/load_telescope_runs.py solsys_code/tests/test_load_telescope_runs.py</automated>
    <fails_when>Prints `Failed` with `- files were modified by this hook` -- the new label or docstring was hand-wrapped against the formatter's preference.</fails_when>
  </verify>
  <acceptance_criteria>
    - `python manage.py test solsys_code.tests.test_cutover_classical_allocations` prints `OK`, and `TestDatabaseScopedIdentityGuard` runs 6 tests.
    - `_REASON_LABELS[_DUPLICATE_IDENTITY]` names both causes and contains the phrase `already holds the derived identity key`.
    - The module docstring contains a re-run gotcha paragraph naming the re-applied fields and the phrase `does not survive the next cutover run`.
    - The no-marker reason string contains the phrase `re-applied from the schedule line` and still contains `CampaignRun pk=`, `Django admin` and `then re-run`.
    - `grep -c '_RUN_DRIFT' solsys_code/management/commands/cutover_classical_allocations.py` prints `0` and `grep -c 'existing_source_line != source_line' ...` prints `1`.
    - The new test asserts exit 0, `updated: 1`, `unexplained: 0`, the reverted `run_status`, and three events re-keyed under `ALLOC:`.
    - Its docstring names PROBE-P4 and distinguishes the matching-marker case from the two refusal cases pinned by its siblings.
    - `pre-commit run ruff --all-files` and `pre-commit run ruff-format --all-files` both print `Passed`.
  </acceptance_criteria>
  <done>Every `duplicate_identity` line the command prints names a cause that is true for the branch that printed it, and the one field-overwrite the command performs on purpose is written down where an operator will read it and asserted where a future change will trip over it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator reading a `--dry-run` summary -> the decision to run the real import | The preview is the control the runbook tells the operator to use first. A create-arm line previewing as `created` when the real pass drops it means the control reports success for work that will not happen. |
| Schedule file (operator-authored) -> `CampaignRun` fields on a claimant row | The cutover treats the file as authoritative for any row whose marker it can prove. That is intended, but it means a file re-run outranks a staff edit made in the admin. |
| Command stderr reason text -> the operator's diagnostic action | A label naming a cause that is false for the branch that printed it sends the operator to look for evidence that does not exist, delaying the real remedy. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-36 | Repudiation | `load_telescope_runs --dry-run` create-arm summary | medium | mitigate | A preview that reports `created` for a line the real pass drops leaves the operator with no record that the line failed until they re-read a stderr line they were told to expect nothing from. Task 1 states the limitation at the fold, in the `--dry-run` help text, and pins it with a test asserting PROBE-P5's exact tuples. |
| T-35-37 | Tampering | the cutover's matching-marker field re-application | medium | accept | A post-import staff edit to `run_status`, `target`, `campaign`, the window or `observation_details` is reverted on the next invocation, with exit 0. Accepted on the third verification pass's three grounds: no calendar duplicate or orphan results, 35-12 truth 5 REQUIRES the matching-marker claimant to convert, and this is the project's documented file-authoritative re-import semantics. Mitigated by documentation and a pinning test, not by a refusal path -- a new refusal path in this command has produced regressions in two consecutive rounds. |
| T-35-38 | Spoofing | `_REASON_LABELS[_DUPLICATE_IDENTITY]` as the shared operator phrase | low | mitigate | A single-cause label prefixed onto a two-cause reason misidentifies the condition, sending an operator hunting a second schedule line that does not exist. Task 2 states both causes behind one greppable anchor so the runbook's two definitions (plan 35-18) can be gated against the same phrase. |
| T-35-39 | Elevation of Privilege | a hand-typed `Source line:` marker in the admin granting a full field overwrite | low | accept | `observation_details` is writable from the admin, `import_campaign_csv` and the campaign submission form, so a matching marker is evidence of format, not of provenance. Carried forward as the coincidental-reliance item the third verification pass recorded (promote provenance into a field the operator cannot overwrite); out of scope for this round, which is explicitly bounded to four items. The caveat Task 2 adds is what makes the residual risk visible in the meantime. |
| T-35-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no package: no `pip install` task, no change to `pyproject.toml`, no new third-party import. Both tests use `call_command` and models already imported by their modules. The package-legitimacy gate does not fire. |
</threat_model>

<verification>
Run all of the following from the repository root before considering the plan complete:

1. `python manage.py test solsys_code.tests.test_load_telescope_runs solsys_code.tests.test_cutover_classical_allocations` -- expect `OK`.
2. `python manage.py test solsys_code.tests.test_allocation_projector solsys_code.tests.test_allocation_projector_signals solsys_code.tests.test_campaign_reconciler solsys_code.tests.test_cutover_classical_allocations solsys_code.tests.test_load_telescope_runs solsys_code.tests.test_observation_projector_signals` -- expect `OK`. This is the five-module surface the third verification pass ran (201 tests) plus `test_allocation_projector_signals`, the module that pass cited for ROADMAP SC-3 and the one covering the receivers that drive the allocation projector plan 35-16 modifies in the same wave -- so the run is larger than 201 and the number is not pinned here.
3. `pre-commit run ruff --all-files` and `pre-commit run ruff-format --all-files` -- expect `Passed` on both.
4. `git status --short` -- expect no modified file beyond the four in `files_modified` and `.planning/`. In particular `docs/`, `src/fomo_db.sqlite3` and `solsys_code/allocation_projector.py` (plan 35-16's file) must be clean.
</verification>

<success_criteria>
- The loader's create-arm limitation is stated in the code with PROBE-P5 named, and pinned by a test asserting the preview/real divergence over the no-existing-run fixture.
- No new preview write path, transient row or rollback probe exists in `load_telescope_runs`.
- `_REASON_LABELS[_DUPLICATE_IDENTITY]` names both causes behind the shared `already holds the derived identity key` anchor.
- The cutover module docstring carries a re-run gotcha naming every re-applied field, and the no-marker remedy names the consequence of restoring a marker.
- PROBE-P4's outcome is pinned as intended by a sixth test in `TestDatabaseScopedIdentityGuard`, with CR-01's guard predicate unchanged.
- The six-module test surface, `ruff` and `ruff-format` all pass.
</success_criteria>

## Artifacts this phase produces

| Artifact | Kind | Where |
|----------|------|-------|
| Scoped create-arm comment naming PROBE-P5 | documentation prose in code | `solsys_code/management/commands/load_telescope_runs.py` (currently lines 307-326) |
| Extended `--dry-run` help text covering the run-level decision | modified command option help | `solsys_code/management/commands/load_telescope_runs.py` (currently lines 165-180) |
| `test_dry_run_of_a_brand_new_line_cannot_predict_a_reconcile_failure` (PROBE-P5) | new pinning test | `solsys_code/tests/test_load_telescope_runs.py`, `TestMalformedTimezoneSkipsOneLine` |
| Two-cause `_REASON_LABELS[_DUPLICATE_IDENTITY]` | modified reason label | `solsys_code/management/commands/cutover_classical_allocations.py` (currently line 180) |
| Re-run gotcha paragraph | documentation prose in code | `solsys_code/management/commands/cutover_classical_allocations.py` module docstring |
| No-marker remedy clause naming the re-application consequence | modified reason string | `solsys_code/management/commands/cutover_classical_allocations.py` (currently lines 436-443) |
| `test_matching_marker_claimant_has_its_staff_edited_fields_reapplied_from_the_line` (PROBE-P4) | new pinning test | `solsys_code/tests/test_cutover_classical_allocations.py`, `TestDatabaseScopedIdentityGuard` |

No new module, class, migration, dependency or management command. No behaviour change: every code edit in this plan is text, and the two tests assert behaviour that already ships.

<output>
Create `.planning/phases/35-allocation-layer-classical-cutover/35-17-SUMMARY.md` when done.
</output>
