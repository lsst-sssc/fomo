---
phase: 35-allocation-layer-classical-cutover
plan: 09
type: execute
gap_closure: true
wave: 1
depends_on: []
files_modified:
  - solsys_code/management/commands/load_telescope_runs.py
  - solsys_code/tests/test_load_telescope_runs.py
  - solsys_code/allocation_projector.py
  - solsys_code/tests/test_allocation_projector.py
  - solsys_code/campaign_reconciler.py
autonomous: true
requirements: [ALLOC-01, ALLOC-02, ALLOC-04]

estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A schedule file whose second line resolves to an `Observatory` with a mistyped IANA `timezone` is skipped and logged per-line -- the command reaches its summary, reports `skipped: 1`, and the FOLLOWING line is still processed (NF-21). One bad row never aborts the whole run, which is the invariant the operator runbook states for this command."
    - "The malformed-timezone stderr line names the stage that actually failed -- the `Observatory`, its obscode and the offending timezone string -- not a bare key repr from the parse bucket."
    - "The aborted line leaves no committed `CampaignRun` row: the `transaction.atomic()` boundary NF-08 added still rolls the write back before the new handler reports the skip."
    - "`reconcile_run(run, dry_run=True)` over an existing night whose operator-edited `night_start_utc`/`night_end_utc` pair is inverted raises the same `ValueError` the immediately following real run raises -- the re-mint branch, not only the create branch, is guarded (NF-20)."
    - "A single legacy `RUN:{pk}:{date}` event attributed to a different run, reached on the takeover path, is reported as `blocked == 1` with exactly one log line -- not `blocked == 2` with two (NF-22)."
    - "`_detach_stale_family_events()`'s return annotation, its docstring and its actual return statement all state a four-element tuple (NF-23)."
    - "[flagged assumption -- edge probe ALLOC-02 `unclassified`] The site-local night boundary's failure mode is an inverted sub-night span, and it is now surfaced identically on both the dry-run and real paths of BOTH `_mint_fields()` caller branches. A null `night_start_utc`/`night_end_utc` field's boundary still depends on `sun_event()` and is deliberately left unchecked on the dry-run path, matching `_span_needs_remint()`'s own null-field convention -- an explicit assumption, written into the helper's docstring."
    - "[flagged assumption -- edge probe ALLOC-04 `unclassified`] `Observatory.timezone` is an unvalidated `CharField` the runbook tells operators to hand-type, so a blank value (`ValueError`) and a typo (`ZoneInfoNotFoundError`, a `KeyError` subclass) are two DIFFERENT exception classes reaching the same per-line body. Both are now handled per-line; neither is allowed to reach the outer loop."
    - "[already covered by 35-01-PLAN.md must_haves] `sun_event()` is still called only when a night is being created or re-minted -- adding the inversion guard to the dry-run paths introduces no astropy call, because a set sub-night pair is resolvable with `zoneinfo` alone."
    - "[already covered by 35-01/35-06-PLAN.md must_haves] `ALLOC:` remains the allocation projector's namespace alone and `RUN:{pk}` remains the reconciler's; the NF-22 fix changes only when a legacy url is recorded as claimed, never which module writes it."
  artifacts:
    - solsys_code/management/commands/load_telescope_runs.py
    - solsys_code/allocation_projector.py
    - solsys_code/campaign_reconciler.py
    - solsys_code/tests/test_load_telescope_runs.py
    - solsys_code/tests/test_allocation_projector.py
  key_links:
    - "`load_telescope_runs.handle()` per-line `try` -> a dedicated `except ZoneInfoNotFoundError` clause ahead of `(ValueError, Observatory.DoesNotExist)` -- the two sibling ingest commands agree again about whether one bad site aborts the batch."
    - "`_raise_if_set_window_inverted()` -> both `if dry_run:` short-circuits in `project_allocation()`'s per-night loop -- one shared guard, so a third caller of `_mint_fields()` cannot reopen NF-10 a third time."
    - "`project_allocation()` takeover branch -> `legacy_urls_claimed` -- claimed on EVERY decision including a block, so `campaign_reconciler`'s `foreign` fold cannot count the same row twice."
  prohibitions:
    - "One bad schedule line must NOT abort the whole `load_telescope_runs` import."
    - "A `--dry-run` preview must NOT hide a condition that makes the immediately following real run raise."
    - "A single calendar row must NOT be reported under a counter more than once."
    - "A function's signature annotation must NOT contradict its own docstring or its return statement."
    - "Dropped as canon security/compliance boilerplate rather than minted here: credential-logging and transport-security prohibitions -- these modules make no network call by contract (TRIG-02) and handle no secret."
---

<objective>
Close the four remaining 35-REVIEW.md warnings that live in the write path: the loader's
exception-routing regression (NF-21, which this phase's own NF-08 fix introduced), the
dry-run inversion guard that covers only one of `_mint_fields()`'s two caller branches
(NF-20), the legacy-event double count on the takeover path (NF-22, which this phase's own
NF-15 fix introduced), and the four-valued function still annotated as returning three
(NF-23).

Purpose: NF-21 makes the operator runbook's stated per-line skip-and-log invariant false for
the command the runbook names first -- one mistyped `Observatory.timezone` aborts the entire
import with a bare traceback, no summary and no subsequent lines processed. NF-20 and NF-22
are each the second half of a finding whose first half was fixed one iteration ago, in the
same function.

Output: one new `except` clause plus its regression test, one shared inversion helper called
from both dry-run branches plus its re-mint twin test, one moved `add()` plus a
counted-once test, and one corrected type annotation.

**Paired docs (CLAUDE.md rule), scoped up front, not as a follow-up:** this plan changes
`load_telescope_runs.py`'s behavior (which stderr surface a malformed site produces) and
`allocation_projector.py`'s reported counters. CLAUDE.md pairs `load_telescope_runs.py` with
`docs/notebooks/pre_executed/load_telescope_runs_demo.ipynb`, and the runbook's per-line
skip-and-log bullet documents the invariant NF-21 restores. Those are owned by
**35-11-PLAN.md Task 1** (the notebook) and **35-10-PLAN.md Task 1** (the runbook bullet),
both of which `depends_on` this plan and regenerate once, after these fixes land. The
reconciler demo notebook, which renders the `ReconcileResult` counters NF-22 corrects, is
owned by **35-10-PLAN.md Task 2**. `campaign_reconciler.py`'s NF-23 change is a type
annotation with no behavioral effect, which CLAUDE.md's rule explicitly excludes ("not pure
refactors or typo fixes"); it rides along with the reconciler notebook regeneration anyway.
</objective>

<execution_context>
@/home/tlister/git/fomo_devel/.claude/gsd-core/workflows/execute-plan.md
@/home/tlister/git/fomo_devel/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-allocation-layer-classical-cutover/35-CONTEXT.md
@.planning/phases/35-allocation-layer-classical-cutover/35-REVIEW.md
@.planning/phases/35-allocation-layer-classical-cutover/35-VERIFICATION.md
@.claude/skills/spike-findings-fomo_devel/SKILL.md
@solsys_code/management/commands/load_telescope_runs.py
@solsys_code/allocation_projector.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Give the loader's write/reconcile call its own ZoneInfoNotFoundError handler</name>
  <files>solsys_code/management/commands/load_telescope_runs.py, solsys_code/tests/test_load_telescope_runs.py</files>
  <read_first>
    - `solsys_code/management/commands/load_telescope_runs.py` L1-12 (imports -- note `zoneinfo` is not imported today), L242-247 (the per-line `try` and where `site` is bound by `get_site()`), L258-271 (NF-08's narrowed `KeyError` catch for the status lookup), L316-340 (the `transaction.atomic()` write and the existing `except (ValueError, Observatory.DoesNotExist)` clause at L337), L342-359 (the two summary lines the command must still reach).
    - `solsys_code/allocation_projector.py` L169-205 -- `_night_span_utc()` and the `ZoneInfo(run.site.timezone)` call sites (`:199`, `:575`) the exception originates from.
    - `35-REVIEW.md` L288-348 -- finding NF-21 in full, including the executed probe transcript at L311-316 and the prescribed handler at L333-348.
    - `35-VERIFICATION.md` L234 -- the verifier's confirmation that `ZoneInfoNotFoundError` subclasses `KeyError` and not `ValueError`.
    - `solsys_code/tests/test_load_telescope_runs.py` L40 (`TestLoadTelescopeRuns` and its fixture conventions) -- reuse its existing `Observatory` and schedule-file helpers rather than writing new ones.
  </read_first>
  <behavior>
    - Test: a two-line schedule file whose FIRST line resolves to an `Observatory` with `timezone='America/Santigo'` (the typo the review reproduced) and whose second line resolves to a correctly configured site. Running `load_telescope_runs` raises nothing; stderr carries one line naming the `Observatory` short name, its obscode and the invalid timezone string; the final summary line reports `skipped: 1`; the SECOND line's `CampaignRun` exists; the first line's `CampaignRun` does not (rolled back by the `transaction.atomic()` boundary).
  </behavior>
  <action>
Write the regression test first, in `solsys_code/tests/test_load_telescope_runs.py`, as a new class `TestMalformedTimezoneSkipsOneLine(TestCase)` (or a new method on `TestLoadTelescopeRuns` if that class already owns the two-line-file helper -- prefer reusing its fixtures over duplicating them). Assert all four properties in the behavior block above, and in particular assert the second line WAS processed: a `skipped: 1` count alone does not prove the loop continued. Any `Target` fixture uses `NonSiderealTargetFactory` (CLAUDE.md). Confirm it fails against today's code with an uncaught exception, not an assertion failure.

Then add `from zoneinfo import ZoneInfoNotFoundError` to the module imports and a dedicated `except ZoneInfoNotFoundError as exc:` clause on the per-line `try`, placed AHEAD of the existing `except (ValueError, Observatory.DoesNotExist)` clause at L337 -- ahead, because `ZoneInfoNotFoundError` subclasses `KeyError`, not `ValueError`, so clause order is what decides which handler sees it. The handler writes one stderr line naming `site.short_name`, `site.obscode`, the offending `site.timezone` value, the exception text and the raw line, increments `run_skipped`, and continues. `site` is bound at L244 by `get_site()`, two statements into the same `try`, and `ZoneInfoNotFoundError` can only originate downstream of it inside `write_and_reconcile_campaign_run()` / `reconcile_run()`, so the local is always bound when this clause runs -- say so in the handler's own comment so a future reader does not "fix" it into an `UnboundLocalError`.

Keep NF-08's narrow `KeyError` catch at L265-270 exactly where it is: it covers the `_CLASSICAL_RUN_STATUS[parsed.status]` lookup and nothing else, which is the whole point of that fix. Keep the `transaction.atomic()` boundary at L322: the new handler must report a skip for a line whose partial write has already been rolled back, which is what makes `CampaignRun.objects.count()` agree with the summary.

Restore the sibling-command symmetry the review names: `cutover_classical_allocations` keeps a per-group and per-event catch-all for this same class of failure, so after this fix both ingest commands agree that one bad site skips one line rather than aborting the batch.
  </action>
  <verify>
    <automated>python manage.py test solsys_code.tests.test_load_telescope_runs.TestMalformedTimezoneSkipsOneLine</automated>
    <fails_when>non-zero exit, `FAILED` / `ERROR` in the output, or the line `Ran 0 tests` (the new class was not collected -- rename the label to the method you actually added)</fails_when>
  </verify>
  <verify>
    <automated>grep -c 'except ZoneInfoNotFoundError' solsys_code/management/commands/load_telescope_runs.py</automated>
    <fails_when>the command prints `0`, or exits 1 -- the dedicated clause is absent</fails_when>
  </verify>
  <verify>
    <automated>python manage.py test solsys_code.tests.test_load_telescope_runs</automated>
    <fails_when>non-zero exit, or `FAILED` / `ERROR` in the output</fails_when>
  </verify>
  <acceptance_criteria>
    - A mistyped `Observatory.timezone` produces one stderr line and `skipped: 1`, and the command exits normally having printed both summary lines.
    - The line following the bad one is processed and its `CampaignRun` exists.
    - The bad line's `CampaignRun` does not exist (rolled back).
    - The new `except` clause precedes the `(ValueError, Observatory.DoesNotExist)` clause in source order.
    - `TestClassicalCalendarUnchangedByCutover` and the existing idempotence tests still pass.
  </acceptance_criteria>
  <reversibility rating="reversible">One import and one `except` clause; revertable with no data or schema effect.</reversibility>
  <done>One mistyped site timezone skips its own line and nothing else; the runbook's "one bad row never aborts the whole run" invariant is true again for this command.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Hoist the dry-run inversion guard so both _mint_fields() caller branches share it</name>
  <files>solsys_code/allocation_projector.py, solsys_code/tests/test_allocation_projector.py</files>
  <read_first>
    - `solsys_code/allocation_projector.py` L679-691 (the `_span_needs_remint()` re-mint branch with its unguarded `if dry_run: continue`), L693-720 (the fixed create branch, whose inline `_night_span_utc()` / `_time_of_day_to_datetime()` / `_raise_if_inverted()` sequence at L712-717 is the code to extract), L169-205 (`_night_span_utc()`, `_time_of_day_to_datetime()`), L286-320 (`_raise_if_inverted()`), L321-356 (`_span_needs_remint()` and its null-field convention).
    - `35-REVIEW.md` L226-286 -- finding NF-20 in full, including the executed probe at L250-257 and the prescribed helper at L267-285.
    - `solsys_code/tests/test_allocation_projector.py` L923-1024 (`TestSubNightWindow`) and the existing `test_dry_run_of_a_brand_new_inverted_window_also_raises` -- the re-mint twin belongs beside it and reuses its fixture.
  </read_first>
  <behavior>
    - Test (the re-mint twin): mint one valid night for a La Silla run with `night_start_utc=23:00` / `night_end_utc=05:00` via a real reconcile, then edit the run's pair to `09:00` / `23:00` so `_span_needs_remint()` returns True. `reconcile_run(run, dry_run=True)` must raise `ValueError` with the same inverted-span message the immediately following `reconcile_run(run)` raises -- assert both raise and that the two messages agree, rather than asserting a hand-copied string.
  </behavior>
  <action>
Write the re-mint twin test first, beside `test_dry_run_of_a_brand_new_inverted_window_also_raises` in `solsys_code/tests/test_allocation_projector.py`, reusing that test's site and run fixtures. Confirm it fails today: the dry run returns `created=1, retired=1` and raises nothing while the real run raises.

Then extract the create branch's inline guard at L712-717 into a module-level helper `_raise_if_set_window_inverted(run, night) -> None` placed next to `_raise_if_inverted()`, and call it from BOTH `if dry_run:` short-circuits -- the re-mint branch at L686 and the create branch at L712. The helper returns immediately when either `night_start_utc` or `night_end_utc` is None (a null field's boundary needs `sun_event()`, which D-13 forbids for an existing night, and which `_span_needs_remint()` itself already treats the same way); otherwise it resolves the pair through `_night_span_utc()` and `_time_of_day_to_datetime()` and hands both datetimes to `_raise_if_inverted()`.

Write the helper's docstring to state the null-field convention explicitly and to name the two branches it serves, so a third caller of `_mint_fields()` added later has a single obvious guard to reuse rather than a third inline copy -- that recurrence is exactly what turned NF-10 into NF-20.

Preserve D-13's zero-astropy property: a set sub-night pair resolves with `zoneinfo` alone, so this adds no `sun_event()` call on any path. `TestNoSunEventRecompute` must still pass unchanged.
  </action>
  <verify>
    <automated>test $(grep -c '_raise_if_set_window_inverted' solsys_code/allocation_projector.py) -ge 3</automated>
    <fails_when>exit 1 -- fewer than three occurrences, i.e. the helper is defined but not called from both the re-mint and the create dry-run branches</fails_when>
  </verify>
  <verify>
    <automated>python manage.py test solsys_code.tests.test_allocation_projector</automated>
    <fails_when>non-zero exit, or `FAILED` / `ERROR` in the output -- in particular a regression in `TestNoSunEventRecompute` or `TestSubNightWindow`</fails_when>
  </verify>
  <acceptance_criteria>
    - `_raise_if_set_window_inverted()` exists as a module-level function and is called from both `if dry_run:` short-circuits in `project_allocation()`'s per-night loop.
    - The re-mint twin test passes and asserts that the dry-run and real-run error messages agree.
    - `TestNoSunEventRecompute` passes -- no new `sun_event()` call was introduced.
    - The helper's docstring states the null-field convention.
  </acceptance_criteria>
  <reversibility rating="reversible">A function extraction plus two call sites; no persisted state.</reversibility>
  <done>A dry run over an operator-inverted sub-night window fails the same way the real run does, on the re-mint branch as well as the create branch.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Count a blocked legacy takeover event once, correct the four-tuple annotation, re-green the suite</name>
  <files>solsys_code/allocation_projector.py, solsys_code/campaign_reconciler.py, solsys_code/tests/test_allocation_projector.py</files>
  <read_first>
    - `solsys_code/allocation_projector.py` L610-650 (the retired branch, which claims the legacy url at `:616` BEFORE its ownership check -- the symmetry to copy) and L653-677 (the takeover branch, whose `continue` at `:663` precedes the `legacy_urls_claimed.add()` at `:664`).
    - `solsys_code/campaign_reconciler.py` L614-631 (`_stale_dated_events()`'s `foreign` computation), L676-678 (`_detach_stale_family_events()`'s signature), L755-760 (its `Returns:` docstring, already correct at four elements), L815-820 (its return statement), L880-902 (`reconcile_run()`'s four-value unpack and the `blocked` fold).
    - `35-REVIEW.md` L350-401 (NF-22 in full, including the executed probe at L374-380 and the prescribed one-line move) and L403-424 (NF-23).
    - `solsys_code/tests/test_allocation_projector.py` L387-510 (`TestRetirePathLegacyEventGuard`) -- the takeover twin belongs beside it, and it already imports `reconcile_run`.
  </read_first>
  <behavior>
    - Test: one legacy `RUN:{pk}:{date}` event for a one-night window, attributed via `CalendarEventMeta.run` to a DIFFERENT `CampaignRun`, reached on the takeover path (no `ALLOC:` event exists for the night yet). `reconcile_run(run)` returns `blocked == 1`, not 2, and emits exactly one "Allocation blocked: legacy event" log line -- assert the count with `assertLogs` rather than eyeballing output. The legacy event survives untouched.
  </behavior>
  <action>
Write the counted-once test first, in a new class `TestTakeoverBlockedCountedOnce(AllocationProjectorTestBase)` beside `TestRetirePathLegacyEventGuard` in `solsys_code/tests/test_allocation_projector.py`. Use `assertLogs` on the `solsys_code.allocation_projector` logger to assert exactly one matching record, and assert `result.blocked == 1` on the `ReconcileResult` that `reconcile_run()` returns (not on `project_allocation()`'s raw totals -- the double count only appears after `campaign_reconciler` folds the `foreign` figure in, which is why 181 green tests did not catch it). Any `Target` fixture uses `NonSiderealTargetFactory`. Confirm it fails today with `blocked == 2` and two log records.

Then make the takeover branch claim the legacy url on EVERY decision, exactly as the retired branch already does: move `legacy_urls_claimed.add(legacy_url)` from L664 up to the first statement inside `if existing is None and legacy_event is not None:`, above the `_may_write()` check, so the `not _may_write(...)` path claims it before its `continue` at L663. Comment it with the reason -- the loop is deciding the legacy event's fate here, including deciding to block, and the downstream `foreign` count exists to catch shapes this loop drops silently, so an unclaimed-but-decided url is a double report.

Then fix NF-23: `_detach_stale_family_events()`'s return annotation at `campaign_reconciler.py:678` reads `tuple[int, int, int]` while the function returns four values and its own `Returns:` docstring already says `tuple[int, int, int, int]`. Change the annotation to match. No other change to that function.

Finally run the full Django suite and both pinned lint gates to prove Tasks 1-3 broke nothing outside these modules.
  </action>
  <verify>
    <automated>python manage.py test solsys_code.tests.test_allocation_projector.TestTakeoverBlockedCountedOnce</automated>
    <fails_when>non-zero exit, `FAILED` / `ERROR` in the output, or the line `Ran 0 tests`</fails_when>
  </verify>
  <verify>
    <automated>python -c "src=open('solsys_code/campaign_reconciler.py').read(); i=src.index('def _detach_stale_family_events('); sig=src[i:src.index(':\n', i)]; assert 'tuple[int, int, int, int]' in sig, 'NF-23 open: ' + sig.splitlines()[-1]; print('NF-23 closed')"</automated>
    <fails_when>non-zero exit, or the output containing `NF-23 open:` -- the signature still declares a three-element tuple</fails_when>
  </verify>
  <verify>
    <automated>python manage.py test solsys_code.tests.test_allocation_projector solsys_code.tests.test_campaign_reconciler solsys_code.tests.test_reconcile_campaign_runs</automated>
    <fails_when>non-zero exit, or `FAILED` / `ERROR` in the output</fails_when>
  </verify>
  <verify>
    <automated>LABELS=$(ls solsys_code/tests/test_*.py solsys_code/solsys_code_observatory/tests/test_*.py | grep -v "tests/test_views\.py$" | sed "s|/|.|g; s|\.py$||" | tr "\n" " "); python manage.py test $LABELS</automated>
    <fails_when>non-zero exit, or `FAILED` / `ERROR` in the output</fails_when>
  </verify>
  <verify>
    <automated>pre-commit run ruff --all-files && pre-commit run ruff-format --all-files</automated>
    <fails_when>non-zero exit, or either hook reporting `Failed`</fails_when>
  </verify>
  <acceptance_criteria>
    - One foreign-attributed legacy `RUN:{pk}:{date}` event on the takeover path yields `ReconcileResult.blocked == 1` and exactly one log record.
    - `legacy_urls_claimed.add(legacy_url)` executes before the `_may_write()` check in the takeover branch.
    - `_detach_stale_family_events()`'s annotation, docstring and return statement all agree on four elements.
    - The full Django suite (every `solsys_code` module except `test_views`) is green.
    - Both pinned ruff hooks pass with no findings.
  </acceptance_criteria>
  <reversibility rating="reversible">One statement moved, one annotation corrected; no persisted state or key-form change.</reversibility>
  <done>NF-22 and NF-23 are closed with a test that fails against the pre-fix tree, and the whole suite is green with Tasks 1-3 in place.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator-authored schedule file -> `load_telescope_runs` | A plain-text file the operator hand-writes crosses into a strict parser and a database write path. |
| admin-editable `Observatory.timezone` -> `ZoneInfo()` | An unvalidated `CharField(max_length=64)` with no choices and no validator is passed straight to `zoneinfo`, from inside a batch loop. |
| admin-editable `CampaignRun.night_start_utc` / `night_end_utc` -> the per-night projector | Staff-editable `TimeField`s decide the span of every minted calendar night for that run. |
| foreign-attributed `CalendarEvent` -> `project_allocation()` takeover branch | An event another run owns crosses the projector's ownership check; a mis-count here misreports what the operator is being told was refused. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-23 | Denial of Service | `load_telescope_runs.handle()` per-line loop (`:337`) | high | mitigate | Task 1's dedicated `except ZoneInfoNotFoundError` clause: one malformed site no longer aborts the whole import mid-batch with a bare traceback and no summary. Availability of the ingest path for every other line in the file is restored, and the failure is reported per-line with the stage that actually failed. |
| T-35-24 | Tampering | `CampaignRun.night_start_utc` / `night_end_utc` via the admin (`allocation_projector.py:679-691`) | medium | mitigate | Task 2's `_raise_if_set_window_inverted()` on both dry-run branches: an operator window edit that inverts the span is surfaced by the preview instead of silently previewing `would_create`/`would_retire` and failing only on the real sweep. |
| T-35-25 | Repudiation | `ReconcileResult.blocked` reporting (`allocation_projector.py:655-664`) | low | mitigate | Task 3's symmetric legacy-url claim: the operator-facing count of what was refused matches the number of rows actually refused, so the sweep's own audit line is truthful. |
| T-35-26 | Information Disclosure | the new malformed-timezone stderr line | low | accept | The message discloses only the `Observatory` short name, obscode and the timezone string the operator typed -- all already visible to any staff user in the admin, and the command is reachable only with shell access to the host. No secret, no credential, no user data. |

**Package legitimacy:** this plan installs no npm/pip/cargo package. `zoneinfo` is a Python
standard-library module (3.9+) already imported by `allocation_projector.py:35`; no
`T-35-SC` supply-chain row and no legitimacy checkpoint applies.
</threat_model>

<verification>
- `python manage.py test solsys_code.tests.test_load_telescope_runs solsys_code.tests.test_allocation_projector solsys_code.tests.test_campaign_reconciler solsys_code.tests.test_reconcile_campaign_runs` -- green, including the three new tests.
- Full Django suite green (`test_views` excluded per project memory).
- `pre-commit run ruff --all-files` and `pre-commit run ruff-format --all-files` clean.
- `grep -c 'except ZoneInfoNotFoundError' solsys_code/management/commands/load_telescope_runs.py` is at least 1.
- `_raise_if_set_window_inverted` appears at least three times in `allocation_projector.py` (definition plus two call sites).
</verification>

<success_criteria>
NF-21, NF-20, NF-22 and NF-23 are closed in code, each with a regression test that fails
against the pre-fix tree, and the full suite plus both pinned lint gates are green.
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this phase produces (35-09)

| Symbol / artifact | File | Change |
|---|---|---|
| `from zoneinfo import ZoneInfoNotFoundError` | `solsys_code/management/commands/load_telescope_runs.py` | new import |
| `except ZoneInfoNotFoundError` clause on the per-line `try` | same | new -- placed ahead of `(ValueError, Observatory.DoesNotExist)`; reports the `Observatory`, obscode and offending timezone, increments `run_skipped`, continues |
| `TestMalformedTimezoneSkipsOneLine` | `solsys_code/tests/test_load_telescope_runs.py` | new test class (or method on `TestLoadTelescopeRuns`) |
| `_raise_if_set_window_inverted(run, night)` | `solsys_code/allocation_projector.py` | new module-level helper |
| `project_allocation()` re-mint dry-run branch (`:686`) | same | changed -- now calls the shared inversion guard before its `continue` |
| `project_allocation()` create dry-run branch (`:712-717`) | same | changed -- inline guard replaced by the shared helper call |
| `project_allocation()` takeover branch legacy-url claim | same | moved -- `legacy_urls_claimed.add()` now precedes the `_may_write()` check (NF-22) |
| re-mint twin of `test_dry_run_of_a_brand_new_inverted_window_also_raises` | `solsys_code/tests/test_allocation_projector.py` | new test |
| `TestTakeoverBlockedCountedOnce` | same | new test class |
| `_detach_stale_family_events()` return annotation | `solsys_code/campaign_reconciler.py` | changed -- `tuple[int, int, int]` to `tuple[int, int, int, int]` (NF-23) |
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/35-allocation-layer-classical-cutover/35-09-SUMMARY.md` when done
</output>
