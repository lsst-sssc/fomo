---
phase: 35-allocation-layer-classical-cutover
plan: 24
type: execute
wave: 2
depends_on: ["35-23"]
gap_closure: true
autonomous: true
requirements: [ALLOC-01, ALLOC-02, ALLOC-04, ALLOC-05]
files_modified:
  - solsys_code/models.py
  - solsys_code/migrations/0021_alter_calendareventmeta_is_verified_and_more.py
  - solsys_code/allocation_projector.py
  - solsys_code/tests/test_allocation_projector.py

estimate:
  tokens: 100000
  raw_tokens: 100000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Correcting an `Observatory` row's `lat` / `lon` / `altitude` / `timezone` in place, without changing `run.site`, re-mints every allocation night already projected at that site on the very next sweep. Today it does not: the round-5 verifier reproduced a ~15-hour error reported as `unchanged`, permanently, with no counter and no log line, because the token records `v2|{site_id}|...` -- which row supplied the position, never the position itself (35-VERIFICATION.md \"Human Verification Required\" #1; 35-UAT.md test 4; user decision 2026-09-16: fix in round 6)."
    - "The token's site component records the site's boundary-relevant CONTENTS -- `lat`, `lon`, `altitude` and `timezone`, the four fields `sun_event()` reads through `to_earth_location()` and `_local_noon_utc()` -- carried as a fixed-width fingerprint beside the existing `site_id`. `site_id` is kept, not replaced: it keeps a stored token legible in a log line and preserves the documented behaviour that a site SWAP re-mints."
    - "Existing rows migrate with no data migration. The version marker moves from `v2` to `v3`, so every token already in the database fails the version test, reads as provenance unrecorded, and resolves once through the bounded legacy branch plan 35-19 built -- the same transition CR-02 used one release earlier, and the same one D-15's no-`RunPython` rule requires."
    - "A site correction whose sun events do not actually move does NOT destroy and re-create the night. When the token's version and every other component match and only the position fingerprint differs, the comparison routes to the existing resolution branch -- one `sun_event()` call, compared against the stored boundary at the established one-minute tolerance -- rather than returning `re-mint` outright. A one-metre altitude correction therefore re-records its token and reports `unchanged`; the verifier's ~15-hour correction re-mints."
    - "[edge probe ALLOC-01 `adjacency` -- RESOLVED, verification: explicit] Three adjacency cases are pinned, each by its own test. (a) A site edit that leaves the computed sunset and sunrise unchanged to the second resolves as CORRECT, records a current-format token and reports `unchanged` -- the comparison's tolerance decides, not the mere fact that an input moved. (b) A swap between two `Observatory` rows with byte-identical position and timezone still re-mints, because `site_id` differs and a site swap is documented as re-minting; the redundant re-mint is the accepted cost of keeping `site_id` in the token. (c) A stored boundary exactly at `_UNRECORDED_PROVENANCE_TOLERANCE` still resolves as correct (the comparison is strictly greater-than) -- the existing pair of boundary tests stays green unedited through the version bump."
    - "[edge probe ALLOC-01 `empty` -- RESOLVED, verification: explicit] The three sub-night window cases behave differently under a site correction, and each has a named test rather than a shared assumption. Null/null and half-null reach the token comparison and re-mint (or resolve within tolerance). A fully-set pair never reaches the token at all -- `_span_needs_remint()` returns False two steps earlier -- so its boundaries are correctly left pinned, and what the site correction actually changes for it is the stored dark-window line, which is now refreshed on the update path (35-REVIEW.md iteration 9, WR-05). A malformed current-version token that does not split into the expected number of parts reads as unrecorded, like `NULL` and the empty string already do."
    - "A fully-set sub-night pair moved across timezones is a decided outcome, not a surprise. The review traced the Chile-to-Sydney case producing an inverted span and a `failed` run, with a `_resolve_site()` retry that can never succeed. Whatever that case actually does in the tree is pinned by a named test, the operator's remedy (correct the sub-night window fields together with the site, because a window pinned to one site's night is not portable to another timezone) is stated in `_span_needs_remint()`'s docstring, and the run's night is proven not destroyed either way -- plan 35-20's compute-before-delete ordering already guarantees that."
    - "The dark-window refresh costs nothing on an idempotent sweep and nothing at all in a preview. It fires only when a recorded current-format token proves the site component moved AND both sub-night fields are set; a `--dry-run` preview reaches the same decision by pure token comparison and reports the counter without paying the `sun_event(kind='dark')` call, so WR-02's deferred preview-raises-`ValueError` surface is not widened."
    - "D-13's astropy budget survives the whole change: an unchanged re-reconcile of a night whose token is in the current format makes zero `sun_event()` calls. `TestNoSunEventRecompute` stays green unedited, and that is the constraint that decides whether this fix is the right one."
    - "A declined night's repeated `sun_event()` call is bounded, deliberate and pinned rather than documented as \"once ever\" and silently false. A night whose provenance is unrecorded and whose re-mint is declined resolves once per sweep, forever -- exactly one `sun_event(kind='sun')` call and one warning per sweep, never more -- and no provenance token is recorded for boundaries that were not re-minted (35-REVIEW.md iteration 9, WR-07; recording one would be the false-provenance claim round 2 was reverted for)."
    - "`is_verified` is documented where a reader looks for it. The field is a load-bearing veto on an automated re-mint and is the one companion-row field neither admin surface makes read-only, while `models.py` still describes it as vestigial with no current writer or reader (35-REVIEW.md iteration 9, WR-08). Its class-docstring paragraph, `verbose_name` and a new `help_text` now state the veto AND state that it does not veto a retirement -- consistent with plan 35-23's CR-05 decision."
    - "[flagged assumption -- edge probe ALLOC-02 `unclassified`] ALLOC-02 promises nights are keyed by the site-local observing night, verified for a Chilean and an Australian site. The assumption this round states rather than hides: an in-place `Observatory` timezone correction re-mints a night's BOUNDARIES under the SAME key, and never re-keys it, because the window nights come from `run.window_start + i` and the url carries that date, not a site-derived one. That is believed correct and unchanged from plan 35-21's identical flagged assumption; changing how a night is keyed is out of this round's scope."
    - "[flagged assumption -- edge probe ALLOC-04 `unclassified`] `load_telescope_runs` is untouched by this round. The assumption that a re-import through the loader still reports `unchanged` for nights whose boundaries did not move -- D-13's astropy-drift contract -- is PINNED by `solsys_code/tests/test_load_telescope_runs.py` staying green unedited in this plan's regression gate, not assumed. The loader writes allocations and lets `reconcile_run()` own the boundaries, so a token format change reaches it only through that call."
  artifacts:
    - "solsys_code/models.py -- `minted_sub_night_window` widened to `max_length=128`; `is_verified` given a corrected `verbose_name` and a new `help_text`; the class docstring's two relevant paragraphs corrected"
    - "solsys_code/migrations/0021_*.py -- schema-only `AlterField` pair, no `RunPython`"
    - "solsys_code/allocation_projector.py -- `_site_position_fingerprint()`, a `v3` token carrying it, a component-wise recorded-token comparison, `_site_provenance_differs()`, the update-path dark-window refresh, and the qualified cost bound"
    - "solsys_code/tests/test_allocation_projector.py -- `TestObservatoryCorrectionRemints`, `TestSetWindowSiteCorrection`, `TestDeclinedNightResolutionCostIsBounded`, plus extensions to `TestProvenanceTokenFormat`'s worst-case width and unrecorded-value cases"
  key_links:
    - "`Observatory.lat`/`lon`/`altitude`/`timezone` -> `to_earth_location()` and `_local_noon_utc()` -> `sun_event()` -> the minted `start_time`/`end_time` -> the recorded token. Four arrows exist today and the last one drops the position, so an in-place correction to the first cannot be detected at the last. That is the escalated decision in one line."
    - "a `v2|` token -> the version-prefix test reads it as unrecorded -> the bounded one-time `sun_event()` resolution -> either a re-mint or a freshly recorded `v3` token. This is how every existing row migrates without a `RunPython` data migration (D-15)."
    - "only the fingerprint differs -> the resolution branch rather than an outright re-mint -> a position correction that does not move the sun events re-records its token and reports `unchanged`. Without this arrow a one-metre altitude fix would destroy and re-create every night at that site for no boundary reason -- and each of those re-mints would then meet plan 35-23's decline guards."
    - "a fully-set sub-night pair -> `_span_needs_remint()` returns False before the token is read -> the plain-update path -> `_site_provenance_differs()` -> a refreshed dark-window line. The token cannot govern a run whose boundaries it never decides, which is WR-05's whole content; the refresh is the only site-derived field left on that path."
    - "the widened re-mint funnel (every `v2|`-token night, on the first sweep after this release) -> plan 35-23's CR-04 fall-through and CR-05 retirement guard. This is why 35-24 runs in wave 2: the funnel must not be widened before both guards exist."
  prohibitions:
    - statement: "The migration must NOT rewrite, translate or backfill any stored token, and must contain no `RunPython`. D-15's no-data-migration rule stands; the format transition is a read-time decision."
      status: flagged-unverified
      verification: backstop
    - statement: "A provenance token must NOT be recorded for boundaries that were not proven. On the declined path nothing is recorded at all; on the update path the token is recorded only when both sub-night fields are set and step 1 has just compared both stored boundaries against them."
      status: flagged-unverified
      verification: backstop
    - statement: "`run.telescope_instrument` and `run.campaign` must NOT enter the token. They are not boundary inputs, and including them would make an ordinary title or campaign change destroy and re-create the night."
      status: flagged-unverified
      verification: backstop
    - statement: "The dark-window refresh must NOT add an astropy call to a `--dry-run` preview. WR-02's preview-raises-`ValueError` surface is user-deferred and must not be widened by this plan."
      status: flagged-unverified
      verification: backstop
---

<objective>
Close the round-5 verifier escalation the user decided to fix, plus the three warnings that ride on
the same machinery.

**The escalation (35-VERIFICATION.md "Human Verification Required" #1; 35-UAT.md test 4):** correcting
an `Observatory` row's `lat` / `lon` / `altitude` / `timezone` in the Django admin without changing
`run.site` leaves every allocation night already projected at that site permanently stale. The
verifier reproduced it against a migrated Django test database: the token read `v2|1|none|none`
before and after, the boundaries did not move, and a ~15-hour error was reported as `unchanged` with
no counter and no log line. `ObservatoryAdmin` declares no `readonly_fields`, so an operator
correcting a site definition lands here directly. The token carries the site's IDENTITY; the
boundaries are a function of its CONTENTS.

**WR-05:** the runbook, `_span_needs_remint()`'s docstring and plan 35-21's own success criterion all
say a site correction re-mints. That is false for a run with both sub-night fields set: the function
returns two steps before the token is ever read. For a same-timezone correction the boundaries are
correctly unchanged but the event's stored dark-window line keeps the old site's numbers forever; for
a cross-timezone correction the span inverts and the run fails with a retry that cannot succeed.
Every existing site test and the notebook demo use the null/null fixture, so the shape these fields
exist for is the untested one.

**WR-07:** the docstring promises at most one `sun_event()` call per unrecorded night, *once ever*.
With plan 35-20's guard a stale night carrying staff state is declined instead of re-minted, so it
never receives a token from the re-mint path either -- and re-resolves on every sweep, forever.

**WR-08:** `is_verified` is now a permanent veto on automated correction, and `models.py` still
documents it as a vestigial field with no current writer or reader. It is also the one companion-row
field neither admin surface makes read-only.

Purpose: make the recorded provenance record the thing the boundaries actually depend on, and make
every remaining claim about that machinery true where a reader will find it.

Output: a `v3` token carrying a site-position fingerprint, a component-wise comparison that
distinguishes "an input moved" from "the boundary moved", a dark-window refresh for the one case the
token cannot govern, and three corrected pieces of documentation with tests behind them.

**Scope is the escalated decision, WR-05, WR-07 and WR-08.** CR-04, CR-05, WR-06 and IN-05 are plan
35-23, wave 1. The round's paired docs are plan 35-25, wave 3. WR-01, WR-02, WR-03's residue,
WR-04(a)/(c), IN-01, IN-03 and IN-06 are user-deferred -- the full ledger is
`<review_dispositions>` in `35-23-PLAN.md`, and nothing is dropped. The assumption-delta decision
this plan implements (`promote` the token's site component from identity to contents) is recorded
once, in `35-23-PLAN.md`'s `<assumption_delta_decision>` block.

**The round is not complete at the end of this plan.** Plan 35-25 still owes the operator runbook and
the re-executed demo notebook; until it lands they describe a sweep that no longer exists.

**Run test modules by explicit dotted path, never bare `python manage.py test`.** A bare run collects
`solsys_code/tests/test_views.py`, which imports `ephem_utils` and pays a ~1.6 GB SPICE kernel
download (CLAUDE.md "Heavy import side effect"), and `TestEphemeris` segfaults in native ASSIST. Use
`python manage.py`, never `./manage.py`.
</objective>

<execution_context>
@/home/tlister/git/fomo_devel/.claude/gsd-core/workflows/execute-plan.md
@/home/tlister/git/fomo_devel/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-allocation-layer-classical-cutover/35-CONTEXT.md
@.planning/phases/35-allocation-layer-classical-cutover/35-REVIEW.md
@.planning/phases/35-allocation-layer-classical-cutover/35-VERIFICATION.md
@.planning/phases/35-allocation-layer-classical-cutover/35-21-PLAN.md
@.planning/phases/35-allocation-layer-classical-cutover/35-23-PLAN.md
@solsys_code/allocation_projector.py
@solsys_code/models.py
@solsys_code/telescope_runs.py
</context>

<prohibitions>
1. No `RunPython` in the new migration, and no management command that rewrites stored tokens. D-15's
   no-data-migration rule stands; the format transition is a read-time decision.
2. Do NOT drop `blank=True` from `minted_sub_night_window` and do NOT otherwise act on IN-01. Only
   `max_length` changes on that field.
3. Do NOT touch `solsys_code/admin.py`. Making `is_verified` read-only would remove the only surface
   at which a staff member can express the re-mint veto at all; WR-08's answer here is to name the
   field's second meaning, not to hide it. `minted_sub_night_window` is already read-only on both
   surfaces (plan 35-19).
4. Do NOT modify `_UNRECORDED_PROVENANCE_TOLERANCE`'s value, `_record_sub_night_provenance()`'s
   scoped-writer contract, `_raise_if_set_window_inverted()`, `night_bounds()`, `_raise_if_inverted()`,
   `_night_span_utc()`, `_time_of_day_to_datetime()`, `_mint_fields()`'s computation, or step 1 and
   step 2 of `_span_needs_remint()`. The cross-timezone inverted-span outcome is PINNED by test as a
   decision, not changed.
5. Do NOT touch plan 35-23's `_remint_decline_reason()` rule set, the CR-04 fall-through, the CR-05
   retirement guard, the `remint_declined` counter or any operator message. This plan runs after
   35-23 in the same round and must not undo it. The only permitted edit inside
   `_remint_decline_reason()` is the added WR-08 docstring sentence named in Task 3.
6. Do NOT add, rename or remove a `ReconcileResult` counter, and do NOT change any event `title` text.
   The `description`'s dark-window line is the single field this plan is allowed to refresh.
7. Do NOT touch `docs/`. Plan 35-25 owns the runbook and `reconcile_campaign_runs_demo.ipynb`, and
   the no-hand-edited-notebook-JSON rule (35-SECURITY.md `T-35-17`) travels with them.
8. Do NOT run any management command against `src/fomo_db.sqlite3`. All verification runs against the
   Django test database; the developer database stays untouched.
9. Do NOT weaken, rename, skip or delete any existing test. `TestNoSunEventRecompute`,
   `TestSubNightWindow`, `TestSubNightWindowSiteDirection`, `TestClearedSubNightFieldRemints`,
   `TestUnrecordedProvenanceNight`, `TestSiteChangeRemints`, `TestMintInputInvariant` and plan
   35-23's two new classes must all stay green unedited. `TestProvenanceTokenFormat` is the one
   exception: its worst-case-width case and its unrecorded-value cases are EXTENDED (never
   weakened) for the new component and the new version marker. If a fixture in
   `test_cutover_classical_allocations.py` genuinely breaks because the format change correctly
   exposed it as stale, that is a Rule 1 deviation to document in the SUMMARY -- scope the repair to
   the single affected test, exactly as plans 35-19 and 35-21 did.
10. Any `Target` fixture must use `tom_targets.tests.factories.NonSiderealTargetFactory`, never
    `SiderealTargetFactory` (CLAUDE.md). `AllocationProjectorTestBase` already does the right thing.
11. Planning-doc, docstring and comment prose: plain English, per CLAUDE.md's Conventions section.
    Write "create or update" or "find-or-create" rather than the database-jargon verb CLAUDE.md
    names, and "model" / "data model" / "case" / "companion-row state" rather than the abstraction
    noun it names. Follow that rule as written there; do not reproduce either forbidden term here.
</prohibitions>

<tasks>

<task type="tracer">
  <name>Task 1: Carry the site's position in the token, and prove an in-place site correction re-mints end to end</name>
  <files>solsys_code/models.py, solsys_code/migrations/0021_alter_calendareventmeta_is_verified_and_more.py, solsys_code/allocation_projector.py, solsys_code/tests/test_allocation_projector.py</files>
  <read_first>
    - `.planning/phases/35-allocation-layer-classical-cutover/35-VERIFICATION.md` -- the `### Human Verification Required` section in full: the probe transcript (`PROBE token= v2|1|none|none`, the unmoved boundaries, the true corrected sunset/sunrise, `PROBE same pk? True`), and why the verifier classified it a decision rather than a gap.
    - `solsys_code/telescope_runs.py:258-300` -- `sun_event()`: the `site.timezone` guard, `site.to_earth_location()` and `_local_noon_utc(date, site.timezone)`. These lines ARE the list of site fields the fingerprint must cover.
    - `solsys_code/solsys_code_observatory/models.py:38-80` -- `Observatory`'s `lat`, `lon`, `altitude` and `timezone` field declarations, including which are nullable, and `to_earth_location()` around `:167`.
    - `solsys_code/allocation_projector.py:380-446` -- `_PROVENANCE_TOKEN_VERSION`, `_sub_night_provenance_token()` and its docstring's four-input argument.
    - `solsys_code/allocation_projector.py:467-595` -- `_span_needs_remint()` in full: step 1's set-field comparisons, step 2's both-set short-circuit, step 3's version-prefix test at `:566-567`, and step 4's unrecorded-provenance branch with its tolerance comparisons and its `_record_sub_night_provenance()` call.
    - `solsys_code/models.py:12-132` -- `CalendarEventMeta`'s class docstring (the `WR-06` paragraph about `is_verified`, and the `minted_sub_night_window` paragraph), the `is_verified` field with its `WR-06` comment, and the `minted_sub_night_window` field block.
    - `solsys_code/migrations/0020_alter_calendareventmeta_minted_sub_night_window.py` -- what the new migration mirrors (schema-only, no `RunPython`).
    - `solsys_code/tests/test_allocation_projector.py:37-143` -- `AllocationProjectorTestBase`: the five `Observatory` fixtures with their real coordinates, `_make_run()`'s defaults (note `night_start_utc`/`night_end_utc` default to null), and `_link_record()`.
    - `solsys_code/tests/test_allocation_projector.py:1978-2026` -- `TestSiteChangeRemints`, whose assertion style the new class follows and which must stay green unedited.
    - `.planning/phases/35-allocation-layer-classical-cutover/35-23-PLAN.md` -- the `<assumption_delta_decision>` block, which is the written record of why the site component is promoted from identity to contents and what would force a later column rename.
  </read_first>
  <action>
In `solsys_code/models.py`:

Change `CalendarEventMeta.minted_sub_night_window`'s `max_length` from 64 to 128 and nothing else on
that field (prohibition 2). The new component pushes the worst-case token past 64; Task 2's extended
width test proves the new bound rather than asserting it here.

Correct the class docstring's `minted_sub_night_window` paragraph and the field's own comment so both
state that the recorded site component is the site's boundary-relevant POSITION -- `lat`, `lon`,
`altitude`, `timezone` -- carried as a fingerprint beside its primary key, not the primary key alone.
Keep the existing sentences that a `NULL` value, the empty string and a pre-release token all mean NOT
RECORDED, and keep the sentence that only the allocation projector writes it. Name the current
version marker in the paragraph so a reader can recognise a stored value.

For WR-08, rewrite the class docstring's `WR-06` paragraph about `is_verified`. It currently states
that no writer sets `False`, that the value is not reachable by re-running any sweep or receiver, and
that the two `calendar.html` branches keyed on it are unreachable. Replace the "not reachable"
conclusion with what is now true: since plan 35-20 the field is a load-bearing veto read by
`allocation_projector._remint_decline_reason()`, so a `False` value permanently prevents an automated
re-mint of that night's boundaries and is reported under `remint_declined`; it does NOT prevent the
night being retired when a linked observation places a block on it (plan 35-23's CR-05 decision).
State the consequence the review names: a historical `False` row from before Phase 34 acquires that
veto, and `is_verified` is the one companion-row field neither admin surface lists in
`readonly_fields`. Keep the sentence about the `calendar.html` branches, which is unchanged.

Set `is_verified`'s `verbose_name` to
`'Whether the telescope label was live-verified against the LCO API (unchecking also vetoes an automated re-mint)'`
and add a `help_text` reading
`'Setting this False permanently prevents the allocation projector from correcting the boundaries of this night: an automated re-mint is declined and reported under remint_declined. It does not prevent the night being retired when a linked observation places a block on it. See the runbook section on remint_declined.'`
Replace the existing `WR-06` comment above the field, which says the `verbose_name` is deliberately
left alone because changing it would need a migration -- that migration is now being written anyway.

Generate the migration with `python manage.py makemigrations solsys_code`. Expect a single file whose
name begins `0021_`; keep whatever name `makemigrations` generates and record it in the SUMMARY if it
differs from this plan's `files_modified` entry. Two `AlterField` operations, no `RunPython`
(prohibition 1).

In `solsys_code/allocation_projector.py`:

Bump `_PROVENANCE_TOKEN_VERSION` from `v2` to `v3` and extend its comment: the bump is what makes
every token already stored read as unrecorded, so each night resolves once through the bounded legacy
branch and re-records in the current format -- the same read-time transition CR-02 used, with no data
migration, and the reason no stored value is ever rewritten.

Add `_site_position_fingerprint(run)` next to `_sub_night_provenance_token()`. It returns the literal
`'none'` when `run.site_id` is None; otherwise the first 16 characters of the lowercase hexadecimal
SHA-256 digest of a canonical string built from `run.site`'s `lat`, `lon`, `altitude` and `timezone`,
in that order, joined by a single comma so the pre-image separator differs from the token's own
separator and nobody mistakes one for the other. Render each float with `repr()`, which in Python 3
gives the shortest representation that round-trips, so the fingerprint is stable across sessions and
across a save/reload cycle; render a null coordinate as `repr(None)` rather than special-casing it.
Docstring must state: (a) which four fields are covered and why exactly those four -- they are what
`sun_event()` reads, through `to_earth_location()` for the position and `_local_noon_utc()` plus the
blank-timezone guard for the timezone; (b) that this reads `run.site`, which `project_allocation()`
has already loaded at the top of the sweep when it builds the site timezone, so the projector's own
path issues no additional query while a caller that has not loaded the relation pays one -- correcting
the "no database access" claim its sibling function makes for itself rather than repeating it
falsely; (c) that a truncated digest is used because the column must stay bounded and a collision
here costs at most a missed re-mint on a position change, which the next genuine change still catches.

Rewrite `_sub_night_provenance_token(run)` to return the version marker, `run.site_id` (rendered
`'none'` when null, unchanged), the fingerprint, and the two sub-night sides, joined by the existing
single `|` separator, in that order. Update its docstring: the boundary inputs are now
`(night_start_utc, night_end_utc, the site's position and timezone, night)`; `site_id` is retained
alongside the fingerprint for legibility and to preserve the documented behaviour that a site swap
re-mints; the night is still deliberately absent for the reason already stated; and
`telescope_instrument`/`campaign` are still deliberately absent for the reason already stated
(prohibition 3). Update the `Returns:` examples to the new five-part form.

Replace `_span_needs_remint()`'s step-3 body with a component-wise comparison. The recorded token is
still trusted only when it starts with the current version marker followed by the separator; add that
it must also split into exactly the number of parts the current format has -- a current-version token
with the wrong part count reads as unrecorded, like `None` and the empty string, and falls through to
step 4. When the token is trusted, compare component by component against the token built for the
run's current fields:

  - the two sub-night sides differ, or the `site_id` differs -> return True immediately. This is
    today's behaviour for both of those inputs and the documented meaning of a site swap.
  - only the position fingerprint differs -> do NOT return True. Fall through to step 4's existing
    resolution branch, which makes one `sun_event()` call, compares the stored boundary against it at
    the established tolerance, and either re-mints or re-records a current-format token. This is what
    keeps a one-metre altitude correction from destroying and re-creating every night at that site.
  - everything matches -> return False, astropy-free, exactly as today.

Update step 3's and step 4's docstring paragraphs accordingly, naming the escalated decision as the
defect being closed and stating plainly that the fingerprint routes to resolution rather than to an
outright re-mint because an input moving is not the same fact as a boundary moving.

Write the test first and watch it fail. Add `TestObservatoryCorrectionRemints` to
`solsys_code/tests/test_allocation_projector.py`, extending `AllocationProjectorTestBase`, placed
immediately after `TestSiteChangeRemints`. It reproduces the verifier's probe exactly: project a
null/null run's nights at the Chilean site, record the minted boundaries and the event's primary key,
then edit that SAME `Observatory` row's `lat`, `lon`, `altitude` and `timezone` in place to the
Australian site's values and save it -- never reassigning `run.site` -- and reconcile again. Assert
`retired == 1` and `created == 1` per night with `unchanged == 0`, a NEW primary key, and both
boundaries equal to the live `sun_event()` values for the corrected position and that night, resolved
with the same expression `night_bounds()` uses. Quote the probe transcript in the test docstring as
the provenance of the fixture, and assert against live `sun_event()` values, never the verifier's
hardcoded timestamps.
  </action>
  <verify>
    <automated>cd /home/tlister/git/fomo_devel && python manage.py test solsys_code.tests.test_allocation_projector.TestObservatoryCorrectionRemints -v 2</automated>
    <fails_when>non-zero exit, or "FAILED" / "ERROR" in the last summary line -- in particular the event's primary key being unchanged after the in-place site edit, or `unchanged=1` instead of `retired=1 / created=1`, which is the verifier's silent-stale result reproduced rather than fixed.</fails_when>
    <automated>cd /home/tlister/git/fomo_devel && python manage.py makemigrations solsys_code --check --dry-run</automated>
    <fails_when>non-zero exit, or output naming a model change with no migration -- the `max_length`, `verbose_name` or `help_text` edits landed in models.py without the 0021 migration being generated.</fails_when>
    <!-- planner-discipline-allow: RunPython -- the literal names a Django migration operation and must be sayable in the prohibition that forbids it. The check below targets the generated migration file, which carries no prose, never this plan; the executor keeps whatever makemigrations produces and adds no comment to it. -->
    <automated>cd /home/tlister/git/fomo_devel && grep -L RunPython solsys_code/migrations/0021_*.py</automated>
    <fails_when>prints nothing, or exits non-zero -- `grep -L` lists files that do NOT contain the pattern, so the new migration's path must be printed. No output means the file contains `RunPython`, breaching D-15 and prohibition 1; a "No such file" error means migration 0021 was never created.</fails_when>
    <automated>cd /home/tlister/git/fomo_devel && grep -c "_PROVENANCE_TOKEN_VERSION = 'v3'" solsys_code/allocation_projector.py && grep -c 'def _site_position_fingerprint' solsys_code/allocation_projector.py</automated>
    <fails_when>either grep prints `0` or exits non-zero (grep -c exits 1 when the count is zero) -- the version marker was never bumped past `v2`, or the fingerprint helper was inlined instead of being a named function the tests and a future reader can reach.</fails_when>
  </verify>
  <acceptance_criteria>
    - `solsys_code/models.py` declares `minted_sub_night_window` with `max_length=128`, still `null=True, blank=True`, and its class-docstring paragraph and field comment both describe the site component as the site's position and timezone rather than its primary key alone.
    - `is_verified` carries the exact `verbose_name` and `help_text` strings named in the action, and the class docstring's paragraph about it states the re-mint veto, the retirement non-veto, the historical-`False` consequence and that the field is not admin-readonly.
    - A single new migration whose name begins `0021_` exists, contains two `AlterField` operations and contains no `RunPython`.
    - `_PROVENANCE_TOKEN_VERSION` is `'v3'` and its comment explains the bump as the read-time transition that avoids a data migration.
    - `_site_position_fingerprint(run)` exists, returns `'none'` for a null site, and otherwise a 16-character lowercase hexadecimal digest over `lat`, `lon`, `altitude` and `timezone`; its docstring names those four fields, explains the `run.site` access honestly and states the truncation trade-off.
    - `_sub_night_provenance_token(run)` returns a five-part token in the order version, `site_id`, fingerprint, start, end, and its docstring's `Returns:` examples show that form.
    - `_span_needs_remint()`'s step 3 compares component by component: a sub-night or `site_id` difference returns True; a fingerprint-only difference falls through to step 4; a current-version token with the wrong part count reads as unrecorded.
    - The verifier's probe re-mints: an in-place edit of the `Observatory` row's `lat`/`lon`/`altitude`/`timezone`, with `run.site` untouched, produces `retired == 1 / created == 1`, a new primary key, and boundaries equal to the live `sun_event()` values for the corrected position.
    - `python manage.py makemigrations solsys_code --check --dry-run` reports no drift.
  </acceptance_criteria>
  <done>An operator correcting a site definition in the admin sees every night already projected at that site corrected on the next sweep -- the edit, through the comparison that used to mask it, to calendar boundaries that equal the corrected position's real sun events -- instead of a ~15-hour error reported as `unchanged` forever.</done>
  <reversibility rating="costly">Three coupled decisions. (a) The `max_length` widening is a schema `AlterField` on a production table; reverting it would truncate stored tokens, though tokens are derived state the unrecorded branch re-establishes, so nothing unrecoverable is lost. (b) The version bump makes every stored token read as unrecorded, so the first sweep after this release re-resolves each night once against a real sun event and re-mints any whose stored boundary sits more than a minute out. Deliberate and bounded, not one-way: no token is rewritten by the migration (prohibition 1), the re-minted events are derived, re-creatable state that 35-SECURITY.md's T-35-06 already dispositions as such, and plan 35-23's CR-04 fall-through and CR-05 retirement guard -- both landed in wave 1, before this plan -- protect every night carrying a human stamp from that funnel. (c) The `is_verified` `verbose_name`/`help_text` change is a label edit carried by the same migration and is reverted by its inverse. Rated `costly` rather than `one-way` for exactly those reasons, and flagged rather than gated so this round stays autonomous.</reversibility>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Refresh the dark-window line for the one case the token cannot govern, and pin the cross-timezone outcome</name>
  <files>solsys_code/allocation_projector.py, solsys_code/tests/test_allocation_projector.py</files>
  <behavior>
    - A same-timezone site correction on a run with BOTH sub-night fields set leaves the boundaries pinned (correctly) and refreshes the event description's dark-window line to the corrected position's numbers.
    - That refresh fires only when a recorded current-format token proves the site component moved AND both sub-night fields are set; on every other sweep it costs zero `sun_event()` calls.
    - A `--dry-run` preview of the same case reports the same counter as the real run without making any `sun_event()` call.
    - A site correction whose dark window happens to be identical to the second makes the preview report `updated` while the real run reports `unchanged` -- a known one-count divergence, pinned rather than hidden.
    - After the refresh, the night's recorded token is the current one, so the next sweep decides it astropy-free.
    - A null/null or half-null run never takes the refresh path: its site change is already decided by the token comparison, and no token is recorded from the update path for it.
    - A fully-set sub-night pair moved to a site in a different timezone produces a pinned, named outcome, and the night's existing event is not destroyed by the attempt.
  </behavior>
  <read_first>
    - `.planning/phases/35-allocation-layer-classical-cutover/35-REVIEW.md` -- the `### WR-05` section in full: the quoted short-circuit, the hand-traced Chile-to-Sydney case, the three options offered, and the list of claims that currently disagree with the code (`docs/runbooks/telescope_runs_calendar.rst:1084-1090`, `35-21-PLAN.md:24`, and the two site tests).
    - `solsys_code/allocation_projector.py:546-558` -- step 1's two comparisons and step 2's both-set short-circuit. Step 2 is the line that makes the token unreachable for a fully-set run, and it is NOT moved (prohibition 4).
    - `solsys_code/allocation_projector.py:652-678` -- `_mint_fields()`: the `sun_event(run.site, night, kind='dark')` call, the two `isoformat()` conversions with `replace(microsecond=0)`, and the `_DARK_WINDOW_PREFIX` line construction. The refresh must build the line by exactly this recipe so a re-minted night and a refreshed night cannot disagree in format.
    - `solsys_code/allocation_projector.py:65` -- `_DARK_WINDOW_PREFIX`; and `:172-190` -- `preserved_dark_window_line()`, the function the refresh replaces on this one transition.
    - `solsys_code/allocation_projector.py:1050-1100` -- the create branch's `dry_run` short-circuit, the shared `if dry_run:` block that only the update path now reaches, the update path's three-field dict, and the comment at `:1092-1095` stating why provenance is recorded on the create path only. That comment gains this task's single stated exception.
    - `solsys_code/tests/test_allocation_projector.py:965-1160` -- `TestSubNightWindow` and `TestSubNightWindowSiteDirection`: how a set/set and a half-null run are fixtured, and how a cross-hemisphere expectation is asserted against live `sun_event()` values.
    - Your own Task 1 `_sub_night_provenance_token()` and the component-wise step-3 comparison.
  </read_first>
  <action>
In `solsys_code/allocation_projector.py`, add `_site_provenance_differs(run, existing)` beside
`_span_needs_remint()`. It is a pure read -- no astropy, no writes -- and returns True only when the
event's recorded token passes the current-version and part-count test AND either its `site_id`
component or its position-fingerprint component differs from the run's current values. A token that
fails the version or part-count test returns False. State that last rule in the docstring as a
deliberate limitation with its reason: an unrecorded token is no evidence that the stored dark-window
line is stale, and refreshing on it would pay one `sun_event(kind='dark')` call per legacy night on
every sweep forever -- the same permanent cost WR-07 documents one branch over. Name the operator's
escape for such a night: clear one sub-night field to route it through the re-mint path, which
recomputes the whole description.

In `project_allocation()`'s plain-update path -- the `else:` branch that builds the three-field dict
for an existing event -- decide the dark-window line as follows, and comment the decision in place:

  - Compute a local flag that is True only when `run.night_start_utc` and `run.night_end_utc` are
    BOTH set and `_site_provenance_differs(run, existing)` is True. The both-set condition is not
    decoration: it is what makes the refresh sound. For a fully-set run, step 1 of
    `_span_needs_remint()` has already compared both stored boundaries against what the current site
    and the current sub-night fields produce and found them equal on this same sweep, so refreshing
    the description and recording the current token are both claims this sweep just proved. For any
    other sub-night case the token comparison has already decided the night, so the refresh is
    unreachable and must stay so.
  - When that flag is set and `dry_run` is False, call `sun_event(run.site, night, kind='dark')` and
    build the line by `_mint_fields()`'s exact recipe. Otherwise keep `preserved_dark_window_line(existing)`.
  - In the shared `if dry_run:` block that this path reaches, when the flag is set count `updated`
    directly and continue, instead of calling `preview_calendar_event_action()`. Comment why: the
    preview already KNOWS the description will change, because the site component of the recorded
    token differs from the run's current site, and it reports that without paying the astropy call
    the real run pays -- which keeps WR-02's deferred preview-raises-`ValueError` surface exactly as
    wide as it was (prohibition 7 of plan 35-23's ledger; WR-02 stays deferred). State the accepted
    one-count divergence in the same comment: when the corrected position happens to produce an
    identical dark window, the preview reports `updated` where the real run reports `unchanged`. That
    divergence is pinned by its own test rather than left for a reader to discover.
  - After the update path's write -- after `update_calendar_event_key_and_fields()` and
    `_link_event_to_run()` -- record the current provenance token when the flag is set, so the next
    sweep decides this night astropy-free and the refresh does not repeat. Amend the existing comment
    that says provenance is recorded on the create path only: name this as its single exception and
    state the proof that makes it truthful (both boundaries were compared against the current inputs
    by step 1 on this same sweep).

Update `_span_needs_remint()`'s step-2 docstring paragraph to say what is actually true, which is
what WR-05 reports the docs currently get wrong: a fully-set sub-night pair pins both boundaries, so
the token -- and therefore the site -- is never consulted for such a run; a site correction changes
only its dark-window line, which the update path refreshes; and a correction to a site in a DIFFERENT
timezone is caught one step earlier, by step 1, because the resolved boundaries move. Add the
operator's remedy for that last case in the same paragraph: a sub-night window pinned to one site's
night is not portable to another timezone, so the window fields must be corrected together with the
site.

Write the tests first and watch them fail. Add `TestSetWindowSiteCorrection` to
`solsys_code/tests/test_allocation_projector.py`, extending `AllocationProjectorTestBase`, placed
immediately after Task 1's class. Cases:

  1. Same-timezone correction, set/set run: fixture a run with both sub-night fields set at the
     Chilean site, project it, then edit that `Observatory` row's `lat`/`lon`/`altitude` in place
     while keeping its timezone, and reconcile. Assert the boundaries and the primary key are
     unchanged, the stored description's dark-window line now equals the line built from a live
     `sun_event(..., kind='dark')` for the corrected position, and the recorded token is the current
     one. `retired == 0`, `created == 0`.
  2. Idempotence and the astropy budget: a further reconcile with nothing changed reports
     `unchanged` and makes zero `sun_event()` calls, asserted with a `patch` on
     `solsys_code.allocation_projector.sun_event` the way `TestNoSunEventRecompute` already does.
  3. Dry-run parity and cost: the same correction previewed reports the same counter as the real run
     and makes zero `sun_event()` calls.
  4. `test_preview_may_over_report_updated_by_one_on_a_site_correction` -- build the divergence
     deliberately by pointing the run at a second `Observatory` row with byte-identical position and
     timezone, so the site component differs but the dark window does not. Assert the preview reports
     `updated` while the real run reports `unchanged`, and say in the docstring that this is a
     deliberate accepted divergence, bounded to one count on exactly this transition.
  5. A null/null run and a half-null run never take the refresh path: after a site correction each is
     decided by the token comparison (re-mint or resolution), and neither has a token written from
     the update path. Assert on the counters and on the branch taken, not on internals.
  6. `test_a_set_window_moved_across_timezones_has_a_pinned_outcome` -- fixture a set/set run at the
     Chilean site with an overnight window, project it, reassign `run.site` to the Australian site,
     save, and reconcile. RUN THIS CASE AND RECORD WHAT ACTUALLY HAPPENS, then pin that: if
     `night_bounds()`'s inversion guard raises, assert the raise with `assertRaises` and assert the
     night's existing event still exists afterwards (plan 35-20's compute-before-delete ordering);
     if instead a valid non-inverted span results, assert the resulting boundaries against live
     `sun_event()` values. Either way the docstring states the outcome as a decision and names the
     operator's remedy. If the observed outcome differs from the review's hand-trace, that is a
     Rule 1 deviation to record in the SUMMARY, not a reason to change the code (prohibition 4).
  </action>
  <verify>
    <automated>cd /home/tlister/git/fomo_devel && python manage.py test solsys_code.tests.test_allocation_projector.TestSetWindowSiteCorrection -v 2</automated>
    <fails_when>non-zero exit, or "FAILED" / "ERROR" in the last summary line -- in particular the stored dark-window line still carrying the pre-correction numbers (the refresh never fired), or a non-zero `sun_event` call count in the idempotence or dry-run cases (the refresh fires on every sweep instead of once, or a preview pays astropy).</fails_when>
    <automated>cd /home/tlister/git/fomo_devel && python manage.py test solsys_code.tests.test_allocation_projector.TestNoSunEventRecompute solsys_code.tests.test_allocation_projector.TestSubNightWindow solsys_code.tests.test_allocation_projector.TestSubNightWindowSiteDirection solsys_code.tests.test_allocation_projector.TestClearedSubNightFieldRemints solsys_code.tests.test_allocation_projector.TestUnrecordedProvenanceNight -v 2</automated>
    <fails_when>non-zero exit, or "FAILED" / "ERROR" in the last summary line -- `assert_not_called()` raising in `TestNoSunEventRecompute` means D-13's astropy budget broke, which neither the version bump nor the refresh is allowed to do once a night's token is in the current format.</fails_when>
    <automated>cd /home/tlister/git/fomo_devel && grep -c 'def _site_provenance_differs' solsys_code/allocation_projector.py</automated>
    <fails_when>prints `0`, or exits non-zero (grep -c exits 1 when the count is zero) -- the refresh decision was inlined into the loop body instead of being a named, separately testable pure read.</fails_when>
  </verify>
  <acceptance_criteria>
    - `_site_provenance_differs(run, existing)` exists, performs no astropy work and no writes, returns False for any token that fails the version or part-count test, and its docstring states that limitation with its reason and the operator's escape.
    - A same-timezone in-place site correction on a set/set run leaves both boundaries and the primary key unchanged and rewrites the description's dark-window line to the corrected position's live `sun_event(kind='dark')` values.
    - The refreshed night carries a current-format token afterwards, and the next reconcile of it makes zero `sun_event()` calls.
    - A `--dry-run` preview of the correction makes zero `sun_event()` calls and reports the same counter as the real run, except in the identical-dark-window case, where the one-count `updated`-versus-`unchanged` divergence is pinned by its own named test.
    - Null/null and half-null runs never reach the refresh path and get no token written from the update path.
    - The cross-timezone set/set case has a named test asserting the outcome actually observed in the tree, and the night's event survives the attempt.
    - `_span_needs_remint()`'s step-2 docstring states that a fully-set pair never consults the token, that the site correction changes only the dark-window line, that a cross-timezone correction is caught by step 1, and what the operator must do about it.
    - `TestNoSunEventRecompute`, `TestSubNightWindow`, `TestSubNightWindowSiteDirection`, `TestClearedSubNightFieldRemints` and `TestUnrecordedProvenanceNight` all pass unedited.
  </acceptance_criteria>
  <done>The one claim WR-05 showed to be false -- that a site correction re-mints every one of a run's nights -- is now either true or accurately qualified everywhere it is made, with the untested fully-set fixture covered and the cross-timezone outcome written down as a decision.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Make the stated cost bound true, widen the format contract's tests, and finish WR-08's documentation</name>
  <files>solsys_code/allocation_projector.py, solsys_code/tests/test_allocation_projector.py</files>
  <behavior>
    - A night whose provenance is unrecorded and whose re-mint is declined makes exactly one `sun_event(kind='sun')` call per sweep -- not zero, not two -- and emits both its warnings each sweep.
    - No provenance token is written for that night: the sweep records nothing it did not prove.
    - A `v2|`-format token reads as unrecorded and resolves once through the legacy branch, then carries a `v3` token.
    - A current-version token with the wrong number of parts reads as unrecorded too.
    - The worst-case token, now carrying the fingerprint, fits within the column's own declared `max_length`.
    - Changing any recorded mint input still re-mints: the sub-night pair, the site's identity, and now the site's position.
  </behavior>
  <read_first>
    - `.planning/phases/35-allocation-layer-classical-cutover/35-REVIEW.md` -- the `### WR-07` section in full (the four things every sweep does for such a night, and its explicit warning that recording a token for boundaries that were not re-minted would recreate round 2's false-provenance mistake), the `### WR-08` section, and the `### WR-01` and `### IN-06` sections so the boundary between what this task fixes and what stays deferred is drawn from the review's own words.
    - `solsys_code/allocation_projector.py:528-535` -- the `Cost bound:` sentence in `_span_needs_remint()`'s step-4 docstring paragraph. This is the sentence WR-01 and WR-07 both indict; this task qualifies the WR-07 half only.
    - `solsys_code/allocation_projector.py:569-595` -- the legacy branch: the single `sun_event()` call, the two tolerance comparisons, the warning, the `return True` on the stale path that records nothing, and the `dry_run`-gated recording on the within-tolerance path.
    - `solsys_code/allocation_projector.py:598-649` -- `_remint_decline_reason()`, especially rule 2's existing sentence about `is_verified` being the production-reachable half.
    - `.planning/phases/35-allocation-layer-classical-cutover/35-23-PLAN.md` -- Task 3's decision that only `confirmed_by` declines a retirement, which the WR-08 docstring sentence must agree with, and the `<review_dispositions>` ledger that keeps WR-01 and IN-06 deferred.
    - `solsys_code/tests/test_allocation_projector.py:2026-2260` -- `TestProvenanceTokenFormat` and `TestMintInputInvariant` in full: the worst-case-width case, the unrecorded-value cases, and the `inspect.getsource()` tripwire that must now also mention the fingerprint.
    - `solsys_code/tests/test_allocation_projector.py:1683-1910` -- `TestRemintHumanConfirmationGuard`, whose confirmed-night fixture the bounded-cost test reuses.
  </read_first>
  <action>
In `solsys_code/allocation_projector.py`, qualify the `Cost bound:` sentence in
`_span_needs_remint()`'s step-4 docstring paragraph. It currently promises at most one
`sun_event(kind='sun')` call per unrecorded night, once ever. Rewrite it to name both escapes
explicitly and to say which one this plan closes:

  - A night whose re-mint is DECLINED (plan 35-20's guard, reported under `remint_declined` after
    plan 35-23's split) never receives a token from the re-mint path, and this branch deliberately
    records nothing on the stale path -- so such a night resolves once per sweep, indefinitely,
    paying exactly one `sun_event(kind='sun')` call and emitting both its warnings each time. State
    that this is accepted rather than fixed, and state the reason it is acceptable in one line: the
    repeated warning is the standing report that a night a person confirmed disagrees with its run,
    and the operator's remedy is in the runbook's `remint_declined` section. State the alternative
    that was rejected and why: recording a token for boundaries that were not re-minted would claim a
    fact the sweep never proved -- the false-provenance mistake round 2 was reverted for.
  - Under `--dry-run` the resolution repeats on every invocation because a preview may not record
    what it proves. Name it as the separate, still-open finding it is (WR-01, user-deferred) and do
    NOT claim it fixed or restate its branch comment.

Add one sentence to `_remint_decline_reason()`'s rule 2 docstring paragraph for WR-08: `is_verified`
is the production-reachable arm of this rule -- the one companion-row field neither admin surface
lists in `readonly_fields` -- and setting it False permanently vetoes automated correction of this
night's boundaries while NOT vetoing its retirement, which is plan 35-23's separate decision for the
retirement branch. Cross-reference the model docstring as the place the field's full meaning is now
recorded. Change nothing else in that function (prohibition 5).

Write the tests first and watch them fail.

Add `TestDeclinedNightResolutionCostIsBounded` to `solsys_code/tests/test_allocation_projector.py`,
immediately after Task 2's class. Fixture a night with unrecorded provenance whose stored boundary is
stale beyond the tolerance and whose companion row carries a confirmation, so its re-mint is
declined. With `solsys_code.allocation_projector.sun_event` patched to pass through to the real
implementation while counting calls, run two consecutive real sweeps and assert: exactly one call per
sweep, both warnings emitted each sweep, `remint_declined == 1` each sweep, and the companion row's
`minted_sub_night_window` still unrecorded after both -- no token was written for boundaries that were
not re-minted. The class docstring states that the repetition is a decision, names the operator's
remedy, and states the bound as "one call per sweep, never more" rather than "once ever".

Extend `TestProvenanceTokenFormat` -- extend, never weaken (prohibition 9):

  1. Add a case for a `v2|`-format token: it reads as unrecorded, resolves through one real
     `sun_event()` call, and a night whose boundaries are correct ends with a `v3` token recorded, so
     the next reconcile makes zero calls. The existing `NULL`, empty-string and pre-35-19 cases stay
     exactly as they are.
  2. Add a case for a current-version token with the wrong number of parts: it also reads as
     unrecorded and takes the same branch.
  3. Extend the worst-case-width case so the constructed token includes the fingerprint component,
     still asserting against
     `CalendarEventMeta._meta.get_field('minted_sub_night_window').max_length` read off the model
     field rather than a hardcoded number. Keep its docstring's note about SQLite not enforcing
     `max_length` and the PostgreSQL deployment being the production target.
  4. Leave the two tolerance-boundary cases untouched -- they pin the comparison operator and must
     stay green through the version bump, which is itself evidence the bump changed nothing in step 4.

Extend `TestMintInputInvariant` with one further case: changing the site's POSITION in place (the
escalated decision's shape) re-mints, alongside the existing sub-night-pair and site-identity cases.
Extend its class docstring's list of recorded mint inputs to include the site's position and
timezone, and extend the `inspect.getsource()` tripwire so it also requires the token function to
mention the fingerprint helper. Keep the docstring's honest statement that the test cannot detect an
input nobody wrote a case for.
  </action>
  <verify>
    <automated>cd /home/tlister/git/fomo_devel && python manage.py test solsys_code.tests.test_allocation_projector.TestDeclinedNightResolutionCostIsBounded solsys_code.tests.test_allocation_projector.TestProvenanceTokenFormat solsys_code.tests.test_allocation_projector.TestMintInputInvariant -v 2</automated>
    <fails_when>non-zero exit, or "FAILED" / "ERROR" in the last summary line -- in particular a call count other than one per sweep on the declined night, a non-null `minted_sub_night_window` on it (a token was recorded for boundaries that were not re-minted), a `v2|` token being trusted rather than resolved, or the worst-case token exceeding the field's declared `max_length`.</fails_when>
    <automated>cd /home/tlister/git/fomo_devel && python manage.py test solsys_code.tests.test_allocation_projector solsys_code.tests.test_allocation_projector_signals solsys_code.tests.test_campaign_reconciler solsys_code.tests.test_reconcile_campaign_runs solsys_code.tests.test_cutover_classical_allocations solsys_code.tests.test_load_telescope_runs solsys_code.tests.test_campaign_views -v 1</automated>
    <fails_when>non-zero exit, or "FAILED" / "ERROR" in the last summary line, or a reported total lower than the count plan 35-23 recorded in its SUMMARY -- anything lower means a test was deleted rather than added to. `test_load_telescope_runs` staying green unedited is what pins the ALLOC-04 flagged assumption. Every module is named by dotted path; a bare `python manage.py test` collects `test_views.TestEphemeris`, which segfaults in native ASSIST and pays the SPICE download.</fails_when>
    <automated>cd /home/tlister/git/fomo_devel && pre-commit run ruff --all-files && pre-commit run ruff-format --all-files</automated>
    <fails_when>either hook prints "Failed" -- D-07's lint gate. Run it through pre-commit, which pins ruff 0.2.1; an unpinned `ruff` on PATH can report findings the enforced gate does not have.</fails_when>
    <automated>cd /home/tlister/git/fomo_devel && python manage.py makemigrations --check --dry-run</automated>
    <fails_when>non-zero exit, or output naming a model change with no migration -- migration 0021 does not cover both field alterations, or another model was touched.</fails_when>
    <automated>cd /home/tlister/git/fomo_devel && git status --short -- docs/ solsys_code/admin.py solsys_code/campaign_reconciler.py solsys_code/campaign_views.py solsys_code/management/ src/fomo_db.sqlite3</automated>
    <fails_when>the command prints any line at all -- every path listed is forbidden to this plan by prohibitions 3, 5, 6, 7 and 8, so a clean run prints nothing.</fails_when>
  </verify>
  <acceptance_criteria>
    - The `Cost bound:` sentence names both escapes, states the declined-night bound as one `sun_event(kind='sun')` call per sweep rather than once ever, gives the reason the repetition is accepted and the alternative that was rejected, and names WR-01's dry-run half as separately open without claiming it fixed.
    - `_remint_decline_reason()`'s rule 2 paragraph states `is_verified`'s veto, that it does not veto a retirement, and where the field's full meaning is recorded.
    - `TestDeclinedNightResolutionCostIsBounded` proves exactly one `sun_event()` call per sweep across two consecutive sweeps, both warnings each sweep, and `minted_sub_night_window` still unrecorded afterwards.
    - `TestProvenanceTokenFormat` gains a `v2|`-token case and a wrong-part-count case, has its worst-case-width case extended to include the fingerprint, and keeps both tolerance-boundary cases unedited and green.
    - `TestMintInputInvariant` gains an in-place site-position case, lists the site's position and timezone among the recorded mint inputs, and its source tripwire requires the fingerprint helper.
    - The seven-module regression run is green with a total no lower than plan 35-23's recorded count; both ruff hooks pass; `makemigrations --check --dry-run` reports no drift.
    - `git status --short` over `docs/`, `solsys_code/admin.py`, `solsys_code/campaign_reconciler.py`, `solsys_code/campaign_views.py`, `solsys_code/management/` and `src/fomo_db.sqlite3` prints nothing.
  </acceptance_criteria>
  <done>Every remaining claim this machinery makes about itself is true where a reader finds it: the cost bound names its escapes, the format contract's boundaries are named tests rather than conventions, and `is_verified`'s second meaning is written on the field an operator actually sees.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Django admin -> `Observatory.lat` / `lon` / `altitude` / `timezone` | `ObservatoryAdmin` declares no `readonly_fields`, so a staff member correcting a site definition rewrites, in one save, the inputs every already-projected night at that site was minted from. This is the escalated decision's reachability path. |
| Stored provenance token -> the re-mint decision | `_span_needs_remint()` parses a database column and decides from it whether a calendar night is still correct. A value writable by anything other than the projector would let that writer control whether a night is ever re-checked. |
| Upgrade event -> the first sweep after this release | A version bump changes what every existing row means, all at once. The blast radius of that re-interpretation is a deploy-time property, not a per-request one. |
| `CalendarEventMeta.is_verified` (staff-writable) -> automated correction | A checkbox labelled about LCO telescope-label verification carries a permanent veto on boundary correction. The gap between the label and the power is itself the finding. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-24-01 | Tampering (data integrity) | `_sub_night_provenance_token()` / `_span_needs_remint()` step 3 | high | mitigate | The escalated decision: the token records which row supplied the position, never the position, so an in-place `Observatory` correction is invisible forever and the calendar shows a ~15-hour-wrong span reported as `unchanged` -- reproduced by the round-5 verifier. Task 1 carries a position fingerprint in the token and bumps the version so existing rows re-resolve once; Task 3 extends the mint-input invariant test so a future added input has a named test adjacent to it. |
| T-35-24-02 | Tampering | the token field as a parse input (`CalendarEventMeta.minted_sub_night_window`) | low | mitigate | The column is read-only on BOTH staff surfaces (plan 35-19) and no `ModelForm` binds it, so no operator-writable path reaches it (prohibition 3 keeps it that way). Abuse analysis of the new component-wise parse: the worst a hostile value could force is a version or part-count mismatch, which routes the night into the bounded one-time resolution -- one extra astropy call, then a comparison against a real sun event. It cannot force a WRONG boundary to be trusted (the branch computes rather than infers), and it cannot force destruction of a confirmed night (plan 35-23's CR-04 and CR-05 guards, landed in wave 1, sit on both delete paths). Residual is bounded, self-extinguishing computation. |
| T-35-24-03 | Denial of Service | the first sweep after this release | medium | accept | Every night carrying a `v2|` token pays one `sun_event(kind='sun')` call, once, in real mode -- the bound plan 35-19 established, applied once more to the whole existing set. Accepted because it is bounded per night and self-extinguishing (the branch records what it proves), and because the runbook's post-upgrade deploy note tells the operator to preview with `--dry-run` first; plan 35-25 extends that note to name this second audit. Stated honestly: the bound is a real-mode bound. Under `--dry-run` the resolution repeats per invocation -- that is WR-01, user-deferred, and this plan does not claim it fixed. |
| T-35-24-04 | Denial of Service | a declined night's per-sweep resolution | medium | accept | WR-07: a night whose provenance is unrecorded and whose re-mint is declined resolves on every sweep forever. Accepted and BOUNDED rather than silently carried: Task 3 pins the cost at exactly one `sun_event(kind='sun')` call and one pair of warnings per sweep, qualifies the docstring that claimed "once ever", and rejects the alternative (recording a token for boundaries that were not re-minted) as the false-provenance claim round 2 was reverted for. The repeated warning is retained deliberately as the standing report that a confirmed night disagrees with its run. |
| T-35-24-05 | Tampering | a token silently truncated by the database | medium | mitigate | SQLite does not enforce `max_length`, so an over-long token would round-trip in development and truncate on the PostgreSQL deployment CLAUDE.md names as the production target -- and a truncated token compares unequal to the freshly built one, re-minting the night on every sweep forever. Task 1 widens the column to 128; Task 3 extends the worst-case-token test to include the fingerprint, still asserting against the field's own declared `max_length` so the assertion and the schema cannot drift. |
| T-35-24-06 | Elevation of Privilege | `CalendarEventMeta.is_verified` as an undocumented veto | medium | mitigate | WR-08: a staff user can permanently freeze a night against automated correction from a checkbox whose label says nothing about that power, and a historical `False` row acquires the veto retroactively. Task 1 states the second meaning in the class docstring, the `verbose_name` and a new `help_text`; Task 3 states it in `_remint_decline_reason()`; plan 35-25 states it in the runbook. Making the field read-only was considered and rejected (prohibition 3): it would remove the only surface at which the veto can be expressed at all, turning an under-documented control into an unreachable one. |
| T-35-24-07 | Information Disclosure | the position fingerprint in a stored token | low | accept | The token is read-only on both admin surfaces and visible only to staff who can already read `Observatory` rows in full, and a 16-character truncated digest over four published observatory parameters discloses nothing those staff cannot read directly. Accepted; no mitigation is warranted, and no secret or credential enters the digest's pre-image. |

ASVS level 1 scope note (block on `high`): the one `high` threat is dispositioned `mitigate` with
named implementation tasks, so nothing blocks. This plan installs no package -- `pyproject.toml` is
untouched and there is no `pip`/`npm`/`cargo` install task -- so `T-35-SC`'s package-legitimacy gate
carries forward from 35-SECURITY.md unchanged and no legitimacy checkpoint applies. `hashlib` is a
Python standard-library module, not a dependency, so it adds no supply-chain surface. The migration is
schema-only with no `RunPython` (prohibition 1), so no data is rewritten and there is no
migration-time integrity surface. The findings are data-integrity, availability, authorisation-label
and documentation-accuracy defects on a staff-reachable automated path, not authentication, session or
injection-class threats, so ASVS L1's controls in those chapters have no new applicable surface.
</threat_model>

<verification>
- The verifier's probe is a committed test: editing an `Observatory` row's `lat`/`lon`/`altitude`/
  `timezone` in place, with `run.site` untouched, re-mints every night already projected at that site
  to the corrected position's real `sun_event()` values, with a new primary key and
  `retired == 1 / created == 1` -- not `unchanged`.
- A position correction that does not move the sun events beyond the one-minute tolerance does NOT
  re-mint: it resolves once, re-records a current-format token and reports `unchanged`.
- A swap between two identically-positioned `Observatory` rows still re-mints on `site_id`, pinned as
  the accepted cost of keeping `site_id` in the token.
- A `v2|` token, a `NULL`, an empty string and a current-version token with the wrong part count all
  read as unrecorded and take the same bounded branch; a night whose boundaries are correct ends with
  a `v3` token and makes zero `sun_event()` calls on the next reconcile.
- The tolerance boundary stays pinned on both sides by the existing unedited tests.
- The worst-case token, including the fingerprint, fits within the column's own declared
  `max_length`.
- A same-timezone site correction on a fully-set run refreshes the dark-window line, records a
  current token, and costs zero astropy on the next sweep and zero in a preview; the one-count
  preview divergence on an identical dark window has its own named test.
- The cross-timezone fully-set case has a named test asserting the outcome observed in the tree, with
  the night's event proven to survive the attempt.
- A declined night with unrecorded provenance makes exactly one `sun_event()` call per sweep across
  two consecutive sweeps, emits both warnings each time, and has no token written for it.
- `TestNoSunEventRecompute` passes unedited -- D-13's astropy budget survives the version bump, the
  fingerprint and the refresh.
- `python manage.py test` over the seven named modules is green with a total no lower than plan
  35-23's recorded count; both ruff hooks pass (D-07); `python manage.py makemigrations --check
  --dry-run` reports no drift; the 0021 migration contains no `RunPython`.
- `git status --short` over `docs/`, `solsys_code/admin.py`, `solsys_code/campaign_reconciler.py`,
  `solsys_code/campaign_views.py`, `solsys_code/management/` and `src/fomo_db.sqlite3` prints
  nothing.
- **Stated residuals, not gaps:** (a) WR-01's dry-run repetition is untouched and explicitly not
  claimed fixed; (b) WR-02's preview-raises-`ValueError` surface is unchanged and deliberately not
  widened; (c) a 16-character truncated digest could in principle collide, costing a missed re-mint
  on one position change, which the next genuine change still catches -- stated in the helper's
  docstring rather than hidden; (d) the operator runbook and the committed demo notebook still
  describe the pre-`v3` behaviour until plan 35-25 lands.
- **Edge-probe accounting for this plan (no silent drops):** 4 of the round's 7 probe items are
  authored here -- ALLOC-01 `adjacency` and ALLOC-01 `empty` as resolved-explicit truths, and
  ALLOC-02 and ALLOC-04 `unclassified` as flagged assumptions. Of the other 3, two are authored in
  plan 35-23 (ALLOC-01 `ordering`, ALLOC-03 `unclassified`) and one in plan 35-25 (ALLOC-05
  `unclassified`). 4 + 2 + 1 = 7 applicable, 0 dropped.
</verification>

<success_criteria>
- The escalated decision is closed: an in-place `Observatory` position or timezone correction is
  detected and re-minted on the next sweep, instead of reading as "nothing changed" forever.
- The recorded token records what the boundaries actually depend on, and the promotion from site
  identity to site contents is a written decision with its own future-rename trigger
  (`35-23-PLAN.md`'s `<assumption_delta_decision>`).
- The comparison distinguishes "an input moved" from "the boundary moved", so a trivial position fix
  costs one resolution rather than a destructive re-mint of every night at that site.
- WR-05 is closed: the fully-set sub-night case is covered by tests, its dark-window line is
  refreshed, and every claim about it agrees with the code.
- WR-07 is closed as a bounded, stated acceptance with a test behind the bound, and no false
  provenance is recorded.
- WR-08 is closed on the model and in the projector; the runbook half is plan 35-25's.
- Existing rows migrate with no data migration: a `v2|` token reads as unrecorded and resolves once
  through the branch plan 35-19 already built (D-15).
- ROADMAP SC-1 and SC-2 hold for a corrected site definition as they already do for a corrected site
  assignment, and ALLOC-02's site-local night promise holds at the corrected position.
- ALLOC-04 is untouched in code and pinned green by the regression gate; ALLOC-05's cutover
  sequencing is unchanged in code and its post-upgrade consequence is written into the runbook by
  plan 35-25.
- The paired-docs obligation for the round is NOT discharged here and is not claimed to be: it
  belongs to plan 35-25, wave 3.
- Nothing outside this plan's four `files_modified` paths changed, and plan 35-23's guards, counter
  and messages are intact.
</success_criteria>

<artifacts>
## Artifacts this phase produces

| Artifact | Kind | Produced by |
|----------|------|-------------|
| `solsys_code/models.py` -- `minted_sub_night_window` widened to `max_length=128`; class-docstring paragraph and field comment corrected to the site's position | modified source | Task 1 |
| `solsys_code/models.py` -- `is_verified`'s new `verbose_name`, new `help_text` and rewritten class-docstring paragraph (WR-08) | modified source | Task 1 |
| `solsys_code/migrations/0021_*.py` | new migration (two schema-only `AlterField` operations, no `RunPython`) | Task 1 |
| `solsys_code/allocation_projector.py` -- `_PROVENANCE_TOKEN_VERSION` bumped to `v3` with an extended comment | modified constant | Task 1 |
| `solsys_code/allocation_projector.py` -- `_site_position_fingerprint()` | new function | Task 1 |
| `solsys_code/allocation_projector.py` -- `_sub_night_provenance_token()` carrying the fingerprint, with a rewritten docstring | modified function | Task 1 |
| `solsys_code/allocation_projector.py` -- `_span_needs_remint()`'s component-wise step-3 comparison and its rewritten step-2/3/4 docstring paragraphs | modified function | Tasks 1, 2, 3 |
| `solsys_code/allocation_projector.py` -- `_site_provenance_differs()` | new function | Task 2 |
| `solsys_code/allocation_projector.py` -- the update path's dark-window refresh, its dry-run counter rule and its single provenance-recording exception | modified function (`project_allocation`) | Task 2 |
| `solsys_code/allocation_projector.py` -- `_remint_decline_reason()`'s WR-08 docstring sentence | modified docstring | Task 3 |
| `solsys_code/tests/test_allocation_projector.py` -- `TestObservatoryCorrectionRemints` | new test class | Task 1 |
| `solsys_code/tests/test_allocation_projector.py` -- `TestSetWindowSiteCorrection` | new test class | Task 2 |
| `solsys_code/tests/test_allocation_projector.py` -- `TestDeclinedNightResolutionCostIsBounded` | new test class | Task 3 |
| `solsys_code/tests/test_allocation_projector.py` -- `TestProvenanceTokenFormat` extended (`v2|` token case, wrong-part-count case, fingerprint in the worst-case width) and `TestMintInputInvariant` extended (in-place position case) | modified test classes | Task 3 |
| `.planning/phases/35-allocation-layer-classical-cutover/35-24-SUMMARY.md` | planning artifact | Task 3 |

**Paired-docs coverage for the whole round.** CLAUDE.md maps `solsys_code/allocation_projector.py`
and `solsys_code/management/commands/reconcile_campaign_runs.py` ->
`docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb`, and the runbook clause is
directory-scoped to any page under `docs/runbooks/` whose documented behaviour a change affects. Both
this plan's changes and plan 35-23's affect `docs/runbooks/telescope_runs_calendar.rst` and that
notebook, so plan **35-25** (wave 3) covers both in one pass against the final tree -- which is why
neither this plan nor 35-23 carries `docs/` in `files_modified`, and why the round is not complete
until 35-25 executes. `solsys_code/models.py` has no paired notebook of its own in the map.
</artifacts>

<output>
Create `.planning/phases/35-allocation-layer-classical-cutover/35-24-SUMMARY.md` when done.

Record in it: the RED failure output for `TestObservatoryCorrectionRemints` (the `unchanged` result
and the unmoved boundaries the verifier also saw); the generated migration's actual filename if it
differs from this plan's `files_modified` entry; the worst-case token string and its measured length
against the field's `max_length`; the exact observed outcome of the cross-timezone set/set case and
whether it matched the review's hand-trace; the measured `sun_event()` call counts for the
declined-night bounded-cost test; the seven-module test count; both ruff results; the
`makemigrations --check` result; and explicitly that `TestNoSunEventRecompute` and
`TestSubNightWindow` were left unedited, since D-13's astropy budget surviving a provenance format
change is the constraint that decides whether this fix is the right one.

Record the exact `is_verified` `verbose_name` and `help_text` strings as shipped, since plan 35-25's
runbook section must agree with them word for word.

Record any Rule 1 deviation in full -- in particular, if a fixture in
`test_cutover_classical_allocations.py` broke because the format change correctly exposed it as
stale, name the test, the reason and the scope of the repair, exactly as plans 35-19 and 35-21 did
for the same file.

State in the SUMMARY that the round is NOT complete at the end of this plan: plan 35-25 (wave 3)
still owes the operator runbook and the re-executed `reconcile_campaign_runs_demo.ipynb` for this
plan's behaviour changes and plan 35-23's.
</output>