---
phase: 29-the-reconciler
plan: 05
type: execute
wave: 4
depends_on: ["29-03", "29-04"]
files_modified:
  - docs/runbooks/telescope_runs_calendar.rst
  - docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb
  - docs/notebooks.rst
  - CLAUDE.md
autonomous: true
requirements: [RECON-01, RECON-06, RECON-08, RECON-09]

must_haves:
  truths:
    - "The operator runbook documents reconcile_campaign_runs, its --dry-run and its skip reasons"
    - "The runbook documents that approve/resolve_site/mark_cancelled/mark_weather_failure each reconcile their run immediately"
    - "No runbook prose references backfill_range_calendar_events as a still-available command"
    - "The runbook's 'Why doesn't the calendar pop-up show a Campaign run block?' section describes automatic ownership for reconciler-owned events instead of admin-only hand-linking"
    - "A pre-executed demo notebook shows a real reconcile sweep, its dry-run, and the queue-vs-classical key split"
  artifacts:
    - path: "docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb"
      provides: "the paired demo notebook for the new command (CLAUDE.md paired-docs rule)"
    - path: "docs/runbooks/telescope_runs_calendar.rst"
      provides: "reconciler operator documentation; backfill section removed"
  key_links:
    - from: "docs/notebooks.rst"
      to: "docs/notebooks/pre_executed/reconcile_campaign_runs_demo"
      via: "Sphinx toctree entry"
      pattern: "reconcile_campaign_runs_demo"
    - from: "CLAUDE.md"
      to: "docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb"
      via: "the paired-docs notebook map gains the new module"
      pattern: "reconcile_campaign_runs_demo.ipynb"
---

<objective>
Ship the paired docs CLAUDE.md and ROADMAP.md require up front for this phase: rewrite the operator
runbook for the reconciler (removing everything `backfill_range_calendar_events` left behind, per
RECON-09's documentation half) and add the pre-executed `reconcile_campaign_runs_demo.ipynb`
notebook, wired into the Sphinx toctree and CLAUDE.md's pairing map.

Purpose: RECON-09 is only half done when the command file is deleted — the requirement says
"retired, from code **and the runbook**", and the runbook currently teaches operators a command that
no longer exists, plus a "hand-link it in the admin" workaround this phase makes unnecessary for
reconciler-owned events. CLAUDE.md's paired-docs rule (breach history: Phase 5, Phase 6, quick task
260726-kdp) makes both artifacts part of the deliverable, not follow-ups.

Output: an updated `docs/runbooks/telescope_runs_calendar.rst`, a new executed notebook, a toctree
entry and a CLAUDE.md map entry.
</objective>

<execution_context>
@/home/tlister/git/fomo_devel/.claude/gsd-core/workflows/execute-plan.md
@/home/tlister/git/fomo_devel/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-the-reconciler/29-CONTEXT.md
@.planning/phases/29-the-reconciler/29-RESEARCH.md
@.planning/phases/29-the-reconciler/29-03-SUMMARY.md
@.planning/phases/29-the-reconciler/29-04-SUMMARY.md
@CLAUDE.md
</context>

## Artifacts this phase produces

See `29-01-PLAN.md`'s "Artifacts this phase produces" section for the phase-wide list of new
symbols. This plan lands the phase's two paired-doc artifacts:

- `docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb` (new)
- a new "How do I get every campaign run onto the calendar?" section in
  `docs/runbooks/telescope_runs_calendar.rst`, plus a `reconcile_campaign_runs` row in that page's
  command cheat-sheet
- a `docs/notebooks.rst` toctree entry and a CLAUDE.md notebook-map entry for the above

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the operator runbook for the reconciler and finish retiring the backfill command</name>
  <files>docs/runbooks/telescope_runs_calendar.rst</files>
  <read_first>
    - docs/runbooks/telescope_runs_calendar.rst (whole file — in particular the cross-reference near
      line 111 in the `backfill_lco_observation_records` section, lines ~474-495 for the section
      being deleted, ~496-535 for the "Campaign run block" section being corrected, ~537-570 for the
      cheat-sheet table, ~570-600 for Troubleshooting, and ~615-630 for the per-record
      skip-and-log bullets)
    - solsys_code/management/commands/reconcile_campaign_runs.py (the actual flag, summary line and
      stderr wording to document — quote what the command really prints, not an approximation)
    - solsys_code/campaign_reconciler.py (`_skip_reason`'s four reason strings and the two key
      families, so the runbook's operator-facing explanation matches the code)
    - .planning/phases/29-the-reconciler/29-RESEARCH.md ("Code Examples" #1 and "State of the Art" —
      the exhaustive list of runbook locations to touch, including the one RECON-09 does not name)
  </read_first>
  <action>
Edit `docs/runbooks/telescope_runs_calendar.rst`.

DELETE: the "How do I backfill calendar events for older approved range-window runs?" section in
full; its `backfill_range_calendar_events` row from the command cheat-sheet table; its bullet in the
"Per-line / per-record skip-and-log behaviour" list; and its mention in the "Observatory missing
timezone" troubleshooting entry — replace that command name with `reconcile_campaign_runs`, since
the failure mode still exists and a different command reports it now. Also fix the cross-reference
in the `backfill_lco_observation_records` section reading "in the same spirit as
``backfill_range_calendar_events`` above" so it points at the reconciler instead.

ADD a new task-oriented section, "How do I get every campaign run onto the calendar?", placed where
the deleted backfill section was, covering:
- What `reconcile_campaign_runs` does: one idempotent sweep over every `CampaignRun`, projecting and
  refreshing its calendar events. Safe to re-run — a second sweep against unchanged state writes
  nothing.
- The dry-run-first workflow, with a `.. code-block:: console` pair showing the dry run then the real
  run, matching the page's existing convention for every other command.
- What the summary line means, quoting the real counter names for both modes.
- The skip reasons an operator will actually see on stderr, each with what to do about it:
  `not approved` (approve or reject the run first), `missing telescope/instrument`, `TBD window`
  (the run has no concrete dates yet — nothing to project), `unresolved site` (resolve it from Sites
  Needing Review, or set a telescope class), plus the `reconcile failed (...)` line for a run whose
  site has no timezone (cross-reference the Troubleshooting entry).
- What an operator sees on the calendar afterwards, in plain terms: a classically-scheduled run shows
  one entry per observing night spanning that site's sunset-to-sunrise; a queue-scheduled or
  class-wide run shows a single entry spanning its whole window, sitting alongside the individual
  observation entries the LCO/Gemini sync commands already create for it.
- That the same reconciliation now happens automatically for a single run the moment staff approve
  it, resolve its site, or mark it cancelled or weather-failed from the approval queue — no command
  run is needed for those.

REWRITE the "Why doesn't the calendar pop-up show a 'Campaign run' block?" section. Its current claim
— that nothing in FOMO fills the ownership link in automatically and hand-linking through the admin
is the only way — is superseded: the reconciler now sets that link on every event it creates or
adopts. Keep the section (the question is still asked) but re-answer it: reconciler-owned events get
the block automatically; the manual admin path remains valid only for events the reconciler never
touches (un-attributed sync-command events, hand-created entries); and an event with no companion
record, or with a blank owning-run value, still means "not owned by any campaign run", which is the
normal state for conferences, proposal deadlines and un-attributed nights. Preserve the two existing
notes about the frozen calendar-event field and about clearing the owning-run value.

ADD a `reconcile_campaign_runs` row to the command cheat-sheet table with `--dry-run (optional)` in
the flags column.

Keep the page's existing tone, heading-underline conventions and synthetic-placeholder policy (no
real contact information, emails or telescope strings anywhere).
  </action>
  <verify>
    <automated>python -m sphinx -b html docs docs/_build/html -q 2>&1 | tail -20 && grep -c 'backfill_range_calendar_events' docs/runbooks/telescope_runs_calendar.rst || true</automated>
  </verify>
  <acceptance_criteria>
    - The Sphinx build completes without errors for `telescope_runs_calendar.rst`.
    - `grep -c 'backfill_range_calendar_events' docs/runbooks/telescope_runs_calendar.rst` is 0.
    - `grep -c 'reconcile_campaign_runs' docs/runbooks/telescope_runs_calendar.rst` is at least 4 (section body, both console examples, cheat-sheet row).
    - The "Campaign run block" section no longer contains the phrase "nothing in FOMO fills that link in automatically".
    - The four skip-reason strings documented on the page match `_skip_reason()`'s strings exactly.
  </acceptance_criteria>
  <done>An operator reading the runbook end-to-end finds the reconciler and no trace of the retired backfill command.</done>
</task>

<task type="auto">
  <name>Task 2: Create and execute the paired reconcile_campaign_runs demo notebook</name>
  <files>docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb, docs/notebooks.rst, CLAUDE.md</files>
  <read_first>
    - docs/notebooks/pre_executed/load_telescope_runs_demo.ipynb (the closest sibling: Django-setup
      boilerplate cell, idempotent seeding, markdown-then-code rhythm, and the `parents[2]`
      repo-root note)
    - docs/notebooks/pre_executed/import_campaign_csv_demo.ipynb (the closest `CampaignRun`-centric
      sibling — how it seeds campaign rows and calls a management command from a notebook)
    - docs/notebooks.rst (the toctree block the new entry joins)
    - solsys_code/management/commands/reconcile_campaign_runs.py and
      solsys_code/campaign_reconciler.py (what the notebook demonstrates)
    - CLAUDE.md ("Paired docs are part of the deliverable" — the notebook pairing map that gains an
      entry, per ROADMAP.md Phase 29's "CLAUDE.md's notebook pairing map gains the new module")
  </read_first>
  <action>
Create `docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb`, following the sibling
notebooks' structure exactly: a title markdown cell naming the module pair it demonstrates
(`solsys_code/campaign_reconciler.py` and
`solsys_code/management/commands/reconcile_campaign_runs.py`) with a bulleted list of what it shows,
then the standard Django-setup markdown+code pair, then alternating markdown/code cells.

The notebook seeds its own data and must be safe to re-run against any dev DB (idempotent
`update_or_create`-style seeding, mirroring the siblings). Demonstrate, in this order:

1. Seeding two `Observatory` rows (one ground site with a real IANA timezone, one satellite site) and
   a campaign `TargetList`.
2. Seeding three `CampaignRun` rows exercising the three branches: a classically-scheduled
   multi-night run (non-queue `source`, resolved ground site), a queue-scheduled run with `source`
   set explicitly to `CampaignRun.Source.LCO_QUEUE`, and a class-wide run with `telescope_class` set
   and no site. Add a markdown cell stating plainly why `source` is set explicitly here and what it
   means for real data: the reconciler branches purely on `CampaignRun.source`, and the real
   pre-v2.2 rows in the dev database still carry `legacy`, so staff must set `source` on genuine
   queue runs through the Django admin before a real sweep renders the queue-versus-classical split
   correctly. Cross-reference the runbook section added in Task 1.
3. A dry-run invocation through `call_command` with the output captured and printed, showing the
   `would_create` counters and that `CalendarEvent.objects.count()` is still 0.
4. The real invocation, then a printed loop over the resulting events showing url, title, start and
   end — making the two key families visible: date-bearing `RUN:{pk}:{date}` rows for the classical
   run, one bare `RUN:{pk}` row each for the queue and class-wide runs. Plain print/loop only; no new
   dependency.
5. A second real invocation showing `created: 0, updated: 0` — the idempotency claim demonstrated
   rather than asserted.
6. A short closing markdown cell noting that approve / resolve_site / mark_cancelled /
   mark_weather_failure each reconcile their own run immediately, so the command is for sweeps and
   backfills rather than routine use.

Execute the notebook and commit it WITH output, the way every other file under
`docs/notebooks/pre_executed/` is committed, using
`jupyter nbconvert --to notebook --execute --inplace` on the new file, run from
`docs/notebooks/pre_executed/` as the working directory (the `parents[2]` repo-root resolution in the
setup cell depends on it — the siblings carry the same note).

Add the toctree entry to `docs/notebooks.rst` alongside the other five pre-executed notebooks, titled
"Reconciling Campaign Runs onto the Calendar".

Add the new pair to CLAUDE.md's notebook pairing map, per ROADMAP.md Phase 29's paired-docs line:
`solsys_code/campaign_reconciler.py` and
`solsys_code/management/commands/reconcile_campaign_runs.py` map to
`reconcile_campaign_runs_demo.ipynb`. Change nothing else in CLAUDE.md.
  </action>
  <verify>
    <automated>python -c "import json; nb=json.load(open('docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb')); code=[c for c in nb['cells'] if c['cell_type']=='code']; assert code; assert sum(1 for c in code if c.get('outputs')) >= 4; print('code cells', len(code), 'with output', sum(1 for c in code if c.get('outputs')))"</automated>
  </verify>
  <acceptance_criteria>
    - The notebook exists with at least 4 executed code cells carrying committed output.
    - `grep -c 'reconcile_campaign_runs_demo' docs/notebooks.rst` is 1.
    - `grep -c 'reconcile_campaign_runs_demo.ipynb' CLAUDE.md` is at least 1.
    - The notebook's own printed output shows a date-bearing `RUN:` key for the classical run and a bare `RUN:` key for the queue and class-wide runs.
    - A second sweep's captured output in the notebook shows `created: 0` and `updated: 0`.
    - `python -m sphinx -b html docs docs/_build/html -q` completes without errors for the notebooks page.
  </acceptance_criteria>
  <done>The paired demo notebook exists, is executed with committed output, and is discoverable from both the Sphinx toctree and CLAUDE.md's pairing map.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| published docs -> operator action | Runbook prose is what a staff member follows against the live dev database; stale prose causes wrong real-world actions |
| notebook execution -> dev database | The demo notebook writes real rows into whichever database the executing kernel is pointed at |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-16 | Repudiation | Runbook still describing a deleted command, or the superseded admin-only ownership workaround | mitigate | RECON-09's documentation half: the acceptance greps assert zero `backfill_range_calendar_events` occurrences and the rewritten "Campaign run block" section; 29-VALIDATION.md additionally requires a human end-to-end read-through, carried out in plan 29-06 |
| T-29-17 | Information Disclosure | Contact PII or real telescope/proposal strings leaking into published docs or committed notebook output | mitigate | The runbook's existing synthetic-placeholder policy is preserved; the notebook seeds its own fixture rows rather than printing real campaign rows, and `CampaignRun` contact fields are never printed |
| T-29-18 | Tampering | Notebook execution writing fixture rows into a real database | accept | Sibling pre-executed notebooks already seed the dev database idempotently; the rows are clearly-labelled demo fixtures and the dev database is gitignored and disposable |
| T-29-SC | Tampering | npm/pip/cargo installs | accept | No packages installed in this phase; no legitimacy audit applies |
</threat_model>

<verification>
- `python -m sphinx -b html docs docs/_build/html -q` builds clean.
- `grep -rn 'backfill_range_calendar_events' docs/` returns nothing.
- The notebook carries committed output for its executed cells.
- `ruff check .` and `ruff format --check .` still clean (pre-commit clears notebook output elsewhere but not under `pre_executed/`).
</verification>

<success_criteria>
- RECON-09 is complete in the runbook as well as the code.
- An operator can run the reconciler correctly from the runbook alone, including reading its summary and skip reasons.
- The paired notebook exists, executes, and is wired into both the Sphinx toctree and CLAUDE.md's pairing map.
</success_criteria>

<output>
Create `.planning/phases/29-the-reconciler/29-05-SUMMARY.md` when done
</output>
