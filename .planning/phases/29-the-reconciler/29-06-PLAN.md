---
phase: 29-the-reconciler
plan: 06
type: execute
wave: 5
depends_on: ["29-05"]
files_modified: []
autonomous: false
requirements: [RECON-07, RECON-09]

must_haves:
  truths:
    - "Every genuinely queue-scheduled CampaignRun on the real 3I/ATLAS campaign carries source=lco_queue or gemini_queue instead of legacy"
    - "The 19 approved, site-resolved 3I/ATLAS runs are visible on the real dev-DB calendar after one reconcile sweep"
    - "The queue runs render as one whole-window container entry each; the classical runs render one entry per night"
    - "A reconciler-owned event's calendar pop-up shows its Campaign run block without any manual admin linking"
    - "No runbook prose left behind assumes backfill_range_calendar_events still exists"
  artifacts:
    - path: ".planning/phases/29-the-reconciler/29-06-SUMMARY.md"
      provides: "the before/after evidence table for the live sweep (the dev DB is gitignored, so the SUMMARY is the durable record — Phase 27-02 precedent)"
  key_links:
    - from: "CampaignRun.source"
      to: "campaign_reconciler stage-1 branch"
      via: "run.source in QUEUE_SOURCES is the only queue/classical discriminator"
      pattern: "QUEUE_SOURCES"
---

<objective>
Close RECON-07 against real data: fix the `source` values D-07 identified, run the first full
reconcile sweep against the dev database, and confirm the 19 approved, site-resolved 3I/ATLAS runs
are actually visible on the calendar in the right two shapes.

Purpose: research measured (2026-08-04) that all 23 approved, site-resolved `CampaignRun` rows on the
real 3I/ATLAS campaign carry `source='legacy'` — none carry `lco_queue`/`gemini_queue` — while the
reconciler branches stage-1 projection purely on `run.source`, with no text-based fallback heuristic
(D-07: a heuristic would be new unverifiable logic that CONTEXT.md's no-scope-creep posture rules
out). Without the data fix, every run would render as a classical per-night projection, including the
8 the spike classified as queue-scheduled. D-07 scopes that fix as a tracked task in this plan, not a
silent assumption or an undocumented operator action.

Output: no code changes. A `29-06-SUMMARY.md` carrying the before/after evidence — the dev database
is gitignored, so the SUMMARY is the durable record (Phase 27-02 precedent).
</objective>

<execution_context>
@/home/tlister/git/fomo_devel/.claude/gsd-core/workflows/execute-plan.md
@/home/tlister/git/fomo_devel/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-the-reconciler/29-CONTEXT.md
@.planning/phases/29-the-reconciler/29-RESEARCH.md
@.planning/phases/29-the-reconciler/29-VALIDATION.md
@.planning/phases/29-the-reconciler/29-05-SUMMARY.md
@CLAUDE.md
</context>

## Artifacts this phase produces

See `29-01-PLAN.md`'s "Artifacts this phase produces" section for the phase-wide list of new
symbols. This plan produces no new code symbols — it exercises the phase's output against real data
and records the evidence in `29-06-SUMMARY.md`.

<tasks>

<task type="auto">
  <name>Task 1: Inventory the real 3I/ATLAS runs and capture the pre-fix dry-run baseline</name>
  <files>(read-only against the dev DB; evidence recorded in 29-06-SUMMARY.md)</files>
  <read_first>
    - .planning/phases/26-canonical-record-spike/26-DECISION.md ("Run-type inventory" finding — the
      human classification that produced the 8 QUEUE / 11 CLASSICAL / 0 SPACE split, and the
      `telescope_instrument`/`site_raw` evidence behind each call)
    - .planning/phases/29-the-reconciler/29-RESEARCH.md ("Common Pitfalls" #1 — the measured
      `source='legacy'` census and why no code-side reverse mapping from obscode to LCO/Gemini site
      code exists)
    - solsys_code/campaign_reconciler.py (`QUEUE_SOURCES` — the exact values a row must carry to take
      the container branch)
    - solsys_code/admin.py (`get_readonly_fields` — confirms `source` is staff-editable for any
      non-`web` row, which is what makes the admin the right tool for the fix)
  </read_first>
  <action>
Using a read-only `python manage.py shell` session against the dev database, produce two things and
record both in the SUMMARY.

First, an inventory table of every APPROVED, site-resolved `CampaignRun` on the real 3I/ATLAS
campaign (the `TargetList` named 3I/ATLAS — resolve it by name, do not hardcode a pk), with columns:
`pk`, `telescope_instrument`, `site` obscode, `site_raw`, `window_start`..`window_end`,
`telescope_class`, current `source`. Against that table, mark which rows correspond to
`26-DECISION.md`'s queue classification, quoting the spike's own reasoning for each (for example, an
`FTN/MuSCAT3` row at obscode F65 is LCO's `ogg` site and therefore queue-scheduled). Re-check the
research census while you are there: if any row already carries `lco_queue`/`gemini_queue`, staff
have started the fix and the checkpoint in Task 2 only needs the remainder.

Second, run `python manage.py reconcile_campaign_runs --dry-run` against the dev database and capture
its full stdout and stderr verbatim. This is the pre-fix baseline: expect it to report per-night
projections for the queue rows too, because they still carry `source='legacy'`. Record the summary
line and the count of runs whose reported action is a per-night creation.

Write no data in this task. The dry-run is guaranteed write-free by plan 29-03's `TestDryRun`.
  </action>
  <verify>
    <automated>python manage.py reconcile_campaign_runs --dry-run</automated>
  </verify>
  <acceptance_criteria>
    - The dry-run command exits 0 and its full output is quoted verbatim in `29-06-SUMMARY.md`.
    - The SUMMARY contains the inventory table with one row per approved, site-resolved 3I/ATLAS run, including each row's current `source` value.
    - The SUMMARY names the specific `pk` values staff must edit in Task 2, with the spike's reasoning for each.
    - `CalendarEvent.objects.count()` measured before and after this task is identical (the dry run wrote nothing).
  </acceptance_criteria>
  <done>Staff have an exact, evidence-backed list of which rows to reclassify, and the pre-fix behaviour is recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: D-07 — staff set `source` on the real queue-scheduled runs via the Django admin</name>
  <files>(no repository files — staff edit `CampaignRun.source` rows in the gitignored dev DB through the Django admin)</files>
  <read_first>
    - .planning/phases/29-the-reconciler/29-06-SUMMARY.md (Task 1's inventory table and the flagged
      `pk` list, with the spike's reasoning per row)
    - .planning/phases/29-the-reconciler/29-CONTEXT.md (D-07 — the decision this task executes)
  </read_first>
  <action>
Present the checkpoint below to the user and stop. Do not edit any `CampaignRun.source` value on
their behalf, and do not add code that infers the queue/classical split — D-07 makes this a human
judgement about real observing runs, and CONTEXT.md rules out a text-based fallback heuristic as new
unverifiable logic. Resume only on the user's explicit signal, then record in `29-06-SUMMARY.md`
exactly which `pk` values they changed, to which value, and any classification they disagreed with.
  </action>
  <what-built>
Everything the reconciler needs is now shipped and green: `reconcile_run()` branches stage-1
projection on `run.source in {lco_queue, gemini_queue}` (D-07: purely on that field, with no
text-based fallback heuristic, because a heuristic would be new unverifiable logic), the
`reconcile_campaign_runs` command sweeps every run with a `--dry-run` mode, all four staff actions
reconcile immediately, and the runbook and demo notebook document all of it.

Task 1 above produced, in `29-06-SUMMARY.md`, an inventory of every approved, site-resolved run on
the real 3I/ATLAS campaign with its current `source` value, and named the specific `pk` values that
`26-DECISION.md` classified as queue-scheduled. Every one of those rows currently carries
`source='legacy'` — a fact about the data, not about the code: the adapters that would have written
a real `source` are v2.3's ADAPT-01..03, which have not shipped, and these rows predate CANON-01.
Until the values are corrected, the reconciler will (correctly, given its input) render those runs as
classical per-night projections instead of whole-window queue containers.

This is the one step in the phase a human must do, because it is a judgement call about real
observing runs that no code in this repository can make: there is no reverse mapping anywhere in the
codebase from an MPC obscode to an LCO or Gemini internal site code, and the spike's 8/11 split was
produced by a person reading telescope and site text.
  </what-built>
  <how-to-verify>
1. Open `29-06-SUMMARY.md` and read the inventory table Task 1 wrote, including the `pk` list it
   flags as queue-scheduled and the reasoning quoted for each.
2. Start the dev server (`python manage.py runserver`) and go to `/admin/solsys_code/campaignrun/`.
3. For each flagged `pk`, open the row, confirm from `telescope_instrument` and `site_raw` that it
   really is a queue-scheduled run, and set **Source** to `LCO queue` or `Gemini queue` as
   appropriate. Leave every genuinely classical row on its current value. `source` is editable here
   for any non-`web` row.
4. Save each row and confirm the change-list now shows the corrected Source values.
5. If you disagree with any of the spike's classifications, say so in your reply rather than changing
   it silently — the split is the evidence base for RECON-07's success criterion.

Nothing else changes: `approval_status`, `site`, `telescope_class` and the windows are all left
exactly as they are.
  </how-to-verify>
  <verify>
    <human-check>The Django admin change-list for Campaign runs shows the flagged queue rows carrying `LCO queue` or `Gemini queue` in the Source column, and every other approved 3I/ATLAS row unchanged.</human-check>
  </verify>
  <acceptance_criteria>
    - The user has replied with an explicit resume signal.
    - `29-06-SUMMARY.md` records each edited `pk`, its old and new `source` value, and any classification the user disagreed with.
    - A read-only shell query confirms `CampaignRun.objects.filter(campaign=<3I/ATLAS>, source__in=['lco_queue', 'gemini_queue']).count()` is non-zero and matches the number of rows the user reported editing.
  </acceptance_criteria>
  <resume-signal>Type "approved" once the queue rows carry their corrected Source values, or describe which rows you changed differently and why.</resume-signal>
  <done>The real queue-scheduled runs carry a `source` the reconciler can branch on, with the edit recorded by pk.</done>
</task>

<task type="auto">
  <name>Task 3: Run the first full reconcile sweep against the dev database and record the outcome</name>
  <files>(writes CalendarEvent/CalendarEventMeta rows in the gitignored dev DB; evidence recorded in 29-06-SUMMARY.md)</files>
  <read_first>
    - .planning/phases/29-the-reconciler/29-06-SUMMARY.md (Task 1's inventory and baseline, plus the
      staff decisions recorded at the Task 2 checkpoint)
    - .planning/phases/29-the-reconciler/29-VALIDATION.md ("Manual-Only Verifications" — the two
      checks that cannot be automated and are carried out here)
    - docs/runbooks/telescope_runs_calendar.rst (the reconciler section written in plan 29-05 — this
      task follows it as written, which is itself a test of whether it is followable)
  </read_first>
  <action>
Follow the runbook's own dry-run-first workflow against the dev database.

1. Re-run `python manage.py reconcile_campaign_runs --dry-run` and capture the output. Compare it
   against Task 1's pre-fix baseline: the corrected queue rows should now report a single
   would-create each instead of a per-night fan-out. Record both outputs side by side in the SUMMARY.
2. Run `python manage.py reconcile_campaign_runs` for real and capture the summary line and every
   stderr skip/failure line verbatim.
3. Re-run it a second time and capture that summary line too — it must report `created: 0` and
   `updated: 0`, which is RECON-01's idempotency claim measured against real data rather than a
   fixture.
4. Query the resulting state and record it as a table in the SUMMARY: for each of the 19 approved,
   site-resolved 3I/ATLAS runs, its `pk`, `source`, the number of `RUN:`-namespaced events it now
   owns, and whether its key form is the bare container or date-bearing per-night. Confirm the split
   matches the corrected data (a whole-window container for each queue run, one entry per night for
   each classical run) and that every event the reconciler wrote has a `CalendarEventMeta` row whose
   `run` points at the right run.
5. Record the count of `CalendarEvent` rows that existed before step 2 and still have an identical
   `modified` timestamp after step 3 — the real-data form of RECON-05's "never touches events it does
   not own", covering the campaign's existing LCO-derived and classical events.

If the sweep reports any skipped or failed run, record each one with its pk and reason and state
whether it is expected (for example a TBD-window run, which has no dates to project) or a genuine gap
that needs following up.
  </action>
  <verify>
    <automated>python manage.py reconcile_campaign_runs --dry-run</automated>
    <human-check>
1. Visit `/calendar/` on the dev server and confirm the 3I/ATLAS runs are visible: each queue run as
   a single entry spanning its whole window, each classical run as one entry per observing night.
2. Click one reconciler-owned entry and confirm its pop-up now shows a **Campaign run** block naming
   the run, its window and its status — with no manual admin linking, which is the WR-03 gap this
   phase closes.
3. Confirm the individual LCO observation entries for `CampaignRun` pk=1 are still present and
   unchanged alongside its new container entry (the two key families coexisting).
4. Read `docs/runbooks/telescope_runs_calendar.rst` end to end and confirm no remaining prose assumes
   `backfill_range_calendar_events` is still an available command (29-VALIDATION.md's second
   manual-only verification — a grep catches the literal name, only a read catches surrounding prose
   that assumed it existed).
    </human-check>
  </verify>
  <acceptance_criteria>
    - The second real sweep's summary line reports `created: 0` and `updated: 0`.
    - `29-06-SUMMARY.md` contains the pre-fix and post-fix dry-run outputs, both real-sweep summary lines, and the per-run outcome table for all 19 runs.
    - Every one of the 19 runs owns at least one `RUN:`-namespaced event, with the key form matching its corrected `source`/`telescope_class`.
    - The SUMMARY records the count of pre-existing calendar events whose `modified` timestamp is unchanged after the sweep.
    - Every skipped or failed run is listed by pk with its reason and an expected/unexpected verdict.
  </acceptance_criteria>
  <done>RECON-07's flagship criterion is demonstrated against the real dev database, with idempotency and non-interference measured rather than assumed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| staff Django admin -> `CampaignRun.source` | A human edit to a field the reconciler branches its entire projection strategy on |
| first full sweep -> live dev-DB calendar | The first time this phase's code writes to a database holding real campaign and observation data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-19 | Tampering | The first live sweep overwriting or deleting a real hand-created or un-attributed calendar event | mitigate | RECON-05's ownership rule is the first filter in every write path (plan 29-01) and is proven by fixture tests (plans 29-01/29-02); Task 3 additionally measures it against real data by recording unchanged `modified` timestamps on pre-existing events, and the dry-run in step 1 is inspected before any write |
| T-29-20 | Tampering | A wrong `source` edit sending a classical run down the container branch (or the reverse), producing a misleading calendar | mitigate | The Task 2 checkpoint gives staff the spike's per-row reasoning and asks them to disagree out loud rather than change silently; the sweep is idempotent, so a corrected `source` plus a re-run converges — the old key is orphaned rather than duplicated, and is reported in Task 3's table |
| T-29-21 | Repudiation | A live-data change with no durable record, since the dev DB is gitignored | mitigate | Phase 27-02's precedent: the before/after evidence lives in `29-06-SUMMARY.md`, including the exact `pk` values edited and both dry-run outputs |
| T-29-22 | Elevation of Privilege (business logic) | The sweep projecting an unapproved web submission onto the public calendar | mitigate | `_skip_reason()`'s `'not approved'` gate (plan 29-01); Task 3 records every skip by pk and reason, so any unapproved row appearing in the created counts would be visible |
| T-29-SC | Tampering | npm/pip/cargo installs | accept | No packages installed in this phase; no legitimacy audit applies |
</threat_model>

<verification>
- `python manage.py reconcile_campaign_runs --dry-run` reports `would_create: 0` after the real sweep.
- A second real sweep reports `created: 0, updated: 0`.
- `python manage.py test solsys_code` still green after the live run (no code changed, but the phase gate is re-confirmed).
- `ruff check .` and `ruff format --check .` clean.
</verification>

<success_criteria>
- The real queue-scheduled runs carry a correct `source`, recorded by pk in the SUMMARY.
- The 19 approved, site-resolved 3I/ATLAS runs are visible on the real calendar in the two correct shapes.
- A reconciler-owned event's pop-up shows its Campaign run block with no manual admin linking.
- Pre-existing events the reconciler does not own are provably untouched.
- The runbook survives a human end-to-end read with no stale backfill assumptions.
</success_criteria>

<output>
Create `.planning/phases/29-the-reconciler/29-06-SUMMARY.md` when done
</output>
