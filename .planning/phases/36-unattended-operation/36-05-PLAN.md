---
phase: 36-unattended-operation
plan: 05
type: execute
wave: 3
depends_on: ["36-01", "36-02", "36-03", "36-04"]
files_modified:
  - docs/runbooks/telescope_runs_calendar.rst
  - docs/notebooks/pre_executed/backfill_lco_observations_demo.ipynb
  - docs/notebooks.rst
  - docs/installation.rst
  - CLAUDE.md
autonomous: true
requirements: [SCHED-08, SCHED-09, SCHED-10, DISCOVER-01]

estimate:
  tokens: 74000
  raw_tokens: 74000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "An operator can set up, or verify, the whole unattended schedule on a fresh host from one runbook section — copy the template, run `check_unattended`, install the printed line, read the log, watch the heartbeat — without reading any source (SC 5, D-05)."
    - "The runbook's new section documents both failure signals: the email (who receives it, what it contains, when it repeats and when it clears) and the heartbeat (what `/start` and `/<exit-code>` mean, and the grace period a little above one 15-minute interval), plus the checklist for when nothing has appeared (D-11, D-12, D-18)."
    - "The runbook's `How do I backfill ObservationRecords without a campaign?` section documents that `--proposal` is now optional and that a bare run sweeps every active watched proposal, with the per-row overrides and the per-row failure isolation (D-07, D-08, D-09)."
    - "The command cheat-sheet carries a row for `run_unattended` and one for `check_unattended`, and the `backfill_lco_observations` row reflects its new optional-argument contract."
    - "`docs/notebooks/pre_executed/backfill_lco_observations_demo.ipynb` contains executed cells that seed `WatchedProposal` rows, run the command bare, show the resulting `last_run_summary` values, and show one proposal failing without stopping the other — with real committed output, against a mocked portal."
    - "`docs/notebooks.rst` lists `backfill_lco_observations_demo` in its Demonstration Notebooks toctree, closing the pre-existing gap RESEARCH.md found."
    - "`CLAUDE.md`'s notebook map records the pairing for every module this phase adds, including that the runbook section — not a notebook — is the paired doc for the runner and `check_unattended`, and why."
    - "No committed doc, notebook cell or runbook example quotes a live setting, a `local_settings.py` value, or any credential; the worked examples use placeholder proposal codes and placeholder paths (Phase 31 constraint, D-15)."
  prohibitions:
    - statement: "No notebook cell, runbook example, or committed doc may quote a live setting, anything from `local_settings.py`, or any credential value (Phase 31 constraint carried into this phase, D-15)."
      status: active
      verification: flagged-unverified
    - statement: "No notebook cell may make a live network call — the portal is mocked exactly as the existing cells mock it, so the notebook executes offline in CI."
      status: active
      verification: flagged-unverified
    - statement: "The worked examples must use placeholder proposal codes, never the real dev-database proposal code, and no real contact information may appear anywhere on the runbook page (existing runbook rule)."
      status: active
      verification: flagged-unverified
    - statement: "Any `Target` fixture in a notebook cell uses `tom_targets.tests.factories.NonSiderealTargetFactory` or the command's own non-sidereal build path, never `SiderealTargetFactory` (CLAUDE.md conventions)."
      status: active
      verification: flagged-unverified
    - statement: "The notebook must clean up every row it creates in its final cell, as the existing cells already do — a demo must not leave residue in the developer database."
      status: active
      verification: flagged-unverified
  artifacts:
    - docs/runbooks/telescope_runs_calendar.rst
    - docs/notebooks/pre_executed/backfill_lco_observations_demo.ipynb
    - docs/notebooks.rst
    - CLAUDE.md
  key_links:
    - "the runbook's new section ↔ `deploy/cron/fomo.crontab.example` and `check_unattended`'s printed line — the section is the only place that tells an operator these two exist and agree"
    - "the runbook's 'nothing has appeared' checklist ↔ `WatchedProposal.last_run_summary` in the admin, `settings.FOMO_LOG_FILE`, and the heartbeat dashboard — the three places D-18 says to look, in that order"
    - "`CLAUDE.md`'s notebook map ↔ every module this phase adds — the map is what makes the paired-docs rule enforceable for the next phase that touches `unattended.py`"
---

<objective>
Deliver the phase's paired docs: the runbook section that makes SC 5 true, the notebook cells that
show the watched-proposal contract with real executed output, and the `CLAUDE.md` map entries that
keep both enforceable for the next phase that touches these modules.

Purpose: CLAUDE.md treats paired docs as part of the deliverable, not polish. This plan runs in
wave 3 because it documents behavior the earlier plans must have actually shipped — a doc written
against a plan rather than against the code is how a runbook goes stale.

Output: a new runbook section plus three amended ones, executed notebook cells, a closed toctree
gap, a cross-reference from the installation guide, and an extended `CLAUDE.md` notebook map.
</objective>

<execution_context>
@/home/tlister/git/fomo_devel/.claude/gsd-core/workflows/execute-plan.md
@/home/tlister/git/fomo_devel/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-unattended-operation/36-CONTEXT.md
@.planning/phases/36-unattended-operation/36-RESEARCH.md
@.planning/phases/36-unattended-operation/36-01-SUMMARY.md
@.planning/phases/36-unattended-operation/36-02-SUMMARY.md
@.planning/phases/36-unattended-operation/36-03-SUMMARY.md
@.planning/phases/36-unattended-operation/36-04-SUMMARY.md
@CLAUDE.md
</context>

## Artifacts this phase produces

New documentation artifacts created by this plan:

- `docs/runbooks/telescope_runs_calendar.rst` — new section `How do I run everything unattended?`
  with the label `.. _unattended-operation:`; new sub-headings for setup, verification, the two
  failure signals, and the "nothing has appeared" checklist; two new cheat-sheet rows
  (`run_unattended`, `check_unattended`) and one amended row (`backfill_lco_observations`); new
  troubleshooting entries
- `docs/notebooks/pre_executed/backfill_lco_observations_demo.ipynb` — new markdown+code cell
  pairs for the watched-proposal contract, with committed executed output
- `docs/notebooks.rst` — one new toctree entry
- `docs/installation.rst` — a cross-reference from `running-management-commands` to the new
  runbook section
- `CLAUDE.md` — notebook-map entries for `solsys_code/unattended.py`,
  `solsys_code/notifications.py`, `run_unattended.py` and `check_unattended.py`

(Code symbols from the other plans are listed in `36-01-PLAN.md` §"Artifacts this phase produces".)

<tasks>

<task type="auto">
  <name>Task 1: The runbook's unattended-operation section</name>
  <files>docs/runbooks/telescope_runs_calendar.rst</files>
  <read_first>
    - `docs/runbooks/telescope_runs_calendar.rst` — the whole page structure, but specifically: the title and heading underline conventions (section underlines are `-` runs matching the heading width, sub-headings are `^`), the `How do I backfill ObservationRecords without a campaign?` section at lines 330-437 (which this task amends), the `.. _command-cheat-sheet:` `list-table` at lines 1506-1544 (three columns: Command / Key flags / One-line description), the `Troubleshooting` section at lines 1544-1754 with its `^`-underlined per-symptom sub-headings, and the `See also` section at the end
    - `deploy/cron/fomo.crontab.example` and `deploy/logrotate/fomo.example` — the artifacts the new section tells an operator to copy
    - `36-01-SUMMARY.md` through `36-04-SUMMARY.md` — what actually shipped, including the final command flags, the banner format, the reminder interval and the suppression behavior; write the section against these, not against the plans
    - `.planning/phases/36-unattended-operation/36-CONTEXT.md` D-05, D-11, D-12, D-14, D-18 — the content the section must cover
  </read_first>
  <action>
    Add a new section to `docs/runbooks/telescope_runs_calendar.rst`, placed after
    `How do I get every campaign run onto the calendar?` and before the
    `Why doesn't the calendar pop-up show an "Attributed campaign run" block?` section, titled
    `How do I run everything unattended?` with an explicit `.. _unattended-operation:` label above
    it so other pages can cross-reference it. Match the page's existing heading-underline style
    exactly. Cover, as `^`-underlined sub-headings in this order:

    1. **What runs, and when.** The one runner command, the four steps in D-01's fixed order, the
       `*/15 * * * *` schedule, and that a failed step never stops the later ones — the tick just
       exits non-zero (D-02, D-04).
    2. **Setting it up on a fresh host.** The numbered walk D-05 specifies: create
       `/var/lock/fomo` and `/var/log/fomo`; put the real `EMAIL_BACKEND` / mail settings and the
       LCO API key in `local_settings.py`; export `FOMO_HEARTBEAT_URL` and `FOMO_BASE_URL` in the
       environment cron sees; run `python manage.py check_unattended`; fix whatever it reports;
       copy the printed cron line into the crontab; drop
       `deploy/logrotate/fomo.example` into `/etc/logrotate.d/fomo`. Name every variable and
       setting, and quote no value — say what to set, never what it is set to.
    3. **Adding a proposal to watch.** The admin changelist, the `is_active` checkbox, the
       `target_list_name` and `attributed_to` overrides, and that nothing else is needed — no
       redeploy, no argument (SC 2, D-06, D-07).
    4. **The two failure signals.** The email: who gets it (every staff user with an email on
       file), the subject shape, what the body carries and deliberately does not (no traceback, no
       request URL, no portal response text), that it arrives once per newly-failing tick with a
       24-hour reminder while the same failure persists, and that a recovery message arrives once
       when a tick next succeeds (D-11, D-13, D-14). The heartbeat: `/start` before the first step
       and `/<exit-code>` after the last, why that catches a tick that never ran or hung as well as
       one that failed, the recommended one-check-per-schedule setup, and a grace period a little
       above one interval (about 20 minutes for the 15-minute schedule) (D-12).
    5. **When nothing has appeared.** The checklist in D-18's order: the admin's
       `WatchedProposal` list — is there an active row, and what does its `last_run_summary` say?
       (start here, because an empty list is a healthy quiet no-op, D-08); the log file, and what
       a tick's START/step/END banner looks like; the heartbeat dashboard's last ping; and the
       `lock held` skip line that says a previous tick was still running.
    6. **Running it by hand.** `python manage.py run_unattended --dry-run` and
       `--step <name>`, and that neither pings the heartbeat nor mails staff, so an operator can
       debug without paging anyone.

    Then amend three existing parts of the page:

    - `How do I backfill ObservationRecords without a campaign?` — document that `--proposal` is
      now optional; that a bare run sweeps every active `WatchedProposal` applying each row's
      `target_list_name` and `attributed_to`; that `--proposal` remains a one-off override which
      does not require the code to be watched; that an empty list is a quiet no-op; and that one
      failing proposal is recorded on its own row and does not stop the others (D-07, D-08, D-09).
      Cross-reference the new unattended section.
    - The `command-cheat-sheet` `list-table` — add a `run_unattended` row (`--dry-run`,
      `--step <name>`, both optional) and a `check_unattended` row (`--send-test-email`,
      optional), and update the `backfill_lco_observations` row's Key flags column to show
      `--proposal` as optional.
    - `Troubleshooting` — add entries for: nothing appearing on the calendar while the tick reports
      success (an empty watched list), repeated `lock held` skip lines (a tick taking longer than
      the interval, or a stale lock), a failure email arriving once then going quiet (the
      suppression rule, not a lost alert), and the heartbeat alerting while the log shows a
      healthy tick (the ping failed but never fails the tick, by design).

    Use placeholder proposal codes and placeholder paths throughout — the page's existing rule that
    no real contact information or real identifier appears anywhere on it applies to this section
    too, and Phase 31's constraint forbids quoting any live setting.
  </action>
  <verify>
    <automated>pre-commit run sphinx-build --all-files</automated>
    <fails_when>non-zero exit, or the output contains "WARNING" / "ERROR" / "Title underline too short" from the new section</fails_when>
    <automated>python -c "src=open('docs/runbooks/telescope_runs_calendar.rst').read();print('How do I run everything unattended?' in src, '_unattended-operation:' in src, 'run_unattended' in src, 'check_unattended' in src, 'FOMO_HEARTBEAT_URL' in src, 'logrotate' in src, 'lock held' in src)"</automated>
    <fails_when>non-zero exit, or any value in the printed tuple is False</fails_when>
    <automated>python -c "src=open('docs/runbooks/telescope_runs_calendar.rst').read();i=src.index('.. _command-cheat-sheet:');j=src.index('Troubleshooting',i);t=src[i:j];print('``run_unattended``' in t, '``check_unattended``' in t)"</automated>
    <fails_when>non-zero exit, or either value is False — the two new commands are mentioned on the page but not in the cheat-sheet table</fails_when>
  </verify>
  <acceptance_criteria>
    - `pre-commit run sphinx-build --all-files` passes with no new warning
    - `docs/runbooks/telescope_runs_calendar.rst` contains the heading `How do I run everything unattended?` preceded by a `.. _unattended-operation:` label
    - That section contains all six sub-headings listed in the action, and names `FOMO_HEARTBEAT_URL`, `FOMO_BASE_URL`, `check_unattended`, `deploy/cron/fomo.crontab.example` and `deploy/logrotate/fomo.example`
    - The `command-cheat-sheet` table contains rows for `run_unattended` and `check_unattended` (the region-scoped probe above confirms both are inside the table, not merely on the page)
    - The `backfill_lco_observations` cheat-sheet row shows `--proposal` as optional
    - The `Troubleshooting` section gains the four new entries named in the action
    - No line of the new content contains an `=`-assignment giving a value to any `FOMO_*` variable, an email address, or a real proposal code
  </acceptance_criteria>
  <done>An operator with no knowledge of this codebase can go from a fresh host to a running, monitored, log-rotated 15-minute schedule using this one section.</done>
</task>

<task type="auto">
  <name>Task 2: The backfill demo notebook's watched-proposal cells</name>
  <files>docs/notebooks/pre_executed/backfill_lco_observations_demo.ipynb, docs/notebooks.rst</files>
  <read_first>
    - `docs/notebooks/pre_executed/backfill_lco_observations_demo.ipynb` — all 17 cells, but especially cell 2 (the Django setup boilerplate and its `FOMO_DATABASE_PATH` handling), cell 4 (`DEMO_PROPOSAL` / `DEMO_TARGET_NAME` / `DEMO_GROUP_NAME_FRAGMENT` constants), cell 6 (the `make_request` patch and `FIRST_PAGE` payload helpers), cell 10 (the real-pass `call_command` with the patch context manager), and cell 16 (the cleanup cell that deletes every row the notebook created)
    - `docs/notebooks.rst` — the `Demonstration Notebooks` toctree and its `Title <path>` entry format
    - `solsys_code/models.py` `WatchedProposal` as shipped — the field names the cells set and read
    - `36-02-SUMMARY.md` — the shipped bare-invocation output format, so the narrative matches what the cells actually print
  </read_first>
  <action>
    Extend `docs/notebooks/pre_executed/backfill_lco_observations_demo.ipynb` with three new
    markdown+code cell pairs, inserted after the existing second-real-pass section (cell 14) and
    before the cleanup section, following the notebook's established style exactly — a markdown
    cell explaining what the next cell demonstrates and why, then a code cell that patches
    `solsys_code.management.commands.backfill_lco_observations.make_request` and calls
    `call_command` with captured stdout/stderr:

    1. **Seeding the watched list, and running with no arguments at all.** Create two
       `WatchedProposal` rows with placeholder codes derived from the notebook's existing
       `DEMO_PROPOSAL` constant (add a second, e.g. a `-B` suffixed sibling), one of them with a
       `target_list_name` override, then `call_command('backfill_lco_observations')` with no
       `--proposal`. Print the summary and show that both codes were queried. The markdown cell
       makes the SC 2 point explicitly: this is the whole of what an operator does — add a row in
       the admin.
    2. **The bookkeeping the sweep writes back.** Re-read both rows and print `last_run_at` and
       `last_run_summary`, with the markdown cell explaining that this is what an operator reads in
       the admin when nothing has appeared.
    3. **Per-proposal failure isolation.** Patch `make_request` with a side effect that raises for
       the first code's URL and returns a normal page for the second, run bare again, and print
       both rows' `last_run_summary` — one starting `failed:` with a class name, the other a real
       counter line — plus the records created for the second proposal. The markdown cell states
       the rule (one bad proposal never blocks the rest) and the credential rule (the summary
       carries the exception's class name and never its message).

    Extend the existing cleanup cell to delete the `WatchedProposal` rows the new cells created, so
    the notebook still leaves the developer database exactly as it found it.

    Make no live network call anywhere — every new cell patches `make_request` the same way the
    existing cells do. Use placeholder proposal codes; never the real dev-database code.

    Re-execute the notebook in place with
    `jupyter nbconvert --to notebook --execute --inplace docs/notebooks/pre_executed/backfill_lco_observations_demo.ipynb`
    and commit it with its output (the pre-commit clear-output hook excludes
    `docs/notebooks/pre_executed`, so committed output is the convention there).

    Finally, add `Backfilling LCO Observations (watched proposals) <notebooks/pre_executed/backfill_lco_observations_demo>`
    to the `Demonstration Notebooks` toctree in `docs/notebooks.rst`, after the
    `Projecting Observation Records onto the Calendar` entry — closing the pre-existing gap
    RESEARCH.md found, where this notebook exists on disk but is unreachable from the docs
    navigation.
  </action>
  <verify>
    <automated>python -c "import json;nb=json.load(open('docs/notebooks/pre_executed/backfill_lco_observations_demo.ipynb'));src=''.join(''.join(c['source']) for c in nb['cells']);code=[c for c in nb['cells'] if c['cell_type']=='code'];print(len(nb['cells']), 'WatchedProposal' in src, sum(1 for c in code if c.get('outputs')))"</automated>
    <fails_when>non-zero exit, a cell count of 17 or fewer (no cells were added), a False for the WatchedProposal check, or an executed-cell count lower than the number of code cells minus zero — meaning the notebook was committed without its output</fails_when>
    <automated>python -c "import json;nb=json.load(open('docs/notebooks/pre_executed/backfill_lco_observations_demo.ipynb'));src=''.join(''.join(c['source']) for c in nb['cells']);print('last_run_summary' in src, 'last_run_at' in src, src.count('make_request'), 'failed:' in src)"</automated>
    <fails_when>non-zero exit, either of the first two values is False, the `make_request` count is lower than it was before this task (a new cell reaches the network unpatched), or the last value is False</fails_when>
    <automated>python -c "src=open('docs/notebooks.rst').read();print('backfill_lco_observations_demo' in src)"</automated>
    <fails_when>non-zero exit, or the printed value is False — the toctree gap is still open</fails_when>
    <automated>python -c "import os,django;os.environ.setdefault('DJANGO_SETTINGS_MODULE','src.fomo.settings');django.setup();from solsys_code.models import WatchedProposal;print(WatchedProposal.objects.count())"</automated>
    <fails_when>non-zero exit, or a printed count greater than 0 after the notebook has been executed — the cleanup cell did not remove the rows the demo created</fails_when>
  </verify>
  <acceptance_criteria>
    - The notebook has at least 6 more cells than the 17 it started with, and every code cell carries committed output
    - The notebook's source contains `WatchedProposal`, `last_run_summary`, `last_run_at`, a bare `call_command('backfill_lco_observations')` with no `--proposal`, and a raising `make_request` side effect
    - Running `jupyter nbconvert --to notebook --execute --inplace` on the notebook exits 0 with no live network access
    - `WatchedProposal.objects.count()` is 0 after execution (the cleanup cell removes the demo rows)
    - `docs/notebooks.rst` lists `notebooks/pre_executed/backfill_lco_observations_demo` in the `Demonstration Notebooks` toctree
    - No cell contains a real proposal code, an email address, or any value read from `local_settings.py`
  </acceptance_criteria>
  <done>The notebook shows the new discovery contract working, failing safely, and recording what happened — with real executed output an operator can read without running anything.</done>
</task>

<task type="auto">
  <name>Task 3: CLAUDE.md's notebook map and the installation cross-reference</name>
  <files>CLAUDE.md, docs/installation.rst</files>
  <read_first>
    - `CLAUDE.md` — the "Paired docs are part of the deliverable" bullet in full, including the module→notebook map, the `campaign_lifecycle_demo.ipynb` precedent for a notebook that covers several modules rather than one, the breach-history sentence at the end, and the `docs/runbooks/` directory-scoped clause added after quick task `260726-kdp`
    - `docs/installation.rst` lines 119-145 — the `.. _running-management-commands:` label and the section body a cross-reference would attach to
    - `docs/runbooks/telescope_runs_calendar.rst` as left by Task 1 — the `.. _unattended-operation:` label the cross-reference targets
  </read_first>
  <action>
    Extend `CLAUDE.md`'s module→notebook map inside the "Paired docs are part of the deliverable"
    bullet with the pairings this phase creates, keeping the existing prose style (a
    `module -> notebook` chain, semicolon-separated, inside the same bullet):

    - `solsys_code/management/commands/backfill_lco_observations.py -> backfill_lco_observations_demo.ipynb`
      already exists in the map — leave it, and note that the watched-proposal contract is covered
      by that same notebook.
    - Add: `solsys_code/unattended.py`, `solsys_code/notifications.py`,
      `solsys_code/management/commands/run_unattended.py` and
      `solsys_code/management/commands/check_unattended.py` are paired with the runbook's
      `How do I run everything unattended?` section (`docs/runbooks/telescope_runs_calendar.rst`),
      **not** a notebook — record the reason explicitly, in one sentence: a runner demo could only
      execute with the portal, the mail backend and the heartbeat all mocked at once, which
      demonstrates the mocking rather than the runner, and 36-CONTEXT.md's discretion note
      sanctions the runbook section as the paired doc in exactly that case. Without that recorded
      reason the next phase that touches `unattended.py` cannot tell a deliberate choice from an
      enforcement hole — which is the failure mode the map's own breach history exists to prevent.

    Add the Phase 36 entry to the breach-history sentence only if a paired-doc miss actually
    occurred during this phase's execution; if the docs shipped with the code, add nothing there.

    In `docs/installation.rst`, add a short cross-reference at the end of the
    `Running FOMO Management Commands` section pointing at the new runbook section with
    `:ref:`unattended-operation`` — one or two sentences saying that most of these commands can be
    run on a schedule with no operator action, and where to read how.
  </action>
  <verify>
    <automated>python -c "src=open('CLAUDE.md').read();print('solsys_code/unattended.py' in src, 'check_unattended' in src, 'run_unattended' in src, 'telescope_runs_calendar.rst' in src)"</automated>
    <fails_when>non-zero exit, or any value in the printed tuple is False — the new modules are not recorded in the paired-docs map</fails_when>
    <automated>python -c "src=open('CLAUDE.md').read();i=src.index('Paired docs are part of the deliverable');j=src.index('## Project', i);print('unattended.py' in src[i:j], 'notifications.py' in src[i:j])"</automated>
    <fails_when>non-zero exit, or either value is False — the entries landed somewhere else in CLAUDE.md rather than inside the paired-docs bullet the rule is enforced from</fails_when>
    <automated>pre-commit run sphinx-build --all-files</automated>
    <fails_when>non-zero exit, or the output contains "undefined label" / "WARNING" for the `unattended-operation` reference</fails_when>
  </verify>
  <acceptance_criteria>
    - `CLAUDE.md`'s "Paired docs are part of the deliverable" bullet names `solsys_code/unattended.py`, `solsys_code/notifications.py`, `run_unattended.py` and `check_unattended.py`, each mapped to the runbook's unattended-operation section (the region-scoped probe above confirms they are inside that bullet)
    - That bullet carries the one-sentence recorded reason for pairing the runner with a runbook section rather than a notebook
    - `docs/installation.rst` contains ``:ref:`unattended-operation``` in the `Running FOMO Management Commands` section
    - `pre-commit run sphinx-build --all-files` passes with no undefined-label warning
  </acceptance_criteria>
  <done>The next agent that changes `unattended.py` is told, by CLAUDE.md itself, which doc it must update — and why that doc is a runbook section rather than a notebook.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed docs and notebooks → the public repository | anything written here is published; a quoted setting value or a real proposal code leaves the machine permanently |
| notebook execution → the developer database | the cells create and delete real rows in `src/fomo_db.sqlite3` |
| notebook execution → the network | the portal is mocked; an unpatched cell would make a real authenticated call with the developer's API key |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36-04 | Information Disclosure | the runbook's setup section and the notebook's committed output | high | mitigate | Every example names a variable or setting and never its value; placeholder proposal codes and placeholder paths throughout; the acceptance criteria assert the absence of `FOMO_*` value assignments, email addresses and real proposal codes (Phase 31 constraint, D-15) |
| T-36-17 | Information Disclosure | an unpatched notebook cell making a live authenticated portal call whose response is then committed as cell output | high | mitigate | Every new cell patches `solsys_code.management.commands.backfill_lco_observations.make_request` exactly as the existing cells do; the verify probe counts `make_request` occurrences so a new cell that skips the patch is visible |
| T-36-18 | Tampering | notebook execution leaving demo rows in the developer database | low | mitigate | The cleanup cell is extended to delete the new `WatchedProposal` rows; a post-execution count probe asserts zero remain |
| T-36-19 | Repudiation | the runbook going stale after a future change to `unattended.py` | medium | mitigate | `CLAUDE.md`'s notebook map gains the explicit runner→runbook pairing plus the recorded reason, so the paired-docs rule is enforceable for the next phase rather than depending on someone remembering |
| T-36-SC | Tampering | npm/pip/cargo installs | high | accept | No new package is installed by this phase — 36-RESEARCH.md §"Package Legitimacy Audit" records zero new dependencies |
</threat_model>

<verification>
- `pre-commit run sphinx-build --all-files` passes with no new warning
- `jupyter nbconvert --to notebook --execute --inplace docs/notebooks/pre_executed/backfill_lco_observations_demo.ipynb` exits 0 offline, and the notebook is committed with output
- `python -c "import os,django;os.environ.setdefault('DJANGO_SETTINGS_MODULE','src.fomo.settings');django.setup();from solsys_code.models import WatchedProposal;print(WatchedProposal.objects.count())"` prints `0` after execution
- `docs/notebooks.rst` lists the backfill demo notebook
- `CLAUDE.md`'s paired-docs bullet names every module this phase added
- `pre-commit run ruff --all-files && pre-commit run ruff-format --all-files` passes
</verification>

<success_criteria>
The phase's paired docs ship with the phase, not after it: one runbook section takes an operator
from a fresh host to a monitored schedule, the notebook shows the new discovery contract with real
executed output, the previously-orphaned notebook is reachable from the docs navigation, and
CLAUDE.md records which doc each new module is paired with and why.
</success_criteria>

<output>
Create `.planning/phases/36-unattended-operation/36-05-SUMMARY.md` when done
</output>
