---
phase: 36-unattended-operation
plan: 06
type: execute
wave: 4
depends_on: ["36-01", "36-03", "36-05"]
files_modified:
  - docs/runbooks/telescope_runs_calendar.rst
  - deploy/cron/fomo.crontab.example
  - solsys_code/management/commands/check_unattended.py
  - solsys_code/tests/test_check_unattended.py
  - solsys_code/unattended.py
  - .planning/phases/36-unattended-operation/36-VERIFICATION.md
autonomous: true
gap_closure: true
gap_ids: [G-36-3]
requirements: [SCHED-08, SCHED-09]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "An operator who configures the heartbeat check using only what the runbook names gets a check that goes late about 15 minutes after a missed tick and alerts about 35 minutes after the last successful ping — not a day later (G-36-3's failed truth, restored)."
    - "The runbook's heartbeat guidance names BOTH knobs the check needs — the expected interval between pings (healthchecks.io spells this `Period`) and the grace time (`Grace`) — states the arithmetic explicitly (the service alerts at last ping + expected interval + grace), and names the 1-day interval default as the trap that silently disables the dead-man layer."
    - "The runbook offers the Cron-type check with `*/15 * * * *` as the drift-free alternative to a 15-minute expected interval, and says to keep the grace time at about 20 minutes because it ALSO bounds the allowed gap between the `/start` ping and the completion ping — so it must not be shrunk to make the total look shorter."
    - "The 'When nothing has appeared' triage item judges a stale ping against expected interval + grace (about 35 minutes with the recommended values), not against the grace time alone."
    - "The troubleshooting section carries an entry for the exact symptom this gap produced — the schedule stopped and the heartbeat never alerted — naming the still-defaulted expected interval as the cause and the fix."
    - "`deploy/cron/fomo.crontab.example` describes the heartbeat backstop with both knobs instead of one, and `check_unattended`'s `[ok]` heartbeat line reminds the operator that the check at the other end still needs its own expected ping interval set."
    - "`36-VERIFICATION.md`'s human-test script and its runbook evidence cell state the corrected time-to-alert, so the verification record no longer carries the instruction that produced G-36-3, and the correction is traceable to the gap id."
    - "Sphinx still builds `docs/runbooks/telescope_runs_calendar.rst` with no new warning, and `python manage.py test solsys_code.tests.test_check_unattended` stays green."
  prohibitions:
    - statement: "No new sentence in any committed file may quote a real heartbeat URL, a real ping UUID, or any other `FOMO_*` value — the corrected guidance names knobs and settings only (D-15, the Phase 31 constraint carried through this phase)."
      status: active
      verification: flagged-unverified
    - statement: "The grace-time recommendation must stay at about 20 minutes. It also bounds the allowed `/start`-to-completion gap, so shrinking it to force a shorter total time-to-alert would page on a merely slow tick."
      status: active
      verification: flagged-unverified
    - statement: "The corrected wording must stay service-agnostic: name the concept (the check's expected interval between pings) first and give healthchecks.io's knob name as the vendor spelling, because the runbook deliberately says 'healthchecks-compatible' and self-hosted or other-vendor endpoints may spell it differently."
      status: active
      verification: flagged-unverified
    - statement: "`ping_heartbeat()` in `solsys_code/unattended.py` is confirmed correct by the debug session and by UAT Test 3's own note — no task may change the ping mechanism, the URL construction, or the exit-code path. The only source edits allowed here are two docstring sentences and one preflight detail string."
      status: active
      verification: flagged-unverified
  artifacts:
    - docs/runbooks/telescope_runs_calendar.rst
    - deploy/cron/fomo.crontab.example
    - solsys_code/management/commands/check_unattended.py
    - solsys_code/tests/test_check_unattended.py
    - .planning/phases/36-unattended-operation/36-VERIFICATION.md
  key_links:
    - "the runbook's heartbeat paragraph ↔ the heartbeat service's actual alert rule (last ping + expected interval + grace) — this is precisely the link that was missing and that produced G-36-3"
    - "the runbook's heartbeat paragraph ↔ `deploy/cron/fomo.crontab.example:31` ↔ `check_heartbeat()`'s `[ok]` detail — the three operator-facing places that describe the backstop and must now agree with each other"
    - "`check_heartbeat()`'s detail string ↔ `solsys_code/tests/test_check_unattended.py` — the new assertion is what stops a later refactor silently dropping the reminder again"
    - "the corrected runbook ↔ `36-VERIFICATION.md`'s human-test script — the wrong instruction was written into the test script itself, so leaving it there would regenerate the gap on the next re-verification"
---

<objective>
Close G-36-3: the runbook's heartbeat guidance names only the check's grace time and never its
expected ping interval, so an operator following it literally gets a check that first alerts about
a day after the schedule stops. Correct the guidance everywhere it appears — the runbook (four
sites plus a new troubleshooting entry), the committed crontab template, two runner docstrings, the
preflight's `[ok]` heartbeat line, and the verification record that scripted the wrong instruction.

Purpose: SC 3's second layer ("a heartbeat that also fires when the scheduler never invoked the
job at all") is the one thing FOMO cannot verify from its own side — it depends entirely on the
operator configuring the external check correctly, which means it depends entirely on this
guidance. The mechanism is already proven working; the documentation is what is broken.

Output: corrected operator-facing guidance in every artifact that carries it, one new
troubleshooting entry, one new preflight reminder with a test that keeps it, and a verification
record that matches.

**No tracer task.** This is gap closure against a shipped phase: every layer already exists and is
proven end-to-end (UAT Test 3's own note records the mechanism working once the interval was set).
A thin vertical slice would add no information, so this plan decomposes into three
correct-and-propagate tasks per the `--no-tracer` exception.
</objective>

<execution_context>
@/home/tlister/git/fomo_devel/.claude/gsd-core/workflows/execute-plan.md
@/home/tlister/git/fomo_devel/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md

# The gap and its diagnosis — read both before editing a single line.
@.planning/phases/36-unattended-operation/36-UAT.md
@.planning/debug/heartbeat-runbook-period-gap.md

# The artifacts being corrected.
@docs/runbooks/telescope_runs_calendar.rst
@deploy/cron/fomo.crontab.example
@solsys_code/management/commands/check_unattended.py
@solsys_code/tests/test_check_unattended.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct the runbook's heartbeat guidance — both knobs, the arithmetic, and the missing troubleshooting entry</name>
  <files>docs/runbooks/telescope_runs_calendar.rst</files>
  <read_first>
    Read `docs/runbooks/telescope_runs_calendar.rst` lines 1538-1548 (the "Heartbeat." paragraph
    under "The two failure signals"), 1549-1581 ("When nothing has appeared"), 2023-2039 (the
    "Repeated 'lock held' lines" fix paragraph), and 2055-2081 (the last troubleshooting entry and
    the "See also" section that follows it). Line numbers were confirmed against the live file at
    plan time; re-confirm each anchor before editing, since earlier edits in this task shift the
    later ones.
  </read_first>
  <action>
    Four corrections plus one addition, all in this one file. The binding fact behind every one of
    them (debug session Resolution.root_cause): a healthchecks-compatible service alerts at
    `last ping + expected-ping-interval + grace`, and the expected interval defaults to 1 day on
    healthchecks.io, so guidance that names only the grace knob yields a check that first alerts
    about 24 h later while looking green and correctly configured.

    (a) Rewrite the "Heartbeat." paragraph (~1538-1547). Keep its first three sentences about
    `/start` and `/<exit-code>` as they are — they are correct. Replace the final sentence (the one
    at ~1543-1547 that tells the operator to point `FOMO_HEARTBEAT_URL` at a healthchecks-compatible
    endpoint and configure one check per schedule, recommending only a grace time of about 20
    minutes) with guidance that states all of the following:
      - The check needs TWO settings, not one. Name the concept first — the check's expected
        interval between pings — then give healthchecks.io's spelling for it (`Period`) in
        parentheses, because the runbook deliberately supports any healthchecks-compatible endpoint
        and other implementations may spell the same concept differently. Do the same for the grace
        time (`Grace`).
      - Set the expected interval to 15 minutes, matching the cron schedule.
      - State the drift-free alternative explicitly: a Cron-type check carrying the same
        ``*/15 * * * *`` expression the crontab line uses, which pegs lateness to the wall-clock
        slot instead of to the last ping.
      - Keep the grace time at about 20 minutes — a little over one interval, so one occasional
        slow tick does not page anyone. Say why it must not be shrunk to shorten the total: because
        the runner sends a `/start` ping, the grace time also bounds the maximum allowed gap between
        that `/start` and the completion ping, so a grace shorter than a slow tick's real runtime
        would page on a healthy-but-slow tick.
      - State the arithmetic plainly: the service alerts at last ping + expected interval + grace.
        With 15 and 20, a stopped schedule shows the check as late about 15 minutes after the missed
        tick and alerts about 35 minutes after the last successful ping.
      - Name the trap in as many words: leaving the expected interval at its default (1 day on
        healthchecks.io) means the first alert arrives about a day later, and the check looks green
        and correctly configured the whole time — the dead-man layer is off without any sign.
      Do not quote a real ping URL or UUID anywhere: `FOMO_HEARTBEAT_URL` is itself a secret
      (D-15, threat T-36-04) and the paragraph names settings, never values.

    (b) Fix triage item 3 under "When nothing has appeared" (~1563-1565). Its staleness bound is
    currently the grace time alone, which is the wrong number and actively misdirects triage.
    Restate it so the operator compares the last ping's age against expected interval + grace
    (about 35 minutes with the recommended values), keeping the rest of the item — that a missing
    or stale ping means the tick never ran or never finished, and to check the log file next.

    (c) Fix the backstop sentence in the "Repeated 'lock held' lines" fix paragraph (~2035-2038).
    It currently attributes the structural backstop to the grace time alone. Restate it as the
    heartbeat's alert window (expected interval + grace), keeping the existing cross-reference to
    "The two failure signals" and the point it is making — that a permanently contended lock
    eventually alerts there too.

    (d) Add a new troubleshooting entry AFTER the existing "The heartbeat alerts even though the log
    shows a healthy tick" entry (which ends ~2068) and BEFORE the "See also" section (~2070). Title
    it exactly: `The heartbeat never alerted although the schedule stopped` — underlined with the
    same `^` character and at least the title's length, matching every sibling entry in this
    section. Give it the same **Cause:** / **Fix:** two-paragraph form the siblings use:
      - **Cause:** the check's expected ping interval is still at its default (1 day on
        healthchecks.io) rather than the 15 minutes the schedule runs at. Because the service alerts
        at last ping + expected interval + grace, setting only the grace time leaves the first alert
        about a day out; the check stays green for that whole time, so the absence of an alert is
        not evidence that the tick ran.
      - **Fix:** set the expected interval to 15 minutes (or switch the check to Cron type with
        ``*/15 * * * *``), leave the grace at about 20 minutes, and cross-reference "The two failure
        signals" above. State the confirmation the operator should then see: with the crontab line
        disabled, the check goes late about 15 minutes after the missed slot and alerts about 35
        minutes after the last ping. Add the standing check an operator can do without waiting:
        compare the interval the check is configured with against the 15-minute cron schedule — they
        must match.

    Keep every edit inside this file's existing RST conventions: the same wrap width as the
    surrounding prose, double-backtick literals for settings and cron expressions, `--` for dashes
    as the rest of the page uses, and `**Bold lead-in.**` for paragraph openers where the siblings
    use them.
  </action>
  <verify>
    <automated>cd /home/tlister/git/fomo_devel && ! grep -nE "grace period a little above|than the configured grace period|The heartbeat's grace period" docs/runbooks/telescope_runs_calendar.rst && [ "$(grep -c Period docs/runbooks/telescope_runs_calendar.rst)" -ge 4 ] && grep -q "expected interval" docs/runbooks/telescope_runs_calendar.rst && grep -qE "35 min" docs/runbooks/telescope_runs_calendar.rst && grep -q "heartbeat never alerted" docs/runbooks/telescope_runs_calendar.rst && grep -q '\*/15 \* \* \* \*' docs/runbooks/telescope_runs_calendar.rst && pre-commit run sphinx-build --all-files > /tmp/fomo-sphinx-36-06.log 2>&1 && ! grep -Eiq "telescope_runs_calendar.*(WARNING|ERROR)" /tmp/fomo-sphinx-36-06.log</automated>
    <human-check>Re-run UAT Test 3 against a live healthchecks-compatible account, configuring a fresh check using ONLY what the corrected runbook paragraph names. Disable the crontab line and confirm the check goes late at roughly 15 minutes and alerts at roughly 35 minutes, with FOMO logging nothing and sending no email. This is the only proof that closes G-36-3's truth end-to-end; no automated gate can reach the external service.</human-check>
  </verify>
  <done>The heartbeat paragraph, triage item 3 and the lock-contention backstop sentence all describe the alert window as expected interval + grace; the 1-day default is named as the trap; the Cron-type alternative is offered; the grace stays at about 20 minutes with its `/start`-to-completion role explained; a new troubleshooting entry covers the never-alerted symptom; and Sphinx builds the page with no new warning.</done>
</task>

<task type="auto">
  <name>Task 2: Propagate the corrected guidance to the crontab template, the runner docstrings, and the preflight's heartbeat line</name>
  <files>deploy/cron/fomo.crontab.example, solsys_code/unattended.py, solsys_code/management/commands/check_unattended.py, solsys_code/tests/test_check_unattended.py, docs/runbooks/telescope_runs_calendar.rst</files>
  <read_first>
    Read `deploy/cron/fomo.crontab.example` lines 26-36 (the schedule comment block containing the
    backstop sentence at line 31); `solsys_code/unattended.py` lines 85-96 (the `TickResult`
    docstring) and 588-596 (the `run_tick()` Returns block) — in both, the backstop sentence names
    the Grace knob alone and wraps across two source lines; `solsys_code/management/commands/
    check_unattended.py` lines 235-244 (`check_heartbeat()`); `solsys_code/tests/
    test_check_unattended.py` lines 196-215 (`TestWarningChecks`, where the sibling heartbeat test
    lives) and the module-level `_FAKE_HEARTBEAT_URL` constant used by the hygiene tests around
    line 332. Re-confirm each anchor against the live files before editing.
  </read_first>
  <action>
    Four edits, all saying the same corrected thing Task 1 established.

    (a) `deploy/cron/fomo.crontab.example`, the backstop sentence at ~line 31. It currently
    attributes the structural backstop to the runner's heartbeat with one knob named. Rewrite it so
    it names both — the check's expected ping interval (15 minutes, matching this schedule;
    healthchecks.io calls it `Period`) and its grace time (about 20 minutes) — keeps the `(D-12)`
    reference, and points at the runbook's "How do I run everything unattended?" section for the
    full setup. Keep the comment inside this file's existing 95-ish column comment wrap and keep the
    file's absolute rule intact: names only, never a value, never a host path.

    (b) `solsys_code/unattended.py`, the two docstring sentences at ~90 and ~593 that name the
    heartbeat backstop using the Grace knob alone. These are consistency-only — they carry no
    operator instruction — so make the minimal edit that stops the repo re-seeding the one-knob
    phrasing: refer to the heartbeat's alert window (D-12), or simply to the heartbeat (D-12), as
    the structural backstop for a lock-contended skip. Change nothing else in either docstring and
    nothing at all in `ping_heartbeat()` — the mechanism is confirmed correct.

    (c) `check_heartbeat()` in `solsys_code/management/commands/check_unattended.py`. The `[ok]`
    branch currently reports only that the URL is set, which is exactly the reassurance an operator
    in this failure mode receives while the check at the other end is misconfigured. Extend that
    detail to: `FOMO_HEARTBEAT_URL: set -- confirm the check's own expected ping interval (Period)
    is 15 min, not its 1-day default`. Wrap the literal across lines to stay inside the 120-column
    limit ruff enforces. Leave the unset branch, `hard=False`, the check name and the docstring
    otherwise untouched: this stays a soft check, and it still prints set/unset only — never the URL
    value, which the existing credential-hygiene tests assert.

    (d) `solsys_code/tests/test_check_unattended.py`. Add one test to `TestWarningChecks` named
    `test_set_heartbeat_reminds_about_the_check_period`: with `override_settings` setting
    `FOMO_HEARTBEAT_URL` to the module's existing `_FAKE_HEARTBEAT_URL` constant (reuse it — do not
    introduce a second fake URL literal), run the command and assert stdout carries `[ok] heartbeat`,
    carries `Period`, and does NOT carry `_FAKE_HEARTBEAT_URL` itself. Give it a one-line comment
    naming G-36-3 as why the reminder exists, so a later refactor sees the reason before deleting
    it. Add nothing else; the file's existing per-file ruff ignores already cover missing test
    docstrings.

    (e) One sentence in `docs/runbooks/telescope_runs_calendar.rst`, in the setup step 4 paragraph
    (~1484) where the preflight's reported prerequisites are listed: extend the clause about
    `FOMO_HEARTBEAT_URL` being set so it also says the preflight reminds you the check at the other
    end still needs its own expected ping interval set — the preflight can only see this host's
    environment variable, never the remote check's configuration. This keeps the runbook and the
    command's actual output in agreement, which is the paired-docs rule for `check_unattended`
    (CLAUDE.md names this runbook section, not a notebook, as that command's paired doc).
  </action>
  <verify>
    <automated>cd /home/tlister/git/fomo_devel && python manage.py test solsys_code.tests.test_check_unattended.TestWarningChecks.test_set_heartbeat_reminds_about_the_check_period && python manage.py test solsys_code.tests.test_check_unattended && ! grep -nE "heartbeat grace period \(D-12\)" deploy/cron/fomo.crontab.example && grep -q "Period" deploy/cron/fomo.crontab.example && ! grep -nE "D-12's grace|heartbeat's grace" solsys_code/unattended.py && pre-commit run ruff --all-files && pre-commit run ruff-format --all-files && pre-commit run sphinx-build --all-files > /tmp/fomo-sphinx-36-06b.log 2>&1 && ! grep -Eiq "telescope_runs_calendar.*(WARNING|ERROR)" /tmp/fomo-sphinx-36-06b.log</automated>
  </verify>
  <done>The crontab template names both knobs; neither runner docstring describes the backstop with the grace knob alone; `check_unattended`'s `[ok]` heartbeat line reminds the operator to set the check's expected ping interval and still prints no URL value; a named test asserts that reminder and the whole `test_check_unattended` module stays green; the runbook's step-4 prose matches what the command now prints; ruff, ruff-format and the Sphinx build all pass.</done>
</task>

<task type="auto">
  <name>Task 3: Correct the verification record so it no longer scripts the instruction that produced the gap</name>
  <files>.planning/phases/36-unattended-operation/36-VERIFICATION.md</files>
  <read_first>
    Read `.planning/phases/36-unattended-operation/36-VERIFICATION.md` lines 49-55 (the frontmatter
    `human_verification` entry for test 3), line 147 (the plan 36-05 must-have row whose evidence
    cell cites the runbook's heartbeat recommendation), and lines 286-291 (the prose "3. The
    heartbeat's dead-man half" human test). Re-confirm the anchors before editing.
  </read_first>
  <action>
    The wrong instruction was not only shipped in the runbook — it was written into this phase's own
    human-test script, which is how UAT Test 3 came to tell the operator to set one knob. Leaving it
    would regenerate G-36-3 at the next re-verification.

    Correct all four sites to the arithmetic Task 1 established (alert at last ping + expected
    interval + grace; 15 + 20 gives late at about 15 minutes and an alert at about 35 minutes):
      - the frontmatter test-3 `test:` string and its `why_human:` string (~51 and ~53)
      - the plan 36-05 must-have evidence row at ~147, whose criterion text and evidence both
        describe the runbook as recommending the grace time alone — restate both halves so the row
        records what the runbook now actually says
      - the prose human test at ~288 and its `**Why human:**` line at ~290

    In each corrected test/expectation, the operator instruction must name both settings and the
    resulting timings, so the script reproduces the working configuration rather than the failing
    one.

    Add one short traceability note directly under the corrected prose human test (~291), in this
    file's existing prose style, recording that this test script and the row at ~147 were corrected
    after G-36-3 (see `36-UAT.md` and `.planning/debug/heartbeat-runbook-period-gap.md`), and that
    the original wording named only the grace time. Do not restate the original wording verbatim —
    point at the gap record for it. Change no verdict, no `✓ VERIFIED` marker, and no other row:
    this is a correction of a written instruction, not a re-verification.
  </action>
  <verify>
    <automated>cd /home/tlister/git/fomo_devel && ! grep -nE "grace period a little above|with a grace period of about 20 minutes|own grace-period timer|recommends ~20 min grace" .planning/phases/36-unattended-operation/36-VERIFICATION.md && grep -q "G-36-3" .planning/phases/36-unattended-operation/36-VERIFICATION.md && grep -qE "35 min" .planning/phases/36-unattended-operation/36-VERIFICATION.md && [ "$(grep -c Period .planning/phases/36-unattended-operation/36-VERIFICATION.md)" -ge 3 ]</automated>
  </verify>
  <done>Test 3's frontmatter entry, its prose script, its `why_human` line and the plan 36-05 evidence row all state the corrected two-knob configuration and the about-35-minute time-to-alert; a note records the correction against G-36-3; no verdict or `✓ VERIFIED` marker was changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed docs and deploy templates → the public repository | anything written here is published permanently; `FOMO_HEARTBEAT_URL` is itself a secret (its path component is the ping token), so a quoted example URL would leak a live dead-man's-switch endpoint |
| this host's environment → the external heartbeat service's configuration | the preflight can observe only that the local environment variable is set; the remote check's own settings are invisible to FOMO, which is why the guidance must be right — nothing downstream can catch it being wrong |
| operator reading the runbook → the production monitoring posture | the runbook is the sole input to how the dead-man layer gets configured; an omission here disables SC 3's second layer with no visible symptom |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36-20 | Information Disclosure | the corrected runbook paragraph, the crontab comment, and `check_heartbeat()`'s extended `[ok]` detail | high | mitigate | Every new sentence names a setting or a knob and never a value; the preflight detail keeps printing set/unset only, and Task 2 re-runs the existing `test_line_carries_no_setting_value` and `test_output_never_contains_a_seeded_value` gates plus a new assertion that the seeded fake URL is absent from stdout |
| T-36-21 | Repudiation | a dead-man layer that looks configured and reports green while never able to fire | high | mitigate | The corrected guidance states the values to set rather than relying on the vendor default (the enabling condition the debug session's AND-gate identified), states the alert arithmetic so an operator can check their own configuration, and adds a troubleshooting entry for the silent never-alerted symptom that previously had no self-serve path |
| T-36-22 | Denial of Service | a grace time shrunk below a slow tick's real runtime to force a shorter total time-to-alert | medium | mitigate | The guidance explains that the grace also bounds the allowed `/start`-to-completion gap, and the plan's prohibitions forbid reducing the ~20-minute recommendation |
| T-36-23 | Tampering | a later refactor silently dropping the preflight's interval reminder | medium | mitigate | `test_set_heartbeat_reminds_about_the_check_period` asserts the reminder, with an inline comment naming G-36-3 as the reason it exists |
| T-36-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs no package — 36-RESEARCH.md's Package Legitimacy Audit records zero new dependencies for this phase, and nothing here adds one |
</threat_model>

<verification>
- `pre-commit run sphinx-build --all-files` passes with no new warning naming `telescope_runs_calendar`
- `python manage.py test solsys_code.tests.test_check_unattended` passes, including the new reminder test
- `pre-commit run ruff --all-files` and `pre-commit run ruff-format --all-files` pass
- No committed file quotes a heartbeat URL, a ping token, or any other `FOMO_*` value
- The runbook, `deploy/cron/fomo.crontab.example` and `check_unattended`'s printed heartbeat line all describe the same two-knob configuration
- Human (end-of-phase, re-runs UAT Test 3): a check configured from the corrected runbook alone goes late at ~15 minutes and alerts at ~35 minutes after the crontab line is disabled, with FOMO logging nothing and mailing nothing
</verification>

<success_criteria>
An operator who reads only the corrected runbook section configures a heartbeat check that actually
fires within tens of minutes of the schedule stopping — SC 3's second layer restored in practice,
not just in mechanism. Every other artifact that describes the backstop agrees with it, the
preflight nudges the one thing it cannot itself verify, a test keeps that nudge, and the phase's own
verification record no longer scripts the instruction that caused G-36-3.
</success_criteria>

<output>
Create `.planning/phases/36-unattended-operation/36-06-SUMMARY.md` when done
</output>
