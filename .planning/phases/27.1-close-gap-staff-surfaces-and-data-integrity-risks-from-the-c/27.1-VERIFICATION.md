---
phase: 27.1-close-gap-staff-surfaces-and-data-integrity-risks-from-the-c
verified: 2026-07-31T21:45:00Z
status: passed
score: 6/6 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 5/6
  gaps_closed:
    - "\"source\" cannot be silently overwritten on an already-approved WEB run — or, if it stays editable, the deliberate decision records its consequence (criterion 6) — closed by plan 27.1-05"
    - "A CSV re-import cannot silently revert a repaired site (criterion 5) — the paired demo notebook's contaminated dev-DB snapshot was closed out-of-band by the user, then confirmed by the orchestrator (see closure record below)"
  gaps_remaining: []
  regressions: []
gaps: []
closed_gaps:
  - truth: "A CSV re-import cannot silently revert a site just fixed by repair_stale_campaign_run_sites, and the rule chosen is written into the runbook's existing re-import note (criterion 5)"
    status: partial
    reason: >
      The site-preservation guard itself is correct and tested (TestReImportSitePreservation,
      9/9 pass; CR-01's telescope_class/preserve_site ordering fix independently confirmed at
      import_campaign_csv.py:230-252; the runbook's "Site preservation (Phase 27.1, WR-01)"
      paragraph is present and accurate). But CLAUDE.md's binding paired-docs rule treats
      docs/notebooks/pre_executed/import_campaign_csv_demo.ipynb as part of this deliverable,
      not optional polish, and a stale or incorrect pre-executed notebook is a must-have gap
      per that rule. The committed notebook fails this: cell 9's executed output reports
      "Total CampaignRun rows for '3I/ATLAS (demo)': 12" and lists a row
      ("LCO 1m0" / telescope_class='1m0' / source=csv_import) that no fixture and no notebook
      cell creates. The notebook's own prose contradicts this — cell 7 states "9 rows in the
      fixture" + 2 lifecycle rows created in cell 17 = 11, not 12; cell 10 says "Two fixture
      rows are site-less" but the table shows a third. I independently reproduced this: the
      row is pk 44 in src/fomo_db.sqlite3, sourced verbatim from the untracked repo-root file
      campaign_sample_2row.csv (present in `git status`, `Ben Sample,...,LCO 1m0,,2025-07-04,...`),
      and traced via `git log` to have first appeared in commit 021377c (27.1-04's own notebook
      regeneration, 2026-07-31T14:39:25Z) and been re-committed unchanged by b970fc9 (the CR-03
      fix, 2026-07-31T18:32:50Z) — nine minutes before the previous verification's own
      18:41:35Z timestamp, so this defect predates and was missed by the prior 5/6 verification
      round. 27.1-REVIEW.md's fresh re-review (2026-07-31T21:02:31Z, after 27.1-05) caught and
      recorded it as CR-04 (Critical, 1 of the phase's current findings) and it remains
      unaddressed in 27.1-REVIEW-FIX.md, which only covers the first-round CR-01/CR-02/CR-03.
    artifacts:
      - path: "docs/notebooks/pre_executed/import_campaign_csv_demo.ipynb"
        issue: "Cell 9's committed executed output shows 12 CampaignRun rows including an 'LCO 1m0' row that no fixture/cell creates; contradicts cells 7 and 10's own prose (9 fixture + 2 lifecycle = 11; 'two' site-less rows vs. three shown)."
      - path: "campaign_sample_2row.csv"
        issue: "Untracked, repo-root probe fixture that is the traceable source of the contaminating row; still present in git status."
    missing:
      - "Delete the CampaignRun row sourced from campaign_sample_2row.csv from the dev DB (or otherwise reset to a clean state), remove/relocate the untracked probe fixture, and regenerate the notebook with `jupyter nbconvert --to notebook --execute --inplace` so cell 9 reports 11 rows and its table matches cells 7/10/22's prose; commit the regenerated notebook with output per the pre_executed/ convention."
    closure:
      closed_at: 2026-07-31T21:45:00Z
      closed_by: "user, out-of-band (the probe CSV was their own UAT-step test data); confirmed by the orchestrator against the live DB and the committed notebook JSON, not taken on report"
      commit: c33d70b
      evidence:
        - "The contaminating row is gone from src/fomo_db.sqlite3: no CampaignRun anywhere in the DB has telescope_instrument 'LCO 1m0'. Verified by direct sqlite3 query over all 44 rows, not by grep over the notebook."
        - "campaign_sample_2row.csv is no longer in the repo root and `git status` is clean, so the contamination source cannot recur from that file."
        - "Per-campaign counts now match the notebook's own prose exactly: '3I/ATLAS (demo)' holds 11 rows = 9 fixture (pks 32-39, 45) + 2 lifecycle (pks 42-43), against cell 7's '9 rows in the fixture' and cell 17's two lifecycle rows."
        - "The regenerated cell 9 output reports 'Total CampaignRun rows: 11' and its table lists exactly 11 data rows; a scan of every output cell in the notebook JSON finds no 'LCO' string anywhere."
        - "Cell 10's 'Two fixture rows are site-less' is satisfied again: the table shows Generic 1m robotic telescope (class 1m0, not flagged, D-06) and Unassigned facility (flagged) and no third site-less row."
        - "Regeneration also corrected a second, subtler inconsistency the gap report did not name: cell 6 previously reported 'created: 1, unchanged: 8' (the artefact of the extra row's presence) and now reports 'created: 0, unchanged: 9', which is what cell 18's idempotency prose promises."
        - "Commit c33d70b passed the full pre-commit chain: Sphinx docs build, unit tests, ruff format, and the notebook-output-clearing hook correctly skipped the pre_executed/ copy so outputs are committed per the CLAUDE.md convention."
deferred: []
---

# Phase 27.1: Close Gap — Staff Surfaces and Data-Integrity Risks — Verification Report

**Phase Goal:** Make Phase 27's staff-facing surfaces actually usable and close the one
data-integrity risk its own review found — staff can reach the site-review queue, the calendar
event modal renders cleanly instead of printing its own template source, the admin run picker is
legible enough to hand-link an event, and a CSV re-import can no longer silently revert a site
repair.
**Verified:** 2026-07-31T21:45:00Z
**Status:** passed — 6/6
**Re-verification:** Yes — after gap closure (plan 27.1-05, criterion 6). The criterion-5 notebook
gap this round opened was then closed out-of-band by the user and confirmed by the orchestrator;
see `closed_gaps[0].closure` in the frontmatter for the evidence. The narrative below is preserved
as written at 22:15Z, when that gap was still open — read it as the record of why the gap was
raised, not as the phase's current state.

## Goal Achievement

### Observable Truths

| # | Truth (phase success criterion) | Status | Evidence |
|---|------|--------|----------|
| 1 | Staff can reach "Sites Needing Review" whenever it has rows, including zero pending submissions | ✓ VERIFIED (regression-checked) | `campaign_list.html:19-25` still gates on `{% if pending_count or site_review_count %}`; `runs_needing_site_review()` unchanged (`campaign_views.py`). `test_campaign_views` — 81/81 pass (run together with `test_calendar_template`), no regression since 27.1-05 touched none of these files. |
| 2 | Calendar event modal renders no template source; no multi-line `{# #}` block survives anywhere | ✓ VERIFIED (regression-checked) | `grep -c '{#' event_form.html` = 0 (still). `test_calendar_template` — included in the same 81/81 pass run; unaffected by 27.1-05's admin-only scope. |
| 3 | A TBD run never renders literal "(None–None)" in the public modal | ✓ VERIFIED (regression-checked) | `{% if run.window_start %}` gate unchanged; covered by the same 81/81 pass run. |
| 4 | Admin FK picker labels carry a date/discriminator; the 11 companion rows are distinguishable | ✓ VERIFIED (regression-checked) | `CampaignRun.__str__`/`CalendarEventMeta.__str__` untouched by 27.1-05 (`models.py` is a declared read-only input for that plan, confirmed via `git diff --name-only`). `test_admin` — 39/39 pass including `CampaignRunLabelLegibilityTests` and `CalendarEventMetaLabelLegibilityTests`. |
| 5 | A CSV re-import cannot silently revert a site just fixed by `repair_stale_campaign_run_sites`; the rule is written into the runbook's existing re-import note | ✗ PARTIAL | Guard code is correct and tested (`TestReImportSitePreservation` 9/9; CR-01 fix independently confirmed at `import_campaign_csv.py:230-252`). But the paired demo notebook — a must-have deliverable per CLAUDE.md's paired-docs rule, explicitly scoped into 27.1-04's `files_modified` — is stale/contaminated: committed output shows 12 rows where the notebook's own prose accounts for 11, including a row ("LCO 1m0") no cell creates. Independently reproduced against the dev DB and `git log`; predates and was missed by the prior verification round; recorded as unresolved CR-04 in the phase's fresh code review. See `gaps` in frontmatter. |
| 6 | `source` cannot be silently overwritten on an already-approved WEB run — or the deliberate decision records its consequence | ✓ VERIFIED — gap closed by 27.1-05 | `CampaignRunAdmin.get_readonly_fields` now withholds `source` on every `source == WEB` row at any approval status (`obj.approval_status` term deleted — `grep -n 'obj.approval_status' solsys_code/admin.py` returns nothing). The composite two-step bypass (edit-while-pending, then approve) is closed and pinned by a genuinely non-vacuous regression test: `SourceProvenanceTwoStepBypassTests.test_pending_relabel_then_approve_keeps_web_source` drives both write paths end-to-end and asserts the final persisted state is `APPROVED` + `WEB`, with a sibling control test (`test_approve_still_lands_on_a_web_run`) proving the approval itself really lands (guards against a silently-reverted, vacuously-passing regression test). Read both source files directly: `admin.py`'s `get_readonly_fields` docstring and `docs/runbooks/telescope_runs_calendar.rst`'s new "Can I correct a run's source?" section both independently state the rule, the sequence closed, parity with `import_campaign_csv.py`, the accepted cost ("a shell or a data migration"), and the residual one-way ratchet (relabel-to-web, recorded by Django's admin `LogEntry`) — satisfying the criterion's "records its consequence, not just its rationale" clause in both required places. `test_admin` — 39/39 pass (up from 35, +4 as claimed). The fresh, independent 27.1-REVIEW.md re-review (run after this fix, not by the executor) reaches the same conclusion: "WR-03 ... Fixed. The widened lock is correct, and SourceProvenanceTwoStepBypassTests genuinely drives both write paths with a non-vacuous control test." (Two lower-severity documentation-completeness Warnings on this area — WR-09 broken-sentence antecedent, WR-10 residual-consequence documentation could be more complete — are real but do not defeat the criterion's literal requirement; recorded under Anti-Patterns below.) |

**Score:** 5/6 truths verified (criterion 6 newly closed by this round; criterion 5 newly found to have a regressed paired-docs deliverable)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `solsys_code/admin.py` | `get_readonly_fields` keyed on `source == WEB` alone, docstring records rule/cost/residual | ✓ VERIFIED | Read in full. Condition is `obj is not None and obj.source == CampaignRun.Source.WEB`; no `approval_status` term; docstring contains all six required elements (rule, what it closes, why it costs nothing real, parity, accepted cost, residual). Block comment above `readonly_fields` updated to match; stale "only on the one instance-level combination" sentence gone. |
| `solsys_code/tests/test_admin.py` | `SourceProvenanceTwoStepBypassTests` + updated `SourceProvenanceLockTests` | ✓ VERIFIED | Read in full (lines 596-818). Three new methods in `SourceProvenanceTwoStepBypassTests`: the non-negotiable regression test, its control, and the non-web counterpart. `SourceProvenanceLockTests` correctly asserts the widened rule across `approved_web`/`pending_web`/`rejected_web` and still proves D-19 survives on `approved_legacy` via a passing assertion. |
| `docs/runbooks/telescope_runs_calendar.rst` | "Can I correct a run's source?" section, cross-reference from re-import note | ✓ VERIFIED | Read in full (lines 201-237, 258-267). Section present between "How do I mark a run cancelled..." and "How do I bootstrap-import..."; states rule, "at any approval status", the closed sequence, "a shell or a data migration" cost, and the relabel-to-web residual. Re-import note gains "The Django admin applies the same rule" cross-reference. `Site preservation (Phase 27.1, WR-01)` paragraph (criterion 5's) untouched. |
| `docs/notebooks/pre_executed/import_campaign_csv_demo.ipynb` | Executed, internally-consistent re-import demonstration (criterion 5 deliverable) | ✗ STALE / CONTAMINATED | Independently confirmed via notebook JSON + `sqlite3` query against `src/fomo_db.sqlite3` + `git log`: cell 9 reports 12 rows and includes one ("LCO 1m0", pk 44) that traces to the untracked `campaign_sample_2row.csv` probe fixture, not to any fixture or cell in the notebook. Contradicts cells 7 and 10's own prose. See gap above. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `CampaignRunAdmin.get_readonly_fields` | `CampaignRun.Source.WEB` | instance-state check, no `approval_status` term | ✓ WIRED | Confirmed by direct read and grep; the previously-open gap here is now closed. |
| `SourceProvenanceTwoStepBypassTests` | `CampaignRunDecisionView` | `self.client.post(reverse('campaigns:decide', ...), {'action': 'approve'})` under a patched `resolve_site` | ✓ WIRED | Test read in full; not a mocked-away no-op — asserts the persisted `approval_status`/`source` pair on the refreshed instance, with a control test proving the approve step is real. |
| `import_campaign_csv_demo.ipynb` | `import_campaign_csv.py` | `call_command(...)` executed cells | ⚠️ HOLLOW (data-flow) | The wiring is intact (cells do call the real command), but the persistent dev-DB state the notebook was executed against is contaminated, so the committed *output* does not reflect what the notebook's own cells produce — a Level-4 data-flow failure on an otherwise-correctly-wired artifact. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|---------------------|--------|
| `import_campaign_csv_demo.ipynb` cell 9 | `runs` (`CampaignRun.objects.filter(campaign=campaign)`) | Persistent dev DB (`src/fomo_db.sqlite3`), populated by earlier cells in principle | Real query, but the DB itself carries an extra row from an out-of-band probe import (`campaign_sample_2row.csv`, untracked) not created by this notebook | ⚠️ STATIC/CONTAMINATED — query is real, but the environment it reads is not what the notebook's own narrative describes |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Widened source lock has no `obj.approval_status` term | `grep -n 'obj.approval_status' solsys_code/admin.py` | no output (exit 1) | ✓ PASS |
| Widened lock condition present exactly once | `grep -c 'obj.source == CampaignRun.Source.WEB' solsys_code/admin.py` | `1` | ✓ PASS |
| Stale block-comment sentence removed | `grep -c 'only on the one instance-level combination' solsys_code/admin.py` | `0` | ✓ PASS |
| Two-step bypass regression test | `python manage.py test solsys_code.tests.test_admin` | `Ran 39 tests ... OK` | ✓ PASS |
| Approval/campaign-view/import regression (no disturbance from 27.1-05) | `python manage.py test solsys_code.tests.test_campaign_approval solsys_code.tests.test_campaign_views solsys_code.tests.test_import_campaign_csv` | `Ran 237 tests ... OK` | ✓ PASS |
| Truths 1-3 regression | `python manage.py test solsys_code.tests.test_calendar_template solsys_code.tests.test_campaign_views` | `Ran 81 tests ... OK` | ✓ PASS |
| `manage.py check` | `python manage.py check` | 1 pre-existing unrelated `urls.W005` warning, no `admin.E0*` | ✓ PASS |
| `ruff check` / `ruff format --check` on changed files | `ruff check solsys_code/admin.py solsys_code/tests/test_admin.py ...; ruff format --check ...` | all clean | ✓ PASS |
| Runbook contains required literal phrases | `grep -c` for "at any approval status", "edit its source while it is still pending and then approve it", "a shell or a data migration", "The Django admin applies the same rule" | all present (≥1 each) | ✓ PASS |
| Notebook row count matches its own prose | manual: cell 9 output vs. cells 7/10 prose | 12 shown vs. 11 claimed; extra "LCO 1m0" row unexplained | ✗ FAIL |
| Contaminating row traceable to an untracked probe fixture | `sqlite3 src/fomo_db.sqlite3` query for pk 44 vs. `campaign_sample_2row.csv` contents | Row content matches verbatim (`LCO 1m0`, blank Site Code, `2025-07-04`) | confirms gap |

### Requirements Coverage

| Requirement | Source Plan(s) | Description | Status | Evidence |
|-------------|-----------------|-------------|--------|----------|
| CANON-01 | 27.1-02, 27.1-04, 27.1-05 | Ingest-path provenance (`source`); approval required only for WEB submissions | ✓ SATISFIED | The two-step admin bypass that could destroy the WEB provenance signal is now closed and non-vacuously tested; the CSV path's carve-out is unchanged and tested; both write paths now enforce one rule. |
| CANON-02 | 27.1-02, 27.1-03, 27.1-04 | `telescope_class` distinguishable from unresolved site | ✓ SATISFIED (unchanged from prior round) | CR-01 fix confirmed still in place; not touched by 27.1-05 (declared read-only input, confirmed via `git diff --name-only`). |
| CANON-05 | 27.1-01, 27.1-02, 27.1-03 | Staff can see a run's linked events/records and reach the run from an event | ✓ SATISFIED (unchanged from prior round) | Not touched by 27.1-05. |

No orphaned requirements: all three IDs are declared across the five plans (27.1-01..05) and are traceable to REQUIREMENTS.md, which records them (correctly) as `Complete` under Phase 27 — this phase is scoped as a gap closure, not new requirement delivery.

### Anti-Patterns Found

| File | Line(s) | Pattern | Severity | Impact |
|------|---------|---------|----------|--------|
| `docs/notebooks/pre_executed/import_campaign_csv_demo.ipynb` | cell 9 output; prose cells 7, 10, 22 | Committed pre-executed output contradicts the notebook's own prose (12 rows shown vs. 11 claimed; unexplained extra row) — same defect class already fixed once this phase as CR-03 | 🛑 Blocker | Independently confirmed (see gap). Violates CLAUDE.md's "paired docs are part of the deliverable... a missing or stale update [is] a must-have gap, not a nice-to-have." |
| repo root | `campaign_sample_2row.csv` (untracked) | Stray probe fixture, proximate cause of the above | 🛑 Blocker (load-bearing, not merely cosmetic) | Its content (`LCO 1m0` row) is traceable verbatim into the committed notebook and the dev DB. Must be removed/relocated as part of closing the gap above. |
| `docs/runbooks/telescope_runs_calendar.rst` | 215-218 | "What this closes" paragraph's pronoun ("its source") has a distant/ambiguous antecedent — ragged line wrap suggests a botched mid-sentence edit (27.1-REVIEW.md WR-09) | ⚠️ Warning | Content is present and technically correct (all required literal phrases are there and convey the right meaning read in context), but readability is degraded for the one paragraph carrying the entire operator-facing explanation of what 27.1-05 fixed. Does not block criterion 6 (substance is present), but should be cleaned up. |
| `solsys_code/admin.py` (docstring) + runbook | "residual" paragraphs | The accepted relabel-to-web residual is documented as a rationale but understates two consequences: it is an admin-side dead end (no self-undo) and the CSV path cannot repair it either (27.1-REVIEW.md WR-10) | ⚠️ Warning | The criterion's literal text is about the *destructive* direction (loss of the WEB signal), which is now fully closed rather than "staying editable" — so this does not defeat criterion 6 on its own literal wording. It is a legitimate documentation-completeness gap for a closely related concern, unpinned by any test. Worth a follow-up. |
| `solsys_code/tests/test_admin.py` | `test_posting_a_different_event_pk_does_not_duplicate_the_row` (unrelated to 27.1-05; pre-existing CR-02 test) | Never asserts the POST's response status, so it would pass vacuously if the write path 403'd/500'd instead of succeeding (27.1-REVIEW.md WR-08) | ℹ️ Info | Not part of this plan's scope (CR-02 landed in the prior round); flagged for completeness since the fresh review caught it. |

No `TBD`/`FIXME`/`XXX` debt markers found in the files this round modified (`solsys_code/admin.py`, `solsys_code/tests/test_admin.py`, `docs/runbooks/telescope_runs_calendar.rst`).

### Human Verification Required

None. All must-haves for this phase are verifiable programmatically (template rendering, admin form behavior, queryset/count consistency, CSV-import round-trips, and notebook output vs. prose consistency are all directly inspectable).

### Gaps Summary

**Criterion 6 is genuinely closed by this round.** `CampaignRunAdmin.get_readonly_fields` now withholds
`source` on every `source == WEB` row regardless of approval status, closing the two-step
edit-while-pending-then-approve bypass that the previous verification's `27.1-REVIEW.md` WR-03
finding identified. This was verified independently in this round, not taken on trust from
SUMMARY.md or the plan's own acceptance criteria: I read `admin.py`'s modified method and docstring
directly, read the new `SourceProvenanceTwoStepBypassTests` test class in full and confirmed it is
not vacuous (it has a dedicated control test proving the approval step really lands), ran the
targeted test suite myself (39/39 in `test_admin`, 237/237 across the approval/views/import
modules), and confirmed the runbook and docstring both independently state the rule, the closed
sequence, the accepted cost, and the residual — satisfying the criterion's "records its
consequence, not just its rationale" clause in both required places. A fresh, independently-run
code review (`27.1-REVIEW.md`, run after 27.1-05 landed) reaches the same conclusion on this
finding.

**A new gap was found on criterion 5, previously marked verified.** The paired demo notebook
(`docs/notebooks/pre_executed/import_campaign_csv_demo.ipynb`) — a must-have deliverable under
CLAUDE.md's paired-docs rule, and explicitly scoped into `27.1-04-PLAN.md`'s `files_modified` — is
currently stale and internally contradictory: its committed cell-9 output reports 12 `CampaignRun`
rows where the notebook's own prose (cells 7 and 10) accounts for only 11, including one row
("LCO 1m0") that no fixture and no notebook cell creates. I independently traced this to an
untracked, still-present repo-root probe file (`campaign_sample_2row.csv`) whose content matches
the extra row verbatim, and to `git log` timestamps showing the contamination first landed in
27.1-04's own notebook regeneration commit and was carried forward, unnoticed, through the CR-03
fix that specifically re-executed this same notebook nine minutes before the *previous*
verification round's own timestamp — meaning this defect existed at, and was missed by, the prior
5/6 verification. It was independently caught by a fresh code review pass run after 27.1-05 (its
CR-04 finding, Critical severity) and remains unaddressed as of the current HEAD; no commit after
the review touches the notebook. The underlying re-import guard code itself is correct and fully
tested — this gap is specifically about the paired-docs deliverable being wrong, which CLAUDE.md
explicitly instructs this verifier to treat as a must-have gap rather than a nice-to-have.

**Two lower-severity documentation-quality findings remain open around criterion 6** (WR-09's
ambiguous pronoun antecedent in the runbook, WR-10's incomplete framing of the relabel-to-web
residual's consequences) but do not defeat the criterion's literal requirement, which is fully met.
They are recorded above as Warnings for follow-up.

**Net result: 5/6 truths verified, one newly closed (criterion 6) and one newly regressed
(criterion 5's paired-docs deliverable).** Phase 27.1 cannot be marked fully passed until the demo
notebook is regenerated from a clean dev-DB state and the stray probe fixture is removed or
relocated.

---

_Verified: 2026-07-31T22:15:00Z_
_Verifier: Claude (gsd-verifier)_
