---
phase: 27.1-close-gap-staff-surfaces-and-data-integrity-risks-from-the-c
plan: 05
subsystem: admin
tags: [django-admin, provenance, campaign-run, source-field, gap-closure]

# Dependency graph
requires:
  - phase: 27.1-02
    provides: "the narrower (APPROVED, WEB)-only source provenance lock this plan widens"
  - phase: 27.1-04
    provides: "import_campaign_csv.py's unconditional source==WEB carve-out, the rule this plan brings the admin to parity with"
provides:
  - "CampaignRunAdmin.get_readonly_fields withholds source on every source==WEB CampaignRun, at any approval status"
  - "SourceProvenanceTwoStepBypassTests pinning the edit-while-pending-then-approve sequence end to end"
  - "Operator runbook section documenting the rule, its cost, and its residual"
affects: [phase-28-attribution-queue, phase-29-reconciler]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Provenance lock keyed on instance state alone (source), not on a state combination, to close a composite-boundary bypass across two independently-legitimate write paths"

key-files:
  created: []
  modified:
    - solsys_code/admin.py
    - solsys_code/tests/test_admin.py
    - docs/runbooks/telescope_runs_calendar.rst

key-decisions:
  - "Chose Option A (close the hole) over Option B (record the consequence): source=web is written by exactly one code path (campaign_views.py), so D-19's correction use case never applied to a web row, and import_campaign_csv.py already enforces this exact rule with no approval-status condition"
  - "The lock remains a one-way ratchet by design: a non-web row can still be relabelled to web (accepted, T-27.1-05-03), since freezing that direction too would re-open D-19 for legacy/csv_import/queue sources"

patterns-established:
  - "When a provenance field's write-legitimacy depends on the current instance state, key the lock on that state alone rather than on a state+status combination — a combination lock is bypassable by any legal sequence that changes the two terms independently"

requirements-completed: [CANON-01]

# Metrics
duration: 6min
completed: 2026-07-31
---

# Phase 27.1 Plan 05: Widen the source provenance lock to every WEB run Summary

**Widened `CampaignRunAdmin.get_readonly_fields`'s source-provenance lock from `(APPROVED, WEB)` to every `source == WEB` row at any approval status, closing the two-step edit-then-approve bypass WR-03 identified and completing Phase 27.1 success criterion 6.**

## Performance

- **Duration:** ~6 min (commit-to-commit)
- **Started:** 2026-07-31T20:42:00Z (approx)
- **Completed:** 2026-07-31T20:47:13Z
- **Tasks:** 3
- **Files modified:** 3

## Accomplishments
- Closed the composite-boundary bypass: editing `source` while `PENDING_REVIEW` then approving via `CampaignRunDecisionView` can no longer land a run on `APPROVED` + non-web
- `SourceProvenanceLockTests` now asserts the widened rule across all three WEB fixture rows (approved/pending/rejected) plus the add form, and still proves D-19 survives on a non-web row via a passing assertion (not prose)
- New `SourceProvenanceTwoStepBypassTests` drives the full edit-then-approve sequence across both write paths with a control test guarding against a vacuous pass
- `admin.py`'s docstring and the operator runbook both record the rule, the sequence closed, parity with `import_campaign_csv.py`, the accepted cost (shell/migration needed to correct a web row's source), and the one-way relabel-to-web residual left open deliberately

## Task Commits

Each task was committed atomically:

1. **Task 1: Widen the source provenance lock and record its consequence in the docstring** - `c426cc3` (fix)
2. **Task 2: Re-point SourceProvenanceLockTests and add the two-step bypass regression test** - `4a1a718` (test)
3. **Task 3: Write the rule, cost and residual into the operator runbook** - `3fff5b3` (docs)

_No plan-metadata commit — this plan runs in worktree mode; the orchestrator makes the final metadata commit after merge._

## Files Created/Modified
- `solsys_code/admin.py` - `get_readonly_fields` condition widened to `obj is not None and obj.source == CampaignRun.Source.WEB` (dropped the `approval_status == APPROVED` term); docstring rewritten to record the rule, the closed sequence, parity with the CSV importer, the accepted cost, and the residual; block comment above `readonly_fields` updated to match
- `solsys_code/tests/test_admin.py` - four `SourceProvenanceLockTests` methods renamed/rewritten for the widened rule; new `test_submitted_source_value_does_bind_on_non_web_row` proves D-19 survives; new `SourceProvenanceTwoStepBypassTests` class (3 methods) pins the cross-path bypass sequence, its control, and the non-web counterpart; module docstring and imports updated (`from unittest.mock import patch`)
- `docs/runbooks/telescope_runs_calendar.rst` - new "Can I correct a run's source?" section between the cancel/weather section and the CSV-import section; re-import gotcha note gains a one-sentence cross-reference beginning "The Django admin applies the same rule"

## Decisions Made
- Followed the plan's Option A rationale verbatim (see PLAN.md objective section "Why Option A"): the four pieces of codebase evidence (WEB is never a guess, the CSV path already enforces this rule, the runbook already states it in Option A's form, Option B cannot close the criterion) all point the same direction, so no re-litigation was needed during execution.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Regenerated the gitignored `src/fomo/_version.py` in this worktree**
- **Found during:** Task 1 verification (`python manage.py check`)
- **Issue:** `manage.py` failed with `ModuleNotFoundError: No module named 'src.fomo._version'`. This file is generated by `setuptools_scm` at build/install time and is gitignored (`.gitignore:29`); the shared venv's editable install points at the main repo checkout, which already has it, but this worktree checkout never had it generated locally, so `import src.fomo` resolved against the worktree's own `src/` (which lacks the file) rather than the main repo's.
- **Fix:** Copied the main repo's `src/fomo/_version.py` verbatim into this worktree's `src/fomo/_version.py`. Confirmed with `git check-ignore -v` that the file is gitignored and cannot be accidentally committed.
- **Files modified:** `src/fomo/_version.py` (gitignored, not tracked, not part of any commit)
- **Verification:** `python manage.py check` and `python manage.py test ...` both run cleanly afterward
- **Committed in:** N/A — the file is gitignored by design and was never staged

---

**Total deviations:** 1 auto-fixed (1 blocking, environment-only, zero product-code impact)
**Impact on plan:** No scope creep; this was purely a local git-worktree environment quirk (a generated, gitignored version file missing from a fresh worktree checkout) unrelated to the plan's actual code changes.

## Issues Encountered

**Two acceptance-criteria grep checks needed line-wrap fixes, not logic changes.** The plan's acceptance criteria for Task 1 (`grep -c 'obj.source == CampaignRun.Source.WEB'` returns `1`, `grep -c 'shell or a data migration'` at least 1) and for Task 3 (`grep -c 'edit its source while it is still pending and then approve it'` returns `1`) require the literal substrings to appear on a single physical line. Initial prose in the docstring and the runbook wrapped these phrases across two lines (single-quoting the Python expression once in prose caused a duplicate match; RST paragraph wrapping split two of the required phrases). Reworded the surrounding prose (kept the same meaning, same required phrases) so each required substring lands on one line. No functional change.

**`grep -c 'obj is not None'` on `solsys_code/admin.py` returns 3, not the plan's stated 2.** The acceptance criterion expected exactly one `obj is not None` occurrence per `get_readonly_fields` method (2 total, code-only). `CalendarEventMetaAdmin.get_readonly_fields`'s pre-existing docstring (written by an earlier plan, out of scope here — "must not touch `CalendarEventMetaAdmin`") already contains a third, prose-only occurrence of the same phrase (`` ``obj is not None`` `` in its own docstring), confirmed present in `git show HEAD~3:solsys_code/admin.py` before this plan touched anything. The substantive requirement — one `obj is not None` code gate in each of the two `get_readonly_fields` methods, add form unaffected in both — is satisfied; this is a grep-heuristic miscount in the plan's acceptance criteria, not a functional gap. Not fixed (would require touching `CalendarEventMetaAdmin`'s docstring, explicitly out of scope for this plan).

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

Phase 27.1 success criterion 6 is fully closed. All 6 criteria of Phase 27.1 are now verified:
- `python manage.py check` clean (1 pre-existing unrelated `urls.W005` warning, not a regression)
- `python manage.py test solsys_code.tests.test_admin` — 39 tests, up from 35 (+4)
- `python manage.py test solsys_code.tests.test_campaign_approval solsys_code.tests.test_campaign_views solsys_code.tests.test_import_campaign_csv` — 237 tests, all pass
- `ruff check .` — 1 pre-existing `D103` in an unrelated notebook file, not a regression (matches plan's stated expectation)
- `ruff format --check .` — 3 pre-existing reformat candidates in unrelated files (already logged in `deferred-items.md`), not a regression
- Sphinx build succeeds, no warning naming `telescope_runs_calendar`
- `git diff --name-only` against the plan's base commit shows exactly the three declared files

No blockers for the orchestrator's merge or for Phase 28/29 planning, which read `source`/`approval_status` as provenance input and now see one consistent rule enforced across both write paths.

---
*Phase: 27.1-close-gap-staff-surfaces-and-data-integrity-risks-from-the-c*
*Completed: 2026-07-31*

## Self-Check: PASSED

- FOUND: solsys_code/admin.py
- FOUND: solsys_code/tests/test_admin.py
- FOUND: docs/runbooks/telescope_runs_calendar.rst
- FOUND: SUMMARY.md
- FOUND commit: c426cc3
- FOUND commit: 4a1a718
- FOUND commit: 3fff5b3
- FOUND commit: 7e56c46
