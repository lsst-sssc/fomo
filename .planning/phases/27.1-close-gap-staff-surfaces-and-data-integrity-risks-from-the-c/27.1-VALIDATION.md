---
phase: "27.1"
slug: "close-gap-staff-surfaces-and-data-integrity-risks-from-the-c"
status: validated
nyquist_compliant: true
wave_0_complete: true
created: "2026-08-31"
---

# Phase 27.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

**Reconstructed from artifacts (State B — no VALIDATION.md existed for this phase).** Built
from `27.1-VERIFICATION.md` (2026-07-31T21:45:00Z re-verification, 6/6 truths, the phase's
own gap-closure evidence chain) and the five plan SUMMARYs, per Phase 30 plan 30-04 (D-08).

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Django test runner (`django.test.TestCase`) — this phase touches `solsys_code/` app code exclusively |
| **Config file** | none dedicated — `pyproject.toml` `testpaths` deliberately excludes `solsys_code/` (CLAUDE.md testing split) |
| **Quick run command** | `python manage.py test solsys_code.tests.test_admin` |
| **Full suite command** | `python manage.py test solsys_code.tests.test_admin solsys_code.tests.test_calendar_template solsys_code.tests.test_campaign_views solsys_code.tests.test_campaign_approval solsys_code.tests.test_import_campaign_csv` |
| **Estimated runtime** | ~10s quick / ~1 min full targeted sweep |

**Invocation caveat:** use `python manage.py`, never `./manage.py`.

---

## Sampling Rate

- **After every task commit:** Run `python manage.py test solsys_code.tests.test_admin`
- **After every plan wave:** Run the full targeted sweep above
- **Before `/gsd-verify-work`:** Full sweep green, plus `ruff check .` / `ruff format --check .` clean on changed files
- **Max feedback latency:** ~10 seconds for the quick command

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 27.1-01 | 01 | 1 | CANON-05 | — | Staff can reach "Sites Needing Review" whenever it has rows | unit/DB | `python manage.py test solsys_code.tests.test_campaign_views` | ✅ exists | ✅ green |
| 27.1-02 | 02 | 1 | CANON-01, CANON-02, CANON-05 | — | Calendar event modal renders no template source; TBD run never renders "(None–None)" | unit/DB | `python manage.py test solsys_code.tests.test_calendar_template` | ✅ exists | ✅ green |
| 27.1-03 | 03 | 2 | CANON-02, CANON-05 | — | Admin FK picker labels carry a date/discriminator | unit/DB | `python manage.py test solsys_code.tests.test_admin` (`CampaignRunLabelLegibilityTests`, `CalendarEventMetaLabelLegibilityTests`) | ✅ exists | ✅ green |
| 27.1-04 | 04 | 2 | CANON-01, CANON-02 | — | CSV re-import cannot silently revert a repaired site | unit/DB | `python manage.py test solsys_code.tests.test_import_campaign_csv.TestReImportSitePreservation` | ✅ exists (9 tests) | ✅ green |
| 27.1-05 | 05 | 3 | CANON-01 | — | `source` cannot be silently overwritten on an already-approved WEB run via the two-step edit-then-approve bypass | unit/DB | `python manage.py test solsys_code.tests.test_admin.SourceProvenanceLockTests solsys_code.tests.test_admin.SourceProvenanceTwoStepBypassTests` | ✅ exists (39 tests total in `test_admin`) | ✅ green |

*Status: ✅ green · ❌ red · ⚠️ flaky*

**Source:** `27.1-VERIFICATION.md`'s Behavioral Spot-Checks table — `test_admin` (39/39),
`test_campaign_approval`+`test_campaign_views`+`test_import_campaign_csv` (237/237),
`test_calendar_template`+`test_campaign_views` (81/81), all independently re-run by the
verifier, not taken from SUMMARY claims.

---

## Wave 0 Requirements

**None — existing test infrastructure (Django `TestCase`, `solsys_code/tests/`) covered all
five plans' requirements from the start; no new framework or fixture scaffolding was needed.**

---

## Manual-Only Verifications

**None recorded.** `27.1-VERIFICATION.md`'s own "Human Verification Required" section states:
"None. All must-haves for this phase are verifiable programmatically (template rendering,
admin form behavior, queryset/count consistency, CSV-import round-trips, and notebook output
vs. prose consistency are all directly inspectable)."

---

## Gap Closure Record (criterion 5 — the paired demo notebook)

The 2026-07-31T21:45:00Z re-verification found `docs/notebooks/pre_executed/import_campaign_csv_demo.ipynb`
stale/contaminated (a probe-fixture row leaking into committed output). This was closed
out-of-band by the user (removing the untracked `campaign_sample_2row.csv` probe fixture and
the contaminating dev-DB row) and confirmed by the orchestrator against the live DB and the
committed notebook JSON — commit `c33d70b`. Full evidence chain recorded in
`27.1-VERIFICATION.md`'s frontmatter `closed_gaps[0]`. Both `gaps: []` and `closed_gaps[0].closure`
in that file confirm final status is fully closed, not merely claimed.

## Post-Phase 27.1 Findings, Verified Closed by Phase 30 Discuss-Phase Scouting

`27.1-REVIEW.md`'s two remaining WARNING-level findings (WR-09 ambiguous pronoun antecedent,
WR-10 residual-consequence documentation) were independently re-verified as already closed
during Phase 30's discuss-phase scouting (`30-CONTEXT.md` §Phase Boundary), prior to this
reconciliation:

- **WR-09:** `docs/runbooks/telescope_runs_calendar.rst`'s "Can I correct a run's source?"
  section reads cleanly with no ambiguous antecedent (re-confirmed present at the current
  line range during this reconciliation).
- **WR-10:** covered by the `.. warning::` block following that section, the matching
  `get_readonly_fields` docstring in `solsys_code/admin.py`, and the pinning test
  `test_relabel_to_web_locks_the_row_and_cannot_be_undone` in `solsys_code/tests/test_admin.py`
  (re-confirmed present at the current line range during this reconciliation).

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references — *satisfied: no Wave 0 needed*
- [x] No watch-mode flags
- [x] Feedback latency < 15s for the quick command
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** validated (Phase 30 plan 30-04, D-08 reconciliation)

## Validation Audit 2026-08-31

Reconciled by Phase 30 plan 30-04 (D-08). This is the newly-created file for a phase that had
none — reconstructed from `27.1-VERIFICATION.md` (the phase's own independently-re-run 6/6
verification pass) rather than by running new tests, per State B of the validate-phase
workflow.

| Metric | Count |
|--------|-------|
| Gaps found | 0 (the one gap the phase itself found — the stale notebook — was already closed by commit `c33d70b` before this reconciliation ran) |
| Resolved | 0 (no new work needed) |
| Escalated | 0 |
