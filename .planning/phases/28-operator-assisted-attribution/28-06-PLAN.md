---
phase: 28-operator-assisted-attribution
plan: 06
type: execute
wave: 5
gap_closure: true
depends_on: ["28-04"]
files_modified:
  - solsys_code/campaign_views.py
  - solsys_code/campaign_attribution.py
  - solsys_code/tests/test_campaign_attribution_views.py
  - solsys_code/tests/test_campaign_attribution.py
autonomous: true
requirements: [ATTRIB-02, ATTRIB-04]

must_haves:
  truths:
    - "An undo naming a run that is not the pair's currently-confirmed run writes nothing at all — no dismissal row, no cleared link — so a stale or forged resubmit can never permanently remove a pair from the queue that was never confirmed"
    - "A genuine undo still clears the link and writes its dismissal inside one transaction, exactly as D-13 requires — the ordering fix does not weaken the undo's attributability"
    - "sole_high_candidate_pk is computed from the orphan's full uncapped candidate list, so it means the same thing under every band filter and matches the server-side gate that actually authorises a bulk confirm"
    - "Every docstring in the two touched write paths describes what the code actually does — no stated contract is left contradicting its implementation"
  artifacts:
    - path: "solsys_code/campaign_views.py"
      provides: "_undo_confirmation() gated so the dismissal is written only after the link-clearing write matched a row"
      contains: "def _undo_confirmation"
    - path: "solsys_code/campaign_attribution.py"
      provides: "Backlog builders computing sole_high_candidate_pk from the full candidate list"
      contains: "full_candidates"
    - path: "solsys_code/tests/test_campaign_attribution_views.py"
      provides: "TestUndoConfirmationOrdering regression class"
      contains: "class TestUndoConfirmationOrdering"
    - path: "solsys_code/tests/test_campaign_attribution.py"
      provides: "TestSoleHighCandidateUnderBandFilter regression class"
      contains: "class TestSoleHighCandidateUnderBandFilter"
  key_links:
    - from: "solsys_code/campaign_views.py _undo_confirmation"
      to: "CalendarEventDismissal / ObservationRecordDismissal"
      via: "dismissal write guarded by a non-zero changed_count from the link-clearing write"
      pattern: "if changed_count:"
    - from: "solsys_code/campaign_attribution.py event_attribution_backlog"
      to: "_sole_high_candidate_pk"
      via: "the unfiltered candidate list, matching campaign_views._is_sole_high_candidate()"
      pattern: "_sole_high_candidate_pk\\(full_candidates\\)"
---

<objective>
Close the two WARNING-level findings `28-VERIFICATION.md` recorded in its Anti-Patterns table and
its Gaps Summary's closing recommendation ("while in the area, considering WR-01's ordering fix"),
and resolve the INFO finding's stated deviation.

- **WR-01 (`campaign_views.py:1441-1485`).** `_undo_confirmation()` writes the "undo implies
  dismissal" row *before* running the conditional update/delete that actually clears the confirmed
  link. Unlike `_confirm()`/`_confirm_selected()` (which always call `is_offered_candidate()` first)
  and `_dismiss()`, this method performs no upstream check that `(kind, orphan_pk, run_pk)` names a
  real, currently-confirmed pair. A stale resubmit after a re-point, or a tampered POST — the view
  validates only that `orphan_pk`/`run_pk` are integers — produces `changed_count == 0` and the
  honest "This candidate no longer exists" message, but the dismissal row for that possibly-unrelated
  pair has already been committed. `candidates_for_event`/`candidates_for_record` exclude every
  dismissed run, so that pair is permanently removed from the queue despite never having been
  associated.

- **WR-02 (`campaign_attribution.py:621-638, 669, 703`).** `_sole_high_candidate_pk()`'s docstring
  requires its argument to be "the orphan's FULL candidate list (uncapped)", but both call sites pass
  the already-band-filtered list. Under `?band=medium`/`?band=low` it therefore always returns
  `None`. This is inert today — the checkbox renders only for a displayed candidate whose run pk
  equals the group's `sole_high_candidate_pk`, and no displayed candidate is High under those
  filters — but it silently breaks the documented invariant, and the display helper now disagrees
  with `campaign_views._is_sole_high_candidate()`, which correctly re-derives from the full list and
  is the thing that actually authorises a bulk-confirm write.

Purpose: WR-01 is a real data-integrity defect on the undo path (ATTRIB-04's reversibility becomes
irreversible for a pair the operator never confirmed). WR-02 is a latent trap for the next consumer
of `sole_high_candidate_pk` — the exact "trust the docstring, get the wrong answer" failure mode this
milestone keeps hitting.

Output: the reordered-and-gated `_undo_confirmation()`, the backlog builders computing
`sole_high_candidate_pk` from the unfiltered list, two regression test classes, and honest docstrings
throughout.

**Do not touch 28-01 through 28-04's PLAN.md/SUMMARY.md files, and do not re-verify the six truths
already marked VERIFIED.** This plan runs in parallel with `28-05-PLAN.md` (wave 5) and shares no
file with it.
</objective>

<execution_context>
@/home/tlister/git/fomo_devel/.claude/gsd-core/workflows/execute-plan.md
@/home/tlister/git/fomo_devel/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-operator-assisted-attribution/28-VERIFICATION.md
@.planning/phases/28-operator-assisted-attribution/28-REVIEW.md
@.planning/phases/28-operator-assisted-attribution/28-CONTEXT.md
</context>

## Parallel-execution constraint (read before editing tests)

`28-05-PLAN.md` creates `solsys_code/tests/test_attribution_template.py`, which imports
`AttributionViewTestBase` from `solsys_code/tests/test_campaign_attribution_views.py`. **Do not
modify `AttributionViewTestBase`** (lines 39-128: `setUpTestData`, `_make_event`, `_make_record`,
`_message_strings`). Task 1 adds a new test class below the existing ones and builds any extra
fixture it needs inside that class's own `setUp`/`setUpTestData`. The two plans share no file, and
this keeps them independent at the source level too.

## Artifacts this phase produces (28-06 share)

| Kind | Name | Location | New/Modified |
|------|------|----------|--------------|
| View method | `AttributionDecisionView._undo_confirmation()` (reordered + gated) | `solsys_code/campaign_views.py` | Modified |
| Function | `event_attribution_backlog()` (full-list sole-high computation) | `solsys_code/campaign_attribution.py` | Modified |
| Function | `record_attribution_backlog()` (full-list sole-high computation) | `solsys_code/campaign_attribution.py` | Modified |
| Docstring | `candidates_for_event()` / `candidates_for_record()` drop-threshold contract (IN-01) | `solsys_code/campaign_attribution.py` | Modified |
| Test class | `TestUndoConfirmationOrdering` | `solsys_code/tests/test_campaign_attribution_views.py` | New |
| Test class | `TestSoleHighCandidateUnderBandFilter` | `solsys_code/tests/test_campaign_attribution.py` | New |

## Paired-docs scope (CLAUDE.md rule)

**No runbook change, and no notebook — recorded as a decision, not an omission.**
`docs/runbooks/telescope_runs_calendar.rst` states that "Undoing a *confirmed* attribution writes a
dismissal for that same pair as part of the same action". That remains exactly true after WR-01's
fix: a genuine undo still writes the dismissal in the same transaction. The only behavior that
changes is a case the runbook never documented and no operator can reach deliberately — an undo
naming a run that is not the pair's current one, which already reported "This candidate no longer
exists" and merely stopped writing a hidden side effect behind that message. WR-02 is inert at the
rendered surface by the reviewer's own analysis, and IN-01 is a docstring correction with no
behavior change. Neither touched module appears in CLAUDE.md's notebook pairing map, and this plan
adds no management command.

<tasks>

<task type="auto">
  <name>Task 1: WR-01 — write the undo's dismissal only after the link-clearing write actually matched a row</name>
  <files>solsys_code/campaign_views.py, solsys_code/tests/test_campaign_attribution_views.py</files>
  <read_first>
    - `solsys_code/campaign_views.py` `AttributionDecisionView._undo_confirmation()` (lines ~1441-1485, the whole method: its current docstring claiming the dismissal is written "FIRST", the outer `transaction.atomic()`, the nested `transaction.atomic()` savepoint around `get_or_create` with its `except IntegrityError: pass`, the event-side conditional `.update(run=None, confirmed_by=None, confirmed_at=None)` keyed on `run_id=run_pk`, the record-side `.delete()` returning a 2-tuple, and the `if changed_count:` success/error message split)
    - `solsys_code/campaign_views.py` `_dismiss()` (lines ~1405-1440, immediately above) — the savepoint discipline this method mirrors, and the `_UNDO_CONFIRMATION_DEFAULT_REASON` constant
    - `solsys_code/campaign_views.py` `_undo_dismissal()` (immediately below) — the `deleted_count`-then-message shape the reordered method should read like
    - `solsys_code/campaign_views.py` `AttributionDecisionView.post()` — confirm for yourself that `orphan_pk`/`run_pk` are validated only as integers, which is why an unchecked pair can reach this method at all
    - `solsys_code/campaign_views.py` `_is_sole_high_candidate()` (lines 1218-1249) and `campaign_attribution.is_offered_candidate()` — the "re-derive server-side, never trust the submitted pair" discipline every other write action in this view already follows
    - `solsys_code/tests/test_campaign_attribution_views.py` lines 176-274 (`TestConfirmUndo`: the confirm-then-undo POST sequence, `self._message_strings(response)` usage, and the `event_attribution_backlog()` assertions — the new class mirrors this shape)
    - `.planning/phases/28-operator-assisted-attribution/28-CONTEXT.md` D-13 (an undo writes a dismissal row, and why the trace must live outside the link row) — the constraint the reorder must preserve
  </read_first>
  <action>
**1a. Reorder and gate.** Restructure `_undo_confirmation()`'s body so that, inside the single outer
`with transaction.atomic():` block:
  1. The link-clearing write runs **first** — the event-side conditional
     `CalendarEventMeta.objects.filter(event_id=orphan_pk, run_id=run_pk).update(run=None, confirmed_by=None, confirmed_at=None)`
     or the record-side `CampaignRunObservation.objects.filter(observation_record_id=orphan_pk, run_id=run_pk).delete()`
     — capturing `changed_count` exactly as it does today.
  2. The dismissal `get_or_create` runs **only** when `changed_count` is truthy, still wrapped in its
     own nested `transaction.atomic()` savepoint with `except IntegrityError: pass` (a prior undo may
     already have dismissed the pair, and a caught `IntegrityError` must not poison the outer block).

Both writes must remain inside the one outer `transaction.atomic()` — D-13 requires the dismissal and
the link-clearing to succeed or fail together, and the reorder must not split them into two
transactions. Do not add an `is_offered_candidate()` call here: an undo operates on a pair that is
already confirmed and therefore no longer *offered*, so that check would reject every legitimate
undo. The `changed_count` gate is the correct upstream check for this method, because the same
conditional filter that clears the link is itself the proof the pair was really confirmed to that
run.

Leave the trailing `if changed_count:` success/error message block unchanged — its behavior is
already correct and its "This candidate no longer exists." branch now tells the truth about what was
written (nothing) instead of masking a committed side effect.

**1b. Fix the docstring, which currently states the opposite of the new code.** Rewrite
`_undo_confirmation()`'s docstring to say: the link-clearing write runs first and the D-13 dismissal
row is written only when it matched a row, both inside one transaction. Keep D-13's rationale (the
undo erases the very fields recording who confirmed it, so the trace has to live elsewhere; a
soft-undo flag would break Phase 27 D-01's invariant that a link row's existence means "confirmed";
the dismissal also stops the matcher immediately re-suggesting the pair just undone). Add the WR-01
reason for the ordering explicitly: `post()` validates only that `orphan_pk`/`run_pk` are integers,
so a stale resubmit after a re-point or a tampered POST can name a pair that was never confirmed to
that run — and because `candidates_for_event`/`candidates_for_record` exclude every dismissed run, an
ungated dismissal would permanently remove that pair from the queue. A stale docstring that
contradicts its implementation is exactly WR-02's defect class; do not leave one behind here.

**1c. Regression tests.** Add `TestUndoConfirmationOrdering(AttributionViewTestBase)` to
`solsys_code/tests/test_campaign_attribution_views.py`, placed immediately after `TestConfirmUndo`.
Do **not** modify `AttributionViewTestBase` (see the parallel-execution constraint above). In this
class's own `setUpTestData`, create a `stale_run`: a second `CampaignRun` in `cls.campaign` (the same
campaign, so the test isolates "this is not the pair's currently-confirmed run" from the
cross-campaign boundary gate, which is a different mechanism tested elsewhere). Log in
`staffcoordinator` in `setUp`, matching `TestConfirmUndo`.

Five tests:
  1. `test_undo_naming_a_run_that_never_confirmed_this_event_writes_no_dismissal` — confirm
     `event` ↔ `campaign_run` through the view, then POST
     `{'action': 'undo_confirmation', 'kind': 'event', 'orphan_pk': event.pk, 'run_pk': stale_run.pk}`.
     Assert: 302; **no** `CalendarEventDismissal` row exists for `(event, stale_run)`; the genuine
     link is untouched (`meta.run_id == campaign_run.pk`, `meta.confirmed_by == staff_user`,
     `confirmed_at` still not None); and `'This candidate no longer exists.'` is in
     `self._message_strings(response)`.
  2. `test_undo_naming_a_wrong_run_leaves_that_pair_still_offerable` — the behavioral consequence
     that makes WR-01 matter rather than a bookkeeping nit. After the failed undo in test 1's
     sequence, assert `stale_run.pk` is still present in
     `{c.run.pk for c in candidates_for_event(event)}`. (Import `candidates_for_event` the same way
     the module already imports its `campaign_attribution` helpers.) This assertion fails on the
     pre-fix code, because the bogus dismissal row excludes `stale_run` permanently.
  3. `test_undo_naming_a_run_that_never_confirmed_this_record_writes_no_dismissal` — the record-side
     equivalent of test 1: assert no `ObservationRecordDismissal` for `(record, stale_run)` and that
     the real `CampaignRunObservation` row still exists.
  4. `test_undo_of_a_never_confirmed_pair_writes_nothing_at_all` — no confirmation is made first;
     POST an undo for `(event, campaign_run)`. Assert `CalendarEventDismissal.objects.count() == 0`
     and the error message. This is the tampered-POST case with no prior state at all.
  5. `test_a_genuine_undo_still_clears_the_link_and_writes_its_dismissal` — the guard against the
     reorder over-gating and silently disabling D-13. Confirm, then undo with the correct `run_pk`;
     assert the link is cleared (`run_id`, `confirmed_by`, `confirmed_at` all None), the
     `CalendarEventDismissal` row exists with `dismissed_by == staff_user` and a non-null
     `dismissed_at`, and the success message reads
     `'Confirmation undone — back in the queue.'` (note the en dash, matching the existing tests).

**1d. Mutation check (record the result in the SUMMARY).** Temporarily restore the original ordering
(dismissal written before the link-clearing write, ungated), run
`python manage.py test solsys_code.tests.test_campaign_attribution_views.TestUndoConfirmationOrdering -v 2`,
and confirm tests 1-4 fail while test 5 still passes. Restore the fix and re-run to green. If any of
tests 1-4 stays green under the original ordering, that test is not binding to WR-01 and must be
rewritten.

Keep `ruff check .` and `ruff format --check .` clean.
  </action>
  <verify>
    <automated>python manage.py test solsys_code.tests.test_campaign_attribution_views -v 2</automated>
    <automated>python manage.py test solsys_code.tests.test_attribution_dismissals solsys_code.tests.test_campaign_attribution -v 1 && ruff check solsys_code/campaign_views.py solsys_code/tests/test_campaign_attribution_views.py && ruff format --check solsys_code/campaign_views.py solsys_code/tests/test_campaign_attribution_views.py</automated>
  </verify>
  <acceptance_criteria>
    - In `_undo_confirmation()`, the `CalendarEventMeta` `.update(...)` / `CampaignRunObservation` `.delete()` call appears **before** the `get_or_create` dismissal write in source order, and the dismissal write is inside an `if changed_count:` branch.
    - Both writes remain inside one outer `transaction.atomic()`; the nested savepoint and `except IntegrityError: pass` around `get_or_create` are preserved.
    - No `is_offered_candidate()` call was added to `_undo_confirmation()` (it would reject every legitimate undo — an already-confirmed pair is no longer offered).
    - `_undo_confirmation()`'s docstring no longer contains the word "FIRST" describing the dismissal write, and states the actual order plus the WR-01 reason for it.
    - All five tests in `TestUndoConfirmationOrdering` pass; `AttributionViewTestBase` is byte-identical to its pre-task state (`git diff` shows no change within lines 39-128).
    - The five pre-existing tests in `TestConfirmUndo` and every test in `TestDismissAndUndoDismissal` pass unchanged — no existing test was edited to accommodate the reorder.
    - Mutation check performed and recorded: with the original ordering restored, tests 1-4 fail and test 5 passes.
  </acceptance_criteria>
  <done>A stale or forged undo submission writes nothing and leaves the pair offerable; a genuine undo still clears the link and records its dismissal in the same transaction.</done>
</task>

<task type="auto">
  <name>Task 2: WR-02 — compute sole_high_candidate_pk from the full candidate list, and make the IN-01 drop-threshold docstring honest</name>
  <files>solsys_code/campaign_attribution.py, solsys_code/tests/test_campaign_attribution.py</files>
  <read_first>
    - `solsys_code/campaign_attribution.py` lines 621-638 (`_sole_high_candidate_pk()` — its docstring's explicit "the orphan's FULL candidate list (uncapped)" contract and the D-09 guardrail rationale)
    - `solsys_code/campaign_attribution.py` lines 640-710 (`event_attribution_backlog()` and `record_attribution_backlog()` in full — the `candidates = candidates_for_event(event)` → band filter → `if not candidates: continue` → `AttributionOrphanGroup(...)` sequence, including `candidates[:MAX_CANDIDATES_PER_ORPHAN]`, `total_candidate_count=len(candidates)` and the trailing `groups.sort(...)`)
    - `solsys_code/campaign_attribution.py` lines 525-545 and 588-605 (`candidates_for_event()` / `candidates_for_record()` — the `if candidate.score <= 0.0: continue` drop and the docstring sentence IN-01 contradicts) and `_build_candidate()`'s `score=round(score, 2)`
    - `solsys_code/campaign_views.py` lines 1218-1249 (`_is_sole_high_candidate()` — the server-side gate that already re-derives from the **unfiltered** `candidates_for_event`/`candidates_for_record` result; this fix makes the display helper agree with it)
    - `src/templates/campaigns/attribution_queue.html` lines ~70-80 and ~156-166 (the `{% if candidate.run.pk == group.sole_high_candidate_pk %}` checkbox gate — the reason this fix is behavior-neutral at the rendered surface)
    - `solsys_code/tests/test_campaign_attribution.py` lines 275-377 (`TestCriterion5RealCase` — the fixture shape and the `assertEqual(candidate.band, ...)` precondition discipline the new class mirrors)
    - `solsys_code/tests/test_campaign_attribution_views.py` lines 701-710 (`TestBandFilterAndBanner._medium_band_event()` — the partial-date-overlap + unresolved-telescope recipe that lands a candidate in the Medium band; adapt it to vary the *run* rather than the event)
  </read_first>
  <action>
**2a. The WR-02 fix.** In both `event_attribution_backlog()` and `record_attribution_backlog()`,
compute the full candidate list once and derive the band-filtered view from it:

  - `full_candidates = candidates_for_event(event)` (resp. `candidates_for_record(record)`).
  - `candidates = [c for c in full_candidates if c.band == band] if band is not None else full_candidates`
  - Keep `if not candidates: continue` unchanged — a group with no candidate *in the requested band*
    is still dropped, which is the existing and correct filter semantics.
  - Keep `candidates=candidates[:MAX_CANDIDATES_PER_ORPHAN]` and
    `total_candidate_count=len(candidates)` bound to the **filtered** list (those two fields describe
    what is displayed, and changing them would alter the "showing top N of M candidates" line).
  - Change only `sole_high_candidate_pk=_sole_high_candidate_pk(full_candidates)`.

Add a short comment at each call site recording why the argument is deliberately the unfiltered list:
`_sole_high_candidate_pk()` documents a full-uncapped-list contract, and
`campaign_views._is_sole_high_candidate()` — the server-side gate that actually authorises a
bulk-confirm write — re-derives from the unfiltered list, so the display helper must agree with it or
the two silently diverge.

State plainly in the same comment that this is behavior-neutral at the rendered surface today: the
checkbox renders only for a *displayed* candidate whose run pk equals `sole_high_candidate_pk`, and a
run appears at most once per orphan, so under a medium/low filter no displayed candidate can match
the sole-High run anyway. The fix restores the documented invariant for the next consumer rather than
changing what staff see. Verify this claim yourself against the template's checkbox condition before
writing the comment — if the reasoning does not hold, stop and report rather than shipping a comment
that asserts something false.

**2b. IN-01 disposition — correct the contract, defer the behavior change.** `28-REVIEW.md` IN-01
observes that `candidates_for_event()`/`candidates_for_record()` apply the `<= 0.0` drop to
`AttributionCandidate.score`, which `_build_candidate()` has already rounded to 2 decimal places, so a
raw weighted sum of e.g. `0.004` is dropped as if it carried no evidence at all — while the docstring
promises the drop means "no evidence on any signal at all".

**Decision: correct the docstrings, do not change the filtering behavior in this plan.** Reasoning,
which must also be recorded in the code comment so it is not silently re-litigated: the reviewer
rates this Info and notes that at the current weights (0.25 / 0.35 / 0.40) a nonzero total below
0.005 is not reachable by any real combination of signals, so the behavior change would be a no-op
today; and the only way to reach the raw score is to widen `_build_candidate()`'s return or add a
field to `AttributionCandidate`, which touches the scoring path whose acceptance test
(`TestCriterion5RealCase`) is the phase's load-bearing check. A zero-value change to a load-bearing
path is a bad trade. The genuine defect IN-01 names — a stated contract that does not match the code
— is closed by making the contract accurate.

So: in both `candidates_for_event()` and `candidates_for_record()`, amend the docstring sentence
about the drop to state that the threshold is applied to the **rounded display score**
(`round(score, 2)`), so a raw total below 0.005 is treated as no evidence. Add a short inline comment
at each `if candidate.score <= 0.0: continue` naming IN-01, recording that the behavior change was
considered and deliberately deferred as inert at the current weights, and stating the trigger that
would make it live: if the weights in `_build_candidate()` are ever re-tuned such that a meaningful
signal can produce a raw total below 0.005, filter on the raw score instead. Change no behavior.

**2c. Regression test for WR-02.** Add `TestSoleHighCandidateUnderBandFilter(TestCase)` to
`solsys_code/tests/test_campaign_attribution.py`. Its class docstring states that this pins
`_sole_high_candidate_pk()`'s documented full-uncapped-list contract under a band filter
(`28-REVIEW.md` WR-02) and keeps the display helper in agreement with
`campaign_views._is_sole_high_candidate()`.

Fixture (`setUpTestData`): a `TargetList` campaign, a `NonSiderealTargetFactory` target added to it
(CLAUDE.md: never `SiderealTargetFactory`), one orphan `CalendarEvent` with its unowned
`CalendarEventMeta`, and **two** `CampaignRun`s in that campaign — one shaped to score High against
the event (mirror `AttributionViewTestBase`'s `campaign_run`: matching telescope, fully-overlapping
window) and one shaped to score Medium (partial date overlap plus an unresolvable telescope string,
per `_medium_band_event()`'s recipe, applied to the run rather than the event). Name them
`high_run` / `medium_run` — **never** a bare `run` attribute on a `TestCase`, which shadows
`unittest.TestCase.run()` and crashes the runner with no per-test traceback (28-02-SUMMARY.md's
documented pitfall). Build the record-side equivalent in the same class.

Four tests:
  1. `test_precondition_the_fixture_really_produces_one_high_and_one_medium_candidate` — read
     `candidates_for_event(event)`, assert exactly two candidates, and assert their bands are
     `BAND_HIGH` and `BAND_MEDIUM` respectively. Without this the remaining tests can pass for the
     wrong reason if the cut-points ever move. Tune the fixture until this test passes on its own
     terms — do not weaken the assertion to match whatever the fixture happens to produce.
  2. `test_sole_high_candidate_pk_survives_a_medium_band_filter_for_events` — assert
     `event_attribution_backlog(band='medium')` returns one group whose `sole_high_candidate_pk`
     equals `high_run.pk` (pre-fix this is `None`), and — to pin that the filter itself still works —
     that the group's displayed `candidates` contain only `medium_run`.
  3. `test_sole_high_candidate_pk_survives_a_medium_band_filter_for_records` — the record-side
     equivalent via `record_attribution_backlog(band='medium')`.
  4. `test_unfiltered_and_high_filtered_backlogs_are_unchanged` — assert `sole_high_candidate_pk`
     equals `high_run.pk` for both `event_attribution_backlog()` and
     `event_attribution_backlog(band='high')`, so the fix is proved not to have altered the two views
     that already behaved correctly.

Keep `ruff check .` and `ruff format --check .` clean.
  </action>
  <verify>
    <automated>python manage.py test solsys_code.tests.test_campaign_attribution -v 2</automated>
    <automated>python manage.py test solsys_code.tests.test_campaign_attribution_views solsys_code.tests.test_attribution_dismissals -v 1 && ruff check solsys_code/campaign_attribution.py solsys_code/tests/test_campaign_attribution.py && ruff format --check solsys_code/campaign_attribution.py solsys_code/tests/test_campaign_attribution.py</automated>
  </verify>
  <acceptance_criteria>
    - Both backlog builders bind a `full_candidates` local before applying the band filter and pass it to `_sole_high_candidate_pk(...)`; `grep -c '_sole_high_candidate_pk(full_candidates)' solsys_code/campaign_attribution.py` returns exactly `2`.
    - `candidates[:MAX_CANDIDATES_PER_ORPHAN]` and `total_candidate_count=len(candidates)` still refer to the band-filtered list, so the "showing top N of M candidates" line is unchanged.
    - `TestCriterion5RealCase` still passes unmodified — the phase's acceptance test is untouched by this change.
    - All four tests in `TestSoleHighCandidateUnderBandFilter` pass, including the precondition test asserting one High and one Medium candidate by band constant, not by score value.
    - The fixture uses `NonSiderealTargetFactory`, and no `TestCase` attribute is named `run`.
    - `candidates_for_event()`/`candidates_for_record()` docstrings state that the drop threshold applies to the rounded display score, and each `if candidate.score <= 0.0: continue` carries an inline comment naming IN-01, the deferral reason, and the re-tuning trigger that would make the behavior change live.
    - No change to `_build_candidate()`, to any weight, or to any band cut-point.
  </acceptance_criteria>
  <done>`sole_high_candidate_pk` means the same thing under every band filter and agrees with the server-side gate that authorises writes; every scoring-path docstring describes what the code actually does.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → `campaigns:attribution_decide` (`action=undo_confirmation`) | `kind`/`orphan_pk`/`run_pk` cross here validated only as integers — the pair they name is attacker- or staleness-controlled |
| Rendered checkbox → `action=confirm_selected` | The template's `sole_high_candidate_pk` decides which rows *display* a checkbox; the server's `_is_sole_high_candidate()` decides which are accepted |
| Dismissal rows → future candidate lists | A dismissal permanently excludes a pair from `candidates_for_event`/`candidates_for_record`, so any path that can write one can remove a pair from the queue |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28.6-01 | Tampering | `AttributionDecisionView._undo_confirmation()` | mitigate | Task 1a: the dismissal `get_or_create` runs only inside `if changed_count:`, so a POST naming a pair that is not currently confirmed to that run writes nothing. Tests 1-4 of `TestUndoConfirmationOrdering` assert no dismissal row and an untouched link. |
| T-28.6-02 | Denial of service (of a queue entry) | `CalendarEventDismissal` / `ObservationRecordDismissal` | mitigate | Same fix: a forged or stale undo can no longer permanently remove an un-confirmed pair from every future candidate list. Test 2 asserts the pair stays offerable via `candidates_for_event()`. |
| T-28.6-03 | Elevation of privilege | Bulk-confirm checkbox gate | accept (already mitigated) | `_is_sole_high_candidate()` re-derives from the full unfiltered candidate list on every `confirm_selected` write and is unchanged by this plan. Task 2a only aligns the *display* helper with it; the display helper never authorises a write, so widening it under a band filter grants nothing. |
| T-28.6-04 | Repudiation | Undo attributability (D-13) | accept (already mitigated, must not regress) | A genuine undo still writes `dismissed_by`/`dismissed_at` in the same transaction as the link-clearing write. Test 5 of `TestUndoConfirmationOrdering` exists specifically so the reorder cannot over-gate and silently drop D-13's trace. |
| T-28.6-05 | Information disclosure | Candidate scoring | accept | Task 2b changes no behavior and no data reaches any new surface; the attribution page remains staff-only behind `StaffRequiredMixin`. |
| T-28.6-SC | Tampering | npm/pip/cargo installs | accept — not applicable | This plan installs **no** packages: both fixes are pure-Python edits to existing modules using imports already present. No Package Legitimacy Audit is required because there are no install tasks. |
</threat_model>

<verification>
Run in order after both tasks:

1. `python manage.py test solsys_code.tests.test_campaign_attribution solsys_code.tests.test_campaign_attribution_views solsys_code.tests.test_attribution_dismissals solsys_code.tests.test_admin -v 1` — the four pre-existing phase-28 modules plus the two new classes, all green, with no pre-existing test edited.
2. `python manage.py makemigrations --check --dry-run solsys_code` — "No changes detected" (this plan touches no model).
3. `ruff check .` and `ruff format --check .` — clean.
4. `git diff solsys_code/tests/test_campaign_attribution_views.py` — confirm `AttributionViewTestBase` (lines 39-128) is unchanged, so `28-05`'s new module can import it.
5. Mutation check re-confirmed and recorded (Task 1d).
</verification>

<success_criteria>
- `28-VERIFICATION.md`'s WR-01 anti-pattern row is closed: `_undo_confirmation()` cannot write a dismissal for a pair it did not actually un-confirm.
- `28-VERIFICATION.md`'s WR-02 anti-pattern row is closed: `_sole_high_candidate_pk()` receives the list its docstring requires, from both call sites.
- IN-01's stated deviation (docstring vs. implementation) is closed, with the behavior change explicitly deferred, its reasoning recorded in the code, and its re-activation trigger named.
- `TestCriterion5RealCase` — the phase's acceptance test — passes unmodified.
- No file belonging to plans 28-01 through 28-04 (`*-PLAN.md`, `*-SUMMARY.md`) is modified, and no file overlaps with `28-05-PLAN.md`.
</success_criteria>

<output>
Create `.planning/phases/28-operator-assisted-attribution/28-06-SUMMARY.md` when done.

The SUMMARY **must** record:
- The Task 1d mutation-check output (which of tests 1-4 failed under the original ordering).
- The IN-01 deferral decision, its reasoning, and the re-tuning trigger that would make the behavior
  change live — so a future reviewer finds a decision rather than an unexplained gap.
</output>
