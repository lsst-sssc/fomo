---
phase: 28-operator-assisted-attribution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - solsys_code/models.py
  - solsys_code/migrations/0013_attribution_dismissals_and_calendar_event_meta_audit.py
  - solsys_code/admin.py
  - solsys_code/tests/test_attribution_dismissals.py
  - solsys_code/tests/test_admin.py
autonomous: true
requirements: [ATTRIB-03, ATTRIB-04]

must_haves:
  truths:
    - "A staff member's rejection of a suggested (orphan, run) pair is stored with who rejected it, when, and why"
    - "The same (orphan, run) pair cannot be rejected twice -- a second dismissal row is refused by the database"
    - "An event attribution records who confirmed it and when, on the event side as well as the record side"
    - "An admin-created event-to-run link is stamped with confirmed_by/confirmed_at, exactly as an admin-created observation link already is"
  artifacts:
    - path: "solsys_code/models.py"
      provides: "CalendarEventDismissal, ObservationRecordDismissal, CalendarEventMeta.confirmed_by/confirmed_at"
      contains: "class CalendarEventDismissal"
    - path: "solsys_code/migrations/0013_attribution_dismissals_and_calendar_event_meta_audit.py"
      provides: "Schema for the two dismissal models plus the two CalendarEventMeta audit fields"
      contains: "CreateModel"
    - path: "solsys_code/admin.py"
      provides: "save_formset CalendarEventMeta run-transition audit stamping"
      contains: "isinstance(instance, CalendarEventMeta)"
    - path: "solsys_code/tests/test_attribution_dismissals.py"
      provides: "Constraint, cascade and audit-default coverage for both dismissal models"
  key_links:
    - from: "solsys_code/admin.py"
      to: "CalendarEventMeta.confirmed_by"
      via: "save_formset run_id None->not-None transition branch"
      pattern: "prior_run_id"
    - from: "solsys_code/models.py"
      to: "CampaignRun"
      via: "dismissal model ForeignKey"
      pattern: "ForeignKey\\(\\s*['\\\"]?CampaignRun"
---

<objective>
Land the schema this phase writes into: two typed per-pair dismissal models (D-05/D-06/D-08) and
the two event-side audit fields that make an event attribution attributable to a person and a
time (D-12), plus the admin write path that must stamp them.

Purpose: every later plan in this phase reads or writes these three things. The matcher (28-02)
excludes dismissed pairs; the POST actions (28-03) create dismissal rows and stamp the audit
fields; the page (28-04) renders the Dismissed and Confirmed sections from them. Nothing else can
start until the migration exists.

Output: `CalendarEventDismissal`, `ObservationRecordDismissal`, `CalendarEventMeta.confirmed_by`,
`CalendarEventMeta.confirmed_at`, migration 0013, an updated D-05 comment that no longer
contradicts the code, and a `save_formset` branch that stamps the new fields on a genuine
run-link transition.
</objective>

<execution_context>
@/home/tlister/git/fomo_devel/.claude/gsd-core/workflows/execute-plan.md
@/home/tlister/git/fomo_devel/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-operator-assisted-attribution/28-CONTEXT.md
@.planning/phases/28-operator-assisted-attribution/28-PATTERNS.md
@.planning/phases/28-operator-assisted-attribution/28-RESEARCH.md
</context>

## Artifacts this phase produces (28-01 share)

| Kind | Name | Location |
|------|------|----------|
| Model | `CalendarEventDismissal` | `solsys_code/models.py` |
| Model | `ObservationRecordDismissal` | `solsys_code/models.py` |
| Model field | `CalendarEventMeta.confirmed_by` | `solsys_code/models.py` |
| Model field | `CalendarEventMeta.confirmed_at` | `solsys_code/models.py` |
| DB constraint | `unique_calendar_event_dismissal_pair` | `solsys_code/models.py` |
| DB constraint | `unique_observation_record_dismissal_pair` | `solsys_code/models.py` |
| Reverse accessor | `CalendarEvent.attribution_dismissals` | via `related_name` |
| Reverse accessor | `ObservationRecord.attribution_dismissals` | via `related_name` |
| Reverse accessor | `CampaignRun.calendar_event_dismissals` | via `related_name` |
| Reverse accessor | `CampaignRun.observation_record_dismissals` | via `related_name` |
| Reverse accessor | `User.confirmed_calendar_event_metas` | via `related_name` |
| Reverse accessor | `User.dismissed_calendar_event_attributions` | via `related_name` |
| Reverse accessor | `User.dismissed_observation_record_attributions` | via `related_name` |
| Migration | `0013_attribution_dismissals_and_calendar_event_meta_audit.py` | `solsys_code/migrations/` |
| Test module | `solsys_code/tests/test_attribution_dismissals.py` | new file |
| Test method | `CampaignRunAdminInlinesTests.test_save_formset_stamps_calendar_event_meta_on_run_transition` | `solsys_code/tests/test_admin.py` |
| Test method | `CampaignRunAdminInlinesTests.test_save_formset_does_not_restamp_calendar_event_meta_on_unrelated_edit` | `solsys_code/tests/test_admin.py` |

<tasks>

<task type="auto">
  <name>Task 1: Two dismissal models, the event-side audit fields, and migration 0013</name>
  <files>solsys_code/models.py, solsys_code/migrations/0013_attribution_dismissals_and_calendar_event_meta_audit.py</files>
  <read_first>
    - solsys_code/models.py (whole file -- `CalendarEventMeta` lines 10-59 including the D-05 comment at lines 34-38, `CampaignRunObservation` lines 330-391 for the exact field/constraint shape to mirror, `CampaignRun.Meta.constraints` for the named-constraint-with-comment convention)
    - solsys_code/migrations/0010_campaignrun_source_telescope_class_campaignrunobservation.py (the CreateModel + AddField operation shape, and the `migrations.swappable_dependency(settings.AUTH_USER_MODEL)` dependency line)
    - .planning/phases/28-operator-assisted-attribution/28-PATTERNS.md (section "Two dismissal models" for the field shape and the on_delete judgement call; section "solsys_code/models.py (modified ...)" for the exact D-05 comment text being replaced)
    - .planning/phases/28-operator-assisted-attribution/28-CONTEXT.md (D-05, D-06, D-08, D-12)
  </read_first>
  <action>
Add two new models to `solsys_code/models.py`, placed after `CampaignRunObservation`.

`CalendarEventDismissal` (D-05/D-06/D-08) with these fields, mirroring
`CampaignRunObservation`'s field shape exactly:
- `event` -- `ForeignKey(CalendarEvent, on_delete=models.CASCADE, related_name='attribution_dismissals', verbose_name='Calendar event')`
- `run` -- `ForeignKey(CampaignRun, on_delete=models.CASCADE, related_name='calendar_event_dismissals', verbose_name='Campaign run')`
- `dismissed_by` -- `ForeignKey(settings.AUTH_USER_MODEL, on_delete=models.SET_NULL, null=True, blank=True, related_name='dismissed_calendar_event_attributions', verbose_name='Dismissed by')`
- `dismissed_at` -- `DateTimeField(null=True, blank=True, verbose_name='Dismissed at')`
- `reason` -- `TextField(blank=True, default='', verbose_name='Why this candidate was rejected')`
- `Meta.constraints` -- a single named `UniqueConstraint(fields=('event', 'run'), name='unique_calendar_event_dismissal_pair')`
- `__str__` returning a readable "dismissed {event} for {run}" style label

`ObservationRecordDismissal` -- identical apart from:
- `observation_record` -- `ForeignKey(ObservationRecord, on_delete=models.CASCADE, related_name='attribution_dismissals', verbose_name='Observation record')`
- `run` `related_name='observation_record_dismissals'`
- `dismissed_by` `related_name='dismissed_observation_record_attributions'`
- constraint `name='unique_observation_record_dismissal_pair'` on `fields=('observation_record', 'run')`

Follow the codebase's named-`UniqueConstraint`-with-an-explanatory-comment convention (never a
bare `unique_together`, which this codebase does not use anywhere). The comment above each
constraint must state: the constraint is per (orphan, run) PAIR and not per orphan, because
D-05 requires dismissing one wrong candidate to leave the orphan's other candidates -- and any
candidate the matcher surfaces later as new runs arrive -- still offered.

Add a comment above each model's `on_delete` choices recording the judgement PATTERNS.md flags
as genuinely new: both FKs are `CASCADE`, not `SET_NULL`. Rationale to write down: a dismissal
row records "this pair was rejected"; if either side of the pair is deleted the pair can never
be suggested again, so the row carries nothing and means nothing -- the same reasoning
`CampaignRunObservation.observation_record`'s existing CASCADE comment gives. This is
deliberately NOT the "survives as an audit trail forever" alternative.

Add two fields to `CalendarEventMeta` (D-12), copying `CampaignRunObservation.confirmed_by` /
`confirmed_at` verbatim apart from `related_name`:
- `confirmed_by` -- `ForeignKey(settings.AUTH_USER_MODEL, on_delete=models.SET_NULL, null=True, blank=True, related_name='confirmed_calendar_event_metas', verbose_name='Confirmed by')`
- `confirmed_at` -- `DateTimeField(null=True, blank=True, verbose_name='Confirmed at')`

Replace the D-05 comment block currently sitting above `CalendarEventMeta.run` (the one ending
"This is a decision, not an omission -- do not 'fix' it here"). Do NOT leave it in place and do
NOT merely append to it -- CLAUDE.md forbids leaving a comment contradicting the code. The
replacement records: Phase 28 D-12 deliberately reopened Phase 27 D-05 and added
`confirmed_by`/`confirmed_at` here, closing the audit asymmetry with the observation link;
ROADMAP Phase 28 criterion 4 requires both a confirmation and an undo to be attributable to a
person and a time, and 27-CONTEXT.md itself named this phase as the place to revisit the gap if
the undo flow proved it painful. Also state the consequence readers need: an event link written
before Phase 28 (e.g. by the admin FK picker) has both fields NULL, so NULL means "confirmed
before audit fields existed", not "unconfirmed" -- the `run` FK being set is still what means
"attributed".

Generate the migration with
`python manage.py makemigrations solsys_code --name attribution_dismissals_and_calendar_event_meta_audit`
and confirm it is numbered 0013. Then hand-add a header comment block to the generated file, in
the style of 0010's header, recording the deliberate choice RESEARCH.md Assumption A4 flags: the
two new models and the two new fields ship in ONE migration rather than split the way 0008/0009
split a rename from a field-add, because these four operations are all additive, share no
existing table's data, and have no regression that could be confused for another's. Do not
hand-author the operations themselves.
  </action>
  <verify>
    <automated>python manage.py makemigrations --check --dry-run && python manage.py migrate solsys_code && ruff check . && ruff format --check .</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "class CalendarEventDismissal(models.Model):" solsys_code/models.py` returns 1
    - `grep -c "class ObservationRecordDismissal(models.Model):" solsys_code/models.py` returns 1
    - `grep -c "unique_calendar_event_dismissal_pair" solsys_code/models.py` returns 1
    - `grep -c "unique_observation_record_dismissal_pair" solsys_code/models.py` returns 1
    - `grep -c "related_name='confirmed_calendar_event_metas'" solsys_code/models.py` returns 1
    - The stale D-05 text is gone: `grep -c "do not \"fix\" it here" solsys_code/models.py` returns 0
    - The replacement comment is present: `grep -v '^#' solsys_code/models.py | grep -c "D-12"` returns at least 1
    - `ls solsys_code/migrations/0013_attribution_dismissals_and_calendar_event_meta_audit.py` succeeds
    - `python manage.py makemigrations --check --dry-run` exits 0 (no model change is left unmigrated)
    - `python manage.py migrate solsys_code` exits 0 against the dev database
    - `ruff check .` exits 0 and `ruff format --check .` exits 0
  </acceptance_criteria>
  <done>Both dismissal models and both `CalendarEventMeta` audit fields exist, migration 0013 applies cleanly, and the D-05 comment describes what the code now does.</done>
</task>

<task type="auto">
  <name>Task 2: Admin save_formset stamps the event-side audit fields on a genuine run-link transition</name>
  <files>solsys_code/admin.py, solsys_code/tests/test_admin.py</files>
  <read_first>
    - solsys_code/admin.py (lines 80-130 for `CalendarEventMetaInline`/`CampaignRunObservationInline`, and `CampaignRunAdmin.save_formset` at lines 220-252 -- the exact method being extended, including the isinstance-gate comment that says CalendarEventMeta "must never be stamped", which this task supersedes)
    - solsys_code/models.py (post-Task-1 state of `CalendarEventMeta`)
    - .planning/phases/28-operator-assisted-attribution/28-PATTERNS.md (section "solsys_code/admin.py (modified ...)" -- the Divergence Note giving the exact prior-value diffing logic)
    - .planning/phases/28-operator-assisted-attribution/28-RESEARCH.md (Pattern 4 and Pitfall 4)
    - solsys_code/tests/test_admin.py (class `CampaignRunAdminInlinesTests`, lines 217-430 -- the existing `test_save_formset_stamps_confirmed_by_and_confirmed_at_on_create`, `test_save_formset_does_not_restamp_on_edit`, and `test_save_formset_does_not_stamp_calendar_event_meta_formset`, whose assertion this task inverts)
  </read_first>
  <action>
Extend `CampaignRunAdmin.save_formset` in `solsys_code/admin.py` with a second `isinstance`
branch for `CalendarEventMeta`.

The branch condition is a `run_id` None-to-not-None TRANSITION, NOT `instance.pk is None`.
`CalendarEventMeta.event` is a `OneToOneField(primary_key=True)`, so every instance flowing
through this formset already has a non-None pk and a `pk is None` gate would silently never
stamp anything (RESEARCH.md Pitfall 4). Read the prior database value before the mutated
in-memory instance hides it -- `formset.save(commit=False)` returns instances whose `run_id` has
already been overwritten -- by fetching
`CalendarEventMeta.objects.filter(pk=instance.pk).values_list('run_id', flat=True).first()` for
each `CalendarEventMeta` instance in the loop. Stamp `confirmed_by = request.user` and
`confirmed_at = timezone.now()` only when the prior value is None and `instance.run_id` is not
None. A row whose `run` is being cleared, re-pointed from one run to another, or left unchanged
must NOT be re-stamped -- this mirrors the "never silently re-attribute someone else's
confirmation" rule the existing `CampaignRunObservation` branch's comment already states.

Import `CalendarEventMeta` into `admin.py` if it is not already imported at module scope.

Update the existing isinstance-gate comment inside `save_formset`: it currently says
`CalendarEventMeta` "has no confirmed_by/confirmed_at fields at all (D-05 declines audit fields
on the event side), so it must never be stamped." Replace that sentence with one recording D-12's
reopening and the reason the two branches use different conditions -- a
`CampaignRunObservation` row's existence IS the confirmation (Phase 27 D-01) whereas a
`CalendarEventMeta` row exists long before any attribution, so only its `run` field becoming
non-null marks the confirmation.

Update the `save_formset` docstring's first line, which currently scopes the method to
"newly created CampaignRunObservation rows only".

In `solsys_code/tests/test_admin.py`, extend `CampaignRunAdminInlinesTests` with two tests:
- `test_save_formset_stamps_calendar_event_meta_on_run_transition` -- drive the
  `CalendarEventMetaInline` formset the same way the existing
  `test_save_formset_stamps_confirmed_by_and_confirmed_at_on_create` drives the observation
  inline (build the formset class via `inline.get_formset(request)`, POST management-form data),
  linking an existing unowned `CalendarEventMeta` row to the run, then assert `confirmed_by`
  equals the request user and `confirmed_at` is not None.
- `test_save_formset_does_not_restamp_calendar_event_meta_on_unrelated_edit` -- an existing
  `CalendarEventMeta` row that already has a `run` and a `confirmed_by` keeps both unchanged
  when some other field (`is_verified`) is edited through the inline.

Replace the existing `test_save_formset_does_not_stamp_calendar_event_meta_formset` (it asserts
the behaviour D-12 deliberately reverses) rather than leaving it failing. If keeping its name is
useful, rewrite its body and docstring to assert the new contract; otherwise delete it and let
the two new tests cover the branch. Note the change in the SUMMARY as a deliberate,
D-12-mandated test revision, not an accidental break.

Test fixtures must use `NonSiderealTargetFactory` for any `Target` (CLAUDE.md), and superuser
(not merely `is_staff=True`) accounts for inline formset tests -- Phase 27-05 recorded that
`DeleteProtectedModelForm.has_changed()` gates on the inline model's own add/change permission.
  </action>
  <verify>
    <automated>python manage.py test solsys_code.tests.test_admin</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "isinstance(instance, CalendarEventMeta)" solsys_code/admin.py` returns 1
    - `grep -c "prior_run_id" solsys_code/admin.py` returns at least 2 (the fetch and the condition)
    - `grep -c "instance.pk is None" solsys_code/admin.py` returns exactly 1 (the pre-existing `CampaignRunObservation` branch only -- the new branch must not reuse it)
    - The superseded comment sentence is gone: `grep -c "must never be stamped" solsys_code/admin.py` returns 0
    - `python manage.py test solsys_code.tests.test_admin.CampaignRunAdminInlinesTests` exits 0
    - `python manage.py test solsys_code.tests.test_admin.CampaignRunAdminInlinesTests.test_save_formset_stamps_calendar_event_meta_on_run_transition` exits 0
    - `grep -c "test_save_formset_does_not_stamp_calendar_event_meta_formset" solsys_code/tests/test_admin.py` returns 0, or that test's body asserts the new stamping contract
    - `ruff check .` exits 0
  </acceptance_criteria>
  <done>An admin-created event-to-run link is stamped with `confirmed_by`/`confirmed_at`; an edit that does not create the link is not.</done>
</task>

<task type="auto">
  <name>Task 3: Constraint, cascade and audit-default tests for both dismissal models</name>
  <files>solsys_code/tests/test_attribution_dismissals.py</files>
  <read_first>
    - solsys_code/tests/test_campaign_run_observation.py (whole file -- the fixture style, the `record_owner`-separate-from-`user` gotcha, and the `transaction.atomic()`-wrapped `assertRaises(IntegrityError)` idiom to copy)
    - solsys_code/models.py (post-Task-1 state of both dismissal models)
    - .planning/phases/28-operator-assisted-attribution/28-VALIDATION.md (Wave 0 Requirements)
  </read_first>
  <action>
Create `solsys_code/tests/test_attribution_dismissals.py` covering both new models, following
`test_campaign_run_observation.py`'s structure and fixture conventions exactly.

Fixtures: a `TargetList` campaign, a `Target` built with `NonSiderealTargetFactory` (never
`SiderealTargetFactory` -- CLAUDE.md), two `CampaignRun` rows, one `CalendarEvent`, one
`ObservationRecord` created with a `record_owner` user kept distinct from the dismissing-staffer
user (`ObservationRecord.user` is `on_delete=DO_NOTHING`, so deleting a still-referenced user
fails SQLite's deferred foreign-key check -- the same trap Phase 27-04 hit).

Tests, for each of the two models:
- The named pair `UniqueConstraint` fires: a second dismissal row for the same (orphan, run) pair
  raises `IntegrityError`. Wrap the failing create in `transaction.atomic()` so the surrounding
  test transaction is not poisoned.
- The constraint is per PAIR, not per orphan: the SAME orphan can be dismissed against a SECOND,
  different run without raising. This is the assertion that pins D-05's stated reason for
  choosing per-pair.
- Deleting the run cascades the dismissal row away; deleting the orphan does too.
- `dismissed_by` / `dismissed_at` default to None and `reason` defaults to `''` when a row is
  created without them (the model does not itself require them -- the POST action in 28-03 is
  what enforces a non-empty reason).
- Deleting the dismissing user leaves the dismissal row alive with `dismissed_by` NULL
  (`SET_NULL`), so a departed staff member never erases the rejection record.

Also add one test asserting `CalendarEventMeta.confirmed_by` / `confirmed_at` default to None on
a freshly created row, mirroring `test_campaign_run_observation.py`'s audit-field-default test --
this is what makes "NULL means confirmed before audit fields existed" a checked fact rather than
a comment.
  </action>
  <verify>
    <automated>python manage.py test solsys_code.tests.test_attribution_dismissals</automated>
  </verify>
  <acceptance_criteria>
    - `python manage.py test solsys_code.tests.test_attribution_dismissals` exits 0
    - `grep -c "NonSiderealTargetFactory" solsys_code/tests/test_attribution_dismissals.py` returns at least 1
    - `grep -c "SiderealTargetFactory" solsys_code/tests/test_attribution_dismissals.py` returns 0 outside the `NonSiderealTargetFactory` occurrences (verify with `grep -o 'SiderealTargetFactory' | wc -l` matching the `NonSiderealTargetFactory` count)
    - `grep -c "assertRaises(IntegrityError)" solsys_code/tests/test_attribution_dismissals.py` returns at least 2 (one per model)
    - `python manage.py test solsys_code.tests.test_campaign_run_observation solsys_code.tests.test_campaign_models` exits 0 (no regression in the neighbouring model suites)
    - `ruff check .` exits 0 and `ruff format --check .` exits 0
  </acceptance_criteria>
  <done>Both dismissal models' constraints, cascades and audit defaults are pinned by tests, and the per-pair (not per-orphan) contract is asserted directly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| staff browser -> Django admin | A staff user submits inline formset data that writes `CalendarEventMeta.run` and, from this plan on, its audit fields |
| application -> database | New tables and new columns; uniqueness is enforced in the database, not in Python |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01 | Repudiation | `CalendarEventMeta.run` set via the admin inline | mitigate | `save_formset` stamps `confirmed_by = request.user` / `confirmed_at = timezone.now()` on a genuine None-to-not-None `run_id` transition (Task 2), so an event attribution created through the admin is attributable to a person and a time exactly as the observation link already is (D-12, ROADMAP criterion 4) |
| T-28-02 | Tampering | `confirmed_by` submitted through the admin inline form | mitigate | `confirmed_by`/`confirmed_at` are stamped server-side from `request.user` and `timezone.now()` in `save_formset`, never bound from POST data; a submitted value for either field is overwritten on the transition path and ignored otherwise |
| T-28-03 | Tampering | Duplicate dismissal rows for the same (orphan, run) pair | mitigate | Named `UniqueConstraint` per pair enforced by the database (Task 1), not by application-level checking -- two concurrent staff dismissals both miss an existing row in Python but only one survives the constraint; Task 3 pins this with an `IntegrityError` assertion |
| T-28-04 | Repudiation | Deleting the dismissing user erases who rejected a pair | accept | `dismissed_by` is `SET_NULL`, matching `CampaignRunObservation.confirmed_by`; the row, its timestamp and its free-text reason survive, which is what D-06 requires. Diverging here would block user deletion for no attribution benefit |
| T-28-05 | Information Disclosure | Free-text `reason` stored on a dismissal row | mitigate | Stored raw and never marked safe; rendering is Django auto-escaped and is covered by 28-04's threat model. This plan adds no rendering path |
| T-28-SC | Tampering | Package-manager installs | accept | No packages are installed by this phase -- RESEARCH.md's Package Legitimacy Audit records that the gate is not triggered and that `difflib` is stdlib. No `[ASSUMED]`/`[SUS]` package exists to gate |
</threat_model>

<verification>
- `python manage.py makemigrations --check --dry-run` exits 0
- `python manage.py migrate solsys_code` applies 0013 with no error
- `python manage.py test solsys_code.tests.test_attribution_dismissals solsys_code.tests.test_admin solsys_code.tests.test_campaign_run_observation solsys_code.tests.test_canonical_record_migration` exits 0
- `ruff check .` and `ruff format --check .` both exit 0
</verification>

<success_criteria>
- Two typed dismissal models exist with real FKs, named per-pair unique constraints, and D-06's who/when/reason fields
- `CalendarEventMeta` carries `confirmed_by`/`confirmed_at` and its D-05 comment describes what the code now does
- Migration 0013 exists, applies cleanly, and leaves `makemigrations --check` clean
- The admin write path stamps the new event-side audit fields on a genuine run-link transition, and only then
</success_criteria>

<output>
Create `.planning/phases/28-operator-assisted-attribution/28-01-SUMMARY.md` when done
</output>
