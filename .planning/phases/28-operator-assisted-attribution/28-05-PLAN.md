---
phase: 28-operator-assisted-attribution
plan: 05
type: execute
wave: 5
gap_closure: true
depends_on: ["28-04"]
files_modified:
  - src/templates/campaigns/attribution_queue.html
  - solsys_code/admin.py
  - solsys_code/tests/test_attribution_template.py
  - solsys_code/tests/test_admin.py
  - docs/runbooks/telescope_runs_calendar.rst
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [ATTRIB-01, ATTRIB-02, ATTRIB-03, ATTRIB-04, ATTRIB-05, ATTRIB-06]

must_haves:
  truths:
    - "Staff can confirm a candidate association one at a time through the rendered attribution-queue UI, not just via a bare test-client POST — the Confirm submitter is not gated by the Dismiss-only reason field, and that fact is proved by a check that never calls self.client.post()"
    - "The Dismiss submitter is still gated client-side by the required reason field, exactly as UI-SPEC's Copywriting Contract requires — the CR-01 fix exempts only Confirm, it does not delete the Dismiss gate"
    - "Both the confirmation and the undo are attributable to a person and a time across every write path that can create the association — including the standalone CalendarEventMetaAdmin page, which 27.1-02's own docstring calls the primary staff surface for hand-linking a run to an event"
    - "A staff user cannot hand-type or leave blank confirmed_by/confirmed_at on the standalone CalendarEventMeta admin page: the fields do not render as editable widgets, and a submitted value does not bind"
    - "Clearing the run on that page also clears the audit fields, so no row ever displays a confirmation for an association that no longer exists"
    - "The operator runbook states that the reason box gates Dismiss only, and that an admin-created event-to-run link is now stamped automatically"
  artifacts:
    - path: "src/templates/campaigns/attribution_queue.html"
      provides: "Confirm submitters exempted from the Dismiss-only required reason field"
      contains: "formnovalidate"
    - path: "solsys_code/tests/test_attribution_template.py"
      provides: "Template-structure regression tests for CR-01 that do not use self.client.post()"
      contains: "formnovalidate"
      min_lines: 90
    - path: "solsys_code/admin.py"
      provides: "CalendarEventMetaAdmin.save_model stamping and audit-field readonly protection"
      contains: "def save_model"
    - path: "solsys_code/tests/test_admin.py"
      provides: "Standalone-admin audit-stamp regression tests"
      contains: "CalendarEventMetaStandaloneAdminAuditStampTests"
    - path: "docs/runbooks/telescope_runs_calendar.rst"
      provides: "Operator note that the reason box gates Dismiss only, and that the admin link path auto-stamps"
      contains: "required for Dismiss"
  key_links:
    - from: "src/templates/campaigns/attribution_queue.html"
      to: "campaigns:attribution_decide"
      via: "Confirm submitter carrying formnovalidate so the browser sends the request"
      pattern: "value=\"confirm\"[^>]*formnovalidate"
    - from: "solsys_code/admin.py CalendarEventMetaAdmin.save_model"
      to: "request.user / django.utils.timezone.now()"
      via: "server-side stamping on a run-link transition"
      pattern: "obj\\.confirmed_by = request\\.user"
    - from: "solsys_code/tests/test_attribution_template.py"
      to: "src/templates/campaigns/attribution_queue.html"
      via: "template source read through django.template.loader origin, no test client"
      pattern: "get_template"
---

<objective>
Close the two BLOCKER gaps `28-VERIFICATION.md` found (score 6/8), both of which break a
guarantee this phase exists to add and neither of which the 124 passing tests could see.

- **CR-01 (truth #4 failed).** In `attribution_queue.html` the per-candidate `<form>` wraps the
  Confirm button, the free-text `reason` input (which carries HTML5 `required`), and the Dismiss
  button together. HTML5 constraint validation runs for *any* submitter in a form unless that
  submitter opts out, so in a real browser clicking **Confirm** is blocked until the operator
  types something into the "Why doesn't this candidate match?" box — even though the server-side
  confirm handlers never read `reason` at all. Every existing test uses `self.client.post()`,
  which bypasses browser-side constraint validation entirely, so the defect shipped green.

- **CR-02 (truth #6 failed).** `CalendarEventMetaAdmin` — the standalone admin page 27.1-02's own
  `get_readonly_fields()` docstring calls "the primary staff surface for hand-linking a run to an
  event" — never adds `confirmed_by`/`confirmed_at` to `readonly_fields` and has no `save_model()`
  stamping override. A staff user linking a run there can leave both blank (an association with no
  attribution at all) or hand-type an arbitrary user and timestamp (fabricated attribution). The
  inline/`save_formset` path is correct and tested; this second, independent write path onto the
  same row is not.

Purpose: ROADMAP criterion 3 ("explicit per-candidate staff confirmation") is only real if the
rendered Confirm control actually submits, and criterion 4 ("both the confirmation and the undo
are attributable to a person and a time") is only real if it holds on every write path, not just
the one the test suite happens to exercise. 28-01-PLAN.md's own must-have — "an admin-created
event-to-run link is stamped with confirmed_by/confirmed_at, exactly as an admin-created
observation link already is" — was delivered for the inline path only.

Output: the two-character-class template fix plus a new test module whose checks are structurally
incapable of repeating the blind spot, the `CalendarEventMetaAdmin` readonly + `save_model` fix
with a seven-test regression class, the runbook's operator-visible consequences of both, and the
`REQUIREMENTS.md` traceability sync `28-VERIFICATION.md` flagged as stale.

**Do not touch 28-01 through 28-04's PLAN.md/SUMMARY.md files, and do not re-verify the six truths
already marked VERIFIED.** This is an add-on plan against already-executed work.
</objective>

<execution_context>
@/home/tlister/git/fomo_devel/.claude/gsd-core/workflows/execute-plan.md
@/home/tlister/git/fomo_devel/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-operator-assisted-attribution/28-VERIFICATION.md
@.planning/phases/28-operator-assisted-attribution/28-REVIEW.md
@.planning/phases/28-operator-assisted-attribution/28-UI-SPEC.md
@.planning/phases/28-operator-assisted-attribution/28-CONTEXT.md
</context>

## Artifacts this phase produces (28-05 share)

| Kind | Name | Location | New/Modified |
|------|------|----------|--------------|
| Template attribute | `formnovalidate` on both per-candidate Confirm submitters | `src/templates/campaigns/attribution_queue.html` | Modified |
| Admin attribute | `CalendarEventMetaAdmin.readonly_fields = ['confirmed_by', 'confirmed_at']` | `solsys_code/admin.py` | New |
| Admin method | `CalendarEventMetaAdmin.save_model()` | `solsys_code/admin.py` | New |
| Test module | `solsys_code/tests/test_attribution_template.py` | `solsys_code/tests/` | New |
| Test class | `AttributionTemplateSourceTests` (`SimpleTestCase`, no DB, no client) | `solsys_code/tests/test_attribution_template.py` | New |
| Test class | `AttributionRenderedFormStructureTests` | `solsys_code/tests/test_attribution_template.py` | New |
| Test helper | `_FormStructureParser` (HTML form-ownership parser) | `solsys_code/tests/test_attribution_template.py` | New |
| Test class | `CalendarEventMetaStandaloneAdminAuditStampTests` | `solsys_code/tests/test_admin.py` | New |
| Docs prose | "reason box is required for Dismiss only" + admin auto-stamp note | `docs/runbooks/telescope_runs_calendar.rst` | Modified |
| Docs sync | ATTRIB-01/02/05/06 checkbox + traceability rows | `.planning/REQUIREMENTS.md` | Modified |

## Paired-docs scope (CLAUDE.md rule)

- **`docs/runbooks/telescope_runs_calendar.rst` — IN SCOPE, in `files_modified` from the start.**
  CLAUDE.md scopes the runbook rule by directory, not by filename, and both fixes change
  operator-visible documented behavior: the runbook's "What a dismissal means" paragraph says the
  reason is "required" without saying *which button* it gates (CR-01 makes that distinction real
  and load-bearing), and its "Behavior change" paragraph tells operators the admin FK-picker path
  "still exists and remains available" without saying that a link created there is now
  auto-stamped and its audit fields no longer hand-editable (CR-02).
- **No demo notebook.** Neither `src/templates/campaigns/attribution_queue.html` nor
  `solsys_code/admin.py` appears in CLAUDE.md's notebook pairing map, and this plan adds no
  management command, so no notebook is created or regenerated. Recorded here so the omission is
  a decision, not a miss.

<tasks>

<task type="auto">
  <name>Task 1: CR-01 — exempt the Confirm submitter from the Dismiss-only required field, and prove it with checks that cannot use the test client</name>
  <files>src/templates/campaigns/attribution_queue.html, solsys_code/tests/test_attribution_template.py</files>
  <read_first>
    - `src/templates/campaigns/attribution_queue.html` (whole file, ~210 lines — both per-candidate action forms at lines 91-102 and 177-188, the two out-of-line bulk forms at 118-123 and 200-205, and the `form="bulk-confirm-events"` / `form="bulk-confirm-records"` checkbox attributes at lines ~76 and ~162 which make HTML5 form *ownership* non-trivial to parse)
    - `.planning/phases/28-operator-assisted-attribution/28-UI-SPEC.md` Copywriting Contract row "Destructive confirmation — Dismiss" (line ~126): the `required` attribute on `reason` is a **mandated** UI-SPEC control ("submit is disabled until the reason field is non-empty, both client-side (HTML `required`) and server-side"). This is why the fix must exempt the Confirm submitter rather than delete `required`.
    - `.planning/phases/28-operator-assisted-attribution/28-REVIEW.md` CR-01 (lines 60-92) — the exact suggested patch.
    - `solsys_code/tests/test_campaign_attribution_views.py` lines 39-128 (`AttributionViewTestBase`: `setUpTestData`'s campaign/target/observatory/`campaign_run`/`staff_user` fixture and the `_make_event()`/`_make_record()` High-band orphan builders this task reuses; note the documented pitfall that the run attribute is named `campaign_run`, never `run`)
    - `solsys_code/campaign_views.py` `AttributionDecisionView._do_confirm_event` / `_do_confirm_record` — confirm for yourself that neither reads `reason`, which is what makes exempting Confirm safe rather than a behavior change
  </read_first>
  <action>
**1a. The template fix.** In `src/templates/campaigns/attribution_queue.html`, add the boolean
attribute `formnovalidate` to the per-candidate Confirm submit button in **both** action forms —
the events table (currently line 98) and the records table (currently line 184). Each becomes:

`<button type="submit" name="action" value="confirm" class="btn btn-sm btn-success" formnovalidate>Confirm</button>`

Leave `required` on both `name="reason"` inputs exactly as it is, and do not add `novalidate` to
either `<form>` — UI-SPEC's Copywriting Contract makes the client-side `required` the Dismiss
confirmation gate, so a form-level opt-out would delete a mandated control while fixing an
unrelated one. `formnovalidate` is the per-submitter opt-out and is the minimal correct fix.

Add a Django template comment (`{% comment %} ... {% endcomment %}`) immediately above the events
table's action form recording, in prose: that `formnovalidate` is deliberate and load-bearing;
that HTML5 constraint validation runs for any submitter in a form unless that submitter opts out,
so without it the `required` reason input blocks Confirm in a real browser even though
`_do_confirm_event`/`_do_confirm_record` never read `reason`; that `required` must stay for the
Dismiss submitter (UI-SPEC Copywriting Contract) and `_dismiss()` re-checks it server-side anyway;
and that this class of defect is invisible to `self.client.post()`, which is why
`solsys_code/tests/test_attribution_template.py` exists. Reference it as CR-01. The records table's
action form gets a one-line comment pointing at the events table's full comment, matching the
existing "See the events table's identical comment above" convention already used in this file.

**1b. The new test module.** Create `solsys_code/tests/test_attribution_template.py`. Module
docstring must state why this module exists separately from
`test_campaign_attribution_views.py`: every check here asserts **HTML structure**, never request
behavior, because `self.client.post()` bypasses browser-side constraint validation and therefore
cannot detect CR-01's whole class of defect. State plainly that adding a `self.client.post()` test
here would defeat the module's purpose.

Implement, in this order:

*`_FormStructureParser(html.parser.HTMLParser)`* — a small parser that resolves HTML5 form
ownership correctly, since this page deliberately uses out-of-line forms:
  - Track a stack of open `<form>` elements, keyed by their `id` attribute (or a generated
    synthetic key when a form has no `id`).
  - For each `<input>`, `<button>`, `<select>` and `<textarea>` start tag, resolve its owning form
    as: the value of its own `form=` attribute when present, otherwise the innermost currently-open
    `<form>`. (The High-band checkboxes carry `form="bulk-confirm-events"`/`form="bulk-confirm-records"`
    and are therefore owned by the bulk forms, not by the enclosing action form — getting this
    wrong would make the invariant below assert against the wrong controls.)
  - Record, per form: the list of owned controls that carry the `required` attribute, and the list
    of owned submit buttons as `(name, value, has_formnovalidate)` triples. A `<button>` with no
    explicit `type` counts as a submit button (that is the HTML default).
  - Expose the collected mapping as a public attribute so both test classes can assert on it.

*`AttributionTemplateSourceTests(django.test.SimpleTestCase)`* — no database, no test client, no
HTTP at all. Resolve the template's on-disk path with
`django.template.loader.get_template('campaigns/attribution_queue.html').origin.name`, read the
file, and assert:
  - `test_every_confirm_submitter_carries_formnovalidate`: using a regex over the raw source that
    matches `<button ...>` tags containing both `name="action"` and `value="confirm"`, assert
    exactly **2** such buttons are found (one per worklist — so a future third Confirm button
    cannot silently skip the attribute and leave this test green) and that every one of them
    contains the substring `formnovalidate`.
  - `test_dismiss_reason_input_is_still_required`: assert exactly **2** `<input ... name="reason" ...>`
    tags exist and each still contains ` required`, so a future "fix" that deletes UI-SPEC's
    Dismiss gate instead of exempting Confirm fails here.
  - `test_no_form_level_novalidate_opt_out`: assert the literal string `novalidate` never appears
    as a standalone `<form>` attribute (search `<form` tags specifically — `formnovalidate` on a
    button contains the substring `novalidate`, so a naive whole-file substring search is wrong and
    must not be used).

*`AttributionRenderedFormStructureTests(AttributionViewTestBase)`* — imported via
`from solsys_code.tests.test_campaign_attribution_views import AttributionViewTestBase`. (The
runner will re-collect that imported base class in this module; it declares no `test_*` methods, so
this is harmless — do not work around it by duplicating the fixture.) In `setUp`, build at least one
event orphan and one record orphan with `self._make_event()` / `self._make_record()`, log in
`self.staff_user`, and `self.client.get(reverse('campaigns:attribution'))` — a GET that renders the
page, never a POST that simulates a submit. Feed `response.content.decode()` through
`_FormStructureParser` and assert:
  - `test_no_confirm_submitter_is_gated_by_a_required_control`: for every parsed form that owns at
    least one `required` control, every owned submit button whose `value` is not `'dismiss'` carries
    `formnovalidate`. Report the offending form id and button value in the assertion message.
  - `test_the_invariant_was_actually_exercised`: assert the parse found **at least two** forms that
    each own a `required` control **and** a `value="confirm"` submitter (one per worklist). Without
    this, the invariant test above passes vacuously whenever the fixture renders no candidate rows —
    which is exactly the false-confidence failure mode CR-01 already demonstrated once. This
    assertion is not optional.
  - `test_dismiss_submitter_is_still_gated_client_side`: assert every owned submitter with
    `value="dismiss"` does **not** carry `formnovalidate`, so the UI-SPEC Dismiss gate survives.

**1c. Mutation check (mandatory, and record the result in the SUMMARY).** Prove the new tests
actually bind to the defect rather than to incidental strings:
  1. Temporarily remove `formnovalidate` from both Confirm buttons in the template.
  2. Run `python manage.py test solsys_code.tests.test_attribution_template -v 2`.
  3. Confirm that **at least one test in each of the two test classes fails** (the source test and
     the rendered-structure test — a fix that satisfied only one of them would leave half the
     blind spot open).
  4. Restore `formnovalidate` and re-run to green.
Record the observed failure names and counts verbatim in the SUMMARY. If either class stays green
with the attribute removed, that class is not testing what it claims and must be rewritten before
this task is done.

Keep `ruff check .` and `ruff format --check .` clean (120-col, single quotes).
  </action>
  <verify>
    <automated>python manage.py test solsys_code.tests.test_attribution_template -v 2 && grep -v '^\s*#' solsys_code/tests/test_attribution_template.py | grep -c 'self\.client\.post(' | grep -qx '0' && grep -c 'formnovalidate' src/templates/campaigns/attribution_queue.html | grep -qx '2' && ruff check solsys_code/tests/test_attribution_template.py && ruff format --check solsys_code/tests/test_attribution_template.py</automated>
    <automated>python manage.py test solsys_code.tests.test_campaign_attribution_views solsys_code.tests.test_campaign_attribution solsys_code.tests.test_attribution_dismissals -v 1</automated>
  </verify>
  <acceptance_criteria>
    - Both per-candidate Confirm buttons in `attribution_queue.html` carry `formnovalidate`; `grep -c 'formnovalidate' src/templates/campaigns/attribution_queue.html` returns exactly `2`.
    - Both `name="reason"` inputs still carry `required` (the UI-SPEC Dismiss gate was exempted for Confirm, not deleted).
    - No `<form>` tag in the template carries a standalone `novalidate` attribute.
    - `solsys_code/tests/test_attribution_template.py` exists and contains **zero** `self.client.post(` calls outside comments (the grep gate above filters comment lines before counting, so a comment mentioning the string cannot flip the gate).
    - `AttributionTemplateSourceTests` subclasses `django.test.SimpleTestCase` and touches neither the database nor the test client — its evidence is the on-disk template source.
    - `AttributionRenderedFormStructureTests` asserts a non-vacuity guard: at least two parsed forms own both a `required` control and a `value="confirm"` submitter. A run where the fixture renders no candidates must FAIL, not pass.
    - The parser resolves an explicit `form=` attribute as the control's owner in preference to the enclosing `<form>` (the High-band checkboxes depend on this).
    - Mutation check performed and its output recorded in the SUMMARY: with `formnovalidate` removed, at least one test in **each** of the two new classes fails; with it restored, all pass.
    - The three pre-existing attribution test modules still pass unchanged (no test was edited to accommodate the fix).
  </acceptance_criteria>
  <done>A staff member clicking Confirm in a real browser submits the request without typing a dismissal reason; a regression that reintroduces the gate fails a test that does not depend on the Django test client; and Dismiss still refuses to submit without a reason, client-side and server-side.</done>
</task>

<task type="auto">
  <name>Task 2: CR-02 — protect and server-stamp confirmed_by/confirmed_at on the standalone CalendarEventMeta admin page</name>
  <files>solsys_code/admin.py, solsys_code/tests/test_admin.py</files>
  <read_first>
    - `solsys_code/admin.py` lines 1-10 (imports: `admin`, `BaseInlineFormSet`, `django.utils.timezone`, and the `CalendarEventMeta, CampaignRun, CampaignRunObservation` model import — `timezone` is already imported, do not re-import it)
    - `solsys_code/admin.py` lines 74-94 (`CalendarEventMetaInline`: its class docstring naming threat `T-28-02` and its `readonly_fields = ['confirmed_by', 'confirmed_at']` — this is the pattern the standalone page must mirror)
    - `solsys_code/admin.py` lines 226-277 (`CampaignRunAdmin.save_formset`, specifically the `elif isinstance(instance, CalendarEventMeta):` branch: the `prior_run_id` lookup via `CalendarEventMeta.objects.filter(pk=instance.pk).values_list('run_id', flat=True).first()`, its comment about reading the prior DB value before the mutated in-memory instance hides it, and the `prior_run_id is None and instance.run_id is not None` transition condition)
    - `solsys_code/admin.py` lines 279-318 (`CalendarEventMetaAdmin` in full: `list_display`, `autocomplete_fields = ['run']`, `list_select_related`, and `get_readonly_fields()`'s existing `list(super().get_readonly_fields(request, obj))` + append-`event`-on-change body, which is what makes a class-level `readonly_fields` compose cleanly rather than conflict)
    - `solsys_code/tests/test_admin.py` lines 99-170 (`CalendarEventMetaStandaloneAdminPkFreezeTests`: the superuser + `CalendarEvent` + `CalendarEventMeta` fixture shape, the `reverse('admin:solsys_code_calendareventmeta_change', args=[pk])` URL form, and the WR-08 discipline of asserting a 302 **and** a genuinely-changed editable field so "nothing was written" can never be confused with "the write path was never reached")
    - `solsys_code/tests/test_admin.py` lines 338-425 (`test_save_formset_stamps_calendar_event_meta_on_run_transition` and `test_save_formset_does_not_restamp_calendar_event_meta_on_unrelated_edit` — the two assertions the new class must mirror for the standalone page)
    - `solsys_code/campaign_views.py` `_undo_confirmation()`'s event branch: `.update(run=None, confirmed_by=None, confirmed_at=None)` — the established semantics that clearing the link also clears its audit fields
  </read_first>
  <action>
**2a. Freeze the audit fields.** Add a class-level attribute to `CalendarEventMetaAdmin`:

`readonly_fields = ['confirmed_by', 'confirmed_at']`

placed directly after `list_select_related`, with a comment citing CR-02 and D-12 and stating that
this mirrors `CalendarEventMetaInline`'s identical line: because Django's `ModelAdmin` renders every
non-readonly, non-excluded model field as an editable widget, without this a staff user could submit
an arbitrary `confirmed_by` (any `User` through the FK widget) and an arbitrary `confirmed_at`,
fabricating attribution to someone who never made the decision. Note that this applies on **add**
as well as change, which is intentional — `save_model()` below is the only writer.

The existing `get_readonly_fields()` composes with this automatically (it starts from
`list(super().get_readonly_fields(request, obj))`, which now returns the class attribute, then
appends `event` on change). Do **not** hard-code the audit fields inside `get_readonly_fields()`
instead — keeping them class-level is what makes the mirror with the inline literal and greppable.
Extend `get_readonly_fields()`'s docstring with one short paragraph recording that the audit fields
are frozen at class level for the CR-02 reason and that the method's own job remains the `event`
primary-key freeze.

**2b. Stamp on the transition.** Add `save_model(self, request, obj, form, change)` to
`CalendarEventMetaAdmin`, mirroring `CampaignRunAdmin.save_formset`'s `CalendarEventMeta` branch:

  - Read the prior DB value **before** delegating to `super()`:
    `prior_run_id = CalendarEventMeta.objects.filter(pk=obj.pk).values_list('run_id', flat=True).first() if obj.pk is not None else None`.
    Guard on `obj.pk is not None` because `event` is this model's primary key; on the add form the
    pk is populated from the submitted `event`, and `.first()` correctly returns `None` for a row
    that does not exist yet. `save_formset`'s own comment already records that "no row exists yet"
    and "row exists with run=None" both mean "not owned by any run", so the transition condition
    fires identically either way.
  - Apply exactly three branches, then call `super().save_model(request, obj, form, change)`:
    1. `obj.run_id is not None and obj.run_id != prior_run_id` → a new confirmation of this pair:
       `obj.confirmed_by = request.user` and `obj.confirmed_at = timezone.now()`.
    2. `obj.run_id is None and prior_run_id is not None` → the link was cleared:
       `obj.confirmed_by = None` and `obj.confirmed_at = None`.
    3. otherwise (`run` unchanged) → touch neither field, so an unrelated edit such as toggling
       `is_verified` never re-attributes someone else's confirmation.

  Document branches 1 and 2 in the method docstring, because both deliberately go beyond the
  literal inline mirror and the reasoning must not have to be rediscovered:
  - Branch 1 covers a **re-point** (run A → run B), which the inline cannot reach at all: inside
    `CampaignRunAdmin`, every inline row belongs to the parent run, so `save_formset`'s comment
    about not re-stamping a re-pointed row describes a case that surface cannot produce. On this
    page a re-point IS reachable through the `run` autocomplete, and leaving the prior stamp would
    attribute run B's confirmation to whoever confirmed run A — precisely the fabricated-attribution
    failure CR-02 names. State that this is a deliberate, reasoned divergence, not drift.
  - Branch 2 exists because 2a makes the audit fields unwritable by hand: without it, clearing
    `run` would leave a row displaying "confirmed by X at T" for an association that no longer
    exists, with no way for staff to correct it. It matches
    `campaign_views._undo_confirmation()`'s established `run=None, confirmed_by=None, confirmed_at=None`
    semantics on the event side.

**2c. The regression tests.** Add `CalendarEventMetaStandaloneAdminAuditStampTests(TestCase)` to
`solsys_code/tests/test_admin.py`, placed immediately after
`CalendarEventMetaStandaloneAdminPkFreezeTests`. Class docstring must state that this closes
`28-VERIFICATION.md` truth 6 / `28-REVIEW.md` CR-02, and that it exercises the **standalone admin
page** through `self.client`, deliberately distinct from
`test_save_formset_stamps_calendar_event_meta_on_run_transition`, which only covers the
inline/formset path.

`setUpTestData` builds: an acting superuser (`force_login`ed in `setUp`), a second `other_staffer`
superuser used as the fabricated/prior attributor, a `TargetList` campaign, two `CampaignRun`s
(`run_a`, `run_b`, both in that campaign — `telescope_instrument` plus `window_start`/`window_end`
are enough; no `Target` is required, but if one is ever needed use
`tom_targets.tests.factories.NonSiderealTargetFactory` per CLAUDE.md, never `SiderealTargetFactory`),
a `CalendarEvent`, and its unowned `CalendarEventMeta` (`run=None`, `is_verified=True`).

Seven tests:
  1. `test_change_form_does_not_expose_the_audit_fields_as_editable` — GET the change URL; assert
     200 and that neither `name="confirmed_by"` nor `name="confirmed_at"` appears in the content.
  2. `test_add_form_does_not_expose_the_audit_fields_as_editable` — GET
     `admin:solsys_code_calendareventmeta_add`; same two absences (and, as a control that the page
     really rendered, assert `name="event"` IS present, matching the existing pk-freeze class's
     add-form test).
  3. `test_linking_a_run_stamps_the_acting_user_and_a_time` — POST the change form with
     `{'run': str(self.run_a.pk), 'is_verified': '', '_save': 'Save'}`; assert 302, then
     `refresh_from_db()` and assert `run_id == run_a.pk`, `confirmed_by == self.superuser`,
     `confirmed_at` is not None, and `is_verified` is now False (the WR-08 "the write path was
     genuinely reached" control).
  4. `test_hand_typed_audit_values_do_not_bind` — capture `before = timezone.now()`, then POST with
     `run` set **and** `confirmed_by` = `other_staffer.pk` and `confirmed_at` = a fixed past
     timestamp string such as `'2020-01-01 00:00:00'`; assert 302, `confirmed_by == self.superuser`
     (not `other_staffer`) and `confirmed_at >= before` (not the fabricated 2020 value).
  5. `test_unrelated_edit_does_not_restamp` — start from a row already linked to `run_a` with
     `confirmed_by=other_staffer` and a fixed `original_confirmed_at`; POST resubmitting the same
     `run` while toggling `is_verified`; assert `confirmed_by`/`confirmed_at` are unchanged and
     `is_verified` did change.
  6. `test_clearing_the_run_clears_the_audit_fields` — from the same already-linked starting state,
     POST with `run` empty (`''`); assert `run_id is None`, `confirmed_by is None`,
     `confirmed_at is None`.
  7. `test_repointing_the_run_restamps_to_the_acting_user` — from a row linked to `run_a` with
     `confirmed_by=other_staffer` and `original_confirmed_at`, POST with `run = run_b.pk`; assert
     `run_id == run_b.pk`, `confirmed_by == self.superuser`, and `confirmed_at > original_confirmed_at`.

Tests 5-7 each need their own starting state; set it up per-test (a helper that creates or updates
the row in place is fine) rather than mutating shared `setUpTestData` state across tests.

Keep `ruff check .` and `ruff format --check .` clean.
  </action>
  <verify>
    <automated>python manage.py test solsys_code.tests.test_admin -v 2</automated>
    <automated>python manage.py makemigrations --check --dry-run solsys_code && ruff check solsys_code/admin.py solsys_code/tests/test_admin.py && ruff format --check solsys_code/admin.py solsys_code/tests/test_admin.py</automated>
  </verify>
  <acceptance_criteria>
    - `CalendarEventMetaAdmin` has a class-level `readonly_fields = ['confirmed_by', 'confirmed_at']`; `grep -n "readonly_fields = \['confirmed_by', 'confirmed_at'\]" solsys_code/admin.py` returns **three** matches (the two existing inlines plus the new standalone admin).
    - `CalendarEventMetaAdmin.save_model` exists, reads `prior_run_id` from the database before calling `super().save_model(...)`, and sets `obj.confirmed_by = request.user` / `obj.confirmed_at = timezone.now()` only on a genuine run change to a non-None value.
    - Clearing `run` sets both audit fields to `None`; an edit that leaves `run` unchanged touches neither.
    - All seven tests in `CalendarEventMetaStandaloneAdminAuditStampTests` pass, and the pre-existing `CalendarEventMetaStandaloneAdminPkFreezeTests` and `CampaignRunAdminInlinesTests` still pass unchanged.
    - Test 4 proves non-binding by value comparison (`confirmed_by == acting superuser`, `confirmed_at >= before`), not merely by asserting the fields are non-null — a null-only assertion would pass even if the fabricated values had bound.
    - Tests 3-7 each assert a genuinely-changed editable field or a 302 alongside the audit assertions, so "the freeze held" is never confusable with "the write path was never reached".
    - `python manage.py makemigrations --check --dry-run solsys_code` reports no changes (this task adds no model fields).
    - `save_model`'s docstring records the re-point and clear branches as deliberate divergences from the inline, with the reason for each.
  </acceptance_criteria>
  <done>A staff user linking a run through `/admin/solsys_code/calendareventmeta/<pk>/change/` produces a row stamped with their own identity and a server-side timestamp; they cannot leave the attribution blank, cannot fabricate it, and cannot leave a stale stamp behind on a cleared link.</done>
</task>

<task type="auto">
  <name>Task 3: Runbook consequences of both fixes, and the REQUIREMENTS.md traceability sync</name>
  <files>docs/runbooks/telescope_runs_calendar.rst, .planning/REQUIREMENTS.md</files>
  <read_first>
    - `docs/runbooks/telescope_runs_calendar.rst` lines 184-262 (the whole "How do I attribute existing calendar events and observation records to a run?" section — in particular the "What a dismissal means" paragraph at ~230-237, which says the reason is "required" without naming which button it gates, and the closing "**Behavior change:**" paragraph at ~255-261, which tells operators the admin FK-picker path "still exists and remains available")
    - `.planning/REQUIREMENTS.md` lines 45-52 (the ATTRIB checklist: 01/02/05/06 unchecked, 03/04 checked) and lines 105-110 (the traceability table rows: ATTRIB-01/02/05/06 "Pending", 03/04 "Complete")
    - `.planning/phases/28-operator-assisted-attribution/28-VERIFICATION.md` lines 88-99 (the Requirements Coverage table and the "Note on REQUIREMENTS.md staleness", which is the authority for this sync — all six are delivered; the checklist simply was not updated)
    - `CLAUDE.md` "Paired docs are part of the deliverable" (the directory-scoped `docs/runbooks/` rule)
  </read_first>
  <action>
**3a. Runbook — the reason box gates Dismiss only.** In the "What a dismissal means" paragraph, after
the existing sentence ending "...from a required free-text reason.", add one sentence in the
runbook's existing operator voice making the CR-01 contract explicit and durable. Content (adapt the
wording to the surrounding prose, keep it implementation-free — do not name `formnovalidate` or
"HTML5 constraint validation" in operator-facing text): the reason box is **required for Dismiss
only**; clicking Confirm on the same row never asks for one, because a confirmation records the pair
itself rather than a rejection of it. The literal phrase `required for Dismiss` must appear, so the
must-have `contains` check binds to the sentence rather than to incidental prose.

**3b. Runbook — the admin path now stamps who and when.** Extend the closing "**Behavior change:**"
paragraph with one or two sentences: a run-to-event link created through the Django admin's
`CalendarEventMeta` page is now stamped automatically with the staff member who saved it and the
time they saved it; those two fields are no longer editable by hand on that page, and clearing the
run clears them too — so an association created through either surface is attributable, not only one
created through the attribution queue. Frame it as what an operator will observe, not as a code
change.

Do not restructure or renumber the section, and do not touch any other runbook section.

**3c. REQUIREMENTS.md sync.** `28-VERIFICATION.md`'s Requirements Coverage table finds all six ATTRIB
requirements satisfied; the checklist and traceability table were simply never updated after Phase 28
completed. With Tasks 1 and 2 of this plan landing the two remaining delivery gaps (ATTRIB-03's
confirm-UI path and ATTRIB-04's attributability), all six are now genuinely complete. Therefore:
  - In the Attribution checklist (lines ~47-52), change `- [ ]` to `- [x]` for **ATTRIB-01,
    ATTRIB-02, ATTRIB-05 and ATTRIB-06**. ATTRIB-03 and ATTRIB-04 are already `[x]`; leave them.
  - In the traceability table (lines ~105-110), change the Status cell from `Pending` to `Complete`
    for those same four rows. ATTRIB-03/ATTRIB-04 already read `Complete`; leave them.
  - Change nothing else in the file — no RECON rows, no v2/v2.3 sections, no wording edits to the
    requirement text itself.

Keep `docs` building: the runbook is reStructuredText and pre-commit runs a Sphinx build, so match
the surrounding paragraph style exactly (no new directives, no new headings, `**bold**` used the same
way the surrounding text already uses it).
  </action>
  <verify>
    <automated>grep -q 'required for Dismiss' docs/runbooks/telescope_runs_calendar.rst && grep -c '^- \[ \] \*\*ATTRIB-' .planning/REQUIREMENTS.md | grep -qx '0' && grep -c '| ATTRIB-0[1-6] | Phase 28 — Operator-Assisted Attribution | Complete |' .planning/REQUIREMENTS.md | grep -qx '6'</automated>
    <automated>python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `docs/runbooks/telescope_runs_calendar.rst` contains the literal phrase `required for Dismiss` inside the "What a dismissal means" paragraph, and states that Confirm never asks for a reason.
    - The runbook's "Behavior change" paragraph states that an admin-created run-to-event link is stamped automatically and that the audit fields are no longer hand-editable there.
    - No operator-facing runbook sentence names `formnovalidate`, `save_model`, `readonly_fields`, or "HTML5 constraint validation" — the runbook describes observable behavior, not implementation.
    - `.planning/REQUIREMENTS.md` has zero unchecked `- [ ] **ATTRIB-` entries remaining.
    - All six ATTRIB rows in the traceability table read `Complete`.
    - No line outside the ATTRIB checklist block and the six ATTRIB traceability rows is modified in `REQUIREMENTS.md` (verify with `git diff --stat` and by reading the diff before committing).
    - The Sphinx docs build in pre-commit passes (the pytest run above includes the `docs` testpath per `pyproject.toml`).
  </acceptance_criteria>
  <done>An operator reading the runbook learns that Confirm never asks for a reason and that the admin link path now records who and when; the requirements checklist matches what Phase 28 actually delivered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → `campaigns:attribution_decide` | Staff-supplied `action`/`kind`/`orphan_pk`/`run_pk`/`reason` cross here; the server re-derives eligibility via `is_offered_candidate()` and never trusts a rendered row |
| Browser → Django admin change/add form | Staff-supplied model field values cross here, including (before this plan) `confirmed_by`/`confirmed_at` — the attribution audit record itself |
| Django admin session → `CalendarEventMeta` row | The audit fields are the record of *who decided what, when*; anything that can write them by hand destroys their evidentiary value |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28.5-01 | Spoofing | `CalendarEventMetaAdmin` change/add form | mitigate | Task 2a: `readonly_fields = ['confirmed_by', 'confirmed_at']` drops both fields from the generated form, so a POSTed `confirmed_by` naming another `User` is never bound. Test 4 asserts the acting superuser wins over a submitted `other_staffer` pk. |
| T-28.5-02 | Repudiation | `CalendarEventMetaAdmin.save_model` | mitigate | Task 2b: `confirmed_by=request.user` / `confirmed_at=timezone.now()` stamped server-side on every run-link transition, so no association created through this surface can exist without attribution. Closes VERIFICATION truth 6. |
| T-28.5-03 | Tampering | Stale audit stamp on a cleared or re-pointed link | mitigate | Task 2b branches 1 and 2: a re-point re-stamps to the acting user, a clear nulls both fields — so a row can never display a confirmation for an association it no longer holds. Matches `campaign_views._undo_confirmation()`'s event-side semantics. |
| T-28.5-04 | Denial of service (of the control itself) | `attribution_queue.html` Confirm submitter | mitigate | Task 1a: `formnovalidate` on the Confirm submitter only. The primary "explicit per-candidate confirmation" control (ROADMAP criterion 3) is currently unusable in a browser; a control nobody can operate is a failed control, not a cosmetic bug. |
| T-28.5-05 | Tampering | Dismissal without a stated reason | accept (already mitigated, must not regress) | `required` stays on both `reason` inputs (UI-SPEC Copywriting Contract) and `_dismiss()` independently refuses an empty reason server-side. Task 1b's `test_dismiss_reason_input_is_still_required` and `test_dismiss_submitter_is_still_gated_client_side` pin both halves so the CR-01 fix cannot silently widen into deleting this gate. |
| T-28.5-06 | Elevation of privilege | Django admin surface reachability | accept | Access to `/admin/solsys_code/calendareventmeta/` already requires `is_staff` plus the per-model change permission; this plan changes what a permitted user may write, not who is permitted. No new surface is exposed. |
| T-28.5-07 | Information disclosure | Rendered template / test fixtures | accept | Neither task renders new data. The new tests create users with throwaway credentials in the test database only; no PII is added to any rendered surface. |
| T-28.5-SC | Tampering | npm/pip/cargo installs | accept — not applicable | This plan installs **no** packages: the fix uses one HTML5 attribute, `django.contrib.admin`, `django.utils.timezone` and stdlib `html.parser`, all already present. No Package Legitimacy Audit is required because there are no install tasks. |
</threat_model>

<verification>
Run in order after all three tasks:

1. `python manage.py test solsys_code.tests.test_attribution_template solsys_code.tests.test_admin solsys_code.tests.test_campaign_attribution solsys_code.tests.test_campaign_attribution_views solsys_code.tests.test_attribution_dismissals -v 1` — the four pre-existing phase-28 modules (124 tests) plus the new one, all green, with no pre-existing test edited to accommodate a fix.
2. `python manage.py makemigrations --check --dry-run solsys_code` — "No changes detected".
3. `ruff check .` and `ruff format --check .` — clean.
4. `python -m pytest -q` — the packaging/docs suite, including the Sphinx docs build over the edited runbook.
5. Mutation check re-confirmed and recorded: removing `formnovalidate` from the template fails at least one test in **each** of the two new test classes.
</verification>

<success_criteria>
- `28-VERIFICATION.md` truth 4 is achievable: the rendered Confirm control submits in a real browser without a dismissal reason, and a regression fails a test that never calls `self.client.post()`.
- `28-VERIFICATION.md` truth 6 is achievable: every write path that can create an event-to-run association — the attribution queue, the `CampaignRunAdmin` inline, and the standalone `CalendarEventMetaAdmin` page — stamps `confirmed_by`/`confirmed_at` server-side and refuses hand-typed values.
- The Dismiss gate that UI-SPEC mandates survives both client-side and server-side, pinned by tests.
- `docs/runbooks/telescope_runs_calendar.rst` describes both operator-visible consequences.
- `.planning/REQUIREMENTS.md` no longer contradicts `ROADMAP.md` on ATTRIB-01/02/05/06.
- No file belonging to plans 28-01 through 28-04 (`*-PLAN.md`, `*-SUMMARY.md`) is modified.
</success_criteria>

<output>
Create `.planning/phases/28-operator-assisted-attribution/28-05-SUMMARY.md` when done.

The SUMMARY **must** record, verbatim:
- The mutation-check output from Task 1c (which tests failed with `formnovalidate` removed, in each of the two classes).
- The `save_model` branch decisions from Task 2b (re-point re-stamps; clear nulls) and the reason each diverges from `save_formset`'s comment, so the next reader does not "correct" them back.
</output>
