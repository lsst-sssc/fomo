---
phase: 33-series-identity-reconciler-inversion
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - src/fomo/settings.py
  - docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb
  - docs/notebooks/pre_executed/campaign_lifecycle_demo.ipynb
autonomous: true
gap_closure: true
requirements: [ANNOT-01, ANNOT-02]

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Executing either pre-executed demo notebook end to end leaves `src/fomo_db.sqlite3` byte-identical: the notebook copies the developer database to a scratch file first and every write it makes lands in that copy (UAT G-33-4, 33-05 P2 / 33-08 P4)."
    - "Each notebook proves where it is writing in its own committed output: the setup cell prints the resolved `settings.DATABASES['default']['NAME']` and asserts it is the scratch copy, not `src/fomo_db.sqlite3`."
    - "`src/fomo/settings.py` resolves the sqlite file from the `FOMO_DATABASE_PATH` environment variable when it is set and non-empty, and from `os.path.join(BASE_DIR, 'fomo_db.sqlite3')` otherwise — an unset variable leaves every existing command, test run and dev server pointed exactly where it is today."
    - "`campaign_lifecycle_demo.ipynb`'s public-campaign-table cell prints only the run's primary key, telescope/instrument and approval status — the two contact fields are gone from both the code and the committed output (UAT G-33-4, 33-05 P1 / 33-08 P5)."
    - "The developer database holds no residue from the two demo notebooks: no `CalendarEvent`, `CampaignRun` or `TargetList` row belonging to 'Reconciler Demo Campaign' or 'Campaign Lifecycle Demo', no event keyed in the lifecycle demo's legacy url namespace, and event pk 335 (`RUN:59:2026-09-02`, detached from run 59 and offered in the staff attribution queue as a HIGH-band candidate) no longer exists."
    - "Every notebook markdown cell that told the reader the notebook writes to the real developer database now describes the scratch-copy mechanism instead, so the prose and the executed behaviour agree."
  artifacts:
    - src/fomo/settings.py
    - docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb
    - docs/notebooks/pre_executed/campaign_lifecycle_demo.ipynb
  key_links:
    - "notebook setup cell -> `FOMO_DATABASE_PATH` in `os.environ` -> `src/fomo/settings.py` `DATABASES['default']['NAME']` -> the scratch sqlite copy. The variable must be set BEFORE `django.setup()`; set afterwards it reaches nothing."
    - "`shutil.copy2(src/fomo_db.sqlite3, scratch)` -> every notebook write -> teardown `shutil.rmtree(scratch_dir)`. The copy is what makes the developer database read-only for the whole notebook run."
    - "This plan's scratch-database mechanism -> plan 33-10's notebook re-executions. 33-10 re-runs both notebooks against the post-fix reconciler; without this plan landing first that re-run re-seeds exactly the residue task 3 cleans."
  prohibitions:
    - "No notebook cell may print or store a run's `contact_person`, `contact_email` or `source` into committed output (33-05 P1 / 33-08 P5, still in force; this plan closes the standing breach)."
    - "No notebook cell may delete a row it did not create earlier in the same notebook run, nor leave demo rows behind outside its own demo-scoped reset (33-05 P2 / 33-08 P4, still in force)."
    - "The `FOMO_DATABASE_PATH` branch must never change the default: with the variable unset or empty, `DATABASES['default']['NAME']` must resolve to the same path it resolves to today (new, this plan)."
    - "The residue cleanup must never delete a row outside the two demo namespaces — no non-demo `CampaignRun`, `CalendarEvent`, `TargetList`, `Observatory` or `User` row may be removed (new, this plan)."
    - "No new `*.md` or README file may be created; only the files listed in `files_modified` are edited (CLAUDE.md, still in force)."
---

<objective>
Close UAT gap G-33-4: make the two pre-executed demo notebooks execute against a scratch copy
of the developer database instead of writing through to `src/fomo_db.sqlite3`, drop the two
contact fields from the lifecycle notebook's committed output, and clean the residue the
earlier executions left behind.

Purpose: both notebooks call `django.setup()` against `src.fomo.settings`, whose `DATABASES`
points at `src/fomo_db.sqlite3`, so `jupyter nbconvert --execute` writes through to the real
developer database. The observable damage today: event pk 335 (`RUN:59:2026-09-02`) sits
detached from run 59 and is offered in the staff attribution queue as a HIGH-band candidate,
and 19 blank-proposal September-2026 demo events clutter the month view under the "Classical
schedule" legend slot. The user reviewed both flagged prohibitions and decided to fix rather
than accept them (33-UAT.md `## Decisions`, test 4, 2026-09-09).

Output: an environment-variable branch in `src/fomo/settings.py`, a scratch-copy setup and
teardown in both notebooks with the resolved database path printed in committed output, a
contact-field-free public-table cell, and a developer database with the demo rows gone.

**Sequencing note.** This plan is deliberately first. Plan 33-10 changes reconciler and staff-
action behaviour and must re-execute both notebooks to show it; run that re-execution before
this mechanism exists and it re-seeds the residue task 3 removes.

**Paired docs (CLAUDE.md rule).** Both notebooks named by the rule are in `files_modified` and
are re-executed here. No page under `docs/runbooks/` documents how a notebook resolves its
database, so no runbook page's documented behaviour is affected by this plan; the runbook prose
for this gap-closure run is owned by plan 33-10 (which mirrors 33-08's ownership note).
</objective>

<execution_context>
@/home/tlister/git/fomo_devel/.claude/gsd-core/workflows/execute-plan.md
@/home/tlister/git/fomo_devel/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-series-identity-reconciler-inversion/33-UAT.md
@.planning/phases/33-series-identity-reconciler-inversion/33-VERIFICATION.md
@.planning/phases/33-series-identity-reconciler-inversion/33-05-SUMMARY.md
@.planning/phases/33-series-identity-reconciler-inversion/33-08-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Notebooks execute against a scratch copy of the developer database</name>
  <precondition>`src/fomo_db.sqlite3` exists in the working tree (it is gitignored and untracked, so a fresh clone will not have one) and `jupyter nbconvert` runs in the active environment.</precondition>
  <reversibility rating="reversible">One `os.getenv` branch in `settings.py` plus two notebook cells; removing them restores today's behaviour exactly.</reversibility>
  <files>src/fomo/settings.py, docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb, docs/notebooks/pre_executed/campaign_lifecycle_demo.ipynb</files>
  <read_first>
    - `src/fomo/settings.py` lines 15-20 — `BASE_DIR = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))`, which resolves to the repo's `src/` directory, and the `import os` already at the top
    - `src/fomo/settings.py` lines 124-131 — the `DATABASES` dict this task edits, currently `'NAME': os.path.join(BASE_DIR, 'fomo_db.sqlite3')`
    - `src/fomo/settings.py` last 15 lines — the `from fomo.local_settings import *` guard and the note that `local_settings.py` can only ASSIGN new settings; `src/fomo/local_settings.py` is gitignored, so the branch must live in the tracked `settings.py`
    - `docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb` cell 2 (the Django setup code cell) — the `repo_root_path = Path.cwd().resolve().parents[2]` discovery, the `manage.py` existence guard, the two `os.environ.setdefault` calls and `django.setup()`
    - the same notebook's cell 1 (markdown, "## Django setup") and cell 18 (markdown, "## The skip rule…"), whose opening sentence currently states in bold that the notebook runs against the real developer database
    - `docs/notebooks/pre_executed/campaign_lifecycle_demo.ipynb` cell 2 (Django setup, including the `django.test.utils.setup_test_environment()` block) and cell 5 (markdown, "## Demo-scoped reset"), plus cell 6's reset code, which names `DEMO_CAMPAIGN_NAME` and `DEMO_LEGACY_EVENT_URL_PREFIX`
    - `.pre-commit-config.yaml` lines 14-22 — the `jupyter-nb-clear-output` hook and its `exclude: ^docs/notebooks/pre_executed`, which is why these two notebooks are committed WITH output; and the `ruff-format` hook's `types_or: [python, pyi, jupyter]`, which formats notebook code cells
    - `CLAUDE.md` §"Conventions" — the paired-docs rule and the notebook regeneration command
  </read_first>
  <action>
    In `src/fomo/settings.py`, resolve the sqlite file through an environment variable with the
    existing path as the fallback: `'NAME'` becomes `os.getenv('FOMO_DATABASE_PATH') or
    os.path.join(BASE_DIR, 'fomo_db.sqlite3')`. Use `or` rather than a two-argument `os.getenv`
    so an empty-string value also falls back. Add a short comment above the dict stating that
    `FOMO_DATABASE_PATH` exists so the pre-executed demo notebooks under
    `docs/notebooks/pre_executed/` can run against a throwaway copy of the developer database
    instead of writing through to it (UAT G-33-4), that an unset variable leaves every command,
    test run and dev server pointed at `src/fomo_db.sqlite3` exactly as before, and that
    production overrides still belong in `local_settings.py`.

    In BOTH notebooks' Django setup code cell, insert the scratch-copy block between the
    `os.environ.setdefault('DJANGO_ALLOW_ASYNC_UNSAFE', 'true')` line and the `django.setup()`
    call — the variable must be exported before Django reads settings, because `django.setup()`
    is what materialises `DATABASES`. The block: import `shutil` and `tempfile` alongside the
    existing `os`/`sys`/`pathlib` imports; build `dev_db_path = repo_root_path / 'src' /
    'fomo_db.sqlite3'`; raise a `RuntimeError` naming that path when it does not exist; create
    `scratch_db_dir = Path(tempfile.mkdtemp(prefix='fomo-notebook-db-'))`; copy the developer
    database to `scratch_db_path = scratch_db_dir / 'fomo_db.sqlite3'` with `shutil.copy2`; and
    set `os.environ['FOMO_DATABASE_PATH'] = str(scratch_db_path)` — a plain assignment, not
    `setdefault`, so a stale value from an earlier run in the same kernel cannot win.

    After `django.setup()` in the same cell, import `settings` from `django.conf`, print the
    resolved `settings.DATABASES['default']['NAME']` next to the existing "Django ready" line,
    and assert it equals `str(scratch_db_path)` with a message saying the notebook must never
    write to the developer database. That assertion is the proof carried in committed output.

    Add a final code cell to each notebook, titled by a preceding markdown cell "## Scratch
    database teardown", that removes `scratch_db_dir` with `shutil.rmtree(..., ignore_errors=True)`
    and prints the removed path plus a one-line restatement that the developer database was never
    opened for writing. In `campaign_lifecycle_demo.ipynb` place both cells after the existing
    "## Summary" markdown cell.

    Update the prose that the mechanism falsifies: the reconcile notebook's "## Django setup"
    markdown cell gains a sentence describing the scratch copy; its "## The skip rule…" markdown
    cell's opening bold sentence is rewritten from "this notebook runs against the real developer
    database" to state that the notebook runs against a throwaway copy made in the setup cell, so
    every row it creates or removes dies with that copy — keep the rest of that cell, including
    the local-noon anchor paragraph, unchanged. In the lifecycle notebook, the "## Demo-scoped
    reset" markdown cell gains a sentence that the reset now runs inside the scratch copy and
    exists so the notebook is re-runnable, not to protect a shared database.

    Do not change the `parents[2]` repo-root discovery, the `DJANGO_SETTINGS_MODULE` value, the
    `DJANGO_ALLOW_ASYNC_UNSAFE` opt-in, or `setup_test_environment()`. Keep notebook code cells
    ruff-format clean (single quotes, 120 columns) — the `ruff-format` hook covers jupyter files.

    Re-execute BOTH notebooks with `jupyter nbconvert --to notebook --execute --inplace` and
    commit them with output. nbconvert runs the kernel with the notebook's own directory as the
    working directory, which is what `parents[2]` expects; if the setup cell raises the
    "No manage.py at …" RuntimeError, re-run with the working directory set to
    `docs/notebooks/pre_executed`.
  </action>
  <acceptance_criteria>
    - `src/fomo/settings.py` contains `os.getenv('FOMO_DATABASE_PATH')` inside the `DATABASES` dict, and `FOMO_DATABASE_PATH=/tmp/fomo-settings-probe.sqlite3 python manage.py shell -c "from django.conf import settings; print(settings.DATABASES['default']['NAME'])"` prints `/tmp/fomo-settings-probe.sqlite3`
    - With the variable unset, the same command prints a path ending in `src/fomo_db.sqlite3`
    - Executing `docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb` and then `docs/notebooks/pre_executed/campaign_lifecycle_demo.ipynb` leaves the md5 of `src/fomo_db.sqlite3` unchanged across both runs
    - Every code cell in both notebooks carries output in the committed file, and the setup cell's output names a path under a `fomo-notebook-db-` temporary directory
    - The teardown cell's output names the removed scratch directory
    - `grep -c 'real developer database' docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb` outputs 0
  </acceptance_criteria>
  <verify>
    <automated>FOMO_DATABASE_PATH=/tmp/fomo-settings-probe.sqlite3 python manage.py shell -c "from django.conf import settings; print(settings.DATABASES['default']['NAME'])" | grep -q '^/tmp/fomo-settings-probe.sqlite3$' && python manage.py shell -c "from django.conf import settings; print(settings.DATABASES['default']['NAME'])" | grep -q 'src/fomo_db.sqlite3$' && echo SETTINGS_BRANCH_OK</automated>
    <fails_when>the line SETTINGS_BRANCH_OK is absent from the output, or the command exits non-zero — either the override was ignored or the default path moved</fails_when>
    <automated>BEFORE=$(md5sum src/fomo_db.sqlite3 | cut -d' ' -f1); jupyter nbconvert --to notebook --execute --inplace docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb && jupyter nbconvert --to notebook --execute --inplace docs/notebooks/pre_executed/campaign_lifecycle_demo.ipynb && AFTER=$(md5sum src/fomo_db.sqlite3 | cut -d' ' -f1) && [ "$BEFORE" = "$AFTER" ] && echo DEV_DB_UNCHANGED</automated>
    <fails_when>the line DEV_DB_UNCHANGED is absent, or nbconvert reports a `CellExecutionError`, or the command exits non-zero — the developer database changed or a notebook cell raised</fails_when>
  </verify>
  <done>Both notebooks execute end to end against a scratch copy, print and assert the resolved database path in committed output, tear the copy down, and leave `src/fomo_db.sqlite3` byte-identical; with `FOMO_DATABASE_PATH` unset nothing about the project's database resolution changes.</done>
</task>

<task type="auto">
  <name>Task 2: Drop the two contact fields from the lifecycle notebook's public-table output</name>
  <files>docs/notebooks/pre_executed/campaign_lifecycle_demo.ipynb</files>
  <read_first>
    - `docs/notebooks/pre_executed/campaign_lifecycle_demo.ipynb` cell 36 (the "## The public campaign table" code cell) — the `for row in table.page.object_list` loop whose f-string prints `pk`, `telescope_instrument`, `approval_status`, `contact_person` and `contact_email`, and the `visible_pks` assertion below it that must keep working unchanged
    - the same notebook's cell 35 (markdown, "## The public campaign table") — the prose that introduces what the cell prints
    - `.planning/phases/33-series-identity-reconciler-inversion/33-VERIFICATION.md` `flagged_prohibitions` — the second entry, which records that the printed values are all the empty string (no contact data was ever committed) and that the field names themselves are the breach
    - `.planning/phases/33-series-identity-reconciler-inversion/33-UAT.md` `## Decisions`, test 4 — the user's "fix both" decision
  </read_first>
  <action>
    In the public-campaign-table code cell, remove `contact_person` and `contact_email` from the
    per-row f-string so each line prints only `pk`, `telescope_instrument` and `approval_status`.
    Leave the `table = response.context['table']` lookup, the `visible_pks` set comprehension, the
    five-run equality assertion and its explanatory comment exactly as they are — the cell's
    purpose is the public-visibility contract, not the contact fields.

    Add a short comment directly above the print loop stating that contact fields are never
    printed into committed notebook output (33-05 P1 / 33-08 P5) even when they are empty, so a
    future edit does not reintroduce them.

    Update the markdown cell above it if it enumerates the printed columns, so the prose matches
    the three remaining fields.

    Re-execute the notebook with `jupyter nbconvert --to notebook --execute --inplace` (it will
    run against the scratch copy from task 1) and commit it with output.
  </action>
  <acceptance_criteria>
    - `grep -c 'contact_person' docs/notebooks/pre_executed/campaign_lifecycle_demo.ipynb` outputs 0
    - `grep -c 'contact_email' docs/notebooks/pre_executed/campaign_lifecycle_demo.ipynb` outputs 0
    - The public-campaign-table cell still asserts the five expected run primary keys and still carries executed output listing five rows
    - Every code cell in the notebook carries output in the committed file
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -c 'contact_person\|contact_email' docs/notebooks/pre_executed/campaign_lifecycle_demo.ipynb)" = "0" && python -c "import json,sys; nb=json.load(open('docs/notebooks/pre_executed/campaign_lifecycle_demo.ipynb')); code=[c for c in nb['cells'] if c['cell_type']=='code']; missing=[i for i,c in enumerate(code) if not c.get('outputs')]; print('code cells', len(code), 'without output', missing); sys.exit(1 if missing else 0)"</automated>
    <fails_when>non-zero exit, or the printed `without output` list is non-empty — either a contact field survived or a cell was committed unexecuted</fails_when>
  </verify>
  <done>The lifecycle notebook neither names nor prints a run's contact fields anywhere in its source or its committed output, and its public-table assertion still passes with real executed output.</done>
</task>

<task type="auto">
  <name>Task 3: Clean the demo residue out of the developer database</name>
  <precondition>`src/fomo_db.sqlite3` exists in the working tree; it is gitignored and untracked, so this task's effect is local and cannot be recorded in a commit.</precondition>
  <reversibility rating="costly">Rows are deleted from an untracked local sqlite file; the pre-cleanup backup copy taken outside the repo is the only undo path.</reversibility>
  <files>src/fomo_db.sqlite3 (untracked, local only — no committed diff; record the observed before/after counts in the SUMMARY)</files>
  <read_first>
    - `.planning/phases/33-series-identity-reconciler-inversion/33-UAT.md` gap `G-33-4` — its `missing:` list, including "re-attach or delete pk 335 as appropriate", and test 4's note naming the observed residue
    - `docs/notebooks/pre_executed/campaign_lifecycle_demo.ipynb` cell 6 — the demo-scoped reset, which is the established definition of "this notebook's own rows": `DEMO_CAMPAIGN_NAME = 'Campaign Lifecycle Demo'` and `DEMO_LEGACY_EVENT_URL_PREFIX = 'LEGACY:campaign-lifecycle-demo:'`
    - `docs/notebooks/pre_executed/campaign_lifecycle_demo.ipynb` cell 4 (`DEMO_STAFF_USERNAME = 'campaign_lifecycle_demo_staff'`), cell 8 (obscodes `Y21`, `Y22`, `Y23`) and cell 32 (`DEMO_OBSERVATION_TARGET_NAME`, `DEMO_OBSERVATION_GROUP_NAME`, observation id `campaign-lifecycle-demo-obs-1`)
    - `docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb` cell 4 — obscodes `X29`/`X30` and `TargetList` name `'Reconciler Demo Campaign'`
    - `solsys_code/models.py` — `CampaignRun`'s `pre_delete` cascade (it removes the run's own writable events) and `CalendarEventMeta`'s `on_delete` behaviour, so the deletion order below is safe
    - `.gitignore` line 64 — `fomo_db.sqlite3`, which is why the backup copy must live OUTSIDE the repository tree (a `.bak` suffix inside `src/` would not be ignored and could be committed by accident)
  </read_first>
  <action>
    Before deleting anything, copy `src/fomo_db.sqlite3` to a directory outside the repository
    (for example one created with `mktemp -d`) and record that path in the SUMMARY as the undo
    path. Then record the "before" counts named in the acceptance criteria.

    Delete, through `python manage.py shell -c` (never `./manage.py`), exactly these rows and
    nothing else, in this order:

    1. `CampaignRun` rows whose `campaign__name` is 'Reconciler Demo Campaign' or 'Campaign
       Lifecycle Demo' — the model's own pre-delete cascade takes their writable events with them.
    2. `CalendarEvent` rows still pointing at either demo campaign through `target_list__name`.
       This is what removes event pk 335 (`RUN:59:2026-09-02`, currently detached from run 59 and
       offered as a HIGH-band attribution candidate). **The decision for pk 335 is DELETE, not
       re-attach:** it is a reconciler-minted entry for a demo campaign that is itself being
       removed, so re-attaching it to run 59 would preserve a row whose run is about to disappear.
    3. `CalendarEvent` rows whose `url` starts with the lifecycle demo's legacy prefix
       `LEGACY:campaign-lifecycle-demo:`.
    4. The two `TargetList` rows named 'Reconciler Demo Campaign' and 'Campaign Lifecycle Demo'.
    5. `ObservationRecord` with `observation_id='campaign-lifecycle-demo-obs-1'`, `ObservationGroup`
       named 'Campaign Lifecycle Demo Group', and the `Target` named 'Campaign Lifecycle Demo
       Series-Identity Target'.
    6. `Observatory` rows with obscode X29, X30, Y21, Y22 or Y23 — but only those no surviving
       `CampaignRun` still references. Leave any that are still referenced and say so in the SUMMARY.
    7. The `User` named 'campaign_lifecycle_demo_staff' — but only if no surviving
       `CalendarEventMeta` row still names it as `confirmed_by`. Leave it and say so otherwise.

    Do not touch any other row. Do not run a bare `.delete()` on an unfiltered queryset. Do not
    delete rows from the two demo namespaces' *future* runs — the notebooks now write to a scratch
    copy, so nothing new arrives here.

    Record in the SUMMARY: the backup path, the before/after counts for each category, and which
    of steps 6 and 7 were skipped because a reference survived.
  </action>
  <acceptance_criteria>
    - `python manage.py shell -c` reports 0 `CalendarEvent` rows whose `target_list__name` is either demo campaign, 0 events whose url starts with the lifecycle legacy prefix, 0 `CampaignRun` rows for either demo campaign, 0 `TargetList` rows with either demo name, and 0 rows for `CalendarEvent` pk 335
    - The same probe reports a NON-zero total `CalendarEvent` count, proving the cleanup was scoped and did not empty the table
    - `git status --porcelain src/fomo_db.sqlite3` prints nothing (the file is untracked and gitignored; this cleanup is local by construction)
    - The SUMMARY records the backup path and the observed before/after counts
  </acceptance_criteria>
  <verify>
    <automated>python manage.py shell -c "
from tom_calendar.models import CalendarEvent
from tom_targets.models import TargetList
from solsys_code.models import CampaignRun
names = ['Reconciler Demo Campaign', 'Campaign Lifecycle Demo']
residue = (
    CalendarEvent.objects.filter(target_list__name__in=names).count()
    + CalendarEvent.objects.filter(url__startswith='LEGACY:campaign-lifecycle-demo:').count()
    + CampaignRun.objects.filter(campaign__name__in=names).count()
    + TargetList.objects.filter(name__in=names).count()
    + CalendarEvent.objects.filter(pk=335).count()
)
total = CalendarEvent.objects.count()
print(f'DEMO_RESIDUE={residue} TOTAL_EVENTS={total}')
" | grep -q 'DEMO_RESIDUE=0 TOTAL_EVENTS=[1-9]' && echo RESIDUE_CLEAN</automated>
    <fails_when>the line RESIDUE_CLEAN is absent, or the probe prints a non-zero DEMO_RESIDUE, or TOTAL_EVENTS is 0 — residue survived, or the cleanup over-reached and emptied the calendar</fails_when>
  </verify>
  <done>The developer database contains no row belonging to either demo campaign, event pk 335 is gone, the rest of the calendar is intact, and the backup path plus before/after counts are recorded in the SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| process environment -> Django settings | `FOMO_DATABASE_PATH` crosses from the shell into `DATABASES['default']['NAME']` |
| notebook execution -> developer database file | `jupyter nbconvert --execute` runs arbitrary project code with write access to whatever sqlite file settings resolve |
| notebook committed output -> public git history | every printed value in a `pre_executed/` notebook is committed verbatim and published through the Sphinx docs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-09-01 | Tampering | `src/fomo/settings.py` `DATABASES` | low | accept | An attacker able to set `FOMO_DATABASE_PATH` in the server process environment already controls the process. The variable is read once at settings import, is never taken from a request, and defaults to today's path when unset or empty. |
| T-33-09-02 | Tampering | demo notebook execution vs. `src/fomo_db.sqlite3` | medium | mitigate | Task 1 copies the database before Django opens it and points `FOMO_DATABASE_PATH` at the copy; the setup cell asserts the resolved path is the copy, and the task's own gate compares the developer file's md5 across a full execution of both notebooks. |
| T-33-09-03 | Information disclosure | `campaign_lifecycle_demo.ipynb` committed output | medium | mitigate | Task 2 removes `contact_person`/`contact_email` from the printed row, and a grep gate keeps both field names out of the notebook file. Values observed to date were empty strings, so no contact data has been published. |
| T-33-09-04 | Tampering | task 3's deletions against the developer database | medium | mitigate | A backup copy is taken outside the repository first; every delete is filtered by an explicit demo name, obscode or url prefix; the gate asserts the residue count is 0 AND the total event count is non-zero, so an over-broad delete fails the gate. |
| T-33-09-05 | Repudiation | demo staff user removal | low | mitigate | The `campaign_lifecycle_demo_staff` user is removed only when no surviving companion row still names it as `confirmed_by`, so no attribution audit trail loses its actor. |

No package-manager install runs in this plan (no npm/pip/cargo command is added or executed), so
the package-legitimacy gate does not apply and no `T-33-09-SC` row is registered.
</threat_model>

## Artifacts this phase produces

Symbols and files this plan creates (new — not expected in the existing API surface):

- `FOMO_DATABASE_PATH` — environment variable read in `src/fomo/settings.py`; when set and
  non-empty it supplies `DATABASES['default']['NAME']`.
- Notebook-local names in both demo notebooks' Django setup cell: `dev_db_path`,
  `scratch_db_dir`, `scratch_db_path`.
- A new "## Scratch database teardown" markdown cell plus its code cell at the end of each demo
  notebook.

No new file is created; no module, class, function, CLI flag or model field is added.

## Edge coverage disposition (spec-less probe fallback)

All six probe rows are `status: unresolved` — they are surfaced here as explicit planner
assumptions, not silently dropped. This plan re-authors none of them: it changes where the demo
notebooks write, not what any requirement's boundary behaviour is.

| Requirement | Category | Disposition in this gap-closure run |
|---|---|---|
| PROJ-04 | adjacency | Carried by plan 33-08's `must_haves` (exact local-noon boundary, both sides). Unchanged here. |
| PROJ-04 | empty | Carried by plan 33-08 (no attributed event, single-night window). Unchanged here. |
| PROJ-04 | ordering | `verification: backstop`; UAT test 3 ACCEPTED the absence evidence (option A, 2026-09-09) and carried a "set an ordering or add the shuffled-insertion test if a reader is added" requirement into Phase 34's context. Not re-opened. |
| ANNOT-01 | unclassified | Now concrete: a human-confirmed attribution versus an automated detach. Authored as a truth in plan 33-10, not here. |
| ANNOT-02 | empty | Carried by plan 33-06 (empty campaign still serves the highlight rule) and re-authored for the click path in plan 33-11. Unchanged here. |
| ANNOT-02 | encoding | Carried by plan 33-06 (`&`, `<`, `"` escape identically in the chip's `title` and `aria-label`). Unchanged here. |

<verification>
- `FOMO_DATABASE_PATH` set -> `manage.py shell` prints the override path; unset -> prints `src/fomo_db.sqlite3`
- A full execution of both notebooks leaves the md5 of `src/fomo_db.sqlite3` unchanged
- Neither contact field name appears anywhere in `campaign_lifecycle_demo.ipynb`
- Every code cell in both notebooks carries committed output
- The demo residue probe reports `DEMO_RESIDUE=0` with a non-zero total event count
- Full project suite (config `workflow.test_command`, verbatim):
  `LABELS=$(ls solsys_code/tests/test_*.py solsys_code/solsys_code_observatory/tests/test_*.py | grep -v "tests/test_views\.py$" | sed "s|/|.|g; s|\.py\$||" | tr "\n" " "); python manage.py test $LABELS && python manage.py test solsys_code.tests.test_views.TestSplitNumberUnitRegex solsys_code.tests.test_views.TestJPLSBDBQuery`
  — known unrelated flake: `test_bootstrap5_rendering.TestBootstrap5Rendering.test_observatory_create_form_submits_to_observatory_url` makes a live MPC network call
- `pre-commit run ruff --all-files` and `pre-commit run ruff-format --all-files` both clean (D-07)
</verification>

<success_criteria>
- Both demo notebooks execute against a scratch copy and leave `src/fomo_db.sqlite3` byte-identical
- The resolved database path is printed AND asserted inside each notebook's committed output
- `settings.py`'s default database path is unchanged when the environment variable is unset
- No contact field name survives in the lifecycle notebook's source or output
- The developer database holds zero rows from either demo campaign, including event pk 335
- The full suite and both ruff gates pass
</success_criteria>

<output>
Create `.planning/phases/33-series-identity-reconciler-inversion/33-09-SUMMARY.md` when done,
recording the backup path used before the cleanup and the observed before/after residue counts
(the cleanup is local to a gitignored file and cannot be shown in a diff).
</output>
