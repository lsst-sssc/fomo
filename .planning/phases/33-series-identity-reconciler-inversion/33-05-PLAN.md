---
phase: 33-series-identity-reconciler-inversion
plan: 05
type: execute
wave: 3
depends_on: ["33-01", "33-02", "33-03", "33-04"]
files_modified:
  - docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb
  - docs/notebooks/pre_executed/campaign_lifecycle_demo.ipynb
  - docs/runbooks/telescope_runs_calendar.rst
autonomous: true
requirements: [ANNOT-01, ANNOT-02, PROJ-04]

estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`reconcile_campaign_runs_demo.ipynb` shows, with real executed output, that a full sweep leaves every non-`RUN:` event's url, title and attribution unchanged — the before/after diff is empty (D-04, ROADMAP criterion 2)."
    - "`reconcile_campaign_runs_demo.ipynb` demonstrates the skip rule: a classical night with an attributed non-`RUN:` event gets no reconciler event, and the run's result reports it as a skipped night (D-01)."
    - "`campaign_lifecycle_demo.ipynb` shows the campaign decoration rendered from the attribution link on a real request, and shows it still rendered after the event's own title and description are rewritten from scratch (ROADMAP criterion 3)."
    - "`campaign_lifecycle_demo.ipynb` shows that clearing the attribution removes the decoration and deletes nothing (ROADMAP criterion 4)."
    - "`docs/runbooks/telescope_runs_calendar.rst` describes `CalendarEventMeta.run` as an attribution throughout — its three affected sections say the event is attributed to the run, never owned by it (D-17)."
    - "The runbook tells an operator what the reconciler now does with a night that already has an attributed event, and that the one-time calendar-title change (campaign name moving out of the title into the pop-up) is expected on the next sweep (D-01, D-12)."
    - "Both notebooks are committed with their executed output, regenerated with `jupyter nbconvert --to notebook --execute --inplace` (CLAUDE.md paired-docs rule)."
  artifacts:
    - docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb
    - docs/notebooks/pre_executed/campaign_lifecycle_demo.ipynb
    - docs/runbooks/telescope_runs_calendar.rst
  key_links:
    - "The notebook's before/after snapshot of every non-`RUN:` event -> D-04's real-DB proof: the second half of criterion 2's evidence, alongside plan 33-01's fixture test."
    - "The runbook's 'Why doesn't the calendar pop-up show a Campaign run block?' section -> the decoration a user actually sees: the operator-facing description of ANNOT-02."
  prohibitions:
    - statement: "No notebook cell may print or store a run's contact_person, contact_email, or source value into committed output."
      status: resolved
      verification: judgment
    - statement: "No notebook cell may leave demo rows behind in the developer database outside its own demo-scoped reset, and none may delete rows it did not create."
      status: resolved
      verification: judgment
---

<objective>
Bring the paired documentation up to what this phase actually changed, and use the reconciler
notebook as the second half of criterion 2's proof.

Purpose: CLAUDE.md's paired-docs rule makes these three artifacts part of this phase's deliverable,
not follow-up polish — the reconciler's behaviour, the modal's contents and the meaning of the run
link all changed, and all three are documented here. D-04 additionally asks the reconciler notebook
to carry a real-database before/after diff, which is evidence a fixture test cannot give.

Output: a re-executed `reconcile_campaign_runs_demo.ipynb` with the skip rule and the empty diff;
a re-executed `campaign_lifecycle_demo.ipynb` showing the decoration surviving a re-projection and
disappearing on unlink; and a runbook whose three affected sections describe attribution.
</objective>

<execution_context>
@/home/tlister/git/fomo_devel/.claude/gsd-core/workflows/execute-plan.md
@/home/tlister/git/fomo_devel/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-series-identity-reconciler-inversion/33-CONTEXT.md
@.planning/phases/33-series-identity-reconciler-inversion/33-01-SUMMARY.md
@.planning/phases/33-series-identity-reconciler-inversion/33-02-SUMMARY.md
@.planning/phases/33-series-identity-reconciler-inversion/33-03-SUMMARY.md
@.planning/phases/33-series-identity-reconciler-inversion/33-04-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconciler demo notebook — the skip rule and the empty real-database diff</name>
  <files>docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb</files>
  <read_first>
    - docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb (all 14 cells — the Django setup cell, the Observatory/TargetList seed cell, the four-run seed cell, the dry-run cell, the real-sweep cell, the idempotency cell, and the summary markdown)
    - docs/notebooks/pre_executed/campaign_lifecycle_demo.ipynb cells 5-6 (the demo-scoped reset pattern that makes a notebook safely re-runnable against any developer database)
    - solsys_code/campaign_reconciler.py as modified by plans 33-01 and 33-04 (`_night_already_attributed()`, `ReconcileResult.skipped_nights`, `event_title()`, `_detach_stale_family_events()`)
    - .planning/phases/33-series-identity-reconciler-inversion/33-CONTEXT.md decisions D-01, D-04, D-12 and the `<code_context>` dev-DB baseline figures
    - .planning/phases/33-series-identity-reconciler-inversion/33-RESEARCH.md "Open Questions" question 1 (the one existing non-`RUN:` row with `run` set, and why the diff cell doubles as that check)
  </read_first>
  <action>
Extend the notebook so it demonstrates what the reconciler now refuses to do, and carries D-04's
real-database evidence.

- Add a markdown + code cell pair, after the existing idempotency cells, implementing D-04's
  before/after diff: snapshot `(pk, url, title, description, start_time, end_time,
  telescope_label_meta.run_id)` for EVERY `CalendarEvent` whose url does not start with the
  reconciler's namespace prefix; run a full `reconcile_campaign_runs` sweep via `call_command`;
  snapshot again; compute the set difference and print it, with an explicit assertion that it is
  empty and a printed count of how many events were compared. The printed output is the evidence —
  make the cell print the comparison count and the (empty) diff, not just assert silently.
- Add a markdown + code cell pair demonstrating the skip rule: seed one blank-url event on one night
  of the existing classical demo run's window, attribute it with a `CalendarEventMeta` row, reconcile
  that run, and print (a) the attributed event's url before and after, showing it unchanged, (b) the
  set of `RUN:` urls the run now owns, showing no event for that night, and (c) the run's result
  including `skipped_nights`. Add a short markdown paragraph naming this as the same handoff rule the
  allocation layer will use in a later phase.
- Update the notebook's opening markdown and its closing summary: the reconciler owns only its own
  key namespace and annotates everything else; a night already attributed to the run through another
  writer's event is left to that event; and the campaign name no longer appears in a reconciler
  event's title because the pop-up renders it from the attribution link.
- Make the new cells demo-scoped and re-runnable: create the demo fixtures with find-or-create
  semantics and clean up only rows the notebook itself created, following the reset pattern the
  lifecycle notebook already uses. Do not delete or modify any pre-existing developer-database row.
- Do not print any run's `contact_person`, `contact_email` or `source` value into committed output.
- Regenerate with `jupyter nbconvert --to notebook --execute --inplace docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb`
  and commit the notebook WITH its output — pre-commit clears notebook output everywhere except this
  directory.
  </action>
  <verify>
    <automated>jupyter nbconvert --to notebook --execute --inplace docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb</automated>
    <fails_when>non-zero exit, or a `CellExecutionError` / traceback in the conversion output — a cell raised during execution</fails_when>
    <automated>python -c "import json;nb=json.load(open('docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb'));print(sum(1 for c in nb['cells'] if c['cell_type']=='code' and c.get('outputs')))"</automated>
    <fails_when>a printed count below 8 — the notebook was committed with cleared output instead of the executed output this directory requires</fails_when>
    <automated>python -c "import json;s=json.dumps(json.load(open('docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb')));print(sum(s.count(t) for t in ('contact_person','contact_email')))"</automated>
    <fails_when>a printed value above 0 — a contact field name reached the committed notebook, risking PII in stored output</fails_when>
    <automated>python -c "import json;s=json.dumps(json.load(open('docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb')));print(s.count('skipped_nights'))"</automated>
    <fails_when>a printed value of 0 — the notebook does not demonstrate the skip rule</fails_when>
  </verify>
  <acceptance_criteria>
    - The notebook contains a cell that snapshots every non-namespace-prefixed `CalendarEvent`, runs a full sweep, snapshots again, and prints both the comparison count and an empty diff.
    - The notebook contains a cell demonstrating an attributed night being skipped, printing the attributed event's url as unchanged and the run's `skipped_nights` value.
    - Every code cell in the committed file carries executed output.
    - `jupyter nbconvert --to notebook --execute --inplace` on this notebook exits 0 with no cell error.
    - The notebook's opening and closing markdown describe the reconciler as annotating events outside its own key namespace.
  </acceptance_criteria>
  <done>The reconciler notebook proves against the real developer database that a full sweep changes nothing outside the reconciler's own namespace, and shows an attributed night being left alone.</done>
</task>

<task type="auto">
  <name>Task 2: Campaign lifecycle notebook — decoration from the link, surviving a rewrite, gone on unlink</name>
  <files>docs/notebooks/pre_executed/campaign_lifecycle_demo.ipynb</files>
  <read_first>
    - docs/notebooks/pre_executed/campaign_lifecycle_demo.ipynb (all 30 cells — especially cell 2's `setup_test_environment()` boilerplate and why it is there, cells 4-6's staff/public clients and demo-scoped reset, cells 19-24's orphan event and attribution confirmation, and cells 25-28's four-way payoff and public campaign table)
    - solsys_code/templatetags/calendar_display_extras.py `campaign_decoration()` as added by plan 33-01
    - src/templates/tom_calendar/partials/event_form.html and src/templates/tom_calendar/partials/calendar.html as modified by plans 33-01 and 33-02
    - solsys_code/campaign_utils.py `unlink_event_from_run()` as added by plan 33-04
    - solsys_code/models.py `CalendarEventMeta` as modified by plan 33-03
    - .planning/phases/33-series-identity-reconciler-inversion/33-CONTEXT.md decisions D-10, D-11, D-13, D-14, D-16 and the `<specifics>` note about the re-projection test
  </read_first>
  <action>
Extend the lifecycle notebook so the campaign-annotation half of the v2.2/v2.4 story is visible,
using the same request-driven style the notebook already uses (`public_client.get(...)` /
`staff_client.get(...)`), never by importing the views module.

- After the existing attribution-confirmation cells, add a markdown + code cell pair that GETs the
  event modal for the newly-attributed orphan event through `public_client` and prints the campaign
  decoration it renders: the attributed-campaign-run label, the campaign name, the telescope and
  instrument, the window and the run status, plus the campaign-table link with its run anchor. Assert
  the response is 200 and that the decoration text is present.
- Add a markdown + code cell pair for ROADMAP criterion 3: assign a brand-new `title` and
  `description` to that same event and save them (standing in for a base-layer re-projection), GET
  the modal and the month calendar again, and print that the decoration is still rendered and the
  event now shows its new title. State in the markdown that this is what "attribution is a link,
  never a text write" buys.
- Add a markdown + code cell pair for ROADMAP criterion 4: call `unlink_event_from_run()` for that
  event and run, print the returned count, then print that the `CalendarEventMeta` row still exists
  with its link and audit fields cleared and `is_verified` unchanged, that the `CalendarEvent` still
  exists with its fields unchanged, and that a fresh modal GET renders no decoration. Print the
  `CalendarEvent` count before and after to show nothing was deleted.
- Add a short markdown + code cell pair introducing the two new link fields: show that a
  `CalendarEventMeta` row can carry an `observation_record` and an `observation_group`, by linking one
  demo event to a demo `ObservationRecord` by hand (this phase writes no such link automatically —
  say so in the markdown, and name the phase that will), and show that the row is offered by the
  event attribution queue exactly as any other unattributed event.
- Update the notebook's opening and closing markdown to add the campaign-annotation story: the run
  link means the event is attributed to that run, the decoration is rendered from that link at
  request time, and the campaign name is no longer written into a reconciler event's title.
- Keep every new fixture inside the notebook's existing demo-scoped reset so the notebook stays
  re-runnable against any developer database. Use `NonSiderealTargetFactory` for any `Target`.
- Do not print any run's `contact_person`, `contact_email` or `source` value into committed output.
- Regenerate with `jupyter nbconvert --to notebook --execute --inplace docs/notebooks/pre_executed/campaign_lifecycle_demo.ipynb`
  and commit with output.
  </action>
  <verify>
    <automated>jupyter nbconvert --to notebook --execute --inplace docs/notebooks/pre_executed/campaign_lifecycle_demo.ipynb</automated>
    <fails_when>non-zero exit, or a `CellExecutionError` / traceback in the conversion output — a cell raised during execution</fails_when>
    <automated>python -c "import json;s=json.dumps(json.load(open('docs/notebooks/pre_executed/campaign_lifecycle_demo.ipynb')));print(sum(s.count(t) for t in ('unlink_event_from_run','observation_group','Attributed campaign run')))"</automated>
    <fails_when>a printed value below 3 — one of the three new demonstrations (unlink, the series-identity links, the decoration label) is missing from the notebook</fails_when>
    <automated>python -c "import json;nb=json.load(open('docs/notebooks/pre_executed/campaign_lifecycle_demo.ipynb'));c=[x for x in nb['cells'] if x['cell_type']=='code'];print(sum(1 for x in c if not x.get('outputs')))"</automated>
    <fails_when>a printed count above 0 — a code cell was committed with no executed output</fails_when>
    <automated>python -c "import json;s=json.dumps(json.load(open('docs/notebooks/pre_executed/campaign_lifecycle_demo.ipynb')));print(s.count('solsys_code.views'))"</automated>
    <fails_when>a printed value above 0 — a cell imports the SPICE-loading views module, which triggers a large kernel download on execution</fails_when>
  </verify>
  <acceptance_criteria>
    - The notebook contains a cell that GETs the event modal through a public client and prints the rendered campaign decoration including the campaign-table link with its run anchor.
    - The notebook contains a cell that rewrites the event's title and description and then shows the decoration still rendered — the criterion 3 demonstration.
    - The notebook contains a cell that calls `unlink_event_from_run()` and prints the cleared link, the surviving row, the surviving event, and the unchanged `CalendarEvent` count.
    - The notebook contains a cell exercising `observation_record` / `observation_group` on a `CalendarEventMeta` row, with markdown stating no code writes them yet in this phase.
    - Every code cell in the committed file carries executed output, and no cell imports `solsys_code.views`.
  </acceptance_criteria>
  <done>The lifecycle notebook shows a user's-eye view of campaign annotation: visible from the link, surviving a re-projection, and gone — with nothing deleted — on unlink.</done>
</task>

<task type="auto">
  <name>Task 3: Operator runbook — attribution wording, the skip rule, and the new links</name>
  <files>docs/runbooks/telescope_runs_calendar.rst</files>
  <read_first>
    - docs/runbooks/telescope_runs_calendar.rst §"How do I attribute existing calendar events and observation records to a run?" (from line ~294)
    - docs/runbooks/telescope_runs_calendar.rst §"How do I get every campaign run onto the calendar?" (from line ~626) — especially the summary-counter block, the five skip reasons, the "What an operator sees on the calendar afterwards" paragraph and the per-night Telescope/Instrument paragraph
    - docs/runbooks/telescope_runs_calendar.rst §"Why doesn't the calendar pop-up show a 'Campaign run' block?" (from line ~719) — the whole section including the admin inline instructions and the 27-UAT Test 9 hint paragraph
    - docs/runbooks/telescope_runs_calendar.rst §"Command cheat-sheet" and §"Troubleshooting" — check whether either references the reconciler's adopt behaviour or the campaign-prefixed event title
    - docs/index.rst line ~24 (the runbook's toctree entry — confirm no structural change is needed)
    - .planning/phases/33-series-identity-reconciler-inversion/33-CONTEXT.md decisions D-01, D-09, D-12, D-13, D-16, D-17
  </read_first>
  <action>
Rewrite the three affected sections so an operator's mental model matches the new behaviour. Prose
changes only — do not restructure the page or move sections.

§"How do I get every campaign run onto the calendar?":

- Replace the paragraph explaining that a per-night entry adopted from the classical-schedule loader
  keeps its own telescope/instrument and window, with the new rule: a night that already has a
  calendar entry attributed to this run gets no reconciler entry at all — the attributed entry is
  that night, and the campaign shows on it as a pop-up decoration. Say plainly that the reconciler
  now only ever creates, updates or removes entries it keyed itself, and never re-keys or edits
  another writer's entry.
- Add a short paragraph on the one-time title change (D-12): reconciler-created entries no longer
  carry the campaign name in their title; the campaign name now appears as a marker on the month cell
  and in the event pop-up. Tell the operator that the first sweep after this change will update the
  titles of the reconciler's existing entries once, and that this is expected, not a fault.
- Leave the summary-counter block, the five skip reasons, and the blank-timezone failure paragraph
  as they are — those are 29-CONTEXT D-05/D-06 behaviour that this phase does not change. If the
  reconciler now reports skipped nights in a way an operator can see, document it here; if it is an
  internal counter only, say nothing about it rather than describing output that is not printed.

§"Why doesn't the calendar pop-up show a 'Campaign run' block?":

- Retitle and rewrite the section around attribution: the pop-up shows an attributed-campaign-run
  block naming the run the entry is attributed to, its telescope and instrument, its window and its
  run status, plus a link to the campaign table that lands on that run's own row. Say the block is
  rendered from the attribution link every time the page is drawn, so it cannot be erased by anything
  that rewrites the entry's own title or description.
- Say what a month cell shows: a small campaign marker on every attributed entry whose run is
  publicly visible, with the campaign name as its tooltip.
- Replace the ownership wording in the admin-inline instructions: a row under a run means the entry
  is attributed to that run; clearing the value un-attributes the entry, removing only the pop-up
  block and the marker — the entry itself, and its telescope-label verification history, survive, and
  nothing is deleted. Add that clearing the link also clears the "confirmed by" record, since a
  confirmation for an attribution that no longer exists would be misleading.
- Add a sentence on the two new read-only fields visible in the admin (observation record and
  observation group): they record which observation an entry was drawn from, are filled in by code
  only, and are empty for every entry today — the phase that fills them is named in the roadmap.
- Keep the 27-UAT Test 9 staff-only "Possible campaign run match" hint paragraph as it is; that
  behaviour is unchanged.

§"How do I attribute existing calendar events and observation records to a run?":

- Sweep the section for ownership wording and restate it as attribution. Add a sentence that
  attributing an entry to a run never changes the entry itself — it adds the campaign decoration and
  nothing else — and that an entry backed by an observation record is offered in this queue exactly
  like any other unattributed entry.

Finally, grep the whole page for any remaining ownership wording about the run link and for any
description of the reconciler adopting or re-keying another writer's entry, and fix what you find.
Build the docs to confirm the page still renders.
  </action>
  <verify>
    <automated>grep -ci 'attributed to' docs/runbooks/telescope_runs_calendar.rst</automated>
    <fails_when>exit 1 (no match), or a printed count below 5 — the attribution wording did not reach the three affected sections</fails_when>
    <automated>grep -n -i 'owning campaign run\|owns the event\|owned by this run' docs/runbooks/telescope_runs_calendar.rst; test $? -eq 1</automated>
    <fails_when>non-zero exit, meaning ownership wording about the run link is still on the page</fails_when>
    <automated>pre-commit run --all-files</automated>
    <fails_when>non-zero exit, or a "Failed" hook result line — the ruff, notebook-output, or Sphinx docs-build hook rejected the change</fails_when>
  </verify>
  <acceptance_criteria>
    - `docs/runbooks/telescope_runs_calendar.rst` contains the phrase `attributed to` and no longer contains `owning campaign run`, `owns the event`, or `owned by this run`.
    - The reconciler section states that a night with an entry already attributed to the run gets no reconciler entry, and that the reconciler never re-keys or edits another writer's entry.
    - The reconciler section documents the one-time title change and where the campaign name now appears instead.
    - The pop-up section describes the month-cell marker, the pop-up block's contents, the anchored campaign-table link, and the two new read-only admin fields.
    - The pop-up section states that clearing the link removes only the decoration, deletes nothing, and also clears the confirmation record.
    - `pre-commit run --all-files` exits 0 (this includes the Sphinx docs build).
  </acceptance_criteria>
  <done>An operator reading the runbook learns what the reconciler now refuses to do, what the calendar now shows and why it cannot be erased, and what clearing an attribution does and does not destroy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer database -> committed notebook output | Pre-executed notebooks in this directory are committed WITH their output, so whatever a cell prints becomes a permanent public artifact in the repository |
| notebook execution -> developer database | These notebooks write real rows against the developer database when executed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-17 | Information Disclosure | committed notebook output | high | mitigate | No cell prints `contact_person`, `contact_email` or `CampaignRun.source`; the D-04 diff cell snapshots only url, title, description, times and the run id. Tasks 1 and 2 each grep the committed JSON for the contact field names |
| T-33-18 | Tampering | notebook execution against the developer database | medium | mitigate | Every new fixture is created with find-or-create semantics inside the notebook's own demo-scoped reset, and no cell deletes or edits a row it did not create — the pattern the lifecycle notebook already establishes. The reconciler notebook's diff cell is read-only apart from the sweep it is measuring |
| T-33-19 | Denial of Service | notebook execution importing the ephemeris path | medium | mitigate | No cell imports `solsys_code.views` or `solsys_code.ephem_utils`; importing either downloads roughly 1.6 GB of SPICE kernels at module load. Task 2 greps the committed notebook for the views module import |
| T-33-20 | Repudiation | stale operator documentation | medium | mitigate | The runbook's three affected sections are rewritten in this plan rather than left to drift; a stale runbook led an operator astray once before in this project (quick task 260726-kdp), which is why the paired-docs rule now covers `docs/runbooks/` by directory |
| T-33-SC | Tampering | package installs | low | accept | This plan runs no `pip`/`npm`/`cargo` install step; RESEARCH.md `## Package Legitimacy Audit` records that the phase installs no external package |
</threat_model>

<verification>
- `jupyter nbconvert --to notebook --execute --inplace` exits 0 for both notebooks with no cell error.
- Every code cell in both committed notebooks carries executed output.
- `pre-commit run --all-files` exits 0 (ruff, ruff-format, notebook-output clearing outside this
  directory, and the Sphinx docs build).
- The project full-suite command (`.planning/config.json` `workflow.test_command`) exits 0.
- `grep -i 'owning campaign run' docs/runbooks/telescope_runs_calendar.rst` finds nothing.

<human-check>
Read the rewritten "Why doesn't the calendar pop-up show a 'Campaign run' block?" section end to
end and confirm an operator who has never read this phase's planning documents could, from that
section alone, answer: what makes the block appear, what makes it disappear, what clearing the link
destroys, and what the two new read-only admin fields are for. Then skim the reconciler notebook's
executed output and confirm the printed before/after diff really is empty and the comparison count
is non-trivial (the developer database held 240 events and 85 companion rows at baseline).
</human-check>
</verification>

<success_criteria>
- All three paired artifacts named by CLAUDE.md's paired-docs rule and by the ROADMAP phase entry are
  updated in this phase, with executed notebook output committed.
- D-04's real-database proof exists: a full sweep leaves every event outside the reconciler's
  namespace unchanged, shown with printed output.
- The runbook describes the run link as an attribution throughout, and documents the skip rule, the
  decoration, the one-time title change, and what an unlink does.
- No committed notebook output carries contact or provenance values.
</success_criteria>

<output>
Create `.planning/phases/33-series-identity-reconciler-inversion/33-05-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

Content created by THIS plan (none exists before it):

- `docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb`: a real-database before/after diff
  cell pair and an attributed-night skip cell pair, with executed output
- `docs/notebooks/pre_executed/campaign_lifecycle_demo.ipynb`: a decoration cell pair, a
  survives-a-rewrite cell pair, an unlink cell pair, and a series-identity-links cell pair, with
  executed output
- `docs/runbooks/telescope_runs_calendar.rst`: rewritten reconciler, pop-up and attribution-queue
  sections

Consumed from earlier plans (created there, not here): `campaign_decoration()`,
`_night_already_attributed()`, `ReconcileResult.skipped_nights`, `unlink_event_from_run()`,
`CalendarEventMeta.observation_record`, `CalendarEventMeta.observation_group`, migration
`0017_calendareventmeta_observation_links`, and the `.cal-campaign-chip` month-cell marker.