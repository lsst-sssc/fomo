---
phase: 33-series-identity-reconciler-inversion
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/templates/campaigns/campaignrun_table.html
  - src/templates/tom_calendar/partials/campaign_chip.html
  - src/templates/tom_calendar/partials/calendar.html
  - src/templates/tom_calendar/partials/event_form.html
  - solsys_code/templatetags/calendar_display_extras.py
  - solsys_code/tests/test_campaign_views.py
  - solsys_code/tests/test_calendar_template.py
autonomous: true
gap_closure: true
requirements: [ANNOT-02]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A GET of the campaign run table page returns HTML containing the `tr:target` highlight rule, for a staff reader and for an anonymous reader alike (D-13, CR-01)."
    - "The `tr:target` rule is still served when the campaign has zero run rows in the table (ANNOT-02 empty edge)."
    - "A campaign name containing `&`, `<` and `\"` is HTML-escaped by Django's autoescape identically in the month-cell chip's `title=` and `aria-label=` attributes — the raw characters never reach the rendered attribute values (ANNOT-02 encoding edge)."
    - "The month-cell chip for a run with no campaign carries a tooltip and accessible name naming that run (`Attributed run #{pk} (no campaign)`), distinct from the tooltip a campaign-bearing run produces (IN-05)."
    - "The month-cell chip carries an `aria-label` and `role=\"img\"`, so the campaign attribution has an accessible name rather than being tooltip-only (IN-03)."
    - "`event_form.html` decides whether to render the attributed-run block from `campaign_decoration()`'s return value alone — there is no second, independent `is_publicly_visible` gate in the template (WR-07)."
    - "`campaign_decoration()`'s `run_pk` key has a rendered consumer: the month-cell chip's no-campaign tooltip (IN-01)."
    - "`test_pending_review_run_shows_no_marker_for_staff_and_anonymous` fails if the `is_publicly_visible` gate in `campaign_decoration()` is deleted — it asserts on a value only the pending fixture can produce (WR-05.1)."
    - "`test_chip_does_not_consume_title_truncation_budget` fails if the chip is folded inside the `truncatechars` filter expression — its fixture title sits at the truncation budget (WR-05.2)."
    - "`test_no_campaign_run_renders_marker_and_no_table_href` fails if the no-campaign fixture stops rendering its own chip — it asserts on that run's own tooltip string, not on the shared `cal-campaign-chip` class (WR-05.3)."
    - "A run that sorts past page 1 of the paginated campaign table has no `id=\"run-{pk}\"` anchor in the page-1 document, and that constraint is pinned by a test rather than being a silent dead link (WR-08, partial)."
  artifacts:
    - src/templates/campaigns/campaignrun_table.html
    - src/templates/tom_calendar/partials/campaign_chip.html
    - src/templates/tom_calendar/partials/calendar.html
    - src/templates/tom_calendar/partials/event_form.html
    - solsys_code/tests/test_campaign_views.py
    - solsys_code/tests/test_calendar_template.py
  key_links:
    - "`campaignrun_table.html`'s `<style>` -> `{% block additional_css %}` declared in `tom_common/base.html:18` — the only rendered CSS hook the base template exposes to a child."
    - "`campaign_decoration(event)` -> `campaign_chip.html` -> the two month-grid loops in `calendar.html` (all-day and timed) — one chip definition, two call sites."
    - "`campaign_decoration(event)` -> `event_form.html`'s single `{% if deco %}` gate — the tag's `is_publicly_visible` rule is the only copy."
  prohibitions:
    - "The decoration must never be written into any `CalendarEvent` field; no template tag in `calendar_display_extras.py` may call `.save()`, `.update()`, `.create()`, `get_or_create()` or `.delete()` (33-01 P2, still in force)."
    - "The month cell and the modal must never render `contact_person`, `contact_email` or `CampaignRun.source` (33-01 P3 / 33-02 P2, still in force)."
    - "The chip must never consume the event title's `truncatechars:18` / `truncatechars:16` budget, and the campaign name must never be written into `CalendarEvent.title` to produce it (33-02 P1, still in force)."
    - "The chip must never render a `title=` or `aria-label=` naming a run that `campaign_decoration()` withheld — a not-publicly-visible run has no chip at all, for staff and anonymous readers alike (new, this plan)."
---

<objective>
Close code-review blocker CR-01 and the display-layer warnings and info items that ride on the
same surface: the D-13 row-highlight CSS that Django has been silently discarding, an
`event_form.html` visibility rule that exists in two places, a month-cell chip that is
tooltip-only and mislabels a campaign-less run, and three month-view tests whose assertions
cannot fail for the reason they claim.

Purpose: UAT test 2 ("View campaign ↗ lands on the highlighted run row") cannot pass until
CR-01 is fixed — the `tr:target` rule has never reached a browser. The WR-05 tests are the
regression net for D-10/D-11's visibility and PII gates; while they pass vacuously, that net
does not exist.

Output: a rendered highlight rule, one chip definition with an accessible name, one
visibility gate, and month-view tests that fail when the thing they name breaks.

**Runbook note:** the operator-facing prose for WR-06 and WR-08 is written by plan 33-08,
which owns `docs/runbooks/telescope_runs_calendar.rst`. This plan writes no documentation
prose; it pins WR-08's constraint with a test that 33-08's runbook paragraph refers to.

**Ordering note:** the scripted mutation proofs in Task 1 and Task 3 `sed`-rewrite tracked
source files — `src/templates/campaigns/campaignrun_table.html` and
`solsys_code/templatetags/calendar_display_extras.py` — and restore them from a backup copy
within the same command, so this plan assumes exclusive working-tree access for the duration
of those two `<verify>` blocks. Nothing else may read those files, run the test suite, or run
`pre-commit --all-files` over the same tree while a mutation proof is in flight. Plan 33-07 is
ordered after this one (wave 2, `depends_on: [33-06]`) for exactly that reason.
</objective>

<execution_context>
@/home/tlister/git/fomo_devel/.claude/gsd-core/workflows/execute-plan.md
@/home/tlister/git/fomo_devel/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-series-identity-reconciler-inversion/33-REVIEW.md
@.planning/phases/33-series-identity-reconciler-inversion/33-CONTEXT.md
@.planning/phases/33-series-identity-reconciler-inversion/33-02-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Render the D-13 row-highlight rule end-to-end, and pin the paginated-anchor constraint</name>
  <files>src/templates/campaigns/campaignrun_table.html, solsys_code/tests/test_campaign_views.py</files>
  <read_first>
    - `src/templates/campaigns/campaignrun_table.html` (the whole file — the `<style>` currently sits between `{% block title %}` at line 3 and `{% block content %}` at line 15)
    - `/home/tlister/venv/devel_fomo311_venv/lib64/python3.11/site-packages/tom_common/templates/tom_common/base.html` lines 1-30 — confirm the block names the base template actually exposes inside `<head>`; `{% block additional_css %}` is declared at line 18 and is empty
    - `solsys_code/tests/test_campaign_views.py` — the `CampaignViewTestBase` fixture (`table_url()`, `staff_user`, `most_recent_run`, `campaign`) and `TestCampaignRunRowAnchor` around lines 633-661
    - `solsys_code/campaign_views.py` around line 130 — `table_pagination = {'per_page': 25}` and `CampaignRunTableView.get_queryset()`'s `exclude()` of pending-review runs, plus the default `window_start` descending ordering
    - `33-REVIEW.md` sections "CR-01" and "WR-08"
  </read_first>
  <behavior>
    - A staff GET of `campaigns:table` for a campaign with runs returns HTML containing `tr:target`.
    - An anonymous GET of the same URL returns HTML containing `tr:target` (the dict-row branch).
    - A GET for a campaign with zero visible run rows still returns HTML containing `tr:target` — the rule ships with the page, not with a row.
    - Given 26+ approved runs in one campaign, the run that sorts last under `window_start` descending has no `id="run-{pk}"` anchor in the unpaginated page-1 document, and the same run's `id="run-{pk}"` IS present on page 2.
  </behavior>
  <action>
    Move the existing `<style>` element in `src/templates/campaigns/campaignrun_table.html` inside
    `{% block additional_css %}...{% endblock %}`, placed after the `{% block title %}` line and
    before `{% block content %}`. Keep the rule body byte-identical (`tr:target` with its
    `background-color` and `box-shadow` declarations) and keep the existing explanatory comment,
    extending it with a one-line note that a top-level node in a template that `{% extends %}`
    is discarded by Django's `ExtendsNode`, which is why the element must live inside a block.
    `additional_css` is the block `tom_common/base.html` declares at line 18 inside `<head>`; it
    is empty in the parent, so `{{ block.super }}` is not needed. Do not move the rule into
    `{% block content %}` — a `<style>` in the body is valid but puts a document-wide rule in the
    per-view content region.

    In `solsys_code/tests/test_campaign_views.py`, extend `TestCampaignRunRowAnchor` with two
    methods asserting the rendered response contains `tr:target` — one logged in as
    `self.staff_user`, one anonymous — placed alongside the existing `id="run-{pk}"` assertions
    so the anchor half and the highlight half of D-13 are covered by the same class. Add a third
    method covering the empty case: create a second `TargetList` campaign with no `CampaignRun`
    rows, GET its table URL, and assert the response is 200 and still contains `tr:target`.

    Add a new test class `TestCampaignRunAnchorPagination(CampaignViewTestBase)` pinning WR-08's
    constraint as a known, documented limitation rather than a silent dead link. Its
    `setUpTestData` creates its own `TargetList` and 26 approved `CampaignRun` rows with
    descending-distinct `window_start` values (so exactly one run falls onto page 2 under
    `CampaignRunTableView`'s default `window_start` descending sort and `per_page: 25`). One
    method asserts the run with the OLDEST `window_start` has no `id="run-{pk}"` in the page-1
    document; a second asserts the same `id="run-{pk}"` IS present when the same URL is fetched
    with `{'page': 2}`. Give the class a docstring naming WR-08, stating that the decoration's
    `#run-{pk}` link carries no page parameter so it lands on page 1, and pointing at the runbook
    section 33-08 adds for the operator-facing wording. Use
    `tom_targets.tests.factories.NonSiderealTargetFactory` for any `Target` fixture (CLAUDE.md).
  </action>
  <verify>
    <automated>python manage.py test solsys_code.tests.test_campaign_views</automated>
    <fails_when>non-zero exit, or the runner summary reads `FAILED (failures=…)` / `FAILED (errors=…)` instead of `OK`</fails_when>
    <automated>grep -n 'additional_css' src/templates/campaigns/campaignrun_table.html</automated>
    <fails_when>no output (exit 1) — the style block was not moved into a rendered block</fails_when>
    <automated>BK=$(mktemp); cp src/templates/campaigns/campaignrun_table.html "$BK"; sed -i 's/block additional_css/block hook_the_parent_never_declares/g' src/templates/campaigns/campaignrun_table.html; python manage.py test solsys_code.tests.test_campaign_views.TestCampaignRunRowAnchor; MUTATED_RC=$?; cp "$BK" src/templates/campaigns/campaignrun_table.html; rm -f "$BK"; grep -q 'block additional_css' src/templates/campaigns/campaignrun_table.html; RESTORED=$?; grep -q 'hook_the_parent_never_declares' src/templates/campaigns/campaignrun_table.html; LEFTOVER=$?; echo "mutated_rc=$MUTATED_RC restored=$RESTORED leftover=$LEFTOVER"; test "$MUTATED_RC" -ne 0 -a "$RESTORED" -eq 0 -a "$LEFTOVER" -ne 0</automated>
    <fails_when>the final `test` exits non-zero. `mutated_rc=0` means the `tr:target` assertions passed while the rule sat in a block the base template never declares — they are asserting on template source, not on the render. `restored=1` or `leftover=0` means the template was not put back: the backup copy did not land, and the working tree is left mutated. The command renames the block rather than deleting the element, so the mutation is a single reversible token substitution and the restore is a byte copy of the pre-mutation file.</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/templates/campaigns/campaignrun_table.html` contains `{% block additional_css %}` and the `tr:target` rule is between it and its `{% endblock %}`.
    - `python manage.py test solsys_code.tests.test_campaign_views` exits 0 with `OK`.
    - `TestCampaignRunRowAnchor` contains a staff assertion and an anonymous assertion that the response body contains `tr:target`, plus a zero-run-campaign assertion.
    - `solsys_code/tests/test_campaign_views.py` contains `class TestCampaignRunAnchorPagination`.
    - The scripted mutation proof in this task's verify block exits 0: with the CSS block renamed to one the base template never declares, `TestCampaignRunRowAnchor` goes red — proving the assertions read the render, not the source text — and the template is restored byte-for-byte from its backup, which the same command asserts before exiting.
  </acceptance_criteria>
  <reversibility rating="reversible">Relocating a template element between blocks is a one-line move with no data or API consequence.</reversibility>
  <done>The campaign table page actually serves the `tr:target` rule to staff and anonymous readers, the empty-campaign case is covered, and WR-08's page-1-only anchor is a pinned constraint with a named test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: One chip definition with an accessible name, one visibility gate</name>
  <files>src/templates/tom_calendar/partials/campaign_chip.html, src/templates/tom_calendar/partials/calendar.html, src/templates/tom_calendar/partials/event_form.html, solsys_code/templatetags/calendar_display_extras.py</files>
  <read_first>
    - `src/templates/tom_calendar/partials/calendar.html` lines 100-120 (the `.cal-campaign-chip` CSS rule) and lines 246-290 (the all-day loop's chip at ~line 253 and the timed loop's chip at ~line 282)
    - `src/templates/tom_calendar/partials/event_form.html` lines 100-192 — the `{% with run=event.telescope_label_meta.run %}` wrapper, the `{% if run.is_publicly_visible %}` gate, the inner `{% campaign_decoration event as deco %}` / `{% if deco %}` gate, the `{% elif not run and request.user.is_staff %}` arm and its `{% endwith %}`
    - `solsys_code/templatetags/calendar_display_extras.py` lines 430-483 — `campaign_decoration()`, its "exactly these keys" docstring contract, the `is_publicly_visible` rule at line 464 and the `run.campaign_id is not None` guard that decides `table_url`
    - `solsys_code/models.py` line 108 — `NO_CAMPAIGN_LABEL = '(no campaign)'`
    - `33-REVIEW.md` sections "WR-07", "IN-01", "IN-03", "IN-05"
  </read_first>
  <behavior>
    - An attributed event whose run HAS a campaign renders one chip with `title="{campaign name}"` and `aria-label="Campaign: {campaign name}"`.
    - An attributed event whose run has NO campaign renders one chip whose `title` and `aria-label` both read `Attributed run #{run pk} (no campaign)` — a value no other fixture can produce.
    - Every chip carries `role="img"`.
    - The all-day cell and the timed cell render the same markup, from one included partial.
    - The modal renders the attributed-run block when and only when `campaign_decoration()` returns a value; the staff attribution-queue hint still renders for an event with no run and a staff viewer; a pending-review run renders neither block for staff or anonymous.
  </behavior>
  <action>
    Create `src/templates/tom_calendar/partials/campaign_chip.html`: a single `<span>` carrying
    `class="cal-campaign-chip"`, `role="img"`, the `&#9873;` glyph, and a `title` plus an
    `aria-label` chosen by a `{% if deco.table_url %}` branch — `table_url` is `None` exactly when
    the run has no campaign, so it is the existing, already-computed campaign-presence signal and
    needs no new key on the tag's dict. The campaign branch renders `title="{{ deco.campaign_name }}"`
    and `aria-label="Campaign: {{ deco.campaign_name }}"`. The no-campaign branch renders
    `Attributed run #{{ deco.run_pk }} {{ deco.campaign_name }}` in both attributes — `campaign_name`
    is already `NO_CAMPAIGN_LABEL` there, which supplies its own parentheses, so the rendered string
    is `Attributed run #12 (no campaign)`. Guard the whole partial with `{% if deco %}` so an
    unattributed or non-public event includes nothing. Rely on Django's autoescape for both
    attributes — do not add `|safe`, and do not hand-escape.

    In `src/templates/tom_calendar/partials/calendar.html`, replace both inline chip spans (the
    all-day loop's and the timed loop's) with
    `{% include 'tom_calendar/partials/campaign_chip.html' with deco=campaign_deco %}`, keeping each
    include in exactly the position the span occupied so the `truncatechars:18` / `truncatechars:16`
    filters stay outside it. Leave the `.cal-campaign-chip` CSS rule (`color: currentColor`,
    `flex-shrink: 0`) unchanged.

    In `src/templates/tom_calendar/partials/event_form.html`, remove the outer
    `{% with run=event.telescope_label_meta.run %}` / `{% endwith %}` wrapper and the
    `{% if run.is_publicly_visible %}` gate, and promote the inner `{% campaign_decoration event as deco %}`
    / `{% if deco %}` to be the only gate on the attributed-run block. Re-point the surviving arm at
    the accessor directly: `{% elif not event.telescope_label_meta.run and request.user.is_staff %}`.
    The staff-hint body reads only `event`, so it needs no other change. Keep both `{% comment %}`
    blocks, updating the D-09/D-10 one to record that `campaign_decoration()` is now the single
    place the `run is None or not run.is_publicly_visible` rule lives (WR-07), and that a reverse
    one-to-one accessor with no row resolves silently in a template because `ObjectDoesNotExist`
    sets `silent_variable_failure`.

    In `solsys_code/templatetags/calendar_display_extras.py`, make no logic change. Extend
    `campaign_decoration()`'s Returns docstring so the "exactly these keys" contract records the
    rendered consumer of each of the two keys that had none: `run_pk` is consumed by
    `campaign_chip.html`'s no-campaign tooltip and accessible name (IN-01), and `table_url` doubles
    as the campaign-presence signal that partial branches on.
  </action>
  <verify>
    <automated>python manage.py test solsys_code.tests.test_calendar_template solsys_code.tests.test_calendar_display_extras solsys_code.tests.test_null_campaign_guards</automated>
    <fails_when>non-zero exit, or the runner summary reads `FAILED (failures=…)` / `FAILED (errors=…)` instead of `OK`</fails_when>
    <automated>grep -c 'aria-label' src/templates/tom_calendar/partials/campaign_chip.html</automated>
    <fails_when>output is `0`, or the file does not exist (exit 2)</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/templates/tom_calendar/partials/campaign_chip.html` exists, contains `role="img"`, and contains both an `aria-label` and a `title` attribute.
    - `src/templates/tom_calendar/partials/calendar.html` contains two `{% include 'tom_calendar/partials/campaign_chip.html'` lines and no remaining inline `cal-campaign-chip` span.
    - `src/templates/tom_calendar/partials/event_form.html` contains exactly one `{% if deco %}` gate for the attributed-run block and no `{% if run.is_publicly_visible %}`.
    - `python manage.py test solsys_code.tests.test_calendar_template solsys_code.tests.test_calendar_display_extras solsys_code.tests.test_null_campaign_guards` exits 0 with `OK`.
    - A month view rendered for an attributed run with `campaign=None` produces the literal `Attributed run #` followed by that run's primary key in the response body.
  </acceptance_criteria>
  <reversibility rating="reversible">Template-only restructure; the tag's return contract is unchanged, so reverting is a straight revert of three template files.</reversibility>
  <done>The chip has one definition, an accessible name, and a run-scoped label when there is no campaign; the modal's visibility rule lives only in `campaign_decoration()`.</done>
</task>

<task type="auto">
  <name>Task 3: Make the month-view tests fail for the reason they claim</name>
  <files>solsys_code/tests/test_calendar_template.py</files>
  <read_first>
    - `solsys_code/tests/test_calendar_template.py` lines 749-800 (`MonthCellCampaignMarkerTest`, its `setUpTestData` and `test_chip_does_not_consume_title_truncation_budget`) and lines 803-915 (`DecorationSurvivalAndGuardsTest`, its five fixtures and `test_no_campaign_run_renders_marker_and_no_table_href` / `test_pending_review_run_shows_no_marker_for_staff_and_anonymous`)
    - `src/templates/tom_calendar/partials/campaign_chip.html` as written by Task 2 — the exact attribute strings the assertions must match
    - `src/templates/tom_calendar/partials/calendar.html` lines 246-290 — the `truncatechars:18` (all-day) and `truncatechars:16` (timed) filters the truncation test targets
    - `33-REVIEW.md` section "WR-05" — all three sub-items and the reviewer's proposed assertions
  </read_first>
  <action>
    Rewrite the three assertions WR-05 identifies so each one fails when its own subject breaks.

    (1) `DecorationSurvivalAndGuardsTest.test_pending_review_run_shows_no_marker_for_staff_and_anonymous`:
    give `pending_run` its own `TargetList` (a second campaign, distinct from `Survival Guard
    Campaign`) in `setUpTestData`, and assert on that campaign's name as the discriminator —
    `assertNotIn` of the rendered tooltip attribute string carrying that name, for the anonymous
    content and the staff content alike. Today the assertion targets `telescope_instrument`, which
    the month cell never renders, and `pending_run` shares `approved_run`'s campaign, so the gate
    under test contributes nothing to the outcome.

    (2) `MonthCellCampaignMarkerTest.test_chip_does_not_consume_title_truncation_budget`: change the
    all-day fixture's title to a string exactly 18 characters long and the timed fixture's title to
    a string exactly 16 characters long, so each sits at its own filter's budget and any character
    the chip contributed inside the filter expression would visibly shorten it. Assert the full
    title survives in the rendered content for both, and assert the chip's own attribute string is
    also present — proving the chip is a sibling of the filtered title, not part of it. Adjust the
    two existing fixture titles and any other assertion in the class that names them.

    (3) `DecorationSurvivalAndGuardsTest.test_no_campaign_run_renders_marker_and_no_table_href`:
    replace the `cal-campaign-chip` class assertion — which `linked_event` and `pii_event` in the
    same September grid already satisfy — with an assertion on the no-campaign chip's own tooltip
    string, `Attributed run #` followed by `self.no_campaign_run.pk` and the no-campaign label.
    Keep the `assertNotIn(self._campaign_table_href())` half but retarget it at
    `self.no_campaign_run` so it means something: assert no `campaigns:table` href for a
    campaign-less run can appear, and note in the docstring that the month cell renders `table_url`
    for no event, so this half is a namespace guard rather than a per-event assertion.

    Add one new method to `DecorationSurvivalAndGuardsTest` for the ANNOT-02 encoding edge: create a
    campaign whose name contains `&`, `<` and a double quote, attribute an event to an approved run
    in that campaign, GET the month view, and assert the raw characters do not appear inside either
    rendered attribute value while their HTML entity forms do — the same escaped form in the `title`
    and in the `aria-label`, so the two attributes agree on what equality of the campaign name
    means. Use `tom_targets.tests.factories.NonSiderealTargetFactory` for any `Target` fixture.
  </action>
  <verify>
    <automated>python manage.py test solsys_code.tests.test_calendar_template</automated>
    <fails_when>non-zero exit, or the runner summary reads `FAILED (failures=…)` / `FAILED (errors=…)` instead of `OK`</fails_when>
    <automated>BK=$(mktemp); cp solsys_code/templatetags/calendar_display_extras.py "$BK"; sed -i 's/if run is None or not run\.is_publicly_visible:/if run is None:/' solsys_code/templatetags/calendar_display_extras.py; python manage.py test solsys_code.tests.test_calendar_template.DecorationSurvivalAndGuardsTest.test_pending_review_run_shows_no_marker_for_staff_and_anonymous; MUTATED_RC=$?; cp "$BK" solsys_code/templatetags/calendar_display_extras.py; rm -f "$BK"; grep -q 'or not run.is_publicly_visible:' solsys_code/templatetags/calendar_display_extras.py; RESTORED=$?; grep -qE '^[[:space:]]*if run is None:$' solsys_code/templatetags/calendar_display_extras.py; LEFTOVER=$?; echo "mutated_rc=$MUTATED_RC restored=$RESTORED leftover=$LEFTOVER"; test "$MUTATED_RC" -ne 0 -a "$RESTORED" -eq 0 -a "$LEFTOVER" -ne 0</automated>
    <fails_when>the final `test` exits non-zero. `mutated_rc=0` means the pending-run test still passed with the visibility half of the guard removed — the assertion does not depend on the gate it names (WR-05.1 unfixed). `restored=1` or `leftover=0` means the guard was not put back: the backup copy did not land, and the working tree is left with a weakened public-visibility gate. The mutation keeps the null half of the guard so the mutated run exercises the visibility rule alone rather than crashing on a null run.</fails_when>
    <automated>LABELS=$(ls solsys_code/tests/test_*.py solsys_code/solsys_code_observatory/tests/test_*.py | grep -v "tests/test_views\.py$" | sed "s|/|.|g; s|\.py\$||" | tr "\n" " "); python manage.py test $LABELS && python manage.py test solsys_code.tests.test_views.TestSplitNumberUnitRegex solsys_code.tests.test_views.TestJPLSBDBQuery</automated>
    <fails_when>non-zero exit, or a `FAILED (…)` summary line. A `ReadTimeout` against `data.minorplanetcenter.net` in `test_bootstrap5_rendering.TestBootstrap5Rendering.test_observatory_create_form_submits_to_observatory_url` is the known network-flaky test recorded in `deferred-items.md` and is not a regression of this plan.</fails_when>
    <automated>pre-commit run ruff --all-files &amp;&amp; pre-commit run ruff-format --all-files</automated>
    <fails_when>non-zero exit, or any hook line reads `Failed`</fails_when>
  </verify>
  <acceptance_criteria>
    - The scripted mutation proof in this task's verify block exits 0: with the public-visibility half of the decoration guard neutralised, `test_pending_review_run_shows_no_marker_for_staff_and_anonymous` goes red, and the same command restores the guard from its backup and asserts the restore landed before exiting.
    - `MonthCellCampaignMarkerTest`'s all-day fixture title is exactly 18 characters and the timed fixture title is exactly 16 characters.
    - `test_no_campaign_run_renders_marker_and_no_table_href` asserts on a string containing `self.no_campaign_run.pk`.
    - `solsys_code/tests/test_calendar_template.py` contains a method whose body asserts on HTML-entity forms of `&`, `<` and `"` appearing in both the `title` and the `aria-label` of the chip.
    - The full project test command exits 0 apart from the known network-flaky observatory-form test.
    - `pre-commit run ruff --all-files` and `pre-commit run ruff-format --all-files` both report no failures.
  </acceptance_criteria>
  <done>Each of the three WR-05 tests now fails when its own subject regresses, and the ANNOT-02 encoding edge is wired.</done>
</task>

</tasks>

## Review finding disposition

| Finding | Disposition | Where |
|---------|-------------|-------|
| CR-01 | Fixed | Task 1 |
| WR-05 | Fixed (all three sub-items) | Task 3 |
| WR-07 | Fixed | Task 2 |
| WR-08 | Partially fixed — constraint pinned by test (Task 1); runbook prose in 33-08 Task 3; positional page resolution deferred, see below | Task 1 + deferred |
| IN-01 | Fixed — `run_pk` gains a rendered consumer | Task 2 |
| IN-03 | Fixed | Task 2 |
| IN-05 | Fixed — distinct run-scoped tooltip chosen over suppressing the chip | Task 2 |

## Explicitly deferred review findings

- **WR-08 (positional page resolution for the `#run-{pk}` link).** The link is made a *known,
  tested* constraint by Task 1 and documented by 33-08 Task 3, but the reviewer's positional-query
  fix is not implemented. Rationale: computing a run's page inside `campaign_decoration()` adds an
  ordered `CampaignRun` query per rendered event, which directly contradicts plan 33-02's must-have
  "no per-event query for run/campaign" and would break its wired guard
  `test_query_count_does_not_grow_with_number_of_attributed_events`. The alternative — a `?run={pk}`
  page-resolution parameter on `CampaignRunTableView` — is a new URL contract with its own
  filter-interaction surface, i.e. feature work rather than gap closure. Neither is a
  context-budget, missing-information or dependency constraint; this is a scope boundary between
  repairing a reported defect and adding a navigation feature, and it is recorded here rather than
  dropped.

## Flagged assumptions

- **`{% block additional_css %}` is the base template's only rendered CSS hook.** Confirmed by
  reading the installed `tom_common/base.html` (line 18, inside `<head>`, empty body). If a future
  TOM Toolkit upgrade renames or removes that block, Task 1's `tr:target` assertions go red rather
  than the highlight silently disappearing again — which is the point of asserting on the rendered
  response instead of on the template source.
- **`table_url is None` is used as the campaign-presence signal in `campaign_chip.html`.** That
  equivalence holds because `campaign_decoration()` sets `table_url` inside `if run.campaign_id is
  not None:` and nowhere else. If a later change makes `table_url` conditional on anything other
  than campaign presence, the chip's no-campaign branch becomes wrong; the encoding/no-campaign
  assertions in Task 3 are what would catch it.

## Artifacts this phase produces

New or changed symbols these plans create — excluded from source-grounding drift verification:

- `src/templates/tom_calendar/partials/campaign_chip.html` (new template partial)
- `{% block additional_css %}` usage in `src/templates/campaigns/campaignrun_table.html`
- `TestCampaignRunAnchorPagination` (new test class, `solsys_code/tests/test_campaign_views.py`)
- New `TestCampaignRunRowAnchor` methods asserting the `tr:target` rule is served, including the
  zero-run-campaign case
- Revised `MonthCellCampaignMarkerTest.test_chip_does_not_consume_title_truncation_budget`
- Revised `DecorationSurvivalAndGuardsTest.test_no_campaign_run_renders_marker_and_no_table_href`
  and `DecorationSurvivalAndGuardsTest.test_pending_review_run_shows_no_marker_for_staff_and_anonymous`
- New campaign-name escaping test method in `DecorationSurvivalAndGuardsTest`
- A second `TargetList` fixture on `DecorationSurvivalAndGuardsTest` owned by `pending_run`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anonymous browser -> `campaigns:table` | An unauthenticated reader renders the campaign run table; the view already excludes pending-review runs. |
| anonymous browser -> `/calendar/` month view | An unauthenticated reader renders every attributed event's chip; campaign names and run identity cross this boundary. |
| campaign/run data -> HTML attribute values | `campaign_name` is operator-supplied text rendered into `title=` and `aria-label=`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-06-01 | Information disclosure | `campaign_chip.html` accessible name | medium | mitigate | The chip is rendered only when `campaign_decoration()` returns a value, and that tag returns `None` for a run that is not publicly visible. Task 3's rewritten pending-run test is the wired proof that adding a second, screen-reader-reachable label does not widen the disclosure surface. |
| T-33-06-02 | Tampering (stored XSS) | campaign name -> `title=` / `aria-label=` | high | mitigate | Both attributes rely on Django's autoescape; the partial adds no `\|safe` and no manual escaping. Task 3's encoding test asserts `&`, `<` and `"` appear only in entity form in both attributes. ASVS L1 output-encoding control. |
| T-33-06-03 | Information disclosure | `event_form.html` single-gate restructure | medium | mitigate | Collapsing two gates into one could widen what the modal shows. The surviving `{% elif not event.telescope_label_meta.run and request.user.is_staff %}` keeps the attribution-queue hint staff-only, and the existing pending-run modal tests (`test_pending_run_shows_no_run_block_to_anonymous_visitor` / `..._to_staff_visitor`) must stay green — they are re-run by Task 2's verify. |
| T-33-06-04 | Information disclosure | paginated campaign table | low | accept | WR-08's dead link exposes nothing; page 2 is reachable by any reader who could reach page 1. The failure mode is navigational, not a disclosure. |
| T-33-06-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no package from any package manager — every change is to files already tracked in this repository — so no package-legitimacy audit or blocking human checkpoint applies. |
</threat_model>

<verification>
- `python manage.py test solsys_code.tests.test_campaign_views solsys_code.tests.test_calendar_template solsys_code.tests.test_calendar_display_extras solsys_code.tests.test_null_campaign_guards` exits 0 with `OK`.
- The project's full-suite command exits 0 apart from the known `data.minorplanetcenter.net` network flake recorded in `deferred-items.md`.
- `pre-commit run ruff --all-files` and `pre-commit run ruff-format --all-files` are clean.
- `git ls-files` lists every path in `files_modified` except the new `campaign_chip.html`, which is a new tracked file added by this plan.
</verification>

<success_criteria>
- CR-01 is closed: a rendered GET of the campaign table page contains the `tr:target` rule for staff and anonymous readers, asserted by tests.
- WR-05, WR-07, IN-01, IN-03 and IN-05 are each closed by a task above; WR-08 is pinned by a test and its remaining half is recorded under "Explicitly deferred review findings".
- Every `must_haves.truths` entry has a wired assertion in `solsys_code/tests/test_campaign_views.py` or `solsys_code/tests/test_calendar_template.py`.
- No `CalendarEvent` field is written anywhere in this plan's diff.
</success_criteria>

<output>
Create `.planning/phases/33-series-identity-reconciler-inversion/33-06-SUMMARY.md` when done
</output>
