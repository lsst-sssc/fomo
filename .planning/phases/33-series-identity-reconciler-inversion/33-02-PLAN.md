---
phase: 33-series-identity-reconciler-inversion
plan: 02
type: execute
wave: 2
depends_on: ["33-01"]
files_modified:
  - src/templates/tom_calendar/partials/calendar.html
  - solsys_code/views.py
  - solsys_code/campaign_tables.py
  - src/templates/campaigns/campaignrun_table.html
  - solsys_code/tests/test_calendar_template.py
  - solsys_code/tests/test_campaign_views.py
autonomous: true
requirements: [ANNOT-02]

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every month-cell entry for an event attributed to a publicly-visible run shows a compact campaign marker carrying the campaign name as its tooltip (D-10/D-11)."
    - "The campaign marker adds no characters to the event title text the month cell truncates — the all-day entry keeps `truncatechars:18` and the timed entry keeps `truncatechars:16` over the event's own title only, and the campaign name appears solely in the marker's tooltip attribute, HTML-escaped by Django autoescaping (ANNOT-02 encoding edge)."
    - "The campaign marker and the modal decoration both survive a from-scratch rewrite of the event's own title and description — nothing about the decoration is stored on the CalendarEvent (ROADMAP criterion 3)."
    - "An event whose attributed run has campaign set to None renders the month cell with a marker and no campaign-table link, and the month view responds 200 — never NoReverseMatch (RESEARCH.md Pitfall 1)."
    - "An event attributed to a run that is not publicly visible shows no marker in the month cell, for staff and anonymous visitors alike."
    - "Rendering a month of events carries no per-event query for the run or its campaign: the month view's event queryset prefetches the companion row with the run and campaign already selected (DISPLAY-09 pattern, RESEARCH.md Pattern 4)."
    - "The decoration's campaign-table link lands on the run's own row: each row of the campaign run table carries an `id` of `run-{pk}` for staff and non-staff rows alike, and the targeted row is visually highlighted (D-13)."
    - "The month cell never renders contact_person, contact_email, or CampaignRun.source."
  artifacts:
    - src/templates/tom_calendar/partials/calendar.html
    - solsys_code/views.py
    - solsys_code/campaign_tables.py
    - src/templates/campaigns/campaignrun_table.html
    - solsys_code/tests/test_calendar_template.py
  key_links:
    - "fomo_render_calendar's event queryset -> Prefetch('telescope_label_meta', queryset=CalendarEventMeta.objects.select_related('run__campaign')) -> campaign_decoration() in the month cell: without this the marker N+1s on every event."
    - "campaign_decoration().table_url (plan 33-01) -> CampaignRunTable row id `run-{pk}` -> the highlighted row: the far end of the decoration's link."
  prohibitions:
    - statement: "The month-cell marker MUST NOT consume any part of the truncated event-title text budget, and MUST NOT be produced by writing campaign text into CalendarEvent.title."
      status: resolved
      verification: judgment
    - statement: "The month cell MUST NOT render contact_person, contact_email, contact_public_opt_in, or CampaignRun.source, and MUST NOT render any field of a run that is not publicly visible."
      status: resolved
      verification: judgment
---

<objective>
Complete the display-time decoration: put the campaign marker in the month cell, make it
N+1-free, and give the decoration's link a real landing spot on the campaign run table.

Purpose: plan 33-01 proved the decoration path through the modal. This plan extends it to the
surface a user actually scans — the month grid — without spending any of the cell's tight title
budget, and closes the loop on D-13's "link back to the run" by adding the anchor and highlight
the link targets.

Output: the marker in both month-cell event loops; an extended prefetch chain in
`fomo_render_calendar`; `run-{pk}` row ids plus a target highlight on `CampaignRunTable`; and
the tests for ROADMAP criterion 3 (decoration survives a from-scratch field rewrite), the
campaign-less guard, the non-public-run gate, and the query count.
</objective>

<execution_context>
@/home/tlister/git/fomo_devel/.claude/gsd-core/workflows/execute-plan.md
@/home/tlister/git/fomo_devel/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-series-identity-reconciler-inversion/33-CONTEXT.md
@.planning/phases/33-series-identity-reconciler-inversion/33-RESEARCH.md
@.planning/phases/33-series-identity-reconciler-inversion/33-PATTERNS.md
@.planning/phases/33-series-identity-reconciler-inversion/33-01-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Month-cell campaign marker, rendered from the link and prefetched</name>
  <files>src/templates/tom_calendar/partials/calendar.html, solsys_code/views.py, solsys_code/tests/test_calendar_template.py</files>
  <read_first>
    - src/templates/tom_calendar/partials/calendar.html lines 1-165 (the `<style>` block, where `.cal-event-classical`, `.cal-event-timed`, `.cal-event-all-day` and the stripe rules live) and lines 165-265 (the `{% load %}` line and both event loops)
    - solsys_code/templatetags/calendar_display_extras.py `campaign_decoration()` as added by plan 33-01, plus `proposal_color` / `status_border_css` / `neutral_slot_color` for the established consumption shape
    - solsys_code/views.py lines 1-45 (import block) and lines 104-120 (`fomo_render_calendar`'s DISPLAY-09 prefetch and `active_todo_count` annotation)
    - solsys_code/tests/test_calendar_template.py lines 43-120 (`CalendarTemplateTest` setup and how it reaches the month view through the test client) and lines 280-320
    - .planning/phases/33-series-identity-reconciler-inversion/33-RESEARCH.md "Architecture Patterns" Pattern 4 and 33-CONTEXT.md D-10, D-11, D-14
  </read_first>
  <action>
Render the campaign marker in the month grid from the same tag the modal uses, and make it cost
no extra query.

`src/templates/tom_calendar/partials/calendar.html`:

- Add `calendar_display_extras` — already on the `{% load tz calendar_tags calendar_display_extras %}`
  line — no load change needed; confirm it before assuming.
- In the `{% for event in day.all_day_events %}` loop and again in the `{% for event in day.events %}`
  loop, call `{% campaign_decoration event as campaign_deco %}` alongside the existing
  `{% proposal_color %}` / `{% status_border_css %}` calls, and render a compact marker inside the
  entry's own div when `campaign_deco` is truthy.
- The marker is a single inline `<span class="cal-campaign-chip" title="{{ campaign_deco.campaign_name }}">`
  holding one short glyph, placed immediately before the truncated title text in the all-day entry
  and immediately after the existing `cal-event-bullet` span in the timed entry. It must NOT be
  placed inside the `{{ event.title|truncatechars:N }}` expression and must not change either
  truncation length: the all-day entry keeps `truncatechars:18` and the timed entry keeps
  `truncatechars:16`, over `event.title` only (D-10's title-budget rule). The campaign name reaches
  the user through the `title` tooltip attribute, where Django's autoescaping handles it.
- Add a `.cal-campaign-chip` rule to the template's existing `<style>` block: small, inline, with a
  margin that separates it from the title, and a foreground colour that stays legible on both the
  white day cell and a proposal-coloured all-day fill. It must not paint a background that competes
  with the proposal fill, must not add a border (the `is_verified == False` dashed border and
  `status_border_css`'s ring already own the entry's border and box-shadow), and must leave the
  `--tel-color` stripe on classical entries untouched.
- Do not add any second campaign signal: `{{ event.title }}` is rendered as stored, and no campaign
  text is inserted into it.

`solsys_code/views.py` — `fomo_render_calendar`'s event queryset (the DISPLAY-09 block):

- Replace the bare `.prefetch_related('telescope_label_meta')` with
  `.prefetch_related(Prefetch('telescope_label_meta', queryset=CalendarEventMeta.objects.select_related('run__campaign')))`,
  importing `Prefetch` from `django.db.models` (the module already imports `Count` and `Q` from
  there) and `CalendarEventMeta` from `solsys_code.models`. Keep the `active_todo_count` annotation
  and the existing `start_time`/`end_time` filters exactly as they are.
- Extend the existing DISPLAY-09 comment above the queryset to say the run and campaign are now
  selected on the prefetched companion row because the month cell's campaign marker dereferences
  `run.campaign.name` per event.
- Change nothing else in `solsys_code/views.py`. This module triggers the SPICE kernel download on
  import: no test, probe, or notebook cell written for this phase may import it — reach the month
  view through the Django test client, as `test_calendar_template.py` already does.

`solsys_code/tests/test_calendar_template.py`:

- Add a test class covering the marker in the month grid: seed an all-day event and a timed event,
  each with a `CalendarEventMeta` row pointing at an approved `CampaignRun` with a campaign, GET the
  month view through the test client, and assert the response contains `cal-campaign-chip` and the
  campaign name inside a `title=` attribute. Use `NonSiderealTargetFactory` for any `Target` fixture.
  </action>
  <verify>
    <automated>python manage.py test solsys_code.tests.test_calendar_template</automated>
    <fails_when>non-zero exit, or "FAILED" / "Error" / "ERROR" appearing in the runner summary line</fails_when>
    <automated>grep -c 'campaign_decoration' src/templates/tom_calendar/partials/calendar.html</automated>
    <fails_when>exit 1 (no match), or a printed count below 2 — the marker is missing from one of the two month-cell event loops</fails_when>
    <automated>grep -c 'truncatechars:18\|truncatechars:16' src/templates/tom_calendar/partials/calendar.html</automated>
    <fails_when>exit 1 (no match), or a printed count other than 2 — a title truncation length was changed or removed</fails_when>
    <automated>grep -c "Prefetch('telescope_label_meta'" solsys_code/views.py</automated>
    <fails_when>exit 1 (no match) or a printed count of 0 — the month view still N+1s on the run and campaign</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/templates/tom_calendar/partials/calendar.html` contains `{% campaign_decoration event as campaign_deco %}` in both the `day.all_day_events` loop and the `day.events` loop, and contains a `.cal-campaign-chip` rule in its `<style>` block.
    - The two `truncatechars` filters still read `truncatechars:18` (all-day) and `truncatechars:16` (timed) and are still applied to `event.title` with nothing else concatenated inside the filter expression.
    - `solsys_code/views.py` imports `Prefetch` from `django.db.models` and `CalendarEventMeta` from `solsys_code.models`, and its month-view queryset passes a `Prefetch` object with `select_related('run__campaign')`.
    - Behavior: a GET of the month view containing an event attributed to an approved run with a campaign returns 200 and its body contains `cal-campaign-chip` and the campaign's name.
    - `pre-commit run ruff --all-files` and `pre-commit run ruff-format --all-files` both exit 0.
  </acceptance_criteria>
  <done>The month grid shows a campaign marker on every attributed, publicly-visible event, the marker costs no title characters and no extra query, and a test proves it renders.</done>
</task>

<task type="auto">
  <name>Task 2: Give the decoration's link a landing spot — anchored, highlighted run rows on the campaign table</name>
  <files>solsys_code/campaign_tables.py, src/templates/campaigns/campaignrun_table.html, solsys_code/tests/test_campaign_views.py</files>
  <read_first>
    - solsys_code/campaign_tables.py lines 1-100 (`CampaignRunTable`, its `Meta`, and the `Accessor(...).resolve(record, quiet=True)` idiom used by `render_run_status` / `render_approval_status` / `render_telescope_class` and why it must work for both dict rows and model rows)
    - solsys_code/campaign_views.py lines 88-175 (`ALLOWED_FIELDS_FOR_NON_STAFF`, which already lists `pk`, and `CampaignRunTableView.get_queryset()`'s staff model-instance vs non-staff `.values()` branches)
    - src/templates/campaigns/campaignrun_table.html (the whole file — the `{% render_table table %}` call and where a `<style>` block can live)
    - solsys_code/tests/test_campaign_views.py (the existing campaign-table view tests and their staff/non-staff client fixtures)
    - .planning/phases/33-series-identity-reconciler-inversion/33-CONTEXT.md decision D-13
  </read_first>
  <action>
D-13's link target: `campaigns:table` with a `#run-{pk}` fragment must land on the run's own row,
highlighted. Today no row carries an id, so the fragment scrolls nowhere.

- In `solsys_code/campaign_tables.py`, add a `row_attrs` entry to `CampaignRunTable.Meta` that sets
  each row's `id` to `run-` followed by the record's primary key. Resolve the pk with
  `Accessor('pk').resolve(record, quiet=True)` rather than `record.pk`: staff rows are model
  instances but non-staff rows are dicts from the view's `.values()` queryset, and only the
  Accessor form works for both — the same reason the three existing `render_*` methods use it.
  `pk` is already in `ALLOWED_FIELDS_FOR_NON_STAFF`, so no view change is needed; confirm that
  before assuming it.
- Add a short comment above `row_attrs` naming D-13 and the calendar decoration as the consumer, so
  a later reader does not delete an id that looks unused from inside this module.
- In `src/templates/campaigns/campaignrun_table.html`, add a `<style>` block (or extend an existing
  one) with a `tr:target` rule that highlights the targeted row — a soft background tint plus a
  left accent, using Bootstrap-compatible colours already present in this project's palette. It
  must be readable against the table's existing `table-bordered table-sm` styling and must not rely
  on JavaScript.
- In `solsys_code/tests/test_campaign_views.py`, add a test class asserting that a GET of
  `campaigns:table` for a campaign with at least one run contains `id="run-{run.pk}"`, for BOTH a
  staff client (model-instance rows) and an anonymous client (dict rows) — the dict-row case is the
  one the Accessor form exists for and the one most likely to regress.
  </action>
  <verify>
    <automated>python manage.py test solsys_code.tests.test_campaign_views</automated>
    <fails_when>non-zero exit, or "FAILED" / "Error" / "ERROR" appearing in the runner summary line</fails_when>
    <automated>grep -c 'row_attrs' solsys_code/campaign_tables.py</automated>
    <fails_when>exit 1 (no match) or a printed count of 0 — the run rows carry no anchor id</fails_when>
    <automated>grep -c ':target' src/templates/campaigns/campaignrun_table.html</automated>
    <fails_when>exit 1 (no match) or a printed count of 0 — the targeted row gets no highlight</fails_when>
  </verify>
  <acceptance_criteria>
    - `solsys_code/campaign_tables.py` `CampaignRunTable.Meta` defines `row_attrs` producing an `id` of the form `run-{pk}`, resolving the pk via `Accessor('pk').resolve(record, quiet=True)`.
    - Behavior: a staff GET of `campaigns:table` for a campaign with one run whose pk is N returns 200 and its body contains `id="run-N"`.
    - Behavior: an anonymous GET of the same URL (the run being approved) returns 200 and its body also contains `id="run-N"` — the dict-row branch.
    - `src/templates/campaigns/campaignrun_table.html` contains a `tr:target` CSS rule.
    - `pre-commit run ruff --all-files` and `pre-commit run ruff-format --all-files` both exit 0.
  </acceptance_criteria>
  <done>Clicking the decoration's campaign link lands on the run's own highlighted row on the campaign table, for staff and public readers alike.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove the decoration is display-time only, guarded, and query-cheap</name>
  <files>solsys_code/tests/test_calendar_template.py</files>
  <read_first>
    - solsys_code/tests/test_calendar_template.py (whole file — `CalendarTemplateTest`, `EventModalCampaignRunLinkTest` as extended by plan 33-01, `EventModalAttributionHintTest`)
    - solsys_code/templatetags/calendar_display_extras.py `campaign_decoration()` as added by plan 33-01
    - src/templates/tom_calendar/partials/calendar.html as modified by Task 1
    - .planning/phases/33-series-identity-reconciler-inversion/33-CONTEXT.md `<specifics>` (the criterion-3 rewrite test) and decisions D-10, D-11, D-14
    - .planning/phases/33-series-identity-reconciler-inversion/33-RESEARCH.md "Common Pitfalls" Pitfall 1 and "Security Domain"
  </read_first>
  <behavior>
    - Test: after rewriting a linked event's `title` and `description` from scratch (assign new values and save), a fresh GET of the month view still contains the campaign marker and a fresh GET of the modal still contains the decoration block — ROADMAP criterion 3.
    - Test: a month view containing an event attributed to an approved run whose `campaign` is None returns 200, contains the campaign marker, and contains no `campaigns:table` href.
    - Test: a month view containing an event attributed to a `pending_review` run contains no campaign marker, for a staff client and for an anonymous client.
    - Test: a month view containing an event attributed to a run whose `contact_person`, `contact_email` and `source` are all set contains none of those three values in its body.
    - Test: rendering a month whose grid holds N (at least 5) events each attributed to a distinct run does not grow the query count with N — asserted with `assertNumQueries` around the month GET, or by comparing the query count for 1 event against the count for N and asserting equality.
  </behavior>
  <action>
Add a test class to `solsys_code/tests/test_calendar_template.py` covering the five behaviors above.
No production file changes in this task.

- The criterion-3 test is the load-bearing one: seed a linked event, GET the month view and the
  modal and assert the decoration is present; then set `event.title` and `event.description` to
  entirely new values and `save()` them (simulating a base-layer re-projection); then GET both again
  and assert the decoration is still present and the new title text is what the cell shows. This is
  what proves the decoration lives on the link and not on the event.
- The query-count test must reach the month view through `self.client.get(...)`, never by importing
  `solsys_code.views` (SPICE kernel side effect). Prefer the count-comparison form (1 event vs N
  events) over a hard-coded `assertNumQueries` number, so an unrelated future query added to the
  month view does not make this test brittle — the assertion that matters is that the count does
  not grow with the number of attributed events.
- The PII test asserts absence of the run's contact and source values in the response body, giving
  T-33-02's mitigation a named regression on the public month surface.
- Use `NonSiderealTargetFactory` for any `Target` fixture and reuse the module's existing month-URL
  helper rather than rebuilding one.
  </action>
  <verify>
    <automated>python manage.py test solsys_code.tests.test_calendar_template</automated>
    <fails_when>non-zero exit, or "FAILED" / "Error" / "ERROR" appearing in the runner summary line</fails_when>
    <automated>LABELS=$(ls solsys_code/tests/test_*.py solsys_code/solsys_code_observatory/tests/test_*.py | grep -v "tests/test_views\.py$" | sed "s|/|.|g; s|\.py\$||" | tr "\n" " "); python manage.py test $LABELS && python manage.py test solsys_code.tests.test_views.TestSplitNumberUnitRegex solsys_code.tests.test_views.TestJPLSBDBQuery</automated>
    <fails_when>non-zero exit, or "FAILED" / "Error" / "ERROR" appearing in either runner summary line</fails_when>
  </verify>
  <acceptance_criteria>
    - Behavior: after assigning a brand-new `title` and `description` to a linked event and saving, a fresh GET of the month view still contains `cal-campaign-chip` and a fresh GET of that event's modal still contains the "Attributed campaign run" label — and the cell shows the new title text.
    - Behavior: a month view containing an event attributed to an approved run with `campaign=None` returns 200 and its body contains no `/campaigns/` table href for that run.
    - Behavior: a month view containing an event attributed to a `pending_review` run contains no `cal-campaign-chip`, for both a staff client and an anonymous client.
    - Behavior: a month view whose events are attributed to runs with `contact_person`, `contact_email` and `source` set contains none of those three values in its body.
    - Behavior: the query count for a month GET holding 5 attributed events equals the query count for the same GET holding 1 attributed event.
    - The project full-suite command from `.planning/config.json` `workflow.test_command` exits 0.
  </acceptance_criteria>
  <done>ROADMAP criterion 3 has a committed regression proving the decoration outlives a from-scratch rewrite of the event's own fields, and the campaign-less, non-public, PII and N+1 boundaries each have a named test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anonymous visitor -> calendar month view | `fomo_render_calendar` renders `calendar.html` for any visitor; every event's decoration is public |
| anonymous visitor -> campaign run table | `CampaignRunTableView` serves non-staff a PII-gated `.values()` queryset; anything added to a row must survive that gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-05 | Denial of Service | month-cell marker for a campaign-less run | high | mitigate | The marker consumes `campaign_decoration()`, which builds the campaign-table URL with `reverse()` and returns `None` for it when `run.campaign_id` is None — the month view never emits a `{% url %}` call against a null campaign pk. Task 3 adds a month-view regression for the `campaign=None` case |
| T-33-06 | Information Disclosure | month-cell marker tooltip | medium | mitigate | `campaign_decoration()` returns `None` for a run that is not publicly visible, so a pending-review run's campaign name never reaches the public month grid; Task 3 asserts the absence for both a staff and an anonymous client, plus the absence of `contact_person` / `contact_email` / `source` |
| T-33-07 | Information Disclosure | `run-{pk}` row ids on the campaign table | low | accept | The row id exposes only a `CampaignRun` primary key on rows the non-staff queryset already returns (`pk` is already in `ALLOWED_FIELDS_FOR_NON_STAFF` and already reachable through existing links); no new field crosses the PII gate |
| T-33-08 | Denial of Service | month-view query volume | medium | mitigate | The `Prefetch(...select_related('run__campaign'))` chain keeps the marker's run/campaign dereference off the per-event path; Task 3's count-comparison test fails if the count grows with the number of attributed events |
| T-33-SC | Tampering | package installs | low | accept | This plan runs no `pip`/`npm`/`cargo` install step; RESEARCH.md `## Package Legitimacy Audit` records that the phase installs no external package |
</threat_model>

<verification>
- `python manage.py test solsys_code.tests.test_calendar_template solsys_code.tests.test_campaign_views` exits 0.
- The project full-suite command (`.planning/config.json` `workflow.test_command`) exits 0.
- `pre-commit run ruff --all-files` and `pre-commit run ruff-format --all-files` exit 0.
- Behavior: the month view renders a campaign marker for an attributed, publicly-visible event and keeps rendering it after the event's title and description are rewritten from scratch.
- Behavior: a GET of the campaign run table contains `id="run-{pk}"` for staff and anonymous readers alike.

<human-check>
Open the month calendar in a browser on a month containing at least one campaign-attributed
event and confirm, by eye: (1) the marker is legible on a proposal-coloured all-day entry, on a
neutral classical entry, and on a timed entry; (2) it does not visually collide with the proposal
colour legend, the terminal-status ring, or the dashed unverified border; (3) hovering it shows the
campaign name; (4) clicking through the modal's campaign link scrolls to and highlights that run's
row on the campaign table.
</human-check>
</verification>

<success_criteria>
- ROADMAP criterion 3 holds end to end: the campaign decoration is visible in the month cell and in
  the modal, and survives a from-scratch rewrite of the event's own title and description.
- ANNOT-02 is satisfied on both display surfaces with no campaign text stored on any `CalendarEvent`.
- The month view's query count does not grow with the number of attributed events.
- The decoration's campaign link has a real, highlighted landing row.
</success_criteria>

<output>
Create `.planning/phases/33-series-identity-reconciler-inversion/33-02-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

Symbols, ids and rules created by THIS plan (none exist in the codebase before it):

- `src/templates/tom_calendar/partials/calendar.html`: the `.cal-campaign-chip` CSS class and the
  campaign-marker span in both month-cell event loops
- `solsys_code/campaign_tables.py`: `CampaignRunTable.Meta.row_attrs` producing the `run-{pk}` row id
- `src/templates/campaigns/campaignrun_table.html`: the `tr:target` highlight rule
- `solsys_code/tests/test_calendar_template.py`: a new month-cell marker test class and a new
  decoration-survival / guard / query-count test class
- `solsys_code/tests/test_campaign_views.py`: a new run-row anchor test class

Consumed from plan 33-01 (created there, not here): `campaign_decoration()` in
`solsys_code/templatetags/calendar_display_extras.py`.