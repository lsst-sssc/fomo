---
phase: 37-status-vocabulary-public-tallies-provenance-blind-gaps
plan: 06
type: execute
wave: 4
depends_on: ["37-04"]
files_modified:
  - solsys_code/templatetags/calendar_display_extras.py
  - src/templates/tom_calendar/partials/event_form.html
  - src/templates/tom_calendar/partials/calendar.html
  - solsys_code/tests/test_calendar_display_extras.py
  - solsys_code/tests/test_calendar_template.py
autonomous: true
requirements: [TALLY-01, UNUSED-01]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The calendar pop-up's attributed-campaign-run block carries the same tally the campaign table shows, rendered read-only from the event's run link under the existing public-visibility gate — no new run detail page and no new route (TALLY-01, D-09)."
    - "An awarded night that came and went with nothing scheduled or observed is visibly different on the calendar from a realised night (UNUSED-01)."
    - "The unused state is carried by two independent channels — a muted chip style and a visible `[U]` token in the rendered label — so it is never colour-alone and a screen reader hears it (D-13)."
    - "The `[U]` token is added at render time and never written into a stored title, so the allocation projector's no-churn contract is untouched and a run's nights never flip one by one across ticks (D-12)."
    - "A cancelled or weathered run's night reads `[C]`/`[W]`, never unused — staff run status always wins (D-14)."
    - "`[U]` gets no ring of its own; the unused signal composes with the existing ring vocabulary rather than adding a fourth ring colour (D-13)."
    - "The legend's `[U]` entry is click-to-filter like the proposal swatches, so showing only the unused nights is one click (D-15)."
    - "The calendar's unused count and the campaign table's unused count come from the same classifier function, so the two surfaces agree by construction (D-15)."
  artifacts:
    - src/templates/tom_calendar/partials/event_form.html
    - src/templates/tom_calendar/partials/calendar.html
  key_links:
    - "calendar_display_extras.run_tally() -> campaign_tally.get_or_compute_tally() -> event_form.html attributed-run block"
    - "calendar_display_extras.unused_night_decoration() -> campaign_tally.is_unused_allocation_night() (the same function the table's count uses)"
    - "calendar.html legend entry -> status_vocabulary.LEGEND -> the click-to-filter handler"
  prohibitions:
    - "An unused-night marker must never be presented as a measurement or as blame: a figure derived from the ten-hours-per-night rule of thumb must always carry its estimate qualifier, and neither the chip, its tooltip nor the legend may name or imply a person responsible for a night going unused."
---

<objective>
Put the tally in the calendar pop-up and make an unused awarded night look unused (TALLY-01
pop-up half, UNUSED-01).

Purpose: D-09 decided that "run detail" means the pop-up's attributed-run block rather than a
new page, and D-12/D-13 decided the unused state is derived at display time and shown through
two channels. Both are display-time decorations read from a link — the Phase 33 pattern this
codebase already uses — so nothing here writes to a calendar event.

Output: two new template tags in `calendar_display_extras`, a tally line in the pop-up's
attributed-run block, a muted `[U]` chip treatment on the calendar, and a click-to-filter legend
entry for unused nights.

**Paired docs:** owned by plan 37-07 (wave 5).
</objective>

<execution_context>
@/home/tlister/git/fomo_devel/.claude/gsd-core/workflows/execute-plan.md
@/home/tlister/git/fomo_devel/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-status-vocabulary-public-tallies-provenance-blind-gaps/37-CONTEXT.md
@.planning/phases/37-status-vocabulary-public-tallies-provenance-blind-gaps/37-RESEARCH.md
@.planning/phases/37-status-vocabulary-public-tallies-provenance-blind-gaps/37-PATTERNS.md
@CLAUDE.md
</context>

<flagged_assumptions>
Edge-probe rows that stayed `unresolved` (`unclassified` — carried forward, not dropped):

- **UNUSED-01 / unclassified** — the probe could not classify a shape axis for "an allocation
  event still standing after its night has passed is visually distinct from a realised night".
  Planner assumption: the observable criteria are the two-channel rule (muted style AND a `[U]`
  token in the label), the D-14 precedence rule (a cancelled or weathered run status wins), and
  the boundary instant (a night is unused strictly after its `end_time`, with no grace period).
  All three are in `must_haves` above and in the task behaviours. The boundary instant in
  particular is a Claude's-Discretion choice recorded here rather than derived from the
  requirement — if a grace period turns out to be wanted, this row needs a human decision.
</flagged_assumptions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The run tally in the calendar pop-up's attributed-run block</name>
  <reversibility rating="reversible">A new tag and a sub-line in one template partial; the tally is read from an existing link and nothing is written.</reversibility>
  <files>solsys_code/templatetags/calendar_display_extras.py, src/templates/tom_calendar/partials/event_form.html, solsys_code/tests/test_calendar_display_extras.py, solsys_code/tests/test_calendar_template.py</files>
  <read_first>
    - `solsys_code/templatetags/calendar_display_extras.py` lines 492-562 — `campaign_decoration()` in full: the `isinstance` guard, the `ObjectDoesNotExist` guard, the `run is None or not run.is_publicly_visible` gate, the `reverse()`-in-Python rationale for `table_url`, and the plain-dict return. This is the exact shape the new tag copies.
    - `solsys_code/templatetags/calendar_display_extras.py` `observation_series_decoration()` — the sibling tag, for how the two visibility gates are documented side by side.
    - `src/templates/tom_calendar/partials/event_form.html` lines 161-200 — the `{% campaign_decoration event as deco %}` block, its WR-04 `window_start` guard comment, and the `{% elif not event.telescope_label_meta.run and request.user.is_staff %}` branch below it.
    - `solsys_code/campaign_tally.py` (from 37-04) — `get_or_compute_tally()`, `tally_segments()` and the tally dict keys.
    - `solsys_code/models.py` lines 418-435 — `is_publicly_visible`.
    - `.planning/phases/37-.../37-CONTEXT.md` D-09.
  </read_first>
  <behavior>
    - `run_tally(event)` returns `None` for a non-`CalendarEvent` value, for an event with no companion row, for a companion row with no run, and for a run that is not publicly visible.
    - For an event attributed to a publicly visible run it returns a plain dict of pre-computed values including the ordered segments, and never raises.
    - The pop-up shows the tally line inside the existing attributed-run block, so the public-visibility check is applied once rather than re-derived.
    - An anonymous visitor sees the tally for an approved run's event and sees no block at all for a pending-review run's event.
    - A run whose unused figure is an estimate renders the estimate qualifier; one whose figure is not yet known renders a not-yet-known word rather than a zero.
  </behavior>
  <action>
Add `run_tally(event)` to `solsys_code/templatetags/calendar_display_extras.py` as a
`@register.simple_tag`, copying `campaign_decoration()`'s structure guard for guard: the
`isinstance(event, CalendarEvent)` check with the same rationale (the create-event form context
has no `event` key, so the tag can be called with a non-event value), the
`event.telescope_label_meta` lookup wrapped in `try/except ObjectDoesNotExist`, and the
`run is None or not run.is_publicly_visible` gate. On success return a plain dict built from
`campaign_tally.get_or_compute_tally(run)` carrying `groups`, `records`, the list from
`tally_segments()` and an `unused_known` flag, plus a short words-only summary string for the
tooltip and the accessible name. Docstring must state: this tag never raises, makes no write of
any kind, and reads the tally from the run link exactly as `campaign_decoration()` reads the
campaign name — the Phase 33 display-time-decoration pattern.

In `src/templates/tom_calendar/partials/event_form.html`, render the tally as a further
sub-line **inside** the existing `{% if deco %}` block's `<div class="col">`, not as a new
top-level `{% if %}` — the block already applies the `is_publicly_visible` gate through
`campaign_decoration()`, so a second gate would re-derive a rule that is already established
there. Call `{% run_tally event as tally %}` and render the segments in their given order with
each letter followed by its count and its word, so the letters are not the only channel. Guard
each compound sub-field the way the existing WR-04 comment guards `window_start`, so a
not-yet-known unused figure never renders as a bare token.

Extend `solsys_code/tests/test_calendar_display_extras.py` with a test class for the tag's five
guard cases and the success case, and `solsys_code/tests/test_calendar_template.py` (or the
existing pop-up rendering test module) with an anonymous-client render assertion for an approved
run's event and a pending-review run's event. `Target` fixtures use `NonSiderealTargetFactory`.
  </action>
  <verify>
    <automated>python manage.py test solsys_code.tests.test_calendar_display_extras solsys_code.tests.test_calendar_template</automated>
    <fails_when>non-zero exit, or "FAILED" / "Error" appears in the run summary, or the summary reports "Ran 0 tests"</fails_when>
    <automated>python -c "import os,django;os.environ.setdefault('DJANGO_SETTINGS_MODULE','src.fomo.settings');django.setup();import solsys_code.templatetags.calendar_display_extras as cde;print(callable(cde.run_tally), cde.run_tally(None), cde.run_tally('not-an-event'))"</automated>
    <fails_when>non-zero exit (an exception here breaks the never-raises contract), or the callable check prints False, or either guard call prints anything other than None</fails_when>
    <automated>python -c "src=open('src/templates/tom_calendar/partials/event_form.html').read();print(src.count('run_tally'), src.count('campaign_decoration'))"</automated>
    <fails_when>non-zero exit, or the run_tally count is 0, or the campaign_decoration count is not 1 (the tally must sit inside the existing block, not in a second gate of its own)</fails_when>
  </verify>
  <acceptance_criteria>
    - `calendar_display_extras.run_tally` exists, is registered as a simple tag, and returns `None` for each of the four guard cases without raising.
    - `event_form.html` calls `{% run_tally event as tally %}` inside the existing `{% if deco %}` block and still calls `{% campaign_decoration event as deco %}` exactly once.
    - An anonymous GET of an approved run's event modal contains the tally; the same GET for a pending-review run's event contains no attributed-run block at all (asserted in tests).
    - A not-yet-known unused figure renders a word, not a zero (asserted in tests).
    - `python manage.py test solsys_code.tests.test_calendar_display_extras solsys_code.tests.test_calendar_template` exits 0.
  </acceptance_criteria>
  <done>Clicking a calendar entry attributed to a publicly visible run shows that run's live tally in the block that already names the campaign — read from the link, gated once, written nowhere.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: An unused awarded night looks unused — muted chip, a [U] token, and one-click filtering</name>
  <reversibility rating="reversible">Display-time derivation plus CSS and a template token; D-12 chose not to write a marker, so a later change to the rule needs no re-title and no migration.</reversibility>
  <files>solsys_code/templatetags/calendar_display_extras.py, src/templates/tom_calendar/partials/calendar.html, solsys_code/tests/test_calendar_display_extras.py, solsys_code/tests/test_calendar_template.py</files>
  <read_first>
    - `solsys_code/templatetags/calendar_display_extras.py` lines 492-562 (`campaign_decoration()` — the guard shape) and the ring/legend functions as rewritten by 37-01.
    - `src/templates/tom_calendar/partials/calendar.html` lines 135-180 (the `cal-legend-chip`, `cal-legend-swatch`, `cal-legend-telescope` and `cal-filtering`/`cal-filter-match` CSS rules), lines 236-300 (both event loops — the all-day branch and the timed branch, with their `data-proposal` attributes, `status_border_css` style strings and `{{ event.title|truncatechars:N }}` render points), lines 315-337 (the three legend loops, including the `observation_status_legend` one) and lines 350-376 (the click-to-filter handler in full).
    - `solsys_code/campaign_tally.py` (from 37-04) — `is_unused_allocation_night()`, the shared classifier this tag must call rather than re-implement.
    - `solsys_code/allocation_projector.py` lines 155-171 — `ALLOC_URL_NAMESPACE` and `allocation_events()`, so the tag can tell an allocation night's event from any other event by its url key.
    - `solsys_code/status_vocabulary.py` (from 37-01) — `MARKER[DisplayState.UNUSED]`, `LABEL`, `LEGEND`, and the ring buckets that deliberately exclude the unused state.
    - `.planning/phases/37-.../37-CONTEXT.md` D-12, D-13, D-14, D-15.
  </read_first>
  <behavior>
    - `unused_night_decoration(event)` returns `None` for any event whose url is not in the `ALLOC:` namespace.
    - It returns `None` for an allocation night whose `end_time` is still in the future.
    - It returns `None` for an allocation night whose run status is cancelled or weathered, whatever the time (D-14).
    - It returns a dict carrying the `[U]` token and its label for an elapsed, still-standing allocation night on a run in any other status.
    - The rendered chip for such a night is visually muted and its label reads with the `[U]` token in front of the stored title text.
    - The stored `CalendarEvent.title` is unchanged by rendering the page — the token exists only in the response body.
    - `status_border_css()` returns no ring for that event on account of its being unused; any ring it already had for another reason is unaffected.
    - Clicking the legend's `[U]` entry filters the month view to the unused nights, and clicking it again clears the filter, matching the proposal swatches' toggle behaviour.
  </behavior>
  <action>
Add `unused_night_decoration(event)` to `solsys_code/templatetags/calendar_display_extras.py`
as a `@register.simple_tag`, guarded exactly like `campaign_decoration()`: the `isinstance`
check, the `try/except ObjectDoesNotExist` companion-row lookup, and a `None` return for any
event whose `url` does not start with `allocation_projector.ALLOC_URL_NAMESPACE`. Read the run
through the companion row's `run` (never a bare attribute chain) and delegate the decision to
`campaign_tally.is_unused_allocation_night(event.end_time, run.run_status)` — this is D-15's
"one shared classifier function", so the tag must not re-implement either half of the rule.
On a True result return a plain dict with the token from
`status_vocabulary.MARKER[DisplayState.UNUSED]`, the label from `LABEL`, and a words-only
tooltip string explaining that the awarded night passed with nothing scheduled or observed.
The docstring must state that the token is added at render time and never written into the
event's title, and that this tag never raises.

In `src/templates/tom_calendar/partials/calendar.html`:

- Call `{% unused_night_decoration event as unused %}` in **both** event loops (the all-day
  branch and the timed branch), beside the existing `{% campaign_decoration %}` call.
- When `unused` is set, add a `cal-event-unused` class to the chip and a `data-unused="1"`
  attribute on the outer `.cal-event` element, and prepend `{{ unused.token }}` to the rendered
  title text. Do not alter the `{{ status_border }}` style fragment — D-13 explicitly rejects a
  fourth ring colour, so the two signals compose instead of merging.
- Add a `.cal-event-unused` CSS rule in the existing `additional_css` style block: reduced
  opacity and a dashed border, consistent with the existing dashed-border treatment used for the
  unverified-label branch, and distinct enough from a realised night to read at a glance. Keep
  it a style-only rule — the text token is the other channel and must not be replaced by it.
- Give the legend's `[U]` entry (already emitted by the `observation_status_legend` loop from
  `status_vocabulary.LEGEND`) the clickable treatment: render the unused entry with the
  `cal-legend-swatch` affordances the proposal swatches use (`role="button"`,
  `aria-pressed="false"`) plus a `data-filter="unused"` attribute.
- Extend the click handler at the bottom of the file so one shared toggle serves both filter
  kinds: a click on a proposal swatch keeps filtering on `data-proposal` exactly as today, and a
  click on the unused entry toggles `cal-filter-match` on elements carrying `data-unused="1"`.
  Keep the existing single-active-filter behaviour (clicking the active entry clears it) and the
  existing `aria-pressed` bookkeeping.

Extend `solsys_code/tests/test_calendar_display_extras.py` with a test class covering the tag's
four decision cases, including both D-14 suppression statuses, and one asserting the stored
title is unchanged after rendering. Extend `solsys_code/tests/test_calendar_template.py` with a
month-view render assertion that an elapsed allocation night carries the muted class, the
`data-unused` attribute and the `[U]` token in its label, and that a future allocation night
carries none of the three.
  </action>
  <verify>
    <automated>python manage.py test solsys_code.tests.test_calendar_display_extras solsys_code.tests.test_calendar_template</automated>
    <fails_when>non-zero exit, or "FAILED" / "Error" appears in the run summary, or the summary reports "Ran 0 tests"</fails_when>
    <automated>python -c "import os,django;os.environ.setdefault('DJANGO_SETTINGS_MODULE','src.fomo.settings');django.setup();import inspect;import solsys_code.templatetags.calendar_display_extras as cde;print(callable(cde.unused_night_decoration), cde.unused_night_decoration(None), 'is_unused_allocation_night' in inspect.getsource(cde.unused_night_decoration))"</automated>
    <fails_when>non-zero exit (an exception breaks the never-raises contract), or the callable check prints False, or the guard call prints anything other than None, or the shared-classifier check prints False</fails_when>
    <automated>python -c "src=open('src/templates/tom_calendar/partials/calendar.html').read();print(src.count('unused_night_decoration'), src.count('data-unused'), 'cal-event-unused' in src, 'data-filter=\"unused\"' in src)"</automated>
    <fails_when>non-zero exit, or the tag-call count is fewer than 2 (both event loops must call it), or the data-unused count is fewer than 2, or either boolean prints False</fails_when>
    <automated>pre-commit run ruff --all-files &amp;&amp; pre-commit run ruff-format --all-files</automated>
    <fails_when>non-zero exit, or either hook line reads "Failed"</fails_when>
  </verify>
  <acceptance_criteria>
    - `calendar_display_extras.unused_night_decoration` exists, returns `None` for a non-event and for a non-allocation event, and calls `campaign_tally.is_unused_allocation_night` rather than re-implementing the rule.
    - Both event loops in `calendar.html` call the tag, and both emit `data-unused` on the outer chip element when it fires.
    - `calendar.html` defines a `.cal-event-unused` style rule and a `data-filter="unused"` legend entry.
    - A month-view render test proves an elapsed allocation night shows the muted class, the `data-unused` attribute and the `[U]` token, while a future one shows none of them.
    - A test proves the stored `CalendarEvent.title` is byte-identical before and after rendering the month view.
    - A test proves a cancelled and a weathered run's elapsed allocation night are not marked unused.
    - `status_border_css()` is unchanged by this task (the unused state remains out of both ring buckets).
    - `pre-commit run ruff --all-files` and `pre-commit run ruff-format --all-files` exit 0.
  </acceptance_criteria>
  <done>An awarded night that passed with nothing scheduled or observed reads as unused in two independent channels, decided by the same function the table's count uses, written nowhere, and filterable from the legend in one click.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Anonymous visitor -> calendar month view and event pop-up | Both are public; every tag called from them renders to an unauthenticated reader. |
| `CalendarEventMeta.run` link -> rendered decoration | The link is what decides whether a campaign fact is shown at all. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-21 | Information Disclosure | `run_tally()` | high | mitigate | Gate the tag exactly like `campaign_decoration()`: `run is None or not run.is_publicly_visible` returns `None`, so a pending-review run's tally never reaches a non-staff visitor. A test asserts the pending case renders no block. |
| T-37-22 | Information Disclosure | the pop-up tally line | medium | mitigate | The line renders counts only — no group name, no record id, no proposal code — so it cannot leak the internal `RequestGroup` identifier the observation-series block is already withheld from anonymous visitors to protect. |
| T-37-23 | Information Disclosure | `unused_night_decoration()` | medium | mitigate | The tag reads `run.run_status` through the guarded companion-row lookup and emits only a fixed token and a fixed label; it never renders run text, so an unattributed or hidden run cannot leak through it. |
| T-37-24 | Tampering | rendered `[U]` token | low | mitigate | The token is prepended by the template from a constant, never written into `CalendarEvent.title`; a test asserts the stored title is unchanged after a render, so a user-editable title cannot acquire a permanent marker this way. |
| T-37-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new package: RESEARCH.md's Package Legitimacy Audit records zero new dependencies for Phase 37, and this plan installs nothing. |

**Canon-referral breadcrumbs (not minted as prohibitions):** template-injection and output-escaping
concerns on rendered decorations are canon — covered by `/gsd-secure-phase` and by the existing
`format_html`/auto-escaping discipline in this module.
</threat_model>

## Artifacts this phase produces

Symbols created by this plan (new — exclude from drift verification):

- `solsys_code/templatetags/calendar_display_extras.py`: `run_tally()` (simple tag),
  `unused_night_decoration()` (simple tag)
- `src/templates/tom_calendar/partials/event_form.html`: the tally sub-line inside the
  attributed-run block
- `src/templates/tom_calendar/partials/calendar.html`: the `cal-event-unused` CSS class, the
  `data-unused` chip attribute, the `data-filter="unused"` legend entry and the extended
  click-to-filter handler

<verification>
- `python manage.py test solsys_code.tests.test_calendar_display_extras solsys_code.tests.test_calendar_template` exits 0.
- `python manage.py test solsys_code` (excluding `test_views.TestEphemeris`) exits 0.
- `pre-commit run ruff --all-files` and `pre-commit run ruff-format --all-files` exit 0.
- A month-view render leaves every `CalendarEvent.title` byte-identical.
</verification>

<success_criteria>
- The pop-up shows a run's tally under the same gate that already shows its campaign.
- An unused awarded night is distinguishable from a realised one by style and by text.
- The unused decision comes from one shared function, and one click on the legend isolates the
  unused nights.
</success_criteria>

<output>
Create `.planning/phases/37-status-vocabulary-public-tallies-provenance-blind-gaps/37-06-SUMMARY.md` when done
</output>
