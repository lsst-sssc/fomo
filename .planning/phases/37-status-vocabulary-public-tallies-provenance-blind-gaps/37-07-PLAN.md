---
phase: 37-status-vocabulary-public-tallies-provenance-blind-gaps
plan: 07
type: execute
wave: 5
depends_on: ["37-01", "37-02", "37-03", "37-05", "37-06"]
files_modified:
  - docs/runbooks/telescope_runs_calendar.rst
  - docs/notebooks/pre_executed/campaign_lifecycle_demo.ipynb
  - docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb
  - docs/notebooks/pre_executed/load_telescope_runs_demo.ipynb
  - docs/notebooks/pre_executed/project_observation_calendar_demo.ipynb
  - solsys_code/status_vocabulary.py
  - solsys_code/tests/test_status_vocabulary.py
  - solsys_code/tests/test_views.py
autonomous: true
requirements: [STATUS-01, TALLY-01, TALLY-02, UNUSED-01, GAPB-01]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The operator runbook documents the final status vocabulary — nine markers with their meanings, including the placed-but-unobserved state and the unused awarded night — with no remaining claim that the letters are provisional (STATUS-01)."
    - "The runbook has a coverage-gap section, written fresh: the feature shipped in v2.0/v2.1 with no operator-facing documentation at all, and GAPB-01 changes what it counts (GAPB-01)."
    - "The runbook's unattended-operation section lists five steps, including the proposal-allocation fetch and what its failure looks like."
    - "`campaign_lifecycle_demo.ipynb` demonstrates the public run tally, the campaign roll-up and an unused awarded night with real executed output (TALLY-01, TALLY-02, UNUSED-01)."
    - "The runbook states how fresh a tally is in operator words: a change to a linked observation record — the projector narrowing a queued request to a placed block — shows on the next page load, while a purely time-driven change lags by at most the cache lifetime, which is named (TALLY-01 'updating as the projector narrows')."
    - "The whole-suite verification an executor actually runs carries its own exclusion of the test known to segfault in native ASSIST, so the exclusion is a property of the invocation rather than a caveat in prose beside it."
    - "Every notebook whose executed output changed because a run-level title now spells `[C]`/`[W]` is regenerated and committed with that output."
    - "After the re-title sweep, no calendar event in the developer database carries a legacy bracket-word status prefix, and the retirement list is removed from the vocabulary module — leaving exactly one spelling of every marker (STATUS-01, RESEARCH Pitfall 1)."
  artifacts:
    - docs/runbooks/telescope_runs_calendar.rst
    - docs/notebooks/pre_executed/campaign_lifecycle_demo.ipynb
  key_links:
    - "status_vocabulary.LEGEND -> the runbook's documented marker table (one vocabulary, documented once)"
    - "unattended.STEPS -> the runbook's unattended step list"
    - "campaign_gap result keys -> the runbook's new coverage-gap section"
---

<objective>
Ship the paired docs this phase owes and close the title migration (CLAUDE.md paired-docs rule,
RESEARCH Pitfall 1).

Purpose: the ROADMAP names `campaign_lifecycle_demo.ipynb` and
`docs/runbooks/telescope_runs_calendar.rst` as this phase's paired docs, and three further
pre-executed notebooks carry executed output that changes when a run-level title respells to
`[C]`/`[W]`. RESEARCH.md also confirmed the runbook has no coverage-gap section at all — this
plan writes one rather than editing a section that does not exist. Finally, the retirement list
that kept legacy-spelled titles ringed during the migration can only be deleted once the
developer database has actually been re-titled, which is why that deletion lands here and not in
37-01.

Output: an updated runbook, four regenerated pre-executed notebooks, and a vocabulary module
with exactly one spelling per marker.
</objective>

<execution_context>
@/home/tlister/git/fomo_devel/.claude/gsd-core/workflows/execute-plan.md
@/home/tlister/git/fomo_devel/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-status-vocabulary-public-tallies-provenance-blind-gaps/37-CONTEXT.md
@.planning/phases/37-status-vocabulary-public-tallies-provenance-blind-gaps/37-RESEARCH.md
@.planning/phases/37-status-vocabulary-public-tallies-provenance-blind-gaps/37-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Runbook — the final vocabulary, the tallies, a coverage-gap section written fresh, and the fifth runner step</name>
  <reversibility rating="reversible">Documentation only.</reversibility>
  <files>docs/runbooks/telescope_runs_calendar.rst</files>
  <read_first>
    - `docs/runbooks/telescope_runs_calendar.rst` lines 116-215 — the "How do LCO/SOAR queue observations get onto the calendar?" section: the marker table, the "This letter vocabulary is provisional" sentence (around line 165), the legend paragraph naming the seven states and the ring rules (lines 169-176), and the "One-time title change" note (around lines 205-212).
    - The same file lines 646-662 — "How do I mark a run cancelled or weathered-out?", which documents the prefix a staff action prepends.
    - The same file lines 1417-1450 — "How do I run everything unattended?" and its numbered four-step list.
    - The same file lines 1900-1948 — the command cheat-sheet, and lines 1948-2292 — Troubleshooting, for where a new symptom entry belongs and what an entry looks like.
    - The same file lines 2292-2302 — "See also", and `docs/index.rst` line 24, where this runbook is wired into the toctree.
    - `solsys_code/status_vocabulary.py` — `LEGEND`, `LABEL`, `MARKER`, `RUN_STATUS_MARKER`, the ring buckets: the source of truth the prose must agree with.
    - `solsys_code/unattended.py` `STEPS` — the five step names and order.
    - `solsys_code/campaign_gap.py` — `clamp_date_range()` (the 90-day default / 180-day cap), `GAP_CACHE_TTL_SECONDS` (the one-hour cached result), `_EXCLUDED_RUN_STATUSES`, `observation_claimed_dates()` and the result-dict keys, for the facts the new section states.
    - `solsys_code/campaign_tally.py` — the tally dict keys, `HOURS_PER_NIGHT`'s meaning via `proposal_allocation`, and `is_unused_allocation_night()`'s rule.
  </read_first>
  <action>
Update `docs/runbooks/telescope_runs_calendar.rst`:

- **Marker table.** Extend it to the final nine-state vocabulary and correct the prose: add the
  weather/technical-failure marker and the unused-awarded-night marker, state that the
  scheduled marker means a block is scheduled and not yet observed (using the portal's own word,
  not the internal one), and state that the cancelled marker covers cancellation by whichever
  layer owns the entry — a staff decision on a run or a portal cancellation on a record — with
  the pop-up's run-status line as the place that distinguishes them. Delete the sentence saying
  the letter vocabulary is provisional and that Phase 37 owns its final wording; it is final now.
  Say plainly that one module defines these markers and that the calendar legend, the entry
  titles and the campaign table's Progress cell all read from it.
- **Ring rules.** Correct the paragraph describing which entries are ringed so it names the
  weathered marker among the terminal ones, and state explicitly that the unused marker adds no
  ring — it is shown by a muted chip and its own token so the signal never depends on colour
  alone.
- **New section: what a run and a campaign actually got.** A new operator-facing section
  describing the public tally: the group and record counts, the four night counts, that anyone
  can see it (not only staff), where it appears (the campaign table's Progress cell, the roll-up
  strip above the table, the campaign list badge, and the calendar pop-up's attributed-run
  block), that nights are counted on the site-local observing night rather than the UTC date,
  and that a run's own status is never changed by any of it — it stays whatever a staff member
  set. Explain the unused figure honestly: an exact count of still-standing elapsed allocation
  nights for a run that projects its own nights, and otherwise an estimate from the proposal's
  unused hours divided by the ten-hours-per-night rule of thumb, shown with its estimate
  qualifier, and shown as not-yet-known before the first successful portal fetch.
  End the section with a short "how fresh is it" paragraph, because the requirement says the
  tally updates as the projector narrows and an operator needs to know what that means in
  practice: a change to a linked observation record — a queued request narrowing to a placed
  block, a block coming back observed — shows **on the next page load**, because the cached
  counts are keyed on the newest linked-record change as well as on the run; a change that comes
  only from the clock — an awarded night elapsing into unused, or a freshly fetched proposal
  allocation — can take up to the cache lifetime, `TALLY_CACHE_TTL_SECONDS` in
  `solsys_code/campaign_tally.py`, one hour by default. Name that constant so an operator who
  wants it shorter knows where to look, and say plainly that nothing here ever changes a run's
  own status.
- **New section: coverage-gap analysis.** Written fresh — a full-text search of this file found
  no existing section. Cover: what the page answers (observable nights not claimed), where it is
  (the "Show Coverage Gaps" button on a campaign's runs page and when it is disabled), the
  90-day default and 180-day cap on the requested range, the one-hour cached result, what claims
  a night (an approved run's window, excluding cancelled/not-awarded/weathered runs, **and now**
  an observed or scheduled observation block on the campaign calendar), what does not claim (a
  queued request's window; an expired, cancelled or failed record), how an observation is
  assigned to a site and what the claimed-but-site-unknown count means, and the needs-review
  lists the page already shows.
- **Unattended section.** Change "four steps" to five, add the proposal-allocation step to the
  numbered list describing what it fetches and stores, and state what its failure looks like:
  the existing failure email names the failing step, the other steps still run, and a portal
  outage shows on the public pages as a not-yet-known unused figure rather than a blank page.
- **Cheat-sheet and Troubleshooting.** Add the new step to the cheat-sheet where the runner's
  steps are listed, and add one troubleshooting entry for "the unused figure says it is not yet
  known", pointing at the proposal-allocation step and at a run with a blank proposal code.
  </action>
  <verify>
    <automated>python -c "src=open('docs/runbooks/telescope_runs_calendar.rst').read();print('[W]' in src, '[U]' in src, 'five steps' in src, 'proposal_allocation' in src, src.count('Coverage'))"</automated>
    <fails_when>non-zero exit, or any of the first four booleans prints False, or the Coverage occurrence count is 0 (no coverage-gap section was written)</fails_when>
    <automated>python -c "src=open('docs/runbooks/telescope_runs_calendar.rst').read();print('vocabulary is provisional' in src, 'four steps' in src)"</automated>
    <fails_when>non-zero exit, or either boolean prints True (the provisional-vocabulary sentence or the four-step count survived)</fails_when>
    <automated>python -c "src=open('docs/runbooks/telescope_runs_calendar.rst').read();print('next page load' in src, 'TALLY_CACHE_TTL_SECONDS' in src)"</automated>
    <fails_when>non-zero exit, or either boolean prints False — the tally section must state both halves of the freshness bound: record-driven changes on the next page load, and time-driven ones within the named cache lifetime</fails_when>
    <automated>pre-commit run --all-files</automated>
    <fails_when>non-zero exit, or any hook line reads "Failed" (the Sphinx docs build runs here and fails on a malformed directive or a broken reference)</fails_when>
  </verify>
  <acceptance_criteria>
    - The runbook's marker table documents all nine markers including the weathered and unused ones.
    - The runbook contains a section documenting the public tally and naming all four of its display surfaces.
    - That section states the freshness bound in operator words — record-driven changes on the next page load, time-driven ones within the named cache lifetime — and names `TALLY_CACHE_TTL_SECONDS`.
    - The runbook contains a coverage-gap section naming both claim sources and the site-unknown count.
    - The unattended section lists five steps and describes the new step's failure signal.
    - The provisional-vocabulary sentence is gone and no "four steps" phrasing remains.
    - `pre-commit run --all-files` exits 0, including the Sphinx docs build hook.
  </acceptance_criteria>
  <done>An operator can read the runbook and decode every marker on the calendar, understand what the tallies count and what the unused estimate is, and find out for the first time how coverage-gap analysis decides a night is claimed.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate the four pre-executed notebooks against the shipped behaviour</name>
  <reversibility rating="reversible">Documentation artefacts regenerated from the code; re-running the generator reproduces them.</reversibility>
  <files>docs/notebooks/pre_executed/campaign_lifecycle_demo.ipynb, docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb, docs/notebooks/pre_executed/load_telescope_runs_demo.ipynb, docs/notebooks/pre_executed/project_observation_calendar_demo.ipynb</files>
  <read_first>
    - `docs/notebooks/pre_executed/campaign_lifecycle_demo.ipynb` — the whole notebook, especially the Django-setup cell's scratch-database routing and its teardown cell (the first and last code cells), the demo-scoped reset cell, the "Campaign decoration, rendered from the link" cell (which GETs the event modal with a public client), the "four-way payoff" cell, and the "The public campaign table" cell that already GETs `campaigns:table` with an anonymous client — the three natural homes for the new demonstrations.
    - `docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb` and `load_telescope_runs_demo.ipynb` — every cell whose printed output contains a run-level status prefix, since those spellings change.
    - `docs/notebooks/pre_executed/project_observation_calendar_demo.ipynb` and its companion `project_observation_calendar_demo.sched06-baseline.json` — check whether the sweep's reported stages or written titles changed at all; the record-level letters are unchanged by D-01, so this notebook may need regeneration only, not new cells.
    - `.planning/phases/35-allocation-layer-classical-cutover/35-11-PLAN.md` — the prior phase's notebook-regeneration plan, for the established cell-level verification style (an executed-count assertion plus targeted content greps).
    - `CLAUDE.md` — the paired-docs rule and the `pre_executed/` committed-output convention.
  </read_first>
  <action>
Add three demonstrations to `docs/notebooks/pre_executed/campaign_lifecycle_demo.ipynb`, each a
markdown cell plus a code cell, placed so they reuse the fixtures the notebook has already
built and leave the scratch-database routing and the teardown cell untouched:

1. **The public run tally.** Immediately after the existing "The public campaign table" cell:
   GET the campaign table with the same anonymous client and show the Progress cell's rendered
   segments for each of the four runs, alongside the campaign roll-up strip's totals. Assert in
   the cell that the anonymous response and a staff response carry the same segments for the
   same run, so the notebook demonstrates the "public, not staff-only" half of TALLY-01.
2. **The tally in the pop-up.** Extend or follow the existing campaign-decoration cell: GET the
   attributed event's modal with the public client and show the tally line inside the
   attributed-run block.
3. **An unused awarded night.** Seed one allocation night whose end time is in the past on a run
   in an ordinary status and one on a cancelled run, render the month view with the public
   client, and show that the first carries the unused token and the muted class while the second
   shows the cancelled marker instead — the D-14 precedence rule, demonstrated. Assert the
   stored titles are unchanged by the render, which is the D-12 no-write contract.

Regenerate every notebook whose executed output is affected, in each case with
`jupyter nbconvert --to notebook --execute --inplace <path>` and commit the executed output
(`pre_executed/` copies keep their output, unlike every other notebook in this repo):

- `campaign_lifecycle_demo.ipynb` (new cells above),
- `reconcile_campaign_runs_demo.ipynb` and `load_telescope_runs_demo.ipynb` (run-level title
  spelling changed from the bracket-word form to the letter form, and `load_telescope_runs` now
  also writes a structured proposal code — show the new field's value in the cell that already
  prints the created run's fields),
- `project_observation_calendar_demo.ipynb` — regenerate and confirm its output; if the sweep's
  reported stages and written titles are genuinely unchanged, say so in the summary rather than
  forcing a diff.

Every regenerated notebook must end with every code cell carrying a non-null execution count,
and none may leave a scratch database behind.
  </action>
  <verify>
    <automated>python -c "import json,sys;paths=['docs/notebooks/pre_executed/campaign_lifecycle_demo.ipynb','docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb','docs/notebooks/pre_executed/load_telescope_runs_demo.ipynb','docs/notebooks/pre_executed/project_observation_calendar_demo.ipynb'];bad=[];
for p in paths:
    nb=json.load(open(p));c=[x for x in nb['cells'] if x['cell_type']=='code'];bad+= [(p,i) for i,x in enumerate(c) if not x.get('execution_count')]
print(len(bad), bad[:5])"</automated>
    <fails_when>non-zero exit, or the unexecuted-cell count is anything other than 0 (a cell with a null execution count means the notebook was committed without being executed)</fails_when>
    <automated>python -c "src=open('docs/notebooks/pre_executed/campaign_lifecycle_demo.ipynb').read();print(src.count('groups'), src.count('[U]'), src.count('run_tally') + src.count('Progress'))"</automated>
    <fails_when>non-zero exit, or any of the three counts is 0 (the tally, the unused token or the Progress cell is not demonstrated)</fails_when>
    <automated>git status --porcelain docs/notebooks/pre_executed/</automated>
    <fails_when>the output lists an untracked scratch database or a stray generated file under that directory, or it lists none of the four notebooks as modified</fails_when>
  </verify>
  <acceptance_criteria>
    - All four notebooks have every code cell carrying a non-null execution count.
    - `campaign_lifecycle_demo.ipynb` contains executed output showing the Progress cell's segments, the campaign roll-up totals, the pop-up tally line and an unused awarded night beside a cancelled one.
    - `campaign_lifecycle_demo.ipynb` asserts, in a cell, that an anonymous and a staff response carry the same tally segments for the same run.
    - `load_telescope_runs_demo.ipynb` shows the created run's structured proposal code in its executed output.
    - No scratch database directory is left behind (the teardown cell still runs last in each notebook).
    - `git status --porcelain docs/notebooks/pre_executed/` lists only the intended notebook modifications.
  </acceptance_criteria>
  <done>The paired notebooks show the shipped behaviour with real executed output: a public tally on the table and in the pop-up, a campaign roll-up, an unused awarded night beside a cancelled one, and run-level titles in their final spelling.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Retire the legacy title spellings once the database has none left</name>
  <reversibility rating="costly">Deleting the retirement list means a deployment that has not yet run a sweep loses the status ring on its legacy-spelled entries until it does; restoring the list is a one-line revert, but the window is visible to users.</reversibility>
  <precondition>Running the reconciler sweep and the projector sweep against the developer database leaves no `CalendarEvent` title beginning with a legacy bracket-word status prefix. If any remain, halt and report rather than deleting the retirement list — that is the exact ordering RESEARCH Pitfall 1 warns about.</precondition>
  <files>solsys_code/status_vocabulary.py, solsys_code/tests/test_status_vocabulary.py, solsys_code/tests/test_views.py, docs/runbooks/telescope_runs_calendar.rst</files>
  <read_first>
    - `solsys_code/status_vocabulary.py` — `RETIRED_TITLE_PREFIXES` and `state_for_title()` as written in 37-01 Task 1, including the comment saying this plan deletes the list.
    - `solsys_code/tests/test_status_vocabulary.py` — the ring-equivalence assertion from 37-01 Task 2 that currently pins a legacy bracket-word title to the terminal ring; it changes here.
    - `solsys_code/tests/test_views.py` line 8 (`from django.test import Client, SimpleTestCase, TestCase`) and line 72 (`class TestEphemeris(TestCase)`) — the import line the tag decorator joins and the class it goes on.
    - `docs/runbooks/telescope_runs_calendar.rst` lines 205-212 — the existing "One-time title change" note from Phase 34, the precedent this task's note mirrors.
    - `solsys_code/management/commands/reconcile_campaign_runs.py` and `project_observation_calendar.py` — the two sweeps that re-derive titles, and their command names and options.
    - `solsys_code/unattended.py` `STEPS` — confirmation that both sweeps also run on every unattended tick, which is why a deployed host re-titles itself within one tick.
  </read_first>
  <behavior>
    - The whole `solsys_code` suite can be run by one command that does not execute the ephemeris test class known to segfault in native ASSIST, without that class being deleted or skipped for everyone.
    - After running both sweeps, a query over `CalendarEvent` for titles beginning with any legacy bracket-word status prefix returns zero rows.
    - `state_for_title()` resolves a letter-marker title to its display state and returns `None` for a legacy bracket-word title.
    - `status_border_css()` still returns the terminal ring for every letter marker in the terminal bucket.
    - The vocabulary module exposes exactly one spelling per marker — the retirement list is gone.
  </behavior>
  <action>
First make the whole-suite check runnable. `solsys_code.tests.test_views.TestEphemeris` segfaults
the runner in native ASSIST — a recorded project gotcha that every plan in this phase repeats as
prose beside its full-suite command, which means the command as written cannot pass. Turn the
caveat into part of the invocation: in `solsys_code/tests/test_views.py`, add Django's test-tag
decorator to that class (extending the existing `from django.test import ...` line) with the tag
name `ephemeris_segfault`, and a one-line comment saying the native integrator crashes the
process rather than failing a test, so the class is excluded by name rather than silently
skipped. Do not delete, rename or `skip` the class — it still runs for anyone who asks for it
directly. Every whole-suite run below then uses `--exclude-tag=ephemeris_segfault`.

Then run the two title-re-deriving sweeps against the developer database —
`python manage.py reconcile_campaign_runs` and `python manage.py project_observation_calendar` —
then count the `CalendarEvent` rows whose title begins with any of the four legacy bracket-word
status prefixes. If that count is not zero, stop and report which rows remain instead of
proceeding; the retirement list exists precisely so those entries keep their ring.

With the count at zero, delete `RETIRED_TITLE_PREFIXES` from `solsys_code/status_vocabulary.py`
and the branch of `state_for_title()` that consults it, leaving the letter-marker matching
(including its trailing-space rule) as the only path. Update the module docstring so it no
longer promises a retirement list, and record in one comment that the migration is complete and
that a deployment re-titles itself within one unattended tick because both sweeps run every
tick.

Update the ring-equivalence test in `solsys_code/tests/test_status_vocabulary.py`: it currently
asserts a legacy bracket-word title and its letter equivalent get the same ring. Change it to
assert the letter marker gets the terminal ring and that a legacy bracket-word title now
resolves to no display state — so the test documents the retirement rather than silently losing
a case.

Add a "One-time title change" note to the runbook's cancelled/weathered section mirroring the
Phase 34 note already in the file: the first sweep after this phase rewrites the run-level
prefixes on existing entries to their letter form, once, and that is expected rather than a
fault.
  </action>
  <verify>
    <automated>python -c "import os,django;os.environ.setdefault('DJANGO_SETTINGS_MODULE','src.fomo.settings');django.setup();from tom_calendar.models import CalendarEvent;from django.db.models import Q;q=Q();
for p in ('[EXPIRED]','[CANCELLED]','[FAILED]','[WEATHERED]'):
    q |= Q(title__startswith=p)
print(CalendarEvent.objects.filter(q).count())"</automated>
    <fails_when>non-zero exit, or the printed count is anything other than 0 (a legacy-spelled title survives, so the retirement list must not be deleted)</fails_when>
    <automated>python -c "import os,django;os.environ.setdefault('DJANGO_SETTINGS_MODULE','src.fomo.settings');django.setup();import solsys_code.status_vocabulary as sv;print(hasattr(sv,'RETIRED_TITLE_PREFIXES'), sv.state_for_title('[C] NTT EFOSC2'), sv.state_for_title('[CANCELLED] NTT EFOSC2'))"</automated>
    <fails_when>non-zero exit, or the hasattr check prints True, or the letter-marker call prints None, or the bracket-word call prints anything other than None</fails_when>
    <automated>python manage.py test solsys_code.tests.test_status_vocabulary solsys_code.tests.test_calendar_display_extras</automated>
    <fails_when>non-zero exit, or "FAILED" / "Error" appears in the run summary, or the summary reports "Ran 0 tests"</fails_when>
    <automated>python -c "src=open('solsys_code/tests/test_views.py').read();print('ephemeris_segfault' in src, 'class TestEphemeris' in src)"</automated>
    <fails_when>non-zero exit, or either boolean prints False — without the tag on the class the exclusion below is a silent no-op and the whole-suite run segfaults, and the class itself must still be there rather than deleted or skipped</fails_when>
    <automated>python manage.py test solsys_code --exclude-tag=ephemeris_segfault</automated>
    <fails_when>non-zero exit (including a segfault's signal exit, which would mean the tag never landed on the ephemeris class), or "FAILED" / "Error" appears in the run summary, or the summary reports "Ran 0 tests"</fails_when>
    <automated>pre-commit run --all-files</automated>
    <fails_when>non-zero exit, or any hook line reads "Failed"</fails_when>
    <human-check>Open the calendar month view on a month containing an elapsed allocation night, a cancelled run night and an observed record, and confirm three things by eye: the unused night is visibly muted and its label starts with the unused token; the cancelled night shows the cancelled marker and its terminal ring; and clicking the unused entry in the legend isolates the unused nights and clicking it again clears the filter. Then open one attributed entry's pop-up and confirm the tally line reads sensibly beside the campaign name.</human-check>
  </verify>
  <acceptance_criteria>
    - A `CalendarEvent` query for legacy bracket-word status prefixes returns 0 rows on the developer database after both sweeps.
    - `solsys_code.status_vocabulary` no longer exposes `RETIRED_TITLE_PREFIXES`.
    - `state_for_title('[C] ...')` resolves to the cancelled display state and `state_for_title('[CANCELLED] ...')` returns `None`.
    - `solsys_code/tests/test_status_vocabulary.py`'s ring test documents the retirement rather than dropping the case.
    - The runbook carries a one-time-title-change note for the run-level prefixes.
    - `solsys_code/tests/test_views.py` tags the ephemeris test class `ephemeris_segfault` and the class is still present and still runnable when asked for by name.
    - `python manage.py test solsys_code --exclude-tag=ephemeris_segfault` exits 0 and `pre-commit run --all-files` exits 0.
  </acceptance_criteria>
  <done>The developer database holds no legacy-spelled status title, the vocabulary module has exactly one spelling per marker, and the runbook tells an operator that the one-time re-title is expected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer database -> committed notebook output | A pre-executed notebook's committed output is public in the repository, so anything it prints is published. |
| Runbook prose -> operator action | Documentation that misstates a credential location or a failure signal misdirects an operator. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-25 | Information Disclosure | regenerated notebook output | high | mitigate | Every notebook runs against a scratch copy of the developer database with demo-scoped fixtures and tears the copy down; no cell may print a contact name, a contact email, an API key or a real `RequestGroup` identifier. Review the committed diff for those before commit. |
| T-37-26 | Information Disclosure | runbook prose | medium | mitigate | The new unattended-step text names the setting key that holds the portal credential, never a value, matching the existing fresh-host section's discipline. |
| T-37-27 | Denial of Service | deleting the retirement list before a deployment is swept | medium | mitigate | The task's precondition halts on any surviving legacy title, and the runbook note tells an operator the re-title happens within one unattended tick because both sweeps run every tick. |
| T-37-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new package: RESEARCH.md's Package Legitimacy Audit records zero new dependencies for Phase 37, and this plan installs nothing. |
</threat_model>

## Artifacts this phase produces

Artifacts created or changed by this plan:

- `docs/runbooks/telescope_runs_calendar.rst`: the extended marker table, a new public-tally
  section, a new coverage-gap-analysis section, the five-step unattended list, a cheat-sheet
  entry, a troubleshooting entry and a one-time-title-change note
- `docs/notebooks/pre_executed/campaign_lifecycle_demo.ipynb`: three new markdown+code cell
  pairs (public run tally and roll-up, pop-up tally, unused awarded night beside a cancelled one)
- `docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb`,
  `load_telescope_runs_demo.ipynb`, `project_observation_calendar_demo.ipynb`: regenerated
  executed output
- Symbol removed: `status_vocabulary.RETIRED_TITLE_PREFIXES`
- `solsys_code/tests/test_views.py`: the `ephemeris_segfault` tag on the ephemeris test class,
  which turns the project's known-issue caveat into an exclusion the invocation carries

<verification>
- `python manage.py test solsys_code --exclude-tag=ephemeris_segfault` exits 0.
- `pre-commit run --all-files` exits 0, including the Sphinx docs build.
- Every code cell in the four pre-executed notebooks carries a non-null execution count.
- A `CalendarEvent` query for legacy bracket-word status prefixes returns 0 rows.
</verification>

<success_criteria>
- The runbook documents the final vocabulary, the public tallies and — for the first time —
  coverage-gap analysis.
- The paired notebooks demonstrate the shipped behaviour with real executed output.
- Exactly one spelling of every status marker exists in the codebase and in the database.
</success_criteria>

<output>
Create `.planning/phases/37-status-vocabulary-public-tallies-provenance-blind-gaps/37-07-SUMMARY.md` when done
</output>
