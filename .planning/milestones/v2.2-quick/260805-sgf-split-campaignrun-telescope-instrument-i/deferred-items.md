# Deferred Items — quick task 260805-sgf

Out-of-scope issues found during execution, not fixed per the SCOPE BOUNDARY rule (only
auto-fix issues directly caused by the current task's changes).

## Pre-existing repo-wide `ruff check .` / `ruff format --check .` failures

None of these files were touched by this plan's tasks (verified via `git status --short`
and `git diff --stat` at each commit — only `solsys_code/campaign_reconciler.py`,
`solsys_code/tests/test_campaign_reconciler.py`, `solsys_code/tests/test_campaign_approval.py`,
`docs/notebooks/pre_executed/reconcile_campaign_runs_demo.ipynb` and
`docs/runbooks/telescope_runs_calendar.rst` were modified). `ruff check`/`ruff format --check`
scoped to just those five files are clean; the failures below are pre-existing on the repo
as a whole:

- `ruff check .`: `D103` (missing docstring in public function)
  `make_gem_record()` in `docs/notebooks/pre_executed/sync_gemini_observation_calendar_demo.ipynb`
  (cell 6).
- `ruff format --check .`: would reformat
  - `.planning/quick/260619-f7u-phase-5-notebook-gap-update-sync-lco-obs/verify_nb.py`
  - `.planning/quick/260619-f7u-phase-5-notebook-gap-update-sync-lco-obs/verify_project.py`
  - `src/fomo/settings.py`

These are almost certainly explained by `pre-commit`'s ruff hooks only linting/formatting
staged files at commit time, not the whole tree, so a pre-existing gap in an untouched file
is invisible until someone runs the repo-wide command directly.

## Sphinx build pre-existing warnings/errors (unrelated to this plan)

`python -m sphinx -b html docs docs/_build/html -q` exits 0 (build succeeds) but emits
docutils-level `ERROR`/`WARNING` lines unrelated to this plan's files:

- `docs/autoapi/fomo/urls/index.rst` -- autoapi-generated docstring formatting issues
  (`Unexpected indentation`, `Definition list ends without a blank line`, `Block quote ends
  without a blank line`).
- `docs/notebooks/pre_executed/sync_lco_observation_calendar_demo.ipynb:2403` -- `Unknown
  target name: "c_n"`.
- `docs/notebooks/ESO_How_to_download_data.ipynb` -- not included in any toctree.
- `WARNING: Cannot resolve import of unknown module fomo._version in fomo` -- expected in
  this worktree, since `src/fomo/_version.py` is a gitignored, `setuptools_scm`-generated
  file this worktree lacked until manually restored during execution (see below).

None of these reference `reconcile_campaign_runs_demo.ipynb` or
`telescope_runs_calendar.rst`, the two files this plan's Task 3 modified.

## Worktree setup gap: missing `src/fomo/_version.py`

This worktree's `src/fomo/_version.py` (gitignored, generated by `setuptools_scm` at
install time via `write_to = "src/fomo/_version.py"` in `pyproject.toml`) did not exist,
so `python manage.py test ...` failed at Django settings import with
`ModuleNotFoundError: No module named 'src.fomo._version'` before any task work could
start. Copied the file verbatim from the main checkout
(`/home/tlister/git/fomo_devel/src/fomo/_version.py`) to unblock test execution --
gitignored, so untracked, and not part of this plan's `files_modified`. Not a repo bug;
worth noting for future worktree-based executors that hit the same import error.
