---
phase: quick-260613-f7d
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/notebooks/ESO_How_to_download_data.ipynb
  - docs/notebooks/data/ADP.2016-11-17T12:51:01.877.fits
  - docs/notebooks/data/ADP.2020-03-24T10:45:21.866.fits
autonomous: true
requirements: [QUICK-260613-f7d]

must_haves:
  truths:
    - "writeFile() writes downloaded ESO files into a data/ subdirectory, not the cwd"
    - "writeFile() creates the target directory if it does not exist"
    - "writeFile() returns the full data/-prefixed path so the existing print shows the data/ location"
    - "The two stray ADP.*.fits files live under docs/notebooks/data/ and are git-ignored, not untracked"
    - "ruff quality gates remain clean"
  artifacts:
    - path: "docs/notebooks/ESO_How_to_download_data.ipynb"
      provides: "writeFile with dirname='data' parameter, makedirs, joined return path"
      contains: "os.makedirs"
    - path: "docs/notebooks/data/ADP.2016-11-17T12:51:01.877.fits"
      provides: "relocated stray download (anonymous example)"
    - path: "docs/notebooks/data/ADP.2020-03-24T10:45:21.866.fits"
      provides: "relocated stray download (authenticated example)"
  key_links:
    - from: "docs/notebooks/ESO_How_to_download_data.ipynb writeFile()"
      to: "docs/notebooks/data/"
      via: "os.path.join(dirname, filename) + os.makedirs(dirname, exist_ok=True)"
      pattern: "os\\.path\\.join\\(dirname"
    - from: "docs/notebooks/data/**"
      to: ".gitignore"
      via: "existing ignore entry (line 160)"
      pattern: "docs/notebooks/data"
---

<objective>
Update the ESO download example notebook so downloaded ADP.*.fits files land in
`docs/notebooks/data/` (a git-ignored directory) instead of the notebook's own
directory, and relocate the two stray files a user already left behind.

Purpose: Keep `docs/notebooks/` clean of large binary downloads. The
`.gitignore` already excludes `docs/notebooks/data/**`, so writing there means
example runs never pollute the working tree with untracked FITS files.

Output: Modified `writeFile()` helper plus relocated stray files under `data/`.
</objective>

<execution_context>
@/home/tlister/git/fomo_devel/.claude/gsd-core/workflows/execute-plan.md
@/home/tlister/git/fomo_devel/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@docs/notebooks/README.md

# Notebook is nbformat 4.4 JSON. Relevant cells (by index in .cells[]):
#  - cell index 1: imports (os, sys, requests, cgi, json, getpass) -- already imports os
#  - cell index 3: defines getDispositionFilename() and writeFile() -- THE cell to edit
#  - cell index 5: anonymous retrieve, call site `filename = writeFile( response )`
#  - cell index 9: authenticated retrieve, call site `filename = writeFile( response )`
# Out of scope (do NOT touch): eso_programmatic.py, and cells using eso.downloadURL
# with hardcoded /apophis/tlister/... paths (cells 16, 27, 28).
@docs/notebooks/ESO_How_to_download_data.ipynb
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redirect writeFile() into a data/ subdirectory</name>
  <files>docs/notebooks/ESO_How_to_download_data.ipynb</files>
  <action>
    Edit ONLY the `source` array of the notebook cell that defines
    `getDispositionFilename` and `writeFile` (cell index 3 in `.cells[]`). This
    is nbformat 4 JSON -- change the `source` line strings in place; do not
    reformat the rest of the file, do not touch `metadata`, `outputs`, or
    `execution_count` (the `jupyter-nb-clear-output` pre-commit hook strips
    those on commit -- expected, leave them).

    Modify the `writeFile` function so its signature becomes
    `def writeFile( response, dirname='data' ):` and, inside the
    `if response.status_code == 200:` block, after obtaining `filename` from
    `getDispositionFilename( response )`:
      - call `os.makedirs(dirname, exist_ok=True)` to create the target dir
        (`os` is already imported in cell index 1 -- do not add a new import)
      - build `filepath = os.path.join(dirname, filename)`
      - open `filepath` (not `filename`) in the `with open(..., 'wb')` block
      - `return filepath` instead of `return filename`

    Leave `getDispositionFilename` unchanged. Do NOT change the call sites in
    cells 5 and 9: they already do `filename = writeFile( response )` and
    `print("Saved file: %s" % (filename))`, which will now naturally print the
    `data/...` path because writeFile returns the joined path. Default
    `dirname='data'` makes the call sites work with no edit.

    Use the Read tool on the notebook first to copy the exact existing source
    strings, then make the minimal substitutions. Preserve trailing newlines /
    `\n` line endings in the source array elements exactly as nbformat expects.
  </action>
  <verify>
    <automated>python -c "import json; nb=json.load(open('docs/notebooks/ESO_How_to_download_data.ipynb')); assert nb['nbformat']==4; cells=[ (''.join(c['source']) if isinstance(c['source'],list) else c['source']) for c in nb['cells'] if c['cell_type']=='code']; src=next(s for s in cells if 'def writeFile' in s); assert chr(39)+'data'+chr(39) in src and 'dirname' in src, 'dirname param missing'; assert 'os.makedirs(dirname, exist_ok=True)' in src, 'makedirs missing'; assert 'os.path.join(dirname, filename)' in src, 'join missing'; assert 'return filepath' in src, 'return filepath missing'; print('OK')"</automated>
  </verify>
  <done>
    writeFile() takes `dirname='data'`, makedirs the dir, writes to and returns
    `os.path.join(dirname, filename)`. JSON still parses as valid nbformat 4.
    getDispositionFilename and the two call sites are unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Relocate stray FITS files and confirm quality gates</name>
  <files>docs/notebooks/data/ADP.2016-11-17T12:51:01.877.fits, docs/notebooks/data/ADP.2020-03-24T10:45:21.866.fits</files>
  <action>
    The two stray files are UNTRACKED in git (confirmed via `git status`), so use
    a plain `mv`, NOT `git mv`:
      - `mkdir -p docs/notebooks/data`
      - `mv docs/notebooks/ADP.2016-11-17T12:51:01.877.fits docs/notebooks/data/`
      - `mv docs/notebooks/ADP.2020-03-24T10:45:21.866.fits docs/notebooks/data/`

    Then confirm the relocated files are git-ignored (matched by the existing
    `docs/notebooks/data/**` entry on .gitignore line 160), not untracked: run
    `git status --short docs/notebooks/` and verify the ADP.*.fits files no
    longer appear as `??`, and `git check-ignore docs/notebooks/data/ADP.2016-11-17T12:51:01.877.fits`
    prints the path.

    Finally run the CLAUDE.md quality gates to confirm no regression on anything
    touched: `ruff check .` and `ruff format --check .`. The notebook and FITS
    files are not ruff targets; this just confirms the working tree is clean.
  </action>
  <verify>
    <automated>test -f docs/notebooks/data/ADP.2016-11-17T12:51:01.877.fits && test -f docs/notebooks/data/ADP.2020-03-24T10:45:21.866.fits && test ! -f docs/notebooks/ADP.2016-11-17T12:51:01.877.fits && test ! -f docs/notebooks/ADP.2020-03-24T10:45:21.866.fits && git check-ignore docs/notebooks/data/ADP.2016-11-17T12:51:01.877.fits >/dev/null && ruff check . && ruff format --check .</automated>
  </verify>
  <done>
    Both ADP.*.fits files exist under docs/notebooks/data/ and no longer in
    docs/notebooks/. git check-ignore confirms they are ignored. `ruff check .`
    and `ruff format --check .` both pass.
  </done>
</task>

</tasks>

<verification>
- Notebook parses as valid nbformat 4 JSON (Task 1 verify).
- writeFile() signature, makedirs, joined path, and return value all present.
- getDispositionFilename and the two call sites (cells 5, 9) unchanged.
- Both stray FITS files relocated under docs/notebooks/data/ and git-ignored.
- ruff check + ruff format --check clean.
</verification>

<success_criteria>
- writeFile(response, dirname='data') creates dirname, writes to and returns
  os.path.join(dirname, filename).
- docs/notebooks/ contains no loose ADP.*.fits; they live under data/ and are ignored.
- Quality gates clean; no out-of-scope cells or eso_programmatic.py touched.
</success_criteria>

<output>
Create `.planning/quick/260613-f7d-modify-docs-notebooks-eso-how-to-downloa/260613-f7d-SUMMARY.md` when done
</output>
