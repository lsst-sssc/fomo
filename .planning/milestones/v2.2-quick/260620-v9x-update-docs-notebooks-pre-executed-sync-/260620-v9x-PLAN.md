---
phase: quick-260620-v9x
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/notebooks/pre_executed/sync_lco_observation_calendar_demo.ipynb
autonomous: true
requirements: [EXTRACT-01, EXTRACT-02]

must_haves:
  truths:
    - "The notebook demonstrates a SOAR multi-config record (SPECTRUM + ARC + LAMP_FLAT) where the resulting CalendarEvent.instrument is the SPECTRUM science config's instrument_type, never the ARC/LAMP_FLAT calibration configs [EXTRACT-02]"
    - "The notebook demonstrates an LCO MUSCAT per-channel record with no flat c_N_exposure_time, where instrument_type is correctly extracted via the per-channel exposure path [EXTRACT-02]"
    - "The notebook demonstrates a fully-malformed record (no recognized configuration_type, no exposure signal) being skipped and counted under extraction_failed, not skipped, visible in the printed run summary line [D-06]"
    - "Each demonstrated CalendarEvent.instrument value is printed so the reader can see the correct extraction result"
  artifacts:
    - path: "docs/notebooks/pre_executed/sync_lco_observation_calendar_demo.ipynb"
      provides: "Phase 6 instrument-extraction demo cells (SOAR multi-config, MUSCAT per-channel, malformed/extraction_failed) appended to the existing notebook, with updated Summary table and teardown, committed WITH output"
      contains: "EXTRACT-02"
  key_links:
    - from: "docs/notebooks/pre_executed/sync_lco_observation_calendar_demo.ipynb"
      to: "solsys_code/management/commands/sync_lco_observation_calendar.py"
      via: "call_command('sync_lco_observation_calendar', '--proposal', ...) exercising _extract_instrument"
      pattern: "sync_lco_observation_calendar"
---

<objective>
Extend the existing demo notebook `docs/notebooks/pre_executed/sync_lco_observation_calendar_demo.ipynb`
(already covering Phase 4 SELECT-01/SYNC-01..04/TERM-01 and Phase 5 SELECT-02/03/04/05) with a new
section demonstrating Phase 6's instrument-type extraction fix: a SOAR multi-config record where the
science SPECTRUM config is correctly picked over ARC/LAMP_FLAT calibration configs, an LCO MUSCAT
per-channel record with no flat exposure key, and a fully-malformed record that is skipped and counted
under the dedicated `extraction_failed` counter rather than `skipped`.

Purpose: Phase 6 shipped `_extract_instrument` with passing tests but no notebook coverage — the demo
notebook is this project's running narrative of `sync_lco_observation_calendar`'s behavior across
phases, and a reader following it currently has no visibility into the instrument-extraction fix.

Output: One notebook edit (no application code, settings, or test changes), regenerated with output
and committed per the project's existing `docs/notebooks/pre_executed/` convention.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@docs/notebooks/pre_executed/sync_lco_observation_calendar_demo.ipynb
@solsys_code/management/commands/sync_lco_observation_calendar.py
@solsys_code/tests/test_sync_lco_observation_calendar.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the demo notebook with EXTRACT-02/D-06 cells, updated Summary table, and teardown</name>
  <files>docs/notebooks/pre_executed/sync_lco_observation_calendar_demo.ipynb</files>
  <read_first>
    - docs/notebooks/pre_executed/sync_lco_observation_calendar_demo.ipynb (full file — mirror existing conventions: Django-setup boilerplate in cell c3d4e5a3 already ran earlier and is in scope; the io.StringIO()/call_command pattern in cell a7b8c9e7; the get_or_create fixture pattern reusing demo_target/demo_user from cell e5f6a7c5; the Phase-5 section starting at markdown cell p5a1b2c301 — same "### <REQ>: <one-liner>" markdown-then-code cell pairing pattern, fixture lists named demo_phase5_select02_ids etc., PASS/FAIL print style; the Summary table in cell b4c5d6e4 with its Requirement|Description|Demonstrated-by columns; the teardown cell d6e7f8a6 which deletes Phase-5 fixtures by observation_id then the Phase-4 fixture then the shared Target/User)
    - solsys_code/management/commands/sync_lco_observation_calendar.py (`_SCIENCE_CONFIGURATION_TYPES = {'EXPOSE', 'REPEAT_EXPOSE', 'SPECTRUM', 'REPEAT_SPECTRUM', 'STANDARD'}`; `_extract_instrument` scans c_1..c_5 for the first config whose `configuration_type` is in that whitelist, falls back to the first config with a populated exposure signal — flat `c_N_exposure_time` or any of the 4 MUSCAT `c_N_ic_1_exposure_time_{g,r,i,z}` keys — falls back further to the flat `instrument_type` key; `InstrumentExtractionError` is raised when none of these yield a value and is caught in `handle()`, incrementing `counters[record.facility]['extraction_failed']` (a counter distinct from `skipped`, present in the per-facility counters dict alongside created/updated/unchanged/skipped, and joined into the printed summary line as `extraction_failed: N`); the failing record's `observation_id` is logged to stderr)
    - solsys_code/tests/test_sync_lco_observation_calendar.py (lines 481-591: `test_extract_02_soar_multi_config_picks_spectrum_not_calibration` builds a SOAR record with `c_1_configuration_type='SPECTRUM'`/`c_1_instrument_type='SOAR_GHTS_REDCAM'`, `c_2_configuration_type='ARC'`/`c_2_instrument_type='SOAR_GHTS_REDCAM_ARC'`, `c_3_configuration_type='LAMP_FLAT'`/`c_3_instrument_type='SOAR_GHTS_REDCAM_LAMPFLAT'`, plus a decoy flat `instrument_type='NOT-THE-SOURCE'`, site='sor'; `test_extract_02_muscat_per_channel_exposure_extracts_instrument` builds an LCO record with `c_1_configuration_type='EXPOSE'`/`c_1_instrument_type='2M0-SCICAM-MUSCAT'` and the 4 per-channel keys `c_1_ic_1_exposure_time_{g,r,i,z}=30.0`, no flat `c_1_exposure_time`, decoy flat `instrument_type='NOT-THE-SOURCE'`; `test_d06_no_extractable_config_logged_and_counted_separately` builds a record with `c_1_configuration_type='ARC'` (calibration-only, never whitelisted), no exposure-time keys, and an explicit `instrument_type': None` override so no flat fallback exists either — asserts `extraction_failed: 1` appears in stdout and the observation_id appears in stderr)
    - .planning/PROJECT.md (Demo Notebooks section: notebooks are DB-dependent, pre-commit clears output cells everywhere except `docs/notebooks/pre_executed/` which is committed WITH output, manual execution required to regenerate)
    - CLAUDE.md (NonSiderealTargetFactory only — never SiderealTargetFactory; FOMO is solar-system-only; this notebook reuses the existing `demo_target`/`demo_user` fixtures, so no new Target factory call is needed)
  </read_first>
  <action>
This is a real JSON `.ipynb` file on disk. Edit it as JSON (preserve `nbformat`/`nbformat_minor`/
`metadata`/`cells` structure and each cell's `cell_type`/`source`/`metadata`/`outputs`/`execution_count`
keys). New code cells use `cell_type` "code", `outputs` empty list, `execution_count` null; new markdown
cells use `cell_type` "markdown". Give each new cell a fresh unique `id`. Do NOT create a new notebook
file — extend the existing one. Do NOT modify `sync_lco_observation_calendar.py` or its test file — this
plan is documentation/demo only.

Insert the new content AFTER the existing Phase-5 SELECT-05 markdown cell (id `p5e5f6a708`) and BEFORE
the existing "## Summary" markdown cell (`b4c5d6e4`). The existing Summary table and teardown cell
(`d6e7f8a6`) are then UPDATED in place (steps e and f). Structure:

(a) Markdown section header cell: "## Phase 6: correct instrument-type extraction (EXTRACT-01/EXTRACT-02)".
Briefly explain that Phase 6 replaced the flat `parameters['instrument_type']` read with a `c_1..c_5`
multi-config scanner (`_extract_instrument`) that distinguishes a record's science configuration from
SOAR's calibration configs and detects LCO MUSCAT's per-channel exposure shape, and introduces a
dedicated `extraction_failed` counter for records where no instrument can be determined at all. Note
that the cells below create their own fixture records (distinct observation_ids from the Phase-4/5
demos above) and are removed by the teardown cell at the end.

(b) EXTRACT-02 SOAR multi-config — one markdown cell + one code cell. Create a SOAR fixture
ObservationRecord via `ObservationRecord.objects.get_or_create` with observation_id 'demo-606001',
facility='SOAR', proposal 'PHASE6-SOAR', site='sor', and a `parameters` dict reusing the existing
'start'/'end' window strings, a decoy flat `instrument_type='NOT-THE-SOURCE'`, and the exact `c_1`/`c_2`/
`c_3` `configuration_type`/`instrument_type` key-value pairs from
`test_extract_02_soar_multi_config_picks_spectrum_not_calibration` (SPECTRUM/SOAR_GHTS_REDCAM,
ARC/SOAR_GHTS_REDCAM_ARC, LAMP_FLAT/SOAR_GHTS_REDCAM_LAMPFLAT). Reuse `demo_target`/`demo_user`. Run
`call_command('sync_lco_observation_calendar', '--proposal', 'PHASE6-SOAR', stdout=io.StringIO(),
stderr=io.StringIO())`. Look up the resulting CalendarEvent by
`LCOFacility().get_observation_url('demo-606001')` and print `event.instrument`, then print a PASS/FAIL
line confirming it equals 'SOAR_GHTS_REDCAM' and is NOT equal to 'SOAR_GHTS_REDCAM_ARC' or
'SOAR_GHTS_REDCAM_LAMPFLAT' (mirror the PASS/FAIL print style used in the Phase-5 SELECT-02 cell).

(c) EXTRACT-02 MUSCAT per-channel — one markdown cell + one code cell. Create one LCO fixture
ObservationRecord with observation_id 'demo-606002', facility='LCO', proposal 'PHASE6-MUSCAT',
site='coj', decoy flat `instrument_type='NOT-THE-SOURCE'`, and the exact `c_1_configuration_type='EXPOSE'`
/`c_1_instrument_type='2M0-SCICAM-MUSCAT'` plus the four `c_1_ic_1_exposure_time_{g,r,i,z}=30.0` keys
from `test_extract_02_muscat_per_channel_exposure_extracts_instrument` (no flat `c_1_exposure_time` key
— the per-channel path is what's being demonstrated). Run
`call_command('sync_lco_observation_calendar', '--proposal', 'PHASE6-MUSCAT', stdout=io.StringIO(),
stderr=io.StringIO())`. Look up the CalendarEvent and print `event.instrument`, then a PASS/FAIL line
confirming it equals '2M0-SCICAM-MUSCAT'.

(d) D-06 malformed record / extraction_failed counter — one markdown cell + one code cell. Create two
fixtures: one malformed LCO record with observation_id 'demo-606003', proposal 'PHASE6-MALFORMED',
site='coj', and the exact malformed shape from `test_d06_no_extractable_config_logged_and_counted_separately`
(`c_1_configuration_type='ARC'`, `c_1_instrument_type='SOMETHING'`, and an explicit override
`instrument_type=None` so no flat fallback exists either); and one baseline-good LCO record with
observation_id 'demo-606004', same proposal 'PHASE6-MALFORMED', site='ogg', default fixture shape (no
extra_params), proving coexistence — the malformed record's failure doesn't block the good one. Capture
both stdout and stderr into separate `io.StringIO()` buffers and run
`call_command('sync_lco_observation_calendar', '--proposal', 'PHASE6-MALFORMED', stdout=stdout_buf,
stderr=stderr_buf)`. Print `stdout_buf.getvalue()` and call out in the surrounding markdown that the
summary line shows `extraction_failed: 1`, not folded into `skipped`. Print `stderr_buf.getvalue()` and
note it contains 'demo-606003' (the failing observation_id, logged for diagnosis). Then print a PASS/FAIL
line confirming a CalendarEvent exists for 'demo-606004' (the good record) and confirming NO
CalendarEvent exists for 'demo-606003' (the malformed one was skipped, not synced).

(e) Update the existing "## Summary" markdown cell (`b4c5d6e4`): change the intro sentence to also
mention Phase 06's EXTRACT-01/EXTRACT-02 instrument-extraction correctness, and ADD two rows to the
existing requirements table, in the same column format (Requirement | Description | Demonstrated by),
after the existing SELECT-05 row:
  - EXTRACT-01 | the command no longer reads a flat instrument_type blindly when c_N_* multi-config keys are present | the Phase 6 cells below (SOAR/MUSCAT fixtures both carry a decoy flat instrument_type that is correctly ignored)
  - EXTRACT-02 | the science configuration (SPECTRUM/EXPOSE/etc.) is picked over SOAR calibration configs (ARC/LAMP_FLAT) or via MUSCAT's per-channel exposure signal when no config has a recognized configuration_type; a record with neither signal is skipped and counted under a dedicated extraction_failed counter | the SOAR multi-config cell, the MUSCAT per-channel cell, and the malformed-record cell (extraction_failed: 1 in the printed summary)
Keep all existing rows (SELECT-01/SYNC-01..04/TERM-01/SELECT-02..05) intact.

(f) Update the existing teardown cell (`d6e7f8a6`): add a Phase-6 fixture id list (e.g.
`demo_phase6_all_ids = ['demo-606001', 'demo-606002', 'demo-606003', 'demo-606004']`) and, for each id,
delete its CalendarEvent (by `LCOFacility().get_observation_url(observation_id)`) and ObservationRecord,
following the exact same loop pattern already used for `demo_phase5_all_ids`. Insert this cleanup before
the existing Phase-4 cleanup lines (or immediately after the Phase-5 loop — your discretion, as long as
it runs before the shared Target/User deletion at the end). Update the final `print('Teardown
complete: ...')` line to mention Phase-6 fixtures are also removed.
  </action>
  <verify>
    <automated>cd /home/tlister/git/fomo && python3 .planning/quick/260620-v9x-update-docs-notebooks-pre-executed-sync-/verify_nb.py</automated>
  </verify>
  <acceptance_criteria>
    - The notebook parses as valid JSON; every cell keeps a valid cell_type ('code' or 'markdown').
    - A SOAR multi-config cell exists demonstrating SPECTRUM is picked over ARC/LAMP_FLAT, with the resulting CalendarEvent.instrument printed and confirmed equal to 'SOAR_GHTS_REDCAM'.
    - A MUSCAT per-channel cell exists demonstrating instrument extraction with no flat c_N_exposure_time key, with CalendarEvent.instrument printed and confirmed equal to '2M0-SCICAM-MUSCAT'.
    - A malformed-record cell exists demonstrating the record is skipped (no CalendarEvent created) and counted under extraction_failed (not skipped) in the printed run summary, with a baseline-good sibling record in the same call_command invocation still producing a CalendarEvent.
    - The Summary table has two new rows (EXTRACT-01/EXTRACT-02) in the existing column format; all prior rows remain intact.
    - The teardown cell removes every Phase-6 fixture's CalendarEvent and ObservationRecord, in addition to all prior cleanup.
    - No facility='SOAR' record incorrectly reuses an LCO site code; site='sor' is used for the SOAR fixture (per the existing fix from quick task 260619-jpr).
    - No Target/User factory calls beyond the existing demo_target/demo_user reuse; if any new Target fixture were added it would use NonSiderealTargetFactory only.
    - sync_lco_observation_calendar.py and its test file are untouched.
  </acceptance_criteria>
  <done>The existing notebook is extended with valid-JSON Phase-6 EXTRACT-01/EXTRACT-02 demo cells (SOAR multi-config, MUSCAT per-channel, malformed/extraction_failed), an updated Summary table, and a teardown that cleans up all new fixtures.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate notebook output and commit per the pre_executed convention</name>
  <files>docs/notebooks/pre_executed/sync_lco_observation_calendar_demo.ipynb</files>
  <read_first>
    - .planning/quick/260619-ml8-fix-pre-commit-jupyter-nb-clear-output-e/ (precedent for regenerating and committing this exact notebook WITH output)
    - .planning/PROJECT.md (Demo Notebooks section: docs/notebooks/pre_executed/ is excluded from the pre-commit output-clearing hook and is committed WITH output, by design, so readers see real results without re-running)
  </read_first>
  <action>
Execute the notebook end-to-end against the local dev DB (e.g. via `jupyter nbconvert --to notebook
--execute --inplace docs/notebooks/pre_executed/sync_lco_observation_calendar_demo.ipynb`, run from the
repo root so Django setup/`DJANGO_SETTINGS_MODULE=src.fomo.settings` resolves as it does for
`manage.py`) so every code cell's `outputs`/`execution_count` reflect a real run, including the new
Phase-6 cells added in Task 1. Confirm the run is clean: no cell raises an exception, the teardown cell's
final print statement appears, and the database is left in the same state it started in (the
notebook's own teardown cell handles this — do not manually clean the DB afterward).

After execution, re-run `ruff check .` and `ruff format --check .` to confirm this notebook edit hasn't
disturbed any tracked `.py` file (it should report no changes, since this plan touches only the `.ipynb`).
Stage and commit only `docs/notebooks/pre_executed/sync_lco_observation_calendar_demo.ipynb` with output
present, matching the committed-WITH-output convention already used by commit `d4bf23f` for this same
file.
  </action>
  <verify>
    <automated>cd /home/tlister/git/fomo && python3 -c "import json; nb=json.load(open('docs/notebooks/pre_executed/sync_lco_observation_calendar_demo.ipynb')); code_cells=[c for c in nb['cells'] if c['cell_type']=='code']; assert all(c.get('outputs') for c in code_cells), 'every code cell must have non-empty outputs after execution'; print(f'{len(code_cells)} code cells all have output')"</automated>
  </verify>
  <acceptance_criteria>
    - The notebook executes end-to-end with no cell raising an exception.
    - Every code cell has a non-empty `outputs` list and a non-null `execution_count` after execution.
    - The new Phase-6 cells' printed output shows the SOAR fixture's instrument as 'SOAR_GHTS_REDCAM', the MUSCAT fixture's instrument as '2M0-SCICAM-MUSCAT', and the malformed-record run summary containing 'extraction_failed: 1'.
    - The teardown cell's final print statement appears in its cell output, confirming all fixtures (Phase 4, 5, and 6) were removed and the dev DB is left clean.
    - `ruff check .` and `ruff format --check .` report no changes (this task touches only the `.ipynb`, no tracked `.py` file).
    - Only `docs/notebooks/pre_executed/sync_lco_observation_calendar_demo.ipynb` is staged and committed, with output present (per the committed-WITH-output convention, commit `d4bf23f` precedent).
  </acceptance_criteria>
  <done>The notebook is executed end-to-end with real output committed, demonstrating the Phase-6 EXTRACT-01/EXTRACT-02 cells alongside all prior Phase 4/5 cells, with a clean teardown and no lint regressions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none meaningful) | This is a documentation/notebook artifact only. The notebook invokes the already-shipped, already-threat-modeled Phase-6 `_extract_instrument` helper via `call_command`; no new code path, input sink, or credential handling is introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-q-01 | Information Disclosure | Demo notebook cells printing command output | accept | The notebook prints only the command's summary line, stderr's logged observation_id, and CalendarEvent fields (instrument/url) — the same category of output the Phase-4/5 demo cells already print. No PII, no secrets, no credentials in any fixture or printed value. |
| T-q-SC | Tampering | npm/pip/cargo installs | accept (N/A) | No package installs — this plan edits an existing tracked file only. No legitimacy checkpoint required. |

No HIGH-severity threats. No blocking human checkpoint needed.
</threat_model>

<verification>
- Notebook parses as valid JSON with EXTRACT-02 SOAR/MUSCAT/malformed cells present (Task 1 automated check, `verify_nb.py`).
- Notebook executes end-to-end with all code cells producing non-empty output, committed WITH output per the `docs/notebooks/pre_executed/` convention (Task 2 automated check).
- `ruff check .` / `ruff format --check .` stay clean (no `.py` files touched by this plan).
- Optional manual verification: open the regenerated notebook and confirm the SOAR cell's printed `event.instrument` reads 'SOAR_GHTS_REDCAM' (never the ARC/LAMP_FLAT calibration values), the MUSCAT cell's printed `event.instrument` reads '2M0-SCICAM-MUSCAT', and the malformed-record cell's printed stdout contains 'extraction_failed: 1' while the baseline-good sibling record still has a CalendarEvent.
</verification>

<success_criteria>
- The existing `sync_lco_observation_calendar_demo.ipynb` additionally demonstrates a SOAR multi-config record (science SPECTRUM config correctly extracted over ARC/LAMP_FLAT calibration configs), an LCO MUSCAT per-channel record (extracted via the per-channel exposure path, no flat `c_N_exposure_time`), and a fully-malformed record (skipped, counted under `extraction_failed`, visible in the printed run summary, with a baseline-good sibling record still synced in the same invocation).
- Each new fixture's resulting `CalendarEvent.instrument` is printed in the notebook output.
- The Summary table lists EXTRACT-01/EXTRACT-02 alongside all existing rows; the teardown removes all new fixtures; the notebook is committed WITH executed output per the `docs/notebooks/pre_executed/` convention.
- No application code (`sync_lco_observation_calendar.py`), settings, or test files are modified.
</success_criteria>

<output>
Create `.planning/quick/260620-v9x-update-docs-notebooks-pre-executed-sync-/260620-v9x-SUMMARY.md` when done.
</output>