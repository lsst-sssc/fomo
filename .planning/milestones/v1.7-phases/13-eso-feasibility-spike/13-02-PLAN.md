---
phase: 13-eso-feasibility-spike
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - .planning/phases/13-eso-feasibility-spike/13-DECISION.md
  - docs/design/eso_feasibility_spike.rst
autonomous: true
requirements: [ESO-04, ESO-05]

must_haves:
  truths:
    - "13-DECISION.md explicitly recommends exactly one of Bridge / Bypass / Not Yet Feasible, with rationale tied directly to the ESO-01 through ESO-03 findings (ESO-04)."
    - "If the recommendation is feasible (Bridge or Bypass), the doc sketches what 'synced' could reasonably mean for a future ESO sync command (banner-only window vs. status-aware), scoped as input to a future milestone rather than implemented here (ESO-05)."
    - "If the recommendation is Bridge and the evidence supports it, the ESO-05 sketch includes a rough effort-sizing estimate for the Bridge work — which tom_eso methods would need real implementations and whether it reads as a small patch, moderate fork, or larger undertaking (D-11)."
    - "A durable summary docs/design/eso_feasibility_spike.rst exists alongside telescope_runs_calendar.rst, stating the same verdict up front, so future milestones can reference it without digging into .planning/."
  artifacts:
    - ".planning/phases/13-eso-feasibility-spike/13-DECISION.md (Recommendation + Future-sync sketch sections completed)"
    - "docs/design/eso_feasibility_spike.rst (durable summary)"
  key_links:
    - "ESO-01/02/03 Findings (from Plan 01) -> the ESO-04 recommendation: the verdict must be justified by the captured evidence, not asserted independently."
    - "13-DECISION.md verdict -> the .rst Key finding one-liner: both docs state the same recommendation (no divergence)."
---

<objective>
Synthesize the evidence captured in Plan 01 into a decision. Complete
`13-DECISION.md` with the ESO-04 recommendation (exactly one of Bridge / Bypass /
Not Yet Feasible, rationale tied to the ESO-01..03 findings) and the ESO-05
future-sync sketch (including D-11 effort-sizing if the recommendation is
Bridge). Then write the durable summary `docs/design/eso_feasibility_spike.rst`
alongside `telescope_runs_calendar.rst` so future milestones can reference the
verdict directly.

This plan writes documentation only — no application code, no management command
(the phase is investigation-only). It depends on Plan 01's captured Findings.

Purpose: Turn real API evidence into an actionable, evidence-grounded
recommendation that seeds a future milestone's requirements.
Output: completed `13-DECISION.md` and new `docs/design/eso_feasibility_spike.rst`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-eso-feasibility-spike/13-CONTEXT.md
@.planning/phases/13-eso-feasibility-spike/13-PATTERNS.md
@.planning/phases/13-eso-feasibility-spike/13-DECISION.md
@.planning/research/SUMMARY.md
@.planning/research/ARCHITECTURE.md
@.planning/research/STACK.md
@.planning/research/FEATURES.md
@docs/design/telescope_runs_calendar.rst
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete 13-DECISION.md — ESO-04 recommendation and ESO-05 future-sync sketch</name>
  <files>.planning/phases/13-eso-feasibility-spike/13-DECISION.md</files>
  <read_first>
    - .planning/phases/13-eso-feasibility-spike/13-DECISION.md (the ESO-01/02/03 Findings written by Plan 01 — the evidence this recommendation must rest on)
    - .planning/research/SUMMARY.md (evidence-then-recommendation framing, confidence-assessment tone to mirror)
    - .planning/research/ARCHITECTURE.md ("Data flow changes" — the Bridge vs. Bypass option definitions; "New vs. Reused Components" — insert_or_create_calendar_event as the reusable landing point)
    - .planning/research/STACK.md (the tom_eso NotImplementedError method inventory — get_observation_status/get_observation_url/data_products — for the D-11 effort-sizing)
    - .planning/research/FEATURES.md (table-stakes vs. deferred features for a future ESO sync command — input to the ESO-05 sketch)
    - .planning/phases/13-eso-feasibility-spike/13-CONTEXT.md (D-10 both docs, D-11 effort-sizing requirement)
  </read_first>
  <action>
    Replace the `## Recommendation (ESO-04)` and `## Future-sync sketch (ESO-05)`
    placeholder sections in `13-DECISION.md` with completed content. Mirror
    research/SUMMARY.md's evidence-then-recommendation ordering.

    `## Recommendation (ESO-04)` — per D-04 (ESO-04 requirement): state exactly
    ONE verdict as a bold one-liner up front — Bridge (patch/work around tom_eso
    so it populates real ObservationRecord rows, then sync as usual), Bypass
    (sync straight from p2api to CalendarEvent, skipping ObservationRecord for
    ESO), or Not Yet Feasible (with the specific blocker). Then give the
    rationale, explicitly referencing the ESO-01, ESO-02, and ESO-03 findings by
    name — the verdict must be derived from them, not asserted independently. If
    credentials were blocked in Plan 01, the honest verdict is Not Yet Feasible
    with the credential blocker named as the gating cause.

    `## Future-sync sketch (ESO-05)` — only if the verdict is feasible (Bridge or
    Bypass), sketch what "synced" could reasonably mean for a FUTURE ESO sync
    command, scoped as input to a future milestone's requirements, NOT
    implemented here (ESO-05 explicitly defers implementation — do not describe
    this as a v1/simplified/placeholder version of shippable work; it is a
    future-scope sketch). Cover: banner-only window vs. status-aware sync (given
    ESO Service Mode has no advance per-OB schedule); the reusable landing point
    (`solsys_code/calendar_utils.py:insert_or_create_calendar_event()`,
    unchanged) and the synthetic-key precedent (`ESO:{p2_environment}/{obId}`,
    following Gemini's `GEM:{prog}/{observation_id}`); and the ESO P2 status
    vocabulary (the 12-code obStatus set) if status-aware.

    If (and only if) the verdict is Bridge, INCLUDE the D-11 effort-sizing
    estimate: name which tom_eso methods would need real implementations
    (`get_observation_status()`, `get_observation_url()`, `data_products()`),
    roughly how much new logic that implies, and whether the change reads as a
    small patch, a moderate fork, or a larger undertaking. This is
    order-of-magnitude scoping only — no code is written against it.

    Keep the Findings sections (ESO-01/02/03) unchanged. Do not introduce any
    unredacted credential-adjacent content.
  </action>
  <verify>
    <automated>D=.planning/phases/13-eso-feasibility-spike/13-DECISION.md; grep -q '## Recommendation (ESO-04)' "$D" && grep -q '## Future-sync sketch (ESO-05)' "$D" && ! grep -q 'completed in Plan 02' "$D" && grep -Eq 'Bridge|Bypass|Not Yet Feasible' "$D"</automated>
    <human-check>The named verdict is justified by explicit reference to the ESO-01/02/03 findings; if the verdict is Bridge, a small-patch/moderate-fork/larger-undertaking effort estimate naming the tom_eso methods is present (D-11).</human-check>
  </verify>
  <acceptance_criteria>
    - `13-DECISION.md` contains a completed `## Recommendation (ESO-04)` section
      naming exactly one of Bridge / Bypass / Not Yet Feasible as a bold verdict,
      with rationale referencing the ESO-01/02/03 findings.
    - `## Future-sync sketch (ESO-05)` is completed (if feasible) or explicitly
      marked not-applicable (if Not Yet Feasible), scoped as future input.
    - If the verdict is Bridge, the D-11 effort-sizing estimate is present.
    - No placeholder markers remain; no unredacted credential-adjacent content.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Write the durable summary docs/design/eso_feasibility_spike.rst</name>
  <files>docs/design/eso_feasibility_spike.rst</files>
  <read_first>
    - docs/design/telescope_runs_calendar.rst (the exact skeleton to follow — title underlined with `=`, Background section, bold "Key finding" one-liner, list-table for the data/method matrix)
    - .planning/phases/13-eso-feasibility-spike/13-DECISION.md (source of the verdict, findings, and sketch to summarize)
    - .planning/phases/13-eso-feasibility-spike/13-PATTERNS.md (the RST skeleton guidance under "docs/design/eso_feasibility_spike.rst")
  </read_first>
  <action>
    Create `docs/design/eso_feasibility_spike.rst` as a durable summary of the
    spike, following `telescope_runs_calendar.rst`'s structure exactly (do not
    invent a new heading convention — the other docs in this directory use `=`
    for the title and `-` for sections):

    - Title underlined with `=` matching the title length.
    - Opening paragraph: "This document records..." — what spike this is, when it
      happened (the investigation date), and what it validated/answered.
    - `Background` section: ESO Phase 2 scheduling model (VLT/Paranal + NTT/La
      Silla), and why FOMO cares — parallel to how telescope_runs_calendar.rst
      frames classical vs. queue scheduling. Note the core constraint that
      reshaped the milestone: installed `tom_eso==0.2.4` cannot create
      ObservationRecord rows or report status through the standard TOM facility
      API.
    - `Key finding` section: the **bold** one-line verdict up front (Bridge /
      Bypass / Not Yet Feasible), matching the same emphasis convention as
      telescope_runs_calendar.rst's bold key-finding line — identical to
      13-DECISION.md's ESO-04 verdict (no divergence between the two docs).
    - A `list-table` section (mirror telescope_runs_calendar.rst's "The Data
      Model" table) for the tom_eso method-availability / OB-status matrix:
      which of `get_observation_status` / `get_observation_url` / `data_products`
      work today vs. need patching, and (if status-aware sync is sketched) the
      ESO P2 obStatus code vocabulary.
    - A short `Future scope` section pointing to 13-DECISION.md for full detail
      and framing ESO-05 as input to a future milestone (not implemented here).

    Do NOT paste raw redacted API-response blobs into the .rst — keep it a
    durable prose+table summary; the verbatim evidence lives in 13-DECISION.md.
    Contains no credential-adjacent content.
  </action>
  <verify>
    <automated>test -f docs/design/eso_feasibility_spike.rst && grep -Eq 'Bridge|Bypass|Not Yet Feasible' docs/design/eso_feasibility_spike.rst && grep -q 'list-table' docs/design/eso_feasibility_spike.rst && head -2 docs/design/eso_feasibility_spike.rst | grep -q '===='</automated>
    <human-check>The .rst renders in the same style as telescope_runs_calendar.rst (title `=`, sections `-`, bold Key finding one-liner) and its verdict matches 13-DECISION.md's ESO-04 verdict.</human-check>
  </verify>
  <acceptance_criteria>
    - `docs/design/eso_feasibility_spike.rst` exists, follows the
      telescope_runs_calendar.rst skeleton (title `=`, `-` sections), and opens
      with a "This document records..." paragraph.
    - It states the same Bridge/Bypass/Not-Yet-Feasible verdict as
      13-DECISION.md as a bold Key-finding one-liner.
    - It includes a list-table method-availability / status matrix.
    - It contains no credential-adjacent content and no raw redacted API blobs.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 13-DECISION.md findings -> docs/design/eso_feasibility_spike.rst | Content copied/summarized from the decision doc into a second committed file; any residual credential-adjacent content would be duplicated, so the summary must stay prose+table only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-05 | Information Disclosure | Copying evidence from 13-DECISION.md into the durable .rst | high | mitigate | The .rst is a prose+table summary only; no raw API-response blobs are pasted in (kept in 13-DECISION.md where they are already D-04 redacted); Task 2 action forbids credential-adjacent content and a human-check confirms. |
| T-13-SC | Tampering | Package installs (npm/pip/cargo) | low | accept | No package installs this phase — this plan writes documentation only. |
</threat_model>

<verification>
- 13-DECISION.md has a completed ESO-04 recommendation (one verdict, rationale
  tied to ESO-01/02/03) and a completed-or-N/A ESO-05 sketch (with D-11
  effort-sizing if Bridge).
- docs/design/eso_feasibility_spike.rst exists, follows the house skeleton, and
  states the same verdict.
- Both docs are free of credential-adjacent content.
</verification>

<success_criteria>
- A single, defensible Bridge/Bypass/Not-Yet-Feasible recommendation exists,
  grounded in the Plan 01 evidence.
- Future milestones can read the durable .rst summary without digging into
  .planning/.
</success_criteria>

<artifacts_produced>
This plan creates/completes:
- `.planning/phases/13-eso-feasibility-spike/13-DECISION.md` — Recommendation
  (ESO-04) and Future-sync sketch (ESO-05) sections completed. COMMITTED.
- `docs/design/eso_feasibility_spike.rst` — durable summary. COMMITTED.
</artifacts_produced>

<output>
Create `.planning/phases/13-eso-feasibility-spike/13-02-SUMMARY.md` when done.
</output>