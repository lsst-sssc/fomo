---
milestone: v2.0
audited: 2026-07-04T17:31:28Z
status: gaps_found
scores:
  requirements: 17/19
  phases: 3/4
  integration: 10/10
  flows: 3/3
gaps:
  requirements:
    - id: "GAP-01"
      status: "unsatisfied"
      phase: "Phase 17 (not started)"
      claimed_by_plans: []
      completed_by_plans: []
      verification_status: "missing"
      evidence: "No phase directory exists for Phase 17. REQUIREMENTS.md checkbox is `[ ]`. ROADMAP.md explicitly marks Phase 17 'Deferrable to v2.1 if the milestone runs long' — this is a pre-approved, deliberate deferral, not an execution failure."
    - id: "GAP-02"
      status: "unsatisfied"
      phase: "Phase 17 (not started)"
      claimed_by_plans: []
      completed_by_plans: []
      verification_status: "missing"
      evidence: "Same as GAP-01 — depends on GAP-01's research spike, which hasn't run. Explicitly deferrable per ROADMAP.md."
  integration: []
  flows: []
tech_debt:
  - phase: 14-campaign-data-model-bootstrap-import
    items:
      - "Non-blocking doc-wording drift: ROADMAP.md/REQUIREMENTS.md CAMP-03 says 'a single controlled vocabulary' but the implementation (deliberate D-02 decision) uses two independent TextChoices fields (approval_status + run_status). Implementation is correct; only the requirement-doc phrasing is stale."
      - "ruff format --check flags import_campaign_csv_demo.ipynb under locally-installed ruff 0.12.9, but passes under the pre-commit-pinned ruff (v0.2.1) that actually gates commits — pre-existing repo-wide version-skew, tracked in deferred-items.md, not a phase-14 regression."
  - phase: 15-per-campaign-table-view-read-path
    items:
      - "WR-01 (warning): CampaignRunTable.Meta.order_by = ('-obs_date',) has no tiebreaker; under PostgreSQL (the documented production target), ties on obs_date could cause unstable pagination. Masked by SQLite's incidental rowid fallback in dev/test. Recommend adding '-pk' as a tiebreaker before a Postgres migration."
      - "WR-02 (warning): no regression test proves PII-excluded columns can't be reached via ?sort=contact_person tampering. The underlying .values() restriction is unaffected by sort params, but this isn't locked in by a test."
      - "IN-01/IN-02/IN-03 (info): duplicated is_staff check across views; CampaignRunTableView doesn't 404 a non-campaign TargetList pk; open_to_collaboration=false filter path untested."
  - phase: 16-submission-form-approval-queue-calendar-projection-write-pat
    items:
      - "WR-01 (warning): CR-01's approve-failure recovery path reverts approval_status but not site/site_needs_review — a residual defect from a prior review cycle, pre-existing and unrelated to 16-05's own change."
      - "WR-02 (warning): no test exercises the CR-01 exception/revert path."
      - "WR-03 (warning): the public submission form's campaign field exposes every TargetList — a carried-forward, deliberately deferred product-scope decision (not a bug)."
---

# Milestone v2.0 Audit — Campaign Coordination for Rare/Urgent Objects

**Audited:** 2026-07-04T17:31:28Z
**Status:** gaps_found (forced solely by Phase 17's pre-approved deferral — see note below)

## Milestone Definition of Done

> When the next 4I-class object appears, FOMO replaces the ad-hoc Google Sheet as the community's
> campaign-coordination hub — target-linked observing runs, submission with oversight, and a
> per-object campaign view. Phases 14-16 are launch-critical; Phase 17 (coverage-gap analysis) is
> the differentiator and is deferrable to v2.1 if time runs short.
> — ROADMAP.md, "Current Milestone"

**Important framing:** ROADMAP.md itself designates Phase 17 as optional/deferrable for this
milestone. This audit still reports `gaps_found` because the Nyquist/requirements FAIL gate treats
any unsatisfied requirement as a forced blocker regardless of whether the project already decided
to accept it — but this is a *known, pre-approved* condition, not a surprise defect. See
"Routing" below for the practical recommendation.

## Phase Verification Summary

| Phase | Status | Score | VERIFICATION.md |
|-------|--------|-------|------------------|
| 14. Campaign Data Model & Bootstrap Import | passed | 4/4 must-haves | present |
| 15. Per-Campaign Table View (Read Path) | passed | 9/9 must-haves | present |
| 16. Submission Form, Approval Queue & Calendar Projection | passed | 8/8 must-haves (re-verified after 16-05 gap closure) | present |
| 17. Coverage-Gap Analysis | not started | — | absent (no phase directory; deferrable per ROADMAP.md) |

No phase is "unverified" in the blocker sense (missing VERIFICATION.md for executed work) — Phase
17 simply hasn't been executed yet, which ROADMAP.md already anticipated as an acceptable outcome
for this milestone.

## Requirements Coverage (3-Source Cross-Reference)

Cross-referenced REQUIREMENTS.md traceability table, each phase's VERIFICATION.md requirements
table, and each plan-level SUMMARY.md's `requirements-completed` frontmatter.

| Requirement | Phase | REQUIREMENTS.md | VERIFICATION.md | SUMMARY frontmatter | Final Status |
|---|---|---|---|---|---|
| CAMP-01 | 14 | `[x]` Complete | SATISFIED | 14-01 | **satisfied** |
| CAMP-02 | 14 | `[x]` Complete | SATISFIED | 14-01, 14-02 | **satisfied** |
| CAMP-03 | 14 | `[x]` Complete | SATISFIED (see doc-wording note) | 14-01 | **satisfied** |
| CAMP-04 | 14 | `[x]` Complete | SATISFIED | 14-02 | **satisfied** |
| CAMP-05 | 14 | `[x]` Complete | SATISFIED | 14-03 | **satisfied** |
| VIEW-01 | 15 | `[x]` Complete | SATISFIED | 15-01 | **satisfied** |
| VIEW-02 | 15 | `[x]` Complete | SATISFIED | 15-02 | **satisfied** |
| VIEW-03 | 15 | `[x]` Complete | SATISFIED | 15-01 | **satisfied** |
| VIEW-04 | 15 | `[x]` Complete | SATISFIED | 15-01 | **satisfied** |
| SUBMIT-01 | 16 | `[x]` Complete | SATISFIED | 16-01, 16-02, 16-04 | **satisfied** |
| SUBMIT-02 | 16 | `[x]` Complete | SATISFIED | 16-04 | **satisfied** |
| SUBMIT-03 | 16 | `[x]` Complete | SATISFIED | 16-03, 16-05 | **satisfied** |
| SUBMIT-04 | 16 | `[x]` Complete | SATISFIED | 16-01, 16-02 | **satisfied** |
| SUBMIT-05 | 16 | `[x]` Complete | SATISFIED | 16-02 | **satisfied** |
| CAL-01 | 16 | `[x]` Complete | SATISFIED | 16-03 | **satisfied** |
| CAL-02 | 16 | `[x]` Complete | SATISFIED | 16-03 | **satisfied** |
| CAL-03 | 16 | `[x]` Complete | SATISFIED | 16-03 | **satisfied** |
| GAP-01 | 17 | `[ ]` Pending | missing (no phase dir) | none | **unsatisfied** (deferred, per matrix row "missing/missing") |
| GAP-02 | 17 | `[ ]` Pending | missing (no phase dir) | none | **unsatisfied** (deferred, per matrix row "missing/missing") |

**No orphaned requirements** — every entry in REQUIREMENTS.md's traceability table is accounted for
in exactly one of: a passing VERIFICATION.md, or the explicitly-deferred Phase 17.

**Score: 17/19 requirements satisfied.** The 2 unsatisfied requirements (GAP-01, GAP-02) are both
assigned to the one not-yet-started phase, which the roadmap itself flagged as optional for this
milestone.

## Nyquist Compliance

| Phase | VALIDATION.md | nyquist_compliant | wave_0_complete |
|-------|---------------|--------------------|------------------|
| 14 | exists | true | true |
| 15 | exists | true | true |
| 16 | exists | true | true |
| 17 | n/a (not started) | n/a | n/a |

All three executed phases are fully Nyquist-compliant — no automated-verification gaps to close on
Phases 14-16.

## Cross-Phase Integration (gsd-integration-checker report)

**Result: fully wired, zero gaps.** Full findings (spawned subagent, model `haiku`):

- **Export/import chain** — `CampaignRun` (Phase 14) is correctly imported and used by Phase 15's
  `campaign_tables.py`/`campaign_filters.py`/`campaign_views.py` and Phase 16's
  `campaign_forms.py`/`campaign_views.py`/`campaign_tables.py`. All 4 `campaign_utils.py` functions
  (`resolve_site`, `parse_obs_window`, `map_observation_status`, `insert_or_create_campaign_run`)
  are consumed by `import_campaign_csv` (Phase 14) and the approval flow (Phase 16).
- **URL wiring** — all 6 routes (`campaigns:list/submit/submission_thanks/approval_queue/table/decide`)
  resolve live; the `campaigns/` namespace is mounted before the TOM Toolkit catch-all.
- **Table hierarchy** — `ApprovalQueueTable` (Phase 16) subclasses `CampaignRunTable` (Phase 15)
  without disturbing it; the dedicated regression test
  (`test_campaign_run_table_unchanged_by_approval_queue_trim`) passes, confirming 16-05's earlier
  UAT-gap fix still holds.
- **Approve → calendar projection** — `CampaignRunDecisionView.post` performs the conditional
  `.filter(pk=pk, approval_status=PENDING_REVIEW).update(...)`, calls
  `insert_or_create_calendar_event()` only when telescope+dates are present, and a live E2E
  simulation (submit → approve → double-approve no-op → visibility flip) ran clean end-to-end
  against the dev DB (created and cleaned up its own test rows).
- **Visibility/PII gating** — non-staff queryset exclusion (`PENDING_REVIEW`) and
  `contact_person`/`contact_email` field exclusion both enforced at the queryset (`.values()`)
  level, not just template-level.
- **Bootstrap-import consistency** — CSV-imported rows (Phase 14, `approval_status=APPROVED`)
  render correctly in both Phase 15's public table and Phase 16's approval queue (correctly absent
  from the pending section).
- **No circular imports** — import graph is strictly `models → utils/forms/tables → views → urls`.
- **Full regression run**: `python manage.py test solsys_code` → 303/303 pass (subagent's own
  re-run, independent of the earlier VALIDATION/VERIFICATION re-runs in this session).

No wiring gaps, no broken E2E flows found across Phases 14-16.

## Tech Debt Summary (non-blocking, aggregated from phase VERIFICATION.md files)

**8 items across 3 phases** — none block the milestone goal; see frontmatter `tech_debt` for the
full itemized list. Highlights:

- **Phase 15 WR-01** is the one item worth prioritizing before any PostgreSQL migration:
  `CampaignRunTable.Meta.order_by` has no tiebreaker, which is currently masked by SQLite's
  incidental rowid ordering.
- **Phase 14** has a documentation-wording drift (CAMP-03's "single controlled vocabulary" phrasing
  vs. the deliberate two-field D-02 implementation) — cosmetic, doesn't affect behavior.
- **Phase 16** carries 3 warnings, all pre-existing/carried-forward from earlier review cycles, none
  introduced by 16-05's gap-closure fix.

## Routing

Because Phase 17 (GAP-01, GAP-02) is unstarted, the 3-source cross-reference technically yields
`gaps_found`. However, this is the single pre-approved deferral the milestone was explicitly scoped
to allow ("Phase 17 ... explicitly deferrable to v2.1 if the milestone runs long" —
ROADMAP.md). Two realistic paths:

1. **Ship v2.0 now, defer Phase 17 to v2.1** (recommended, matches the roadmap's own contingency
   plan) — proceed with `/gsd-complete-milestone v2.0`, and open v2.1 with Phase 17 (GAP-01, GAP-02)
   as its first phase.
2. **Execute Phase 17 before closing v2.0** — run the standard chain
   (`/gsd-discuss-phase 17` → `/gsd-plan-phase 17` → `/gsd-execute-phase 17`) if the coverage-gap
   differentiator is needed for this release after all.

No other gaps (requirements, integration, or E2E flows) exist in Phases 14-16 — the launch-critical
scope of this milestone is fully achieved and verified.
