---
phase: 03-classical-calendar-ingest
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - docs/notebooks/pre_executed/load_telescope_runs_demo.ipynb
autonomous: true
requirements: [INGEST-01, INGEST-02, INGEST-03]

must_haves:
  truths:
    - "A demo notebook under docs/notebooks/pre_executed/ shows load_telescope_runs ingesting a sample schedule file and the resulting CalendarEvents"
    - "The notebook runs cleanly end-to-end with the Django setup boilerplate (sys.path + DJANGO_ALLOW_ASYNC_UNSAFE) from PROJECT.md and contains executed output cells"
  artifacts:
    - path: "docs/notebooks/pre_executed/load_telescope_runs_demo.ipynb"
      provides: "Executed demo of the load_telescope_runs command (Phase 03 Definition of Done)"
      contains: "load_telescope_runs"
  key_links:
    - from: "docs/notebooks/pre_executed/load_telescope_runs_demo.ipynb"
      to: "solsys_code/management/commands/load_telescope_runs.py"
      via: "call_command('load_telescope_runs', ...) or ./manage.py invocation"
      pattern: "load_telescope_runs"
---

<objective>
Produce the Phase 03 demo notebook showing the `load_telescope_runs` command in
action: seed the 4 Observatory records, write a small sample schedule file,
invoke the command, and display the resulting `CalendarEvent` rows (title,
start/end times, description). This satisfies the project's per-phase
Definition of Done (PROJECT.md "Demo Notebooks").

Purpose: Closes the Phase 03 Definition of Done with a runnable, glanceable
demonstration of the ingest feature.
Output: `docs/notebooks/pre_executed/load_telescope_runs_demo.ipynb` (executed,
with output cells committed).
</objective>

## Artifacts this phase produces

New symbols/files created by this plan (exclude from drift checks):

- `docs/notebooks/pre_executed/load_telescope_runs_demo.ipynb` (new file)

Consumed (not created) by this plan — already exist after Plan 03-01:
the `load_telescope_runs` management command; `tom_calendar.models.CalendarEvent`;
`solsys_code.solsys_code_observatory.models.Observatory`;
`solsys_code.telescope_runs` helpers.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-classical-calendar-ingest/03-CONTEXT.md
@solsys_code/management/commands/load_telescope_runs.py
@docs/notebooks/pre_executed/telescope_runs_demo.ipynb
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create and execute the load_telescope_runs demo notebook</name>
  <files>docs/notebooks/pre_executed/load_telescope_runs_demo.ipynb</files>
  <read_first>
    - docs/notebooks/pre_executed/telescope_runs_demo.ipynb (the Phase 01 demo
      notebook — mirror its structure, the Django setup boilerplate cell, and
      its markdown/explanation style)
    - .planning/PROJECT.md ("Demo Notebooks" + "Django setup boilerplate for
      notebooks" — the exact setup cell with sys.path repo_root via
      `Path.cwd().resolve().parents[N]`, `DJANGO_SETTINGS_MODULE='src.fomo.settings'`,
      `DJANGO_ALLOW_ASYNC_UNSAFE='true'`, then `django.setup()`; this notebook
      lives at depth `docs/notebooks/pre_executed/` so use `parents[2]`)
    - solsys_code/management/commands/load_telescope_runs.py (the command being
      demonstrated; invoke it via `django.core.management.call_command`)
    - solsys_code/tests/test_telescope_runs.py (the 4-site Observatory seeding
      pattern to reproduce in a notebook cell so get_site() resolves)
  </read_first>
  <action>
    Create `docs/notebooks/pre_executed/load_telescope_runs_demo.ipynb`. First
    code cell: the Django setup boilerplate from PROJECT.md verbatim, with
    `repo_root = str(Path.cwd().resolve().parents[2])` (notebook is two levels
    under repo root) and `django.setup()`. Next cell: seed the 4 Observatory
    records (Magellan-Clay/268, Magellan-Baade/269, NTT/809, FTS/E10) using the
    same field values as `test_telescope_runs.py`'s `setUpTestData` via
    `Observatory.objects.update_or_create(obscode=..., defaults=...)` so the demo
    is idempotent and works against any dev DB. Next cell: write a small sample
    schedule file (a few lines including `NTT EFOSC2 allocation 9-13 July` and
    at least one other valid line such as an `FTS ...` run) to a temp path using
    `tempfile`. Next cell: invoke the command via
    `call_command('load_telescope_runs', <path>)` and show its stdout summary.
    Next cell: query and display the created `CalendarEvent` rows — iterate the
    queryset filtered/ordered by `start_time` and print `title`, `start_time`,
    `end_time`, and `description` for each, so the dark-window/status/source-line
    description content (D-06) is visible. Add a final markdown cell noting this
    demonstrates INGEST-01/02/03 and idempotency (optionally re-run the command
    and show the summary reports `updated: 0`). Interleave brief markdown cells
    explaining each step (mirror the Phase 01 demo's narrative style). Execute
    the notebook top-to-bottom so output cells are populated, then save the
    executed notebook (pre_executed notebooks keep their output — they are NOT
    cleared by the notebook-output pre-commit hook for files under
    `docs/notebooks/pre_executed/`; confirm against telescope_runs_demo.ipynb
    which retains output). Note: importing solsys_code.telescope_runs transitively
    is fine, but importing solsys_code.ephem_utils/views triggers a ~1.6 GB SPICE
    download — only import what the demo needs (Observatory, CalendarEvent,
    call_command); do NOT import ephem_utils/views.
  </action>
  <verify>
    <automated>cd /home/tlister/git/fomo && jupyter nbconvert --to notebook --execute --stdout docs/notebooks/pre_executed/load_telescope_runs_demo.ipynb > /dev/null && echo "NOTEBOOK EXECUTES CLEAN"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/notebooks/pre_executed/load_telescope_runs_demo.ipynb` exists.
    - The notebook executes top-to-bottom without error
      (`jupyter nbconvert --to notebook --execute` succeeds, exit 0).
    - The notebook contains a cell invoking `load_telescope_runs` (grep the
      `.ipynb` JSON for the string `load_telescope_runs`) and a cell displaying
      created `CalendarEvent` rows.
    - The notebook does NOT import `solsys_code.ephem_utils` or
      `solsys_code.views` (grep the `.ipynb` JSON: neither string present),
      avoiding the SPICE-kernel download side effect.
    - The saved notebook has executed output cells (non-empty `outputs`).
  </acceptance_criteria>
  <done>An executed demo notebook under docs/notebooks/pre_executed/ shows the ingest command running and the resulting CalendarEvents, satisfying the Phase 03 Definition of Done.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| notebook author -> dev DB | The notebook seeds Observatory rows and creates CalendarEvents in the local dev SQLite DB; runs at developer trust on a local machine. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-N1 | Tampering | notebook writes to dev DB | accept | Developer-run documentation artifact against a local dev SQLite DB; uses `update_or_create` for idempotent seeding. No production or network exposure. Same trust level as running `./manage.py shell`. |
| T-03-N2 | Info Disclosure | committed notebook output cells could leak local data | accept | Output cells contain only the demo's own seeded Observatory/CalendarEvent data (no secrets, no PII); equivalent to the existing Phase 01 pre_executed demo notebook. |

No high-severity threats. No package installs in this plan (no Package
Legitimacy Gate applicable).
</threat_model>

<verification>
- `jupyter nbconvert --to notebook --execute docs/notebooks/pre_executed/load_telescope_runs_demo.ipynb` exits 0 (notebook runs clean end-to-end).
- The notebook visibly invokes `load_telescope_runs` and displays resulting CalendarEvents.
- No import of `solsys_code.ephem_utils`/`solsys_code.views` (no SPICE download).
</verification>

<success_criteria>
- A demo notebook under `docs/notebooks/pre_executed/` demonstrates the `load_telescope_runs` command and its CalendarEvent output, executed with committed output cells, satisfying the Phase 03 Definition of Done (PROJECT.md).
</success_criteria>

<output>
Create `.planning/phases/03-classical-calendar-ingest/03-02-SUMMARY.md` when done.
</output>
