---
phase: 16-submission-form-approval-queue-calendar-projection-write-pat
plan: 04
type: execute
wave: 4
depends_on: ["16-02", "16-03"]
files_modified:
  - solsys_code/campaign_views.py
  - src/templates/campaigns/campaign_list.html
  - src/templates/campaigns/campaignrun_table.html
  - solsys_code/tests/test_campaign_views.py
autonomous: true
requirements: [SUBMIT-01, SUBMIT-02]
must_haves:
  truths:
    - "Non-staff visitors see approved AND rejected rows on the per-campaign table; only pending_review is hidden (D-09/SUBMIT-02)."
    - "Staff continue to see every row regardless of approval_status (unchanged)."
    - "A 'Submit a Run' entry button appears on the campaigns list page and each per-campaign table page (SUBMIT-01 discoverability)."
    - "A staff-only 'N pending review' banner on the campaigns list links to the approval queue and appears only when is_staff and pending rows exist (D-01 reachability)."
  artifacts:
    - "solsys_code/campaign_views.py::CampaignRunTableView.get_queryset (modified) + CampaignListView.get_context_data (pending_count)"
    - src/templates/campaigns/campaign_list.html
    - src/templates/campaigns/campaignrun_table.html
    - solsys_code/tests/test_campaign_views.py
  key_links:
    - "Non-staff branch of get_queryset gains .exclude(approval_status=PENDING_REVIEW) before .values(...)."
    - "campaign_list banner links to campaigns:approval_queue; entry buttons link to campaigns:submit."
---

<objective>
Complete the write path's public surface: hide `pending_review` rows from non-staff on the Phase 15
per-campaign table (D-09/SUBMIT-02), and add the discoverability entry points fixed by the
UI-SPEC — a "Submit a Run" button on the campaigns list and per-campaign table pages, plus a
staff-only "N pending review" banner linking to the approval queue.

Purpose: Delivers SUBMIT-02 (new submissions invisible on public views until approved) and closes
the SUBMIT-01/D-01 discoverability loop so the form and approval queue built in Plans 02-03 are
reachable. This is the final wave — it depends on the `campaigns:submit` (Plan 02) and
`campaigns:approval_queue` (Plan 03) URLs existing.
Output: modified `get_queryset`/`CampaignListView`, two modified templates, and extended
`test_campaign_views.py`.
</objective>

<artifacts_produced>
- `solsys_code/campaign_views.py` — modified `CampaignRunTableView.get_queryset` (non-staff
  `.exclude(approval_status=PENDING_REVIEW)`); modified `CampaignListView` to add a `pending_count`
  context value for the staff banner
- `src/templates/campaigns/campaign_list.html` — modified: header "Submit a Run" button + staff-only
  pending banner
- `src/templates/campaigns/campaignrun_table.html` — modified: header "Submit a Run" button
- `solsys_code/tests/test_campaign_views.py` — extended with the D-09 visibility tests
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-submission-form-approval-queue-calendar-projection-write-pat/16-RESEARCH.md
@.planning/phases/16-submission-form-approval-queue-calendar-projection-write-pat/16-PATTERNS.md
@.planning/phases/16-submission-form-approval-queue-calendar-projection-write-pat/16-UI-SPEC.md
@solsys_code/campaign_views.py
@src/templates/campaigns/campaign_list.html
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: D-09 non-staff visibility filter + pending_count context</name>
  <files>solsys_code/campaign_views.py, solsys_code/tests/test_campaign_views.py</files>
  <read_first>
    - solsys_code/campaign_views.py lines 26-96 (ALLOWED_FIELDS_FOR_NON_STAFF, CampaignRunTableView.get_queryset existing D-13 branch, CampaignListView)
    - solsys_code/tests/test_campaign_views.py (existing Phase 15 fixtures — 30 runs cycling approval statuses incl. PENDING_REVIEW; staff/anonymous client patterns to extend)
    - .planning/phases/16-submission-form-approval-queue-calendar-projection-write-pat/16-RESEARCH.md (Pattern 6 — the exact .exclude() placement; D-09)
    - .planning/phases/16-submission-form-approval-queue-calendar-projection-write-pat/16-CONTEXT.md (D-09, D-10 — list page unchanged)
  </read_first>
  <behavior>
    - Anonymous client GET the per-campaign table -> response context/rows exclude every
      pending_review run; approved AND rejected rows still appear (D-09).
    - Staff client GET the same table -> every row appears including pending_review (unchanged).
    - `CampaignListView` context contains `pending_count` = number of pending_review runs across all
      campaigns (used by the staff banner); the campaigns list itself still lists the same campaigns
      as Phase 15 (D-10 — list membership unchanged).
  </behavior>
  <action>
    In `CampaignRunTableView.get_queryset`, modify ONLY the non-staff branch per RESEARCH.md Pattern
    6: before the existing `return qs.values(*ALLOWED_FIELDS_FOR_NON_STAFF)`, insert `qs =
    qs.exclude(approval_status=CampaignRun.ApprovalStatus.PENDING_REVIEW)`. Leave the staff branch
    (`if self.request.user.is_staff: return qs.select_related('site')`) untouched and do NOT modify
    `ALLOWED_FIELDS_FOR_NON_STAFF` (D-09 changes row filtering, not column selection).

    In `CampaignListView`, add/extend `get_context_data` to include `pending_count =
    CampaignRun.objects.filter(approval_status=CampaignRun.ApprovalStatus.PENDING_REVIEW).count()`
    (the template gates its display on `request.user.is_staff`, so the count is harmless to compute
    for everyone; only staff ever see the banner). Do NOT change `CampaignListView.queryset` — the
    campaigns-list membership is unchanged this phase (D-10).

    Extend `solsys_code/tests/test_campaign_views.py` with a D-09 test class (or methods) asserting:
    anonymous client cannot see any pending_review run's identifying data on the table while approved
    and rejected rows remain visible; staff sees all rows. If the existing fixture's 30-row cycle
    already includes PENDING_REVIEW rows, reuse it; otherwise add explicit pending/approved/rejected
    rows. Assert against the rendered response or the view's queryset. Never use
    `SiderealTargetFactory` (CLAUDE.md).
  </action>
  <verify>
    <automated>./manage.py test solsys_code.tests.test_campaign_views</automated>
  </verify>
  <done>
    Non-staff table queryset excludes pending_review (approved+rejected still shown); staff unchanged;
    `pending_count` in the campaigns-list context; existing Phase 15 tests still green.
  </done>
  <acceptance_criteria>
    - `grep -n "exclude(approval_status=CampaignRun.ApprovalStatus.PENDING_REVIEW)" solsys_code/campaign_views.py` matches inside the non-staff branch of `get_queryset`.
    - `grep -n "pending_count" solsys_code/campaign_views.py` matches in `CampaignListView`.
    - `ALLOWED_FIELDS_FOR_NON_STAFF` is unchanged (same 17 entries as before).
    - `./manage.py test solsys_code.tests.test_campaign_views` reports OK, including the new D-09
      assertions (anonymous excludes pending, shows approved+rejected; staff sees all).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Entry-point buttons + staff pending banner (templates)</name>
  <files>src/templates/campaigns/campaign_list.html, src/templates/campaigns/campaignrun_table.html</files>
  <read_first>
    - src/templates/campaigns/campaign_list.html (existing heading + list-group / empty-state structure to wrap in a d-flex header row)
    - src/templates/campaigns/campaignrun_table.html (existing heading to add the button beside)
    - .planning/phases/16-submission-form-approval-queue-calendar-projection-write-pat/16-UI-SPEC.md (Entry points section — exact button placement/classes and the staff-only banner markup; Copywriting Contract; Color roles)
  </read_first>
  <action>
    In `src/templates/campaigns/campaign_list.html`: wrap the existing `<h4>Campaigns</h4>` heading
    in a `<div class="d-flex justify-content-between align-items-center mb-4">` row with a
    `<a class="btn btn-primary" href="{% url 'campaigns:submit' %}">Submit a Run</a>` to its right
    (do not disturb the list-group / empty-state body below). Immediately below that header row, add
    the staff-only pending banner EXACTLY per UI-SPEC Entry points: `{% if request.user.is_staff and
    pending_count %}` -> an `alert alert-warning d-flex justify-content-between align-items-center
    mb-4` with `<i class="fa fa-exclamation-circle mr-2"></i>{{ pending_count }} submission{{
    pending_count|pluralize }} pending review` and a `<a href="{% url 'campaigns:approval_queue' %}"
    class="btn btn-sm btn-warning">Review queue</a>`, closed with `{% endif %}`.

    In `src/templates/campaigns/campaignrun_table.html`: add the same `<a class="btn btn-primary"
    href="{% url 'campaigns:submit' %}">Submit a Run</a>` button beside that page's `<h4>` heading
    using the same `d-flex justify-content-between align-items-center` header-row idiom, so a visitor
    on one campaign's runs can submit directly. Do not add the pending banner here (banner lives on
    the list page only, per UI-SPEC).
  </action>
  <verify>
    <automated>python -c "import django,os; os.environ.setdefault('DJANGO_SETTINGS_MODULE','src.fomo.settings'); django.setup(); from django.template.loader import get_template; t=get_template('campaigns/campaign_list.html'); t2=get_template('campaigns/campaignrun_table.html'); print('templates load OK')"</automated>
  </verify>
  <done>
    Campaigns list shows a "Submit a Run" button and (for staff, when pending rows exist) a "N pending
    review" banner linking to the approval queue; per-campaign table page shows a "Submit a Run"
    button. Both templates load without syntax errors.
  </done>
  <acceptance_criteria>
    - `grep -n "campaigns:submit" src/templates/campaigns/campaign_list.html` and
      `grep -n "campaigns:submit" src/templates/campaigns/campaignrun_table.html` both match.
    - `grep -n "campaigns:approval_queue" src/templates/campaigns/campaign_list.html` matches inside a
      `{% if request.user.is_staff and pending_count %}` block.
    - The template-load verify one-liner prints `templates load OK`.
    - `./manage.py test solsys_code.tests.test_campaign_views` still passes (no template regressions).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anonymous request → per-campaign table | Unapproved (pending) submissions must not leak to the public |
| non-staff render → staff pending banner | Banner + approval-queue link must be staff-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-07 | Information Disclosure | `CampaignRunTableView.get_queryset` non-staff branch | high | mitigate | Queryset-level `.exclude(approval_status=PENDING_REVIEW)` (not a template conditional) so pending rows never enter the non-staff SELECT — mirrors Phase 15 D-13 discipline; proven by the anonymous-client D-09 test |
| T-16-03 | Information Disclosure / Elevation of Privilege | staff pending banner + approval-queue link | medium | mitigate | Banner gated on `{% if request.user.is_staff %}`; the linked `ApprovalQueueView` is itself hard-gated by `StaffRequiredMixin` (Plan 03) — the template gate is convenience, not the security boundary |
| T-16-SC | Tampering (supply chain) | package installs | low | accept | No new packages this phase |
</threat_model>

<verification>
- `./manage.py test solsys_code.tests.test_campaign_views` passes (Phase 15 + new D-09 tests).
- Template-load one-liner prints OK.
- `ruff check` / `ruff format --check` clean on `campaign_views.py` and the test module.
- Phase gate: full `./manage.py test solsys_code` green (expect a slow first run — cold-cache SPICE
  download per CLAUDE.md), plus `ruff check .` and `ruff format --check .` clean.
</verification>

<success_criteria>
- SUBMIT-02/D-09: non-staff see approved+rejected but not pending_review; staff see all.
- SUBMIT-01 discoverability: "Submit a Run" reachable from the campaigns list and per-campaign table.
- D-01 reachability: staff pending banner links to the approval queue when pending rows exist.
- D-10: campaigns-list membership unchanged.
</success_criteria>

<output>
Create `.planning/phases/16-submission-form-approval-queue-calendar-projection-write-pat/16-04-SUMMARY.md` when done.
</output>
