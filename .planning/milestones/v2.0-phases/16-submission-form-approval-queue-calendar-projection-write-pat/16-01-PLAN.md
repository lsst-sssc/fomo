---
phase: 16-submission-form-approval-queue-calendar-projection-write-pat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - solsys_code/mixins.py
  - solsys_code/campaign_forms.py
  - src/fomo/settings.py
  - solsys_code/tests/test_campaign_forms.py
autonomous: true
requirements: [SUBMIT-01, SUBMIT-04]
must_haves:
  truths:
    - "A plain forms.Form (not ModelForm) exposes the D-05 intake field subset with only campaign required at the field level."
    - "contact_person and contact_email are required at the form-validation level (D-06)."
    - "A hidden honeypot field named alt_contact_info exists, renders as HiddenInput, and its clean method never raises."
    - "A StaffRequiredMixin redirects non-staff/anonymous users to LOGIN_URL via dispatch."
    - "An email backend is configured so send_mail() never hangs on localhost:25 in dev/tests."
  artifacts:
    - solsys_code/mixins.py
    - solsys_code/campaign_forms.py
    - solsys_code/tests/test_campaign_forms.py
  key_links:
    - "CampaignRunSubmissionForm.clean_alt_contact_info returns the value without raising (honeypot trip is handled in the view, not the form)."
    - "settings.EMAIL_BACKEND set to the console backend so dev send_mail() is visible and non-blocking."
---

<objective>
Build the three leaf dependencies the rest of Phase 16 rests on: the `StaffRequiredMixin`
(first hard `is_staff` view gate in this app), the public `CampaignRunSubmissionForm` (plain
`forms.Form` + honeypot), and an email backend setting so `send_mail()` works in dev/tests.

Purpose: These three artifacts have no dependency on the campaign view/URL layer and can be
built and unit-tested in isolation, unblocking the submission view (Plan 02) and the approval
queue (Plan 03).
Output: `solsys_code/mixins.py`, `solsys_code/campaign_forms.py`, an `EMAIL_BACKEND` in
`src/fomo/settings.py`, and `solsys_code/tests/test_campaign_forms.py`.
</objective>

<artifacts_produced>
Symbols and files this phase creates (source-grounding reference for downstream review):
- `solsys_code/mixins.py` (new file) — class `StaffRequiredMixin` (dispatch-level `is_staff` gate)
- `solsys_code/campaign_forms.py` (new file) — class `CampaignRunSubmissionForm(forms.Form)`;
  fields: `campaign`, `telescope_instrument`, `site_raw`, `obs_date`, `ut_start`, `ut_end`,
  `filters_bandpass`, `observation_details`, `open_to_collaboration`, `contact_person`,
  `contact_email`, `comments`, and hidden honeypot field `alt_contact_info`; method
  `clean_alt_contact_info`
- `src/fomo/settings.py` — new `EMAIL_BACKEND` setting (console backend for dev)
- `solsys_code/tests/test_campaign_forms.py` (new file)
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-submission-form-approval-queue-calendar-projection-write-pat/16-RESEARCH.md
@.planning/phases/16-submission-form-approval-queue-calendar-projection-write-pat/16-PATTERNS.md
@.planning/phases/16-submission-form-approval-queue-calendar-projection-write-pat/16-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: StaffRequiredMixin + EMAIL_BACKEND setting</name>
  <files>solsys_code/mixins.py, src/fomo/settings.py</files>
  <read_first>
    - solsys_code/apps.py (module/import conventions in this app)
    - .planning/phases/16-submission-form-approval-queue-calendar-projection-write-pat/16-PATTERNS.md (section "solsys_code/mixins.py" — the SuperuserRequiredMixin analog to copy verbatim, renamed)
    - src/fomo/settings.py (find the end of the file / where to add EMAIL_BACKEND; confirm no existing EMAIL_BACKEND before adding)
  </read_first>
  <action>
    Create `solsys_code/mixins.py` with class `StaffRequiredMixin` copied verbatim from the
    installed `tom_common/mixins.py::SuperuserRequiredMixin` shape (16-PATTERNS.md), renaming to
    `StaffRequiredMixin` and swapping the predicate `u.is_superuser` -> `u.is_staff`. Use
    `@method_decorator(user_passes_test(lambda u: u.is_staff))` on `dispatch(self, *args, **kwargs)`
    which calls `super().dispatch(...)`. Add a one-line class docstring (D101 is enforced outside
    tests). `user_passes_test` with no explicit `login_url` redirects failing users to
    `settings.LOGIN_URL` (`/accounts/login/`, already configured) — do not pass a custom login_url.

    In `src/fomo/settings.py`, add `EMAIL_BACKEND = 'django.core.mail.backends.console.EmailBackend'`
    (dev default — visible in runserver stdout; Django's test runner auto-substitutes the locmem
    backend during `./manage.py test` regardless, so `mail.outbox` assertions work without
    override_settings). Add a short comment noting that production must set a real
    `EMAIL_BACKEND`/`EMAIL_HOST`/`EMAIL_HOST_USER`/`EMAIL_HOST_PASSWORD`/`DEFAULT_FROM_EMAIL` in
    `local_settings.py` (imported at the end of settings.py). Place the setting before the
    `local_settings.py` import block so a deployment override wins.
  </action>
  <verify>
    <automated>python -c "import django,os; os.environ.setdefault('DJANGO_SETTINGS_MODULE','src.fomo.settings'); django.setup(); from solsys_code.mixins import StaffRequiredMixin; from django.conf import settings; assert 'console' in settings.EMAIL_BACKEND; print('OK')"</automated>
  </verify>
  <done>
    `StaffRequiredMixin` importable from `solsys_code.mixins`; `settings.EMAIL_BACKEND` is the
    console backend; no import errors.
  </done>
  <acceptance_criteria>
    - `solsys_code/mixins.py` defines `class StaffRequiredMixin` whose `dispatch` is wrapped with
      `user_passes_test(lambda u: u.is_staff)` (grep: `is_staff` present, `is_superuser` absent).
    - `src/fomo/settings.py` contains `EMAIL_BACKEND` set to
      `django.core.mail.backends.console.EmailBackend`.
    - The verify one-liner prints `OK` (imports succeed, backend assertion holds).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CampaignRunSubmissionForm (plain forms.Form + honeypot) + form tests</name>
  <files>solsys_code/campaign_forms.py, solsys_code/tests/test_campaign_forms.py</files>
  <read_first>
    - solsys_code/forms.py (EphemerisForm — the crispy FormHelper + Layout(Fieldset..., FormActions(Submit...)) house style to copy)
    - solsys_code/models.py lines 31-128 (CampaignRun field inventory; telescope_instrument has NO blank=True — this is why a ModelForm is forbidden)
    - .planning/phases/16-submission-form-approval-queue-calendar-projection-write-pat/16-RESEARCH.md (Pattern 1 — the concrete CampaignRunSubmissionForm field list, honeypot clean, and layout; Pitfall 3 and Pitfall 5)
    - .planning/phases/16-submission-form-approval-queue-calendar-projection-write-pat/16-UI-SPEC.md (Layout Contract "Page 3", Honeypot Field Contract, Copywriting Contract for labels)
  </read_first>
  <behavior>
    - A form bound to only `{campaign, contact_person, contact_email}` (valid campaign pk) is valid;
      all other fields are optional.
    - A form missing `campaign` is invalid (campaign is the only required model-backed field).
    - A form missing `contact_person` or `contact_email` is invalid (D-06 form-level requirement),
      even though those fields are `blank=True` on the model.
    - `alt_contact_info` is `required=False`, uses `forms.HiddenInput`, and a form with
      `alt_contact_info` filled is still `is_valid() == True` (clean does not raise — the honeypot
      trip is a view-layer decision, Plan 02).
    - `site_raw`'s form label is "Observing site" (not the model verbose_name "Original site code text").
  </behavior>
  <action>
    Create `solsys_code/campaign_forms.py` defining `CampaignRunSubmissionForm(forms.Form)` — a
    plain `forms.Form`, NEVER a `ModelForm` (a ModelForm would derive `required` from the model and
    wrongly force `telescope_instrument` required, contradicting D-05; see Pitfall 3). Declare
    explicit fields per D-05 / RESEARCH.md Pattern 1: `campaign =
    forms.ModelChoiceField(queryset=TargetList.objects.all(), required=True)`;
    `telescope_instrument`, `site_raw` (label 'Observing site'), `obs_date` (DateField),
    `ut_start`/`ut_end` (DateTimeField), `filters_bandpass`, `observation_details` (Textarea),
    `open_to_collaboration` (BooleanField) — all `required=False`; `contact_person`
    (CharField, `required=True`) and `contact_email` (EmailField, `required=True`) per D-06;
    `comments` (Textarea, `required=False`). Add the honeypot: `alt_contact_info =
    forms.CharField(required=False, widget=forms.HiddenInput())` and a `clean_alt_contact_info`
    method that returns `self.cleaned_data.get('alt_contact_info', '')` and DELIBERATELY does not
    raise `ValidationError` (SUBMIT-04 — a tripped bot must get no error signal). Build
    `self.helper = FormHelper()` in `__init__` with a `Layout` following EphemerisForm's style:
    `campaign` alone first, `Fieldset('Run details', ...)`, `Fieldset('Contact', 'contact_person',
    'contact_email', 'comments')`, then the honeypot wrapped as `Div('alt_contact_info',
    css_class='d-none')` (belt-and-suspenders; the HiddenInput widget does the real hiding), and
    `FormActions(Submit('submit', 'Submit run for review'))`. Import `TargetList` from
    `tom_targets.models`.

    Create `solsys_code/tests/test_campaign_forms.py` with a `TestCase` covering the five behaviors
    above. Use `TargetList.objects.create(name='3I/ATLAS')` for the campaign fixture (never
    `SiderealTargetFactory` — CLAUDE.md). Instantiate the form with data dicts and assert
    `is_valid()` / `errors` keys. Assert `isinstance(form.fields['alt_contact_info'].widget,
    forms.HiddenInput)` and `form.fields['telescope_instrument'].required is False`.
  </action>
  <verify>
    <automated>./manage.py test solsys_code.tests.test_campaign_forms</automated>
  </verify>
  <done>
    All test_campaign_forms tests pass; the form is a `forms.Form` subclass (not ModelForm);
    honeypot is a HiddenInput non-required field whose clean never raises; contact fields required.
  </done>
  <acceptance_criteria>
    - `grep -n "class CampaignRunSubmissionForm(forms.Form)" solsys_code/campaign_forms.py` matches;
      `grep -c "ModelForm" solsys_code/campaign_forms.py` returns 0.
    - `./manage.py test solsys_code.tests.test_campaign_forms` reports OK with tests for: minimal
      valid submission, missing campaign invalid, missing contact_person invalid, missing
      contact_email invalid, honeypot-filled still valid, honeypot widget is HiddenInput.
    - `form.fields['contact_person'].required` and `form.fields['contact_email'].required` are both
      `True`; `form.fields['telescope_instrument'].required` is `False`.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anonymous browser → submission form | Untrusted public input crosses into a Django form |
| non-staff request → staff-gated view | Privilege boundary enforced by StaffRequiredMixin (applied in Plan 03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-01 | Denial of Service / Tampering | `CampaignRunSubmissionForm` honeypot field | medium | mitigate | Hidden `alt_contact_info` field (HiddenInput, non-obvious name); `clean_alt_contact_info` never raises so the bot gets no error signal — the drop happens in the view (Plan 02) |
| T-16-03 | Information Disclosure / Elevation of Privilege | `StaffRequiredMixin` | high | mitigate | Dispatch-level `user_passes_test(is_staff)` redirect; the actual gate is applied to `ApprovalQueueView`/decision view in Plan 03 |
| T-16-05 | Tampering | submission/approval POST forms | medium | mitigate | Django `CsrfViewMiddleware` already active; crispy-forms emits `{% csrf_token %}` automatically (verified in templates, Plans 02/03) |
| T-16-SC | Tampering (supply chain) | package installs | low | accept | No new packages introduced this phase (RESEARCH.md Package Legitimacy Audit: N/A) — nothing to install |
</threat_model>

<verification>
- `./manage.py test solsys_code.tests.test_campaign_forms` passes.
- `python -c` import check for `StaffRequiredMixin` and `EMAIL_BACKEND` prints OK.
- `ruff check solsys_code/mixins.py solsys_code/campaign_forms.py solsys_code/tests/test_campaign_forms.py src/fomo/settings.py` and `ruff format --check` clean.
</verification>

<success_criteria>
- `StaffRequiredMixin` importable and gates on `is_staff`.
- `CampaignRunSubmissionForm` is a plain `forms.Form` exposing exactly the D-05 field subset,
  with campaign/contact_person/contact_email required and a non-raising HiddenInput honeypot.
- `EMAIL_BACKEND` console backend configured; production override documented.
</success_criteria>

<output>
Create `.planning/phases/16-submission-form-approval-queue-calendar-projection-write-pat/16-01-SUMMARY.md` when done.
</output>
