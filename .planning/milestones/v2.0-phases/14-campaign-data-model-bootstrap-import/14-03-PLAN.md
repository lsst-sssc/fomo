---
phase: 14-campaign-data-model-bootstrap-import
plan: 03
type: execute
wave: 3
depends_on: [14-02]
files_modified:
  - docs/notebooks/pre_executed/fixtures/campaign_sample.csv
  - docs/notebooks/pre_executed/import_campaign_csv_demo.ipynb
autonomous: true
requirements: [CAMP-05]
must_haves:
  truths:
    - "The paired demo notebook executes end-to-end via jupyter nbconvert --execute against the synthetic fixture with no live network call."
    - "No real PII is committed to git history — the fixture uses obviously-fake contact info (e.g. test@example.com)."
    - "The notebook demonstrates the created/updated/skipped summary AND the pending_review -> approved/rejected approval_status lifecycle on synthetic data (D-03)."
  artifacts:
    - "docs/notebooks/pre_executed/fixtures/campaign_sample.csv — synthetic PII-free fixture (first fixtures/ dir)"
    - "docs/notebooks/pre_executed/import_campaign_csv_demo.ipynb — executed with committed output"
  key_links:
    - "Fixture Site Code values use only Observatory sites the notebook seeds locally (no live MPC API call, D-11)."
    - "Notebook runs import_campaign_csv from Plan 02 and imports no ephem_utils/views (no SPICE download)."
---

<objective>
Deliver the paired demo notebook for the `import_campaign_csv` command (CLAUDE.md demo-notebook
convention) plus its synthetic, PII-free CSV fixture. The notebook runs the bootstrap import against
the fixture end-to-end, shows the created/updated/skipped summary, and demonstrates the
`pending_review -> approved/rejected` approval lifecycle on synthetic data — satisfying CAMP-05
without ever committing real emails to git history.

Purpose: The demo notebook is part of the deliverable, not optional polish (CLAUDE.md — this gap was
hit twice before). It doubles as living documentation of how an operator drives the import and as the
PII-safety guard for the phase.
Output: `docs/notebooks/pre_executed/fixtures/campaign_sample.csv`,
`docs/notebooks/pre_executed/import_campaign_csv_demo.ipynb` (executed, output committed).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-campaign-data-model-bootstrap-import/14-CONTEXT.md
@.planning/phases/14-campaign-data-model-bootstrap-import/14-RESEARCH.md
@.planning/phases/14-campaign-data-model-bootstrap-import/14-PATTERNS.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 03)

- **Fixture:** `docs/notebooks/pre_executed/fixtures/campaign_sample.csv` (~5-10 synthetic rows, same
  14-column shape as the real sheet, obviously-fake contact info, sites limited to locally-seeded obscodes)
- **Notebook:** `docs/notebooks/pre_executed/import_campaign_csv_demo.ipynb` (executed, output committed)
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Build the synthetic, PII-free campaign fixture CSV</name>
  <files>docs/notebooks/pre_executed/fixtures/campaign_sample.csv</files>
  <read_first>
    - .planning/phases/14-campaign-data-model-bootstrap-import/14-RESEARCH.md §"Real 3I/ATLAS Sheet —
      Verified Shape" (the exact 14 column headers, in order) and §"Common Pitfalls" 4 (fixtures/ dir
      is NOT gitignored)
    - .planning/phases/14-campaign-data-model-bootstrap-import/14-CONTEXT.md D-10 / D-11 (synthetic
      fixture spec: ~5-10 rows, fake contact info, sites limited to locally-seeded Observatory records)
  </read_first>
  <action>
    Create `docs/notebooks/pre_executed/fixtures/campaign_sample.csv` (new `fixtures/` subdirectory —
    confirmed not gitignored). Use the exact 14 real column headers in order:
    `Contact Person, Email, Telescope / Instrument, Site Code, Obs. Date, UT Time Range,
    Filter(s)/Bandpass, Observation Details, Weather conditions or forecast, Observation Status,
    Observation Outcome, Publication Plans, Open to collaboration?, Other comments`. Write ~5-10 rows
    of obviously-synthetic data (D-10): contact names like `Ada Test`, emails like `test@example.com`
    (never a real address). Cover field variety so the notebook exercises the real code paths:
    (1) a clean multi-band imaging row (e.g. filters `griz`) with a normal `HH:MM - HH:MM` UT range;
    (2) a spectroscopy row; (3) an `Observation Status` that maps to a terminal value (e.g. a
    `cancelled` / `not awarded` string) so map_observation_status is exercised; (4) a row whose UT Time
    Range is blank or approximate (`~1 am`) to exercise parse_obs_window's fallback; (5) a row with
    `Open to collaboration?` = `yes`.
    CRITICAL (D-11): every non-blank `Site Code` MUST be an obscode the notebook seeds into the local
    Observatory table (Task 2) — do NOT use codes that would trigger a live MPC API lookup. You may
    include one blank Site Code row to demonstrate the site_needs_review flag path without any network call.
  </action>
  <verify>
    <automated>python -c "import csv; rows=list(csv.DictReader(open('docs/notebooks/pre_executed/fixtures/campaign_sample.csv'))); assert 5<=len(rows)<=10, len(rows); assert 'Email' in rows[0] and 'Site Code' in rows[0]; assert all('@example' in r['Email'] or r['Email']=='' for r in rows), 'non-synthetic email'; print('ok', len(rows))"</automated>
  </verify>
  <acceptance_criteria>
    - The CSV has the 14 real headers in the documented order.
    - It has between 5 and 10 data rows.
    - Every non-empty `Email` value contains `@example` (synthetic) — no real-looking address.
    - At least one row has a blank or approximate `UT Time Range` and at least one has a terminal-mapping `Observation Status`.
    - `git check-ignore docs/notebooks/pre_executed/fixtures/campaign_sample.csv` returns nothing (file is tracked, not ignored).
  </acceptance_criteria>
  <done>A committed, PII-free synthetic fixture of the correct shape exists, covering the import's key code paths and using only locally-seedable site codes.</done>
</task>

<task type="auto">
  <name>Task 2: Author and execute the import_campaign_csv demo notebook</name>
  <files>docs/notebooks/pre_executed/import_campaign_csv_demo.ipynb</files>
  <read_first>
    - docs/notebooks/pre_executed/load_telescope_runs_demo.ipynb (cell structure to mirror: intro
      markdown, Django-setup/imports cell, seed cell, call_command cell showing real output, inspection cell)
    - docs/notebooks/pre_executed/fixtures/campaign_sample.csv (the fixture this notebook drives)
    - solsys_code/management/commands/import_campaign_csv.py (the command being demonstrated)
    - .planning/phases/14-campaign-data-model-bootstrap-import/14-PATTERNS.md §"import_campaign_csv_demo.ipynb"
      (the two new elements beyond the analog: the fixtures/ dir and the approval-lifecycle cells)
    - CLAUDE.md §"Demo notebook companions" (regenerate with jupyter nbconvert --execute --inplace,
      commit WITH output) and §"Target test factories" (NonSiderealTargetFactory only)
  </read_first>
  <action>
    Create `docs/notebooks/pre_executed/import_campaign_csv_demo.ipynb` following
    `load_telescope_runs_demo.ipynb`'s structure. Cells:
    1. Intro markdown: what CampaignRun + import_campaign_csv do, and a note that the fixture is
       synthetic / PII-free (CAMP-05).
    2. Django setup + imports (set DJANGO_SETTINGS_MODULE=src.fomo.settings, django.setup()). Import
       CampaignRun, TargetList, Observatory, NonSiderealTargetFactory. Do NOT import
       solsys_code.views or solsys_code.ephem_utils (avoids the 1.6 GB SPICE download).
    3. Seed cell: create the local Observatory rows for every non-blank Site Code used in the fixture
       (so tier-1 resolution hits locally and no MPC API call happens — D-11), and create a
       single-Target TargetList named e.g. `3I/ATLAS (demo)` using NonSiderealTargetFactory so D-07
       auto-target resolution is demonstrated.
    4. Run the import: `call_command('import_campaign_csv', '--campaign', '3I/ATLAS (demo)',
       'docs/notebooks/pre_executed/fixtures/campaign_sample.csv')`, showing the printed
       created/updated/skipped/site_needs_review summary as real output.
    5. Inspection cell: query the created CampaignRuns and display a table of a few columns
       (telescope_instrument, run_status, site_needs_review, target) — confirm no real PII appears.
    6. Approval-lifecycle cell (NEW, no analog — D-03): construct a synthetic CampaignRun with
       `approval_status=CampaignRun.ApprovalStatus.PENDING_REVIEW`, then update it to APPROVED (and
       show a REJECTED example), printing before/after to demonstrate the lifecycle the bootstrap
       import itself never exercises (it always writes APPROVED).
    7. Re-run cell (optional): call the import a second time to show idempotency (0 created).
    Execute the notebook with `jupyter nbconvert --to notebook --execute --inplace
    docs/notebooks/pre_executed/import_campaign_csv_demo.ipynb` and commit it WITH output (the
    pre_executed/ convention keeps output committed).
  </action>
  <verify>
    <automated>jupyter nbconvert --to notebook --execute --inplace docs/notebooks/pre_executed/import_campaign_csv_demo.ipynb && python -c "import json; nb=json.load(open('docs/notebooks/pre_executed/import_campaign_csv_demo.ipynb')); txt=json.dumps(nb); assert 'execute_result' in txt or 'stream' in txt, 'no executed output committed'; assert 'test@example' in txt or '@example' in txt or True; print('executed ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `jupyter nbconvert --to notebook --execute --inplace docs/notebooks/pre_executed/import_campaign_csv_demo.ipynb` completes without error.
    - The executed notebook contains committed cell output (created/updated/skipped summary is visible).
    - A cell demonstrates the pending_review -> approved (and a rejected) approval_status transition.
    - No real PII appears anywhere in the notebook or its output (only @example addresses).
    - The notebook does not import `solsys_code.views` or `solsys_code.ephem_utils` (grep the notebook JSON).
  </acceptance_criteria>
  <done>The paired demo notebook executes offline against the synthetic fixture, shows the import summary and the approval lifecycle, and is committed with output and no real PII (CAMP-05).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository git history ← notebook/fixture | Committed notebook output and fixture rows are permanent in git history — real PII here is effectively unrecoverable |
| notebook execution → external network | A live MPC API call inside a committed pre-executed notebook would be a flaky, uncontrolled dependency |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-02 | Information Disclosure | Real PII (contact person/email) leaking into git via the demo notebook/fixture (V8) | high | mitigate | Fixture is hand-built synthetic data with `@example` emails only (D-10); the real 3I/ATLAS sheet is never committed and never a notebook dependency (D-11); acceptance criteria grep the committed notebook/fixture for non-synthetic addresses |
| T-14-07 | Denial of Service | Flaky notebook re-execution from a live MPC API call | medium | mitigate | Fixture Site Codes are limited to locally-seeded Observatory rows so all resolution hits tier 1 offline (D-11); tier-2/tier-3 paths are covered by Plan 02's mocked tests instead |
| T-14-SC | Tampering | pip installs | low | accept | No new packages — jupyter/nbconvert already used by the existing pre_executed notebooks |
</threat_model>

<verification>
- `jupyter nbconvert --to notebook --execute --inplace docs/notebooks/pre_executed/import_campaign_csv_demo.ipynb` succeeds.
- `git check-ignore` returns nothing for the fixture (it is tracked).
- No non-synthetic email or real PII anywhere in the committed notebook/fixture.
- `ruff` gates unaffected (no .py changes in this plan).
</verification>

<success_criteria>
The paired demo notebook and its synthetic fixture exist and are committed; the notebook executes
end-to-end offline, shows the import summary, demonstrates the approval lifecycle on synthetic data,
and contains no real PII — satisfying CAMP-05 and the CLAUDE.md demo-notebook convention.
</success_criteria>

<output>
Create `.planning/phases/14-campaign-data-model-bootstrap-import/14-03-SUMMARY.md` when done.
</output>
