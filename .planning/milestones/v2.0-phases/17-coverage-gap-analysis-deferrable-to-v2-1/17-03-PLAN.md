---
phase: 17-coverage-gap-analysis-deferrable-to-v2-1
plan: 03
type: execute
wave: 3
depends_on: ['17-02']
files_modified:
  - src/templates/campaigns/campaignrun_gap_analysis.html
  - src/templates/campaigns/campaignrun_table.html
  - solsys_code/tests/test_campaign_gap.py
autonomous: false
requirements: [GAP-02]

must_haves:
  truths:
    - "The gap-analysis page renders the gap-date list (or a 'No gaps found' empty state), the 'Last computed' timestamp, the scoped selection form, and the wait-state notice before submission (D-09/D-10, UI-SPEC)."
    - "Undated runs (D-08) render in a 'Needs review: undated runs' alert-warning block, never silently dropped; the D-03 'observability unknown' caveat shows only when its count > 0."
    - "The per-campaign table page shows a 'Show Coverage Gaps' button linking to the gap page; when D-14 applies (no target, or no run with a resolved site) the button is disabled with the explanatory helper text, not a dead clickable button."
    - "The IDOR 400 case re-renders the selection form with the error copy, never a raw Django debug/400 page."
  artifacts:
    - src/templates/campaigns/campaignrun_gap_analysis.html
    - src/templates/campaigns/campaignrun_table.html
    - solsys_code/tests/test_campaign_gap.py
  key_links:
    - "campaignrun_table.html's button links to campaigns:gap_analysis and gates on the D-14 availability flag supplied by the view."
    - "campaignrun_gap_analysis.html is the template_name CampaignGapAnalysisView renders."
---

<objective>
Render the gap-analysis page and the campaign-table trigger button to the UI-SPEC contract, gate
the button per D-14, and prove the gating with an integration test — then a human verifies the page
visually.

Purpose: Completes GAP-02's user-facing surface (D-09's own page, D-14's disabled-with-reason
button, D-08/D-03/D-10 status copy) matching the existing Bootstrap 4 / crispy campaign templates.
Output: `campaignrun_gap_analysis.html`, the modified `campaignrun_table.html`, the
`TestGapAnalysisButton` integration test, and a human-verify checkpoint.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-coverage-gap-analysis-deferrable-to-v2-1/17-CONTEXT.md
@.planning/phases/17-coverage-gap-analysis-deferrable-to-v2-1/17-UI-SPEC.md
@.planning/phases/17-coverage-gap-analysis-deferrable-to-v2-1/17-PATTERNS.md
@src/templates/campaigns/campaignrun_table.html
@solsys_code/campaign_views.py
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 03)

New template `src/templates/campaigns/campaignrun_gap_analysis.html` (extends
`tom_common/base.html`), rendering: page title `{{ campaign.name }} — Coverage Gaps`; the crispy
`CampaignGapAnalysisForm` inside a `card bg-light p-3` panel with the wait-state notice as static
help text under the 'Update Results' button; a 'Last computed: …' 14px muted caption; the gap-date
list or the 'No gaps found' empty state; the D-08 'Needs review: undated runs' alert-warning block;
the D-03 'observability unknown' caveat (shown only when count > 0); the IDOR/400 form-level
`alert-danger`; and a 'Back to Campaign Table' `btn-outline-primary` link.

Modified `src/templates/campaigns/campaignrun_table.html`: a 'Show Coverage Gaps' `btn-primary`
button (D-09) added to the header row, gated on the D-14 availability flag — disabled + helper text
when unavailable.

In `solsys_code/tests/test_campaign_gap.py`: new `TestGapAnalysisButton` class.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Gap-analysis page template + table-page trigger button (GAP-02, D-08/D-09/D-14, UI-SPEC)</name>
  <files>src/templates/campaigns/campaignrun_gap_analysis.html, src/templates/campaigns/campaignrun_table.html</files>
  <read_first>
    - src/templates/campaigns/campaignrun_table.html (block names, header row, card bg-light p-3 panel, btn-primary conventions)
    - src/templates/campaigns/campaign_list.html (fa fa-exclamation-circle alert-warning pending-review banner style to mirror for D-08)
    - .planning/phases/17-coverage-gap-analysis-deferrable-to-v2-1/17-UI-SPEC.md (Copywriting Contract + Interaction States — every row is load-bearing; the checker verifies each)
    - solsys_code/campaign_views.py (exact context keys CampaignGapAnalysisView supplies: form, campaign, result dict keys, D-14 availability flag + message)
  </read_first>
  <action>
    Create campaignrun_gap_analysis.html extending tom_common/base.html, matching the Bootstrap 4 /
    django-crispy-forms conventions of the existing campaign templates (do NOT invent a new visual
    language). Implement, verbatim to the UI-SPEC Copywriting Contract:
    - Page title (h4 font-weight-bold): '{{ campaign.name }} — Coverage Gaps'.
    - Selection form inside a `card bg-light p-3 mb-4` panel; render the crispy form via
      {% crispy form %} or {% load crispy_forms_tags %} as the other campaign forms do; the submit
      button reads 'Update Results'. Directly under it, static help text (14px muted): 'Computing
      observable nights can take up to a minute for a full 180-day window. Results are cached for
      one hour afterward.' — visible BEFORE submission (D-09 is a plain non-htmx GET; no spinner).
    - When a result is present: a 14px muted caption 'Last computed: {{ result.computed_at }} UTC'
      directly above the gap list; then the gap-date list. Empty state (no gaps): h5
      font-weight-bold 'No gaps found' plus the body copy 'Every observable night in this window is
      already claimed by an approved run. Try widening the date range (up to 180 days) or choosing a
      different site.'
    - D-08 block (render only when undated count > 0): an `alert-warning` block, heading 'Needs
      review: undated runs', body '{{ count }} run(s) for this campaign have no observation date or
      start time and could not be checked against these dates.' Mirror campaign_list.html's
      alert-warning style. If the view also supplies target-unattributed runs (multi-target,
      RESEARCH.md Pitfall 4), surface them in the same needs-review area.
    - D-03 caveat (render only when unknown_date_count > 0): 14px muted caption '{{ n }} date(s)
      could not be evaluated for this site and are excluded from this list.' Omit entirely when zero.
    - IDOR/400 case: a form-level `alert-danger` with 'That target or site isn't part of this
      campaign. Choose one from the list below.' re-showing the form — never a raw 400 page.
    - A 'Back to Campaign Table' `btn-outline-primary` link to campaigns:table.
    Color discipline (UI-SPEC): btn-primary/accent blue only on 'Update Results' and the back link;
    never on list items or informational text. Use only Bootstrap 4 spacer utilities (no inline
    32px/64px styles).

    Modify campaignrun_table.html: in the existing header `d-flex justify-content-between` row, add
    a 'Show Coverage Gaps' `btn-primary` button (plain text, no icon, matching the icon-free
    'Submit a Run' button already there). Gate it on the view-supplied D-14 availability flag: when
    available, an `<a class="btn btn-primary" href="{% url 'campaigns:gap_analysis' pk=campaign.pk %}">`;
    when unavailable, render a `disabled` button (btn-primary disabled) with 14px muted helper text
    beside it: 'Coverage-gap analysis needs at least one campaign target and at least one run with a
    resolved site.' Prefer disabled-with-explanation over fully hidden so the control's absence is
    legible (UI-SPEC Interaction States). Note: CampaignRunTableView must supply the availability
    flag to its context — if it does not yet, add it there by reusing the view's
    gap_analysis_available(campaign) helper from Plan 02 (import/reference, do not duplicate the
    logic).
  </action>
  <verify>
    <automated>./manage.py shell -c "import django.template.loader as l; l.get_template('campaigns/campaignrun_gap_analysis.html'); l.get_template('campaigns/campaignrun_table.html'); print('templates load')"</automated>
  </verify>
  <acceptance_criteria>
    - Both templates load without a TemplateSyntaxError.
    - The gap page renders title, scoped form, wait-notice, last-computed caption, gap list / empty state, D-08 alert-warning (when applicable), D-03 caveat (only when count>0), and back link per UI-SPEC.
    - The table page shows a 'Show Coverage Gaps' button, disabled-with-helper-text when D-14 applies.
  </acceptance_criteria>
  <done>Both templates render to the UI-SPEC contract; the trigger button is gated per D-14.</done>
</task>

<task type="auto">
  <name>Task 2: TestGapAnalysisButton — D-14 gating integration test (GAP-02)</name>
  <files>solsys_code/tests/test_campaign_gap.py</files>
  <read_first>
    - solsys_code/tests/test_campaign_gap.py (existing fixture base + view tests to extend)
    - .planning/phases/17-coverage-gap-analysis-deferrable-to-v2-1/17-VALIDATION.md (TestGapAnalysisButton is the expected class name)
    - src/templates/campaigns/campaignrun_table.html (final markup to assert against)
  </read_first>
  <action>
    Append a TestGapAnalysisButton class (name is load-bearing per VALIDATION.md), using the same
    LocMemCache @override_settings + cache.clear() setUp and NonSiderealTargetFactory fixtures.
    GET the campaigns:table page for three cases and assert on the rendered HTML:
    - A campaign with a target and at least one CampaignRun with a resolved site → the 'Show
      Coverage Gaps' link is present and enabled (href to campaigns:gap_analysis, not disabled).
    - A campaign with zero targets → the button is present but disabled with the D-14 helper text
      (D-14); assert the explanatory copy appears and no live href/enabled button.
    - A campaign whose runs all have site=None (no resolved site) → button disabled with the same
      helper text (D-14).
    Assert against response content (assertContains / assertNotContains) so the gating is proven at
    the rendered-template level, not just in view context.
  </action>
  <verify>
    <automated>./manage.py test solsys_code.tests.test_campaign_gap.TestGapAnalysisButton -v 2</automated>
  </verify>
  <acceptance_criteria>
    - TestGapAnalysisButton passes for all three cases (enabled, no-target-disabled, no-site-disabled).
    - The disabled cases assert the D-14 explanatory helper text is present and the button is not a live link.
  </acceptance_criteria>
  <done>The D-14 button-gating behavior is proven at the rendered-template level by TestGapAnalysisButton.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify the gap-analysis page and trigger button</name>
  <files>src/templates/campaigns/campaignrun_gap_analysis.html, src/templates/campaigns/campaignrun_table.html</files>
  <action>
    Human-verification checkpoint (no code changes). The executor pauses here and presents the
    running gap-analysis page and trigger button for visual/interaction review against the UI-SPEC,
    following the how-to-verify steps below.
  </action>
  <what-built>
    The coverage-gap analysis page (`campaigns:gap_analysis`) and the 'Show Coverage Gaps' trigger
    button on the per-campaign table page, rendered to the UI-SPEC contract: scoped target/site/
    end-date form, wait-state notice, 'Last computed' timestamp, gap-date list / 'No gaps found'
    empty state, D-08 'Needs review: undated runs' warning, D-03 observability-unknown caveat, and
    the D-14 disabled-with-reason button state.
  </what-built>
  <how-to-verify>
    1. Start the dev server: `./manage.py runserver`.
    2. Ensure an Observatory with a timezone set exists for a campaign's runs (e.g. seed obscode
       '268' via the demo/CreateObservatory flow if the dev DB has none), and a campaign
       (TargetList) with at least one target and one approved CampaignRun with a resolved site.
    3. Visit the campaign table page (`/campaigns/<pk>/`). Confirm the 'Show Coverage Gaps'
       btn-primary button appears in the header row (plain text, no icon).
    4. For a campaign with no target or no resolved-site run, confirm the button is disabled with
       the helper text 'Coverage-gap analysis needs at least one campaign target and at least one
       run with a resolved site.'
    5. Click 'Show Coverage Gaps'. Confirm the gap page shows the scoped site (and target, if
       multi-target) dropdown, the wait-state notice under 'Update Results', and — after choosing a
       site and clicking Update Results — a gap-date list (or 'No gaps found'), a 'Last computed:'
       timestamp, and (if any undated runs exist) the 'Needs review: undated runs' warning. A full
       180-day run may take up to ~1 minute (expected, per RESEARCH.md); a small default window is
       faster.
    6. Confirm the styling matches the existing campaign pages (Bootstrap 4, card bg-light panel,
       btn-primary only on the CTA/back link).
  </how-to-verify>
  <resume-signal>Type "approved" or describe visual/interaction issues to fix.</resume-signal>
  <verify>
    <human-check>Reviewer confirms the gap page and D-14-gated button match the UI-SPEC contract (copy, color, spacing, states).</human-check>
  </verify>
  <done>Human reviewer types "approved" (or all reported visual/interaction issues are fixed and re-approved).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rendered trigger button → server | The D-14 disabled button is a UX affordance only; the real access/scope control lives server-side in Plan 02's view. A user can still hand-craft a GET to the gap URL. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-04 | Tampering | D-14 client-side button gating bypass | low | accept | The hidden/disabled button is defense-in-depth only; a direct GET to campaigns:gap_analysis is fully re-validated server-side by CampaignGapAnalysisView (T-17-01/T-17-02 in Plan 02). No client-side gate is relied upon for security. |
| T-17-SC | Tampering | pip installs | low | accept | No new packages introduced by this plan (template + test changes only). |
</threat_model>

<verification>
- Both templates load and render without TemplateSyntaxError.
- `./manage.py test solsys_code.tests.test_campaign_gap` is fully green (all classes across Plans 01-03).
- Human-verify checkpoint approved.
- UI-SPEC Copywriting Contract strings appear verbatim in the rendered pages.
</verification>

<success_criteria>
- GAP-02 user surface complete: the gap page and the D-14-gated trigger button match the UI-SPEC.
- D-08/D-03/D-10 status copy rendered; IDOR 400 re-renders the form, never a debug page.
</success_criteria>

<notes>
Demo-notebook check (CLAUDE.md convention): this plan changes templates and tests only; it does NOT
change the behavior of telescope_runs.py, load_telescope_runs.py, or the LCO/Gemini sync commands.
No paired demo-notebook update is required for this phase.
</notes>

<output>
Create `.planning/phases/17-coverage-gap-analysis-deferrable-to-v2-1/17-03-SUMMARY.md` when done.
</output>
