---
phase: 17-coverage-gap-analysis-deferrable-to-v2-1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/17-coverage-gap-analysis-deferrable-to-v2-1/17-GAP-01-DECISION.md
  - solsys_code/campaign_gap.py
  - solsys_code/tests/test_campaign_gap.py
autonomous: true
requirements: [GAP-01, GAP-02]

must_haves:
  truths:
    - "17-GAP-01-DECISION.md exists and documents the dark-window-only decision (D-01/D-02) with rationale citing pre-milestone research."
    - "observable_dates(site, start, end) returns dates whose dark window is non-zero and skips a per-date ValueError as unknown (D-03/D-04)."
    - "claimed_dates() excludes runs whose run_status is cancelled/not_awarded/weather_tech_failure and derives the night from obs_date, else from ut_start in the site-local observing-night convention (D-05/D-06/D-07)."
    - "A CampaignRun with neither obs_date nor ut_start is flagged undated, never dropped and never counted as claiming a date (D-08)."
    - "clamp_date_range enforces the 90-day default and 180-day hard cap regardless of client input (D-11)."
    - "The cache key includes all four dimensions (campaign, target, site, date range); a null target is encoded explicitly, never omitted (D-10)."
  artifacts:
    - .planning/phases/17-coverage-gap-analysis-deferrable-to-v2-1/17-GAP-01-DECISION.md
    - solsys_code/campaign_gap.py
    - solsys_code/tests/test_campaign_gap.py
  key_links:
    - "campaign_gap.py depends only on solsys_code.telescope_runs.sun_event for ephemerides — never the heavy SPICE-loading ephemeris/views modules at module scope."
    - "get_or_compute_gap reads/writes django.core.cache.cache with a 3600s TTL and stamps computed_at."
---

<objective>
Deliver the GAP-01 decision artifact plus the pure-logic core of the coverage-gap computation
(`campaign_gap.py`) and its unit tests — no view, form, URL, or template concerns.

Purpose: This is the "genuinely novel synthesis" heart of GAP-02 — composing
`telescope_runs.sun_event()` (observable side) with a `CampaignRun` query (claimed side) into a
set-difference, cached with a TTL. Building and unit-testing it in isolation keeps the heavy view
wiring (Plan 02) and templates (Plan 03) thin.
Output: `17-GAP-01-DECISION.md`, `solsys_code/campaign_gap.py`, and unit tests in
`solsys_code/tests/test_campaign_gap.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-coverage-gap-analysis-deferrable-to-v2-1/17-CONTEXT.md
@.planning/phases/17-coverage-gap-analysis-deferrable-to-v2-1/17-RESEARCH.md
@.planning/phases/17-coverage-gap-analysis-deferrable-to-v2-1/17-PATTERNS.md
@solsys_code/telescope_runs.py
@solsys_code/models.py
@solsys_code/campaign_utils.py
@.planning/milestones/v1.7-phases/13-eso-feasibility-spike/13-DECISION.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 01)

New file `.planning/phases/17-coverage-gap-analysis-deferrable-to-v2-1/17-GAP-01-DECISION.md`.

New module `solsys_code/campaign_gap.py` exporting:
- Constant `GAP_CACHE_TTL_SECONDS = 3600`
- Constant `DEFAULT_WINDOW_DAYS = 90`
- Constant `MAX_WINDOW_DAYS = 180`
- Constant `_EXCLUDED_RUN_STATUSES` (frozenset of CANCELLED / NOT_AWARDED / WEATHER_TECH_FAILURE)
- `clamp_date_range(today, requested_end) -> tuple[date, date]`
- `build_gap_cache_key(campaign_pk, target_pk, site_pk, start, end) -> str`
- `_observing_night_date(ut_start, tz_name) -> date` (local-noon-anchored night label, D-07)
- `observable_dates(site, start, end) -> set[date]`
- `claimed_dates(campaign, target, site) -> tuple[set[date], list, list]`
  (returns claimed-date set, undated-run list, target-unattributed-run list)
- `_compute_gap(campaign, target, site, start, end) -> dict`
- `get_or_compute_gap(campaign, target, site, start, end) -> dict`
  (dict keys: `gap_dates`, `claimed_dates`, `observable_dates`, `undated_runs`,
  `unattributed_runs`, `unknown_date_count`, `computed_at`)

New test module `solsys_code/tests/test_campaign_gap.py` with test classes
`TestObservableDates`, `TestClaimedDates`, `TestClampDateRange` (extended by later plans).
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Write the GAP-01 dark-window-only decision artifact (GAP-01, D-02)</name>
  <files>.planning/phases/17-coverage-gap-analysis-deferrable-to-v2-1/17-GAP-01-DECISION.md</files>
  <read_first>
    - .planning/milestones/v1.7-phases/13-eso-feasibility-spike/13-DECISION.md (shape to follow loosely — title/status/decision-with-rationale headers, not length)
    - .planning/phases/17-coverage-gap-analysis-deferrable-to-v2-1/17-CONTEXT.md (D-01, D-02)
  </read_first>
  <action>
    Create the decision artifact satisfying GAP-01's success criterion per D-02 (a short written
    decision doc, not a multi-day spike log). Title `# Phase 17: Coverage-Gap Analysis - GAP-01
    Decision`; a `**Decided:** 2026-07-04` line; a `**Status:**` line noting the decision was
    reached during discuss-phase and is documented after the fact. Then a `## Decision` section
    stating the choice: dark-window-only observability (a non-zero -15 degree astronomical dark
    window that night) reusing telescope_runs' sun_event()/get_site() helpers, NOT true
    target-altitude/airmass filtering. A `## Rationale` section citing that pre-milestone research
    (.planning/research/ARCHITECTURE.md's explicit Decision, PITFALLS.md, SUMMARY.md, STACK.md)
    already unanimously recommends this and that re-running the question at plan time would surface
    no new information (D-01). A `## Consequences` section noting: no module-scope dependency on
    the heavy SPICE-loading ephemeris module is introduced; the only ephemeris cost is
    sun_event()'s measured ~520ms/call (RESEARCH.md Pitfall 1), which shapes the cache/date-range
    design in Plans 02-03. Keep it under ~40 lines.
  </action>
  <verify>
    <automated>test -f .planning/phases/17-coverage-gap-analysis-deferrable-to-v2-1/17-GAP-01-DECISION.md && grep -qi 'dark-window' .planning/phases/17-coverage-gap-analysis-deferrable-to-v2-1/17-GAP-01-DECISION.md</automated>
  </verify>
  <acceptance_criteria>
    - The file exists in the phase directory.
    - It states the dark-window-only decision with a rationale referencing the pre-milestone research.
    - It follows 13-DECISION.md's title/status/decision-with-rationale shape.
  </acceptance_criteria>
  <done>17-GAP-01-DECISION.md exists, documents the dark-window-only decision with rationale, satisfying GAP-01 (verified by document review, not a code test).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the campaign_gap.py pure-computation module (GAP-02)</name>
  <files>solsys_code/campaign_gap.py</files>
  <read_first>
    - solsys_code/telescope_runs.py (sun_event(site, date, kind) signature and its site.timezone / ValueError contract; _local_noon_utc's local-noon-anchored convention)
    - solsys_code/models.py (CampaignRun.RunStatus, ApprovalStatus, obs_date, ut_start, site FK, target FK)
    - solsys_code/campaign_utils.py (module-docstring + "never raise for expected messy data" discipline to mirror)
    - solsys_code/solsys_code_observatory/models.py (Observatory.timezone IANA field, to_earth_location)
    - .planning/phases/17-coverage-gap-analysis-deferrable-to-v2-1/17-PATTERNS.md (Pattern assignments for campaign_gap.py)
  </read_first>
  <action>
    Create solsys_code/campaign_gap.py as a pure-logic helper module mirroring campaign_utils.py's
    role (no view/request concerns). Module docstring must explain that this module depends only on
    telescope_runs.sun_event for ephemerides and must never pull in the heavy SPICE-loading
    ephemeris or views module at module scope (CLAUDE.md "Heavy import side effect"). IMPORTANT: do
    NOT write the literal module name of that heavy ephemeris module, nor the string
    `from solsys_code.views import`, anywhere in this file (docstring, comments, or code) — refer to
    it by description only ("the heavy SPICE-loading ephemeris module") so the phase's static
    import-guard grep stays clean. Import only: stdlib logging/datetime, zoneinfo.ZoneInfo,
    django.core.cache.cache, django.utils.timezone, CampaignRun from solsys_code.models, and
    sun_event from solsys_code.telescope_runs.

    Implement, per the decisions:
    - Module constants GAP_CACHE_TTL_SECONDS=3600 (D-10), DEFAULT_WINDOW_DAYS=90 and
      MAX_WINDOW_DAYS=180 (D-11), and a frozenset _EXCLUDED_RUN_STATUSES of
      CampaignRun.RunStatus.CANCELLED / NOT_AWARDED / WEATHER_TECH_FAILURE (D-05).
    - clamp_date_range(today, requested_end): start=today; default end=today+90d; if requested_end
      is None return (start, default_end); else return (start, min(requested_end, today+180d)).
      Never trust a client end date past the 180-day cap (D-11, RESEARCH.md Pattern 3).
    - build_gap_cache_key(campaign_pk, target_pk, site_pk, start, end): stable delimited string
      including all four dimensions; encode a None target_pk as the literal 'none' rather than
      omitting the segment, so a null-target request never collides with a differently-scoped one
      (D-10, RESEARCH.md Security Domain cache-collision control).
    - _observing_night_date(ut_start, tz_name): convert the aware ut_start to the site's local time
      via ZoneInfo(tz_name); return the observing-night label using the same local-noon anchor
      sun_event uses — the local date if local time >= noon, else local date minus one day (D-07).
      This keeps the claimed side aligned with sun_event's observable side and avoids an
      off-by-one-night at sites west of UTC.
    - observable_dates(site, start, end): iterate each date in [start, end] inclusive, call
      sun_event(site, d, kind='dark'); add d to the set on success (any non-zero dark window
      counts, D-04); on ValueError log at debug and skip that date as unknown, never abort the loop
      (D-03) — mirror RESEARCH.md Pattern 2 / campaign_utils' log+skip discipline. Return the set.
    - claimed_dates(campaign, target, site): query CampaignRun.objects.filter(campaign=campaign,
      site=site, approval_status=APPROVED).exclude(run_status__in=_EXCLUDED_RUN_STATUSES). Target
      handling per RESEARCH.md Pitfall 4 / D-12: if the campaign has exactly one target, do NOT
      filter by target (the single target is implied, and real imported runs commonly have
      target=None); if the campaign has more than one target, filter target=selected target
      strictly and collect target=None rows into a separate "unattributed" list (do not count them
      as claiming any specific target's date). For each counted run, derive the night: obs_date if
      set, else _observing_night_date(ut_start, site.timezone) when ut_start is set (D-06/D-07). A
      run with neither obs_date nor ut_start goes to an "undated" list, never into the claimed set
      (D-08). Return (claimed_set, undated_runs, unattributed_runs).
    - _compute_gap(campaign, target, site, start, end): obs = observable_dates(site, start, end);
      claimed, undated, unattributed = claimed_dates(campaign, target, site); gap = obs - claimed
      (plain stdlib set difference). Return a dict with sorted gap_dates, claimed_dates,
      observable_dates lists, undated_runs, unattributed_runs, and unknown_date_count (dates in
      range that raised ValueError — total range days minus |observable ∪ claimed-in-range|; compute
      as range_len minus number of dates that sun_event evaluated successfully).
    - get_or_compute_gap(campaign, target, site, start, end): build the cache key; cache.get; on hit
      return the cached dict unchanged (its original computed_at must survive — never re-stamp on a
      hit, D-10); on miss call _compute_gap, set result['computed_at']=timezone.now(),
      cache.set(key, result, timeout=GAP_CACHE_TTL_SECONDS), return it.

    Follow project conventions: single quotes, 120-col, Google-style docstrings, snake_case, module
    logger = logging.getLogger(__name__).
  </action>
  <behavior>
    - clamp_date_range(today, None) == (today, today+90d)
    - clamp_date_range(today, today+500d) == (today, today+180d)  # cap enforced
    - clamp_date_range(today, today+30d) == (today, today+30d)     # inside cap, honoured
    - build_gap_cache_key(1, None, 5, d0, d1) contains '1', 'none', '5' and both isoformat dates; differs from build_gap_cache_key(1, 7, 5, d0, d1)
    - observable_dates over a small range returns the dates whose dark window exists; a date raising ValueError is absent from the set, loop still completes
    - claimed_dates excludes an approved-but-cancelled run and a pending-review run; includes an approved+completed run keyed to its obs_date
    - a run with ut_start but no obs_date is keyed to the site-local observing-night date, not the raw UTC date
    - a run with neither obs_date nor ut_start appears in undated_runs and not in the claimed set
  </behavior>
  <verify>
    <automated>ruff check solsys_code/campaign_gap.py && python -c "import ast,sys; t=ast.parse(open('solsys_code/campaign_gap.py').read()); names={n.name for node in ast.walk(t) if isinstance(node,(ast.FunctionDef,)) for n in [node]}; req={'clamp_date_range','build_gap_cache_key','observable_dates','claimed_dates','get_or_compute_gap','_compute_gap','_observing_night_date'}; sys.exit(0 if req<=names else 1)"</automated>
  </verify>
  <acceptance_criteria>
    - All exported symbols in the Artifacts section exist and pass ruff.
    - The module imports sun_event from telescope_runs and never the heavy SPICE-loading ephemeris/views module.
    - The behaviors above are satisfiable (verified by Task 3's unit tests).
  </acceptance_criteria>
  <done>campaign_gap.py exists with all listed functions/constants, passes ruff, and imports only telescope_runs for ephemerides.</done>
</task>

<task type="auto">
  <name>Task 3: Unit tests for campaign_gap.py + module-scope import guard (GAP-02, GAP-01 transitively)</name>
  <files>solsys_code/tests/test_campaign_gap.py</files>
  <read_first>
    - solsys_code/tests/test_campaign_views.py (CampaignViewTestBase fixture shape: setUpTestData, TargetList.objects.create, staff User)
    - solsys_code/tests/test_sync_gemini_observation_calendar.py (@override_settings precedent)
    - .planning/phases/17-coverage-gap-analysis-deferrable-to-v2-1/17-PATTERNS.md (cache test-isolation + NonSiderealTargetFactory guidance)
    - solsys_code/solsys_code_observatory/models.py (Observatory fields needed to build a fixture site with a timezone set)
  </read_first>
  <action>
    Create solsys_code/tests/test_campaign_gap.py using django.test.TestCase. Decorate the cache-
    touching classes with @override_settings(CACHES={'default': {'BACKEND':
    'django.core.cache.backends.locmem.LocMemCache'}}) and call cache.clear() in setUp
    (belt-and-suspenders per RESEARCH.md Pitfall 2). Always use
    tom_targets.tests.factories.NonSiderealTargetFactory for any Target fixture — never
    SiderealTargetFactory (CLAUDE.md). Build fixture Observatory rows directly with a timezone set
    (e.g. obscode '268', timezone 'America/Santiago', a real altitude) so sun_event works without a
    live MPC call, following the seeding convention in load_telescope_runs / import_campaign_csv.

    Implement these test classes (names are load-bearing — VALIDATION.md's commands reference them):
    - TestClampDateRange: default window is 90 days; a 500-day requested end clamps to 180; a
      30-day request is honoured (D-11).
    - TestObservableDates: over a small (2-3 day) range against the fixture site, returns dates with
      a non-zero dark window (D-04); monkeypatch/patch sun_event to raise ValueError for one date
      and assert that date is skipped while the loop completes (D-03). Keep the real-sun_event range
      small (2-3 days) to stay well under the ~520ms/call cost.
    - TestClaimedDates: an approved+completed run with obs_date is claimed; an approved-but-cancelled
      run and a pending-review run are NOT claimed (D-05); a run with ut_start but no obs_date keys
      to the site-local observing-night date (D-06/D-07); test_undated_runs_flagged: a run with
      neither obs_date nor ut_start appears in the undated list and not the claimed set (D-08).

    Add a static import-guard test (a plain TestCase method or module-level assertion) that reads
    solsys_code/campaign_gap.py's source and asserts no real import line pulls in the heavy
    SPICE-loading ephemeris module or does `from solsys_code.views import` — mirror the grep in the
    verify block below so the two agree.
  </action>
  <verify>
    <automated>./manage.py test solsys_code.tests.test_campaign_gap.TestClampDateRange solsys_code.tests.test_campaign_gap.TestObservableDates solsys_code.tests.test_campaign_gap.TestClaimedDates -v 2 && grep -rnE "^[[:space:]]*(from|import)[[:space:]].*ephem_utils|^[[:space:]]*from solsys_code.views import" solsys_code/campaign_gap.py solsys_code/tests/test_campaign_gap.py; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - TestClampDateRange, TestObservableDates, TestClaimedDates (incl. test_undated_runs_flagged) pass.
    - The import-guard grep returns zero matches (exit status 1 from grep, asserted).
    - Cache is isolated via override_settings + cache.clear() so tests pass inside the full suite too.
  </acceptance_criteria>
  <done>Unit tests for the pure-computation module pass, and no phase module imports the heavy SPICE-loading ephemeris/views module at module scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client GET params → gap computation | campaign/target/site/date-range values arriving from the request cross into cache-key construction and the ORM query (validated in Plan 02; this plan supplies the collision-safe key builder and the server-side clamp). |
| computed result → shared FileBasedCache | one campaign's gap result must never be readable under another campaign/target/site's key. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-02 | Denial of Service | clamp_date_range (unbounded date range) | high | mitigate | clamp_date_range hard-caps the span at 180 days regardless of any client-supplied end, bounding worst-case sun_event() calls per request (~94s at 180d, RESEARCH.md Pitfall 1). Server-side only. |
| T-17-03 | Information Disclosure | build_gap_cache_key (cross-campaign cache collision) | medium | mitigate | Cache key includes all four dimensions (campaign, target, site, date range); a null target is encoded as 'none', never omitted, so results never leak across scopes. |
| T-17-SC | Tampering | pip installs | low | accept | This phase introduces no new packages (RESEARCH.md Package Legitimacy Audit: none); nothing to vet. |
</threat_model>

<verification>
- `./manage.py test solsys_code.tests.test_campaign_gap` is green.
- `ruff check solsys_code/campaign_gap.py` and `ruff format --check solsys_code/campaign_gap.py` are clean.
- The import-guard grep over campaign_gap.py + test_campaign_gap.py returns zero matches.
- 17-GAP-01-DECISION.md exists and documents the dark-window-only decision.
</verification>

<success_criteria>
- GAP-01 satisfied: a citable dark-window-only decision artifact exists.
- GAP-02 core logic exists and is unit-tested: observable/claimed/gap set computation, D-03 skip,
  D-05 exclusions, D-06/D-07 night derivation, D-08 undated flagging, D-10 cache key, D-11 clamp.
- No phase module imports the heavy SPICE-loading ephemeris/views module at module scope.
</success_criteria>

<notes>
Demo-notebook check (CLAUDE.md convention): this plan adds a NEW module (campaign_gap.py) and
reads telescope_runs.sun_event()/get_site() read-only. It does NOT change the behavior of
telescope_runs.py, load_telescope_runs.py, sync_lco_observation_calendar.py, or
sync_gemini_observation_calendar.py. Per CONTEXT.md's confirmation that this phase's ephemeris
dependency is limited to those helpers used read-only, NO paired demo-notebook update is required
for this phase. This conclusion is stated explicitly rather than silently omitted.
</notes>

<output>
Create `.planning/phases/17-coverage-gap-analysis-deferrable-to-v2-1/17-01-SUMMARY.md` when done.
</output>

<!-- planner-discipline-allow: ephem_utils -->
<!-- planner-discipline-allow: from solsys_code.views import -->
