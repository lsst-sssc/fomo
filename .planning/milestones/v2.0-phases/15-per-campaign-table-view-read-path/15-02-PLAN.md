---
phase: 15-per-campaign-table-view-read-path
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - solsys_code/apps.py
  - src/templatetags/solsys_code_extras.py
  - src/templates/solsys_code/partials/campaign_links.html
  - src/templates/solsys_code/partials/campaigns_nav_link.html
  - solsys_code/tests/test_campaign_views.py
autonomous: true
requirements: [VIEW-02]

must_haves:
  truths:
    - "The target-detail page for a Target that belongs to a campaign shows one 'View {campaign.name} Runs' link per matching campaign, each linking to that campaign's table (D-01/D-02)"
    - "The target-detail page for a Target in zero campaigns shows no campaign link (empty partial output, no placeholder)"
    - "A Target's campaigns are discovered via TargetList membership (TargetList.objects.filter(targets=target, campaign_runs__isnull=False).distinct()), NOT via CampaignRun.target (D-01)"
    - "Every page's navbar shows a single-word 'Campaigns' entry linking to campaigns:list (D-03)"
  artifacts:
    - src/templates/solsys_code/partials/campaign_links.html
    - src/templates/solsys_code/partials/campaigns_nav_link.html
  key_links:
    - "SolsysCodeConfig.target_detail_buttons() second entry + new nav_items() method both route through tom_common's show_individual_app_partial — module_buttons.html needs NO change"
    - "campaign_links / campaigns_nav_link inclusion tags reverse campaigns:table and campaigns:list (URL namespace created in Plan 01)"
---

<objective>
Wire the campaign table into FOMO's navigation: a per-campaign link (one per matching campaign)
injected into the Target detail page via the existing `target_detail_buttons()` hook, and a new
"Campaigns" navbar entry via a first-ever `nav_items()` hook — completing VIEW-02.

Purpose: A coordinator can reach a campaign's table from the relevant target-detail page and from a
global navbar entry (VIEW-02).
Output: extended `apps.py` hooks, two new inclusion tags, two partials, and integration tests.
</objective>

<artifacts_this_phase_produces>
New symbols introduced by this plan:

- `SolsysCodeConfig.nav_items()` (AppConfig method — first `nav_items()` consumer in the codebase)
- Second entry in `SolsysCodeConfig.target_detail_buttons()` (campaign_links partial)
- Inclusion tags `campaign_links(context)` and `campaigns_nav_link(context)`
  (`src/templatetags/solsys_code_extras.py`)
- New files: `src/templates/solsys_code/partials/campaign_links.html`,
  `src/templates/solsys_code/partials/campaigns_nav_link.html`
- New test class(es) appended to `solsys_code/tests/test_campaign_views.py`
  (TestCampaignDetailIntegration / navbar test)

Note: this plan touches none of the four CLAUDE.md demo-notebook modules, so no paired demo
notebook is in scope.
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-per-campaign-table-view-read-path/15-CONTEXT.md
@.planning/phases/15-per-campaign-table-view-read-path/15-RESEARCH.md
@.planning/phases/15-per-campaign-table-view-read-path/15-PATTERNS.md
@.planning/phases/15-per-campaign-table-view-read-path/15-UI-SPEC.md
@.planning/phases/15-per-campaign-table-view-read-path/15-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend apps.py hooks + add campaign_links / campaigns_nav_link inclusion tags</name>
  <files>solsys_code/apps.py, src/templatetags/solsys_code_extras.py</files>
  <read_first>
    - solsys_code/apps.py (existing target_detail_buttons() and data_services() — mirror the dict-entry shape)
    - src/templatetags/solsys_code_extras.py (existing ephem_button inclusion tag — style/register conventions)
    - .planning/phases/15-per-campaign-table-view-read-path/15-RESEARCH.md (Architecture Pattern 2 target_detail_buttons receives full context incl. context['target']; Pattern 3 nav_items context method called with empty dict; module_buttons.html needs no change)
    - .planning/phases/15-per-campaign-table-view-read-path/15-PATTERNS.md (apps.py + solsys_code_extras.py sections)
  </read_first>
  <action>
    Edit solsys_code/apps.py: append a SECOND dict to the `target_detail_buttons()` return list —
    partial `f'{self.name}/partials/campaign_links.html'`, context
    `'src.templatetags.solsys_code_extras.campaign_links'` (D-01/D-02). Add a new method
    `nav_items(self)` returning a single dict — partial
    `f'{self.name}/partials/campaigns_nav_link.html'`, context
    `'src.templatetags.solsys_code_extras.campaigns_nav_link'`, `'position': 'left'` (D-03). Do NOT
    modify src/templates/tom_targets/partials/module_buttons.html — its `'ephem_button' in
    button.context` dict-KEY check never matches the new campaign_links entry, which correctly falls
    through to the generic show_individual_app_partial branch (RESEARCH Pattern 2).

    Edit src/templatetags/solsys_code_extras.py: add `from tom_targets.models import TargetList`.
    Add `@register.inclusion_tag('solsys_code/partials/campaign_links.html', takes_context=True)`
    tag `campaign_links(context)` that reads `context.get('target')` and returns `{'campaigns':
    TargetList.objects.filter(targets=target, campaign_runs__isnull=False).distinct()}` when target
    is set, else `{'campaigns': TargetList.objects.none()}` (D-01 — discovery via TargetList
    membership, never via CampaignRun.target). Add
    `@register.inclusion_tag('solsys_code/partials/campaigns_nav_link.html', takes_context=True)`
    tag `campaigns_nav_link(context)` returning `{}` (static link; the navbar hook calls its context
    method with an empty dict per RESEARCH Pattern 3, and the partial gets `request` automatically
    via takes_context for the active-nav check).
  </action>
  <verify>
    <automated>./manage.py shell -c "from django.apps import apps; a = apps.get_app_config('solsys_code'); tdb = a.target_detail_buttons(); assert any('campaign_links' in e['partial'] for e in tdb), tdb; nav = a.nav_items(); assert any('campaigns_nav_link' in e['partial'] for e in nav), nav; from src.templatetags.solsys_code_extras import campaign_links, campaigns_nav_link; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - solsys_code/apps.py target_detail_buttons() returns 2 entries, the second referencing campaign_links.html
    - solsys_code/apps.py defines a nav_items() method returning an entry referencing campaigns_nav_link.html
    - src/templatetags/solsys_code_extras.py defines campaign_links and campaigns_nav_link inclusion tags
    - `grep -c 'campaign_runs__isnull=False' src/templatetags/solsys_code_extras.py` returns >= 1 (D-01 reverse-relation discovery)
    - `grep -c 'CampaignRun.target' src/templatetags/solsys_code_extras.py` returns 0 (discovery is NOT via CampaignRun.target)
    - module_buttons.html is unchanged (not in this plan's diff)
    - The shell verify command prints OK
  </acceptance_criteria>
  <done>Both hooks registered; both inclusion tags import cleanly and query via TargetList membership.</done>
</task>

<task type="auto">
  <name>Task 2: Create campaign_links.html and campaigns_nav_link.html partials</name>
  <files>src/templates/solsys_code/partials/campaign_links.html, src/templates/solsys_code/partials/campaigns_nav_link.html</files>
  <read_first>
    - src/templates/solsys_code/partials/ephem_button.html (existing btn btn-info button-row precedent to match)
    - .planning/phases/15-per-campaign-table-view-read-path/15-UI-SPEC.md (Target-detail integration + Navbar integration + Copywriting Contract)
    - .planning/phases/15-per-campaign-table-view-read-path/15-RESEARCH.md (Pattern 2 loop template; Pattern 3 navbar li markup)
  </read_first>
  <action>
    Create src/templates/solsys_code/partials/campaign_links.html: loop `{% for campaign in
    campaigns %}` emitting one `<a class="btn btn-info mr-2 mb-2" href="{% url 'campaigns:table'
    pk=campaign.pk %}">View {{ campaign.name }} Runs</a>` per campaign (Copywriting Contract:
    "View {name} Runs", not a generic label so 2+ buttons are distinguishable; `mr-2 mb-2` gives
    8px wrap gaps per UI-SPEC). Zero campaigns → the loop emits nothing (no placeholder text,
    matching the existing absent-button convention). Reuse `btn btn-info` to match the pre-existing
    "Make Ephemeris" button in the same row (UI-SPEC deliberate exception).

    Create src/templates/solsys_code/partials/campaigns_nav_link.html: a single navbar `<li
    class="nav-item {% if request.resolver_match.namespace == 'campaigns' %}active{% endif %}"><a
    class="nav-link" href="{% url 'campaigns:list' %}">Campaigns</a></li>` — single word, sentence
    case, no icon/dropdown, matching the existing Targets/Data/Users navbar entries (UI-SPEC Navbar
    integration).
  </action>
  <verify>
    <automated>test -f src/templates/solsys_code/partials/campaign_links.html && test -f src/templates/solsys_code/partials/campaigns_nav_link.html && grep -q "campaigns:table" src/templates/solsys_code/partials/campaign_links.html && grep -q "campaigns:list" src/templates/solsys_code/partials/campaigns_nav_link.html && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - src/templates/solsys_code/partials/campaign_links.html exists, loops over campaigns, and reverses campaigns:table with pk=campaign.pk
    - The link text is "View {{ campaign.name }} Runs" and the class is btn btn-info (matches ephem_button precedent)
    - src/templates/solsys_code/partials/campaigns_nav_link.html exists, reverses campaigns:list, and text is exactly "Campaigns"
    - The verify command prints OK
  </acceptance_criteria>
  <done>Both partials render one link per campaign (target detail) and a single navbar entry.</done>
</task>

<task type="auto">
  <name>Task 3: Integration tests for VIEW-02 (target-detail links + navbar entry)</name>
  <files>solsys_code/tests/test_campaign_views.py</files>
  <read_first>
    - solsys_code/tests/test_campaign_views.py (Plan 01 fixtures/conventions — reuse setUpTestData shape; created in Plan 01)
    - solsys_code/models.py (CampaignRun.campaign FK + TargetList membership; note target is optional/nullable)
    - .planning/phases/15-per-campaign-table-view-read-path/15-RESEARCH.md (Test Map row VIEW-02: TestCampaignDetailIntegration)
    - CLAUDE.md (NonSiderealTargetFactory mandate; ./manage.py test solsys_code)
  </read_first>
  <action>
    Append a `TestCampaignDetailIntegration(TestCase)` class to
    solsys_code/tests/test_campaign_views.py. Build fixtures with a campaign `TargetList` that
    CONTAINS a specific Target as a member (via `target_list.targets.add(target)` using a target
    from `NonSiderealTargetFactory` per CLAUDE.md — never SiderealTargetFactory), at least one
    `CampaignRun` linked to that campaign, and a second unrelated Target that is NOT a member of any
    campaign. Tests:
    (1) `test_target_detail_shows_campaign_link` — GET the tom_targets target-detail URL
    (`reverse('tom_targets:detail', kwargs={'pk': member_target.pk})`) as an anonymous client;
    assert the response content contains the campaign's per-campaign table URL
    (`reverse('campaigns:table', kwargs={'pk': campaign.pk})`) and the "View {name} Runs" text —
    proving the campaign_links partial rendered via the target_detail_buttons hook (D-01/D-02).
    (2) `test_target_detail_no_campaign_for_unrelated_target` — GET the detail page for the
    non-member Target; assert the campaign table URL is NOT present (empty partial, D-01 discovery is
    membership-scoped).
    (3) `test_navbar_shows_campaigns_entry` — GET any page rendering the navbar (e.g. the campaigns
    list page `reverse('campaigns:list')`); assert the response content contains the campaigns:list
    href and the text "Campaigns" (D-03 navbar entry). Confirm the tom_targets detail URL name by
    reading the installed tom_targets urls if the reverse fails; adjust the name accordingly.
  </action>
  <verify>
    <automated>./manage.py test solsys_code.tests.test_campaign_views.TestCampaignDetailIntegration 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - solsys_code/tests/test_campaign_views.py defines class TestCampaignDetailIntegration with tests for the campaign link on a member target's detail page, its absence for a non-member target, and the navbar Campaigns entry
    - `./manage.py test solsys_code.tests.test_campaign_views.TestCampaignDetailIntegration` exits 0
    - `./manage.py test solsys_code` exits 0 (full app suite green, no regressions)
    - `grep -c 'SiderealTargetFactory' solsys_code/tests/test_campaign_views.py` shows only NonSiderealTargetFactory is used (no bare SiderealTargetFactory import)
  </acceptance_criteria>
  <done>VIEW-02 proven: campaign link appears on member target detail, absent otherwise, navbar entry present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anonymous browser → target-detail page (campaign_links inclusion tag) | Unauthenticated request triggers a per-render TargetList membership query scoped by the rendered target |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-04 | Information Disclosure | campaign_links inclusion tag surfacing campaigns unrelated to the rendered target, or non-campaign TargetLists | low | mitigate | Query is `TargetList.objects.filter(targets=target, campaign_runs__isnull=False).distinct()` — scoped to the specific target's memberships AND to lists with >=1 run; no cross-target or non-campaign leakage (D-01/Pitfall 3) |
| T-15-05 | Information Disclosure | Navbar/campaign links exposing the read path to anonymous users | low | accept | Intentional per D-04 (campaign list + table are open to anonymous, matching AUTH_STRATEGY='READ_ONLY'); contact PII gating is enforced in Plan 01's table view (T-15-01), not here |

No package-manager installs occur in this plan, so no supply-chain (T-15-SC) threat or install
checkpoint applies. ASVS enforcement level 1; blocking severity threshold high — no high-severity
threats in this plan (contact-PII exposure is handled and test-proven in Plan 01).
</threat_model>

<verification>
- `./manage.py test solsys_code.tests.test_campaign_views.TestCampaignDetailIntegration` — VIEW-02 green
- `./manage.py test solsys_code` — full app suite green (no regressions)
- `ruff check .` and `ruff format --check .` — clean (CLAUDE.md quality gates)
- Manual: load a target-detail page for a 3I/ATLAS member target → campaign link visible; check navbar on any page → "Campaigns" entry present
</verification>

<success_criteria>
- VIEW-02: campaign link reachable from the relevant target-detail page (one per matching campaign) and a navbar "Campaigns" entry exposes the campaigns list — proven by TestCampaignDetailIntegration
- D-01: campaign discovery via TargetList membership, not CampaignRun.target — enforced in the inclusion tag and proven by the non-member-target test
</success_criteria>

<output>
Create `.planning/phases/15-per-campaign-table-view-read-path/15-02-SUMMARY.md` when done
</output>
