---
phase: 32-adapter-consolidation
plan: 03
type: execute
wave: 3
depends_on: ["32-02"]
files_modified:
  - solsys_code/management/commands/sync_lco_observation_calendar.py
  - solsys_code/tests/test_sync_lco_observation_calendar.py
  - docs/notebooks/pre_executed/sync_lco_observation_calendar_demo.ipynb
  - docs/runbooks/telescope_runs_calendar.rst
autonomous: true
requirements: [ADAPT-02, ADAPT-03, ADAPT-04, ADAPT-05]

estimate:
  tokens: 78000
  raw_tokens: 78000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Running `sync_lco_observation_calendar` creates or updates one CampaignRun per synced ObservationRecord, and the command module issues no direct CalendarEvent write of its own."
    - "Every CampaignRun the command writes has exactly one CampaignRunObservation row linking the ObservationRecord it was written from, with confirmed_by unset and confirmed_at set — created before that record's first reconcile, so dispatch sees the link on the very first sync."
    - "A record with facility='SOAR' produces a CampaignRun with source='soar_queue'; a record with facility='LCO' produces source='lco_queue'; no SOAR record is ever written under lco_queue."
    - "A second identical sync leaves CampaignRun.objects.count() and CampaignRunObservation.objects.count() unchanged and reports every record unchanged."
    - "Syncing a proposal with zero matching ObservationRecords creates zero CampaignRun rows and zero CalendarEvent rows and exits 0."
    - "Re-syncing records already synced under the pre-cutover direct-CalendarEvent path leaves CalendarEvent.objects.count() unchanged: each pre-existing url-keyed event is adopted into its new CampaignRun and re-keyed into the RUN: namespace, not duplicated."
    - "A SOAR record reaches the calendar through a resolved Observatory site, because no CampaignRun.TelescopeClass value exists for a 4m aperture and D-12 forbids adding one."
    - "An LCO record's run takes the reconciler's container branch because telescope_class is set; a SOAR record's run — telescope_class blank, site resolved — takes the SAME container branch because plan 32-01 added a dispatch condition for a run with exactly one linked CampaignRunObservation. Both facilities therefore own exactly ONE CalendarEvent per synced record, not one-per-night, and the runbook says so."
    - "Once synced, a SOAR record's projected CalendarEvent's start_time/end_time equal the record's own scheduled_start/scheduled_end whenever both are set on the ObservationRecord — the reconciler's precision-narrowing capability (32-01) applies to SOAR exactly as it already does to LCO, so no calendar-fidelity regression exists at cutover for either facility."
    - "Every CampaignRun the command writes carries approval_status=approved, so no synced record is written-but-never-projected for want of approval."
    - "A record whose telescope class cannot be derived and which has no resolved site is reported as not projected with the reconciler's own skip reason, never silently counted as a success, and projects on the next sync once the missing Observatory row exists — at which point its single observation link routes it straight to the container branch, not a transitional per-night state."
    - "A reschedule that only changes the time of day within the same observing night(s) still marks the run 'updated', not 'unchanged': the run's own diff loop detects the change through the observation_details description text embedding the new times, even though window_start/window_end (date-only) do not change."
    - "The DISPLAY-01 telescope-label verification signal survives cutover: CalendarEventMeta.is_verified is False for a record whose live telescope-API lookup failed and True otherwise."
    - { statement: "Two ObservationRecords whose observing windows are exactly adjacent or identical produce two distinct CampaignRun rows, because each record's portal URL is its own source_identifier — window equality never merges them.", verification: backstop }
  artifacts:
    - solsys_code/management/commands/sync_lco_observation_calendar.py
    - solsys_code/tests/test_sync_lco_observation_calendar.py
    - docs/notebooks/pre_executed/sync_lco_observation_calendar_demo.ipynb
    - docs/runbooks/telescope_runs_calendar.rst
  key_links:
    - "sync_lco_observation_calendar.handle() -> campaign_utils.write_and_reconcile_campaign_run(observation_record=record) (the exact-identity CampaignRunObservation link, created before that call's own reconcile_run() runs)"
    - "event_fields.pop('url') (the record's portal URL, from facility.get_observation_url()) -> both the adopt_event lookup key and CampaignRun.source_identifier — one binding, two uses, never re-derived"
    - "record.facility -> _SOURCE_BY_FACILITY -> CampaignRun.Source.LCO_QUEUE / SOAR_QUEUE"
    - "CampaignRunObservation link (this command) -> reconcile_run()'s observation_links.count() == 1 dispatch condition (32-01) -> _reconcile_container() -> _linked_observation_window() -> record.scheduled_start/scheduled_end (the precision an operator sees on the calendar)"
    - "campaign_reconciler.owned_events(run) -> CalendarEventMeta.is_verified (DISPLAY-01 signal preserved after the reconciler owns the event)"
  prohibitions:
    - "The LCO/SOAR adapter must never re-acquire a direct CalendarEvent write path — every calendar write reaches CalendarEvent transitively through reconcile_run()."
    - "The adapter-created CampaignRunObservation link must never be routed through campaign_attribution's scorer or the staff confirmation queue; it is structural exact identity, not a scored candidate."
    - "A SOAR-sourced run must never be written under source='lco_queue' — the two facilities are distinguished for the same reason ESO_QUEUE was added rather than folded into LCO_QUEUE."
    - "The command must never silently swallow a reconcile_run() skip: a record whose run was written but not projected must be reported on stderr and counted separately."
    - "This plan must never claim or test that a SOAR record owns N per-night events — that was the pre-redesign (rejected) behaviour; plan 32-01's dispatch condition means a linked SOAR record owns exactly one event, identically to LCO."
---

<objective>
Cut `sync_lco_observation_calendar` over from writing `CalendarEvent` rows directly to
creating or updating one `CampaignRun` per synced `ObservationRecord`, linking the
realising `ObservationRecord` by exact identity, and branching the `Source` value per
facility so SOAR records land under the `SOAR_QUEUE` value plan 32-01 added.

Purpose: ADAPT-02 and ADAPT-03. SOAR is the facility that proves the pattern generalises,
because `SOARFacility` inherits a real portal read-back from `LCOFacility` (D-01, gap
G-31-3). This is one command with two source values, not a fourth command. Because this
command is what creates the exact-identity `CampaignRunObservation` link plan 32-01's
reconciler consumes, it is also where ADAPT-02/03's "no calendar-fidelity regression at
cutover" bar is actually exercised: a SOAR record's run reaches the calendar through a
resolved `Observatory` site (there is no 4m telescope class), but once its link exists it
takes the SAME container branch an LCO record already takes and narrows to the record's own
scheduled/observed times exactly as LCO does — not the N-per-night shape an earlier design
would have produced.

Output: a rewired command, exact-identity linking, per-facility source branching, no-churn
and cutover-simulation tests, a regenerated demo notebook, and the runbook section for both
facilities.
</objective>

<execution_context>
@/home/tlister/git/fomo_devel/.claude/gsd-core/workflows/execute-plan.md
@/home/tlister/git/fomo_devel/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-adapter-consolidation/32-CONTEXT.md
@.planning/phases/32-adapter-consolidation/32-PATTERNS.md
@.planning/phases/32-adapter-consolidation/32-01-SUMMARY.md
@.planning/phases/32-adapter-consolidation/32-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewire the LCO/SOAR sync to write a CampaignRun per ObservationRecord</name>
  <files>solsys_code/management/commands/sync_lco_observation_calendar.py</files>
  <read_first>
    - solsys_code/management/commands/sync_lco_observation_calendar.py (the whole file — imports at 1-18, `_FAILURE_PREFIX_BY_STATUS` at 28-33, `_COUNTER_KEYS`/`_new_counters` at 39-49, `_build_event_fields` at 130-220, `handle()` at 270-365 with the facility dispatch at 289 and the write at 341-349)
    - solsys_code/campaign_utils.py (write_and_reconcile_campaign_run and adopt_event_into_run as landed by plan 32-01, including the step ordering that creates the CampaignRunObservation link BEFORE calling reconcile_run())
    - solsys_code/campaign_reconciler.py (_skip_reason at 193-214; reconcile_run's three-condition dispatch, including the new `observation_links.count() == 1` condition plan 32-01 added; _reconcile_container at 249-281 including the adopt step AND the `_linked_observation_window()` precision read plan 32-01 added; owned_events at 108-115)
    - solsys_code/calendar_utils.py (derive_telescope_class at 150-200 and aperture_class_from_telescope_code at 89-120)
    - solsys_code/models.py (CampaignRun.Source including SOAR_QUEUE, CampaignRun.TelescopeClass at 139-165, CampaignRunObservation at 390-451 and its `related_name='observation_links'`)
    - solsys_code/management/commands/load_telescope_runs.py (as rewired by plan 32-02 — the cutover shape to mirror)
  </read_first>
  <precondition>Plan 32-01's `SOAR_QUEUE` migration and its reconciler dispatch/precision changes are applied: `python manage.py shell -c "from solsys_code.models import CampaignRun; print(CampaignRun.Source.SOAR_QUEUE.value)"` prints `soar_queue`, and `grep -c 'observation_links.count() == 1' solsys_code/campaign_reconciler.py` returns at least `1`.</precondition>
  <reversibility rating="costly">D-05 hard cutover, and the second of three. Reverting after `soar_queue`-sourced rows exist means a data migration, not a code revert (D-01's one-way rating, already gated at plan 32-01's checkpoint).</reversibility>
  <behavior>
    - For each synced record exactly one `CampaignRun` exists afterwards, keyed by `source_identifier` equal to the record's portal URL.
    - `source` is `'lco_queue'` for a record with `facility='LCO'` and `'soar_queue'` for `facility='SOAR'`.
    - Exactly one `CampaignRunObservation` row exists per synced record, with `run` set to that record's run, `confirmed_by` unset, and `confirmed_at` set.
    - A second identical sync leaves both `CampaignRun.objects.count()` and `CampaignRunObservation.objects.count()` unchanged and reports every record `unchanged`.
    - `window_start` and `window_end` equal the UTC dates of the record's derived start and end times.
    - An LCO record whose telescope label yields a class (`2m0`/`1m0`/`0m4`) gets that `telescope_class`, `site` unset, and `site_raw` carrying the derived telescope label; its run takes the reconciler's container branch via the `telescope_class` condition.
    - A SOAR record, for which no class value exists (D-12 keeps `4m0` out of `CampaignRun.TelescopeClass`), gets `site` resolved to the `Observatory` whose obscode is `I33` and `telescope_class` left blank; its run ALSO takes the container branch, but via plan 32-01's `observation_links.count() == 1` condition, since this command's own `observation_record=record` argument creates that single link before the helper's `reconcile_run()` call runs.
    - Both an LCO run and a SOAR run therefore own exactly ONE `CalendarEvent` per synced record (via `campaign_reconciler.owned_events(run)`), never one-per-night — the shape difference between the two facilities is which dispatch condition routes them there, not the calendar shape itself.
    - A synced record's projected event's `start_time`/`end_time` equal `record.scheduled_start`/`scheduled_end` whenever both are set on the `ObservationRecord`, for BOTH facilities — this is `_linked_observation_window()` (32-01) doing its job, not new code in this command.
    - When the `I33` `Observatory` row does not exist, the run is still written with `site=None` and `site_needs_review=True`, and the record is reported as not projected with the reconciler's own reason — never counted as a success.
    - `site_needs_review` is `True` only when no telescope class could be derived and no site resolved.
    - `approval_status` is `'approved'` on every written run.
    - A record whose run cannot be projected (the reconciler returns a `skipped_reason`) is reported on stderr with that reason and counted in a dedicated `not_projected` counter, per facility.
    - `CalendarEventMeta.is_verified` is `False` for a record whose live telescope-API lookup failed and `True` otherwise, on the event the reconciler wrote.
    - A record that fails instrument extraction or field building is still counted in its existing `extraction_failed`/`skipped` counter and writes no `CampaignRun`.
  </behavior>
  <action>
    Replace the command's direct calendar write with the shared helper, in one commit.
    <!-- planner-discipline-allow: insert_or_create_calendar_event -->

    Imports: drop `insert_or_create_calendar_event` from the `solsys_code.calendar_utils`
    import block, keeping every other name in it. Add `derive_telescope_class` to that same
    block. Add `from solsys_code.campaign_utils import write_and_reconcile_campaign_run`,
    `from solsys_code.campaign_reconciler import owned_events`, `from solsys_code.models
    import CampaignRun` (keep the existing `CalendarEventMeta` import), and
    `from tom_calendar.models import CalendarEvent` for the read-only adopt lookup.

    Add two module-level mappings next to `_FAILURE_PREFIX_BY_STATUS`:
    - `_SOURCE_BY_FACILITY = {'LCO': CampaignRun.Source.LCO_QUEUE, 'SOAR':
      CampaignRun.Source.SOAR_QUEUE}` — keyed identically to the existing `facilities`
      dispatch dict at line 289, so `record.facility` drives both the facility instance and
      the source value from the same value with no new dispatch mechanism.
    - `_RUN_STATUS_BY_OBSERVATION_STATUS = {'CANCELED': CampaignRun.RunStatus.CANCELLED,
      'WINDOW_EXPIRED': CampaignRun.RunStatus.NOT_AWARDED, 'FAILURE_LIMIT_REACHED':
      CampaignRun.RunStatus.WEATHER_TECH_FAILURE, 'NOT_ATTEMPTED':
      CampaignRun.RunStatus.WEATHER_TECH_FAILURE, 'COMPLETED':
      CampaignRun.RunStatus.OBSERVED}` with a comment stating it is a hand-typed snapshot of
      the same four failure states `_FAILURE_PREFIX_BY_STATUS` snapshots plus `COMPLETED`,
      and must be updated alongside it. Records whose status is absent from the mapping get
      `REQUESTED` when `record.scheduled_start is None` (still queued) and `PLANNED`
      otherwise.

    Add `'not_projected'` to `_COUNTER_KEYS` so every facility's counter dict carries it
    automatically, and add it to the per-facility summary line in the same phrasing as the
    existing six counts.

    Naming, so the two dicts in this loop never get confused: `event_fields` is the existing
    calendar-event dict `_build_event_fields()` returns, and `run_fields` is the new
    `CampaignRun` dict built below. Rename the existing local from `fields` to `event_fields`
    at its assignment and at both `pop()` sites; every reference below uses one name or the
    other explicitly, never a bare `fields`.

    In the per-record loop, keep `_build_event_fields(record, facility)` and both existing
    `event_fields.pop()` calls and every existing skip/counter branch exactly as they are —
    they still produce the derived telescope label, instrument, title, description, times and
    proposal. Note that `url = event_fields.pop('url')` (today at line 329) is where `url` is
    bound: it is the record's portal URL from `facility.get_observation_url()`, and it is the
    single value used below as BOTH the adopt lookup key and the run's `source_identifier`,
    exactly as the retired `insert_or_create_calendar_event({'url': url}, …)` call used it as
    the calendar lookup key. Do not re-derive it. Then, in place of the retired calendar
    write:
    - Build `telescope_instrument = f'{event_fields["telescope"]}/{event_fields["instrument"]}'`.
    - Derive `telescope_class = derive_telescope_class('', telescope_instrument)` and set
      `site_raw = event_fields['telescope']`. For an LCO record this yields `2m0`/`1m0`/`0m4`, so
      set `site=None`: a class-wide queue allocation is exactly what `CampaignRun`'s own
      field comment describes, with per-site detail living on the linked
      `ObservationRecord`s through `CampaignRunObservation` (CANON-04) rather than on the
      run — which is precisely the link this command now creates. This run reaches the
      calendar's container branch through the `telescope_class` condition, unaffected by
      anything below.
    - For a SOAR record `derive_telescope_class` returns `''` by design: D-12 keeps `4m0`
      out of `CampaignRun.TelescopeClass` and the function carries an explicit "do not fix
      this by adding 4m0 to the model" comment. A run with a blank class and no site is
      rejected by `_skip_reason()` as `'unresolved site'` and never reaches the calendar, so
      resolve a real site instead: add a module-level `_OBSCODE_BY_FACILITY = {'SOAR':
      'I33'}` constant (Cerro Pachón; verify the value against the `Observatory` table
      before relying on it, and against the MPC Observatory Codes list if the row is
      absent) and set
      `site = Observatory.objects.filter(obscode=_OBSCODE_BY_FACILITY[record.facility]).first()`
      when the facility has an entry and no class was derived. Import `Observatory` from
      `solsys_code.solsys_code_observatory.models`. Comment the constant with the reason it
      exists — a 4m aperture has no class vocabulary, so this SOAR record needs a resolved
      site purely to satisfy `_skip_reason()`'s gate. Its calendar SHAPE (one narrowing
      event, not per-night) comes from plan 32-01's `observation_links.count() == 1`
      dispatch condition below, once this task's own `observation_record=record` argument
      creates that link — say so in the comment so a later reader does not conflate "why
      does this record need a site" with "why does this record's event narrow".
    - Set `site_needs_review = not telescope_class and site is None`. A record left with
      neither is written anyway and reported through the `not_projected` path below, so it
      projects on the next sync once an operator creates the missing `Observatory` row —
      and, because its `CampaignRunObservation` link was already created on the FIRST sync
      attempt (the write happens even when the reconciler later skips projection), that next
      successful sync routes it straight to the container branch, with no transitional
      per-night state to pass through.
    - Look up the pre-cutover event read-only with
      `existing_event = CalendarEvent.objects.filter(url=url).first()` — `url` being the
      portal URL popped from `event_fields` above — and pass it as `adopt_event=` so the
      reconciler's container adopt step re-keys it instead of minting a second event. This
      adopt step is dispatch-agnostic (32-01 Task 3): it behaves identically whether the run
      reached the container branch via `telescope_class` (LCO) or via the single-link
      condition (SOAR).
    - Build the run dict as `run_fields`, distinct from `event_fields`:
      `campaign` = `event_fields['target_list']` (already computed by `_build_event_fields` as
      the record target's alphabetically-first `TargetList`, and may be `None`); `target` =
      `record.target`; `telescope_instrument`; `site`; `site_raw`; `site_needs_review`;
      `telescope_class`; `window_start` = `event_fields['start_time'].date()`; `window_end` =
      `event_fields['end_time'].date()`; `observation_details` = `event_fields['description']`
      (this text embeds the literal start/end time strings, which is what keeps a
      same-night reschedule from being masked as `unchanged` by
      `insert_or_create_campaign_run()`'s field-diff loop even though `window_start`/
      `window_end` alone would not change — see Task 2's
      `test_reschedule_within_same_night_updates_run`); `source` =
      `_SOURCE_BY_FACILITY[record.facility]`; `approval_status` =
      `CampaignRun.ApprovalStatus.APPROVED` (required — without it `_skip_reason()` returns
      `'not approved'` and the record never reaches the calendar); `run_status` from the
      mapping above.
    - Call `write_and_reconcile_campaign_run({'source_identifier': url}, run_fields,
      observation_record=record, adopt_event=existing_event)`.
      `observation_record=record` is what makes the `CampaignRunObservation` link exact
      identity rather than a scored guess — this command writes exactly one run per record,
      so there is no candidate ambiguity for the attribution scorer to resolve — AND it is
      what makes plan 32-01's precision-narrowing dispatch condition fire correctly for a
      SOAR record's very first sync, since the helper creates the link before calling
      `reconcile_run()`. Say both things in a comment at the call site, so a future reader
      does not "fix" the link by routing it through Phase 28's confirmation queue, and does
      not treat the resulting narrowing as accidental.
    - Count the returned `action` into the existing created/updated/unchanged counters, and
      when `reconcile.skipped_reason` is not `None`, write the observation id and the reason
      to stderr and increment `not_projected`.
    - Preserve the DISPLAY-01 signal: replace the existing per-event
      `CalendarEventMeta.objects.update_or_create(...)` call with
      `CalendarEventMeta.objects.filter(event__in=owned_events(run)).update(is_verified=not
      telescope_api_failed)`, run after the helper returns so the reconciler has already
      created or adopted the event and `_link_event_to_run()` has already created its
      companion row. `owned_events()` matches both the container `RUN:{pk}` key and the
      per-night `RUN:{pk}:{date}` form, so this scoping is correct regardless of which
      dispatch condition routed the run. Keep the existing comment's intent: the signal is
      always reconciled to the current sync's outcome, regardless of whether any field
      changed.

    Update the `Command` class docstring, `help` string and `handle()` docstring: the
    command now creates or updates a `CampaignRun` per record, links the realising
    `ObservationRecord` automatically, and the calendar entry appears because the reconciler
    projected it — one entry per record for both facilities, narrowing to the record's own
    scheduled/observed times as they become known.
  </action>
  <verify>
    <automated>python manage.py test solsys_code.tests.test_sync_lco_observation_calendar</automated>
    <fails_when>non-zero exit, or the output contains "FAILED" or "ERROR:"</fails_when>
    <automated>grep -v '^ *#' solsys_code/management/commands/sync_lco_observation_calendar.py | grep -c 'insert_or_create_calendar_event'</automated>
    <fails_when>output is anything other than "0" (a direct calendar write survives in the command)</fails_when>
    <automated>grep -v '^ *#' solsys_code/management/commands/sync_lco_observation_calendar.py | grep -c 'SOAR_QUEUE'</automated>
    <fails_when>output is "0" (SOAR records are not branched to their own source value)</fails_when>
    <automated>python manage.py check</automated>
    <fails_when>non-zero exit, or the output contains "ImportError" or "System check identified" followed by a non-zero issue count</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -v '^ *#' solsys_code/management/commands/sync_lco_observation_calendar.py | grep -c 'insert_or_create_calendar_event'` returns `0`.
    - `_SOURCE_BY_FACILITY` exists at module level with exactly the keys `'LCO'` and `'SOAR'`.
    - `_RUN_STATUS_BY_OBSERVATION_STATUS` exists and covers every key of `_FAILURE_PREFIX_BY_STATUS` plus `'COMPLETED'`.
    - `'not_projected'` is a member of `_COUNTER_KEYS` and appears in the summary line format string.
    - `grep -v '^ *#' solsys_code/management/commands/sync_lco_observation_calendar.py | grep -c 'ApprovalStatus.APPROVED'` returns at least `1` — every run this command writes is approved, or `_skip_reason()` returns `'not approved'` and nothing reaches the calendar.
    - The two dicts are named apart: `grep -v '^ *#' solsys_code/management/commands/sync_lco_observation_calendar.py | grep -c 'run_fields'` returns at least `1`, and no bare `fields` local survives in `handle()` (`grep -nE '^ *fields[ .=\[]' solsys_code/management/commands/sync_lco_observation_calendar.py` returns nothing).
    - `url` is bound once, by the existing `event_fields.pop('url')`, and is passed both as the `adopt_event` lookup filter and as the `source_identifier` value — it is never recomputed at the call site.
    - `observation_record=record` is passed at the `write_and_reconcile_campaign_run` call site.
    - `owned_events` is imported from `solsys_code.campaign_reconciler` and used to scope the `is_verified` update.
    - `python manage.py test solsys_code.tests.test_sync_lco_observation_calendar` passes.
  </acceptance_criteria>
  <done>The command writes one `CampaignRun` per `ObservationRecord` through the shared helper with the correct per-facility source value, links the realising record by exact identity so plan 32-01's dispatch condition can route a SOAR record to the container branch, contains no direct `CalendarEvent` write, and still reports the telescope-label verification signal.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Linking, SOAR-source, precision-narrowing, no-churn and cutover tests for the queue adapter</name>
  <files>solsys_code/tests/test_sync_lco_observation_calendar.py</files>
  <read_first>
    - solsys_code/tests/test_sync_lco_observation_calendar.py (the whole file — the `NonSiderealTargetFactory` import at line 16, `test_sync_04_rerun_updates_in_place_no_churn_on_unchanged` at 379-421, and `test_select_05_soar_record_uses_soar_facility_instance` at 608)
    - solsys_code/management/commands/sync_lco_observation_calendar.py (as rewired in Task 1)
    - solsys_code/tests/test_campaign_run_observation.py (existing CampaignRunObservation assertions and fixture conventions)
    - solsys_code/campaign_reconciler.py (_reconcile_container's adopt step and `_linked_observation_window()`, both landed by plan 32-01 — the paths these tests exercise through the real command)
  </read_first>
  <behavior>
    - `test_campaignrun_created_per_record`: one synced record yields exactly one `CampaignRun` whose `source_identifier` equals the record's portal URL, and whose `approval_status` is `CampaignRun.ApprovalStatus.APPROVED`. Assert the `approval_status` field on every run the test writes, not just the count — an unapproved run is written but silently never projected, so the count alone would still pass.
    - `test_observation_link_created_with_no_confirming_user`: exactly one `CampaignRunObservation` exists for the record, with `confirmed_by` unset and `confirmed_at` set.
    - `test_soar_record_uses_soar_queue_source`: extend the existing `test_select_05_soar_record_uses_soar_facility_instance` fixture so a `facility='SOAR'` record produces `source='soar_queue'`.
    - `test_lco_record_uses_lco_queue_source`: a `facility='LCO'` record produces `source='lco_queue'`.
    - `test_no_soar_record_written_under_lco_queue`: after a mixed LCO/SOAR sync, no run whose linked record has `facility='SOAR'` has `source='lco_queue'`.
    - `test_rerun_no_churn_campaignrun_and_link`: a second identical sync leaves both `CampaignRun.objects.count()` and `CampaignRunObservation.objects.count()` unchanged and reports every record `unchanged`.
    - `test_empty_proposal_selection_writes_nothing`: a `--proposal` value matching no record yields zero `CampaignRun` and zero `CalendarEvent` rows and exits 0.
    - `test_cutover_does_not_duplicate_existing_event`: pre-create a `CalendarEvent` keyed on the record's portal URL exactly as the pre-cutover command would have, then sync once; assert `CalendarEvent.objects.count()` is unchanged, the event's `url` is now `RUN:{run.pk}`, and its `CalendarEventMeta.run` points at the new run.
    - `test_soar_record_projects_via_resolved_site`: with an `Observatory` fixture carrying obscode `I33`, a SOAR record's run has `site` set, `telescope_class` blank, and produces at least one `CalendarEvent`.
    - `test_soar_and_lco_both_own_exactly_one_narrowing_event`: the cross-plan contract 32-03 Task 3's runbook wording depends on. Sync one LCO record and one SOAR record whose windows each span the SAME three nights, then assert BOTH runs own exactly ONE `CalendarEvent` each, scoped with `campaign_reconciler.owned_events(run).count() == 1` for each — the LCO run via the `telescope_class` dispatch condition, the SOAR run via plan 32-01's `observation_links.count() == 1` condition. This is the assertion that keeps the runbook honest — if a future change routes SOAR back to the per-night branch, this test fails before the docs go stale.
    - `test_soar_event_narrows_with_scheduled_times`: give the synced SOAR record's `ObservationRecord` a `scheduled_start`/`scheduled_end` inside its request window, sync, and assert the projected `CalendarEvent.start_time`/`end_time` equal those exact values, not the run's whole-window default — the reconciler's precision-narrowing capability (32-01) applies to SOAR, proving no calendar-fidelity regression exists for either facility at cutover.
    - `test_reschedule_within_same_night_updates_run`: sync a record once, then change only the time-of-day portion of its `parameters['start']`/`['end']` (same UTC date) and sync again; assert the run reports `updated`, not `unchanged` — `window_start`/`window_end` alone would look unchanged to the diff loop, but `observation_details` embeds the new time strings, so the run (and, through it, the projected event) is correctly refreshed rather than silently masked.
    - `test_unprojectable_record_reported_not_counted_as_success`: a SOAR record with no `I33` `Observatory` row is reported on stderr with the reconciler's `'unresolved site'` reason and increments `not_projected`, and is not counted as created/updated/unchanged.
    - `test_missing_observatory_is_self_healing`: after creating the missing `I33` `Observatory` row, a second sync of the same record projects it via the container branch (its `CampaignRunObservation` link was already created on the first, unprojected sync) — the run written by the first sync is reused, not duplicated, and owns exactly one event, not a transitional per-night set.
    - `test_is_verified_signal_survives_cutover`: a record whose live telescope-API lookup failed leaves `CalendarEventMeta.is_verified` `False` on the projected event; a successful one leaves it `True`.
    - `test_identical_windows_do_not_merge_records`: the backstop case — two records with identical start and end times produce two distinct `CampaignRun` rows, because each record's portal URL is its own `source_identifier`.
  </behavior>
  <action>
    Extend `solsys_code/tests/test_sync_lco_observation_calendar.py` with the fourteen cases in
    `<behavior>`, mirroring the existing no-churn test's fixture and mocking style rather
    than inventing a new one — the existing tests already stub the facility portal
    read-back, and those stubs are what the new tests should reuse.

    Keep every existing test passing. Where an existing assertion depends on the old title
    format or on the command writing a `CalendarEvent` directly, update it to the
    reconciler-owned form and add a comment naming Phase 32 as where the behaviour moved, so
    the change is visible rather than silently absorbed. In particular the title prefixes
    `[QUEUED]`, `[EXPIRED]`, `[FAILED]` and `[UNVERIFIED]` no longer reach the calendar —
    the reconciler's title vocabulary is `[CANCELLED]`/`[WEATHERED]` only, and Phase 35 is
    where the richer vocabulary returns. Any existing test asserting one of those prefixes
    on a `CalendarEvent` must be rewritten to assert the corresponding `run_status` on the
    `CampaignRun` instead, with a comment saying so. This vocabulary narrowing is the ONLY
    fidelity change this cutover ships — do not conflate it with the time-span precision,
    which `test_soar_event_narrows_with_scheduled_times` proves is fully preserved.

    Build the `test_cutover_does_not_duplicate_existing_event` fixture by creating the
    `CalendarEvent` directly with the field values the retired write produced (url, title,
    description, start_time, end_time, telescope, instrument, proposal, target_list) — do
    not sync twice, because the first sync would already create the new-shape row and the
    test would prove nothing.

    Use `tom_targets.tests.factories.NonSiderealTargetFactory` for every `Target` fixture,
    never `SiderealTargetFactory`.
  </action>
  <verify>
    <automated>python manage.py test solsys_code.tests.test_sync_lco_observation_calendar solsys_code.tests.test_campaign_run_observation</automated>
    <fails_when>non-zero exit, or the output contains "FAILED" or "ERROR:"</fails_when>
    <automated>python manage.py test solsys_code.tests.test_campaign_attribution solsys_code.tests.test_campaign_attribution_views solsys_code.tests.test_calendar_display_extras solsys_code.tests.test_campaign_reconciler</automated>
    <fails_when>non-zero exit, or the output contains "FAILED" or "ERROR:"</fails_when>
  </verify>
  <acceptance_criteria>
    - All fourteen test methods named in `<behavior>` exist in `solsys_code/tests/test_sync_lco_observation_calendar.py`.
    - `test_cutover_does_not_duplicate_existing_event` asserts `CalendarEvent.objects.count()` both before and after the sync and that the two are equal.
    - `test_observation_link_created_with_no_confirming_user` asserts `confirmed_by is None` and `confirmed_at is not None`.
    - `test_campaignrun_created_per_record` asserts `run.approval_status == CampaignRun.ApprovalStatus.APPROVED`.
    - `test_soar_and_lco_both_own_exactly_one_narrowing_event` asserts an exact event count of `1` for BOTH the LCO run and the SOAR run over the same three-night window, each scoped with `owned_events(run)`.
    - `test_soar_event_narrows_with_scheduled_times` asserts the projected event's `start_time`/`end_time` equal the record's `scheduled_start`/`scheduled_end`, not the run's whole-window default.
    - `grep -o 'SiderealTargetFactory' solsys_code/tests/test_sync_lco_observation_calendar.py | wc -l` equals `grep -o 'NonSiderealTargetFactory' solsys_code/tests/test_sync_lco_observation_calendar.py | wc -l`.
    - Every pre-existing test in the file still passes; any assertion rewritten for the reconciler-owned title vocabulary carries a comment naming Phase 32.
    - `python manage.py test solsys_code.tests.test_sync_lco_observation_calendar` passes with zero failures and zero errors.
  </acceptance_criteria>
  <done>The queue adapter's exact-identity linking, per-facility source branching, precision-narrowing, idempotency, empty-selection and cutover behaviour are all proven against the new `CampaignRun` write path, and LCO and SOAR are proven to reach the calendar with the same one-event-per-record, narrowing shape.</done>
</task>

<task type="auto">
  <name>Task 3: Paired docs — LCO/SOAR demo notebook and runbook</name>
  <files>docs/notebooks/pre_executed/sync_lco_observation_calendar_demo.ipynb, docs/runbooks/telescope_runs_calendar.rst</files>
  <read_first>
    - docs/notebooks/pre_executed/sync_lco_observation_calendar_demo.ipynb (current cells, fixture setup and narrative)
    - docs/runbooks/telescope_runs_calendar.rst (the section documenting `sync_lco_observation_calendar`, plus the shared reconciler-behaviour prose plan 32-01 added, and the section plan 32-02 updated for the classical command)
    - solsys_code/management/commands/sync_lco_observation_calendar.py (as rewired in Task 1)
    - CLAUDE.md ("Paired docs are part of the deliverable")
  </read_first>
  <action>
    Update both paired artifacts in this plan, not as a follow-up — the command's documented
    effect has changed for both facilities.

    `docs/notebooks/pre_executed/sync_lco_observation_calendar_demo.ipynb`: keep the existing
    narrative arc and add cells that print, with real executed output, (a) the `CampaignRun`
    rows the command now creates for both an LCO record and a SOAR record, showing `source`
    (`lco_queue` versus `soar_queue`), `source_identifier`, `telescope_class`,
    `window_start`, `window_end` and `approval_status`; (b) the matching
    `CampaignRunObservation` rows, showing `confirmed_by` unset; (c) the `CalendarEvent`
    rows the reconciler projected, showing the `RUN:{pk}` url form for BOTH facilities and
    `owned_events(run).count() == 1` for each; (d) a cell narrowing a SOAR record's event by
    setting `scheduled_start`/`scheduled_end` on its `ObservationRecord` and re-syncing,
    printing the event's `start_time`/`end_time` before and after; and (e) a second identical
    sync reporting every record `unchanged`, with `CampaignRun.objects.count()` and
    `CalendarEvent.objects.count()` printed before and after. Update the prose so it no
    longer describes the command as writing calendar events itself, and so it does NOT
    describe SOAR as producing one event per night — both facilities produce one narrowing
    event per record. Use `NonSiderealTargetFactory` for any `Target` fixture. Regenerate
    with
    `jupyter nbconvert --to notebook --execute --inplace docs/notebooks/pre_executed/sync_lco_observation_calendar_demo.ipynb`
    and commit the executed output.

    `docs/runbooks/telescope_runs_calendar.rst`, `sync_lco_observation_calendar` section:
    state that the command now creates or updates one `CampaignRun` per synced
    `ObservationRecord` and that the calendar entry appears because the reconciler projected
    it; that the record's portal URL is the run's `source_identifier`, which is what makes a
    re-sync update rather than duplicate; that SOAR-sourced records are written under a
    dedicated `soar_queue` source value, distinct from `lco_queue`, for the same reason
    `eso_queue` was added rather than folded into `lco_queue`; and that the realising
    `ObservationRecord` is now linked automatically at creation time by exact identity, so
    an operator no longer has to attribute an LCO or SOAR record by hand through the
    attribution queue.
    Record the shape an operator will see, matching what plan 32-01's checkpoint approved:
    both an LCO and a SOAR record's run own exactly ONE calendar entry — an LCO record's run
    reaches the calendar's container branch because it derives a telescope class
    (`2m0`/`1m0`/`0m4`); a SOAR record's run reaches the SAME container branch because it has
    exactly one exact-identity `CampaignRunObservation` link (a SOAR record needs a resolved
    `Observatory` site instead of a telescope class only to satisfy the reconciler's
    approval/site gate, not because its calendar shape differs). That one entry starts life
    spanning the record's whole request window and then narrows automatically — with no
    staff action — to the portal-scheduled block and then the actually-observed block, as
    `record.scheduled_start`/`scheduled_end` become known, for BOTH facilities: this is the
    SAME precision the pre-cutover direct-write code already provided, preserved rather than
    lost. State plainly that this is unlike the classical adapter's per-night events (plan
    32-02) and Gemini's per-night events (plan 32-04), which have no `ObservationRecord`
    link to narrow against.
    The one real fidelity change at cutover is title-vocabulary, not time-span: the
    `[QUEUED]`, `[EXPIRED]`, `[FAILED]` and `[UNVERIFIED]` title prefixes no longer appear —
    the reconciler's title vocabulary is `[CANCELLED]` and `[WEATHERED]` only, and the finer
    distinctions are carried on the run's `run_status` field until Phase 35 unifies the
    status vocabulary. Name Phase 35 explicitly so the narrowing reads as a scheduled step
    rather than a regression.
    Also state that a SOAR record reaches the calendar through a resolved `Observatory`
    rather than a telescope class, because `CampaignRun.TelescopeClass` has no 4m value by
    design (D-12), and name the obscode the operator must have in the `Observatory` table
    (`I33`, Cerro Pachón) for SOAR records to project. Say that a record with neither a
    class nor a site is reported as not projected and stays off the calendar until the
    `Observatory` row exists, that its run and its `CampaignRunObservation` link are still
    written on that first attempt, and that the next sync therefore projects it straight to
    its narrowing container-branch entry with no transitional per-night state and no manual
    repair — one command re-run is the whole fix.
  </action>
  <verify>
    <automated>jupyter nbconvert --to notebook --execute --inplace docs/notebooks/pre_executed/sync_lco_observation_calendar_demo.ipynb</automated>
    <fails_when>non-zero exit, or the output contains "CellExecutionError"</fails_when>
    <automated>grep -c 'soar_queue' docs/runbooks/telescope_runs_calendar.rst</automated>
    <fails_when>output is "0" (the runbook does not document the new SOAR source value)</fails_when>
    <automated>grep -c 'Phase 35' docs/runbooks/telescope_runs_calendar.rst</automated>
    <fails_when>output is "0" (the title-vocabulary narrowing is recorded with no named follow-up)</fails_when>
    <automated>grep -c '568' docs/runbooks/telescope_runs_calendar.rst</automated>
    <fails_when>output is anything other than "0" (a stray, incorrect Gemini-North obscode reference has leaked into this plan's runbook edit — Gemini North is T15, never 568, and this plan does not even need to mention Gemini's obscode)</fails_when>
    <automated>grep -ci 'one event per record\|owns exactly one' docs/runbooks/telescope_runs_calendar.rst</automated>
    <fails_when>output is "0" (the runbook does not state that both LCO and SOAR own exactly one narrowing calendar entry, not one-per-night)</fails_when>
    <automated>python -c "import json,sys; nb=json.load(open('docs/notebooks/pre_executed/sync_lco_observation_calendar_demo.ipynb')); sys.exit(0 if any(c.get('outputs') for c in nb['cells'] if c['cell_type']=='code') else 1)"</automated>
    <fails_when>non-zero exit (the committed notebook has no executed output in any code cell)</fails_when>
    <automated>pre-commit run ruff --all-files &amp;&amp; pre-commit run ruff-format --all-files</automated>
    <fails_when>non-zero exit, or the output contains "Failed"</fails_when>
  </verify>
  <acceptance_criteria>
    - The notebook contains executed cells showing an LCO run with `source='lco_queue'` and a SOAR run with `source='soar_queue'`, plus their `CampaignRunObservation` links, and both runs' `owned_events(run).count() == 1`.
    - The notebook contains a cell showing a SOAR record's event narrowing once `scheduled_start`/`scheduled_end` are set, and a before/after `CalendarEvent.objects.count()` for a second identical sync.
    - `docs/runbooks/telescope_runs_calendar.rst` names `soar_queue`, the portal-URL identity key, the automatic exact-identity link, the title-prefix narrowing, and Phase 35.
    - The runbook states that BOTH an LCO run and a SOAR run own exactly one calendar entry that narrows automatically to the record's scheduled/observed times — it contains no claim, blanket or otherwise, that a SOAR record produces one entry per night, and no occurrence of `568` anywhere.
    - The runbook names `I33` as the obscode SOAR records need and explains that the resolved site satisfies the reconciler's approval/site gate, not the calendar shape.
    - `pre-commit run ruff --all-files` and `pre-commit run ruff-format --all-files` are both clean.
  </acceptance_criteria>
  <done>The queue demo notebook shows both facilities' new write paths with real executed output, including the precision-narrowing behaviour, and the runbook documents what an operator will now see — one narrowing entry per record for both facilities, with only the title-prefix vocabulary narrowed and Phase 35 named as where it returns.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LCO/SOAR portal API -> `_build_event_fields` | Remote facility read-back; already an existing trust boundary with its own failure handling, unchanged by this plan |
| `ObservationRecord.parameters` (JSON) -> CampaignRun fields | Already-validated facility data becomes run field text and, through the reconciler, shared calendar content |
| command -> CampaignRunObservation rows | Batch code now writes attribution links that previously only a staff confirmation produced |
| CampaignRunObservation link -> reconciler dispatch/precision (32-01) | This command is what supplies the link plan 32-01's reconciler reads for both branch selection and calendar time precision — a defect here (e.g. linking the wrong record) now has a calendar-visible consequence, not just an attribution-table one |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-10 | Tampering | An adapter-written `CampaignRunObservation` is mistaken for a staff-confirmed attribution, weakening ATTRIB-03's "no association without explicit staff confirmation" | medium | mitigate | The link is structural exact identity, not the scorer's output: this command writes exactly one run per record, so no candidate ambiguity exists. `confirmed_by=None` distinguishes it from a human confirmation, and `CampaignRun.source` records which ingest path made it. Documented at the call site and in the runbook |
| T-32-11 | Denial of Service | A duplicate calendar event minted at cutover for every already-synced record | high | mitigate | Task 1's read-only `url` lookup plus the `adopt_event=` bridge into the container adopt step landed by plan 32-01; asserted by `test_cutover_does_not_duplicate_existing_event`. The adopt step is dispatch-agnostic, so this holds whether the run reaches the container branch via `telescope_class` or via the single-observation-link condition |
| T-32-12 | Spoofing | A SOAR-sourced run misclassified under `lco_queue`, misattributing a facility's allocation | medium | mitigate | `_SOURCE_BY_FACILITY` is keyed identically to the existing facility dispatch dict so both read the same `record.facility` value; asserted by `test_no_soar_record_written_under_lco_queue` |
| T-32-13 | Repudiation | The DISPLAY-01 telescope-label verification signal is lost once the reconciler owns the event, so an unverified label renders as verified | medium | mitigate | Task 1 re-scopes the `is_verified` write through `owned_events(run)` after the helper returns; `owned_events()` matches both the container and per-night url forms, so this holds regardless of which branch a run takes; asserted by `test_is_verified_signal_survives_cutover` |
| T-32-14 | Information Disclosure | A caught exception's message, which can embed facility parameter content, is interpolated into a new stderr line | low | mitigate | The new `not_projected` stderr line interpolates only the observation id and the reconciler's own fixed skip-reason vocabulary, never a caught exception — matching the existing SYNC-09/D-11 rule already applied to the telescope-API failure message in this command |
| T-32-19 | Tampering | This command's `observation_record=record` argument is the sole supplier of the link plan 32-01's `_linked_observation_window()` reads for calendar precision — if this command ever linked the wrong record, or linked more than one record to a run, the calendar-visible time span would be wrong or (per 32-01's guard) silently fall back to the whole-window default | medium | mitigate | This command creates exactly one link per synced record, keyed on that record's own portal URL as the run's `source_identifier` — there is no code path in this command that could link a second record to the same run. The ambiguous-link guard itself lives in and is tested by plan 32-01 (T-32-19 there); this entry records that this command is the guard's sole producer for LCO/SOAR |
| T-32-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no packages; every module touched already exists with existing dependencies |
</threat_model>

## Artifacts this phase produces

- `solsys_code/management/commands/sync_lco_observation_calendar.py::_SOURCE_BY_FACILITY` (module-level mapping, keys `'LCO'`/`'SOAR'`)
- `solsys_code/management/commands/sync_lco_observation_calendar.py::_RUN_STATUS_BY_OBSERVATION_STATUS` (module-level mapping)
- `solsys_code/management/commands/sync_lco_observation_calendar.py::_OBSCODE_BY_FACILITY` (module-level mapping, `{'SOAR': 'I33'}`)
- `'not_projected'` added to `sync_lco_observation_calendar._COUNTER_KEYS`
- New test methods in `solsys_code/tests/test_sync_lco_observation_calendar.py`: `test_campaignrun_created_per_record`, `test_observation_link_created_with_no_confirming_user`, `test_soar_record_uses_soar_queue_source`, `test_lco_record_uses_lco_queue_source`, `test_no_soar_record_written_under_lco_queue`, `test_rerun_no_churn_campaignrun_and_link`, `test_empty_proposal_selection_writes_nothing`, `test_cutover_does_not_duplicate_existing_event`, `test_soar_record_projects_via_resolved_site`, `test_soar_and_lco_both_own_exactly_one_narrowing_event`, `test_soar_event_narrows_with_scheduled_times`, `test_reschedule_within_same_night_updates_run`, `test_unprojectable_record_reported_not_counted_as_success`, `test_missing_observatory_is_self_healing`, `test_is_verified_signal_survives_cutover`, `test_identical_windows_do_not_merge_records`

<verification>
- `python manage.py test solsys_code.tests.test_sync_lco_observation_calendar solsys_code.tests.test_campaign_run_observation solsys_code.tests.test_campaign_attribution solsys_code.tests.test_campaign_reconciler` passes.
- `grep -v '^ *#' solsys_code/management/commands/sync_lco_observation_calendar.py | grep -c 'insert_or_create_calendar_event'` returns `0`.
- `docs/notebooks/pre_executed/sync_lco_observation_calendar_demo.ipynb` re-executes cleanly and is committed with output.
- `grep -c '568' docs/runbooks/telescope_runs_calendar.rst` returns `0`.
- `pre-commit run ruff --all-files` and `pre-commit run ruff-format --all-files` are clean.
</verification>

<success_criteria>
Running `sync_lco_observation_calendar` creates or updates a `CampaignRun` per synced
observation, links the realising `ObservationRecord` automatically by exact identity, writes
SOAR-sourced records under a dedicated `soar_queue` source value, re-runs with no churn, and
both an LCO and a SOAR record's run own exactly one calendar entry that narrows automatically
to the record's own scheduled/observed times — proving no calendar-fidelity regression at
cutover for either facility.
</success_criteria>

<output>
Create `.planning/phases/32-adapter-consolidation/32-03-SUMMARY.md` when done.
</output>
