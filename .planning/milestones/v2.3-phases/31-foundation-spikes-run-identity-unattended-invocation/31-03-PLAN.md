---
phase: 31-foundation-spikes-run-identity-unattended-invocation
plan: 03
type: execute
wave: 3
depends_on: ["31-02"]
files_modified:
  - .planning/phases/31-foundation-spikes-run-identity-unattended-invocation/31-DECISION.md
  - tmp/31-classical-samples/
autonomous: false
requirements: [SCHEMA-03]

estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "31-DECISION.md answers explicitly whether the classical adapter's 5-minute telescope/instrument/start_time tolerance match is a sufficient write-time identity surface on its own or needs a facility-specific key (roadmap Success Criterion 3)"
    - "If the answer is that it needs a facility-specific key, 31-DECISION.md names the concrete failing case that forces one"
    - "31-DECISION.md states how many real classical schedule files were obtained from the operator and inspected, and if the number is zero it says so in those words rather than reasoning from the three illustrative lines in docs/design/telescope_runs_calendar.rst"
    - "31-DECISION.md records, for every classical run state actually observed in whatever samples were available, whether a proposal code was present in that state (D-07)"
    - statement: "31-DECISION.md reconciles D-07's remembered status words against the parser's own KNOWN_STATUSES set, and states which vocabulary the remembered words belong to"
      verification: backstop
  artifacts:
    - path: ".planning/phases/31-foundation-spikes-run-identity-unattended-invocation/31-DECISION.md"
      provides: "The SCHEMA-03 evidence section and the SCHEMA-03 recommendation"
      contains: "### SCHEMA-03"
  key_links:
    - from: ".planning/phases/31-foundation-spikes-run-identity-unattended-invocation/31-DECISION.md"
      to: "solsys_code/telescope_runs.py"
      via: "the SCHEMA-03 finding is argued against the parser's real status vocabulary and ParsedRun field list"
      pattern: "KNOWN_STATUSES"
  prohibitions:
    - statement: "MUST NOT treat the three illustrative schedule lines in docs/design/telescope_runs_calendar.rst as a representative sample of real classical schedule files; if no real operator-supplied file was obtained, the document must say so in those words"
      status: unverified
      verification: judgment
---

<objective>
Settle whether the classical ingest path's existing tolerance-windowed match is a sufficient
write-time identity surface on its own, or needs a facility-specific key - and answer it against
real classical schedule files rather than the three illustrative lines that exist in this repo's
own documentation.

Purpose: RESEARCH.md's Open Question 1 establishes that no real classical schedule file exists
anywhere in this repository. The only classical-schedule content in the tree is three example
lines inside `docs/design/telescope_runs_calendar.rst`, none of which carries a proposal code, and
the parser has no proposal-code field at all. D-07 records the operator's recollection that a
proposal code may appear for some run states but not others, and explicitly forbids assuming the
tolerance match is either the ceiling or obsolete without checking. Only the operator can supply
the file that closes this.

Output: a `#### SCHEMA-03 evidence` subsection and a `### SCHEMA-03` recommendation subsection in
`31-DECISION.md`.
</objective>

<execution_context>
@/home/tlister/git/fomo_devel/.claude/gsd-core/workflows/execute-plan.md
@/home/tlister/git/fomo_devel/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-foundation-spikes-run-identity-unattended-invocation/31-CONTEXT.md
@.planning/phases/31-foundation-spikes-run-identity-unattended-invocation/31-RESEARCH.md
@.planning/phases/31-foundation-spikes-run-identity-unattended-invocation/31-PATTERNS.md
@.planning/phases/31-foundation-spikes-run-identity-unattended-invocation/31-02-SUMMARY.md
</context>

## Artifacts this phase produces

See the table in `31-01-PLAN.md` under the same heading. This plan creates
`tmp/31-classical-samples/` (git-excluded, holding whatever the operator supplies) and appends two
subsections to the `31-DECISION.md` that plan 31-01 created.

## Flagged assumptions (edge-probe, unresolved)

| Requirement | Probe category | Probe question | Assumption carried |
|---|---|---|---|
| SCHEMA-03 | unclassified | (unclassified - review manually) | Assumed not applicable as a data-shape edge: SCHEMA-03's deliverable is a documented sufficiency verdict, not an input/output behaviour. The substitute acceptance bar is the roadmap Success Criterion 3 truth in `must_haves` above (an explicit sufficient-or-not answer with the failing case named). Carried unresolved rather than dismissed. |

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Obtain a real classical schedule file from the operator, or record that none exists</name>
  <files>tmp/31-classical-samples/</files>
  <action>Supply one or more real classical schedule files, or confirm that none is obtainable</action>
  <instructions>
    I have already searched this repository for real classical schedule content and there is none:
    the only classical run lines anywhere in the tree are three illustrative examples inside
    `docs/design/telescope_runs_calendar.rst`, and none of them carries a proposal code. The parser
    (`solsys_code/telescope_runs.py`) has no proposal-code field, and its `KNOWN_STATUSES` set holds
    `allocation`, `proposed`, `confirmed`, `cancelled` and `not confirmed` - none of which are the
    words D-07 remembers ("planned", "observed"), so the remembered vocabulary may belong to a
    different system entirely.

    This is the one thing in Phase 31 that cannot be automated: only you have access to a real
    classical schedule. Please do one of:

    1. Drop one or more real (or representative historical) classical schedule files into
       `tmp/31-classical-samples/` - the directory is git-excluded, so nothing there will be
       committed. These should be the kind of file `python manage.py load_telescope_runs <filepath>`
       consumes: a text file of classical run lines, one per line. If a file carries PI names,
       proposal titles or contact details, that is fine - it stays in the git-excluded directory and
       only structural facts will be quoted into the committed document.
    2. Point me at a URL or a path outside this repository that I can read.
    3. Tell me no real file is obtainable right now.

    Option 3 is a legitimate answer. If you choose it, I will record "0 real classical schedule
    files were available" as an explicit dated finding, exactly as `26-DECISION.md` recorded its own
    zero-real-rows cases, and the SCHEMA-03 verdict will be labelled unconfirmed rather than being
    reasoned from the three documentation examples as if they were a sample.
  </instructions>
  <verification>I will count the files in `tmp/31-classical-samples/`, run each one's lines through `parse_run_line` to confirm they parse as classical run lines, and report the count and the distinct status words found before going further.</verification>
  <resume-signal>Type "done" once files are in place, paste a path or URL, or type "none available"</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Inspect whatever classical schedule samples are available and record what they carry per run state</name>
  <precondition>`tmp/31-classical-samples/` exists and either contains at least one file or the operator has explicitly stated no real file is obtainable; the previous checkpoint establishes one or the other.</precondition>
  <files>.planning/phases/31-foundation-spikes-run-identity-unattended-invocation/31-DECISION.md, tmp/31-classical-samples/</files>
  <read_first>
    - `.planning/phases/31-foundation-spikes-run-identity-unattended-invocation/31-DECISION.md` (append to it, never rewrite it)
    - `solsys_code/telescope_runs.py` lines 30-45 (`KNOWN_STATUSES`) and lines 300-400 (`ParsedRun`'s full field list and `parse_run_line`'s status-extraction logic, including the parenthesised and bare-word branches and the `ValueError` raised for an unrecognised status)
    - `solsys_code/management/commands/load_telescope_runs.py` lines 20-25 (`_START_TIME_MATCH_TOLERANCE`), lines 105-125 (the `filepath` and `--campaign` arguments) and lines 200-220 (the tolerance-match lookup dict)
    - `docs/design/telescope_runs_calendar.rst` lines 169-209 ("Classical Run Input Format" - the three illustrative lines and the documented parsing rules)
    - `.planning/phases/31-foundation-spikes-run-identity-unattended-invocation/31-RESEARCH.md` section "Open Questions" item 1
    - `.planning/milestones/v2.2-phases/26-canonical-record-spike/26-DECISION.md` lines 62-72 and 106-129 - the honest-gap framing to copy when real data is absent
  </read_first>
  <action>
    Insert a `#### SCHEMA-03 evidence - classical schedule-file sample inspection` subsection into
    `31-DECISION.md`, under the `## Findings` section immediately after the existing
    `#### SCHEMA-02 evidence` subsection and before the `## Recommendation` heading plan 31-02
    added. Do not append it at the end of the file: evidence lives under Findings and verdicts live
    under Recommendation, and plan 31-04 still has evidence subsections of its own to insert.

    Open the section with the count, stated plainly and dated: how many real classical schedule
    files were obtained, how many lines they contain in total, and where they came from. If the
    count is zero, state "0 real classical schedule files were available" in those words and say
    what was substituted, mirroring the honest-gap framing `26-DECISION.md` uses for its own
    zero-real-rows cases. Do not paper over a zero.

    For whatever samples exist, run each line through `parse_run_line` from
    `solsys_code.telescope_runs` in a read-only Django shell (this is a pure-function parse; it
    touches no database row and needs no disposable copy) and record: the distinct status words that
    actually occur, how many lines carry each, how many lines `parse_run_line` rejects with a
    `ValueError` for an unrecognised status, and - the question D-07 actually asks - for each
    distinct status word, whether any line in that state carries something that looks like a
    proposal code, with the token quoted structurally.

    Quote only structural facts into the committed document: the status word, the presence or
    absence of a proposal-code-shaped token, and the token's format if present. Do not quote PI
    names, proposal titles, contact details, or whole lines from an operator-supplied file - those
    stay in the git-excluded samples directory. Where a redacted example is genuinely needed, quote
    a synthetic line built to the same format instead and label it as synthetic.

    Reconcile D-07's remembered status words against the parser's real vocabulary: `KNOWN_STATUSES`
    contains neither "planned" nor "observed", while `CampaignRun.RunStatus` does contain `PLANNED`
    and `OBSERVED`. State which vocabulary the operator's recollection most likely refers to, and
    whether the samples support or contradict it. This is a real finding either way and belongs in
    the document.

    Record the count of `ParsedRun` fields available at write time and note that none of them is a
    proposal code today, so any facility-specific key would require a parser change in a later
    phase - state that as a consequence, not as work to do here.

    Tag the section: `Tag: **Confirmed against real rows**` only for facts read out of an actual
    operator-supplied file, `Tag: **Constructed-input code-path check**` for anything derived from
    the three documentation examples or from a synthetic line. If the count was zero, the whole
    section carries the constructed-input tag and says so.
  </action>
  <verify>
    <automated>mkdir -p tmp/31-classical-samples &amp;&amp; N=$(find tmp/31-classical-samples -type f | wc -l); echo "CLASSICAL_SAMPLE_FILES=$N"; D=.planning/phases/31-foundation-spikes-run-identity-unattended-invocation/31-DECISION.md; grep -q '^#### SCHEMA-03 evidence - classical schedule-file sample inspection' "$D" &amp;&amp; grep -qE "(0 real classical schedule files were available|CLASSICAL_SAMPLE_FILES=$N)" "$D"</automated>
    <fails_when>non-zero exit - the SCHEMA-03 evidence heading is absent, or the document records neither the explicit zero-files sentence nor a sample count matching the number of files actually present in `tmp/31-classical-samples/`. A section that quietly reasons from the documentation examples matches neither.</fails_when>
    <automated>D=.planning/phases/31-foundation-spikes-run-identity-unattended-invocation/31-DECISION.md; S=$(sed -n '/^#### SCHEMA-03 evidence/,/^#### /p' "$D"); printf '%s' "$S" | grep -q 'KNOWN_STATUSES' &amp;&amp; printf '%s' "$S" | grep -qE 'planned|PLANNED' &amp;&amp; printf '%s' "$S" | grep -qE 'Tag: \*\*(Confirmed against real rows|Constructed-input code-path check)\*\*'</automated>
    <fails_when>non-zero exit - the evidence section does not reference the parser's own status vocabulary, does not address D-07's remembered status word, or carries no confidence tag.</fails_when>
    <automated>D=.planning/phases/31-foundation-spikes-run-identity-unattended-invocation/31-DECISION.md; ! grep -REiq 'api[_-]?key[[:space:]]*[:=]|[0-9a-f]{32,}|[[:alnum:]._%+-]+@[[:alnum:].-]+\.[[:alpha:]]{2,}' "$D"</automated>
    <fails_when>non-zero exit - the committed decision document now contains an assignment-shaped credential token, a 32-or-more-character hex string, or an email address, meaning operator-supplied file content or a secret was quoted verbatim into a committed artifact.</fails_when>
  </verify>
  <acceptance_criteria>
    - `31-DECISION.md` contains a heading whose text is exactly `#### SCHEMA-03 evidence - classical schedule-file sample inspection`
    - The section's first paragraph states a file count and a line count, both as numbers, and a date
    - If the file count is zero, the section contains the sentence "0 real classical schedule files were available"
    - The section contains a per-status-word table with a column recording whether a proposal-code-shaped token was present
    - The section names `KNOWN_STATUSES` and states which status vocabulary D-07's remembered words belong to
    - The section contains no PI name, proposal title, email address or contact detail from any operator-supplied file
    - `git ls-files tmp/` prints nothing
    - `git status --porcelain -- solsys_code src` prints nothing
  </acceptance_criteria>
  <done>Whatever classical schedule evidence exists has been parsed and recorded per run state, the sample count is stated honestly including a zero, and no operator-supplied personal detail reached the committed document.</done>
</task>

<task type="auto">
  <name>Task 3: Write the SCHEMA-03 recommendation - is the tolerance match a sufficient write-time identity surface</name>
  <files>.planning/phases/31-foundation-spikes-run-identity-unattended-invocation/31-DECISION.md</files>
  <read_first>
    - `.planning/phases/31-foundation-spikes-run-identity-unattended-invocation/31-DECISION.md` - the SCHEMA-03 evidence section written by task 2, and the `### SCHEMA-02` recommendation written by plan 31-02 (the identity field the classical path must either fill or deliberately leave blank)
    - `solsys_code/management/commands/load_telescope_runs.py` lines 20-25 and 200-220 - the 5-minute tolerance and the three-field lookup dict, verbatim
    - `.planning/milestones/v2.2-phases/26-canonical-record-spike/26-DECISION.md` lines 835-859 - Phase 26's read-time per-adapter identity mapping, which recorded the classical path as tolerance-match with no single identity string
    - `.planning/phases/31-foundation-spikes-run-identity-unattended-invocation/31-RESEARCH.md` section "Common Pitfalls" Pitfall 3 - the two accommodations available (leave the field blank, or synthesize a deterministic key) and the warning against making the field required before confirming the classical path has a value for it
  </read_first>
  <action>
    Insert a `### SCHEMA-03 - classical write-time identity surface` subsection under the existing
    `## Recommendation` heading in `31-DECISION.md`, immediately after the `### SCHEMA-02`
    subsection so the verdicts stay in requirement order.

    Answer the roadmap's Success Criterion 3 question in the first sentence, as a yes or a no: is
    the existing 5-minute telescope/instrument/start_time tolerance match sufficient as a write-time
    identity surface on its own, or does the classical path need a facility-specific key.

    If the answer is that it needs one, name the concrete failing case - the specific situation in
    which two genuinely distinct classical runs, or one run seen twice, cannot be told apart by
    telescope, instrument and a start time within five minutes. Candidate cases to test the answer
    against, each of which must be either ruled out with a reason or named as the failing case: two
    instruments swapped on the same telescope on the same night; a run whose parsed start time
    shifts by more than the tolerance because the site's night convention changed (the Las Campanas
    both-inclusive versus ESO noon-to-noon rules that `_iter_run_nights` already dispatches on); a
    re-issued schedule file in which a run's status changed but nothing else did; and two different
    proposals allocated the same telescope, instrument and night.

    If the answer is that the tolerance match is sufficient, state the same list and say why each
    case does not break it, and state under what future condition the answer would change - the
    tolerance match's sufficiency is a property of today's data volume and cadence, not a theorem.

    Then state the concrete consequence for the SCHEMA-02 field: does the classical path leave the
    identity field blank and continue to rely on the tolerance match (which requires the field to be
    nullable, per RESEARCH.md Pitfall 3), or does it write a deterministic synthesized key, and if
    so state that key's exact format. If a proposal code turned out to be available for some run
    states, state whether it should be used, and what happens for the states where it is absent -
    a key available only sometimes is not an identity key unless the absent case has a stated
    fallback.

    Record the confidence honestly: if task 2 recorded zero real files, this verdict is
    `Tag: **Constructed-input code-path check**` and the section must say that Phase 32 should
    re-check it against the first real file it sees. Do not upgrade the tag on the strength of the
    documentation examples.

    Close by naming what Phase 32 inherits: whether a parser change is needed, and whether the
    identity field's nullability chosen in plan 31-02 is compatible with this answer. If it is not,
    say so plainly - a contradiction between the SCHEMA-02 and SCHEMA-03 verdicts is a finding the
    document must surface, not smooth over.
  </action>
  <verify>
    <automated>D=.planning/phases/31-foundation-spikes-run-identity-unattended-invocation/31-DECISION.md; grep -q '^### SCHEMA-03 - classical write-time identity surface' "$D" &amp;&amp; S=$(sed -n '/^### SCHEMA-03 - classical write-time identity surface/,/^## /p' "$D"); printf '%s' "$S" | grep -qE 'sufficient|insufficient|not sufficient' &amp;&amp; printf '%s' "$S" | grep -qE '5[- ]minute|_START_TIME_MATCH_TOLERANCE|minutes=5' &amp;&amp; printf '%s' "$S" | grep -q 'Phase 32'</automated>
    <fails_when>non-zero exit - the SCHEMA-03 recommendation heading is absent, the section never states a sufficiency verdict, never references the existing tolerance, or never names what Phase 32 inherits.</fails_when>
    <automated>D=.planning/phases/31-foundation-spikes-run-identity-unattended-invocation/31-DECISION.md; S=$(sed -n '/^### SCHEMA-03 - classical write-time identity surface/,/^## /p' "$D"); test "$(printf '%s' "$S" | grep -cE 'instrument|night convention|status changed|proposal')" -ge 4</automated>
    <fails_when>non-zero exit - fewer than four lines in the SCHEMA-03 recommendation reference the candidate failing cases, which is what a one-line verdict with no case analysis behind it looks like.</fails_when>
  </verify>
  <acceptance_criteria>
    - `31-DECISION.md` contains a heading whose text is exactly `### SCHEMA-03 - classical write-time identity surface`
    - The section's first sentence answers sufficient or not sufficient, without hedging
    - All four candidate failing cases (instrument swap, night-convention shift, re-issued file with a changed status, two proposals sharing a telescope, instrument and night) are each either ruled out with a reason or named as the failing case
    - The section states whether the classical path leaves the SCHEMA-02 identity field blank or writes a synthesized key, and gives that key's format if it writes one
    - The section carries a confidence tag consistent with task 2's sample count - constructed-input if that count was zero
    - The section states whether a parser change is needed in Phase 32 and whether this answer is compatible with plan 31-02's nullability decision
    - `git status --porcelain -- solsys_code src` prints nothing
  </acceptance_criteria>
  <done>The classical path's write-time identity surface has an explicit sufficiency verdict with the failing case named if one exists, the consequence for the SCHEMA-02 field is stated, and the verdict's confidence matches the evidence actually available.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator-supplied classical schedule file -> committed decision document | the file is untrusted external content and may carry PI names, proposal titles and contact details that must not reach a committed artifact |
| operator-supplied file -> `parse_run_line` | a malformed or hostile line reaches a parser that raises `ValueError` on an unknown status; the inspection must handle that as data, not as a crash |
| operator recollection (D-07) -> recorded finding | a remembered status vocabulary that does not match the parser's own is a contradiction the document must surface rather than silently adopt |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-07 | Information Disclosure | operator-supplied schedule file quoted into `31-DECISION.md` | high | mitigate | Raw files stay in the git-excluded `tmp/31-classical-samples/`. Only structural facts (status word, presence and format of a proposal-code-shaped token) are quoted; any illustrative line in the committed document is synthetic and labelled as such. Task 2's third verify command fails the task if an email address or credential-shaped token reaches the document. |
| T-31-08 | Denial of Service | `parse_run_line` raising on an unrecognised status in a supplied file | low | accept | The parser's `ValueError` on an unknown status is documented behaviour and is itself a finding worth recording (how many lines a real file would reject today). The inspection counts rejections rather than aborting on the first one. |
| T-31-09 | Repudiation | a sufficiency verdict recorded with a confidence tag stronger than its evidence | medium | mitigate | Task 3 pins the tag to task 2's measured sample count: a zero-file inspection forces the constructed-input tag and an explicit instruction for Phase 32 to re-check against the first real file. |
| T-31-SC | Tampering | package-manager installs | low | accept | This plan installs no package. |
</threat_model>

<verification>
Run from the repository root. `parse_run_line` is a pure function; inspecting sample lines needs no
database and no disposable copy. Do not run the `solsys_code` suite unqualified.

1. `find tmp/31-classical-samples -type f | wc -l` matches the file count stated in the document.
2. `31-DECISION.md` contains both the `#### SCHEMA-03 evidence` and `### SCHEMA-03` headings.
3. The SCHEMA-03 recommendation section answers sufficient or not sufficient in its first sentence.
4. No email address, credential-shaped token or long hex string appears anywhere in
   `31-DECISION.md`.
5. `git ls-files tmp/` prints nothing.
6. `git status --porcelain -- solsys_code src` prints nothing.
</verification>

<success_criteria>
- Roadmap Success Criterion 3 is met: the document answers explicitly whether the classical
  tolerance-windowed match is a sufficient write-time identity surface on its own or needs a
  facility-specific key, with the failing case named if it needs one.
- The number of real classical schedule files inspected is stated honestly, including a zero.
- D-07's remembered status words are reconciled against the parser's real vocabulary.
- No operator-supplied personal or proposal detail reached a committed artifact.
</success_criteria>

<output>
Create `.planning/phases/31-foundation-spikes-run-identity-unattended-invocation/31-03-SUMMARY.md` when done
</output>
