---
phase: 31-foundation-spikes-run-identity-unattended-invocation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/31-foundation-spikes-run-identity-unattended-invocation/31-DECISION.md
  - tmp/31_dbsnapshot_probe.py
  - tmp/31-dbsnapshot.txt
  - tmp/31_constraint_probe.py
  - tmp/31-constraint-probe.txt
  - tmp/31-spike-db-copy.sqlite3
autonomous: true
requirements: [SCHEMA-01, SCHEMA-02]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "31-DECISION.md states a real, dated count of existing CampaignRun rows and of rows whose campaign FK is null, taken from src/fomo_db.sqlite3 during this phase (roadmap Success Criterion 1)"
    - "31-DECISION.md records the real dev DB size/mtime fingerprint before and after every read-only probe, and states the two values are identical"
    - "31-DECISION.md lists every non-test source file in solsys_code/ and src/ that reads the campaign FK, with file:line citations, so the blast radius of a nullable FK is measured rather than assumed"
    - "31-DECISION.md carries verbatim probe output showing, for each of the three candidate schema shapes, whether a write collides with unique_campaign_run_resolved_window and with unique_campaign_run_tbd_natural_key (roadmap Success Criterion 2)"
    - "Every write-probe in this plan ran against tmp/31-spike-db-copy.sqlite3, and the real src/fomo_db.sqlite3 fingerprint is unchanged from the value recorded at the start of the plan"
    - statement: "Two non-campaign runs differing only in which candidate shape is in use are each shown to collide or not collide, with the observed IntegrityError (or its absence) quoted verbatim"
      verification: backstop
  artifacts:
    - path: ".planning/phases/31-foundation-spikes-run-identity-unattended-invocation/31-DECISION.md"
      provides: "Phase 31 decision document - header, investigation-only framing, and the SCHEMA-01/SCHEMA-02 evidence sections"
      contains: "## Findings"
  key_links:
    - from: "tmp/31_dbsnapshot_probe.py"
      to: ".planning/phases/31-foundation-spikes-run-identity-unattended-invocation/31-DECISION.md"
      via: "probe stdout captured to tmp/31-dbsnapshot.txt and quoted verbatim into the SCHEMA-01 evidence section"
      pattern: "NULL_CAMPAIGN_ROWS="
    - from: "tmp/31_constraint_probe.py"
      to: ".planning/phases/31-foundation-spikes-run-identity-unattended-invocation/31-DECISION.md"
      via: "probe stdout captured to tmp/31-constraint-probe.txt and quoted verbatim into the SCHEMA-02 evidence section"
      pattern: "unique_campaign_run_resolved_window"
  prohibitions:
    - statement: "MUST NOT record a finding in 31-DECISION.md as evidence-backed unless the probe behind it actually ran during this phase; every finding carries one of the two fixed confidence tags, and anything not actually observed is labelled unconfirmed rather than quietly upgraded"
      status: unverified
      verification: judgment
    - statement: "MUST NOT write to the operator's real dev database at src/fomo_db.sqlite3; every write-probe targets the disposable copy only, and the real file size/mtime fingerprint is recorded before and after and must be identical"
      status: unverified
      verification: judgment
---

<objective>
Gather the real, executable evidence the SCHEMA-01 and SCHEMA-02 verdicts will rest on, and
create `31-DECISION.md` carrying it.

Purpose: the roadmap's Success Criteria 1 and 2 set the acceptance bar at "backed by executable
evidence against the real dev-DB rows... not by argument" (D-05, D-08). This plan produces that
evidence: a dated snapshot of the real `CampaignRun` population, a grep-verified inventory of
every read path that would have to survive a nullable `campaign` FK, and a constraint probe that
puts all three candidate schema shapes against both existing partial `UniqueConstraint`s on a
disposable copy of the dev database.

Output: `31-DECISION.md` with its header, investigation-only framing, and three evidence
subsections under `## Findings`; git-excluded probe scripts and transcripts under `tmp/`.
</objective>

<execution_context>
@/home/tlister/git/fomo_devel/.claude/gsd-core/workflows/execute-plan.md
@/home/tlister/git/fomo_devel/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-foundation-spikes-run-identity-unattended-invocation/31-CONTEXT.md
@.planning/phases/31-foundation-spikes-run-identity-unattended-invocation/31-RESEARCH.md
@.planning/phases/31-foundation-spikes-run-identity-unattended-invocation/31-PATTERNS.md
@.planning/phases/31-foundation-spikes-run-identity-unattended-invocation/31-VALIDATION.md
</context>

## Artifacts this phase produces

New files created by Phase 31. None of these exist yet, so none is a source-drift candidate.

| Artifact | Committed? | Created by |
|---|---|---|
| `.planning/phases/31-foundation-spikes-run-identity-unattended-invocation/31-DECISION.md` | yes | plan 31-01 creates it; 31-02, 31-03 and 31-04 append to it |
| `docs/design/run_identity_and_unattended_invocation_spike.rst` | yes | plan 31-05 |
| `tmp/31_dbsnapshot_probe.py` | no - `tmp/` is git-excluded (`.gitignore:145`) | plan 31-01 |
| `tmp/31-dbsnapshot.txt` | no | plan 31-01 |
| `tmp/31_constraint_probe.py` | no | plan 31-01 |
| `tmp/31-constraint-probe.txt` | no | plan 31-01 |
| `tmp/31-spike-db-copy.sqlite3` | no - deleted by plan 31-05 | plan 31-01 |
| `tmp/31-host-probe.txt` | no | plan 31-04 |
| `tmp/31-container-probe.txt` | no | plan 31-04 |
| `tmp/31-classical-samples/` | no - operator-supplied inputs | plan 31-03 |

`docs/design/design.rst` is the only pre-existing committed file this phase edits (plan 31-05
adds one toctree entry). No `solsys_code/` or `src/` symbol is created, renamed or modified.

## Flagged assumptions (edge-probe, unresolved)

The deterministic edge-probe returned 8 applicable items across this phase's four requirements
and resolved none of them. That is the expected outcome: the probe taxonomy (adjacency,
empty-input, ordering) is tuned for algorithmic requirements, and SCHEMA-01/02 are investigation
deliverables rather than behaviours. Each item is carried explicitly rather than dropped.

| Requirement | Probe category | Probe question | Assumption carried |
|---|---|---|---|
| SCHEMA-01 | unclassified | (unclassified - review manually) | Assumed not applicable: SCHEMA-01's deliverable is a documented verdict, not an input/output behaviour. The substitute acceptance bar is the roadmap Success Criterion 1 truth in `must_haves` above (a real, dated dev-DB count). |
| SCHEMA-02 | adjacency | When two things are exactly equal or just touch, do they merge, collide, or separate? | Genuinely applicable, and answered by task 3 block (C): two runs sharing `telescope_instrument` and an identical window under one shared campaign value is exactly the adjacency case, and the probe measures whether it collides. |
| SCHEMA-02 | empty | What is the result for empty, single-element, or null input? | Genuinely applicable, and answered by task 3 block (B): a null `campaign` under Option A is the null-input case, and SQLite never treats NULL as equal in a unique index, so the probe must record whether the constraint silently stops discriminating. |
| SCHEMA-02 | ordering | When elements compare equal, is output order specified and stable? | Assumed not applicable: a schema-shape decision produces no ordered output. Carried unresolved; no substitute criterion authored. |

The remaining 4 items (SCHEMA-03 x1, SCHED-07 x3) are carried in plans 31-03 and 31-04.

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end slice - one probe becomes one dated, evidence-tagged finding in the committed decision doc</name>
  <files>.planning/phases/31-foundation-spikes-run-identity-unattended-invocation/31-DECISION.md, tmp/31_dbsnapshot_probe.py, tmp/31-dbsnapshot.txt</files>
  <read_first>
    - `.planning/phases/31-foundation-spikes-run-identity-unattended-invocation/31-PATTERNS.md` (the `26-DECISION.md` header pattern, the per-finding evidence pattern, the fingerprint-before/after discipline, and the fixed two-tag confidence vocabulary, all quoted verbatim there)
    - `.planning/milestones/v2.2-phases/26-canonical-record-spike/26-DECISION.md` lines 1-60 (header and framing shape) and lines 34-40 (fingerprint discipline)
    - `solsys_code/models.py` lines 78-360 (`CampaignRun`: the `campaign` FK, the `Source` TextChoices vocabulary, the two partial `UniqueConstraint`s, the window-pairing `CheckConstraint`, and `__str__`)
    - `.gitignore` line 145 (`tmp/`) and line 64 (`fomo_db.sqlite3`), confirming both the probe location and the dev database are git-excluded
  </read_first>
  <action>
    This is the thin end-to-end slice for the whole phase: write one probe, run it for real
    against the real dev database, and land its output as a dated, confidence-tagged finding
    inside a committed `31-DECISION.md`. Every later task in every later plan repeats this shape,
    so prove it works once before expanding.

    Write `tmp/31_dbsnapshot_probe.py` as a read-only Django shell script, invoked as
    `python manage.py shell < tmp/31_dbsnapshot_probe.py` (the invocation `26-DECISION.md:368`
    records for its own probes). It must never call a write-style ORM method, never select
    `contact_person` or `contact_email` values, and must print machine-greppable `KEY=value`
    lines in five lettered blocks.

    Block (A): `FINGERPRINT_BEFORE=<size> <mtime>`, read via `os.stat` on the resolved
    `settings.DATABASES['default']['NAME']`, in the same `%s %Y` form `26-DECISION.md` records.

    Block (B): the `CampaignRun` population. Print `TOTAL_CAMPAIGNRUN_ROWS`,
    `NULL_CAMPAIGN_ROWS` (via a `campaign__isnull=True` filter), `DISTINCT_CAMPAIGNS`,
    `RESOLVED_WINDOW_ROWS` (`window_start__isnull=False`), `TBD_WINDOW_ROWS`
    (`window_start__isnull=True`), and one `SOURCE_<value>=<n>` line per value of
    `CampaignRun.Source` that actually occurs, so the per-ingest-path picture D-06 asks to
    confirm is measured rather than assumed.

    Block (C): collision headroom for a shared-campaign shape. Group existing rows by
    `(telescope_instrument, window_start, window_end)` ignoring `campaign` entirely and print
    `DUP_TELINST_WINDOW_TUPLES_IGNORING_CAMPAIGN=<n>`, the count of tuples occurring more than
    once. This is the direct real-data measurement of RESEARCH.md Pitfall 2: it says how many
    existing rows would already collide if every non-campaign run were collapsed onto one shared
    `campaign` value. Print the equivalent for the TBD branch grouped by
    `(telescope_instrument, contact_person)` as
    `DUP_TELINST_CONTACT_TUPLES_IGNORING_CAMPAIGN=<n>`, printing only the count, never the
    contact values themselves.

    Block (D): `FINGERPRINT_AFTER=<size> <mtime>`, then `FINGERPRINT_UNCHANGED=PASS` when the two
    fingerprints match and `FINGERPRINT_UNCHANGED=FAIL` otherwise.

    Block (E): the two existing constraints, printed from `CampaignRun._meta.constraints` so the
    document quotes the live definitions rather than a remembered copy - name, `fields` tuple and
    `condition` for `unique_campaign_run_resolved_window` and `unique_campaign_run_tbd_natural_key`.

    Run it, capturing stdout and stderr to `tmp/31-dbsnapshot.txt`.

    Then create
    `.planning/phases/31-foundation-spikes-run-identity-unattended-invocation/31-DECISION.md`
    following the `26-DECISION.md` header pattern quoted in 31-PATTERNS.md: a title line, an
    `**Investigated:**` date line, a `**Status:**` line stating the document is built up across
    the five plans of Phase 31, then an investigation-only framing paragraph stating explicitly
    that no `CampaignRun` migration is applied, no adapter code is written, and the only committed
    artifacts of this phase are this file and one `docs/design/` page. State the evidence posture
    per track in that paragraph: the schema track follows Phase 26's disposable-file-copy posture
    (write-probes run for real against `tmp/31-spike-db-copy.sqlite3`, with no rollback anywhere,
    because the whole file is throwaway), while read-only probes touch the real
    `src/fomo_db.sqlite3` under the fingerprint-before/after discipline only.

    Add the headings `## Findings`, then `### Schema/identity track (SCHEMA-01/02/03)`, then
    `#### SCHEMA-01 evidence - real dev-DB snapshot`. Under the last one quote the probe output
    verbatim in a fenced block, state the dated fingerprint before and after and that they are
    identical, and close with a tag from the fixed vocabulary - `Tag: **Confirmed against real
    rows**` for anything the probe actually read. Add a short prose reading of what the numbers
    mean for D-06's low-migration-risk premise: whether the null-campaign count is in fact zero,
    and whether block (C) shows any pre-existing tuple collisions.

    Record in that section, per 31-PATTERNS.md's "No Analog Found" entry, that none of the three
    candidate schema shapes has a precedent anywhere in this codebase - the precedent that exists
    is for the investigation process and document structure, not for the schema shape itself.

    Do not modify any file under `solsys_code/` or `src/`. Do not create or edit a migration.
  </action>
  <verify>
    <automated>python manage.py shell &lt; tmp/31_dbsnapshot_probe.py &gt; tmp/31-dbsnapshot.txt 2&gt;&amp;1 &amp;&amp; grep -q '^FINGERPRINT_UNCHANGED=PASS$' tmp/31-dbsnapshot.txt &amp;&amp; grep -q '^NULL_CAMPAIGN_ROWS=' tmp/31-dbsnapshot.txt &amp;&amp; grep -q '^TOTAL_CAMPAIGNRUN_ROWS=' tmp/31-dbsnapshot.txt &amp;&amp; grep -q 'unique_campaign_run_tbd_natural_key' tmp/31-dbsnapshot.txt</automated>
    <fails_when>non-zero exit - the shell script raised (Django propagates the traceback and exits non-zero), or the transcript is missing `FINGERPRINT_UNCHANGED=PASS`, `NULL_CAMPAIGN_ROWS=`, `TOTAL_CAMPAIGNRUN_ROWS=` or the constraint-inventory line. A probe that silently did nothing produces a transcript with none of these keys.</fails_when>
    <automated>D=.planning/phases/31-foundation-spikes-run-identity-unattended-invocation/31-DECISION.md; test -f "$D" &amp;&amp; grep -q '^## Findings' "$D" &amp;&amp; grep -q '^#### SCHEMA-01 evidence' "$D" &amp;&amp; grep -q '^FINGERPRINT_UNCHANGED=PASS$' "$D" &amp;&amp; grep -q '^NULL_CAMPAIGN_ROWS=' "$D" &amp;&amp; grep -q 'Confirmed against real rows' "$D"</automated>
    <fails_when>non-zero exit - `31-DECISION.md` does not exist, or is missing the `## Findings` heading, the SCHEMA-01 evidence heading, the verbatim `FINGERPRINT_UNCHANGED=PASS` and `NULL_CAMPAIGN_ROWS=` transcript lines, or a confidence tag. A section written from memory instead of from the transcript carries no `FINGERPRINT_UNCHANGED=PASS` line.</fails_when>
  </verify>
  <acceptance_criteria>
    - `tmp/31_dbsnapshot_probe.py` exists and contains no occurrence of `.save(`, `.create(`, `.update(` or `.delete(`
    - `tmp/31-dbsnapshot.txt` contains lines matching `FINGERPRINT_BEFORE=`, `FINGERPRINT_AFTER=`, `FINGERPRINT_UNCHANGED=PASS`, `TOTAL_CAMPAIGNRUN_ROWS=`, `NULL_CAMPAIGN_ROWS=`, `DISTINCT_CAMPAIGNS=`, `RESOLVED_WINDOW_ROWS=`, `TBD_WINDOW_ROWS=` and `DUP_TELINST_WINDOW_TUPLES_IGNORING_CAMPAIGN=`
    - `31-DECISION.md` contains an `**Investigated:**` line, a `**Status:**` line, and an explicit statement that no schema migration is applied by this phase
    - `31-DECISION.md` quotes the probe transcript verbatim inside a fenced block rather than paraphrasing it
    - `31-DECISION.md` states the same fingerprint value twice, before and after, and asserts they are identical
    - `git status --porcelain -- solsys_code src` prints nothing
  </acceptance_criteria>
  <done>The real dev-DB population is measured, the measurement is committed inside `31-DECISION.md` with a confidence tag and a matching before/after fingerprint, and the real database is provably unmodified.</done>
</task>

<task type="auto">
  <name>Task 2: Inventory every read path that reads the campaign FK - the Option A blast radius</name>
  <files>.planning/phases/31-foundation-spikes-run-identity-unattended-invocation/31-DECISION.md</files>
  <read_first>
    - `.planning/phases/31-foundation-spikes-run-identity-unattended-invocation/31-DECISION.md` (created by task 1 - append to it, never rewrite it)
    - `.planning/phases/31-foundation-spikes-run-identity-unattended-invocation/31-RESEARCH.md` section "Common Pitfalls" Pitfall 1 and section "Open Questions" item 2, which both specify this inventory as the evidentiary bar, modelled on `26-DECISION.md`'s SPIKE-04 blast-radius table
    - `solsys_code/models.py` line 352 (`CampaignRun.__str__`, which reads `self.campaign.name` unconditionally)
    - `solsys_code/campaign_reconciler.py` lines 170-180, 255-265 and 393-402
    - `solsys_code/campaign_attribution.py` around lines 397 and 541
    - `solsys_code/campaign_tables.py` around lines 467 and 538
    - `solsys_code/admin.py` around line 225 and `solsys_code/campaign_views.py` lines 360-410 and 975-1005 (the `select_related` traversals)
  </read_first>
  <action>
    Produce the grep-verified inventory RESEARCH.md Open Question 2 asks for, so the Option A
    (nullable FK) evaluation rests on a counted blast radius rather than the handful of sites the
    research pass happened to notice.

    Derive the inventory fresh; do not copy it from RESEARCH.md. Search `solsys_code/` and `src/`
    for four distinct access classes, excluding `/migrations/` and `/tests/` from the source
    inventory but recording the excluded test-file count separately as a number:

    Class (a) - direct attribute read of the related object, occurrences of `.campaign.` in
    `*.py`. These raise `AttributeError` on a null FK.
    Class (b) - related-object assignment or pass-through, occurrences of `.campaign` not
    followed by a dot. These survive a null value without raising.
    Class (c) - queryset traversals that assume a joinable row: `select_related('campaign'`,
    `run__campaign`, and `campaign__` lookups.
    Class (d) - template-variable reads of `campaign.` under `src/templates/` and any template
    directory inside `solsys_code/`. These fail silently in Django templates rather than raising,
    so they are a different risk class from (a).

    Append a `#### SCHEMA-01 evidence - campaign FK read-path blast radius` subsection to
    `31-DECISION.md` containing one table with a row per site: file, line, access class, the exact
    expression, and a verdict column stating whether that site would raise, silently render blank,
    or be unaffected if `campaign` were null. Quote the grep commands used verbatim so the
    inventory is reproducible, and state the totals per class plus the excluded test-file count.

    Close with an explicit reading for the D-05 comparison: how many sites would need a null guard
    under Option A, and whether any of them sits on a hot path (a queryset whose `select_related`
    would need revisiting) versus a cold path (an admin label or a `__str__`). Tag the grep-derived
    parts `Tag: **Confirmed against real rows**`; tag the hot-versus-cold judgement
    `Tag: **Constructed-input code-path check**`, since no profiling was run.

    Do not edit any of the files inventoried.
  </action>
  <verify>
    <automated>MISS=0; for f in $(grep -rl '\.campaign\b' --include='*.py' solsys_code src | grep -v '/migrations/' | grep -v '/tests/'); do grep -q "$f" .planning/phases/31-foundation-spikes-run-identity-unattended-invocation/31-DECISION.md || { echo "MISSING $f"; MISS=1; }; done; test "$MISS" = 0</automated>
    <fails_when>non-zero exit, or any line beginning `MISSING ` naming a non-test source file that reads the FK but is absent from the inventory table. An empty inventory fails on the first file the grep finds.</fails_when>
    <automated>D=.planning/phases/31-foundation-spikes-run-identity-unattended-invocation/31-DECISION.md; grep -q '^#### SCHEMA-01 evidence - campaign FK read-path blast radius' "$D" &amp;&amp; grep -q 'solsys_code/models.py:352' "$D" &amp;&amp; grep -q 'select_related' "$D" &amp;&amp; grep -Eq 'Tag: \*\*(Confirmed against real rows|Constructed-input code-path check)\*\*' "$D"</automated>
    <fails_when>non-zero exit - the blast-radius heading is absent, the `__str__` site is not cited with its file:line, no queryset-traversal class is recorded, or the section carries no confidence tag.</fails_when>
  </verify>
  <acceptance_criteria>
    - `31-DECISION.md` contains a heading whose text is exactly `#### SCHEMA-01 evidence - campaign FK read-path blast radius`
    - The inventory table cites `solsys_code/models.py:352` and gives its verdict under a null campaign
    - The inventory table has at least one row in each of classes (a), (b), (c) and (d), or explicitly records a zero count for any class with no occurrences
    - The section quotes the grep commands used, so a reader can re-derive the same list
    - The section states a total per class, the excluded test-file count, and the count of sites that would need a null guard under Option A
    - `git status --porcelain -- solsys_code src` prints nothing
  </acceptance_criteria>
  <done>Every source and template site that reads the campaign FK is listed with file:line and a stated behaviour under a null campaign, and the count of sites needing a guard under Option A is recorded.</done>
</task>

<task type="auto">
  <name>Task 3: Probe all three candidate schema shapes against both existing partial constraints on a disposable DB copy</name>
  <files>.planning/phases/31-foundation-spikes-run-identity-unattended-invocation/31-DECISION.md, tmp/31_constraint_probe.py, tmp/31-constraint-probe.txt, tmp/31-spike-db-copy.sqlite3</files>
  <read_first>
    - `.planning/phases/31-foundation-spikes-run-identity-unattended-invocation/31-PATTERNS.md` section "`tmp/31_constraint_probe.py`" - the lettered-block structure, the `transaction.atomic()`-around-an-expected-`IntegrityError` idiom, and the disposable-copy discipline, all quoted from `26-DECISION.md:366-434`
    - `.planning/phases/31-foundation-spikes-run-identity-unattended-invocation/31-RESEARCH.md` section "Architecture Patterns" Pattern 1 (the three candidate shapes and their Django idioms) and section "Common Pitfalls" Pitfalls 2 and 3
    - `solsys_code/models.py` lines 280-320 (`CampaignRun.Meta.constraints`: the two partial `UniqueConstraint`s and the window-pairing `CheckConstraint`, with the comments explaining why `window_start`/`window_end` are deliberately excluded from the TBD constraint)
    - `solsys_code/management/commands/sync_lco_observation_calendar.py` around line 341 (the LCO read-time identity value, the portal request `url`)
    - `solsys_code/management/commands/sync_gemini_observation_calendar.py` around lines 150 and 163 (the Gemini read-time identity value, constructed as a `GEM:` prefix plus program and observation id)
    - `solsys_code/management/commands/load_telescope_runs.py` lines 20-25 and 200-220 (`_START_TIME_MATCH_TOLERANCE` and the classical tolerance-match lookup dict)
    - `tmp/31-dbsnapshot.txt` (task 1's transcript, for the fingerprint to compare against)
  </read_first>
  <action>
    Measure, against a real schema on a throwaway database, which of the three candidate shapes
    from D-05 can actually give a non-campaign run a persistent identity without colliding with
    the two constraints that already exist.

    First copy the dev database with `cp src/fomo_db.sqlite3 tmp/31-spike-db-copy.sqlite3`. Never
    point anything at the live file for a write.

    Write `tmp/31_constraint_probe.py`, run as `python manage.py shell < tmp/31_constraint_probe.py`.
    Re-point the default connection at the copy at the top of the script before any query runs:
    close `connections['default']`, set its `settings_dict['NAME']` to the absolute path of
    `tmp/31-spike-db-copy.sqlite3`, and let Django reconnect. Do not edit
    `src/fomo/local_settings.py` - it is a live machine-local override holding a real credential
    and must not be touched by this phase at all. If the connection re-point proves unreliable,
    the documented fallback is a throwaway settings module placed under `tmp/` and selected via
    `PYTHONPATH=tmp DJANGO_SETTINGS_MODULE=<module>` (`manage.py` uses `os.environ.setdefault`, so
    an already-set value wins), but still never the machine-local override.

    Immediately after re-pointing, assert the guard: read back the connection's resolved database
    name, print it, and if its basename is not `31-spike-db-copy.sqlite3`, print a `FAIL:` line and
    raise `SystemExit` before any write happens. On success print exactly `GUARD_DISPOSABLE_COPY=OK`.

    Apply the candidate schema changes to the copy in-process with Django's schema editor rather
    than by writing a migration file: `connection.schema_editor()` with `alter_field`, `add_field`
    and `add_constraint`, plus `CampaignRun.add_to_class` so the in-memory model knows about any
    field added. Writing a real migration under `solsys_code/migrations/` is out of scope this
    phase - the roadmap's Success Criterion 5 requires the source tree to be unchanged.

    Structure the probe as lettered blocks, each printing `PASS:` or `FAIL:` lines.

    Block (A) - constraint inventory. Print both existing constraints' names, `fields` tuples and
    `condition`s from `CampaignRun._meta.constraints`, and assert that the candidate identity field
    name does not appear in either field tuple unless the design deliberately folds it in.

    Block (B) - Option A, nullable FK. Use `alter_field` to make the `campaign` FK nullable, create
    a row with a null campaign, and confirm no `IntegrityError`. Then create a second row with a
    null campaign and an identical `telescope_instrument`, `window_start` and `window_end`, and
    record what happens. This is the load-bearing measurement: SQLite never treats NULL as equal in
    a unique index, so `unique_campaign_run_resolved_window` may silently stop discriminating for
    exactly the rows this phase is trying to give identity to. Print the observed outcome either
    way - a `PASS:` line reading "constraint no longer fires, second row created" is a finding, not
    a failure.

    Block (C) - Option B, single shared sentinel. Create one `TargetList` via `get_or_create` on an
    exact `name`, point two genuinely distinct non-campaign runs at it with the same
    `telescope_instrument` and the same resolved window, and record whether the second write raises.
    Wrap the expected-failure write in `transaction.atomic()` so an `IntegrityError` does not poison
    the connection - that is the sole reason for the block, never to roll back a real write. Repeat
    for the TBD branch, where the discriminator is `contact_person`.

    Block (D) - Option C, per-proposal placeholder. Create two `TargetList` rows named for two
    different proposal identifiers, write the same two runs one under each, and confirm they do not
    collide; then write two runs under the same proposal placeholder with the same
    telescope/instrument/window and confirm they do collide. Record the open design question
    RESEARCH.md names for this option: what happens to a placeholder's existing rows once that
    proposal later acquires a real coordinated campaign.

    Block (E) - the candidate write-time identity field. Use `add_field` to add a nullable
    `source_identifier`-style `CharField` and `add_constraint` to add a partial `UniqueConstraint`
    over it conditioned on the field being non-null, so classical rows that have no natural single
    value can leave it blank (RESEARCH.md Pitfall 3). Then write one row per ingest path using each
    one's real identity value: the LCO portal request `url`, a constructed Gemini `GEM:`-prefixed
    program and observation identifier, and a deterministic classical key synthesized from
    telescope, instrument and start time. Re-write each row a second time through a find-or-create
    style lookup keyed on the new field and confirm the second pass creates nothing new. Then
    confirm both original constraints still fire unchanged for a genuine duplicate, proving the new
    field is additive rather than a replacement.

    Capture stdout and stderr to `tmp/31-constraint-probe.txt`. Append a
    `#### SCHEMA-02 evidence - candidate-shape constraint probe` subsection to `31-DECISION.md`
    quoting the transcript verbatim, with one short prose reading per block and a three-column
    comparison table (shape / which constraint it collides with / what it costs) covering all three
    candidates. Tag the copied-real-rows parts `Tag: **Confirmed against real rows**` and anything
    exercised with synthetic inputs `Tag: **Constructed-input code-path check**`, per
    31-PATTERNS.md's fixed vocabulary. Record the real dev DB fingerprint again at the end of the
    section and state that it matches task 1's value.
  </action>
  <verify>
    <automated>cp src/fomo_db.sqlite3 tmp/31-spike-db-copy.sqlite3 &amp;&amp; python manage.py shell &lt; tmp/31_constraint_probe.py &gt; tmp/31-constraint-probe.txt 2&gt;&amp;1 &amp;&amp; grep -q '^GUARD_DISPOSABLE_COPY=OK$' tmp/31-constraint-probe.txt &amp;&amp; test "$(grep -c '^PASS:' tmp/31-constraint-probe.txt)" -ge 5 &amp;&amp; test "$(grep -c '^FAIL:' tmp/31-constraint-probe.txt)" -eq 0</automated>
    <fails_when>non-zero exit - the probe raised, the DB-path guard line is absent (meaning the guard never ran and the write target was never proven), fewer than five `PASS:` lines were produced (fewer than one per block, which is what a probe that stopped early looks like), or any `FAIL:` line is present.</fails_when>
    <automated>test "$(stat -c '%s %Y' src/fomo_db.sqlite3)" = "$(grep '^FINGERPRINT_BEFORE=' tmp/31-dbsnapshot.txt | head -1 | cut -d= -f2-)"</automated>
    <fails_when>non-zero exit - the real dev database's size or mtime differs from the value task 1 recorded, meaning a write reached the live file instead of the disposable copy. An absent or empty `FINGERPRINT_BEFORE=` line also fails, because the comparison operand would be empty.</fails_when>
    <automated>D=.planning/phases/31-foundation-spikes-run-identity-unattended-invocation/31-DECISION.md; grep -q '^#### SCHEMA-02 evidence - candidate-shape constraint probe' "$D" &amp;&amp; grep -q 'unique_campaign_run_resolved_window' "$D" &amp;&amp; grep -q 'unique_campaign_run_tbd_natural_key' "$D" &amp;&amp; grep -q 'GUARD_DISPOSABLE_COPY=OK' "$D"</automated>
    <fails_when>non-zero exit - the SCHEMA-02 evidence heading is absent, one or both constraint names are missing from the section, or the guard line was never carried into the document (which is what quoting a paraphrase instead of the transcript looks like).</fails_when>
  </verify>
  <acceptance_criteria>
    - `tmp/31_constraint_probe.py` exists and re-points the default connection before any query, then prints `GUARD_DISPOSABLE_COPY=OK`
    - `tmp/31_constraint_probe.py` contains no occurrence of `local_settings`
    - `tmp/31-constraint-probe.txt` contains at least one `PASS:` line for each of blocks (A) through (E) and zero `FAIL:` lines
    - `git status --porcelain -- solsys_code/migrations` prints nothing, proving no migration file was created
    - `31-DECISION.md` contains a three-row comparison table naming all three candidate shapes, each with an explicit collides/does-not-collide verdict against both existing constraints
    - `31-DECISION.md` records what block (B) observed for two null-campaign rows sharing a telescope, instrument and window
    - `31-DECISION.md` shows a row per ingest path in block (E) with the actual identity value each would write
    - `git status --porcelain -- solsys_code src` prints nothing
  </acceptance_criteria>
  <done>All three candidate shapes have been exercised for real against both existing partial constraints on a throwaway copy of the dev database, the outcomes are quoted verbatim in `31-DECISION.md`, and the live database is byte-identical to its state at the start of the plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| real dev database -> disposable probe copy | a mis-pointed connection would write into the operator's live `src/fomo_db.sqlite3` |
| machine-local settings override / environment -> committed decision document | `src/fomo/local_settings.py` is git-excluded and holds a live credential; this plan's evidence discipline is "quote output verbatim", which is exactly how such a value reaches a committed file |
| real `CampaignRun` rows (contact person, contact email) -> committed decision document | the model carries submitter contact fields that are PII and are gated in the app's own read paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-01 | Information Disclosure | `31-DECISION.md` verbatim evidence quoting | high | mitigate | Probe scripts print counts, field names and constraint definitions only. Neither probe selects `contact_person` or `contact_email` values (task 1 block (C) prints a count, never the values). Neither probe reads `src/fomo/local_settings.py`, `settings.FACILITIES` or any environment variable. Plan 31-05 runs the credential-shaped-token gate over both committed artifacts before the phase seals. |
| T-31-02 | Tampering | write-probe target selection in `tmp/31_constraint_probe.py` | high | mitigate | Hard guard: the probe reads back the resolved database name and raises `SystemExit` before any write unless the basename is `31-spike-db-copy.sqlite3`, printing `GUARD_DISPOSABLE_COPY=OK` on success. Task 3's second verify command independently re-compares the live database's size/mtime against the value task 1 recorded. |
| T-31-03 | Tampering | accidental source edit during investigation | medium | mitigate | Every task's acceptance criteria include `git status --porcelain -- solsys_code src` printing nothing; the schema changes are applied in-process via `connection.schema_editor()` so no migration file is ever written. |
| T-31-SC | Tampering | package-manager installs | low | accept | This plan installs no package. RESEARCH.md's Package Legitimacy Audit records the phase as not applicable: `flock` is a pre-existing OS binary and no `pip install` occurs in any task. |
</threat_model>

<verification>
Run from the repository root. Use `python manage.py`, never `./manage.py`. Do not run the
`solsys_code` suite unqualified - `solsys_code.tests.test_views.TestEphemeris` segfaults in native
ASSIST and importing `solsys_code.ephem_utils` triggers a large SPICE kernel download. No module
this plan touches imports it.

1. `python manage.py shell < tmp/31_dbsnapshot_probe.py` re-runs clean and reprints
   `FINGERPRINT_UNCHANGED=PASS`.
2. `python manage.py shell < tmp/31_constraint_probe.py` re-runs clean against a fresh copy, with
   `GUARD_DISPOSABLE_COPY=OK` and zero `FAIL:` lines.
3. `stat -c '%s %Y' src/fomo_db.sqlite3` matches the `FINGERPRINT_BEFORE=` value in
   `tmp/31-dbsnapshot.txt`.
4. `git status --porcelain -- solsys_code src` prints nothing.
5. `git ls-files tmp/` prints nothing - no disposable probe artifact was committed.
6. `31-DECISION.md` exists and contains `## Findings`, `### Schema/identity track`, and all three
   `#### SCHEMA-0*` evidence headings this plan authors.
</verification>

<success_criteria>
- `31-DECISION.md` exists, is committed, and carries a header, an investigation-only framing
  paragraph, and three evidence subsections built from real probe output.
- Roadmap Success Criterion 1's evidence half is satisfied: a real, dated count of existing
  `CampaignRun` rows and of null-campaign rows, from `src/fomo_db.sqlite3`.
- Roadmap Success Criterion 2's evidence half is satisfied: all three candidate shapes exercised
  against both existing partial constraints, with per-ingest-path identity values written and
  re-written.
- The live dev database and the entire `solsys_code/` and `src/` source tree are unchanged.
</success_criteria>

<output>
Create `.planning/phases/31-foundation-spikes-run-identity-unattended-invocation/31-01-SUMMARY.md` when done
</output>