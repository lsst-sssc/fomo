---
phase: 08-telescope-label-verification-sidecar
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/templates/tom_calendar/partials/calendar.html
  - solsys_code/tests/test_calendar_template.py
autonomous: true
requirements: [DISPLAY-02, DISPLAY-03]
must_haves:
  truths:
    - "A fallback-labeled event (sidecar is_verified=False) renders with a 2px dashed border on both the all-day and timed render branches, distinguishable without reading the title."
    - "A fallback-labeled event renders a native title= tooltip explaining the label is an unverified coarse fallback."
    - "A verified event and an event with no sidecar row both render with no dashed border and no tooltip, with no template error."
  artifacts:
    - "src/templates/tom_calendar/partials/calendar.html has dashed-border + tooltip branches in both the all-day and timed loops"
    - "solsys_code/tests/test_calendar_template.py (new) renders the calendar via Client and asserts the dashed-border and tooltip markers"
  key_links:
    - "event.telescope_label_meta.is_verified == False (reverse O2O from Plan 01) -> dashed border + title attribute"
    - "missing sidecar row / is_verified True -> falls through to unstyled/verified branch (no 500)"
---

<objective>
Render the fallback-vs-verified telescope-label distinction in the calendar UI: a dashed border
(DISPLAY-02) plus a native hover tooltip (DISPLAY-03) on fallback-labeled events, on both the all-day
and timed render branches, with verified and no-sidecar-row events unaffected.

Purpose: Operators can scan the calendar grid for fallback-labeled events without opening an event or
reading truncated title text, and hover to learn why the label is an estimate.
Output: Two conditional branches added to `calendar.html` and a new template-rendering test establishing
the first `calendar.html` view-level test pattern in this codebase.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-telescope-label-verification-sidecar/08-CONTEXT.md
@.planning/phases/08-telescope-label-verification-sidecar/08-RESEARCH.md
@.planning/phases/08-telescope-label-verification-sidecar/08-PATTERNS.md
@.planning/phases/08-telescope-label-verification-sidecar/08-UI-SPEC.md
@.planning/phases/08-telescope-label-verification-sidecar/08-VALIDATION.md
@CLAUDE.md
@.planning/phases/08-telescope-label-verification-sidecar/08-01-SUMMARY.md
</context>

## Artifacts this phase produces (Plan 02)

- **Modified template:** `src/templates/tom_calendar/partials/calendar.html`
  - all-day loop: new `{% elif event.telescope_label_meta.is_verified == False %}` branch with `border: 2px dashed rgba(0, 0, 0, 0.65);` and a `title=` tooltip
  - timed loop: new `{% if event.telescope_label_meta.is_verified == False %}` / `{% else %}` branch on the outer `cal-event-timed` div with the same dashed border + `title=` tooltip
- **New test file:** `solsys_code/tests/test_calendar_template.py` — first `calendar.html` view-level rendering test in this codebase (Client-based, per the test-views precedent)

<tasks>

<task type="auto">
  <name>Task 1: Add dashed-border + tooltip branches to calendar.html (all-day and timed)</name>
  <files>src/templates/tom_calendar/partials/calendar.html</files>
  <read_first>
    - src/templates/tom_calendar/partials/calendar.html (the all-day loop at lines 153-164 with the existing `[QUEUED]` `{% if %}/{% else %}` branch at lines 158-162; the timed loop at lines 170-183 which currently has no conditional border)
    - .planning/phases/08-telescope-label-verification-sidecar/08-PATTERNS.md ("src/templates/tom_calendar/partials/calendar.html — dashed-border + tooltip" section — exact before/after for both branches)
    - .planning/phases/08-telescope-label-verification-sidecar/08-UI-SPEC.md (Color, Interaction Contract, Copywriting Contract — dashed-border spec, tooltip copy, `[QUEUED]` precedence)
    - .planning/phases/08-telescope-label-verification-sidecar/08-RESEARCH.md (Pattern 2 `== False` idiom, Pitfall 4 both-signals-same-task)
    - .planning/phases/08-telescope-label-verification-sidecar/08-CONTEXT.md (D-02, D-03, D-04)
  </read_first>
  <action>
    Edit the all-day-event loop (`{% for event in day.all_day_events %}`). The existing chain is
    `{% if event.title|slice:":9" == "[QUEUED] " %}` ... `{% else %}` ... `{% endif %}`. Insert a new
    middle branch between them: `{% elif event.telescope_label_meta.is_verified == False %}`. In that
    branch render `<div class="cal-event-all-day" style="background-color: {{ event.color }}; border: 2px dashed rgba(0, 0, 0, 0.65);" title="...">`
    where the `title` is the D-04 fallback tooltip (plain-language, explaining the label could not be
    verified against the LCO API and is a coarse fallback; e.g. the working text
    "Telescope label is an estimate — could not be verified against the LCO API; showing a coarse fallback
    label (1m0/0m4/2m0/4m0)."). The `[QUEUED]` branch stays FIRST and takes precedence (a queued +
    fallback event renders as `[QUEUED]` grey this phase — reconciling the two is Phase 9's concern). The
    `{% else %}` verified branch stays exactly as today (`background-color: {{ event.color }};`, no border
    change).

    Edit the timed-event loop (`{% for event in day.events %}`). It currently has a flat
    `<div class="cal-event cal-event-timed" hx-get="{% url 'calendar:update-event' event.id %}">` with no
    conditional. Wrap that opening div in `{% if event.telescope_label_meta.is_verified == False %}` /
    `{% else %}` / `{% endif %}`: the fallback branch renders the same div PLUS
    `style="border: 2px dashed rgba(0, 0, 0, 0.65);"` and the same `title="..."` tooltip on that outer
    div (the hoverable element, per Pitfall 4 — not a child span); the `{% else %}` branch renders the
    div exactly as today. Preserve the existing `hx-get` attribute in both branches.

    Use the `== False` comparison deliberately (NOT bare `{% if not ... %}` and NOT `|default:True`): a
    missing sidecar row (silenced `DoesNotExist` → empty/None) and an explicit `is_verified=True` both
    fall through to the verified/unstyled branch, matching the documented "missing row = verified"
    default; only a persisted `is_verified=False` triggers the dashed border + tooltip.

    Ship the dashed border AND the tooltip together in this one task, on the same wrapping div, for both
    branches (Pitfall 4 — do not split). Use the exact same tooltip copy string in all branches so the
    rendering test can assert one substring. Do NOT touch `CalendarEvent.color` or add any
    proposal-color/status logic (Phase 9). Do NOT introduce a dash-style border for any signal other
    than verification (D-03 reserves dash-style for this phase).
  </action>
  <verify>
    <automated>python -c "s=open('src/templates/tom_calendar/partials/calendar.html').read(); assert s.count('is_verified == False')==2, s.count('is_verified == False'); assert s.count('2px dashed rgba(0, 0, 0, 0.65)')==2; assert 'title=' in s and 'estimate' in s; print('template branches OK')"</automated>
  </verify>
  <acceptance_criteria>
    - The all-day loop has a `{% elif event.telescope_label_meta.is_verified == False %}` branch with `border: 2px dashed rgba(0, 0, 0, 0.65);` and a `title=` tooltip; the `[QUEUED]` branch remains first.
    - The timed loop wraps its outer `cal-event-timed` div in an `{% if event.telescope_label_meta.is_verified == False %}` / `{% else %}` chain, the fallback branch adding the same dashed border + `title=` tooltip on the outer (hoverable) div, preserving `hx-get`.
    - `is_verified == False` appears exactly twice (once per branch); `2px dashed rgba(0, 0, 0, 0.65)` appears exactly twice; the tooltip copy substring is present.
    - The verified `{% else %}` branches are unchanged from today (no border added). `CalendarEvent.color` logic is untouched.
  </acceptance_criteria>
  <done>Both render branches show a dashed border + hover tooltip for fallback-labeled events only, with verified and no-sidecar-row events unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add a calendar-template rendering test (verified / fallback / no-row)</name>
  <files>solsys_code/tests/test_calendar_template.py</files>
  <read_first>
    - solsys_code/solsys_code_observatory/tests/test_views.py (the only Client-based view-test precedent in the repo — setUp + `self.client.get/post` + status_code/assertContains idiom)
    - src/templates/tom_calendar/partials/calendar.html (the markers to assert: `2px dashed rgba(0, 0, 0, 0.65)` and the tooltip substring)
    - .planning/phases/08-telescope-label-verification-sidecar/08-RESEARCH.md (Validation Architecture: Wave 0 gap — no calendar.html rendering test exists yet; Assumptions Log A1 — exercise the missing-row path with a real test)
    - .planning/phases/08-telescope-label-verification-sidecar/08-PATTERNS.md ("New template-rendering test" section — adapt the Client pattern; use NonSiderealTargetFactory per CLAUDE.md if a Target fixture is needed)
    - solsys_code/models.py (CalendarEventTelescopeLabel from Plan 01)
  </read_first>
  <action>
    Create `solsys_code/tests/test_calendar_template.py` establishing the first `calendar.html`
    view-level rendering test in this codebase. Use `django.test.Client` and `django.test.TestCase`.
    Hit the calendar URL via `reverse('calendar:calendar')` (the `tom_calendar` urls are included under
    namespace `calendar` in tom_common; the view also accepts `?year=&month=&utc_offset=0` query params).

    The calendar view buckets events: an event with `start_time.date() == end_time.date() == day`
    renders in the TIMED branch; an event spanning more than one day (`start_time.date() != end_time.date()`)
    renders in the ALL-DAY branch. Construct fixtures so the requested month/year window contains the
    test events (set `?year=`/`?month=` to the fixture's month), covering both branches.

    Create CalendarEvent fixtures directly (the model is `tom_calendar.models.CalendarEvent`). For each
    branch (all-day and timed), create:
    - a fallback event with `CalendarEventTelescopeLabel.objects.create(event=ev, is_verified=False)`,
    - a verified event with `CalendarEventTelescopeLabel.objects.create(event=ev, is_verified=True)`,
    - a no-sidecar-row event (create the CalendarEvent only, no sidecar row).
    If any CalendarEvent field requires a Target, use `NonSiderealTargetFactory` (never `SiderealTargetFactory`)
    per CLAUDE.md.

    Assertions:
    - `response.status_code == 200` and the response renders without raising (proves the no-sidecar-row event does not 500 — A1).
    - For the fallback event(s): `self.assertContains(response, '2px dashed rgba(0, 0, 0, 0.65)')` and
      `self.assertContains(response, <tooltip substring>)` (e.g. `'estimate'` or a longer unique slice of the D-04 copy).
    - For the verified and no-row events: assert the dashed-border marker count in the response equals the
      number of fallback events only (e.g. `self.assertEqual(response.content.decode().count('2px dashed rgba(0, 0, 0, 0.65)'), <num_fallback_events>)`), proving verified/no-row events do NOT get the dashed border.

    If `reverse('calendar:calendar')` does not resolve, fall back to the literal path the include
    produces (`/calendar/`) — confirm by reading the include in tom_common's urls — but prefer the named
    reverse. Keep `ruff check .` / `ruff format --check .` clean.
  </action>
  <verify>
    <automated>./manage.py test solsys_code.tests.test_calendar_template -v2</automated>
  </verify>
  <acceptance_criteria>
    - `solsys_code/tests/test_calendar_template.py` exists, uses `Client` + `TestCase`, and requests the calendar page for the fixture's month/year.
    - It asserts the dashed-border marker and tooltip substring are present for fallback events, and that the dashed-border marker count equals the fallback-event count (verified + no-row events excluded).
    - It asserts the page renders with status 200 and no exception for a CalendarEvent with no sidecar row (exercises the silenced-`DoesNotExist` path — A1).
    - Both all-day and timed branches are exercised.
    - `./manage.py test solsys_code.tests.test_calendar_template` passes; `ruff` clean.
  </acceptance_criteria>
  <done>The calendar rendering test proves the dashed border + tooltip appear only for fallback events and that no-sidecar-row events render cleanly, on both branches.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB (CalendarEvent / sidecar) → rendered HTML | Template interpolates `{{ event.color }}` (existing) and a fixed tooltip string + fixed CSS literal (new). The new `title=`/`style=` additions interpolate NO DB-sourced free text. |
| User browser → calendar view | Existing read-only calendar view (TOM `AUTH_STRATEGY='READ_ONLY'`), unchanged — no new view or permission boundary added. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-04 | Tampering | New `title=`/`style=` attributes in calendar.html | low | mitigate | Both new attributes interpolate only a fixed, hardcoded tooltip sentence (D-04) and a fixed dashed-border CSS literal — never `event.proposal`/`event.title`/any DB free text. Django autoescaping continues to protect the existing `{{ event.title }}` text rendering, unchanged. No raw variable reaches the new attributes. |
| T-08-05 | Denial of service | Reverse-O2O accessor read per event in the month-grid loop (N+1) | low | accept | Per CONTEXT.md locked discretion (DISPLAY-09 deferred to v2): accept the per-event reverse-accessor read as-is for current calendar-event volume; do NOT build a batching template tag this phase. Documented mitigation lives in research for a future phase. |
| T-08-06 | Denial of service | Missing sidecar row raising on template read (would 500) | low | mitigate | The `== False` comparison + Django's silenced `ObjectDoesNotExist` (`silent_variable_failure=True`) degrade a missing row to the verified branch; the new rendering test (Task 2) asserts a no-row event renders 200 with no exception (A1). |
| T-08-SC | Tampering | npm/pip/cargo installs | n/a | accept | This plan installs no new packages; no package-legitimacy checkpoint required. |
</threat_model>

<verification>
- `./manage.py test solsys_code.tests.test_calendar_template` passes (fallback→dashed+tooltip, verified/no-row→neither, no-row→no 500, both branches).
- `./manage.py test solsys_code` (full app suite) green at wave merge.
- `ruff check .` and `ruff format --check .` clean.
- Manual spot-check (optional, covered by automated test): load `/calendar/` with a fallback event present and confirm the dashed border renders and hover shows the tooltip.
</verification>

<success_criteria>
- A fallback-labeled event is visually distinguishable (dashed border) from a verified one without opening the event or reading the title (ROADMAP Phase 8 success criterion 3 — DISPLAY-02).
- Hovering a fallback-labeled event shows the verification tooltip (criterion 4 — DISPLAY-03).
- A load_telescope_runs event (no sidecar row) renders as verified via the documented default with no error (criterion 2, read-side half).
- DISPLAY-02 and DISPLAY-03 fully delivered (both render branches + rendering test).
</success_criteria>

<output>
Create `.planning/phases/08-telescope-label-verification-sidecar/08-02-SUMMARY.md` when done.
</output>
