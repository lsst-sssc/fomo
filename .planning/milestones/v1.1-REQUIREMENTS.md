# Requirements Archive: Telescope Runs Calendar — v1.1 Classical Run Ingest

**Archived:** 2026-06-16
**Original:** .planning/REQUIREMENTS.md (Defined 2026-06-13)
**Core Value:** A `load_telescope_runs` management command turns classical-schedule run lines into accurate, idempotent `tom_calendar.CalendarEvent`s — one per observing night — using Stage 1's `telescope_runs.SITES`/`get_site()`/`sun_event()` for sunset/sunrise times.

## v1 Requirements — All Complete

### Run Line Parsing

- [x] **PARSE-01**: Parse a classical run line (e.g. `NTT EFOSC2 allocation 9-13 July`, `Magellan IMACS 13-19 July (proposed)`, `Magellan Proto-Lightspeed Jul 8-12 (proposed)`) into `(telescope, instrument, status, year, month, day1, day2)`, handling both month-before-range (`9-13 July`) and month-after-range (`Jul 8-12`) date orderings
- [x] **PARSE-02**: Hyphenated instrument names (e.g. `Proto-Lightspeed`) parse correctly as a single instrument token
- [x] **PARSE-03**: A run line with no year defaults to the current year; a run starting in late December rolls into the next calendar year

### Classical Ingest

- [x] **INGEST-01**: `load_telescope_runs` management command expands a parsed run `S..E` into `E - S + 1` nightly `CalendarEvent`s (one per evening date `d`, `start_time = sunset(d)`, `end_time = sunrise(d+1)`), using `telescope_runs.get_site()`/`sun_event()`
- [x] **INGEST-02**: Each created event sets `telescope`/`instrument` from the parsed line, with a glanceable `title` and a `description` containing the -15° dark window times plus the original run line text
- [x] **INGEST-03**: Running the command twice on the same input file does not create duplicate `CalendarEvent`s (idempotent)

## Traceability — Final Status

| Requirement | Phase | Status | Evidence |
|-------------|-------|--------|----------|
| PARSE-01 | Phase 2 | Complete — validated | 10 test methods pass in `test_telescope_runs.py`; all 3 date-range orderings covered |
| PARSE-02 | Phase 2 | Complete — validated | `Proto-Lightspeed` parses correctly; test `test_parse_run_line_month_after_hyphenated_instrument` passes |
| PARSE-03 | Phase 2 | Complete — validated | Year defaults to current year; December→January rollover test passes |
| INGEST-01 | Phase 3 | Complete — validated | `test_creates_one_event_per_night` (5 events for 9-13 July); UAT test 1 confirmed live |
| INGEST-02 | Phase 3 | Complete — validated | `test_event_fields_set_from_parsed_run`; UAT test 3 confirmed title/description live |
| INGEST-03 | Phase 3 | Complete — validated | `test_idempotent_rerun_no_duplicates`; UAT test 5 confirmed live (`unchanged: 5`) |

## Requirement Outcomes

| Requirement | Outcome | Notes |
|-------------|---------|-------|
| PARSE-01 | Validated as specified | Three regex patterns tried in order cover all known date orderings |
| PARSE-02 | Validated as specified | Instrument token is everything between telescope and date-range match |
| PARSE-03 | Validated with refinement | Year rolls to +1 only when month==12 and day2 < day1 (December-start, January-end crossing) |
| INGEST-01 | Validated as specified | `_iter_run_nights()` uses `timedelta` arithmetic for correct month/year rollover |
| INGEST-02 | Validated as specified | Description format: dark-window ISO datetimes, Status, Source line (D-06 order confirmed) |
| INGEST-03 | Validated as specified | `get_or_create` key: `(telescope, instrument, start_time)` — microsecond-strip required for stable key |

## Out of Scope (Unchanged)

| Feature | Reason |
|---------|--------|
| Stage 3 — FTS/MuSCAT4 queue window banners | Deferred; FTS queue input format is still an open item |
| Stage 4 — observation-record sync to calendar | Deferred; depends on Stages 1-3 |
| `tom_calendar` UI/template changes or DB migrations | Not needed — `CalendarEvent` already has the required fields |
| Distinguishing Magellan Baade vs Clay in `telescope` | Open item; ephemeris is identical, both at Las Campanas |
| Replacing `SITES`'s hardcoded telescope-name → obscode mapping with a data-driven `Observatory.short_name` lookup | Flagged as Stage 2+ consideration; not required for Stage 2 success criteria |
