---
phase: 11-code-refactoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - solsys_code/calendar_utils.py
  - solsys_code/management/commands/sync_lco_observation_calendar.py
autonomous: true
requirements: [REFAC-01, REFAC-02]

must_haves:
  truths:
    - "SITE_TELESCOPE_MAP, _extract_instrument, and the LCO/SOAR telescope helpers can be imported from solsys_code.calendar_utils without importing any management command module"
    - "insert_or_create_calendar_event(lookup, fields) creates, updates, or leaves a CalendarEvent unchanged and returns (event, action)"
    - "sync_lco_observation_calendar imports the moved symbols from .calendar_utils and no longer defines them locally"
    - "sync_lco_observation_calendar delegates its CalendarEvent create-or-update to insert_or_create_calendar_event; the CalendarEventTelescopeLabel sidecar write remains in the command"
    - "./manage.py test solsys_code passes with no behavior change"
  artifacts:
    - path: "solsys_code/calendar_utils.py"
      provides: "Shared LCO/SOAR telescope-mapping helpers and insert_or_create_calendar_event"
      contains: "def insert_or_create_calendar_event"
    - path: "solsys_code/management/commands/sync_lco_observation_calendar.py"
      provides: "LCO/SOAR sync command delegating to calendar_utils"
      contains: "from .calendar_utils import"
  key_links:
    - from: "solsys_code/management/commands/sync_lco_observation_calendar.py"
      to: "solsys_code/calendar_utils.py"
      via: "relative import of moved symbols + insert_or_create_calendar_event"
      pattern: "from \\.calendar_utils import"
---

<objective>
Create the new `solsys_code/calendar_utils.py` module holding (a) the LCO/SOAR
telescope-mapping and instrument-extraction symbols extracted from
`sync_lco_observation_calendar.py` (REFAC-01, D-02) and (b) the shared
`insert_or_create_calendar_event()` create-or-update helper (REFAC-02, D-03).
Then refactor `sync_lco_observation_calendar.py` to import those symbols from
`.calendar_utils` and to delegate its CalendarEvent create-or-update logic to
the shared function — removing the duplicated implementation.

Purpose: Eliminate duplicated telescope-mapping and create-or-update code so all
three management commands can share one implementation (REFAC-01, REFAC-02).
Output: New `solsys_code/calendar_utils.py`; refactored
`sync_lco_observation_calendar.py`.

This is a pure, behavior-neutral refactor. No new features, no fixture-shape
changes, no CLI/parameter changes. Per CLAUDE.md, the paired demo notebook
`docs/notebooks/pre_executed/sync_lco_observation_calendar_demo.ipynb` is
intentionally NOT in `files_modified`: the notebook exercises the management
command's public CLI, which is unchanged, and does not import the internal
helpers being relocated — CLAUDE.md exempts "pure refactors" from the paired-
notebook rule. Behavior-neutrality is verified end-to-end by the existing
`./manage.py test solsys_code` suite (D-03 Discretion: no new unit tests).
</objective>

<execution_context>
@/home/tlister/git/fomo_devel/.claude/gsd-core/workflows/execute-plan.md
@/home/tlister/git/fomo_devel/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-code-refactoring/11-CONTEXT.md
@solsys_code/management/commands/sync_lco_observation_calendar.py
@solsys_code/management/commands/sync_gemini_observation_calendar.py
@solsys_code/telescope_runs.py
</context>

<artifacts_this_phase_produces>
New file:
- `solsys_code/calendar_utils.py`

New symbol (created in this plan):
- `insert_or_create_calendar_event(lookup: dict[str, Any], fields: dict[str, Any]) -> tuple[CalendarEvent, str]`

Symbols relocated into `solsys_code/calendar_utils.py` (moved verbatim from
`sync_lco_observation_calendar.py`, now importable from `calendar_utils`):
- Constants: `SITE_TELESCOPE_MAP`, `_API_TIMEOUT_SECONDS`, `_SCIENCE_CONFIGURATION_TYPES`, `_MUSCAT_CHANNEL_SUFFIXES`
- Exception: `InstrumentExtractionError`
- Functions: `_aperture_class_from_telescope_code`, `_derive_telescope`, `_resolve_placement_block`, `_coarse_telescope_label`, `_extract_instrument`, `_find_science_config`, `_find_exposure_signal_config`, `_has_muscat_exposure_signal`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Create solsys_code/calendar_utils.py with extracted symbols and the shared create-or-update helper</name>
  <files>solsys_code/calendar_utils.py</files>
  <read_first>
    - solsys_code/management/commands/sync_lco_observation_calendar.py (source of the verbatim symbols and their docstrings/comments to move)
    - solsys_code/management/commands/sync_gemini_observation_calendar.py (reference create-or-update idiom at lines 162-173 — the target form for insert_or_create_calendar_event)
    - solsys_code/solsys_code_observatory/utils.py (existing solsys_code utility-module precedent: no __all__, public-by-naming)
    - .planning/phases/11-code-refactoring/11-CONTEXT.md (D-01, D-02, D-03 and the Claude's Discretion save-behavior note)
  </read_first>
  <action>
    Create the new module `solsys_code/calendar_utils.py` (a flat module, not a
    sub-package, mirroring `solsys_code/telescope_runs.py`). Add a module
    docstring describing it as shared LCO/SOAR telescope-mapping helpers and the
    CalendarEvent create-or-update helper.

    Move these symbols VERBATIM (identical code, docstrings, and inline comments)
    out of `sync_lco_observation_calendar.py` into this module, per D-02:
    constants `SITE_TELESCOPE_MAP`, `_API_TIMEOUT_SECONDS`,
    `_SCIENCE_CONFIGURATION_TYPES`, `_MUSCAT_CHANNEL_SUFFIXES`; exception
    `InstrumentExtractionError`; functions `_aperture_class_from_telescope_code`,
    `_derive_telescope`, `_resolve_placement_block`, `_coarse_telescope_label`,
    `_extract_instrument`, `_find_science_config`, `_find_exposure_signal_config`,
    `_has_muscat_exposure_signal`. Do NOT move `_failure_prefix`,
    `_FAILURE_PREFIX_BY_STATUS`, `_title_for`, `_time_window`,
    `_build_event_fields`, or `_parse_proposal_arg` — those stay in the command
    (Task 2 removes the moved ones from the command).

    Add the imports the moved code requires: `from typing import Any`,
    `from urllib.parse import urljoin`, `import requests`,
    `from django import forms`, `from tom_common.exceptions import
    ImproperCredentialsException`, `from tom_observations.facilities.lco import
    LCOFacility`, `from tom_observations.facilities.ocs import make_request`, and
    `from tom_calendar.models import CalendarEvent`. Keep the `facility:
    LCOFacility` type hint on `_resolve_placement_block` exactly as in the source
    (it accepts an LCOFacility or SOARFacility at runtime).

    Add the new function `insert_or_create_calendar_event(lookup: dict[str, Any],
    fields: dict[str, Any]) -> tuple[CalendarEvent, str]` with a Google-style
    docstring (D103 is enforced). Implement it to mirror the Gemini idiom: call
    `CalendarEvent.objects.get_or_create(**lookup, defaults=fields)`; if created,
    return `(event, 'created')`; otherwise compute `changed = [f for f, v in
    fields.items() if getattr(event, f) != v]`, and if `changed` is non-empty set
    every field from `fields` onto the event and call
    `event.save(update_fields=changed)` then return `(event, 'updated')`, else
    return `(event, 'unchanged')` with no save (preserving the no-churn contract).
    Document that `action` is one of `'created'`, `'updated'`, `'unchanged'` and
    that callers own counter updates and any sidecar writes.

    Follow project style: single quotes, 120-char lines, no `__all__`,
    public-by-naming (no leading underscore = public). Do not add `from __future__
    import annotations`.
  </action>
  <verify>
    <automated>python -c "import django,os; os.environ.setdefault('DJANGO_SETTINGS_MODULE','src.fomo.settings'); django.setup(); from solsys_code.calendar_utils import SITE_TELESCOPE_MAP, InstrumentExtractionError, _extract_instrument, _derive_telescope, _resolve_placement_block, _coarse_telescope_label, _aperture_class_from_telescope_code, _find_science_config, _find_exposure_signal_config, _has_muscat_exposure_signal, insert_or_create_calendar_event; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `solsys_code/calendar_utils.py` exists and imports cleanly under Django settings `src.fomo.settings`.
    - All twelve relocated symbols plus `insert_or_create_calendar_event` are importable from `solsys_code.calendar_utils` (the verify command exits 0).
    - `grep -n "def insert_or_create_calendar_event" solsys_code/calendar_utils.py` returns one match with signature `(lookup: dict[str, Any], fields: dict[str, Any]) -> tuple[CalendarEvent, str]`.
    - The function returns the literal strings `'created'`, `'updated'`, `'unchanged'` (verifiable: `grep -c "'unchanged'" solsys_code/calendar_utils.py` >= 1 and each literal present).
    - `event.save(update_fields=changed)` appears in the function body (no bare `event.save()` in `insert_or_create_calendar_event`).
    - `ruff check solsys_code/calendar_utils.py` and `ruff format --check solsys_code/calendar_utils.py` are clean (no F401 unused-import, no D103 missing-docstring).
  </acceptance_criteria>
  <done>calendar_utils.py exists with all D-02 symbols and insert_or_create_calendar_event; imports cleanly; ruff clean.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor sync_lco_observation_calendar.py to import from .calendar_utils and use insert_or_create_calendar_event</name>
  <files>solsys_code/management/commands/sync_lco_observation_calendar.py</files>
  <read_first>
    - solsys_code/management/commands/sync_lco_observation_calendar.py (current state — the symbols to delete and the get_or_create block at lines 617-628 to replace; sidecar write at lines 635-637 to keep)
    - solsys_code/calendar_utils.py (the module created in Task 1 — confirm the exported symbol names before importing)
  </read_first>
  <action>
    Delete from this file the twelve symbols moved into `calendar_utils.py` in
    Task 1 (constants `SITE_TELESCOPE_MAP`, `_API_TIMEOUT_SECONDS`,
    `_SCIENCE_CONFIGURATION_TYPES`, `_MUSCAT_CHANNEL_SUFFIXES`; exception
    `InstrumentExtractionError`; functions `_aperture_class_from_telescope_code`,
    `_derive_telescope`, `_resolve_placement_block`, `_coarse_telescope_label`,
    `_extract_instrument`, `_find_science_config`, `_find_exposure_signal_config`,
    `_has_muscat_exposure_signal`). Keep `_failure_prefix`,
    `_FAILURE_PREFIX_BY_STATUS`, `_title_for`, `_time_window`,
    `_build_event_fields`, `_parse_proposal_arg`, and the `Command` class.

    Add a relative import from `.calendar_utils` bringing in exactly the symbols
    still referenced by the retained code: `InstrumentExtractionError`,
    `_coarse_telescope_label`, `_derive_telescope`, `_extract_instrument`,
    `_resolve_placement_block`, and `insert_or_create_calendar_event`.
    (`_build_event_fields` calls `_extract_instrument`, `_coarse_telescope_label`,
    `_resolve_placement_block`, `_derive_telescope` and raises
    `InstrumentExtractionError`; `handle()` catches `InstrumentExtractionError`.)

    Remove now-unused top-of-file imports that were only needed by the moved
    functions: `requests`, `from django import forms`, `from urllib.parse import
    urljoin`, `from tom_common.exceptions import ImproperCredentialsException`,
    `from tom_observations.facilities.ocs import make_request`. Keep imports still
    used by retained code: `datetime`/`timezone`, `Any`, `BaseCommand`/
    `CommandParser`, `CalendarEvent`, `LCOFacility`, `SOARFacility`,
    `ObservationRecord`, and `CalendarEventTelescopeLabel`.

    Replace the create-or-update block in `handle()` (currently
    `CalendarEvent.objects.get_or_create(url=url, defaults=fields)` followed by the
    `if created` / `else` compare-and-`event.save()` branch, lines ~617-628) with
    a single call: `event, action = insert_or_create_calendar_event({'url': url},
    fields)`, then increment `counters[record.facility][action]` (since `action`
    is exactly one of `'created'`/`'updated'`/`'unchanged'`, matching existing
    counter keys). Keep the `url = fields.pop('url')` and `telescope_api_failed =
    fields.pop('telescope_api_failed')` pops and the telescope-API-failed stderr
    line/counter exactly as they are. KEEP the
    `CalendarEventTelescopeLabel.objects.update_or_create(event=event,
    defaults={'is_verified': not telescope_api_failed})` sidecar write as a
    separate statement after the call (D-03: LCO-specific, not folded into the
    shared function).

    Do not introduce the word "upsert" anywhere. Single quotes, 120-char lines.
  </action>
  <verify>
    <automated>./manage.py test solsys_code 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "^(SITE_TELESCOPE_MAP|_API_TIMEOUT_SECONDS|_SCIENCE_CONFIGURATION_TYPES|_MUSCAT_CHANNEL_SUFFIXES) *=" solsys_code/management/commands/sync_lco_observation_calendar.py` returns no matches (constants removed).
    - `grep -nE "^def (_aperture_class_from_telescope_code|_derive_telescope|_resolve_placement_block|_coarse_telescope_label|_extract_instrument|_find_science_config|_find_exposure_signal_config|_has_muscat_exposure_signal)\b|^class InstrumentExtractionError\b" solsys_code/management/commands/sync_lco_observation_calendar.py` returns no matches (moved definitions removed).
    - `grep -n "from .calendar_utils import" solsys_code/management/commands/sync_lco_observation_calendar.py` returns one match importing the six required symbols.
    - `grep -n "insert_or_create_calendar_event(" solsys_code/management/commands/sync_lco_observation_calendar.py` returns one call site using `{'url': url}` as the lookup.
    - `grep -n "CalendarEventTelescopeLabel.objects.update_or_create" solsys_code/management/commands/sync_lco_observation_calendar.py` still returns one match (sidecar write retained).
    - `grep -ic "upsert" solsys_code/management/commands/sync_lco_observation_calendar.py` returns 0.
    - `./manage.py test solsys_code` reports OK with the same test count as before (no failures, no errors) — behavior-neutral.
    - `ruff check solsys_code/management/commands/sync_lco_observation_calendar.py` and `ruff format --check` are clean (no F401 unused imports remain).
  </acceptance_criteria>
  <done>sync_lco no longer defines the moved symbols, imports them from .calendar_utils, uses insert_or_create_calendar_event, keeps the sidecar write, and the full solsys_code test suite passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LCO Observation Portal API → `_resolve_placement_block` | Untrusted JSON response parsed when resolving a placed record's block. This boundary is relocated from the command into `calendar_utils.py` unchanged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering/Info-disclosure | `_resolve_placement_block` (relocated) | accept | Behavior is moved verbatim; the existing broad `except` that never logs/stringifies the caught exception (SYNC-09/D-11) and `.get()`-based block-field access (T-07-03) are preserved unchanged. No new attack surface introduced by relocation. |
| T-11-02 | Tampering | refactor correctness | mitigate | Behavior-neutrality enforced by the full `./manage.py test solsys_code` suite (186 tests) gating Task 2; any semantic drift fails the gate. |
</threat_model>

<verification>
- `solsys_code/calendar_utils.py` imports cleanly and exports all D-02 symbols plus `insert_or_create_calendar_event`.
- `sync_lco_observation_calendar.py` defines none of the moved symbols and imports them from `.calendar_utils`.
- `./manage.py test solsys_code` passes (no failures/errors), confirming behavior-neutrality.
- `ruff check .` and `ruff format --check .` clean for both modified files.
</verification>

<success_criteria>
- REFAC-01: `SITE_TELESCOPE_MAP`, `_extract_instrument`, and related LCO/SOAR helpers are importable from `solsys_code.calendar_utils` without importing any management command module.
- REFAC-02 (LCO portion): `sync_lco_observation_calendar` delegates its CalendarEvent create-or-update to `insert_or_create_calendar_event()`; the prior duplicated get_or_create/save block is gone.
- All `./manage.py test solsys_code` tests pass with no behavior change.
</success_criteria>

<output>
Create `.planning/phases/11-code-refactoring/11-01-SUMMARY.md` when done.
</output>
