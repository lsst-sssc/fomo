---
phase: 11-code-refactoring
plan: 02
type: execute
wave: 2
depends_on: ['11-01']
files_modified:
  - solsys_code/management/commands/load_telescope_runs.py
  - solsys_code/management/commands/sync_gemini_observation_calendar.py
  - docs/design/telescope_runs_calendar.rst
  - .planning/MILESTONES.md
autonomous: true
requirements: [REFAC-02]

must_haves:
  truths:
    - "load_telescope_runs delegates its CalendarEvent create-or-update to insert_or_create_calendar_event; the prior get_or_create/save block is gone"
    - "sync_gemini_observation_calendar delegates its CalendarEvent create-or-update to insert_or_create_calendar_event; the prior get_or_create/save block is gone"
    - "The word 'upsert' does not appear in docs/design/telescope_runs_calendar.rst or .planning/MILESTONES.md"
    - "./manage.py test solsys_code passes with no behavior change"
  artifacts:
    - path: "solsys_code/management/commands/load_telescope_runs.py"
      provides: "Classical run ingest command delegating create-or-update to calendar_utils"
      contains: "from solsys_code.calendar_utils import insert_or_create_calendar_event"
    - path: "solsys_code/management/commands/sync_gemini_observation_calendar.py"
      provides: "Gemini sync command delegating create-or-update to calendar_utils"
      contains: "insert_or_create_calendar_event"
  key_links:
    - from: "solsys_code/management/commands/load_telescope_runs.py"
      to: "solsys_code/calendar_utils.py"
      via: "insert_or_create_calendar_event call with (telescope, instrument, start_time) lookup"
      pattern: "insert_or_create_calendar_event\\("
    - from: "solsys_code/management/commands/sync_gemini_observation_calendar.py"
      to: "solsys_code/calendar_utils.py"
      via: "insert_or_create_calendar_event call with url lookup"
      pattern: "insert_or_create_calendar_event\\("
---

<objective>
Complete REFAC-02 across the two remaining consumers: refactor
`load_telescope_runs.py` and `sync_gemini_observation_calendar.py` to delegate
their CalendarEvent create-or-update logic to the shared
`insert_or_create_calendar_event()` (created in Plan 11-01), removing each
command's duplicated get_or_create/compare/save block. Then replace the three
"upsert" occurrences in `docs/design/telescope_runs_calendar.rst` (2) and
`.planning/MILESTONES.md` (1) with plain English (D-04).

Purpose: Finish removing the duplicated create-or-update pattern so all three
commands share one implementation, and remove "upsert" jargon from live docs
(REFAC-02, D-04).
Output: Refactored `load_telescope_runs.py` and
`sync_gemini_observation_calendar.py`; "upsert"-free `.rst` and `MILESTONES.md`.

This is a pure, behavior-neutral refactor. No new features, no CLI/parameter
changes, no fixture-shape changes. Per CLAUDE.md, the paired demo notebooks
`docs/notebooks/pre_executed/load_telescope_runs_demo.ipynb` and
`sync_gemini_observation_calendar_demo.ipynb` are intentionally NOT in
`files_modified`: the notebooks exercise the public management-command CLI, which
is unchanged, and do not import the internal create-or-update code being
relocated — CLAUDE.md exempts "pure refactors" from the paired-notebook rule.
Behavior-neutrality is verified end-to-end by the existing `./manage.py test
solsys_code` suite (D-03 Discretion: no new unit tests).
</objective>

<execution_context>
@/home/tlister/git/fomo_devel/.claude/gsd-core/workflows/execute-plan.md
@/home/tlister/git/fomo_devel/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-code-refactoring/11-CONTEXT.md
@.planning/phases/11-code-refactoring/11-01-PLAN.md
@solsys_code/management/commands/load_telescope_runs.py
@solsys_code/management/commands/sync_gemini_observation_calendar.py
</context>

<artifacts_this_phase_produces>
No new symbols. This plan consumes `insert_or_create_calendar_event` (created in
Plan 11-01) from two additional call sites and edits two documentation files.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Refactor load_telescope_runs.py to use insert_or_create_calendar_event</name>
  <files>solsys_code/management/commands/load_telescope_runs.py</files>
  <read_first>
    - solsys_code/management/commands/load_telescope_runs.py (current state — the get_or_create/compare/save block at lines 91-112 to replace)
    - solsys_code/calendar_utils.py (the insert_or_create_calendar_event signature and return contract from Plan 11-01)
  </read_first>
  <action>
    Add `from solsys_code.calendar_utils import insert_or_create_calendar_event`
    to the imports (absolute import is acceptable here, matching the existing
    `from solsys_code.telescope_runs import ...` style already in this file;
    relative `from ..calendar_utils import ...` is also acceptable — match the
    file's existing import convention, which uses the absolute
    `solsys_code....` form).

    Replace the create-or-update block (currently
    `CalendarEvent.objects.get_or_create(telescope=parsed.telescope,
    instrument=parsed.instrument, start_time=start_time, defaults={'end_time':
    end_time, 'title': title, 'description': description})` followed by the
    `if created` / `else` compare-and-`event.save()` branch, lines ~91-112) with a
    single call:
    `event, action = insert_or_create_calendar_event(
        {'telescope': parsed.telescope, 'instrument': parsed.instrument, 'start_time': start_time},
        {'end_time': end_time, 'title': title, 'description': description})`.
    Then map `action` to the existing per-line counters: increment
    `created_count` when `action == 'created'`, `updated_count` when `'updated'`,
    `unchanged_count` when `'unchanged'`. Keep the surrounding loop, the
    `_iter_run_nights` helper, the field derivation (sunset/sunrise/dark-window,
    title, description), the `except (ValueError, Observatory.DoesNotExist)`
    handler, and the final summary line exactly as they are.

    Note the lookup keys `(telescope, instrument, start_time)` are NOT part of the
    `fields` dict — they are the idempotency key, so they must not appear in the
    second argument (matching the current `defaults` which excludes them). If the
    `CalendarEvent` import becomes unused after this change, remove it; otherwise
    keep it. Single quotes, 120-char lines. Do not introduce the word "upsert".
  </action>
  <verify>
    <automated>./manage.py test solsys_code 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "insert_or_create_calendar_event" solsys_code/management/commands/load_telescope_runs.py` returns the import plus exactly one call site.
    - The call's lookup dict contains keys `telescope`, `instrument`, `start_time` and the fields dict contains `end_time`, `title`, `description` (and none of the lookup keys).
    - `grep -n "get_or_create" solsys_code/management/commands/load_telescope_runs.py` returns no matches (duplicated block removed).
    - `created_count`, `updated_count`, `unchanged_count` are each incremented based on the returned `action` value.
    - `./manage.py test solsys_code` reports OK with no failures/errors.
    - `ruff check` and `ruff format --check` clean for this file (no unused `CalendarEvent` import left behind).
  </acceptance_criteria>
  <done>load_telescope_runs delegates create-or-update to insert_or_create_calendar_event with the (telescope, instrument, start_time) lookup; tests pass; ruff clean.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor sync_gemini_observation_calendar.py to use insert_or_create_calendar_event</name>
  <files>solsys_code/management/commands/sync_gemini_observation_calendar.py</files>
  <read_first>
    - solsys_code/management/commands/sync_gemini_observation_calendar.py (current state — the get_or_create/compare/save block at lines 162-173 to replace)
    - solsys_code/calendar_utils.py (the insert_or_create_calendar_event signature and return contract from Plan 11-01)
  </read_first>
  <action>
    Add `from solsys_code.calendar_utils import insert_or_create_calendar_event`
    to the imports (match this file's existing absolute-import style, e.g.
    `from tom_calendar.models import CalendarEvent`).

    Replace the create-or-update block (currently `event, created_flag =
    CalendarEvent.objects.get_or_create(url=url, defaults=fields)` followed by the
    `if created_flag` / `else` compare-and-`event.save(update_fields=changed)`
    branch, lines ~162-173) with a single call:
    `event, action = insert_or_create_calendar_event({'url': url}, fields)`.
    Then map `action` to the existing per-site counters:
    `counters[site_key][action] += 1` (since `action` is exactly one of
    `'created'`/`'updated'`/`'unchanged'`, matching the counter keys). Keep all
    surrounding logic unchanged: the password-strip `safe_params` (GEM-SECURE-01),
    site/telescope derivation, obsid/window/instrument derivation, title building,
    the `url` construction, the `fields` dict assembly, the
    `except (KeyError, ValueError)` handler, and the summary lines.

    If `CalendarEvent` is no longer referenced after this change, remove its
    import; otherwise keep it. Single quotes, 120-char lines. Do not introduce the
    word "upsert".
  </action>
  <verify>
    <automated>./manage.py test solsys_code 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "insert_or_create_calendar_event" solsys_code/management/commands/sync_gemini_observation_calendar.py` returns the import plus exactly one call site using `{'url': url}` as the lookup.
    - `grep -n "get_or_create" solsys_code/management/commands/sync_gemini_observation_calendar.py` returns no matches (duplicated block removed).
    - `counters[site_key][action] += 1` (or an equivalent action-keyed increment) is present; the explicit `if created_flag` / `else` compare-and-save branch is gone.
    - The `safe_params` password-strip line (GEM-SECURE-01) remains unchanged.
    - `./manage.py test solsys_code` reports OK with no failures/errors.
    - `ruff check` and `ruff format --check` clean for this file.
  </acceptance_criteria>
  <done>sync_gemini delegates create-or-update to insert_or_create_calendar_event with the url lookup; password-strip preserved; tests pass; ruff clean.</done>
</task>

<task type="auto">
  <name>Task 3: Replace "upsert" with plain English in design doc and MILESTONES</name>
  <files>docs/design/telescope_runs_calendar.rst, .planning/MILESTONES.md</files>
  <read_first>
    - docs/design/telescope_runs_calendar.rst (lines ~222 and ~254 — the two "upsert" occurrences and their sentence context)
    - .planning/MILESTONES.md (line ~60 — the one "upsert" occurrence and its sentence context)
    - .planning/phases/11-code-refactoring/11-CONTEXT.md (D-04 — plain-English replacement, no "upsert" anywhere)
  </read_first>
  <action>
    In `docs/design/telescope_runs_calendar.rst`:
    - Line ~222: change "the sync **upserts** a ``CalendarEvent``" to "the sync
      **creates or updates** a ``CalendarEvent``".
    - Line ~254: change "upsert ``CalendarEvent`` rows keyed on the portal ``url``"
      to "create or update ``CalendarEvent`` rows keyed on the portal ``url``".

    In `.planning/MILESTONES.md`:
    - Line ~60: change "upsert via ``get_or_create`` keyed on
      ``(telescope, instrument, start_time)`` with conditional save." to
      "creating or updating via ``get_or_create`` keyed on
      ``(telescope, instrument, start_time)`` with conditional save."

    Preserve all surrounding RST/Markdown formatting (double-backtick code spans,
    bold markers, sentence punctuation). Do not reflow or otherwise edit
    unrelated text. This aligns with CLAUDE.md's plain-English-over-DB-jargon
    convention (no "upsert" in planning/docs artifacts).
  </action>
  <verify>
    <automated>grep -ic "upsert" docs/design/telescope_runs_calendar.rst .planning/MILESTONES.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -ic "upsert" docs/design/telescope_runs_calendar.rst` returns 0.
    - `grep -ic "upsert" .planning/MILESTONES.md` returns 0.
    - `grep -n "creates or updates" docs/design/telescope_runs_calendar.rst` returns the replacement at the former line ~222; "create or update" present at former line ~254.
    - `.planning/MILESTONES.md` line ~60 reads "creating or updating via ``get_or_create``".
    - No other lines in either file changed (diff limited to the three sentences).
  </acceptance_criteria>
  <done>Zero "upsert" occurrences remain in either file; replacements read naturally; no unrelated edits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure refactor + doc edit. No new trust boundary is introduced or moved; the relocated network-parsing boundary was handled in Plan 11-01. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03 | Tampering | refactor correctness | mitigate | Behavior-neutrality enforced by the full `./manage.py test solsys_code` suite gating Tasks 1 and 2; any semantic drift fails the gate. |
| T-11-04 | Info-disclosure | sync_gemini password handling | mitigate | The `safe_params` password-strip (GEM-SECURE-01) is explicitly preserved; acceptance criteria assert it remains unchanged. |
</threat_model>

<verification>
- Both commands delegate create-or-update to `insert_or_create_calendar_event` and no longer call `CalendarEvent.objects.get_or_create` directly.
- `./manage.py test solsys_code` passes (no failures/errors), confirming behavior-neutrality across all three commands.
- Zero "upsert" occurrences in `docs/design/telescope_runs_calendar.rst` and `.planning/MILESTONES.md`.
- `ruff check .` and `ruff format --check .` clean for both modified command files.
</verification>

<success_criteria>
- REFAC-02 (complete): all three commands (`load_telescope_runs`,
  `sync_lco_observation_calendar`, `sync_gemini_observation_calendar`) delegate
  their CalendarEvent create-or-update to `insert_or_create_calendar_event()`; the
  duplicated blocks are absent from each command.
- The word "upsert" does not appear in `docs/design/telescope_runs_calendar.rst`
  or `.planning/MILESTONES.md`.
- All `./manage.py test solsys_code` tests pass with no behavior change.
</success_criteria>

<output>
Create `.planning/phases/11-code-refactoring/11-02-SUMMARY.md` when done.
</output>
