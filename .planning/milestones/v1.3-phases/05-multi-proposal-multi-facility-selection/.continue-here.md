---
context: phase
phase: 05-multi-proposal-multi-facility-selection
task: null
total_tasks: null
status: in_progress
last_updated: 2026-06-19T06:06:49.356Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: node-version — `gsd-tools.cjs` requires Node 20 (`||=` syntax); the default
  `node` on PATH is v14 and throws `SyntaxError: Unexpected token '||='`. — Run
  `source ~/.nvm/nvm.sh && nvm use 20` (or equivalent) **before every** `gsd-tools.cjs` /
  `gsd_run` invocation in any new shell.
- [ ] CONSTRAINT: roadmap-detail-sections — `gsd-tools roadmap.get-phase` only reads
  `### Phase N:` sections **inlined in `.planning/ROADMAP.md` itself** — it does not follow
  `See milestones/vX.Y-ROADMAP.md` pointer notes. The v1.3 milestone roadmap creation (commit
  `80f5147`) deviated from the established convention (compare `ce7de9e` for v1.2, which did
  inline detail while active) and left Phase 5/6/7 detail only in
  `milestones/v1.3-ROADMAP.md`. This was already fixed and committed (`ed182c0`) — if you see
  `"error": "malformed_roadmap"` again for phase 6 or 7 when their turn comes, the same fix
  (copy the `### Phase N:` block from the milestone file into the main `ROADMAP.md`'s
  `## Phase Details` section) applies.

**Do not proceed until both boxes are checked / re-verified true.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| gsd_run query commit silently truncating | `gsd_run query commit "..." --files ...` returned `committed: false` with output cut off mid pre-commit hook (right after "Run unit tests....", no Passed/Failed), on two separate attempts including with a 540s Bash timeout. | advisory | Verify hooks actually pass with a direct `pre-commit run --files <path>` first (fast, ~12s, all green incl. unit tests on this repo), then fall back to a plain `git commit -m "$(cat <<'EOF' ... EOF)"` heredoc instead of the `gsd_run` wrapper if the wrapper misbehaves again. |

<current_state>
Mid `/gsd:plan-phase 5` orchestrator run, in `.claude/gsd-core/workflows/plan-phase.md` step 5.1
("Standard Research Decision"). All required-reading files for plan-phase have been loaded this
session (ui-brand.md, revision-loop.md, gate-prompts.md, agent-contracts.md, gates.md). No
research, planner, or plan-checker subagent has been spawned yet — nothing past orchestrator
setup has happened.

The init JSON for phase 5 (`gsd_run query init.plan-phase 5`) resolves cleanly:
- `phase_dir`: `.planning/phases/05-multi-proposal-multi-facility-selection`
- `phase_req_ids`: `SELECT-02, SELECT-03, SELECT-04, SELECT-05`
- `has_context`: true, `has_research`: false, `has_plans`: false, `phase_status`: "Pending"
- `MVP_MODE`: false (config default, no per-phase override)
- `context_path`: `.planning/phases/05-multi-proposal-multi-facility-selection/05-CONTEXT.md`

05-CONTEXT.md has been read and is fully decided (D-01 through D-08, all locked, "Claude's
Discretion" only covers cosmetic formatting choices). AI-SPEC gate (step 4.5) was checked and
correctly skipped — phase goal has no AI/LLM keywords.

An `AskUserQuestion` was presented for the step-5.1 research gate ("Research before planning
Phase 5...?") with "Skip research" recommended (because CONTEXT.md already pins exact
file/line locations in the existing `sync_lco_observation_calendar.py`, exact helper
signatures, and the `SOARFacility(LCOFacility)` subclass relationship — this is a well-scoped
extension of known code, not new architecture). The user's reply was a context/token-budget
warning ("almost out of tokens, save checkpoint here"), **not** an answer to the question. The
research decision is still open.
</current_state>

<completed_work>

Completed Tasks:
- Activated Serena project + read instructions manual — Done
- Loaded plan-phase.md + all 4 required-reading reference files — Done
- Ran `init.plan-phase 5`, hit `roadmap.get-phase 5` returning `found:false` / `malformed_roadmap` — Done (diagnosed)
- Diagnosed root cause via git history comparison (v1.2's `ce7de9e` vs v1.3's `80f5147`) — Done
- Fixed `.planning/ROADMAP.md` by inlining `### Phase 5/6/7:` detail sections (copied verbatim
  from `milestones/v1.3-ROADMAP.md`, which already had the content) — Done
- Verified fix with `gsd_run query roadmap.get-phase 5` (now `found: true` with full goal/success_criteria/section) — Done
- Committed the ROADMAP.md fix as `ed182c0` (plain `git commit`, not the `gsd_run` wrapper — see anti-pattern above) — Done
- Re-ran `init.plan-phase 5` to confirm `phase_req_ids` now resolves — Done
- Read `05-CONTEXT.md` in full — Done
- AI-SPEC gate (step 4.5): checked, correctly skipped (no AI keywords) — Done
- Step 5.1 research-decision gate: question presented to user — **In progress, unanswered**
</completed_work>

<remaining_work>

- Re-present (or otherwise resolve) the step-5.1 research-decision question. If resuming and the
  user has no further input, "Skip research" is the recommended default given how complete
  05-CONTEXT.md already is — but do not silently assume it; confirm with the user first unless
  explicitly told to just proceed.
- If "Skip research" → proceed to plan-phase.md step 6 (Check Existing Plans: none exist yet for
  phase 5, so just continue) → step 7 (load context paths, already known) → step 7.5 (Nyquist
  artifacts check — research_enabled is true and has_research is currently false with no
  `--research` flag, so this gate's no-research-path exemption applies: Nyquist is "not
  applicable for this run", skip silently) → step 7.8 (pattern mapper, optional — check
  `workflow.pattern_mapper` config, likely true by default; CONTEXT.md exists so it has files to
  extract) → step 7.9 (API-SURFACE regen, gated on `intel.enabled`, likely false) → step 8 (spawn
  `gsd-planner` Agent — this is the first real subagent spawn of this run).
- After planner returns `## PLANNING COMPLETE`: step 10 (spawn `gsd-plan-checker`), step 12
  revision loop if needed (max 3 iterations), step 13/13a (Requirements + Decision coverage
  gates — phase_req_ids is `SELECT-02..05`, all must appear in some plan's `requirements`
  field), step 13b/13c/13d (STATE.md update, ROADMAP wave annotation, commit PLAN.md files).
- Step 14: present final status / Next Up block to user (offer `/gsd-execute-phase 5`).
</remaining_work>

<decisions_made>

- Inlined Phase 5/6/7 detail into `.planning/ROADMAP.md` rather than trying to make
  `roadmap.get-phase` follow the `milestones/v1.3-ROADMAP.md` pointer note, because the tool's
  behavior is consistent with how every prior *active* milestone (v1.1, v1.2) was structured —
  this is a project convention mismatch / bug introduced when v1.3's roadmap was created, not a
  tool limitation to work around differently.
- Recommended "Skip research" over "Research first" for Phase 5, because CONTEXT.md's
  `<canonical_refs>` and `<code_context>` sections already contain the kind of detail research
  would normally surface (exact existing function names, line numbers, library subclass
  relationships, test-fixture conventions) — research would be largely redundant here.
</decisions_made>

<blockers>
- Research-decision gate unanswered: blocks step 6 onward until resolved. Workaround: re-ask on
  resume, or take explicit instruction to proceed with skip-research.
- (Resolved) Node v14 default on PATH breaks gsd-tools.cjs: workaround is `nvm use 20` per shell.
- (Resolved) `gsd_run query commit` truncated/failed silently: workaround is direct
  `pre-commit run` + plain `git commit` heredoc.
</blockers>

## Required Reading (in order)
1. `.planning/phases/05-multi-proposal-multi-facility-selection/05-CONTEXT.md` — the locked
   decisions (D-01..D-08) every plan for this phase must cover.
2. `.claude/gsd-core/workflows/plan-phase.md` — the orchestrator workflow this session is
   mid-way through (currently at step 5.1).
3. `.planning/milestones/v1.3-ROADMAP.md` §"Phase 5" — original source of truth for the
   success criteria now also inlined in `.planning/ROADMAP.md`.
4. `.planning/HANDOFF.json` — machine-readable mirror of this file, for `/gsd-resume-work`.

## Infrastructure State
- No background processes running. No servers started. No subagents currently active (none
  have been spawned yet this run).
- Git branch `issue37-telescope-runs-calendar`, 11 commits ahead of
  `origin/issue37-telescope-runs-calendar` (10 pre-existing + this session's `ed182c0`).
- Working tree has pre-existing, unrelated changes not touched this session:
  `src/fomo/settings.py` (modified, unstaged) and `docs/notebooks/eso_programmatic.py`
  (untracked). Leave these alone — they predate this session and are not part of Phase 5 work.

<context>
This is a faithful in-progress `/gsd:plan-phase 5` run, paused at the very first interactive gate
(research decision) because the user flagged they were low on context tokens. No scope creep
occurred — the only code/doc change made was the ROADMAP.md structural fix, which was a genuine
bug blocking the workflow tooling itself (verified by reproducing the failure, diagnosing root
cause via git history, and confirming the fix resolves it), not a tangential improvement.
</context>

<next_action>
Start with: re-ask the user the step-5.1 research-decision question for Phase 5 ("Research
first" vs "Skip research", recommending Skip given how complete 05-CONTEXT.md is). Then resume
plan-phase.md from step 6 onward through to spawning `gsd-planner`.
</next_action>
