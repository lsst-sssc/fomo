---
phase: 26-canonical-record-spike
plan: 03
type: execute
wave: 3
depends_on: ["26-01", "26-02"]
files_modified:
  - .planning/phases/26-canonical-record-spike/26-DECISION.md
  - docs/design/canonical_record_spike.rst
  - docs/design/design.rst
scratch_artifacts:
  # This plan CREATES no throwaway artifacts. The list below is the DISCARD CHECKLIST — the
  # union of plans 26-01 and 26-02's scratch_artifacts. Every entry must be gone from disk (or,
  # for tracked files, restored to its committed state) by the end of task 3.
  - local_settings.py                                              # rm -f; then `ls` must error
  - tmp/                                                           # rm -rf; then `ls` must error
  - tmp/26_snapshot_probe.py
  - tmp/26_row_counts.py
  - tmp/26_integrity_check.py
  - tmp/26_reconciler_prototype.py
  - tmp/26-spike-db-snapshot.sqlite3
  - tmp/26-spike-db-copy.sqlite3
  - tmp/26-adopt-copy.sqlite3
  - tmp/26-gapfill-copy.sqlite3
  - tmp/26-rejected-baseline-copy.sqlite3
  - tmp/26-baseline-snapshot.txt
  - tmp/26-row-counts-before.txt
  - tmp/26-row-counts-after.txt
  - tmp/26-realdb-fingerprint-before.txt
  - tmp/26-rename-measurement.txt
  - tmp/26-integrity-check.txt
  - tmp/26-prototype-counts.txt
  - spike/26-canonical-record-probe                                # git branch -D; never merged
  - solsys_code/models.py                                          # must be unmodified on the real branch
  - solsys_code/migrations/0008_scratch_canonical_record_probe.py  # must not exist on the real branch
  - solsys_code/admin.py                                           # must be unmodified
  - solsys_code/management/commands/sync_lco_observation_calendar.py  # must be unmodified
  - solsys_code/tests/test_admin.py                                # must be unmodified
  - solsys_code/tests/test_calendar_template.py                    # must be unmodified
  - solsys_code/tests/test_sync_lco_observation_calendar.py        # must be unmodified
  - solsys_code/tests/test_load_telescope_runs.py                  # must be unmodified
autonomous: false
requirements: [SPIKE-01, SPIKE-02, SPIKE-03, SPIKE-04]
user_setup: []

must_haves:
  truths:
    - "26-DECISION.md's `## Recommendation` section states a locked, falsifiable verdict for all four of SPIKE-01..04, each grounded in the Findings plans 26-01 and 26-02 recorded rather than asserted fresh — so Phases 27, 28 and 29 each execute a decision instead of making one."
    - "The `source` vocabulary is fixed at all six values (the five roadmap values plus LEGACY for the 31 pre-milestone rows), with the three adapter values documented as not yet produced by any code path, and `source` recorded as staying out of both existing partial unique constraints."
    - "The derivation rule `approval_status == APPROVED` AND `source != WEB` means *no approval was required* (as distinct from *a human approved this*) is recorded explicitly, so downstream code does not re-invent it and does not add a fourth approval value."
    - "One canonical reconciler event-key scheme is stated — `RUN:{run_pk}:{date}` with `{date}` always the site-local observing night — together with an explicit answer to the stage-2 fan-out question: a class-wide run produces a single class-wide event per day, not one event per candidate site."
    - "The migration and attribution strategy is stated with the companion-record rename checklist naming every integration point (admin registration, LCO sync command, view `prefetch_related` string, calendar template) plus the additional consumers plan 26-01 actually measured, and the `related_name='telescope_label_meta'`-stays-unchanged decision recorded as the reason two of them needed no edit."
    - "The folded `calendar_utils.py` private-helper todo is recorded as a naming *recommendation* Phase 27 may execute alongside its own work — not as code written here, and explicitly not closed by this phase."
    - "A durable page at `docs/design/canonical_record_spike.rst` carries the settled vocabulary, key scheme and rename checklist forward outside `.planning/`, is reachable from the `docs/design/design.rst` Design Notes toctree, and builds clean under the exact pre-commit `sphinx-build` invocation."
    - "Every throwaway artifact is gone: the scratch branch is deleted unmerged, `local_settings.py` and `tmp/` no longer exist on disk, no `solsys_code/` file differs from its committed state, and the real `src/fomo_db.sqlite3` still matches the fingerprint recorded at the start of the phase."
  artifacts:
    - path: ".planning/phases/26-canonical-record-spike/26-DECISION.md"
      provides: "Completed decision doc — Findings from plans 26-01/26-02 plus a Recommendation section covering SPIKE-01..04 and a resolved Durable-summary pointer"
      contains: "## Recommendation"
    - path: "docs/design/canonical_record_spike.rst"
      provides: "The durable, redaction-free summary of the settled vocabulary, event-key scheme and rename checklist (ROADMAP criterion 5)"
      contains: "Background"
    - path: "docs/design/design.rst"
      provides: "Design Notes toctree extended with the new page so it is reachable rather than orphaned"
      contains: "canonical_record_spike"
  key_links:
    - from: "plan 26-02's three-way prototype counts"
      to: "the adopt-vs-gap-fill verdict in 26-DECISION.md"
      via: "a blocking decision checkpoint that presents the measured numbers, not an abstract argument"
      pattern: "gap-fill"
    - from: "26-DECISION.md's Recommendation"
      to: "docs/design/canonical_record_spike.rst"
      via: "the durable summary restating the same verdicts, with the archival-path caveat pointing back at the full-detail companion"
      pattern: "26-DECISION\\.md"
    - from: "docs/design/canonical_record_spike.rst"
      to: "docs/design/design.rst toctree"
      via: "one new bare-basename line appended after uncertain_scheduling_spike"
      pattern: "canonical_record_spike"
---

<objective>
Turn plans 26-01 and 26-02's measured evidence into locked decisions, publish them where Phases
27-29 will actually read them, and leave the repository with no trace of the investigation
except the two documentation deliverables.

Three things happen here: the adopt-vs-gap-fill question is settled from the prototype's real
numbers (D-11); `26-DECISION.md`'s `## Recommendation` section is completed for all four of
SPIKE-01..04; and the durable `docs/design/canonical_record_spike.rst` page is written, wired
into the Design Notes toctree, and proven to build — followed by a full, verified discard of
every throwaway artifact the phase created.

Still **investigation-only**. This plan writes documentation only: no schema migration, no
reconciler module, no attribution UI. The scratch branch `spike/26-canonical-record-probe` is
deleted unmerged in task 3 and every `solsys_code/` file returns to its committed state.

Purpose: make every v2.2 decision durable and readable outside `.planning/`, and close the phase
with a provably clean working tree.
Output: a completed `26-DECISION.md`; a new `docs/design/canonical_record_spike.rst` and its
toctree entry; a repository with no scratch branch, no `local_settings.py`, no `tmp/`, and no
modified `solsys_code/` file.
</objective>

<execution_context>
@/home/tlister/git/fomo_devel/.claude/gsd-core/workflows/execute-plan.md
@/home/tlister/git/fomo_devel/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-canonical-record-spike/26-CONTEXT.md
@.planning/phases/26-canonical-record-spike/26-RESEARCH.md
@.planning/phases/26-canonical-record-spike/26-PATTERNS.md
@.planning/phases/26-canonical-record-spike/26-VALIDATION.md
@.planning/phases/26-canonical-record-spike/26-DECISION.md
@docs/design/uncertain_scheduling_spike.rst
@docs/design/eso_feasibility_spike.rst
@docs/design/design.rst
</context>

<phase_nature_note>
No-shippable-code investigation spike (mirrors Phase 13's and Phase 18's plan 02, which likewise
wrote only the decision doc's recommendation plus a durable `.rst`). Verification is structural
and evidence-shaped: required section headers present, the verdict actually stated, the toctree
entry present, a clean `sphinx-build`, a clean `git status`, and the literal absence on disk of
every scratch artifact.

Preconditions inherited from plans 26-01 and 26-02: `26-DECISION.md` exists with a populated
`## Findings` section (the D-04 snapshot, D-16, SPIKE-02's four adapter mappings, the
stage-2/stage-0 inventory, the RECON-07 baseline, SPIKE-04 criterion 4 (a)/(b)/(c), SPIKE-01
criterion 1, and SPIKE-03 criterion 3) plus placeholder `## Recommendation` and
`## Durable summary` headers; `tmp/26-prototype-counts.txt` holds the three-way scenario tallies;
the scratch branch and `local_settings.py` are still live at the start of this plan and are torn
down in task 3.

**CLAUDE.md paired-docs rule:** ROADMAP.md records "Paired docs: None" for Phase 26 —
investigation only, no module behaviour changes — so no demo notebook and no `docs/runbooks/`
page is triggered. The `docs/design/` page written here is a phase deliverable in its own right
(ROADMAP criterion 5), not a paired doc. Do not regenerate any notebook.
</phase_nature_note>

<terminology_note>
CLAUDE.md's planning-doc terminology rule applies to both documents written here: plain English
over database jargon. Write "create or update" / "find-or-create" / "create the record if
missing, otherwise update it in place" rather than "upsert", and "model" / "data model" rather
than "shape". This matters more than usual for the `docs/design/` page, which is published
documentation read outside the planning workflow.
</terminology_note>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Settle adopt-vs-gap-fill from the prototype's measured numbers (D-11)</name>
  <read_first>
    - tmp/26-prototype-counts.txt (plan 26-02's per-scenario summaries: in-window event counts, url-key lists, created/updated/unchanged tallies, and the three `IDEMPOTENT_RERUN` tokens)
    - .planning/phases/26-canonical-record-spike/26-DECISION.md (the `### SPIKE-03 criterion 3` Finding — the three-way comparison and the measured site-local-versus-UTC night sets)
    - .planning/phases/26-canonical-record-spike/26-CONTEXT.md (D-11's framing of the two variants and the rejected baseline; D-09's identity-versus-ownership split; the locked "no automatic merging of suspected duplicate associations" constraint)
    - .planning/REQUIREMENTS.md (RECON-05 — the reconciler never creates, modifies or deletes an event it does not own; ATTRIB-06 — attribution completable before the first full reconcile sweep)
  </read_first>
  <action>
    Read the measured evidence, form a recommendation, and pause for the user to confirm or
    override it. Write nothing to `26-DECISION.md` in this task — task 2 owns that write.

    Before pausing, prepare the decision brief from the real numbers rather than from the
    abstract argument: for each of the three scenarios, state the in-window event count actually
    produced, how many events were created versus updated in place versus left unchanged, how
    many companion rows ended up carrying a `run` FK, and the `IDEMPOTENT_RERUN` token. Then state
    which option you recommend and why, in terms of what the calendar actually looks like — that
    is CONTEXT.md's own framing, and the reason D-11 was deliberately left to measurement instead
    of being decided up front.
  </action>
  <decision>
    Which write strategy does the Phase 29 reconciler use for a run whose window is already
    partly covered by attributed adapter events: **adopt** the existing events, or **gap-fill**
    around them?
  </decision>
  <context>
    Plan 26-02 built both against the real case — `CampaignRun` pk=1, a 15-night window
    (2026-07-07 to 2026-07-21) of which 11 nights already carry LCO queue events (ids 53-63),
    each now carrying a companion row whose `run` FK points at pk=1.

    Claude will fill in the measured figures from `tmp/26-prototype-counts.txt` when presenting
    this checkpoint. The expected pattern from CONTEXT.md D-11 is 15 events for adopt, 15 for
    gap-fill, and 26 for the rejected always-mint baseline — but the presented numbers are the
    ones actually measured, and any divergence from the expectation is itself part of the brief.

    Both live options produce the same event count. The difference is who owns the 11 already
    covered nights, and therefore what breaks if two writers disagree about them.

    The third scenario — the reconciler always minting its own `RUN:1:{date}` key for all 15
    nights regardless of coverage — is **not** on the table. It is the rejected baseline, recorded
    in the decision doc as the visible double-booking that ATTRIB-06 exists to prevent.
  </context>
  <options>
    <option id="adopt">
      <name>Adopt — update the attributed event in place under its existing LCO url key</name>
      <pros>Makes the reconciler authoritative over the whole window pipeline immediately: one writer, one set of times, no dependency on v2.3's adapter rewiring before stages 3 and 4 behave consistently for the 11 LCO nights. The companion `run` FK already exists on those rows, so ownership is unambiguous.</pros>
      <cons>Biggest blast radius — the reconciler writes to events the LCO sync command also writes to, so the two can fight over the same row's title and times until v2.3 rewires the adapters. Any bug in the reconciler is a bug that mutates real, externally-keyed portal events rather than only its own synthetic ones.</cons>
    </option>
    <option id="gapfill">
      <name>Gap-fill — create nothing for a night that already has an attributed adapter event</name>
      <pros>Respects RECON-05 most strictly: the reconciler only ever touches keys it minted itself, so an un-attributed or third-party event can never be modified even by accident. The 11 originals' stored values and modification timestamps are never written by the reconciler at all.</pros>
      <cons>Stages 3 and 4 for those 11 nights keep coming from the LCO sync command until v2.3's adapter rewiring lands, so the calendar's behaviour is split by provenance for a whole milestone — two code paths producing superficially similar events with different update rules.</cons>
    </option>
  </options>
  <resume-signal>
    Reply `adopt` or `gapfill` to lock the strategy, or describe a different treatment (for
    example: gap-fill now with an explicit adopt migration deferred to v2.3) and Claude will
    record that instead.
  </resume-signal>
  <acceptance_criteria>
    - The decision brief presented to the user quotes the three measured in-window event counts,
      the created/updated/unchanged tallies, and the three `IDEMPOTENT_RERUN` tokens from
      `tmp/26-prototype-counts.txt` — not the expected figures from CONTEXT.md.
    - Claude states an explicit recommendation with a reason grounded in the measured calendar
      state before pausing.
    - The user has replied with `adopt`, `gapfill`, or an alternative treatment.
    - `26-DECISION.md` is unchanged by this task.
  </acceptance_criteria>
  <done>The adopt-vs-gap-fill strategy is locked by an explicit user decision made against the measured three-way comparison, ready for task 2 to record.</done>
</task>

<task type="auto">
  <name>Task 2: Complete 26-DECISION.md's Recommendation for SPIKE-01..04</name>
  <files>.planning/phases/26-canonical-record-spike/26-DECISION.md</files>
  <read_first>
    - .planning/phases/26-canonical-record-spike/26-DECISION.md (every Finding written by plans 26-01 and 26-02 — each recommendation must rest on one of them, never assert something fresh)
    - .planning/phases/26-canonical-record-spike/26-CONTEXT.md (D-01..D-20 in full — this section restates the locked decisions and grounds them in the Findings; D-05 class-wide semantics, D-06 the widened site-less vocabulary, D-07 space-mission spanning event, D-08 stage 0, D-09 identity versus ownership, D-10 site-local night, D-12 LEGACY, D-13 the full vocabulary, D-14 the derivation rule, D-16 the PROJECT.md discrepancy, and the Folded Todos section)
    - .planning/ROADMAP.md (§"Phase 26: Canonical-Record Spike" — the five success criteria this doc must satisfy; §"Locked constraints" — the six constraints this phase executes rather than re-opens; the Phase 27/28/29 entries, so each recommendation names the phase that consumes it)
    - .planning/REQUIREMENTS.md (§"Spike" SPIKE-01..04 verbatim; §"Out of Scope" — the six excluded approaches the recommendation must not quietly re-admit)
    - .planning/milestones/v2.1-phases/18-uncertain-scheduling-investigation-spike/18-DECISION.md (the Recommendation-section format and depth precedent: one subsection per criterion, each restating the locked decision and then grounding it in the Findings above)
    - .planning/todos/pending/2026-07-02-rename-calendar-utils-py-private-helpers-to-reflect-shared-m.md (the folded todo, recorded here as a recommendation and deliberately left open)
    - solsys_code/calendar_utils.py (lines 84, 106, 129, 229, 258 — the five cross-module-consumed underscore-prefixed helpers `_aperture_class_from_telescope_code`, `_derive_telescope`, `_resolve_placement_block`, `_extract_instrument`, `_coarse_telescope_label`)
    - solsys_code/management/commands/import_campaign_csv.py (line 194 — already writes `ApprovalStatus.APPROVED`, so the importer's real behaviour change under CANON-01 is writing `source`, not changing `approval_status`)
    - solsys_code/campaign_gap.py (the asset-aware `claimed_dates()` whose ground-versus-space treatment D-07 keeps the calendar consistent with)
  </read_first>
  <action>
    Replace the `## Recommendation` placeholder in `26-DECISION.md` with a completed section.
    Leave every Finding written by plans 26-01 and 26-02 unchanged. Mirror 18-DECISION.md's
    evidence-then-recommendation ordering: each subsection restates the locked CONTEXT.md decision
    and then points at the Finding that grounds it, rather than arguing it afresh. Name the phase
    that consumes each one (27, 28 or 29).

    `### Criterion 1 / SPIKE-01 — the source vocabulary and its constraint interaction`
    - Fix the vocabulary at six values: the five roadmap values (web submission, classical file,
      LCO queue, Gemini queue, CSV import) plus `LEGACY`. Record that the three adapter values are
      declared but **not yet produced by any code path**, awaiting v2.3's ADAPT-01..03, and that
      declaring the full set now costs nothing because Django `TextChoices` values are
      validation-only — adding or removing one later is a no-op `AlterField` (D-13).
    - Record `LEGACY` as the value the 31 pre-milestone rows get, with the reason: nothing in the
      data can discriminate provenance (`original_obs_date_raw` is set on only 2 rows, making it a
      parse-failure marker rather than an import signature), so any per-row inference would be
      guesswork. Record that blanket `CSV_IMPORT` was considered and rejected as an unverifiable
      assertion written into 31 rows, doubly uncertain for pk=1 which is on the Didymos 2026
      campaign rather than 3I/ATLAS (D-12).
    - Record that `source` and the CANON-02 field stay out of **both** existing partial unique
      constraints, citing the SPIKE-01 Finding's negative controls as the executable proof, and
      state the consequence plainly: attribution, not the constraint, is what connects
      same-physical-run rows arriving from different sources.
    - Record D-14's derivation rule verbatim as a rule downstream code reads rather than
      re-invents: `approval_status == APPROVED` **and** `source != WEB` means *no approval was
      required*, as distinct from *a human approved this*. Record that a fourth `NOT_REQUIRED`
      approval value was considered and rejected because every existing reader of
      `approval_status` — the approval-queue filters, the non-staff visibility gate,
      `CampaignRunTable`, `ApprovalQueueTable`, and `CampaignRunDecisionView`'s conditional update
      — would have to handle it, for a distinction `source` already carries.
    - Record D-17's correction so Phase 27 plans the right change: there are zero
      `PENDING_REVIEW` rows, and `import_campaign_csv.py:194` already writes
      `ApprovalStatus.APPROVED`, so the importer's real behaviour change under CANON-01 is writing
      `source`, not changing what it writes for `approval_status`.

    `### Criterion 2 / SPIKE-02 — per-adapter identity key to run`
    - Restate the four mappings from the Finding as the locked mapping Phases 27-29 consume, in a
      form that lets a reader take any existing calendar event and say which run it would belong
      to. Carry the confidence distinction forward explicitly rather than flattening it: the
      classical and LCO mappings are confirmed against real rows; the Gemini `GEM:{prog}/{obsid}`
      mapping and the `CAMPAIGN:{pk}[:{date}]` mapping are constructed-input code-path checks
      because zero rows of either kind exist in the dev DB.
    - Record the consequence D-19 forces: the classical adapter has no string identity key at all,
      so RECON-05's ownership scoping cannot lean on `url` for those 9 events — which is precisely
      why ownership lives on the companion record instead (criterion 3).

    `### Criterion 3 / SPIKE-03 — the canonical event key and the stage-2 answer`
    - State the one canonical scheme: the reconciler passes a namespaced url of the form
      `RUN:{run_pk}:{date}` as its `insert_or_create_calendar_event()` lookup, and `{date}` is
      **always** the site-local observing night derived in the site's timezone, never the UTC date
      of whatever `start_time` the current stage happens to produce. Ground it in the Finding's
      measured site-local-versus-UTC night comparison, and state the consequence: stages 3 and 4
      change an event's times but never its key.
    - Record the identity-versus-ownership split (D-09) as two separate mechanisms, with the hard
      ownership rule: no companion row, or a companion row whose `run` link is unset, means "not
      mine, never touch". Note it is already provable against the live data — the 9 classical
      events have no companion row at all.
    - Answer the stage-2 fan-out question explicitly, because ROADMAP criterion 3 requires an
      explicit answer: a class-wide run produces a **single class-wide event per day**
      (00:00–23:59, labelled with the class, no site), **not** one event per candidate site. Cite
      the measured cost from plan 26-01's inventory — pk=29's window length multiplied by the
      number of `1m0` sites in `SITE_TELESCOPE_MAP` — and label that product a computed figure
      from real field values rather than an executed check. Record that stage 3 narrows to the
      real site when an `ObservationRecord` appears, which is the pipeline working as designed.
    - Record D-07: a space-mission run gets one spanning event covering the whole window, not one
      event per day, so the calendar and `campaign_gap.claimed_dates()` do not disagree about the
      same run. Record that the uniform "treat every site-less run as daily 00:00–23:59"
      alternative was rejected for exactly that reason.
    - Record D-06: the CANON-02 field carries a three-meaning "why is there no site" vocabulary —
      telescope-class allocation, space mission, and unresolved/failed-to-resolve — not a
      telescope-class-only vocabulary, because the live data has five space-mission rows against
      two class-wide ones. State the final value set, and state a recommendation on whether the
      field keeps the name `telescope_class` given the widened meaning, so Phase 27 does not have
      to make that call mid-implementation.
    - Record D-08's stage 0, "allocated but unscheduled": a run with no window start produces no
      calendar event, but the reconciler counts and reports it in its summary the way
      `import_campaign_csv` already reports `site_needs_review` — visibly pending rather than
      silently skipped. Name the real rows this covers from plan 26-01's inventory. This gives
      RECON-06's "reported and skipped" a defined case.
    - Record the adopt-vs-gap-fill verdict locked in task 1, with the measured numbers behind it
      and the rejected always-mint baseline's count stated as the visible double-booking ATTRIB-06
      exists to prevent.

    `### Criterion 4 / SPIKE-04 — migration and attribution strategy`
    - State the migration shape as proven, not proposed: one `RenameModel` followed by the
      `AddField` operations, hand-authored rather than autodetected (non-interactive
      autodetection cannot tell a rename from a delete-plus-create, and because the companion
      record's event link is its actual primary key, that would drop the table and lose every
      row). Record that no data-migration backfill step is needed because `source` is backfilled
      by a single static field default, and that the `run` link is nullable, blankable and clears
      to null on run deletion. Cite the Finding's identical before/after row counts as the proof
      it preserved the 11 real companion rows.
    - Present the rename checklist as a table Phase 27 executes: the four integration points
      ROADMAP criterion 4 names — admin registration, the LCO sync command, the view
      `prefetch_related` string, and the calendar template — plus every additional consumer plan
      26-01 actually measured, including the admin reverse-URL name derived from the model's
      lowercased class name and the class-name references in the test modules. For each, record
      whether it broke and whether it broke loudly or silently.
    - Record the `related_name='telescope_label_meta'`-stays-unchanged decision and its reason: it
      is what makes the view prefetch and the calendar template safe by construction, and renaming
      it would break both with no static check to catch it. Record the renamed class name and why
      it was chosen for generality over a link-specific alternative that would misdescribe the 11
      existing telescope-label-only rows.
    - State the attribution strategy: ownership on the companion record's run link, no automatic
      merging of suspected duplicates, per-candidate staff confirmation only, and attribution
      completable before the first full reconcile sweep — naming Phase 28 as the consumer and
      Phase 29 as the phase that depends on it.

    `### Recorded findings that correct the planning docs`
    - Point at plan 26-01's D-16 Finding and state the instruction for Phases 27-29 plainly: do
      not trust PROJECT.md's Phase 25 paragraph. Record that correcting PROJECT.md is deliberately
      a **separate todo**, outside this phase's investigation-only boundary — **do not edit
      PROJECT.md in this plan**.

    `### Recommended naming posture for calendar_utils.py (folded todo)`
    - Record a recommendation, not code. Name the five cross-module-consumed helpers by name and
      state that their leading underscore now misrepresents a module that is a real shared API
      with multiple consumers, so Phase 27 — which will be editing these modules anyway — should
      drop the underscore as part of its own work rather than as a separate cleanup pass. Note the
      second half of the todo: `calendar_utils.py`-owned tests still live in
      `test_sync_lco_observation_calendar.py` and belong in their own module.
    - State explicitly that recording this recommendation does **not** close the todo, which stays
      open until code actually lands.

    Finally, replace the `## Durable summary` placeholder with a pointer to
    `docs/design/canonical_record_spike.rst` (written in task 3).

    Redaction discipline throughout: real people's names may describe a finding, but no email
    address and no name+email pairing may appear anywhere in the file.
  </action>
  <verify>
    <automated>D=.planning/phases/26-canonical-record-spike/26-DECISION.md; test -f "$D" && grep -q '## Recommendation' "$D" && grep -q 'Criterion 1' "$D" && grep -q 'Criterion 2' "$D" && grep -q 'Criterion 3' "$D" && grep -q 'Criterion 4' "$D" && grep -q 'LEGACY' "$D" && grep -q 'RUN:{run_pk}:{date}' "$D" && grep -q 'site-local' "$D" && grep -q "related_name='telescope_label_meta'" "$D" && grep -q 'calendar_utils' "$D" && grep -q '_coarse_telescope_label' "$D" && grep -q 'canonical_record_spike' "$D" && grep -q 'Confirmed against real rows' "$D" && grep -q 'Constructed-input code-path check' "$D" && ! grep -qE '[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}' "$D" && ! grep -qi 'upsert' "$D" && git diff --quiet -- .planning/PROJECT.md && test -f .planning/todos/pending/2026-07-02-rename-calendar-utils-py-private-helpers-to-reflect-shared-m.md</automated>
  </verify>
  <acceptance_criteria>
    - `26-DECISION.md` has a `## Recommendation` section with one subsection per SPIKE criterion
      (1 through 4), each restating the locked decision and citing the Finding that grounds it.
    - The `source` vocabulary lists six values, marks the three adapter values as not yet produced
      by any code path, and states that neither new field enters either partial unique constraint.
    - The `approval_status == APPROVED` and `source != WEB` derivation rule appears verbatim, with
      the rejected fourth approval value and its five affected readers named.
    - The event-key scheme is stated as `RUN:{run_pk}:{date}` with `{date}` explicitly the
      site-local observing night, and the stage-2 fan-out question is answered with "a single
      class-wide event per day, not one per candidate site".
    - D-06's three-meaning vocabulary, D-07's spanning space-mission event, and D-08's stage 0 all
      appear with the real rows they cover named by pk.
    - The rename checklist names all four ROADMAP-listed integration points plus the additional
      consumers measured in plan 26-01, and records the
      `related_name='telescope_label_meta'`-stays-unchanged decision with its reason.
    - The adopt-vs-gap-fill verdict matches task 1's user decision and quotes the measured counts.
    - The folded todo appears as a naming recommendation naming all five helpers, with an explicit
      statement that this does not close it; the todo file still exists under
      `.planning/todos/pending/`.
    - `.planning/PROJECT.md` is unmodified (`git diff --quiet` passes) — the D-16 correction is a
      separate todo.
    - The `## Durable summary` placeholder now points at `docs/design/canonical_record_spike.rst`.
    - No email address and no occurrence of the word "upsert" appears in the file.
  </acceptance_criteria>
  <done>26-DECISION.md is complete: Findings from plans 26-01/26-02 plus a Recommendation section that locks all four SPIKE criteria, the folded-todo naming posture, and the D-16 instruction, with the Durable-summary pointer resolved.</done>
</task>

<task type="auto">
  <name>Task 3: Publish the durable docs/design page and discard every throwaway artifact</name>
  <files>docs/design/canonical_record_spike.rst, docs/design/design.rst, .planning/phases/26-canonical-record-spike/26-DECISION.md</files>
  <read_first>
    - docs/design/uncertain_scheduling_spike.rst (the closest analog, read in full — the opening paragraph pattern with its archival-path caveat, the Background / Key finding / Decisions / Future scope section order, the `=`-for-H1 and `-`-for-H2 underline convention, and the `.. list-table::` usage with `:header-rows: 1` and explicit `:widths:`)
    - docs/design/eso_feasibility_spike.rst (the second analog — its alternate section naming and its 3-column `.. list-table::` variant)
    - docs/design/design.rst (lines 36-46 — the Design Notes toctree whose five bare-basename entries the new page joins, appended in creation order rather than alphabetically)
    - .planning/phases/26-canonical-record-spike/26-PATTERNS.md ("`docs/design/<name>.rst`" and "`docs/design/design.rst` — toctree entry" — including the correction that the entry belongs in `design.rst`, not `docs/index.rst`, and the note that an orphaned page still builds without error because the pre-commit `sphinx-build` has no `-W` flag; "Throwaway/git-excluded investigation-code pattern" — the discard mechanics)
    - .planning/phases/26-canonical-record-spike/26-DECISION.md (the completed Recommendation from task 2 — the durable page restates these verdicts and must not diverge from them)
    - .planning/phases/26-canonical-record-spike/26-RESEARCH.md ("Investigation Methodology" item 1 step 8 — the explicit discard sequence; "Common Pitfalls" 5 — why a scratch branch, not gitignore, is the isolation mechanism for tracked files)
    - .planning/phases/26-canonical-record-spike/26-VALIDATION.md (Evidence Map row 5 — the exact pre-commit `sphinx-build` invocation; sampling step 7 — why a leaked gitignored `local_settings.py` is invisible to `git status` and must be checked for by literal absence on disk)
  </read_first>
  <action>
    Work on the real phase-26 branch throughout. If the scratch branch is still checked out,
    switch off it first and never merge it.

    Part A — the durable page. Create `docs/design/canonical_record_spike.rst` following the two
    existing spike pages' shape: `=` underline for the H1 (at least as long as the title), `-` for
    H2. Open with the established four-part paragraph — what was investigated, the date of the
    live investigation, an explicit statement that no schema migration, no reconciler and no
    attribution UI was built during this spike, and a pointer to the full-detail companion
    `26-DECISION.md` at
    `.planning/phases/26-canonical-record-spike/26-DECISION.md`, carrying the same archival caveat
    both analogs use (this project's milestone-archival workflow moves completed phase directories
    to `.planning/phases-archive/` once their milestone closes, so check there first if the
    original path no longer resolves).

    Then the section sequence the analogs use: `Background`, `Key finding` (a bolded
    one-paragraph verdict before any detail table), a decisions section, and `Future scope`.
    Because this phase settles roughly twenty named decisions, use **one `.. list-table::` per
    thematic cluster** rather than a single unreadable table — the natural clusters are the
    `source` vocabulary and approval gating, the reconciler event key and ownership, the stage
    semantics (class-wide, space mission, stage 0), and the migration and rename checklist. Give
    every table `:header-rows: 1` and an explicit `:widths:` list, and name the consuming phase
    (27, 28 or 29) in a column so a future reader can see who acts on each row.

    Carry three things across from the decision doc that a summary is most likely to drop:
    the site-local-observing-night rule attached to the event key (not just the key format); the
    explicit stage-2 answer (a single class-wide event per day, not one event per candidate site);
    and the confidence distinction on the Gemini identity mapping, stated in the same sentence as
    the mapping itself rather than as a footnote.

    Use RST double-backticks for every identifier — single backticks and Markdown-style code spans
    render as literal backtick text. Reference the companion doc by plain filename in double
    backticks with a prose parenthetical, matching both analogs, rather than introducing a Sphinx
    cross-reference role neither uses. Restrict yourself to the directive set already used in
    `docs/design/` (`.. toctree::`, `.. list-table::`, `.. image::`). This is published
    documentation: no PII, and plain English over database jargon.

    Part B — the toctree. Append one new line, `canonical_record_spike` (bare basename, no
    extension, no path), to the `docs/design/design.rst` toctree immediately after
    `uncertain_scheduling_spike`, matching the existing creation-order style. This step is easy to
    silently miss: a page that exists but is not in this toctree still builds without error and is
    simply unreachable from the docs navigation, because the pre-commit `sphinx-build` invocation
    carries no `-W`/nitpicky flag.

    Part C — the build gate. Run the exact pre-commit invocation:
    `sphinx-build -M html ./docs ./_readthedocs -T -E -d ./docs/_build/doctrees -D exclude_patterns=notebooks/*,_build`
    It must exit 0. Fix any RST error (most likely an underline shorter than its title, or a
    `.. list-table::` whose row bullets are misaligned) before proceeding.

    Part D — the final real-DB fingerprint, run **before** anything is deleted. Re-run
    `stat -c '%s %Y' src/fomo_db.sqlite3` and compare it against
    `tmp/26-realdb-fingerprint-before.txt`. They must match — this is the phase-level proof that no
    command ever wrote to the real dev DB. Then record that fingerprint value verbatim into the
    snapshot Finding in `26-DECISION.md`, so the evidence survives the deletion of `tmp/` and the
    date-pinned snapshot (D-04) is identified by size and mtime as well as by date.

    Part E — the discard, in this order:
    1. Confirm the current branch is the real phase-26 branch, not `spike/26-canonical-record-probe`.
    2. Delete the scratch branch with a force delete (`git branch -D spike/26-canonical-record-probe`).
       Force is correct and expected here: the branch is deliberately unmerged, and Git refusing a
       safe delete is the mechanism working, not a warning to work around. Never merge it.
    3. `rm -f local_settings.py`, then confirm its literal absence with an `ls` that must error.
       A leaked override is gitignored and therefore invisible to `git status`, so checking for
       absence on disk is a separate necessary step, not implied by a clean status.
    4. `rm -rf tmp/`, then confirm its literal absence the same way.
    5. Confirm no `solsys_code/` file differs from its committed state and that
       `solsys_code/migrations/0008_scratch_canonical_record_probe.py` does not exist on this
       branch — the scratch migration, the model rename, the two import-site fixes and the four
       test-module edits must all be gone with the branch.
    6. Confirm `git status --porcelain` shows only this phase's documentation deliverables
       (`26-DECISION.md`, `docs/design/canonical_record_spike.rst`, `docs/design/design.rst`) and
       nothing else — no scratch script, no database file, no `solsys_code/` change.
    7. Confirm no scratch artifact was ever committed anywhere, by checking that
       `git log --all --oneline` returns nothing for the scratch migration path.

    Part F — prove the repository still works from a pristine state. Run `ruff check .`,
    `ruff format --check .`, `python -m pytest`, and the single-module Django quick run
    `./manage.py test solsys_code.tests.test_calendar_template` (roughly 22 seconds; Django builds
    its own isolated test database, so this never touches `src/fomo_db.sqlite3`, and with
    `local_settings.py` now gone it runs against the real settings module exactly as any developer
    would). All four must pass.
  </action>
  <verify>
    <automated>test -f docs/design/canonical_record_spike.rst && grep -q '^canonical_record_spike$' <(sed -n '39,60p' docs/design/design.rst | sed 's/^ *//') && grep -q '26-DECISION.md' docs/design/canonical_record_spike.rst && grep -q 'phases-archive' docs/design/canonical_record_spike.rst && grep -q 'list-table' docs/design/canonical_record_spike.rst && ! grep -qi 'upsert' docs/design/canonical_record_spike.rst && ! grep -qE '[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}' docs/design/canonical_record_spike.rst && sphinx-build -M html ./docs ./_readthedocs -T -E -d ./docs/_build/doctrees -D exclude_patterns=notebooks/*,_build && ! test -e local_settings.py && ! test -e tmp && ! test -e solsys_code/migrations/0008_scratch_canonical_record_probe.py && [ -z "$(git branch --list 'spike/26-canonical-record-probe')" ] && git rev-parse --abbrev-ref HEAD | grep -qv 'spike/26-canonical-record-probe' && git diff --quiet -- solsys_code/ && [ -z "$(git log --all --oneline -- 'solsys_code/migrations/0008_scratch_canonical_record_probe.py')" ] && [ -z "$(git status --porcelain | grep -v -e '26-DECISION.md' -e 'docs/design/canonical_record_spike.rst' -e 'docs/design/design.rst' -e '26-0._-SUMMARY.md' -e '.planning/')" ] && grep -q "$(stat -c '%s %Y' src/fomo_db.sqlite3)" .planning/phases/26-canonical-record-spike/26-DECISION.md && ruff check . && ruff format --check . && python -m pytest && ./manage.py test solsys_code.tests.test_calendar_template</automated>
  </verify>
  <acceptance_criteria>
    - `docs/design/canonical_record_spike.rst` exists with Background, a bolded one-paragraph Key
      finding, per-cluster `.. list-table::` decision tables each carrying `:header-rows: 1` and
      explicit `:widths:`, and a Future scope section — matching the two existing spike pages'
      shape.
    - Its opening paragraph names what was investigated and its date, states explicitly that no
      migration, reconciler or attribution UI was built, and points at `26-DECISION.md` with the
      `.planning/phases-archive/` caveat.
    - The page states the event key together with the site-local-observing-night rule, answers the
      stage-2 fan-out question explicitly, and states the Gemini mapping's constructed-input
      confidence in the same sentence as the mapping.
    - `docs/design/design.rst`'s toctree contains a bare `canonical_record_spike` line immediately
      after `uncertain_scheduling_spike`.
    - The exact pre-commit `sphinx-build` invocation exits 0.
    - `local_settings.py` and `tmp/` no longer exist on disk (an `ls` of each errors), the branch
      `spike/26-canonical-record-probe` no longer exists, and HEAD is not that branch.
    - `git diff --quiet -- solsys_code/` passes and
      `solsys_code/migrations/0008_scratch_canonical_record_probe.py` does not exist; `git log
      --all` shows that path was never committed.
    - `git status --porcelain` lists only this phase's documentation deliverables.
    - `src/fomo_db.sqlite3`'s current size+mtime fingerprint matches the value recorded in
      `26-DECISION.md`, which itself was compared against
      `tmp/26-realdb-fingerprint-before.txt` before `tmp/` was deleted.
    - `ruff check .`, `ruff format --check .`, `python -m pytest`, and
      `./manage.py test solsys_code.tests.test_calendar_template` all pass from the pristine tree.
  </acceptance_criteria>
  <done>The durable design page is published, reachable from the Design Notes toctree and building clean; every throwaway artifact — scratch branch, `local_settings.py`, `tmp/`, the scratch migration and every scratch `solsys_code/` edit — is provably gone; and the pristine repository passes lint, both test suites' relevant runs, and the real-DB fingerprint check.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `26-DECISION.md` (built from real dev-DB rows) → the published `docs/design/` page | The durable page is rendered to a public documentation site, so it is a second, wider disclosure boundary for anything PII-bearing that survived into the decision doc (RESEARCH.md Pitfall 6, Phase 18 D-01) |
| scratch branch + gitignored scratch files → the real phase-26 branch's committed history | This is the plan where the leak is finally closed or permanently baked in: after this plan's commit, anything still in the working tree is either committed or silently carried into Phase 27 (RESEARCH.md Pitfall 5) |
| `local_settings.py` left on disk → every future `manage.py` invocation in this repo | A surviving override silently repoints the whole project's database at a deleted scratch path, and is invisible to `git status` because it is gitignored (26-VALIDATION.md sampling step 7) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-01 | Information Disclosure | Real `contact_person`/`contact_email` values reaching `26-DECISION.md` or the published `docs/design/canonical_record_spike.rst` | high | mitigate | Both tasks' `<automated>` verify runs an email-regex negative grep over the file it writes; the durable page is additionally gated because it is published documentation. Real names may describe a finding; addresses and name+email pairings never appear. |
| T-26-03 | Tampering | Scratch branch, `local_settings.py`, `tmp/`, the scratch migration, or any scratch `solsys_code/` edit surviving into the real branch or the commit | high | mitigate | Task 3 part E deletes each one in a fixed order and then proves absence three independent ways: literal `test -e` checks on disk (because gitignored files are invisible to `git status`), `git branch --list` for the scratch branch, and `git diff --quiet -- solsys_code/` plus a `git log --all` check on the scratch migration path proving it was never committed. `git status --porcelain` is additionally filtered to show only the three documentation deliverables. |
| T-26-02 | Tampering | An undetected earlier write to the real `src/fomo_db.sqlite3` being silently forgotten when the evidence file is deleted | medium | mitigate | Task 3 part D re-runs the fingerprint comparison against `tmp/26-realdb-fingerprint-before.txt` **before** `tmp/` is deleted, and copies the fingerprint value into `26-DECISION.md` so the check remains reproducible after teardown; the `<automated>` verify re-derives the live fingerprint and greps for it in the committed doc. |
| T-26-06 | Denial of Service | A `docs/design/` page that exists but is missing from the toctree, silently unreachable from the published navigation | low | mitigate | Sphinx's default build does not warn on an orphaned page (the pre-commit invocation carries no `-W` flag), so the toctree line is asserted directly by a grep over `docs/design/design.rst`'s toctree block rather than relied on the build to catch. |
| T-26-07 | Repudiation | The durable page and the decision doc stating different verdicts, so a Phase 27-29 reader acts on the wrong one | medium | mitigate | Task 3 is sequenced after task 2 and reads the completed Recommendation as its source; the page restates those verdicts rather than deriving them, and both documents name the same consuming phase per decision. |
| T-26-SC | Tampering | npm/pip/cargo package installs | — | accept | No package-manager install occurs in this phase. RESEARCH.md's Package Legitimacy Audit records "Not applicable"; CONTEXT.md locks "no new dependencies". No legitimacy checkpoint is required. |
</threat_model>

<verification>
- `26-DECISION.md` is complete: Findings from plans 26-01/26-02 plus a `## Recommendation` section
  covering SPIKE-01..04, the folded-todo naming posture, the D-16 instruction, and a resolved
  `## Durable summary` pointer.
- The adopt-vs-gap-fill verdict recorded matches the user's task-1 decision and quotes the
  measured counts from plan 26-02's prototype.
- `docs/design/canonical_record_spike.rst` exists, follows the two existing spike pages' shape,
  states the same verdicts as the decision doc, and is listed in `docs/design/design.rst`'s
  toctree.
- The exact pre-commit `sphinx-build` invocation exits 0.
- `.planning/PROJECT.md` is unmodified and the folded todo remains in
  `.planning/todos/pending/` — both are deliberately outside this phase's boundary.
- No email address and no occurrence of "upsert" appears in either written document.
- Every throwaway artifact is gone: no scratch branch, no `local_settings.py`, no `tmp/`, no
  `solsys_code/` diff, and no commit anywhere touching the scratch migration path.
- `src/fomo_db.sqlite3` still matches the fingerprint recorded at the start of the phase.
- `ruff check .`, `ruff format --check .`, `python -m pytest` and
  `./manage.py test solsys_code.tests.test_calendar_template` all pass from the pristine tree.
- The committed diff for this plan contains only `26-DECISION.md`,
  `docs/design/canonical_record_spike.rst` and `docs/design/design.rst`.
</verification>

<success_criteria>
- All five ROADMAP Phase 26 success criteria are satisfied across the three plans: the `source`
  vocabulary with its executable coexistence proof (criterion 1); the per-adapter identity mapping
  (criterion 2); one canonical event-key scheme plus an explicit stage-2 fan-out answer
  (criterion 3); the migration and attribution strategy with the full rename checklist and the
  `related_name`-unchanged decision (criterion 4); and a durable, readable `docs/design/` page
  outside `.planning/` (criterion 5).
- Phases 27, 28 and 29 can each execute a decision rather than make one.
- The repository is left exactly as the phase found it, apart from two documentation files —
  provably, not by assertion.
</success_criteria>

<artifacts_produced>
**Committed (survives phase close) — the phase's complete deliverable set:**
- `.planning/phases/26-canonical-record-spike/26-DECISION.md` — completed: `## Recommendation`
  section added covering SPIKE-01..04, the folded-todo naming posture, the D-16 instruction and
  the real-DB fingerprint; `## Durable summary` pointer resolved.
- `docs/design/canonical_record_spike.rst` — **new page** (the third `docs/design/` spike page,
  after `eso_feasibility_spike.rst` and `uncertain_scheduling_spike.rst`).
- `docs/design/design.rst` — one new toctree line, `canonical_record_spike`.

**Created by this plan and discarded within it (task 3 part E):**
- Nothing new. This plan creates no throwaway artifact; it *removes* every artifact plans 26-01
  and 26-02 created — `local_settings.py`, the whole `tmp/` directory and its seven captured
  output files, five scratch SQLite copies, four evidence scripts, the git branch
  `spike/26-canonical-record-probe`, and with it the scratch-only symbols
  `CalendarEventMeta`, `CalendarEventMeta.run`, `CampaignRun.Source`, `CampaignRun.source`,
  `CampaignRun.telescope_class`, the file
  `solsys_code/migrations/0008_scratch_canonical_record_probe.py`, the `RUN:1:{date}`
  calendar-event keys minted into the scenario copies, and every scratch edit to
  `solsys_code/models.py`, `solsys_code/admin.py`,
  `solsys_code/management/commands/sync_lco_observation_calendar.py`,
  `solsys_code/tests/test_admin.py`, `solsys_code/tests/test_calendar_template.py`,
  `solsys_code/tests/test_sync_lco_observation_calendar.py` and
  `solsys_code/tests/test_load_telescope_runs.py`.

**Deliberately NOT touched by this phase:**
- `.planning/PROJECT.md` — the stale Phase 25 paragraph is recorded as a finding; correcting it is
  a separate todo (D-16).
- `.planning/todos/pending/2026-07-02-rename-calendar-utils-py-private-helpers-to-reflect-shared-m.md`
  — folded as a recommendation only; the todo stays open until code lands.
- Any `docs/runbooks/` page or demo notebook — ROADMAP records "Paired docs: None" for this phase.
</artifacts_produced>

<output>
Create `.planning/phases/26-canonical-record-spike/26-03-SUMMARY.md` when done.
</output>
