---
phase: 29-the-reconciler
plan: 04
type: execute
wave: 3
depends_on: ["29-02"]
files_modified:
  - solsys_code/campaign_views.py
  - solsys_code/tests/test_campaign_approval.py
  - solsys_code/tests/test_admin.py
  - solsys_code/management/commands/backfill_range_calendar_events.py
  - solsys_code/tests/test_backfill_range_calendar_events.py
autonomous: true
requirements: [RECON-08, RECON-09]

must_haves:
  truths:
    - "Approving a run reconciles it immediately through the shared reconciler function"
    - "Resolving a run's site reconciles it immediately and still keeps site_needs_review=True when reconciliation fails"
    - "Marking a run cancelled or weather-failed reconciles it immediately, applying the [CANCELLED]/[WEATHERED] title in place"
    - "_project_calendar_event and _calendar_event_title no longer exist anywhere in the codebase"
    - "backfill_range_calendar_events no longer exists as a command or a test module"
    - "The approval-queue test suite asserts RUN: keys and patches the reconciler module, and passes"
  artifacts:
    - path: "solsys_code/campaign_views.py"
      provides: "four staff-action call sites calling reconcile_run(); no projection helpers of its own"
      contains: "from .campaign_reconciler import reconcile_run"
  key_links:
    - from: "solsys_code/campaign_views.py"
      to: "solsys_code/campaign_reconciler.py"
      via: "the four staff actions call the shared per-run function inline and synchronously"
      pattern: "reconcile_run\\(run\\)"
---

<objective>
Execute D-01 and RECON-08/RECON-09: rewire `approve()`, `_resolve_site()` and `_set_run_status()`
(serving `mark_cancelled` and `mark_weather_failure`) to call `campaign_reconciler.reconcile_run()`,
delete `_project_calendar_event()`/`_calendar_event_title()`/`_RUN_STATUS_CALENDAR_PREFIX` outright,
delete `backfill_range_calendar_events` and its test module, and rewrite the approval-queue test
suite onto `RUN:` keys and the reconciler's patch targets.

Purpose: with the reconciler as the sole projection authority, a staff decision and a batch sweep
provably agree (D-03), and the backfill-command-per-gap pattern is retired for good. The test-suite
rewrite here is planned first-order work, not incidental breakage: research Pitfall 3 identified it
as a direct consequence of the locked D-01 decision.

Output: a rewired `campaign_views.py` with 110 lines of projection code deleted, two deleted files,
and a green `test_campaign_approval.py`.

**Known red window:** after Task 1, `solsys_code.tests.test_campaign_approval` is red by design —
its patch targets no longer exist. Tasks 2 and 3 in this same plan restore it. Do not stop after
Task 1.
</objective>

<execution_context>
@/home/tlister/git/fomo_devel/.claude/gsd-core/workflows/execute-plan.md
@/home/tlister/git/fomo_devel/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-the-reconciler/29-CONTEXT.md
@.planning/phases/29-the-reconciler/29-RESEARCH.md
@.planning/phases/29-the-reconciler/29-PATTERNS.md
@.planning/phases/29-the-reconciler/29-02-SUMMARY.md
@CLAUDE.md
</context>

## Artifacts this phase produces

See `29-01-PLAN.md`'s "Artifacts this phase produces" section for the phase-wide list of new
symbols. This plan creates no new symbols; it **removes** these:

- `solsys_code/campaign_views.py` — `_calendar_event_title()`, `_project_calendar_event()`,
  `_RUN_STATUS_CALENDAR_PREFIX` (the last one moved to `campaign_reconciler.py` in plan 29-01 as the
  public `RUN_STATUS_CALENDAR_PREFIX`)
- `solsys_code/management/commands/backfill_range_calendar_events.py` — whole file
- `solsys_code/tests/test_backfill_range_calendar_events.py` — whole file

<tasks>

<task type="auto">
  <name>Task 1: Rewire the four staff actions onto reconcile_run() and delete the retired code paths</name>
  <files>solsys_code/campaign_views.py, solsys_code/tests/test_admin.py, solsys_code/management/commands/backfill_range_calendar_events.py, solsys_code/tests/test_backfill_range_calendar_events.py</files>
  <read_first>
    - solsys_code/campaign_views.py lines 425-554 (the code being deleted) and lines 556-908
      (`CampaignRunDecisionView.post()`, `_resolve_site()`, `_set_run_status()` — the three methods
      and four actions being rewired, including their load-bearing ordering comments)
    - solsys_code/campaign_reconciler.py (`reconcile_run`, `ReconcileResult`,
      `RUN_STATUS_CALENDAR_PREFIX` — plans 29-01/29-02's output)
    - solsys_code/management/commands/backfill_range_calendar_events.py (read before deleting —
      confirms `_project_calendar_event` is its only reason to exist)
    - .planning/phases/29-the-reconciler/29-RESEARCH.md ("Code Examples" #1 — the exhaustive
      retirement checklist)
  </read_first>
  <action>
**Rewire, in `campaign_views.py`:**

`approve()` (around line 649) — replace the `_project_calendar_event(run)` call inside its
`try/except ValueError` with `reconcile_run(run)`, keeping the swallow-and-log shape byte-identical
apart from the wording of the log message (D-04: `approve()` has no retry surface, so an expected
`sun_event` `ValueError` still leaves the run approved without a calendar entry). The outer
`except Exception:` revert-to-PENDING_REVIEW block stays exactly as it is — a non-`ValueError` raised
by `reconcile_run()` must still fall through to it.

`_resolve_site()` (around line 803) — replace `created = _project_calendar_event(run)` with a
`reconcile_run(run)` call whose returned `ReconcileResult` drives the two success messages: use
`result.skipped_reason is None` where the old bool was used (this is the documented successor to the
old `True`/`False` return). The non-reverting `except Exception:` block, the warning message text,
and the rule that `site_needs_review = False` is written only after the call returns without raising
all stay unchanged — that ordering is load-bearing and documented in the method's docstring.

`_set_run_status()` (around lines 876-905) — delete the `matching_events` query
(`Q(url=f'CAMPAIGN:{run.pk}') | Q(url__startswith=f'CAMPAIGN:{run.pk}:')`), the
`if matching_events.exists():` guard, and the per-event `insert_or_create_calendar_event(...)` loop.
Replace all of it with a single `reconcile_run(run)` call inside the same non-reverting
`try/except Exception:` the method already uses (D-04: this action structurally resembles
`_resolve_site()`'s handling, so it keeps that shape — `run_status` is already committed by the
conditional `.update()` above and must never be reverted by a calendar failure). The reconciler now
builds the `[CANCELLED]`/`[WEATHERED]` title and the `Run status:` description line itself, so the
prefix lookup and the title-helper call disappear from this method. Update the docstring: the old
"never fabricate an event for a run that never projected one" paragraph must be replaced by the new
truth — `reconcile_run()`'s stage-0 guard is what decides that, so a TBD run or an unresolved-site
run still gets no event, while an approved, site-resolved run that never had one now gets its events
created here (calendar events are a function of run state, D-01).

Add `from .campaign_reconciler import reconcile_run` to the module's relative-import block.

**Delete, in `campaign_views.py`:** `_calendar_event_title()`, `_project_calendar_event()` and
`_RUN_STATUS_CALENDAR_PREFIX` (lines ~425-554), and `_ACTION_TO_RUN_STATUS` stays (it is still the
POST-action whitelist). Then prune the imports ruff now flags as unused — expect `sun_event`,
`insert_or_create_calendar_event` and `time as dt_time` to go, and `timedelta` to go once line 537's
loop is gone. `Observatory`, `datetime`, `date`, `Q` and `CalendarEvent` all still have other users
in the file; let `ruff check` decide rather than guessing. Update the module docstring's opening
paragraph if it names the deleted projection helpers.

**Delete these two files outright:** `solsys_code/management/commands/backfill_range_calendar_
events.py` and `solsys_code/tests/test_backfill_range_calendar_events.py` (RECON-09). Both must be
removed with `git rm` so the deletion is staged, not left as untracked leftovers.

**Update `solsys_code/tests/test_admin.py` around line 1010:** a comment-only reference to
`_project_calendar_event()`. Reword it to name `campaign_reconciler.reconcile_run()`'s stage-0
`'TBD window'` skip instead. No functional change to that test.
  </action>
  <verify>
    <automated>python manage.py test solsys_code.tests.test_campaign_reconciler solsys_code.tests.test_reconcile_campaign_runs solsys_code.tests.test_admin -v2</automated>
  </verify>
  <acceptance_criteria>
    - `python manage.py test solsys_code.tests.test_campaign_reconciler solsys_code.tests.test_admin` passes (`test_campaign_approval` is knowingly red until Task 3 — do not run it as this task's gate).
    - `grep -rn '_project_calendar_event\|_calendar_event_title' --include=*.py solsys_code/` returns nothing.
    - `git ls-files solsys_code/management/commands/backfill_range_calendar_events.py solsys_code/tests/test_backfill_range_calendar_events.py` returns nothing (both deletions staged).
    - `grep -c 'CAMPAIGN:' solsys_code/campaign_views.py` is 0.
    - `grep -n 'from .campaign_reconciler import reconcile_run' solsys_code/campaign_views.py` matches, and `grep -c 'reconcile_run(run)' solsys_code/campaign_views.py` is 3 (approve, resolve_site, set_run_status — the last serving both status actions).
    - `python manage.py reconcile_campaign_runs --dry-run` still exits 0 (nothing imported the deleted command).
    - `ruff check .` and `ruff format --check .` are clean (proves no unused imports were left behind).
  </acceptance_criteria>
  <done>All four staff actions reconcile through the shared function; the retired projection path and the backfill command are gone from the codebase.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite the approval-queue projection tests — TestApproval, TestCalendarProjection, TestCalendarNoChurn, TestGeminiFtScenario</name>
  <files>solsys_code/tests/test_campaign_approval.py</files>
  <read_first>
    - solsys_code/tests/test_campaign_approval.py lines 1-20 (module docstring, which describes the
      retired `CAMPAIGN:` key scheme), 190-250 (`TestApproval`), 326-470 (`TestCalendarProjection`),
      1743-1780 (`TestCalendarNoChurn`), 2495-2560 (`TestGeminiFtScenario`)
    - solsys_code/campaign_reconciler.py (`run_container_url`, `run_night_url`, `owned_events`,
      `event_title` — what the assertions must now expect)
    - solsys_code/campaign_views.py (Task 1's rewired call sites — the new patch targets)
  </read_first>
  <action>
Rewrite the four classes named above in `solsys_code/tests/test_campaign_approval.py`. This is
mechanical but not blind: three behaviour changes must be reflected in the expectations, not merely
string-substituted.

Substitutions:
- Every `url=f'CAMPAIGN:{run.pk}'` / `url__startswith=f'CAMPAIGN:{run.pk}:'` assertion becomes the
  `RUN:` equivalent. Prefer importing `run_container_url`/`run_night_url`/`owned_events` from
  `solsys_code.campaign_reconciler` and asserting through them rather than re-deriving the literal
  key format a second time in the tests.
- Every `patch('solsys_code.campaign_views.sun_event', ...)` becomes
  `patch('solsys_code.campaign_reconciler.sun_event', ...)`.
- Every `patch('solsys_code.campaign_views.insert_or_create_calendar_event', ...)` becomes
  `patch('solsys_code.campaign_reconciler.insert_or_create_calendar_event', ...)`.

Behaviour changes to encode (do not preserve the old assertions):
1. **A single-night ground run's key is now date-bearing.** `TestApproval` and
   `TestCalendarProjection` currently assert a bare `CAMPAIGN:{pk}` event for a single-night run;
   the locked scheme (26-DECISION.md Criterion 3) gives a classical run `RUN:{pk}:{date}` for every
   night including the only one. Assert the date-bearing key and assert that no bare `RUN:{pk}`
   event exists for a classical run.
2. **A satellite run still gets exactly one bare `RUN:{pk}` whole-day-span event** — that assertion
   only changes namespace.
3. **`TestGeminiFtScenario`'s range-window expectations are unchanged in count** (one event per
   night) but change in key form and in title: the reconciler's `event_title()` produces the same
   `campaign: telescope_instrument (window S..E)` string, so the range-suffix assertions should keep
   passing; verify rather than assume, and update the class docstring's reference to
   `_project_calendar_event()`.
4. `TestCalendarNoChurn` keeps its meaning: re-approving (or re-resolving) an already-projected run
   must not bump `modified`. Point it at the new keys and confirm it still passes — the reconciler's
   existing-event field set (`title`, `description`, `target_list`) is what makes it hold.

Also update the module-level docstring at the top of the file (lines 5-10) which describes the
retired bare-key/`CAMPAIGN:` scheme.
  </action>
  <verify>
    <automated>python manage.py test solsys_code.tests.test_campaign_approval.TestApproval solsys_code.tests.test_campaign_approval.TestCalendarProjection solsys_code.tests.test_campaign_approval.TestCalendarNoChurn solsys_code.tests.test_campaign_approval.TestGeminiFtScenario -v2</automated>
  </verify>
  <acceptance_criteria>
    - The four named test classes pass.
    - `grep -n "patch('solsys_code.campaign_views.sun_event\|patch('solsys_code.campaign_views.insert_or_create_calendar_event" solsys_code/tests/test_campaign_approval.py` returns nothing within these four classes.
    - A test asserts a single-night classical run's key is date-bearing and that no bare `RUN:{pk}` row exists for it.
    - `ruff check solsys_code/tests/test_campaign_approval.py` and `ruff format --check` on it are clean.
  </acceptance_criteria>
  <done>The projection and no-churn test classes assert the reconciler's key scheme and patch its module.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite the remaining approval-queue classes and restore a fully green suite</name>
  <files>solsys_code/tests/test_campaign_approval.py</files>
  <read_first>
    - solsys_code/tests/test_campaign_approval.py lines 472-640 (`TestRunStatusChange`), 970-1200
      (`TestSitesNeedingReview`, including line ~1042's `_project_calendar_event` patch and line
      ~1067's ValueError-re-raise docstring), 1336-1440 (`TestPlaceholderSiteReplacement`)
    - solsys_code/campaign_views.py (Task 1's rewired `_set_run_status()` and `_resolve_site()`)
    - solsys_code/campaign_reconciler.py (`RUN_STATUS_CALENDAR_PREFIX`, `event_title`,
      `event_description`, `_skip_reason`)
  </read_first>
  <action>
Rewrite `TestRunStatusChange`, `TestSitesNeedingReview` (its calendar-projection assertions only —
leave its site-resolution and racing-POST assertions alone) and `TestPlaceholderSiteReplacement`'s
calendar assertions, then bring the whole app suite back to green.

Substitutions: same `CAMPAIGN:` -> `RUN:` and `campaign_views` -> `campaign_reconciler` patch-target
rules as Task 2, plus one more: `patch('solsys_code.campaign_views._project_calendar_event',
side_effect=RuntimeError('boom'))` (around line 1042) becomes
`patch('solsys_code.campaign_views.reconcile_run', side_effect=RuntimeError('boom'))` — patch the
name where `campaign_views` looks it up, not where it is defined, or the view will still call the
real function.

Behaviour changes to encode:
1. **`_set_run_status()` can now create events, not only update them.** The old method never
   fabricated an event for a run that had none; the reconciler's stage-0 guard is now what decides.
   So: a TBD-window run or an unresolved-site run marked cancelled must still end with zero events
   (assert the guard still holds, and keep those tests), but an APPROVED, site-resolved run with no
   prior event that is marked cancelled now ends with its per-night events created and titled
   `[CANCELLED] ...`. Rewrite any assertion that asserted zero events for that third case.
2. **`[CANCELLED]`/`[WEATHERED]` titles and the `Run status:` description line are now built by the
   reconciler.** Assert against `campaign_reconciler.RUN_STATUS_CALENDAR_PREFIX` and
   `event_title(run)`/`event_description(run)` rather than re-deriving the strings, so the tests
   cannot drift from the single source of truth. The prefix strings themselves must stay
   byte-identical to `calendar_display_extras._TERMINAL_PREFIXES` members — keep any existing test
   that asserts that.
3. **`TestSitesNeedingReview`'s failure paths keep their meaning:** a reconcile failure during
   `resolve_site` must still leave `site_needs_review=True` and surface the warning message; only the
   patch target changes. Update the line ~1067 docstring's reference to `_project_calendar_event()`.

Finally run the whole app test suite and fix whatever else the rewire surfaced. Per CLAUDE.md and
the project memory, invoke it as `python manage.py test solsys_code` (never `./manage.py`); if you
need the unfiltered project suite for any reason, exclude `test_views.TestEphemeris`, which segfaults
in native ASSIST for reasons unrelated to this phase.
  </action>
  <verify>
    <automated>python manage.py test solsys_code -v1</automated>
  </verify>
  <acceptance_criteria>
    - `python manage.py test solsys_code` passes with zero failures and zero errors.
    - `grep -c 'CAMPAIGN:' solsys_code/tests/test_campaign_approval.py` is 0.
    - `grep -rn "patch('solsys_code.campaign_views._project_calendar_event" solsys_code/` returns nothing.
    - A test asserts that an approved, site-resolved run with no prior event gets `[CANCELLED]`-titled events created when marked cancelled, and a separate test asserts a TBD-window run marked cancelled still ends with zero events.
    - `ruff check .` and `ruff format --check .` are clean.
  </acceptance_criteria>
  <done>The whole `solsys_code` suite is green on the reconciler's key scheme and patch targets.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| staff POST -> `CampaignRunDecisionView` | Untrusted request data crosses here; unchanged by this plan (`StaffRequiredMixin`, `http_method_names = ['post']`, the fixed `_ACTION_TO_RUN_STATUS` whitelist and the conditional `.update()` staleness claims all stay) |
| view -> reconciler | A staff decision now triggers a write path shared with the batch command |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-12 | Elevation of Privilege (business logic) | `reconcile_run()` fired for a run in an invalid state by a rewired call site | mitigate | The existing guards stay untouched — `approve()`'s conditional `.update()` double-approve no-op, `_resolve_site()`'s approval/`site_needs_review` precondition and site-claim `.update()`, `_set_run_status()`'s APPROVED precondition and staleness check; `reconcile_run()` is additionally idempotent and re-checks approval itself, so a redundant call is safe |
| T-29-13 | Spoofing / Tampering | The four POST actions | accept (unchanged) | `StaffRequiredMixin` + POST-only + the fixed action whitelist are untouched by this phase; V4 Access Control has no new surface (RESEARCH.md Security Domain) |
| T-29-14 | Repudiation | A reconcile failure silently leaving a run approved with no calendar entry | mitigate | D-04's asymmetric handling is preserved verbatim: `approve()` logs and swallows only the expected `ValueError`; `_resolve_site()` logs with `logger.exception`, warns the user and keeps `site_needs_review=True` so the row stays in its retry surface; `_set_run_status()` warns and tells the user to retry the same action |
| T-29-15 | Tampering | Deleting the backfill command while something still imports it | mitigate | Its only importer was its own test module, deleted in the same task; the acceptance grep proves no `_project_calendar_event` reference survives anywhere |
| T-29-SC | Tampering | npm/pip/cargo installs | accept | No packages installed in this phase; no legitimacy audit applies |
</threat_model>

<verification>
- `python manage.py test solsys_code` green.
- `grep -rn '_project_calendar_event\|_calendar_event_title\|backfill_range_calendar_events' --include=*.py solsys_code/` returns nothing.
- `ruff check .` and `ruff format --check .` clean.
</verification>

<success_criteria>
- All four staff actions reconcile their run immediately through `reconcile_run()` (RECON-08).
- The retired projection helpers and the backfill command are gone from the codebase (RECON-09, code half).
- The approval-queue suite asserts `RUN:` keys, patches the reconciler module, and passes.
</success_criteria>

<output>
Create `.planning/phases/29-the-reconciler/29-04-SUMMARY.md` when done
</output>
