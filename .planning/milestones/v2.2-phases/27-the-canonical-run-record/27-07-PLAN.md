---
phase: 27-the-canonical-run-record
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - src/templates/campaigns/approval_queue.html
  - solsys_code/tests/test_campaign_approval.py
  - solsys_code/templatetags/attribution_display_extras.py
  - src/templates/tom_calendar/partials/event_form.html
  - solsys_code/tests/test_calendar_template.py
  - docs/runbooks/telescope_runs_calendar.rst
autonomous: true
requirements: [CANON-02, CANON-05]
gap_closure: true

must_haves:
  truths:
    - "On the approval queue page, the 'Sites Needing Review — action required' card renders as the FIRST section, above both 'Pending Review' and 'Recently Decided' — so it is the first thing staff see even when it is the only actionable table (27-UAT.md Test 8)"
    - "A staff user viewing the calendar-event modal for an event with no CalendarEventMeta.run link, for which a HIGH-band attribution-queue candidate already exists, sees a 'Possible campaign run match' hint naming the candidate run and linking into the attribution queue (27-UAT.md Test 9)"
    - "An anonymous or non-staff visitor never sees that hint, even for the same unlinked event"
    - "An unlinked event with zero attribution candidates (e.g. a conference/proposal-deadline entry with no campaign) still renders the modal cleanly, with no hint and no error"
    - "An event whose CalendarEventMeta.run is already set never shows the hint, only the existing Campaign run block (when publicly visible)"
    - "event_form.html's WR-03 comment no longer claims 'no production code writes CalendarEventMeta.run yet' — Phase 29's reconciler has shipped and does write it automatically for events it owns"
  artifacts:
    - path: "src/templates/campaigns/approval_queue.html"
      provides: "Sites Needing Review card moved to the top of {% block content %}"
    - path: "solsys_code/templatetags/attribution_display_extras.py"
      provides: "high_band_attribution_candidates simple_tag wrapping campaign_attribution.candidates_for_event()"
      exports: ["high_band_attribution_candidates"]
    - path: "src/templates/tom_calendar/partials/event_form.html"
      provides: "staff-only HIGH-band candidate hint for an unlinked event, refreshed WR-03 comment"
    - path: "solsys_code/tests/test_calendar_template.py"
      provides: "EventModalAttributionHintTest regression coverage for the new hint"
    - path: "solsys_code/tests/test_campaign_approval.py"
      provides: "corrected section-order assertion (Sites Needing Review now precedes Pending Review / Recently Decided)"
    - path: "docs/runbooks/telescope_runs_calendar.rst"
      provides: "updated prose for both the approval-queue section order and the calendar-modal hint behavior"
  key_links:
    - from: "src/templates/campaigns/approval_queue.html"
      to: "rendered HTML order"
      via: "static block order, no Python change"
      pattern: "Sites Needing Review.*Pending Review"
    - from: "src/templates/tom_calendar/partials/event_form.html"
      to: "solsys_code/templatetags/attribution_display_extras.py"
      via: "{% load attribution_display_extras %} + {% high_band_attribution_candidates event as ... %}"
      pattern: "high_band_attribution_candidates"
    - from: "src/templates/tom_calendar/partials/event_form.html"
      to: "campaigns:attribution"
      via: "hint link href with ?band=high"
      pattern: "campaigns:attribution.*band=high"
---

<objective>
Close the two NEW minor gaps found during 27-UAT.md's re-verification pass (Test 8, Test 9),
run after Phase 27.1 already closed the first three gaps from the same UAT document. Both
root causes are already fully diagnosed (see the debug sessions below) — this plan implements
the documented fixes, it does not re-diagnose anything.

1. `.planning/debug/approval-queue-section-order.md`: the "Sites Needing Review — action
   required" card is hardcoded as the THIRD section in `approval_queue.html`, below "Pending
   Review" and "Recently Decided" — even though it's the only actionable table when there are
   zero pending submissions. Pure template block order, no Python/view involvement.

2. `.planning/debug/calendar-event-run-link-inconsistent.md`: the calendar-event modal
   (`event_form.html`) renders NOTHING for an unlinked event (`CalendarEventMeta.run` unset),
   even when `campaign_attribution.candidates_for_event()` already scores a HIGH-band match for
   it — confirmed live for the real 2026-07-16 `[EXPIRED] 2m0 2M0-SCICAM-MUSCAT` event
   (score 0.8, band high, against `CampaignRun` pk=1). Both the reconciler and the attribution
   scorer are confirmed correct; this is a pure UX/discoverability gap in one template. The same
   file's WR-03 comment is also stale post-Phase-29 and gets refreshed in the same task.

Purpose: an operator on the approval queue sees the actionable table first, not buried below an
informational one; an operator looking at an unlinked calendar event can tell why it's unlinked
and what one click fixes it, instead of the modal staying silent.

Output: reordered `approval_queue.html`; a new `attribution_display_extras` template tag; an
updated `event_form.html` with the staff-only hint block and a refreshed WR-03 comment; new/
corrected regression tests; an updated `docs/runbooks/telescope_runs_calendar.rst`.
</objective>

<execution_context>
@/home/tlister/git/fomo_devel/.claude/gsd-core/workflows/execute-plan.md
@/home/tlister/git/fomo_devel/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-the-canonical-run-record/27-UAT.md
@.planning/debug/approval-queue-section-order.md
@.planning/debug/calendar-event-run-link-inconsistent.md
@src/templates/campaigns/approval_queue.html
@src/templates/tom_calendar/partials/event_form.html
@solsys_code/campaign_attribution.py
@solsys_code/templatetags/calendar_display_extras.py
@solsys_code/tests/test_calendar_template.py
@solsys_code/tests/test_campaign_approval.py
@docs/runbooks/telescope_runs_calendar.rst
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move Sites Needing Review to the top of the approval queue</name>
  <files>src/templates/campaigns/approval_queue.html, solsys_code/tests/test_campaign_approval.py</files>
  <action>
    Gap 1 (27-UAT.md Test 8; root cause confirmed in
    .planning/debug/approval-queue-section-order.md — pure static HTML ordering, no
    queryset/context dependency between the three tables, so reordering is safe).

    In src/templates/campaigns/approval_queue.html, move the entire "Sites Needing Review —
    action required" card (the `&lt;div class="card border-warning mt-4"&gt;...&lt;/div&gt;` block,
    currently the last thing in `{% block content %}`) to the TOP of `{% block content %}`,
    immediately after the `&lt;h4 class="font-weight-bold mb-4"&gt;Approval Queue&lt;/h4&gt;` heading and
    before the "Pending Review" `&lt;h5&gt;` + `{% render_table pending_table %}` pair. Drop the
    card's `mt-4` class (it no longer follows other content) and add `mb-4` in its place so it
    keeps a visible gap before "Pending Review". The "Pending Review" and "Recently Decided"
    blocks are otherwise unchanged — they just now render after the card instead of before it.
    No changes to ApprovalQueueView or any Python file: `pending_table`, `decided_table`, and
    `review_table` are three independent context vars, none gated on the others.

    Then fix the one existing test that locks in the OLD order:
    solsys_code/tests/test_campaign_approval.py's
    `TestApprovalQueueSitesNeedingReviewGrouping.test_d07_order_preserved_decided_precedes_sites_needing_review`
    currently asserts `decided_index &lt; review_index` (i.e. Sites Needing Review comes after
    Recently Decided) and its class docstring claims this "preserv[es] D-07's locked document
    order". That lock is exactly what 27-UAT.md Test 8 overturned by explicit operator
    direction ("I would still like the 'Sites Needing Review - action required' section to be
    at the top not the bottom"). Rename the test to
    `test_sites_needing_review_now_precedes_pending_and_decided` and invert it: assert the
    "Sites Needing Review" index is LESS than both the "Pending Review" index and the "Recently
    Decided" index. Update the class docstring to say the original D-07/27.1-03 bottom
    placement was reordered per 27-UAT.md Test 8 gap closure (cite
    .planning/debug/approval-queue-section-order.md), not "preserved". Leave
    `test_sites_needing_review_renders_as_distinguishing_action_required_card` unchanged — the
    `border-warning` card styling assertion is unaffected by position.
  </action>
  <verify>
    <automated>cd /home/tlister/git/fomo_devel && python manage.py test solsys_code.tests.test_campaign_approval.TestApprovalQueueSitesNeedingReviewGrouping -v 2 && ruff check src/templates/campaigns/approval_queue.html solsys_code/tests/test_campaign_approval.py && ruff format --check solsys_code/tests/test_campaign_approval.py</automated>
  </verify>
  <done>GET /campaigns/approval-queue/ (staff) renders "Sites Needing Review — action required" before both "Pending Review" and "Recently Decided" in the response HTML; the renamed/inverted test proves it; ruff clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Surface a HIGH-band attribution candidate in the unlinked-event modal</name>
  <files>solsys_code/templatetags/attribution_display_extras.py, src/templates/tom_calendar/partials/event_form.html, solsys_code/tests/test_calendar_template.py</files>
  <behavior>
    Gap 2 (27-UAT.md Test 9; root cause confirmed in
    .planning/debug/calendar-event-run-link-inconsistent.md — the reconciler and attribution
    scorer are both already correct; the only gap is that event_form.html renders nothing for
    an unlinked event even when a HIGH-band candidate already exists). Append a new
    `EventModalAttributionHintTest(TestCase)` to solsys_code/tests/test_calendar_template.py
    (same file as the existing `EventModalCampaignRunLinkTest`, which already exercises this
    partial) with these fixtures and tests:

    setUpTestData: a shared `TargetList` campaign; a staff `User`
    (`is_staff=True`); a `CampaignRun` in that campaign with `telescope_instrument='FTS/MuSCAT4'`,
    `window_start=date(2026,7,7)`, `window_end=date(2026,7,21)`,
    `approval_status=CampaignRun.ApprovalStatus.APPROVED` (`matched_run`); an unlinked
    `CalendarEvent` in the SAME campaign (`target_list=campaign`) titled
    `'[EXPIRED] 2m0 2M0-SCICAM-MUSCAT'` with `telescope='2m0'`, `instrument='FTS/MuSCAT4'`
    (identical to `matched_run.telescope_instrument`, guaranteeing a deterministic
    `instrument_similarity` of 1.0 — do not use two different-but-similar strings, the fixture
    must not depend on fuzzy-match tuning), `start_time`/`end_time` both `2026-07-16` (fully
    inside the run's window), plus a `CalendarEventMeta` row with `is_verified=True, run=None`
    (`unlinked_event_with_candidate` — mirrors the real dev-DB pk=59 shape exactly); a second
    unlinked `CalendarEvent` with NO `target_list` and NO `CalendarEventMeta` row at all
    (`unlinked_event_no_campaign` — a conference/proposal-deadline shape, so
    `candidates_for_event()` returns `[]` via `_eligible_runs_for_event()`'s `target_list_id is
    None` gate); a third `CalendarEvent` in the same campaign WITH a `CalendarEventMeta` row
    whose `run=matched_run` already (`linked_event`).

    Test methods (fixed string `'Possible campaign run match'` is the pinned label the
    implementation below must render verbatim, so these assertions are exact, not
    approximate):
    - `test_staff_sees_high_band_hint_for_unlinked_event`: log in as staff, GET the modal for
      `unlinked_event_with_candidate`, assert 200, assert `'Possible campaign run match'` in
      the content, assert `f"{reverse('campaigns:attribution')}?band=high"` in the content.
    - `test_anonymous_does_not_see_hint`: no login, GET the same modal, assert 200, assert
      `'Possible campaign run match'` NOT in the content and the attribution-queue URL NOT in
      the content.
    - `test_no_candidate_event_shows_no_hint`: staff login, GET the modal for
      `unlinked_event_no_campaign`, assert 200, assert `'Possible campaign run match'` NOT in
      the content.
    - `test_linked_event_shows_run_block_not_hint`: staff login, GET the modal for
      `linked_event`, assert 200, assert `'Possible campaign run match'` NOT in the content,
      assert the existing Campaign-run block still renders (`reverse('campaigns:table',
      args=[campaign.pk])` in the content).
    - `test_stale_wr03_comment_removed_from_template_source`: read
      `src/templates/tom_calendar/partials/event_form.html`'s source directly (same
      `Path(...).read_text()` pattern as the existing
      `test_template_source_never_contains_pending_review_literal`), assert the literal string
      `'no production code writes CalendarEventMeta.run yet'` is NOT present.
  </behavior>
  <action>
    RED: write the test class and methods above first; they must fail against the current
    (unmodified) templatetag/template.

    GREEN — implement:

    1. Create solsys_code/templatetags/attribution_display_extras.py: a new template-tag
       module (module docstring mirroring calendar_display_extras.py's style, explaining this
       is presentation glue over campaign_attribution.py's pure matcher, kept as its own file
       rather than folded into calendar_display_extras.py's color/visual-encoding tags or into
       campaign_attribution.py itself, whose own docstring forbids it depending on the
       template layer). One `@register.simple_tag` function
       `high_band_attribution_candidates(event)` that imports `campaign_attribution` from
       `solsys_code` and returns
       `[c for c in campaign_attribution.candidates_for_event(event) if c.band ==
       campaign_attribution.BAND_HIGH]` — a thin filter over the existing, already
       dismissal-aware, campaign-boundary-gated scorer; never raises (delegates entirely to
       `candidates_for_event()`, which itself never raises).

    2. In src/templates/tom_calendar/partials/event_form.html:
       a. Change `{% load bootstrap4 %}` to `{% load bootstrap4 attribution_display_extras %}`.
       b. Refresh the WR-03 paragraph inside the second `{% comment %}` block (the one starting
          "D-08: event.telescope_label_meta is CalendarEventMeta's reverse one-to-one
          accessor..."). Replace the sentence "WR-03: no production code writes
          CalendarEventMeta.run yet -- _project_calendar_event() creates the events but
          deliberately does not claim ownership of them, and the automatic writer is deferred
          to the Phase 29 reconciler (canonical_record_spike.rst, "Ownership rule")." with text
          stating that Phase 29's reconciler (campaign_reconciler.py's `reconcile_run()`, both
          the container and classical-nights branches) now writes `CalendarEventMeta.run`
          automatically for every event it creates or adopts, unconditionally, via
          `_link_event_to_run()`; an event from a pipeline the reconciler doesn't own (raw
          `sync_lco_observation_calendar`/`sync_gemini_observation_calendar`/
          `load_telescope_runs` output not yet adopted by any run) still has no link until a
          staff member hand-links it via the admin `CalendarEventMetaInline` picker or confirms
          a match in the Phase 28 attribution queue — and note that the block added below now
          surfaces a HIGH-band attribution-queue candidate for exactly that still-unlinked
          case, so the modal is never silently empty when a good match already exists. Keep the
          cross-reference to docs/runbooks/telescope_runs_calendar.rst's "Why doesn't the
          calendar pop-up show a 'Campaign run' block?" anchor.
       c. Change `{% if run.is_publicly_visible %}` to also add
          `{% elif not run and request.user.is_staff %}` (matching this codebase's existing
          `request.user.is_staff` convention, e.g. campaign_list.html's nav-banner gate — not
          bare `user.is_staff` — so the gate is unambiguously server-derived from the request,
          not a template context-processor assumption). `not run` is true both when
          `event.telescope_label_meta` has no companion row at all and when the companion row
          exists with `run` unset — both are the same "not yet attributed" state
          `candidates_for_event()` already treats as an orphan; do not special-case them
          separately. Staff-only: the attribution queue itself is `StaffRequiredMixin`-gated,
          and an offered candidate run may not yet be publicly visible, so this hint must never
          reach an anonymous or non-staff visitor (Information Disclosure — see threat_model
          T-27-21 below).
          Inside the `elif` branch: call
          `{% high_band_attribution_candidates event as attribution_candidates %}`; if
          `attribution_candidates` is non-empty, render a `form-row`/`col` block with a
          `&lt;label&gt;Possible campaign run match&lt;/label&gt;` (exact text, pinned by the tests above),
          one line per candidate showing `{{ candidate.run }}` (CampaignRun's own discriminating
          `__str__`, e.g. "#3 Didymos 2026 | FTS/MuSCAT4 | 2026-07-07..2026-07-21 | no site" —
          already includes pk/campaign/telescope/window/site, no extra formatting needed) next
          to a `&lt;span class="badge badge-light border"&gt;{{ candidate.score }}&lt;/span&gt;` (same
          badge class attribution_queue.html already uses for its score chip), and a single
          link below the list with text "Confirm in the attribution queue ↗" (mirroring this
          file's existing "View campaign ↗"/"View  ↗" outward-link style),
          `href="{% url 'campaigns:attribution' %}?band=high"`, `target="_blank"` — `?band=high`
          reuses `AttributionQueueView`'s existing, already server-validated `band` GET param
          (an unrecognised value there silently falls back to "all bands", never an error) to
          narrow the queue to the relevant confidence tier without any new backend work.
  </action>
  <verify>
    <automated>cd /home/tlister/git/fomo_devel && python manage.py test solsys_code.tests.test_calendar_template -v 2 && ruff check solsys_code/templatetags/attribution_display_extras.py solsys_code/tests/test_calendar_template.py src/templates/tom_calendar/partials/event_form.html && ruff format --check solsys_code/templatetags/attribution_display_extras.py solsys_code/tests/test_calendar_template.py</automated>
  </verify>
  <done>Staff-logged-in modal for an unlinked event with a HIGH-band candidate shows "Possible campaign run match" and a link to the attribution queue filtered to band=high; anonymous visitors, no-candidate events, and already-linked events never show it; the stale WR-03 comment sentence is gone from the template source; all five new tests pass alongside the full existing EventModalCampaignRunLinkTest and TemplateCommentSyntaxSweepTest suites in the same file.</done>
</task>

<task type="auto">
  <name>Task 3: Update the paired runbook and run repo-wide quality gates</name>
  <files>docs/runbooks/telescope_runs_calendar.rst</files>
  <action>
    Per CLAUDE.md's paired-docs convention (docs/runbooks/ is in scope, scoped by directory,
    and docs/runbooks/telescope_runs_calendar.rst is wired into the Sphinx toctree at
    docs/index.rst:24): both gaps closed by Tasks 1-2 change behavior this runbook already
    documents, so update both sections.

    a. "How do I reach the approval queue?" section: it currently lists the two queues as
       bullets in the order "Pending Review" then "Sites Needing Review — action required".
       Swap the bullet order to match the new page layout from Task 1 (Sites Needing Review
       first, Pending Review second), and add one short sentence after the bullets noting that
       Sites Needing Review now renders first on the page — the only queue that's actionable
       when no submissions are pending — followed by Pending Review then Recently Decided (cite
       the 27-UAT.md Test 8 gap-closure round). Leave the rest of the section (the banner
       "Behavior change" paragraphs) untouched — they describe the nav-banner gating condition,
       which Task 1 does not change.

    b. "Why doesn't the calendar pop-up show a 'Campaign run' block?" section: after the
       existing final paragraph ("An event with no companion record at all, or with the run
       link left blank, still means 'not owned by any campaign run' -- never 'needs fixing'.
       ... why those entries show no Campaign run block."), add a new paragraph documenting
       the Task 2 gap closure: when a High-band attribution-queue candidate already exists for
       one of these still-unlinked events, the pop-up now shows a "Possible campaign run match"
       hint naming the candidate run and linking straight to the attribution queue (filtered to
       the High band) to confirm it. Note it is staff-only (the attribution queue itself
       requires staff, and a candidate run may not yet be publicly visible), and that an event
       with zero candidates (the conference/proposal-deadline case just described) still shows
       nothing extra — the hint only ever names a real match that already passed the same
       scoring the attribution queue itself uses (cross-reference the "How do I attribute
       existing calendar events and observation records to a run?" section above).

    Then run the full repo-wide quality gates this codebase's pre-commit hooks enforce (ruff
    lint/format across the whole tree, plus the Sphinx doc build that would otherwise only be
    caught at commit time) to confirm nothing from Tasks 1-2 regressed elsewhere.
  </action>
  <verify>
    <automated>cd /home/tlister/git/fomo_devel && ruff check . && ruff format --check . && sphinx-build -M html ./docs ./_readthedocs -T -E -d ./docs/_build/doctrees -D exclude_patterns=notebooks/*,_build</automated>
  </verify>
  <done>Both runbook sections describe the current (post-gap-closure) behavior exactly; ruff check and ruff format --check are clean repo-wide; the Sphinx build completes with no errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Anonymous/public browser -> calendar event modal | `tom_calendar:update-event` has no `login_required` — any visitor can GET/POST it |
| Staff browser -> attribution queue | `campaigns:attribution` is `StaffRequiredMixin`-gated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-21 | Information Disclosure | `event_form.html`'s new candidate-hint block | mitigate | Gate the whole block on `request.user.is_staff` (server-derived from `request`, not client input) — an unconfirmed candidate `CampaignRun` may not be `is_publicly_visible` yet, so its identity (telescope/instrument/window/site via `{{ candidate.run }}`) must never reach an anonymous or non-staff visitor; proven by `test_anonymous_does_not_see_hint` |
| T-27-22 | Tampering | `?band=high` query param on the new attribution-queue link | accept | Already validated server-side in `AttributionQueueView.get_context_data` (`if band not in _ATTRIBUTION_BANDS: band = None`) before this plan — a tampered/invalid value degrades to "all bands", never an error; no new server code introduced by this plan touches that validation |
| T-27-23 | Denial of Service | `high_band_attribution_candidates` calling `candidates_for_event()` on every modal render for an unlinked event | accept | Bounded by `_eligible_runs_for_event()`'s campaign-only filter (typically a handful of rows per campaign) — the same cost model the attribution queue page itself already exercises on every load; no unbounded scan introduced |

No new npm/pip/cargo installs in this plan (template, template-tag, and doc changes only) — Package Legitimacy Gate not applicable.
</threat_model>

<verification>
Run, in order:
1. `python manage.py test solsys_code.tests.test_campaign_approval.TestApprovalQueueSitesNeedingReviewGrouping solsys_code.tests.test_calendar_template -v 2` — both gap-closure test surfaces green, including the five new `EventModalAttributionHintTest` methods and the inverted order test.
2. `ruff check .` and `ruff format --check .` — repo-wide, clean.
3. `sphinx-build -M html ./docs ./_readthedocs -T -E -d ./docs/_build/doctrees -D exclude_patterns=notebooks/*,_build` — doc build succeeds (proves the runbook edits are valid RST).
4. Manual smoke (optional, automation above is sufficient to close both gaps): open `/campaigns/approval-queue/` as staff and confirm Sites Needing Review is the first card; open `/calendar/`, click the real 2026-07-16 `[EXPIRED] 2m0 2M0-SCICAM-MUSCAT` event as staff and confirm the new hint appears and links to `/campaigns/attribution/?band=high`.
</verification>

<success_criteria>
- Sites Needing Review renders before Pending Review and Recently Decided on `/campaigns/approval-queue/` (27-UAT.md Test 8 closed).
- An unlinked calendar event with a HIGH-band attribution candidate shows a staff-only "Possible campaign run match" hint linking to the attribution queue; anonymous visitors, no-candidate events, and already-linked events never show it (27-UAT.md Test 9 closed).
- event_form.html's WR-03 comment accurately reflects that Phase 29's reconciler writes `CalendarEventMeta.run` automatically for events it owns.
- `docs/runbooks/telescope_runs_calendar.rst` documents both new behaviors.
- `ruff check .`, `ruff format --check .`, and the Sphinx doc build all pass.
</success_criteria>

<output>
Create `.planning/phases/27-the-canonical-run-record/27-07-SUMMARY.md` when done
</output>
