---
phase: 30-v2-2-tech-debt-cleanup
plan: 04
type: execute
wave: 3
depends_on: ["30-01", "30-02", "30-03"]
files_modified:
  - .planning/phases/26-canonical-record-spike/26-VALIDATION.md
  - .planning/phases/27-the-canonical-run-record/27-VALIDATION.md
  - .planning/phases/27.1-close-gap-staff-surfaces-and-data-integrity-risks-from-the-c/27.1-VALIDATION.md
  - .planning/phases/28-operator-assisted-attribution/28-VALIDATION.md
  - .planning/phases/29-the-reconciler/29-VALIDATION.md
  - .planning/v2.2-MILESTONE-AUDIT.md
autonomous: true
requirements: [D-08, D-09, D-11]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-08: phases 26, 27, 27.1, 28 and 29 each have a VALIDATION.md carrying a real verdict from validate-phase rather than an unpromoted draft or no file at all (ROADMAP criterion 4)"
    - "D-08: 27.1, which had no VALIDATION.md at all, now has one"
    - "D-09: .planning/v2.2-MILESTONE-AUDIT.md states the true disposition of every tech-debt item it lists, and each already-closed item cites where it was closed, so /gsd-complete-milestone reads a correct record (ROADMAP criterion 5)"
    - "D-09: the audit's lint/format section records the misdiagnosis — the repo was clean under the pinned ruff and the findings came from an unpinned 0.15.20 — instead of recommending a cleanup pass that must not be run (D-05)"
    - "D-09: the audit's Nyquist coverage table reflects the verdicts task 1 and task 2 produced, not the pre-Phase-30 draft states"
    - "D-11: the record is internally consistent — the ROADMAP Phase 30 goal was corrected on 2026-08-31 before planning, and the audit now agrees with it rather than contradicting it"
  artifacts:
    - .planning/phases/26-canonical-record-spike/26-VALIDATION.md
    - .planning/phases/27-the-canonical-run-record/27-VALIDATION.md
    - .planning/phases/27.1-close-gap-staff-surfaces-and-data-integrity-risks-from-the-c/27.1-VALIDATION.md
    - .planning/phases/28-operator-assisted-attribution/28-VALIDATION.md
    - .planning/phases/29-the-reconciler/29-VALIDATION.md
    - .planning/v2.2-MILESTONE-AUDIT.md
  key_links:
    - "validate-phase verdict per phase -> that phase's VALIDATION.md front matter -> the audit's Nyquist Coverage table"
    - ".planning/v2.2-MILESTONE-AUDIT.md -> /gsd-complete-milestone -> what gets archived as the milestone's record"
  prohibitions:
    - statement: "No repo-wide reformat (D-05): the audit must not carry forward a recommendation to run a formatter or autofix pass over the tree, because the premise behind that recommendation was false"
      status: enforced
      verification: "the audit's lint/format section names the version mismatch as the finding and the two-place fix as its closure; `git show --stat` for this plan's commits lists only `.planning/` paths, so no source file was touched at all"
---

<objective>
Close the milestone's two record-keeping items: reconcile the five phase `VALIDATION.md`
files so each carries a real verdict (D-08), and amend `.planning/v2.2-MILESTONE-AUDIT.md`
so every tech-debt item it lists has a true disposition (D-09).

This runs last on purpose. The audit is the file `/gsd-complete-milestone` reads, so it has
to describe the state of the world after plans 30-01, 30-02 and 30-03 have landed — Phase 30's
own SUMMARY would not be consulted. Its Nyquist coverage table likewise has to record the
verdicts this plan's own first two tasks produce, not the draft states that preceded them.

Purpose: three separate phases each rediscovered, logged and deferred the same non-finding
because the record said it was open. Correcting the record is what actually stops the loop.
Output: five reconciled `VALIDATION.md` files and an audit whose every row states what really
happened and where.
</objective>

<execution_context>
@/home/tlister/git/fomo_devel/.claude/gsd-core/workflows/execute-plan.md
@/home/tlister/git/fomo_devel/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-v2-2-tech-debt-cleanup/30-CONTEXT.md
@.planning/v2.2-MILESTONE-AUDIT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile the three earlier phases' validation files (26, 27, 27.1)</name>
  <files>.planning/phases/26-canonical-record-spike/26-VALIDATION.md, .planning/phases/27-the-canonical-run-record/27-VALIDATION.md, .planning/phases/27.1-close-gap-staff-surfaces-and-data-integrity-risks-from-the-c/27.1-VALIDATION.md</files>
  <precondition>The three phase directories exist and phases 26, 27 and 27.1 are complete — 26 and 27 already carry a `status: draft` VALIDATION.md seeded by plan-phase, while 27.1 has none at all and its file will be created by the validate-phase run.</precondition>
  <read_first>
    - `.planning/v2.2-MILESTONE-AUDIT.md` §"Nyquist Coverage" — the table naming, per phase,
      whether a file exists, its `status`, its `nyquist_compliant` value and the classification
      (NOT-VALIDATED vs MISSING). This is the starting state task 3 will later overwrite.
    - `.planning/phases/26-canonical-record-spike/26-VALIDATION.md` and
      `.planning/phases/27-the-canonical-run-record/27-VALIDATION.md` — read the front matter
      of each (`status`, `nyquist_compliant`, `wave_0_complete`, `created`) so you can tell a
      genuine promotion from a file that was merely re-saved.
    - `.planning/phases/30-v2-2-tech-debt-cleanup/30-CONTEXT.md` §D-08 — the decision scoping
      this as its own plan so its cost stays visible.
  </read_first>
  <action>
    Invoke the `gsd-validate-phase` skill once per phase, for phases `26`, `27` and `27.1`, in
    that order. Let each run produce or promote that phase's `VALIDATION.md`; do not hand-edit
    the files to fake a verdict. A run that concludes a phase is NOT Nyquist-compliant is a
    valid, useful outcome — record it as such rather than retrying until it says otherwise.

    Phase 27.1 has no `VALIDATION.md` at all, so its run creates one. Its phase directory is
    `.planning/phases/27.1-close-gap-staff-surfaces-and-data-integrity-risks-from-the-c/` and
    the file belongs there, named for the phase the way the other four are.

    For each phase, capture two facts for task 3 to use: the resulting `status` value and the
    resulting `nyquist_compliant` value from the file's front matter. Write them into this
    plan's SUMMARY as a small table — task 3's audit edit depends on them, and re-deriving them
    later from memory is how a stale table gets written a second time.

    If a validate-phase run cannot complete for a phase (for example it needs an artifact that
    was archived), do not fabricate a verdict and do not silently skip it. Record what it needed
    and why it could not run, leave that phase's file in its existing state, and carry the real
    situation into task 3's table — an honest "could not be reconciled, because X" is a correct
    record; a green row that nothing produced is not.

    Commit the promoted files. This task touches `.planning/` only; no source file changes.
  </action>
  <verify>
    <automated>for f in .planning/phases/26-canonical-record-spike/26-VALIDATION.md .planning/phases/27-the-canonical-run-record/27-VALIDATION.md .planning/phases/27.1-close-gap-staff-surfaces-and-data-integrity-risks-from-the-c/27.1-VALIDATION.md; do echo "== $f"; test -f "$f" && sed -n '1,10p' "$f"; done</automated>
    <fails_when>a "== path" header is printed with no front matter beneath it (the file does not exist), or a printed `status:` value is still `draft` for a phase whose validate-phase run reported success</fails_when>
    <automated>test -f .planning/phases/27.1-close-gap-staff-surfaces-and-data-integrity-risks-from-the-c/27.1-VALIDATION.md && echo 271_EXISTS</automated>
    <fails_when>non-zero exit or "271_EXISTS" absent — phase 27.1's previously-missing validation file was not created</fails_when>
    <automated>git status --porcelain -- solsys_code src docs pyproject.toml CLAUDE.md</automated>
    <fails_when>prints any line — this task must touch `.planning/` only, and a source or docs path appearing here means a validate-phase run modified something outside its remit</fails_when>
  </verify>
  <acceptance_criteria>
    - All three files exist: `26-VALIDATION.md`, `27-VALIDATION.md` and (newly created) `27.1-VALIDATION.md`.
    - `grep -h '^status:' .planning/phases/26-canonical-record-spike/26-VALIDATION.md .planning/phases/27-the-canonical-run-record/27-VALIDATION.md .planning/phases/27.1-close-gap-staff-surfaces-and-data-integrity-risks-from-the-c/27.1-VALIDATION.md` prints three lines, none of which is `status: draft`, unless the SUMMARY records a named reason a specific phase could not be reconciled.
    - Each of the three files' front matter carries an explicit `nyquist_compliant` value (either boolean is acceptable — the verdict is the deliverable, not a particular answer).
    - This plan's SUMMARY contains a three-row table of phase / resulting `status` / resulting `nyquist_compliant`, which task 3 consumes.
    - `git status --porcelain -- solsys_code src docs pyproject.toml CLAUDE.md` prints nothing.
  </acceptance_criteria>
  <reversibility rating="reversible">Planning artifacts under `.planning/`; re-runnable at any time and no code path depends on them.</reversibility>
  <done>Phases 26, 27 and 27.1 each have a VALIDATION.md carrying a verdict validate-phase produced, 27.1's created from nothing, with the resulting status and compliance values recorded for task 3.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile the two later phases' validation files (28, 29)</name>
  <files>.planning/phases/28-operator-assisted-attribution/28-VALIDATION.md, .planning/phases/29-the-reconciler/29-VALIDATION.md</files>
  <precondition>Phases 28 and 29 are complete and each already carries a `status: draft` VALIDATION.md seeded by plan-phase.</precondition>
  <read_first>
    - `.planning/phases/28-operator-assisted-attribution/28-VALIDATION.md` and
      `.planning/phases/29-the-reconciler/29-VALIDATION.md` — the front matter of each, for the
      same before/after comparison task 1 made.
    - This plan's in-progress SUMMARY table from task 1 — task 3 needs all five rows together,
      so append to that table rather than starting a second one.
  </read_first>
  <action>
    Invoke the `gsd-validate-phase` skill once per phase, for phases `28` and `29`. Same rules
    as task 1: let the runs produce the verdicts, do not hand-edit a file to manufacture one,
    and treat a non-compliant verdict as a real, recordable outcome.

    These two phases are the ones whose substance is most heavily covered by other means — the
    audit notes 288 passing integration tests, per-phase UAT documents, code reviews with fix
    rounds, a security pass and a UI review — so a verdict that reads as thinner than that
    coverage suggests should be reported as-is rather than argued with. The point of D-08 is
    that the files stop being unpromoted drafts, not that they all come back green.

    Append the two resulting rows (phase / `status` / `nyquist_compliant`) to the SUMMARY table
    task 1 started, giving task 3 the complete five-row picture.

    Commit the promoted files. This task touches `.planning/` only.
  </action>
  <verify>
    <automated>for f in .planning/phases/28-operator-assisted-attribution/28-VALIDATION.md .planning/phases/29-the-reconciler/29-VALIDATION.md; do echo "== $f"; sed -n '1,10p' "$f"; done</automated>
    <fails_when>a printed `status:` value is still `draft` for a phase whose validate-phase run reported success, or a front-matter block is missing its `nyquist_compliant` key</fails_when>
    <automated>grep -l '^status: draft' .planning/phases/*/[0-9]*-VALIDATION.md 2>/dev/null | wc -l</automated>
    <fails_when>prints a number greater than 0 without a matching named reason in this plan's SUMMARY — an unpromoted draft survived across all five phases, which is exactly the state D-08 exists to end</fails_when>
    <automated>git status --porcelain -- solsys_code src docs pyproject.toml CLAUDE.md</automated>
    <fails_when>prints any line — this task must touch `.planning/` only</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -h '^status:' .planning/phases/28-operator-assisted-attribution/28-VALIDATION.md .planning/phases/29-the-reconciler/29-VALIDATION.md` prints two lines, neither of which is `status: draft`, unless the SUMMARY records a named reason.
    - `ls .planning/phases/*/[0-9]*-VALIDATION.md | wc -l` prints 5 — one file per phase 26, 27, 27.1, 28, 29.
    - This plan's SUMMARY table now has five rows.
    - `git status --porcelain -- solsys_code src docs pyproject.toml CLAUDE.md` prints nothing.
  </acceptance_criteria>
  <done>All five v2.2 phases have a VALIDATION.md carrying a real verdict, and the five-row status table is complete for task 3.</done>
</task>

<task type="auto">
  <name>Task 3: Amend the milestone audit with the true disposition of every tech-debt item</name>
  <files>.planning/v2.2-MILESTONE-AUDIT.md</files>
  <read_first>
    - `.planning/v2.2-MILESTONE-AUDIT.md` in full, but especially §"Tech Debt" (its lint/format
      subsection, its "Code-review findings deliberately accepted" table, its "Bookkeeping"
      bullet and its item-total line) and §"Nyquist Coverage" (its per-phase table and the
      paragraph beneath it). These are the sections being amended; every other section stays.
    - `.planning/phases/30-v2-2-tech-debt-cleanup/30-CONTEXT.md` §"Phase Boundary" — the
      verified-status table that is the evidence for the WR-09, WR-10 and ruff dispositions,
      and §D-09, which names exactly what each closed item must cite.
    - `.planning/phases/30-v2-2-tech-debt-cleanup/30-01-SUMMARY.md`,
      `30-02-SUMMARY.md` and `30-03-SUMMARY.md` — what those plans actually delivered, so the
      dispositions cite real outcomes rather than plan intentions.
    - This plan's own SUMMARY table from tasks 1 and 2 — the five verdicts that replace the
      Nyquist Coverage table's draft rows.
    - `.planning/ROADMAP.md` §"Phase 30" — the corrected goal and the six-item scope list, so
      the audit and the roadmap tell the same story.
  </read_first>
  <action>
    Amend `.planning/v2.2-MILESTONE-AUDIT.md` in place. Keep every existing section heading and
    the file's overall structure intact — `/gsd-complete-milestone` reads this file, so
    restructuring it is a risk with no benefit. Add dispositions; do not reorganise.

    **Anchor by content, not by line number.** Plans 30-01 and 30-03 both inserted prose into
    `docs/runbooks/telescope_runs_calendar.rst` earlier in the file than the WR-09 and WR-10
    evidence, so the `:305-308` and `:347-357` citations recorded during discuss-phase have
    shifted. Re-derive every runbook line number by grepping for the passage's own text at the
    moment you write the citation, and pair each number with a short quoted phrase so a future
    reader can re-find it even after the next edit moves it again.

    **The lint/format subsection.** Replace its closing recommendation — a single autofix and
    format pass under the pinned version — with the verified finding, because that
    recommendation's premise was false and acting on it is now forbidden by D-05. Record: under
    the pinned pre-commit ruff (`.pre-commit-config.yaml`'s `astral-sh/ruff-pre-commit` rev)
    the repository reports zero lint errors and 89 of 89 files already formatted; the three
    phases' identical findings came from a dev environment running ruff 0.15.20, reached by
    following CLAUDE.md's then-documented unpinned command. Cite where it was closed: Phase 30
    plan 30-02 pinned `pyproject.toml`'s dev extra to the pre-commit rev and routed CLAUDE.md's
    documented gate through `pre-commit run`. State explicitly that no file was reformatted.

    **The code-review findings table.** Give every row a disposition, either as a new column or
    as a per-row suffix — whichever keeps the table readable — with these outcomes:
    - 27 IN-01 (no query-cap test): still accepted, unchanged; the reason already in the row
      stands.
    - 27 IN-02 (attribution `approval_status`): CLOSED by Phase 30 plan 30-01, at both
      eligibility gates rather than the one the audit named, per decisions D-01, D-02 and D-03.
    - 27 WR-01 (`telescope_class` recomputation): CLOSED by Phase 30 plan 30-03, per D-04, with
      the guard reporting each firing on stderr and in the summary line.
    - 27.1 WR-09 (ambiguous antecedent): ALREADY CLOSED before Phase 30 — the review's own
      suggested replacement text is verbatim in place in the runbook's source-lock section.
      Cite the freshly re-derived line range and a quoted phrase from it.
    - 27.1 WR-10 (residual-consequence documentation): ALREADY CLOSED before Phase 30 — covered
      by the runbook's warning block in the same section, the matching section of
      `solsys_code/admin.py`'s `get_readonly_fields` docstring, and the pinning test
      `test_relabel_to_web_locks_the_row_and_cannot_be_undone` in
      `solsys_code/tests/test_admin.py`. Cite the test by name, not only by line number.
    - 28 IN-01 (rounded-vs-raw candidate score): still deferred, unchanged; the re-tuning
      trigger recorded in the code comment stands.
    - 29 (stale docstring names in the reconciler): CLOSED by Phase 30 plan 30-02, with the one
      deliberately-kept historical mention in `reconcile_campaign_runs.py` noted so it is not
      re-flagged as a leftover.

    **The bookkeeping bullet.** `26-DECISION.md`'s header preamble: CLOSED by Phase 30 plan
    30-02.

    **The item-total line.** Recompute it so it reports the true split — how many items were
    closed by Phase 30, how many were already closed before it, how many remain deliberately
    accepted or deferred — rather than a bare count of open items.

    **The Nyquist Coverage table.** Replace each row's `status`, `nyquist_compliant`,
    classification and action with the values tasks 1 and 2 produced, taken from this plan's
    SUMMARY table. Rewrite the paragraph beneath it: its current text says no phase has a
    reconciled file and that `nyquist_compliant: false` is not authoritative, which stops being
    true the moment this plan lands. Keep its closing note that the substance is well covered by
    other means (the integration tests, the UAT documents, the code reviews, the security pass
    and the UI review) — that context is still accurate and still useful.

    **Record the roadmap correction (D-11).** Add one short note in the Tech Debt section
    stating that the ROADMAP Phase 30 goal was rewritten on 2026-08-31, before planning, after
    discuss-phase scouting verified the WR-09, WR-10 and ruff items — so a reader comparing the
    original goal text with this audit sees why they differ instead of treating it as a
    contradiction.

    Touch no file other than the audit. In particular, do not edit any `VALIDATION.md` here:
    tasks 1 and 2 own those, and hand-editing one after the fact would defeat the point of
    running validate-phase at all.
  </action>
  <verify>
    <automated>grep -c '30-01\|30-02\|30-03' .planning/v2.2-MILESTONE-AUDIT.md</automated>
    <fails_when>prints fewer than 4 — the closed items do not cite the Phase 30 plans that closed them, so a reader cannot check the claim</fails_when>
    <automated>grep -c '0.15.20' .planning/v2.2-MILESTONE-AUDIT.md</automated>
    <fails_when>prints 0 — the lint/format section does not record the version mismatch that was the actual finding</fails_when>
    <automated>grep -c 'test_relabel_to_web_locks_the_row_and_cannot_be_undone' .planning/v2.2-MILESTONE-AUDIT.md</automated>
    <fails_when>prints 0 — WR-10's closure is asserted without citing the test that pins it</fails_when>
    <automated>grep -c '^status: draft' .planning/v2.2-MILESTONE-AUDIT.md; grep -c 'draft' .planning/v2.2-MILESTONE-AUDIT.md</automated>
    <fails_when>the second count is unchanged from its pre-task value while tasks 1 and 2 reported successful promotions — the Nyquist Coverage table still describes the pre-Phase-30 draft states</fails_when>
    <automated>grep -cE '^#{2,3} ' .planning/v2.2-MILESTONE-AUDIT.md</automated>
    <fails_when>prints a different number than it did before this task — a section heading was added, removed or renamed, and /gsd-complete-milestone parses this file</fails_when>
    <automated>git status --porcelain -- solsys_code src docs pyproject.toml CLAUDE.md .planning/phases</automated>
    <fails_when>prints any line — this task must touch `.planning/v2.2-MILESTONE-AUDIT.md` only</fails_when>
  </verify>
  <acceptance_criteria>
    - Every row of the "Code-review findings deliberately accepted" table carries an explicit disposition; `grep -c 'IN-02' .planning/v2.2-MILESTONE-AUDIT.md` prints at least 1 and that row names plan 30-01.
    - `grep -c '0.15.20' .planning/v2.2-MILESTONE-AUDIT.md` prints at least 1, and the lint/format section no longer presents a repo-wide autofix-and-format pass as the closing action.
    - `grep -c 'test_relabel_to_web_locks_the_row_and_cannot_be_undone' .planning/v2.2-MILESTONE-AUDIT.md` prints at least 1.
    - Every runbook line-number citation in the audit resolves: for each cited range, `sed -n '<range>p' docs/runbooks/telescope_runs_calendar.rst` contains the phrase quoted beside it in the audit.
    - The Nyquist Coverage table's five rows match, value for value, the five-row table in this plan's SUMMARY.
    - `grep -cE '^#{2,3} ' .planning/v2.2-MILESTONE-AUDIT.md` is unchanged from its pre-task value.
    - `grep -c '2026-08-31' .planning/v2.2-MILESTONE-AUDIT.md` prints at least 1 — the D-11 roadmap correction is on the record.
    - `git diff --name-only` for this task lists exactly `.planning/v2.2-MILESTONE-AUDIT.md`.
  </acceptance_criteria>
  <done>Every tech-debt item in the audit states what really happened and where it was closed; the Nyquist table carries this plan's real verdicts; the lint/format section records a misdiagnosis rather than an action that must not be taken; and the D-11 roadmap correction is noted so the roadmap and the audit agree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.planning/v2.2-MILESTONE-AUDIT.md` -> `/gsd-complete-milestone` | An automated milestone-close reads this file and acts on what it says; a wrong disposition here propagates into the archived record with no further review |
| validate-phase subagent -> the repository working tree | A skill invoked for its `.planning/` output has write access to the whole tree |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-12 | Repudiation | The audit's per-item dispositions — a "closed" claim with no citation cannot be checked, which is how the same non-finding was reopened three times | medium | mitigate | Task 3 requires every closed item to cite where it was closed, with runbook line numbers freshly re-derived and paired with a quoted phrase so the citation survives later edits; the acceptance criteria resolve each cited range against the runbook |
| T-30-13 | Tampering | A `VALIDATION.md` hand-edited to show a verdict no validate-phase run produced | medium | mitigate | Tasks 1 and 2 forbid hand-editing and require the verdict to come from the skill run; task 3 is forbidden from editing any `VALIDATION.md`, and its `git status` gate proves it did not |
| T-30-14 | Tampering | A validate-phase run writing outside `.planning/` | low | mitigate | Both tasks gate on `git status --porcelain` over the source, docs and config paths printing nothing |
| T-30-15 | Information Disclosure | The audit records file paths, line numbers and test names for an internal project | low | accept | All of it is already committed in the same repository; no credential, endpoint or personal data is added |
| T-30-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no packages and modifies no dependency manifest — `files_modified` contains six `.planning/` markdown paths and nothing else |
</threat_model>

<verification>
1. `ls .planning/phases/*/[0-9]*-VALIDATION.md | wc -l` prints 5 — including a newly created file for 27.1, which had none.
2. `grep -l '^status: draft' .planning/phases/*/[0-9]*-VALIDATION.md | wc -l` prints 0, or every remaining draft has a named reason recorded in this plan's SUMMARY (ROADMAP criterion 4).
3. The audit's Nyquist Coverage table matches this plan's five-row SUMMARY table value for value.
4. Every closed tech-debt item in the audit cites where it was closed, and every runbook line-number citation resolves against the current file (ROADMAP criterion 5).
5. `grep -c '0.15.20' .planning/v2.2-MILESTONE-AUDIT.md` prints at least 1 and the lint/format section presents the version mismatch as the finding.
6. `grep -cE '^#{2,3} ' .planning/v2.2-MILESTONE-AUDIT.md` is unchanged — the file `/gsd-complete-milestone` reads still has its original section structure.
7. `git show --stat` for this plan's commits lists only `.planning/` paths.

Paired-docs note (CLAUDE.md rule): this plan touches no module in the paired-artifact map and
no page under `docs/runbooks/`, so no notebook or runbook update is in scope here. It reads the
runbook to re-derive citations; it does not edit it.
</verification>

<success_criteria>
- ROADMAP criterion 4 holds: phases 26, 27, 27.1, 28 and 29 each have a reconciled `VALIDATION.md` carrying a real verdict rather than an unpromoted draft or no file at all.
- ROADMAP criterion 5 holds: `.planning/v2.2-MILESTONE-AUDIT.md` states the true disposition of every tech-debt item it lists, with each already-closed item citing where it was closed.
- The record is internally consistent: the audit, the corrected ROADMAP Phase 30 goal (D-11) and the three code plans' outcomes all describe the same milestone.
</success_criteria>

## Artifacts this phase produces

Files created or promoted by THIS plan (newly created — not pre-existing, so drift
verification should not expect to find them in the current tree):

**`.planning/phases/27.1-close-gap-staff-surfaces-and-data-integrity-risks-from-the-c/27.1-VALIDATION.md`**
- Newly created by the validate-phase run; this phase had no validation file at all

**`26-VALIDATION.md`, `27-VALIDATION.md`, `28-VALIDATION.md`, `29-VALIDATION.md`**
- Existing draft files promoted in place; their front-matter `status` and `nyquist_compliant` values change

**`.planning/v2.2-MILESTONE-AUDIT.md`**
- A new per-item disposition on every row of the "Code-review findings deliberately accepted" table (as a new column or a per-row suffix)
- A rewritten closing statement in the lint/format subsection
- A rewritten item-total line
- Rewritten rows and explanatory paragraph in the Nyquist Coverage table
- A new one-line note recording the D-11 ROADMAP goal correction

**This plan's SUMMARY**
- A five-row phase / `status` / `nyquist_compliant` table, produced by tasks 1 and 2 and consumed by task 3

<output>
Create `.planning/phases/30-v2-2-tech-debt-cleanup/30-04-SUMMARY.md` when done.
</output>