---
phase: 18-uncertain-scheduling-investigation-spike
plan: 02
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - .planning/phases/18-uncertain-scheduling-investigation-spike/18-DECISION.md
  - docs/design/uncertain_scheduling_spike.rst
autonomous: true
requirements: [SCHED-01]
user_setup: []

must_haves:
  truths:
    - "18-DECISION.md confirms the window-schema decision (nullable window_start/window_end DateField pair) as validated against the real rows, restating it as the locked schema Phase 19 implements (SCHED-01 criterion 1)."
    - "18-DECISION.md names the chosen fuzzy-match library — rapidfuzz, difflib, or an explicit split verdict — with rationale derived directly from Plan 01's recorded D-09 scores, not asserted independently (SCHED-01 criterion 4)."
    - "18-DECISION.md records the obscode-widening verdict: no widening needed — 250/274/289 are 3-char codes that fit Observatory.obscode's max_length=4, and 500@-170 is correctly flagged rather than truncated (SCHED-01 criterion 5)."
    - "18-DECISION.md restates the TBD natural-key recommendation (fold contact_person into the key for null-window rows via a partial/conditional UniqueConstraint) as a locked finding for Phase 19 to implement (SCHED-01 criterion 2, per D-06)."
    - "18-DECISION.md enumerates the CSV range/TBD parsing-rule recommendations for Phase 20, naming the existing _HHMM_RANGE/_APPROX_HOUR/_BARE_HOUR_UTC regexes and the D-04 check-BOTH-columns finding, framed as recommendations Phase 20 implements (not shipped here) (SCHED-01 criterion 3)."
    - "docs/design/uncertain_scheduling_spike.rst exists as a durable summary that states the same fuzzy-library verdict and window-schema decision up front, so future milestones can reference it without digging into .planning/."
  artifacts:
    - ".planning/phases/18-uncertain-scheduling-investigation-spike/18-DECISION.md (Recommendation section completed for all 5 SCHED-01 criteria; Durable-summary pointer resolved)"
    - "docs/design/uncertain_scheduling_spike.rst (durable summary, alongside eso_feasibility_spike.rst / telescope_runs_calendar.rst)"
  key_links:
    - "Plan 01's recorded D-09 scores and resolve_site results -> the fuzzy-library and obscode-widening verdicts: both must be derived from the captured evidence, not restated as opinion."
    - "18-DECISION.md verdict -> the .rst Key-finding one-liner: both docs state the same fuzzy-library choice and window-schema decision (no divergence)."
---

<objective>
Synthesize Plan 01's captured evidence into decisions and record them so Phases
19-21 can implement without re-deriving anything. Complete `18-DECISION.md`'s
`## Recommendation` section covering all five SCHED-01 criteria: the (already
locked) window schema confirmed against real rows (criterion 1); the fuzzy-match
library verdict grounded in the recorded D-09 scores (criterion 4); the
obscode-widening verdict — default "no" (criterion 5); the TBD natural-key
recommendation restated for Phase 19 (criterion 2, per D-06); and the CSV
range/TBD parsing-rule recommendations for Phase 20 (criterion 3, per D-03/D-04).
Then write the durable summary `docs/design/uncertain_scheduling_spike.rst`
alongside `eso_feasibility_spike.rst` so future milestones can read the verdict
directly.

This plan writes documentation only — no application code, no schema migration
(the phase is investigation-only). It depends on Plan 01's Findings. Producing a
durable `.rst` summary in addition to `18-DECISION.md` follows Phase 13's D-10
dual-doc pattern; per CONTEXT.md this is a sanctioned discretion call, chosen here
because the window-schema and TBD-natural-key decisions are foundational
architecture Phase 19 hard-depends on and belong in the durable design-docs set.

Purpose: Turn real live-test evidence into locked, actionable decisions that gate
the v2.1 scheduling work.
Output: completed `18-DECISION.md` and new
`docs/design/uncertain_scheduling_spike.rst`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-uncertain-scheduling-investigation-spike/18-CONTEXT.md
@.planning/phases/18-uncertain-scheduling-investigation-spike/18-RESEARCH.md
@.planning/phases/18-uncertain-scheduling-investigation-spike/18-PATTERNS.md
@.planning/phases/18-uncertain-scheduling-investigation-spike/18-DECISION.md
@docs/design/eso_feasibility_spike.rst
@docs/design/telescope_runs_calendar.rst
</context>

<phase_nature_note>
No-shippable-code investigation spike (mirrors Phase 13's Plan 02, which likewise
wrote only the decision doc's recommendation plus a durable `.rst`). The
Nyquist/automated-test framing does not literally apply — verification is
structural (required section headers present; verdict stated; no PII leak; house
`.rst` skeleton followed). The existing test suites stay green (nothing in scope
touches a tested module).
</phase_nature_note>

<tasks>

<task type="auto">
  <name>Task 1: Complete 18-DECISION.md — Recommendation for all five SCHED-01 criteria</name>
  <files>.planning/phases/18-uncertain-scheduling-investigation-spike/18-DECISION.md</files>
  <read_first>
    - .planning/phases/18-uncertain-scheduling-investigation-spike/18-DECISION.md (the criterion 2-5 Findings written by Plan 01 — the evidence every recommendation must rest on)
    - .planning/phases/18-uncertain-scheduling-investigation-spike/18-RESEARCH.md ("Open Questions" §1 — the split-verdict possibility for the fuzzy library; "Common Pitfalls" — the cutoff-normalization caveat to cite; "Architectural Responsibility Map" — which tier each downstream capability lands in)
    - .planning/phases/18-uncertain-scheduling-investigation-spike/18-CONTEXT.md (D-06 contact_person-in-key; D-04 check-both-columns; D-09 obscode-widening default "no"; the locked window schema)
    - solsys_code/campaign_utils.py (the _HHMM_RANGE/_APPROX_HOUR/_BARE_HOUR_UTC regex names and parse_obs_window's exact-date-only contract, to reference by name for Phase 20)
  </read_first>
  <action>
    Replace the `## Recommendation` placeholder in `18-DECISION.md` with a completed
    section covering all five SCHED-01 criteria as locked, falsifiable decisions.
    Keep the Plan 01 Findings sections unchanged. Mirror 13-DECISION.md's
    evidence-then-recommendation ordering: each recommendation references the Plan 01
    Finding it rests on.

    - Window schema (criterion 1): confirm the locked decision — a nullable
      `window_start`/`window_end` `DateField` pair (not datetime, not a
      `DateRangeField`) — is validated against the real cell shapes from Plan 01's
      criterion-3 Finding (single night -> window_start == window_end; range ->
      distinct dates; TBD -> both null). State it as the schema Phase 19 implements.
    - Fuzzy-match library (criterion 4): state the verdict — rapidfuzz, difflib, or
      an explicit split verdict (e.g. difflib is sufficient for near-exact code
      matches / rapidfuzz for the long-name cases, per RESEARCH Open Questions §1) —
      derived from Plan 01's recorded D-09 scores, citing specific cases. If the
      verdict is rapidfuzz, note that Phase 21 must add it to `pyproject.toml`
      explicitly (it is only incidentally present via `cleo`, per RESEARCH Pitfall 4)
      and that a straight fuzzy match will NOT bridge JPL/SPICE `500@-170` notation to
      MPC `274` (a distinct Phase 21 problem, per D-09).
    - Obscode widening (criterion 5): record the verdict — no widening needed;
      250/274/289 are 3-char codes fitting `Observatory.obscode` max_length=4, and
      the length guard correctly flags `500@-170` rather than truncating (per D-09).
      Only revisit if a future real code exceeds the length.
    - TBD natural key (criterion 2): restate the D-06 decision as a locked finding
      for Phase 19 — fold `contact_person` (an existing CampaignRun field) into the
      natural key for null-`window_start` rows, via a partial/conditional
      `UniqueConstraint` (exact mechanism is Phase 19's to design), citing the real
      two-JWST-row collision from Plan 01 as the evidence.
    - CSV range/TBD parsing rules (criterion 3): enumerate the recommended parsing
      approach for Phase 20 — one rule per real shape from Plan 01's Finding — as a
      pattern-per-shape extension of the existing `_HHMM_RANGE`/`_APPROX_HOUR`/
      `_BARE_HOUR_UTC` discipline (name them so Phase 20 need not rediscover them),
      NOT a permissive general date parser. Include the D-04 finding that
      range-detection must inspect BOTH `Obs. Date` and `UT Time Range`, and the
      D-05/D-07 "never raise on messy non-key fields; flag needs-review" posture.
      Frame these as recommendations Phase 20 implements — do not describe them as a
      simplified/placeholder version of shippable work.

    Remove the `## Recommendation` `<!-- completed in Plan 02 -->` marker. Introduce
    no email address or contact-person+email pairing.
  </action>
  <verify>
    <automated>D=.planning/phases/18-uncertain-scheduling-investigation-spike/18-DECISION.md; grep -q '## Recommendation' "$D" && ! grep -q 'completed in Plan 02' "$D" && grep -qiE 'rapidfuzz|difflib' "$D" && grep -q 'contact_person' "$D" && grep -qE '_HHMM_RANGE|_APPROX_HOUR|_BARE_HOUR_UTC' "$D" && ! grep -nE '[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}' "$D"</automated>
    <human-check>Each of the five criteria's recommendations references the Plan 01 Finding it rests on; the fuzzy-library verdict cites specific recorded D-09 scores; the obscode verdict is explicitly "no widening"; no scope-reduction language ("v1", "placeholder", "simplified") frames the Phase 19/20 recommendations.</human-check>
  </verify>
  <acceptance_criteria>
    - `18-DECISION.md` has a completed `## Recommendation` section with locked
      decisions for all five SCHED-01 criteria, each tied to a Plan 01 Finding.
    - Criterion 4 names a library (or explicit split verdict) grounded in the
      recorded scores; criterion 5 records "no obscode widening"; criterion 2
      restates the contact_person-in-key decision for Phase 19; criterion 3 names the
      three existing regexes and the D-04 both-columns finding for Phase 20.
    - No placeholder marker remains; the email-address grep is clean.
  </acceptance_criteria>
  <done>18-DECISION.md's ## Recommendation section is complete for all five SCHED-01 criteria, each tied to a Plan 01 Finding, with no placeholder marker and no email-address leak.</done>
</task>

<task type="auto">
  <name>Task 2: Write the durable summary docs/design/uncertain_scheduling_spike.rst</name>
  <files>docs/design/uncertain_scheduling_spike.rst</files>
  <read_first>
    - docs/design/eso_feasibility_spike.rst (the exact skeleton to follow — title underlined with `=`, Background / Key finding sections, bold key-finding one-liner, a list-table for the matrix)
    - docs/design/telescope_runs_calendar.rst (the house style analog: `=` title, `-` sections, bold key-finding line, list-table for the data model)
    - .planning/phases/18-uncertain-scheduling-investigation-spike/18-DECISION.md (source of the verdicts, findings, and recommendations to summarize)
  </read_first>
  <action>
    Create `docs/design/uncertain_scheduling_spike.rst` as a durable summary of the
    spike, following `eso_feasibility_spike.rst`'s structure exactly (do not invent a
    new heading convention — `=` for the title, `-` for sections):

    - Title underlined with `=` matching the title length.
    - Opening paragraph: "This document records..." — what spike this is, when it ran,
      and what it settled (the five SCHED-01 decisions) against the real 3I/ATLAS
      sheet.
    - `Background` section: why FOMO needs a window representation — the real sheet's
      space-mission rows (JWST/HST/Swift) carry ranges or still-TBD dates, not single
      nights — and why this spike gates Phases 19-21. Parallel to how
      eso_feasibility_spike.rst frames its milestone-shaping constraint.
    - `Key finding` section: the **bold** one-line verdicts up front — the chosen
      fuzzy-match library and the confirmed window schema — matching the bold
      key-finding convention and identical to 18-DECISION.md's Recommendation (no
      divergence between the two docs).
    - A `list-table` section mapping each SCHED-01 criterion to its decision (window
      schema; TBD contact_person-in-key; CSV range/TBD parsing rules; fuzzy-match
      library; resolve_site()/obscode-widening verdict) and the phase that implements
      it (19, 19, 20, 21, none/confirmed).
    - A short `Future scope` section pointing to 18-DECISION.md for full evidence and
      framing the recommendations as input to Phases 19-21 (not implemented here).

    Do NOT paste raw redacted CSV cell blocks into the `.rst` — keep it a durable
    prose+table summary; the verbatim evidence lives in 18-DECISION.md. Introduce no
    Contact Person / Email content.
  </action>
  <verify>
    <automated>F=docs/design/uncertain_scheduling_spike.rst; test -f "$F" && grep -qiE 'rapidfuzz|difflib' "$F" && grep -q 'list-table' "$F" && head -3 "$F" | grep -qE '====' && ! grep -nE '[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}' "$F"</automated>
    <human-check>The .rst renders in the same style as eso_feasibility_spike.rst (title `=`, sections `-`, bold Key-finding one-liner) and its fuzzy-library verdict and window-schema decision match 18-DECISION.md's Recommendation exactly.</human-check>
  </verify>
  <acceptance_criteria>
    - `docs/design/uncertain_scheduling_spike.rst` exists, follows the
      eso_feasibility_spike.rst skeleton (title `=`, `-` sections), and opens with a
      "This document records..." paragraph.
    - It states the same fuzzy-library verdict and window-schema decision as
      18-DECISION.md as bold Key-finding one-liners.
    - It includes a list-table mapping each SCHED-01 criterion to its decision and
      implementing phase.
    - It contains no Contact Person / Email content and no raw redacted CSV blocks.
  </acceptance_criteria>
  <done>docs/design/uncertain_scheduling_spike.rst exists in the house .rst style, states the same fuzzy-library verdict and window schema as 18-DECISION.md, includes the criterion→decision→phase list-table, and has no PII leak.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 18-DECISION.md findings -> docs/design/uncertain_scheduling_spike.rst | Content summarized from the decision doc into a second committed file; any residual PII would be duplicated, so the summary stays prose+table only (no raw evidence blocks). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-05 | Information Disclosure | Copying evidence from 18-DECISION.md into the durable .rst | high | mitigate | The .rst is a prose+table summary only; no raw redacted CSV blocks are pasted in (they stay in 18-DECISION.md); Task 2's action forbids Contact Person / Email content and the email-address grep gate plus a human-check confirm. |
| T-18-SC | Tampering | Package installs (npm/pip/cargo) | low | accept | No package installs this plan — it writes documentation only. |
</threat_model>

<verification>
- 18-DECISION.md has a completed `## Recommendation` covering all five SCHED-01
  criteria, each tied to a Plan 01 Finding, with no placeholder marker left.
- docs/design/uncertain_scheduling_spike.rst exists, follows the house skeleton, and
  states the same fuzzy-library verdict and window schema.
- Both docs are free of email addresses and contact-person+email pairings.
- `ruff check .` / `ruff format --check .` stay clean (docs do not trigger ruff).
</verification>

<success_criteria>
- A single set of locked decisions for all five SCHED-01 criteria exists, grounded
  in Plan 01's evidence, sufficient to gate Phases 19-21 without re-deriving.
- Future milestones can read the durable .rst summary without digging into
  .planning/.
- Zero PII leakage into version control.
</success_criteria>

<artifacts_produced>
This plan creates/completes:
- `.planning/phases/18-uncertain-scheduling-investigation-spike/18-DECISION.md` —
  `## Recommendation` section completed for all five SCHED-01 criteria. COMMITTED.
- `docs/design/uncertain_scheduling_spike.rst` — durable summary. COMMITTED.
</artifacts_produced>

<output>
Create `.planning/phases/18-uncertain-scheduling-investigation-spike/18-02-SUMMARY.md` when done.
</output>
