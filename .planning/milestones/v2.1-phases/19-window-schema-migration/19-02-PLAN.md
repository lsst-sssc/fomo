---
phase: 19-window-schema-migration
plan: 02
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - solsys_code/campaign_gap.py
  - solsys_code/tests/test_campaign_gap.py
autonomous: true
requirements: [SCHED-02, SCHED-03]

must_haves:
  truths:
    - "claimed_dates() marks every date in [window_start, window_end] as claimed for a counted run"
    - "A TBD run (window_start is None) is collected into undated_runs and never added to the claimed set"
    - "The existing single-target vs multi-target unattributed_runs bucketing is unchanged"
    - "campaign_gap.py has no dead _observing_night_date helper and no unused zoneinfo/time imports (ruff clean)"
  artifacts:
    - "solsys_code/campaign_gap.py (rewritten claimed_dates; deleted _observing_night_date)"
    - "solsys_code/tests/test_campaign_gap.py (window-based TestClaimedDates)"
  key_links:
    - "qs.only('pk', 'window_start', 'window_end') keeps the claimed-side queryset PII-free (never fetches contact_person/contact_email)"
    - "campaign_gap must not import solsys_code.views/ephem_utils at module scope (heavy SPICE download)"
---

<objective>
Rewrite campaign_gap.claimed_dates() to read the new window_start/window_end date pair
directly — claiming every date in the inclusive range and bucketing TBD (null-window) runs
as undated — and delete the now-obsolete _observing_night_date() helper and its dead imports.

Purpose: coverage-gap analysis is a direct consumer of the old obs_date/ut_start fields; it
must keep working against the new schema (phase success criterion 5). This is a
field-rename-plus-simplification, NOT the asset-aware ground-vs-space claimed_dates() rewrite
— that is explicitly Phase 20 (ASSET-02). Phase 19 only makes claimed_dates() read the new fields.
Output: a window-native claimed_dates() and its rewritten tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-window-schema-migration/19-CONTEXT.md
@.planning/phases/19-window-schema-migration/19-RESEARCH.md
@.planning/phases/19-window-schema-migration/19-PATTERNS.md
@.planning/phases/19-window-schema-migration/19-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite claimed_dates() to iterate the window range; delete _observing_night_date()</name>
  <files>solsys_code/campaign_gap.py</files>
  <read_first>
    - solsys_code/campaign_gap.py (imports lines 16-24; _observing_night_date lines 87-106; observable_dates lines 109-135; claimed_dates lines 138-211, esp. the .only() at line 182 and the obs_date/ut_start branch at lines 196-209)
    - .planning/phases/19-window-schema-migration/19-RESEARCH.md (Code Examples: claimed_dates() rewrite; Pitfall 4 dead-code/imports; State of the Art table)
    - .planning/phases/19-window-schema-migration/19-PATTERNS.md (campaign_gap.py section: use RESEARCH Code Examples verbatim; delete _observing_night_date and its imports)
  </read_first>
  <behavior>
    - A counted approved run with window_start=D1, window_end=D3 contributes D1, D2, D3 to the claimed set (inclusive, n_days = (window_end - window_start).days + 1).
    - A counted approved run with window_start == window_end == D contributes exactly D (single night).
    - A run with window_start is None (TBD) is appended to undated_runs and never added to claimed.
    - Cancelled / not_awarded / weather_tech_failure runs and pending_review runs still claim nothing (unchanged _EXCLUDED_RUN_STATUSES / approval filter).
    - Multi-target campaign: target=None rows still go to unattributed_runs; single-target campaign still does not filter by target (bucketing rule preserved).
  </behavior>
  <action>
    Replace claimed_dates() (lines 138-211) with the window-native version from RESEARCH.md's
    Code Examples section: change the queryset column restriction from qs.only('pk', 'obs_date',
    'ut_start') to qs.only('pk', 'window_start', 'window_end') (keeps it PII-free — D-13/WR-01);
    replace the per-run obs_date/ut_start/_observing_night_date branch with: if
    run.window_start is None -> undated_runs.append(run); else claim every date from
    window_start through window_end inclusive via a timedelta loop
    (n_days = (run.window_end - run.window_start).days + 1). Preserve the single-target vs
    multi-target unattributed_runs bucketing and the _EXCLUDED_RUN_STATUSES exclude exactly as
    they are. Delete the _observing_night_date() function (lines 87-106) entirely, and remove
    the now-unused imports it required: `from zoneinfo import ZoneInfo` and the `time` name from
    `from datetime import date, time, timedelta` (keep date and timedelta — still used). Update
    claimed_dates()'s docstring to describe the window-range claiming and drop the obsolete
    D-06/D-07 obs_date/ut_start-night-derivation wording. Do NOT add any ground-vs-space /
    observations_type logic here — that is Phase 20 (ASSET-02); this claims every date in the
    window for every counted run regardless of site type.
  </action>
  <verify>
    <automated>ruff check solsys_code/campaign_gap.py && python manage.py test solsys_code.tests.test_campaign_gap</automated>
  </verify>
  <acceptance_criteria>
    - `ruff check solsys_code/campaign_gap.py` reports no F401 (unused import) or other errors — proves _observing_night_date and its ZoneInfo/time imports were fully removed (Pitfall 4).
    - grep confirms `_observing_night_date` no longer appears in solsys_code/campaign_gap.py.
    - grep confirms `qs.only(` in claimed_dates references `window_start` and `window_end` and NOT `obs_date`/`ut_start`.
    - `python manage.py test solsys_code.tests.test_campaign_gap` passes (after Task 2 updates the fixtures/assertions).
  </acceptance_criteria>
  <done>claimed_dates() reads window_start/window_end directly, claims full inclusive ranges, buckets TBD runs as undated, keeps the PII-free .only() and unattributed bucketing; _observing_night_date and its dead imports are gone.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite TestClaimedDates for the window schema (delete the night-derivation case)</name>
  <files>solsys_code/tests/test_campaign_gap.py</files>
  <read_first>
    - solsys_code/tests/test_campaign_gap.py (TestClaimedDates line 124: test_approved_completed_run_claimed_via_obs_date line 165, test_ut_start_only_keys_to_site_local_observing_night line 189, test_undated_runs_flagged line 199; TestClaimedDatesMultiTarget line 215; and every obs_date/ut_start reference in fixture seeding across the file — 31 total)
    - solsys_code/campaign_gap.py (post-Task-1 claimed_dates signature/behavior)
    - .planning/phases/19-window-schema-migration/19-RESEARCH.md (Pitfall 5 — which tests need scenario rethink vs rename)
    - CLAUDE.md (NonSiderealTargetFactory only for Target fixtures; ./manage.py test solsys_code)
  </read_first>
  <action>
    Update every fixture that seeds CampaignRuns with obs_date/ut_start/ut_end to use
    window_start/window_end instead. Rewrite test_approved_completed_run_claimed_via_obs_date to
    construct a run with window_start == window_end (rename it to reflect window claiming, e.g.
    test_approved_run_claims_its_single_night_window). DELETE
    test_ut_start_only_keys_to_site_local_observing_night entirely (Pitfall 5 — it tested the
    now-deleted _observing_night_date fallback path; there is no ut_start-to-night derivation
    anymore, so the test is untestable, not renameable). Keep test_undated_runs_flagged but seed
    its undated run with both window fields null. Add a new case asserting a run with
    window_start != window_end claims every date in the inclusive range (proves the range-claim
    loop). Preserve TestClaimedDatesMultiTarget's unattributed-bucketing assertions, updating
    only the field names in its fixtures. Do NOT reintroduce any obs_date/ut_start reference.
  </action>
  <verify>
    <automated>python manage.py test solsys_code.tests.test_campaign_gap</automated>
  </verify>
  <acceptance_criteria>
    - `python manage.py test solsys_code.tests.test_campaign_gap` passes with 0 failures/errors.
    - grep confirms `test_ut_start_only_keys_to_site_local_observing_night` no longer exists in the file.
    - grep confirms zero occurrences of `obs_date`, `ut_start`, or `ut_end` in solsys_code/tests/test_campaign_gap.py.
    - A test asserting inclusive multi-date range claiming (window_start != window_end) is present.
  </acceptance_criteria>
  <done>test_campaign_gap exercises window-based claiming (single night + inclusive range), the TBD→undated bucket, and preserved multi-target unattributed bucketing; the obsolete night-derivation test is deleted.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 02)

- Rewritten `campaign_gap.claimed_dates()` — now reads window_start/window_end (signature unchanged: returns (claimed, undated_runs, unattributed_runs))
- Removed `campaign_gap._observing_night_date()` — deleted helper
- New test case in `test_campaign_gap.py` for inclusive multi-date range claiming
- Removed test case `test_ut_start_only_keys_to_site_local_observing_night`
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| claimed_dates queryset → cached gap result | Fetched CampaignRun fields flow into undated_runs/unattributed_runs and are cached; must never include contact PII |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-02 | Information Disclosure | claimed_dates() queryset column set | high | mitigate | Keep the explicit `.only('pk', 'window_start', 'window_end')` restriction (never contact_person/contact_email) so cached CampaignRun instances carry no PII — preserves the existing D-13/WR-01 discipline while swapping field names (RESEARCH V4/Information Disclosure) |
| T-19-SC | Tampering | package installs | low | accept | No new packages this phase (RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `ruff check solsys_code/campaign_gap.py` clean (no dead-import fallout from removing _observing_night_date).
- `python manage.py test solsys_code.tests.test_campaign_gap` green.
- `python manage.py test solsys_code` full app suite green at wave merge.
</verification>

<success_criteria>
- Coverage-gap analysis reads window_start/window_end and keeps working (phase success criterion 5).
- No asset-awareness added (that is Phase 20); every counted run claims its full window regardless of site type.
- PII-free queryset restriction preserved.
</success_criteria>

<output>
Create `.planning/phases/19-window-schema-migration/19-02-SUMMARY.md` when done.
</output>
