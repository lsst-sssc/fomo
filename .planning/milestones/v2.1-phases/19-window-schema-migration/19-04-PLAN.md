---
phase: 19-window-schema-migration
plan: 04
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - solsys_code/management/commands/import_campaign_csv.py
  - docs/notebooks/pre_executed/import_campaign_csv_demo.ipynb
  - solsys_code/tests/test_import_campaign_csv.py
autonomous: true
requirements: [SCHED-02, SCHED-04]

must_haves:
  truths:
    - "import_campaign_csv maps a parsed single-night CSV row to window_start == window_end (single-night collapse)"
    - "The natural-key lookup passed to insert_or_create_campaign_run keys on window_start, not ut_start"
    - "Two distinct same-telescope/same-date rows with unparseable UT do not silently merge into one row nor crash the import — the duplicate is logged and skipped (D-07/D-08-consistent), never fabricated with a fake sub-second offset"
    - "The import_campaign_csv_demo.ipynb notebook executes end-to-end without a FieldError and its ordering/query cells reference window_start"
  artifacts:
    - "solsys_code/management/commands/import_campaign_csv.py (window natural key + collision rethink)"
    - "docs/notebooks/pre_executed/import_campaign_csv_demo.ipynb (regenerated, committed with executed output)"
    - "solsys_code/tests/test_import_campaign_csv.py (window-key import tests)"
  key_links:
    - "insert_or_create_campaign_run itself is unchanged (generic over lookup/fields); only what the command passes changes"
    - "parse_obs_window is unchanged this phase (still returns a single date + ut_start/ut_end); the command maps its date into window_start/window_end and drops the time-of-day components"
---

<objective>
Update the bootstrap CSV importer to the window schema: key the natural-key lookup on
window_start (the single observing date) instead of ut_start, set window_end to the same date
(single-night collapse), stop passing the removed obs_date/ut_start/ut_end fields, and replace
the now-broken sub-second collision-offset mechanism with a log-and-skip approach consistent
with D-07/D-08. Then update the paired demo notebook per CLAUDE.md's demo-notebook-companion rule.

Purpose: import_campaign_csv is a direct consumer of the old fields and of the old natural key;
it must keep importing rows against the new schema (phase success criterion 5). This phase does
NOT add range/TBD parsing — parse_obs_window still returns a single date (Phase 20 owns
IMPORT-01/02); the importer only remaps the parsed date into the window representation.
Output: a window-native importer, its regenerated demo notebook, and rewritten import tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-window-schema-migration/19-CONTEXT.md
@.planning/phases/19-window-schema-migration/19-RESEARCH.md
@.planning/phases/19-window-schema-migration/19-PATTERNS.md
@.planning/phases/19-window-schema-migration/19-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Window natural key + collision rethink in import_campaign_csv; rewrite import tests</name>
  <files>solsys_code/management/commands/import_campaign_csv.py, solsys_code/tests/test_import_campaign_csv.py</files>
  <read_first>
    - solsys_code/management/commands/import_campaign_csv.py (parse_obs_window call lines 104-106; seen_fallback_keys init line 82 and its collision loop lines 118-132; fields dict lines 139-159 esp. obs_date line 146 and ut_end line 147; the insert_or_create_campaign_run lookup dict line 162)
    - solsys_code/campaign_utils.py (parse_obs_window lines 186-244 — UNCHANGED this phase, still returns (obs_date, ut_start, ut_end, ut_needs_review); insert_or_create_campaign_run lines 277-310 — UNCHANGED, generic over lookup/fields; docstring line 291 mentions the old key)
    - solsys_code/tests/test_import_campaign_csv.py (test_duplicate_unparseable_ut_time_rows_do_not_merge line 421; test_idempotent_rerun_no_duplicates line 511; test_creates_campaignrun_with_existing_observatory line 311; parse_obs_window tests lines 213-272 which stay as-is)
    - .planning/phases/19-window-schema-migration/19-RESEARCH.md (Pitfall 5 — the two named tests need scenario rethink, not rename; Architectural Responsibility Map: import stays same lookup, now against window_start)
    - .planning/phases/19-window-schema-migration/19-PATTERNS.md (import_campaign_csv.py section — lookup key + seen_fallback_keys rethink)
  </read_first>
  <action>
    In import_campaign_csv.py handle(): keep calling parse_obs_window(...) (unchanged) and use
    its returned obs_date as the single observing date. In the fields dict (lines 139-159):
    remove 'obs_date': obs_date and 'ut_end': ut_end (those model fields no longer exist); add
    'window_end': obs_date (single-night collapse — window_end equals the observing date). In the
    insert_or_create_campaign_run lookup dict (line 162): change
    {'campaign': ..., 'telescope_instrument': ..., 'ut_start': ut_start} to
    {'campaign': ..., 'telescope_instrument': ..., 'window_start': obs_date} — window_start is the
    natural-key discriminator now (single night == obs_date). insert_or_create_campaign_run itself
    needs no change (it is generic over lookup/fields); update its docstring line ~291 mention of
    the old key to window_start.

    Replace the seen_fallback_keys sub-second offset mechanism (lines 82, 118-132): window_start is
    date-only, so offsetting by timedelta seconds no longer disambiguates and would produce a
    time component that does not fit a DateField. Instead, track seen keys as
    (campaign.pk, telescope_instrument, obs_date); when a row's key has already been seen in this
    batch, do NOT create a colliding duplicate — log a warning naming telescope_instrument and
    obs_date (consistent with D-07/D-08's log-what-you-drop philosophy), increment skipped_count,
    and continue to the next row without calling insert_or_create_campaign_run. This keeps the
    import within Phase 19 scope (Phase 20 handles real range/TBD disambiguation). Preserve the
    ut_needs_review-driven WARNING wording intent (that the operator should check the source rows'
    UT cells) in the skip log. Remove the now-unused `from datetime import timedelta` import if it
    becomes unused after removing the offset (run ruff to confirm).

    In test_import_campaign_csv.py: leave the parse_obs_window unit tests (lines 213-272) and the
    map_observation_status tests untouched — parse_obs_window is unchanged. Update
    test_creates_campaignrun_with_existing_observatory and other import assertions that read
    obs_date/ut_start/ut_end on the created CampaignRun to assert window_start == window_end ==
    the expected date. Rewrite test_duplicate_unparseable_ut_time_rows_do_not_merge to assert the
    new behavior: two distinct same-telescope/same-date unparseable-UT rows produce exactly one
    CampaignRun plus a logged skip of the duplicate (not two rows via a fake offset, not a crash).
    Update test_idempotent_rerun_no_duplicates to key on the window natural key (re-running the
    same CSV creates no new rows and updates in place). Use NonSiderealTargetFactory for any Target
    fixture (CLAUDE.md).
  </action>
  <verify>
    <automated>ruff check solsys_code/management/commands/import_campaign_csv.py && python manage.py test solsys_code.tests.test_import_campaign_csv</automated>
  </verify>
  <acceptance_criteria>
    - `python manage.py test solsys_code.tests.test_import_campaign_csv` passes with 0 failures/errors.
    - grep confirms the insert_or_create_campaign_run lookup dict in import_campaign_csv.py keys on `window_start` and no longer references `ut_start`.
    - grep confirms the fields dict sets `window_end` and no longer sets `obs_date`/`ut_end`.
    - grep confirms no `timedelta(seconds=` sub-second offset remains in import_campaign_csv.py.
    - A test asserts two same-telescope/same-date unparseable-UT rows yield exactly one row plus a logged skip (no merge, no crash, no fabricated offset).
  </acceptance_criteria>
  <done>The importer keys the natural-key lookup on window_start, sets window_end for a single night, drops the removed fields, and log-and-skips genuine same-date collisions instead of fabricating a sub-second offset; import tests prove it.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate the import_campaign_csv demo notebook against the window schema</name>
  <files>docs/notebooks/pre_executed/import_campaign_csv_demo.ipynb</files>
  <read_first>
    - docs/notebooks/pre_executed/import_campaign_csv_demo.ipynb (all cells; the cell containing CampaignRun.objects.filter(campaign=campaign).order_by('obs_date', 'ut_start') near line 309, plus any cell displaying obs_date/ut_start/ut_end columns)
    - solsys_code/management/commands/import_campaign_csv.py (post-Task-1 behavior, so the demo output matches)
    - CLAUDE.md (demo-notebook-companion convention: pre_executed/ notebooks are committed WITH executed output, regenerated via jupyter nbconvert --to notebook --execute --inplace; import_campaign_csv_demo.ipynb is import_campaign_csv.py's paired notebook)
    - .planning/phases/19-window-schema-migration/19-RESEARCH.md (Runtime State Inventory — Build artifacts row flagging this notebook; not on CONTEXT.md's original 15-file list but required)
  </read_first>
  <action>
    Update every notebook cell that references the removed fields: change the ordering/query cell
    CampaignRun.objects.filter(campaign=campaign).order_by('obs_date', 'ut_start') to order_by
    the window schema (e.g. F('window_start').desc(nulls_last=True) to mirror the new default sort,
    or a plain .order_by('window_start')), and update any cell that reads or displays
    obs_date/ut_start/ut_end columns to use window_start/window_end. If the notebook builds an
    example CSV and imports it, confirm the displayed result rows show window_start/window_end.
    Then regenerate the executed output in place with
    `jupyter nbconvert --to notebook --execute --inplace docs/notebooks/pre_executed/import_campaign_csv_demo.ipynb`
    (this notebook must be committed WITH its executed output per CLAUDE.md's pre_executed
    convention). Ensure the execution completes without a FieldError referencing obs_date/ut_start/ut_end.
  </action>
  <verify>
    <automated>jupyter nbconvert --to notebook --execute --inplace docs/notebooks/pre_executed/import_campaign_csv_demo.ipynb</automated>
  </verify>
  <acceptance_criteria>
    - `jupyter nbconvert --to notebook --execute --inplace docs/notebooks/pre_executed/import_campaign_csv_demo.ipynb` completes with exit 0 (no FieldError).
    - grep confirms the notebook JSON contains no `obs_date`, `ut_start`, or `ut_end` field references and does contain `window_start`.
    - The notebook is committed with non-empty executed cell outputs (pre_executed convention).
  </acceptance_criteria>
  <done>The paired demo notebook executes cleanly against the window schema and is committed with regenerated executed output, satisfying CLAUDE.md's demo-notebook-companion rule.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 04)

- import_campaign_csv natural-key lookup now keys on `window_start` (was ut_start); fields dict sets `window_end` (removed obs_date/ut_end)
- Replaced the seen_fallback_keys sub-second-offset collision mechanism with a log-and-skip-duplicate approach (behavior change, not a rename)
- Regenerated `docs/notebooks/pre_executed/import_campaign_csv_demo.ipynb` (window-schema cells, executed output committed)
- Rewritten import tests: window-key creation, single-night collapse, same-date collision log-and-skip, idempotent rerun
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSV file → CampaignRun rows | Batch import of untrusted sheet data; concurrent/repeat imports must not create colliding rows |
| skip logging → operator | Skipped-duplicate diagnostics must name only natural-key fields, never row PII |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-01 | Tampering/Repudiation | import natural-key lookup | high | mitigate | The window natural-key lookup is backed by the Plan-01 resolved-window UniqueConstraint, so get_or_create stays race-safe; genuine same-date collisions are logged and skipped rather than silently merged or crashing the batch (RESEARCH V11) |
| T-19-06 | Information Disclosure | duplicate-skip log line | low | mitigate | Log only natural-key fields (telescope_instrument, obs_date) in the skip warning — never Contact Person/Email — mirroring the existing WR-06 PII-safe skip-logging discipline already in this command |
| T-19-SC | Tampering | package installs | low | accept | No new packages this phase (RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `ruff check .` clean for import_campaign_csv.py.
- `python manage.py test solsys_code.tests.test_import_campaign_csv` green.
- `jupyter nbconvert --to notebook --execute --inplace docs/notebooks/pre_executed/import_campaign_csv_demo.ipynb` succeeds; notebook committed with output.
- `python manage.py test solsys_code` full app suite green at wave merge.
</verification>

<success_criteria>
- The importer works against the window schema with a window_start natural key and single-night collapse.
- Same-date collisions are handled by log-and-skip (D-07/D-08-consistent), not a fabricated offset.
- The paired demo notebook is regenerated and committed (CLAUDE.md compliance).
</success_criteria>

<output>
Create `.planning/phases/19-window-schema-migration/19-04-SUMMARY.md` when done.
</output>
