---
phase: 20-range-tbd-import-asset-aware-coverage-gap
plan: 04
type: execute
wave: 3
depends_on: ['20-02', '20-03']
files_modified:
  - docs/notebooks/pre_executed/fixtures/campaign_sample.csv
  - docs/notebooks/pre_executed/import_campaign_csv_demo.ipynb
autonomous: true
requirements: [IMPORT-01, IMPORT-02]

must_haves:
  truths:
    - "campaign_sample.csv contains at least one date-range Obs. Date row and at least one genuinely-unparseable TBD Obs. Date row (in addition to the existing exact-date rows)."
    - "The demo notebook has an executed cell that imports the fixture via import_campaign_csv and displays, for the range row, the resulting window_start/window_end, and for the TBD row, window_needs_review=True and the preserved original_obs_date_raw — real executed output committed (pre_executed convention)."
    - "The notebook re-executes cleanly end-to-end against the migrated dev DB (nbconvert --execute exits 0), and stays idempotent on repeat runs (import via the command's get_or_create; the TBD row carries a Contact Person so its natural key is stable)."
  artifacts:
    - "docs/notebooks/pre_executed/fixtures/campaign_sample.csv — a range row and a TBD row added"
    - "docs/notebooks/pre_executed/import_campaign_csv_demo.ipynb — a cell demonstrating the new range/TBD import behavior with committed executed output"
  key_links:
    - "The notebook exercises Plan 03's parse_obs_window range/TBD parsing and import_campaign_csv counter/flag wiring end-to-end against Plan 02's original_obs_date_raw/window_needs_review columns on the migrated dev DB (src/fomo_db.sqlite3)"
  prohibitions:
    - statement: "The added TBD/range fixture rows must not carry real personal data — Contact Person/Email stay synthetic, matching the existing fixture's synthetic-only discipline."
      status: resolved
      verification: "New rows reuse the existing fixture's clearly-synthetic Name/Email style (e.g. '<Name> Test', '<name>@example.com'); no real 3I/ATLAS contributor data is introduced."
---

<objective>
Keep the import_campaign_csv demo notebook a truthful demonstration of the command's behavior by
adding fixture rows and an executed cell that exercise the new range/TBD import path (IMPORT-01,
IMPORT-02). RESEARCH confirmed the notebook currently demonstrates none of this phase's new
behavior — its fixture is all exact-date rows — so without this it would silently drift out of
sync with the command it documents.

Purpose: This is the only executed, end-to-end demonstration of import_campaign_csv. CLAUDE.md's
demo-notebook mandatory-sync list does not formally include this command, but RESEARCH strongly
recommends scoping the update explicitly rather than skipping it (matching the spirit of CLAUDE.md's
"this gap was hit twice already" warning). This plan runs LAST (Wave 3) so it exercises the final
parser + import behavior against the migrated dev DB.

Output: an updated fixture CSV and a re-executed demo notebook with committed output.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-range-tbd-import-asset-aware-coverage-gap/20-CONTEXT.md
@.planning/phases/20-range-tbd-import-asset-aware-coverage-gap/20-RESEARCH.md

# Files this plan changes
@docs/notebooks/pre_executed/fixtures/campaign_sample.csv
</context>

<tasks>

<task type="auto">
  <name>Task 1: Demonstrate range/TBD import in the demo notebook</name>
  <files>docs/notebooks/pre_executed/fixtures/campaign_sample.csv, docs/notebooks/pre_executed/import_campaign_csv_demo.ipynb</files>
  <read_first>
    - docs/notebooks/pre_executed/fixtures/campaign_sample.csv (current 6 exact-date synthetic rows and the 14-column header)
    - docs/notebooks/pre_executed/import_campaign_csv_demo.ipynb (the call_command('import_campaign_csv', ...) cell ~line 234, the Observatory-seeding update_or_create cell ~line 163, and the idempotency note ~line 326 — the notebook connects to the dev DB and is re-runnable)
    - .planning/phases/20-range-tbd-import-asset-aware-coverage-gap/20-RESEARCH.md (the demo-notebook finding — recommends adding a range row + a TBD row + a cell showing window_needs_review/original_obs_date_raw)
    - solsys_code/management/commands/import_campaign_csv.py (final Plan 03 behavior — the summary counters and field wiring the cell will surface)
  </read_first>
  <action>
    Add two rows to campaign_sample.csv, matching the existing 14-column header and synthetic
    Name/Email style: (1) a date-range Obs. Date row (e.g. Obs. Date = "2025-08-01 to 2025-08-15")
    with a Site Code that the notebook already seeds/resolves locally (reuse one of the fixture's
    existing resolvable codes such as F65 or 309, and a distinct Telescope / Instrument so it does
    not collide with an existing row's natural key); (2) a genuinely-unparseable TBD Obs. Date row
    (e.g. Obs. Date = "TBD pending Cycle 2" or "2025-12-?") with a non-blank Contact Person (so its
    TBD natural key (campaign, telescope_instrument, contact_person) is stable and the import stays
    idempotent on re-run) and a distinct Telescope / Instrument. Add a markdown+code cell after the
    existing import cell that, following the notebook's existing query style, fetches the two new
    CampaignRuns and prints: for the range row, window_start and window_end (showing the window was
    created, not skipped — IMPORT-01); for the TBD row, window_needs_review (True) and
    original_obs_date_raw (the preserved raw text) plus window_start/window_end being None
    (IMPORT-02). Optionally note the updated import-summary line now reports the window_needs_review
    counter. Confirm migration 0006 is already applied to the dev DB (Plan 02 applied it; if a fresh
    dev DB is in use, run `python manage.py migrate solsys_code` first — note ./manage.py is not
    executable, use `python manage.py ...`). Regenerate the notebook with executed output via
    `jupyter nbconvert --to notebook --execute --inplace docs/notebooks/pre_executed/import_campaign_csv_demo.ipynb`
    and commit the notebook WITH its output (pre_executed/ copies are committed with output, unlike
    the pre-commit-cleared notebooks elsewhere). Do not introduce any real contributor PII — keep the
    added rows synthetic. If adding rows changes any hardcoded row-count wording in the notebook's
    markdown, update that wording to match.
  </action>
  <verify>
    <automated>jupyter nbconvert --to notebook --execute --inplace docs/notebooks/pre_executed/import_campaign_csv_demo.ipynb</automated>
  </verify>
  <acceptance_criteria>
    - campaign_sample.csv has a date-range Obs. Date row and a TBD/unparseable Obs. Date row (with a non-blank Contact Person), both matching the 14-column header and synthetic-data style
    - The notebook has a cell that prints window_start/window_end for the range row and window_needs_review + original_obs_date_raw for the TBD row
    - `jupyter nbconvert --to notebook --execute --inplace docs/notebooks/pre_executed/import_campaign_csv_demo.ipynb` exits 0 (notebook runs end-to-end against the migrated dev DB)
    - The committed notebook contains executed output for the new demonstration cell (pre_executed convention)
    - No real 3I/ATLAS contributor names/emails added (rows are synthetic)
  </acceptance_criteria>
  <done>
    The demo notebook demonstrates the new range and TBD import behavior with real executed output,
    keeping it an accurate, re-runnable demonstration of import_campaign_csv.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Synthetic fixture CSV -> dev DB | The notebook imports a synthetic, committed fixture into the local dev SQLite DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-08 | Information Disclosure | committed fixture CSV + executed notebook output | low | mitigate | Added rows use clearly-synthetic Names/Emails matching the existing fixture; no real contributor PII enters the committed CSV or the notebook's committed output. |
| T-20-SC | Tampering | package installs | n/a | accept | No new dependency; jupyter/nbconvert are existing dev tooling. No Package Legitimacy Gate needed. |
</threat_model>

<verification>
- `jupyter nbconvert --to notebook --execute --inplace docs/notebooks/pre_executed/import_campaign_csv_demo.ipynb` exits 0
- `ruff check .` and `ruff format --check .` clean (fixture/notebook edits do not introduce lint issues)
- Notebook committed with executed output; new demonstration cell present
</verification>

<success_criteria>
- IMPORT-01/IMPORT-02: the notebook demonstrably imports a range row into a window and a TBD row as a flagged window_needs_review record with preserved original_obs_date_raw, with committed executed output
- The notebook remains re-runnable (idempotent) against a dev DB that already holds a prior execution's rows
</success_criteria>

## Artifacts this phase produces (Plan 04)

- Range + TBD demonstration rows — `docs/notebooks/pre_executed/fixtures/campaign_sample.csv`
- Executed range/TBD import demonstration cell — `docs/notebooks/pre_executed/import_campaign_csv_demo.ipynb`

<output>
Create `.planning/phases/20-range-tbd-import-asset-aware-coverage-gap/20-04-SUMMARY.md` when done.
</output>
