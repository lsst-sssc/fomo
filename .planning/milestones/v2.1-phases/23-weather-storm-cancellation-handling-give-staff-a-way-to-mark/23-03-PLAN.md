---
phase: 23-weather-storm-cancellation-handling-give-staff-a-way-to-mark
plan: 03
type: execute
wave: 2
depends_on: [23-02]
files_modified:
  - solsys_code/tests/test_campaign_approval.py
autonomous: true
requirements: [D-06, D-07]
user_setup:
  - service: dev-database (operator data entry, no external credentials)
    why: "D-06 requires the real GS-2026A-FT-115 informational CampaignRun row to exist in the live dev DB (Didymos 2026 TargetList, pk=1, currently 0 runs). This flows through the EXISTING admin/approval paths — 'no new code path, just new data' — so it is operator data entry, not code. The Plan 03 tests prove the mechanism handles this exact row shape end-to-end."
    dashboard_config:
      - task: "Create the FT-115 CampaignRun via Django admin (or the existing CSV/submission path) under the Didymos 2026 TargetList (pk=1) with: telescope_instrument='Gemini-South GMOS-S', site_raw='I11', window_start=2026-07-13, window_end=2026-07-16, contact_person='Thomas-Osip', observation_details noting 'GS-2026A-FT-115, 6.50 awarded hours' (informational only — NOT a real Gemini ODB submission), target left unset (None), run_status=REQUESTED."
        location: "Django admin -> Solsys_code -> Campaign runs -> Add, or the existing campaign submission form"
      - task: "Let the approval queue resolve the site: 'I11' Tier-2-resolves to 'Gemini South Observatory, Cerro Pachon' (ground-based) via the live MPC ObsCodes API, with a real America/Santiago timezone backfilled automatically. Approve the run through the approval queue. If the storm hits FT-115, mark it Weathered from the Decided table exactly like the Magellan runs (D-07, no special-casing). Because its window is a 4-day range, no CAMPAIGN:{pk} CalendarEvent is projected — marking it weathered sets run_status only and never crashes (verified by this plan's tests)."
        location: "Approval queue -> Decided table"
must_haves:
  truths:
    - "resolve_site('I11') resolves to a ground-based, non-placeholder Observatory (name contains 'Gemini South', real longitude/latitude, non-blank IANA timezone) with no manual admin edit needed (D-06)."
    - "The Gemini FT-115 run (Didymos 2026 TargetList, I11 site, range window 2026-07-13..2026-07-16, target=None) flows through the SAME approve -> mark-status mechanism as any Magellan run, with no special-casing (D-07)."
    - statement: "Approving the range-window Gemini FT-115 run projects NO CalendarEvent, and marking it CANCELLED/WEATHER_TECH_FAILURE afterward sets run_status without crashing and without fabricating a CalendarEvent (RESEARCH Pitfall 1, verified with the real D-06 seed values)."
      verification: backstop
  artifacts:
    - solsys_code/tests/test_campaign_approval.py
  key_links:
    - "The Gemini scenario exercises Plan 02's CampaignRunDecisionView._set_run_status() and its existence-guarded calendar sync — this plan adds no production code, only the fixture + verification the existing mechanism handles it."
    - "resolve_site('I11') routes through the existing Tier-2 MPC lookup + MPCObscodeFetcher.to_observatory() timezone backfill (quick task 260716-h8c)."
---

<objective>
Represent the real Gemini Fast-Turnaround program GS-2026A-FT-115 as an informational
`CampaignRun` and prove the whole storm-cancellation mechanism handles it with no special-casing
(D-06, D-07). This plan adds NO production code — every capability already exists (Plan 02's
`_set_run_status()` action, the existing `resolve_site()` Tier-2 MPC resolver, the range-window
skip-by-design projection). It delivers the reproducible test encoding of the Gemini scenario plus
an operator runbook for creating the live row.

Purpose: When the storm also wipes FT-115 (a 6.50-hour GMOS-S allocation, window 13-16 July), staff
mark it the same way as the Magellan runs. Because its window is a 4-day range, it never had a
projected calendar event — so marking it weathered must set run_status without crashing or
fabricating an event. This plan proves exactly that against the real seed values.

Output: A verified `resolve_site('I11')` test (Gemini South, ground-based, real timezone) exercising
the ACTUAL Tier-2 single-code resolver path via a patched `MPCObscodeFetcher.query`/`to_observatory`
(NOT a `BULK_MPC_FIXTURE` entry — see REVIEW finding #2 below), and an end-to-end Gemini scenario test
(create → approve → mark weathered → mark cancelled → no event fabricated). The live production row is
operator data entry (see user_setup).

Scope boundary (REVIEW finding #5, LOW, acknowledged): the live GS-2026A-FT-115 dev-DB row is
deliberately operator data entry in `user_setup`, NOT a committed fixture or seed command. This is an
intentional, acceptable boundary per RESEARCH Assumption A3 — FT-115 "flows through the same
admin-create-then-approve … path every other CampaignRun uses," so there is no new code path to
commit, only data. CI and code review DO fully verify the *mechanism* that handles this exact row
shape: Task 1 proves I11 site resolution and Task 2 proves the end-to-end create → approve →
mark-status → no-event-fabrication flow against the real D-06 seed values. Only the single live
convenience row itself is operator-created; its behavior is not left unverified.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-weather-storm-cancellation-handling-give-staff-a-way-to-mark/23-CONTEXT.md
@.planning/phases/23-weather-storm-cancellation-handling-give-staff-a-way-to-mark/23-RESEARCH.md
@.planning/phases/23-weather-storm-cancellation-handling-give-staff-a-way-to-mark/23-PATTERNS.md

# Depends on Plan 02's _set_run_status() action (read-only reference):
@solsys_code/campaign_views.py
@solsys_code/campaign_utils.py
</context>

## Artifacts this phase produces (Plan 03)

New symbols created by this plan (excluded from source-drift verification as newly-created):

- New test method `test_resolve_site_i11_resolves_gemini_south_ground_based` — exercises
  `resolve_site()`'s ACTUAL Tier-2 path (single-code `MPCObscodeFetcher.query()` → `to_observatory()`)
  by patching `MPCObscodeFetcher.to_observatory` with a `side_effect` that creates a real ground I11
  Observatory row, mirroring the Phase 21 P04 precedent (STATE.md: "Mocked
  MPCObscodeFetcher.to_observatory() directly (side_effect creating a real Observatory row)").
  **Corrected per REVIEW finding #2 (HIGH, Codex):** the original plan's "add `'I11'` to
  `BULK_MPC_FIXTURE`" approach is WRONG — that fixture feeds `query_all()`/`build_site_candidates()`
  (the bulk candidate-pool / fuzzy-match-widget path), NOT `resolve_site()`, which calls single-code
  `query()` → `to_observatory()` at `campaign_utils.py:184-200`. `to_observatory()`
  (`solsys_code_observatory/utils.py`) reads single-code response keys the bulk-fixture shape does not
  supply, so a `BULK_MPC_FIXTURE`-only test would either hit the live network on Tier-2 or exercise
  the wrong path entirely, giving false confidence about D-06. **No `BULK_MPC_FIXTURE` `'I11'` entry
  is added.**
- New ground-based `I11` `Observatory` fixture (created directly in `setUpTestData`, the same way
  `TestCalendarProjection` creates its `F65` `ground_site`) so the end-to-end scenario approval
  resolves the site deterministically offline via Tier 1.
- New test class `TestGeminiFtScenario` (end-to-end create → resolve site → approve → mark
  weathered → mark cancelled → no CalendarEvent fabricated) in `test_campaign_approval.py`.
- No production-code symbols are created by this plan (D-06/D-07 are "no new code path, just new
  data").

<tasks>

<task type="auto">
  <name>Task 1: Verify resolve_site('I11') resolves Gemini South as a ground-based site with a real timezone</name>
  <files>solsys_code/tests/test_campaign_approval.py</files>
  <read_first>
    - solsys_code/campaign_utils.py — `resolve_site()` (lines ~161-234): CONFIRM REVIEW finding #2
      directly. The resolver walks Tier 1 (`Observatory.objects.get(obscode=code)`) then Tier 2
      (`fetcher = MPCObscodeFetcher(); fetcher.query(code, timeout=10)` then `fetcher.to_observatory()`
      at lines 184-200). It NEVER calls `query_all()`. Therefore `BULK_MPC_FIXTURE` (which feeds
      `query_all()`/`build_site_candidates()`) does NOT influence `resolve_site()` at all — adding
      `'I11'` to it would leave Tier 2 hitting the live MPC network (or, if `query()` is left
      unmocked and offline, falling through to a tier-3 placeholder), NOT proving the D-06 claim.
      Also read `is_placeholder_observatory()`, `NEEDS_REVIEW_NAME_PREFIX` (to assert the resolved
      Observatory is NOT a placeholder).
    - solsys_code/solsys_code_observatory/utils.py — `MPCObscodeFetcher.query()` and
      `to_observatory()` (~lines 119-154): confirm `to_observatory()` reads single-code response keys
      (`obscode`, `rhocosphi`, `rhosinphi`, `created_at`, `updated_at`,
      `uses_two_line_observations`, ...) with no `.get()` defaults — a `BULK_MPC_FIXTURE`-shaped entry
      does not supply these — plus its lat/lon-derived timezone backfill (quick task 260716-h8c).
    - solsys_code/tests/test_campaign_approval.py — the CORRECT precedent for driving Tier 2 offline:
      the Phase 21 P04 tests (search `to_observatory` / `side_effect`) mock
      `MPCObscodeFetcher.to_observatory` directly with a `side_effect` that creates and returns a real
      `Observatory` row (STATE.md notes a bare `query()` mock raises `MissingDataException` because
      `to_observatory()` needs those keys); `TestApprovalSiteResolution` (~lines 428-479) patches
      `MPCObscodeFetcher.query` to control Tier 2 and shows the `resolve_site(...)` +
      `Observatory.objects` assertion style; `TestCalendarProjection.setUpTestData` (~lines 308-325)
      creates the deterministic `F65` `ground_site` `Observatory` row (the Tier-1 shape to copy for
      the I11 fixture: `obscode`, `name`, `short_name`, `lat`, `lon`, `altitude`, `timezone`,
      `observations_type=Observatory.OPTICAL_OBSTYPE`); `ISOLATED_TEST_CACHES` /
      `@override_settings(CACHES=ISOLATED_TEST_CACHES)` cache-isolation the base class applies.
    - RESEARCH.md Code Examples: I11 = "Gemini South Observatory, Cerro Pachon", ground-based
      (verified against minorplanetcenter.net ObsCodes.html); Cerro Pachón is at ~lat -30.24,
      lon -70.74, altitude ~2722 m, timezone America/Santiago (realistic fixture values — exact
      precision is NOT load-bearing; the load-bearing assertions are non-placeholder + ground-based +
      non-blank timezone).
  </read_first>
  <action>
    Do NOT add `'I11'` to `BULK_MPC_FIXTURE` (REVIEW finding #2, HIGH): that fixture only feeds the
    bulk `query_all()`/`build_site_candidates()` widget path, which `resolve_site()` never calls.
    Instead, exercise `resolve_site()`'s ACTUAL Tier-2 path (Tier-1 miss → `MPCObscodeFetcher.query()`
    single-code → `to_observatory()`, at `campaign_utils.py:184-200`) deterministically and offline
    using the established Phase 21 P04 precedent: `patch('solsys_code.campaign_utils.MPCObscodeFetcher.query')`
    (a plain MagicMock so the Tier-2 network call is a no-op that does not raise) AND
    `patch('solsys_code.campaign_utils.MPCObscodeFetcher.to_observatory')` with a `side_effect` that
    creates and returns a real ground-based `I11` `Observatory` row —
    `Observatory.objects.create(obscode='I11', name='Gemini South Observatory, Cerro Pachon', short_name='Gemini South', lat=-30.2407, lon=-70.7366, altitude=2722.0, timezone='America/Santiago', observations_type=Observatory.OPTICAL_OBSTYPE)`
    (realistic Cerro Pachón values; exact lat/lon/altitude precision is not load-bearing). This is the
    same shape STATE.md records for Phase 21 P04 ("Mocked MPCObscodeFetcher.to_observatory() directly
    (side_effect creating a real Observatory row), since to_observatory() reads several MPC-response
    dict keys with no defaults and a bare query() mock would raise MissingDataException"). Add a short
    comment in the test citing finding #2 and the reason `BULK_MPC_FIXTURE` is NOT used here. Then add
    `test_resolve_site_i11_resolves_gemini_south_ground_based` calling `resolve_site('I11')` under
    those two patches and assert: the returned first element is an `Observatory` (not None); it is NOT
    a placeholder (`is_placeholder_observatory(obs)` is False AND its name does not start with
    `NEEDS_REVIEW_NAME_PREFIX`); its name/short_name reflects Gemini South; its `observations_type` is
    the ground/optical (non-satellite) type; its `timezone` is a non-blank IANA zone
    (`'America/Santiago'`); and `needs_review` (the second return element) is False — proving no manual
    admin edit is needed before a Decided-table status change (D-06). Keep the test fully offline (no
    live MPC call). This documents, in-plan and in the test, exactly why the corrected single-code
    Tier-2 mock is the right resolver path so this does not get re-flagged in a future review.
  </action>
  <verify>
    <automated>./manage.py test solsys_code.tests.test_campaign_approval</automated>
  </verify>
  <acceptance_criteria>
    - `./manage.py test solsys_code.tests.test_campaign_approval` passes, including
      `test_resolve_site_i11_resolves_gemini_south_ground_based`.
    - Source assertion (REVIEW finding #2): the new test patches BOTH
      `MPCObscodeFetcher.query` and `MPCObscodeFetcher.to_observatory` (grep for `to_observatory` +
      `side_effect` near the I11 test); it does NOT add or depend on a `BULK_MPC_FIXTURE` `'I11'`
      entry (`grep -n "I11" solsys_code/tests/test_campaign_approval.py` shows only the new test /
      Observatory fixture, never a `BULK_MPC_FIXTURE` key).
    - Behavior: `resolve_site('I11')` returns `(observatory, needs_review)` where the observatory is a
      non-placeholder, ground-based (non-satellite) Gemini South `Observatory` with a non-blank IANA
      timezone (`America/Santiago`) and `needs_review is False` — proving the Tier-2 single-code path
      resolves it with no manual admin edit.
    - The test makes no live network call (Tier-2 `query()` and `to_observatory()` are both patched).
    - `ruff check` / `ruff format --check` clean on the test file.
  </acceptance_criteria>
  <done>
    I11 is proven to resolve to a ground-based Gemini South Observatory with a real timezone via the
    existing resolver, no manual edit required; test passes; ruff clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: End-to-end Gemini FT-115 scenario — same mechanism, no special-casing, no event fabricated</name>
  <files>solsys_code/tests/test_campaign_approval.py</files>
  <read_first>
    - solsys_code/tests/test_campaign_approval.py — `CampaignApprovalTestBase` / `_make_pending_run`
      (its default kwargs and `**overrides` mechanism); `TestCalendarProjection`
      (`test_approve_range_run_creates_no_calendar_event` around line 351 is the closest analog for
      the range-window no-event case) and the Plan 02 `TestRunStatusChange` class (mark-status POST
      pattern this scenario reuses).
    - solsys_code/campaign_views.py — Plan 02's `_set_run_status()` and its existence-guarded
      calendar sync (read-only; this scenario exercises it, does not modify it).
    - CONTEXT.md <specifics> — the exact D-06 seed values: campaign = "Didymos 2026" TargetList,
      telescope_instrument = "Gemini-South GMOS-S", window_start = 2026-07-13, window_end =
      2026-07-16, contact_person = "Thomas-Osip", run_status starts REQUESTED, target = None
      (RESEARCH Open Question 2 recommendation: leave target unset unless operator specifies).
    - CLAUDE.md — `NonSiderealTargetFactory` convention (only relevant if a Target is fixtured;
      D-06 recommends target=None so none is expected).
  </read_first>
  <action>
    Add a `TestGeminiFtScenario(CampaignApprovalTestBase)` class that builds the FT-115 informational
    run with the exact D-06 seed values: create a `TargetList` named "Didymos 2026", then a
    `CampaignRun` under it with `telescope_instrument='Gemini-South GMOS-S'`, `site_raw='I11'`,
    `window_start=date(2026, 7, 13)`, `window_end=date(2026, 7, 16)`, `contact_person='Thomas-Osip'`,
    `observation_details` noting the informational allocation, `target=None`,
    `approval_status=PENDING_REVIEW`. For deterministic offline site resolution, create the ground
    `I11` `Observatory` row directly in `setUpTestData` (the same deterministic way
    `TestCalendarProjection` sets up its `F65` `ground_site`), so approving the run resolves the site
    via Tier 1 with no live MPC call and no mock needed. (Task 1 already separately proves the Tier-2
    resolver path for I11 — this scenario's point is the run_status / no-event-fabrication mechanism,
    not re-proving site resolution.) Log in as staff, approve the run, and assert: (a) approval
    succeeds and, because the window is a 4-day range, `CalendarEvent.objects.filter(url=f'CAMPAIGN:{run.pk}').count() == 0`
    (range-window projection is skipped by design). Then POST `action=mark_weather_failure` and assert:
    (b) the response is a normal redirect (no 500/IntegrityError), (c) `run.run_status ==
    CampaignRun.RunStatus.WEATHER_TECH_FAILURE`, and (d) the CAMPAIGN:{pk} CalendarEvent count is
    STILL 0 (no event fabricated — RESEARCH Pitfall 1, now proven against the real Gemini range
    values, D-07). Then POST `action=mark_cancelled` and assert this is a REAL state transition (NOT
    an idempotent no-op — REVIEW finding, Codex MEDIUM: `WEATHER_TECH_FAILURE` and `CANCELLED` are two
    distinct `RunStatus` values): `run.run_status == CampaignRun.RunStatus.CANCELLED` after this second
    POST, and STILL no CalendarEvent is fabricated (count stays 0). This test creates no new production
    code — it exercises Plan 02's already-built `_set_run_status()` end-to-end to prove the "same
    mechanism, no special-casing" claim (D-07).
  </action>
  <verify>
    <automated>./manage.py test solsys_code.tests.test_campaign_approval.TestGeminiFtScenario</automated>
  </verify>
  <acceptance_criteria>
    - `./manage.py test solsys_code.tests.test_campaign_approval.TestGeminiFtScenario` passes.
    - Behavior: approving the range-window FT-115 run creates 0 CalendarEvents for its url.
    - Behavior (BACKSTOP): marking it weathered returns a 302 redirect (no exception), sets
      `run_status = WEATHER_TECH_FAILURE`, and leaves the CAMPAIGN:{pk} event count at 0.
    - Behavior: a follow-up `mark_cancelled` POST is a REAL transition (WEATHER_TECH_FAILURE →
      CANCELLED, two distinct RunStatus values — not a no-op): `run_status == CANCELLED` afterward and
      STILL no CalendarEvent is fabricated (count stays 0).
    - Source assertion: the run is created with `target=None` and the exact D-06 field values
      (`grep -n "Gemini-South GMOS-S\|Thomas-Osip\|Didymos 2026" solsys_code/tests/test_campaign_approval.py`).
    - `./manage.py test solsys_code` green (full suite, no regression from Plans 01/02 merged).
    - `ruff check` / `ruff format --check` clean.
  </acceptance_criteria>
  <done>
    The Gemini FT-115 range-window run is proven to flow through the same approve → mark-status
    mechanism with no special-casing, projecting no event and never crashing or fabricating one;
    tests pass; full suite green; ruff clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test/operator → existing resolve_site() Tier-2 MPC lookup | I11 resolution reads public MPC ObsCodes data (read-only, no secrets); production row creation is operator data entry through existing authenticated admin/approval paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-07 | Denial of Service | range-window FT-115 run marked cancelled/weathered | high | mitigate | Inherits Plan 02's existence guard (`CalendarEvent.objects.filter(url=f'CAMPAIGN:{run.pk}').exists()`) — this plan's end-to-end test proves the guard holds for the real range-window Gemini data (no IntegrityError, no event fabricated). |
| T-23-01g | Elevation of Privilege | mark-status on the Gemini run | high | mitigate | Inherits Plan 02's server-side `approval_status == APPROVED` business-logic guard and `StaffRequiredMixin`; D-07 explicitly routes FT-115 through the same guarded mechanism, no bypass. |
| T-23-08 | Information Disclosure | I11 Tier-2 MPC lookup | low | accept | Reads only public MPC ObsCodes data; tests mock the fetch (no live call). No new secret or PII surface introduced. |
| T-23-SC | Tampering | package installs | n/a | accept | This plan installs no npm/pip/cargo packages; RESEARCH confirms no new dependencies. |

Block-on `high`: T-23-07 and T-23-01g are high and both mitigated by the inherited Plan 02 guards,
verified end-to-end here with the real Gemini seed values. V2/V3/V4/V5/V6 add no new surface — this
plan is test + operator data entry only.
</threat_model>

<verification>
- `./manage.py test solsys_code.tests.test_campaign_approval` green (Task 1 + Task 2 tests, plus
  Plan 02's tests in the same module).
- `./manage.py test solsys_code` green (full suite, Plans 01/02/03 merged — phase gate).
- `ruff check .` and `ruff format --check .` clean.
- Operator runbook (user_setup) followed to create the live FT-115 row via existing admin/approval
  paths.
</verification>

<success_criteria>
- I11 resolves to a ground-based Gemini South Observatory with a real timezone via the existing
  resolver, no manual edit needed.
- The Gemini FT-115 range-window run flows through the same approve → mark-status mechanism as the
  Magellan runs (D-07), projecting no event and never crashing or fabricating one.
- The live FT-115 informational row exists in the dev DB via operator data entry (user_setup).
</success_criteria>

<output>
Create `.planning/phases/23-weather-storm-cancellation-handling-give-staff-a-way-to-mark/23-03-SUMMARY.md` when done.
</output>
