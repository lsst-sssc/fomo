---
phase: 25-range-window-calendarevent-projection-allow-approved-site-re
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - solsys_code/campaign_views.py
  - solsys_code/tests/test_campaign_approval.py
autonomous: true
requirements: [FIX-01, FIX-02, FIX-03, FIX-04, FIX-05, FIX-06, FIX-07]
must_haves:
  truths:
    - "Approving an APPROVED ground range-window run projects one CalendarEvent per night, count == (window_end - window_start).days + 1 (15 for an 8/1..8/15 fixture), each with dip-corrected sun_event() times (FIX-01/FIX-02)."
    - "The first night's start_time matches sun_event(site, window_start).sunset and the last night's end_time matches sun_event(site, window_end).sunrise — not window_start's sunrise for both (FIX-03)."
    - "Range-window per-night events use the url key CAMPAIGN:{pk}:{date.isoformat()}; a single-night run (window_start == window_end) keeps the bare CAMPAIGN:{pk} key unchanged (D-03)."
    - "Every range-window event title carries the window-context suffix ' (window {window_start}..{window_end})'; single-night titles stay byte-identical to today's unsuffixed format (D-06)."
    - "A satellite range-window run still produces exactly one whole-day-span event under the bare CAMPAIGN:{pk} key with unchanged 00:00->23:59 UTC date-math, and its title carries the window suffix (FIX-04/D-05)."
    - "A sun_event() ValueError on a mid-window night leaves the already-created earlier nights' events in place (partial projection, no transaction.atomic rollback) and approval still succeeds because approve() swallows the raise (RESEARCH Assumption A3 / Open Question 2)."
    - "_set_run_status() updates EVERY matching night's event (both the bare key and the CAMPAIGN:{pk}: per-night keys) with the [CANCELLED]/[WEATHERED] prefix, and the window suffix survives the status change (FIX-05/D-04, Pitfall 1)."
    - "The full solsys_code test suite passes green after the rewrite (FIX-01..FIX-07)."
  prohibitions:
    - statement: "TBD runs (window_start is None) still get zero CalendarEvents on approve"
      status: must-hold
      verification: test
    - statement: "Runs with missing telescope_instrument or an unresolved site still get zero CalendarEvents on approve"
      status: must-hold
      verification: test
    - statement: "_set_run_status() never fabricates an event for a run that never projected one"
      status: must-hold
      verification: test
    - statement: "The three guard-exclusion tests (test_approve_tbd_run_creates_no_calendar_event, test_approve_without_telescope_instrument_creates_no_calendar_event, test_sun_event_valueerror_skips_projection_without_reverting_approval) pass with their bodies unchanged (FIX-07)"
      status: must-hold
      verification: test
    - statement: "StaffRequiredMixin + the approval_status business-logic guards in _set_run_status()/_resolve_site() are preserved exactly, not weakened (D-04)"
      status: must-hold
      verification: source
  artifacts:
    - solsys_code/campaign_views.py
    - solsys_code/tests/test_campaign_approval.py
  key_links:
    - "_calendar_event_title(run) is the SINGLE shared title builder used by both _project_calendar_event() (creation) and _set_run_status() (status update) — prevents the D-06 window suffix from being silently stripped on a status change (Pitfall 1)."
    - "The url__startswith prefix uses a trailing colon f'CAMPAIGN:{run.pk}:' so run.pk=3 never matches CAMPAIGN:34:... (Pitfall 2)."
---

<objective>
Fix `_project_calendar_event()` and `_set_run_status()` in `solsys_code/campaign_views.py` so an
approved, site-resolved range-window `CampaignRun` (window_start != window_end) projects a
multi-day calendar presence instead of being silently invisible, and revise the four Phase 19/23
tests in `test_campaign_approval.py` that currently encode the zero-event behavior as correct.

Purpose: Closes the diagnosed gap (`.planning/debug/range-window-calendar-event.md`) where the
real GS-2026A-FT-115 Gemini South allocation (and every range-window run) never reaches the
calendar. Implements CONTEXT.md D-01 (guard fix), D-02 (per-night ground expansion), D-03
(per-night url key), D-04 (multi-event status sync), D-05 (satellite branch date-math unchanged),
D-06 (window-context title suffix).

Output: Rewritten projection + status-sync logic, one shared title helper, and a test suite that
asserts per-night projection (counts 15/15/4/15) while keeping the TBD / unresolved-site /
missing-telescope_instrument exclusions intact.

Note (visible planning record): this phase has no SPEC.md and no REQUIREMENTS.md-mapped IDs; the
spec-less edge-probe fallback was SKIPPED by the orchestrator (no requirement IDs to probe). The
must_haves above were derived directly from CONTEXT.md D-01..D-07 and RESEARCH.md's FIX-01..FIX-08
test map, which serve as the de facto edge-coverage table for this gap-closure phase.
</objective>

<artifacts_this_phase_produces>
Symbols this plan creates or renames (source-grounding exclusion list for drift verification):

- `_calendar_event_title(run: CampaignRun) -> str` — NEW module-level helper in
  `solsys_code/campaign_views.py` (base title + conditional D-06 window suffix).
- New per-night `CalendarEvent.url` key format `CAMPAIGN:{run.pk}:{night.isoformat()}` (e.g.
  `CAMPAIGN:34:2026-07-13`) — a string format, not a named constant.
- New import symbols pulled into `campaign_views.py`: `timedelta` (datetime), `Q` (django.db.models).
- Renamed / new test methods in `solsys_code/tests/test_campaign_approval.py`:
  - `TestCalendarProjection.test_approve_range_run_creates_one_event_per_night` (renamed from
    `test_approve_range_run_creates_no_calendar_event`).
  - `TestCalendarProjection.test_approve_range_run_space_site_creates_single_whole_day_span_event`
    (NEW, FIX-04).
  - `TestCalendarProjection.test_approve_range_run_partial_projection_on_mid_window_sun_event_error`
    (NEW — locks RESEARCH Assumption A3 / Open Question 2).
  - `TestRunStatusChange.test_mark_range_window_run_updates_every_night_event` (renamed from
    `test_mark_range_window_run_does_not_crash_and_creates_no_event`).
  - `TestSitesNeedingReview.test_resolve_range_run_projects_per_night_calendar_events` (renamed from
    `test_resolve_range_tbd_run_clears_flag_with_no_calendar_event`).
  - `TestSitesNeedingReview.test_resolve_tbd_run_clears_flag_with_no_calendar_event` (NEW —
    genuine window-null TBD-resolve case, preserves the coverage the misnamed test lost).
  - `TestGeminiFtScenario.test_gemini_ft115_range_window_projects_per_night_events` (renamed from
    `test_gemini_ft115_range_window_flows_through_same_mechanism_no_event_fabricated`).
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-range-window-calendarevent-projection-allow-approved-site-re/25-CONTEXT.md
@.planning/phases/25-range-window-calendarevent-projection-allow-approved-site-re/25-RESEARCH.md
@.planning/phases/25-range-window-calendarevent-projection-allow-approved-site-re/25-PATTERNS.md
@.planning/debug/range-window-calendar-event.md
@solsys_code/campaign_views.py
@solsys_code/tests/test_campaign_approval.py
@solsys_code/telescope_runs.py
@solsys_code/calendar_utils.py
@solsys_code/management/commands/load_telescope_runs.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract shared title helper + rewrite _project_calendar_event() for per-night ground projection</name>
  <files>solsys_code/campaign_views.py</files>
  <read_first>
    - solsys_code/campaign_views.py (imports lines 14-52; module dicts 375-389; `_project_calendar_event()` 392-455 in full — the current guard at 412, event_fields at 414-419, satellite branch 420-429, ground branch 442-455)
    - solsys_code/telescope_runs.py (`sun_event(site, date, kind='sun')` at ~251 — returns (setting, rising) astropy Time, raises ValueError on unset timezone / not-exactly-2 crossings)
    - solsys_code/calendar_utils.py (`insert_or_create_calendar_event(lookup, fields)` at ~318 — get_or_create on the exact `lookup` dict; no-churn diff on `fields`)
    - solsys_code/management/commands/load_telescope_runs.py (the `n_nights = day2 - day1 + 1` / `first_night + timedelta(days=i)` inclusive-range idiom at ~76-89 — copy the loop SHAPE only, not the title/description format or the {telescope,instrument,start_time} lookup key)
    - .planning/phases/25-.../25-RESEARCH.md (Pitfall 1 title-drift; Pattern 1 per-night loop; Code Examples guard before/after)
    - .planning/phases/25-.../25-PATTERNS.md (the campaign_views.py analog section with the verbatim current/target guard and satellite/ground branch excerpts)
  </read_first>
  <action>
    In solsys_code/campaign_views.py make these changes (all values below are concrete and locked):

    (a) Imports. Change the datetime import on line 16 from `from datetime import date, datetime` to
    `from datetime import date, datetime, timedelta` (timedelta is NOT currently imported and the
    per-night loop needs it). Add `Q` to the django.db.models import on line 24 so it reads
    `from django.db.models import Case, CharField, Count, EmailField, F, Q, Value, When` (Q is needed
    by Task 2's combined queryset; add it now while editing imports).

    (b) New module-level helper. Add `_calendar_event_title(run: CampaignRun) -> str` immediately
    above `_project_calendar_event()` (after the `_ACTION_TO_RUN_STATUS` dict, ~line 390). It returns
    the base title `f'{run.campaign.name}: {run.telescope_instrument}'`, and when
    `run.window_start != run.window_end` it appends the D-06 window-context suffix so the returned
    string becomes `f'{base} (window {run.window_start}..{run.window_end})'`. For a single-night run
    (window_start == window_end) it returns the base unchanged (byte-identical to today's title).
    Give it a Google-style one-line docstring. This helper is the SINGLE source of truth for the
    title and MUST be reused by Task 2 (Pitfall 1) — do not duplicate the format string.

    (c) Guard (D-01, current line 412). Replace the equality-clause guard with the truthiness guard:
    require `run.telescope_instrument and run.site and run.window_start and run.window_end` — drop the
    `run.window_start == run.window_end` sub-clause and add the `run.window_end` truthiness check
    (REQUIRED, not optional — a half-open window with window_end None must not reach the date-math).
    TBD (window_start None), unresolved-site (site None), and missing telescope_instrument runs stay
    excluded by the surviving truthiness checks.

    (d) event_fields title (current line 415). Change the inline title f-string to a call to the new
    helper: set `'title': _calendar_event_title(run)`. This one change covers BOTH branches (the
    satellite branch and the ground branch reuse event_fields), applying the window suffix uniformly
    — this resolves RESEARCH Open Question 1 / Assumption A2 in the affirmative and is REQUIRED for
    Pitfall-1 consistency (see acceptance criteria).

    (e) Satellite branch (current lines 420-429). Leave the date-math EXACTLY as-is: start_time =
    datetime.combine(window_start, 00:00, utc), end_time = datetime.combine(window_end, 23:59, utc),
    one event under the bare `{'url': f'CAMPAIGN:{run.pk}'}` key, `return True`. Only the title flows
    through the helper now (via step d) — no other satellite-branch edit (D-05).

    (f) Ground branch (current lines 442-455). Replace the single-night construction with an inclusive
    per-night loop mirroring load_telescope_runs' E - S + 1 idiom: compute
    `n_nights = (run.window_end - run.window_start).days + 1` (always >= 1) and `is_range = n_nights > 1`.
    For `i in range(n_nights)`: `night = run.window_start + timedelta(days=i)`. Wrap the existing
    `sun_event(run.site, night, kind='sun')` call in the SAME try/except ValueError block that exists
    today — log via logger.debug referencing `night` (not run.window_start) and re-raise (CR-01: never
    swallow; callers own revert-vs-retry). Build `night_fields = dict(event_fields)`, set its
    start_time from that night's sunset and end_time from that night's sunrise, both via
    `.to_datetime(timezone=dt_timezone.utc).replace(microsecond=0)` exactly as today. Choose the url
    key inside the loop: `f'CAMPAIGN:{run.pk}'` when `not is_range` (single-night — byte-identical to
    today's bare key), else `f'CAMPAIGN:{run.pk}:{night.isoformat()}'` (D-03 per-night key). Call
    `insert_or_create_calendar_event({'url': url}, fields=night_fields)` once per night. After the loop
    `return True`. Partial projection on a mid-loop ValueError is the accepted behavior (RESEARCH
    Assumption A3) — do NOT wrap the loop in transaction.atomic().

    (g) Docstrings/comments (Pitfall 4). Update `_project_calendar_event()`'s docstring (392-407) and
    the guard's inline comment (408-411) so they describe the NEW behavior: a ground range-window run
    now projects one dip-corrected event per night; only TBD runs, unresolved-site runs, and
    missing-telescope_instrument runs are excluded from projection. Describe this by concept — do not
    leave any wording claiming a range run is excluded or never gets an event.
  </action>
  <acceptance_criteria>
    - `grep -n "def _calendar_event_title" solsys_code/campaign_views.py` returns exactly one match, defined above `_project_calendar_event`.
    - The datetime import line contains `timedelta`; the django.db.models import line contains `Q` (`grep -n "from datetime import" solsys_code/campaign_views.py` shows timedelta; `grep -n "from django.db.models import" solsys_code/campaign_views.py` shows Q).
    - The `_project_calendar_event()` guard no longer tests window_start against window_end for equality, and requires run.window_end truthiness (the guard admits a range-window run where window_start != window_end).
    - The ground branch computes `n_nights = (run.window_end - run.window_start).days + 1` and calls `sun_event(run.site, night, kind='sun')` once per night inside the loop.
    - A range-window ground run yields per-night events keyed `CAMPAIGN:{pk}:{ISO date}`; a single-night ground run yields exactly the bare `CAMPAIGN:{pk}` key (verified by the single-night regression test below still passing).
    - The satellite branch's start_time/end_time construction (00:00 / 23:59 UTC combine) is unchanged; only its title now comes from `_calendar_event_title(run)`.
    - Single-night regression + guard-exclusion tests still pass (the range tests are expected RED until Task 3): `./manage.py test solsys_code.tests.test_campaign_approval.TestCalendarProjection.test_approve_single_night_ground_run_creates_dip_corrected_calendar_event solsys_code.tests.test_campaign_approval.TestCalendarProjection.test_approve_single_night_space_run_creates_midnight_utc_placeholder_event solsys_code.tests.test_campaign_approval.TestCalendarProjection.test_approve_tbd_run_creates_no_calendar_event solsys_code.tests.test_campaign_approval.TestCalendarProjection.test_approve_without_telescope_instrument_creates_no_calendar_event solsys_code.tests.test_campaign_approval.TestCalendarProjection.test_sun_event_valueerror_skips_projection_without_reverting_approval -v 2` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>./manage.py test solsys_code.tests.test_campaign_approval.TestCalendarProjection.test_approve_single_night_ground_run_creates_dip_corrected_calendar_event solsys_code.tests.test_campaign_approval.TestCalendarProjection.test_approve_single_night_space_run_creates_midnight_utc_placeholder_event solsys_code.tests.test_campaign_approval.TestCalendarProjection.test_approve_tbd_run_creates_no_calendar_event solsys_code.tests.test_campaign_approval.TestCalendarProjection.test_approve_without_telescope_instrument_creates_no_calendar_event solsys_code.tests.test_campaign_approval.TestCalendarProjection.test_sun_event_valueerror_skips_projection_without_reverting_approval -v 2</automated>
  </verify>
  <done>Shared `_calendar_event_title()` helper exists; the guard admits range-window runs; the ground branch projects one dip-corrected event per night with per-night url keys for ranges and the bare key for single nights; the satellite branch date-math is untouched; single-night and guard-exclusion regression tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite _set_run_status() to update every night's event with the shared title</name>
  <files>solsys_code/campaign_views.py</files>
  <read_first>
    - solsys_code/campaign_views.py (`_set_run_status()` 708-763 in full — the guard 727-746, refresh_from_db 747, and the single-key calendar-sync block 749-760 that must become multi-event; `_RUN_STATUS_CALENDAR_PREFIX` dict 375-382; the `_calendar_event_title()` helper added in Task 1)
    - .planning/phases/25-.../25-RESEARCH.md (Pitfall 1 — the title-drift regression; Pitfall 2 — trailing-colon prefix; D-04 required rewrite Code Example)
    - .planning/phases/25-.../25-PATTERNS.md (the `_set_run_status()` analog section with the verbatim current block and the D-04 target)
  </read_first>
  <action>
    In `_set_run_status()` (solsys_code/campaign_views.py), replace ONLY the calendar-sync block at
    lines 749-760 (the `if CalendarEvent.objects.filter(url=f'CAMPAIGN:{run.pk}').exists():` single-event
    check-and-update). Leave everything above it unchanged — the approval_status business-logic guard
    (727-733), the conditional `.filter(pk=pk, approval_status=APPROVED).update(...)` claim, the
    `updated_count == 0` short-circuit (739-745), and `run.refresh_from_db()` (747) are preserved
    exactly (D-04: "its guard logic ... is unaffected and must be preserved exactly").

    Rewrite the sync block to update EVERY CalendarEvent belonging to this run:
    - Build `matching_events = CalendarEvent.objects.filter(Q(url=f'CAMPAIGN:{run.pk}') | Q(url__startswith=f'CAMPAIGN:{run.pk}:'))`.
      The startswith prefix MUST carry the trailing colon `f'CAMPAIGN:{run.pk}:'` (Pitfall 2 — without
      it, run.pk=3 collides with CAMPAIGN:34:...). This one query covers both the legacy bare-key
      single-night event and the new per-night events.
    - If `matching_events.exists()`: read `prefix = _RUN_STATUS_CALENDAR_PREFIX[new_run_status]`, then
      loop `for event in matching_events:` and call `insert_or_create_calendar_event({'url': event.url},
      fields={...})` per event. The title MUST be `f'{prefix} {_calendar_event_title(run)}'` — reuse the
      Task 1 helper, NEVER a re-derived `f'{run.campaign.name}: {run.telescope_instrument}'` inline
      string (Pitfall 1: a re-derived unsuffixed title would differ from the stored range title, the
      no-churn diff would treat it as a change, save it, and silently strip the D-06 window suffix from
      every night's event). The description stays
      `f'{run.observation_details}\nRun status: {run.get_run_status_display()}'` as today.
    - Preserve the existing "never fabricate an event for a run that never projected one" behavior: the
      `if matching_events.exists()` gate means a run with zero matching events (TBD/unresolved-site/
      missing-telescope_instrument) gets its run_status set but no event is created.

    Update the `_set_run_status()` docstring (720-725) by concept so it no longer describes a range run
    as one that never projected an event — the runs that never project are only TBD, unresolved-site,
    and missing-telescope_instrument runs; a range run now has one-or-more events that all get updated.
  </action>
  <acceptance_criteria>
    - `_set_run_status()` filters CalendarEvent with BOTH `Q(url=f'CAMPAIGN:{run.pk}')` and `Q(url__startswith=f'CAMPAIGN:{run.pk}:')` (trailing colon present).
    - The per-event title is built as `f'{prefix} {_calendar_event_title(run)}'` (calls the shared helper; no inline campaign.name/telescope_instrument f-string in this function).
    - The approval_status guard, conditional `.update()`, `updated_count == 0` short-circuit, and `refresh_from_db()` above the sync block are byte-unchanged.
    - Single-night status-change tests pass: `./manage.py test solsys_code.tests.test_campaign_approval.TestRunStatusChange.test_mark_cancelled_single_night_updates_existing_event_in_place solsys_code.tests.test_campaign_approval.TestRunStatusChange.test_mark_weather_failure_uses_distinct_weathered_prefix solsys_code.tests.test_campaign_approval.TestRunStatusChange.test_mark_status_on_non_approved_run_rejected solsys_code.tests.test_campaign_approval.TestRunStatusChange.test_mark_status_lost_update_race_warns_no_calendar_mutation -v 2` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>./manage.py test solsys_code.tests.test_campaign_approval.TestRunStatusChange.test_mark_cancelled_single_night_updates_existing_event_in_place solsys_code.tests.test_campaign_approval.TestRunStatusChange.test_mark_weather_failure_uses_distinct_weathered_prefix solsys_code.tests.test_campaign_approval.TestRunStatusChange.test_mark_status_on_non_approved_run_rejected solsys_code.tests.test_campaign_approval.TestRunStatusChange.test_mark_status_lost_update_race_warns_no_calendar_mutation -v 2</automated>
  </verify>
  <done>_set_run_status() finds and updates every matching night's event via the combined trailing-colon queryset, builds titles through the shared helper so the window suffix survives [CANCELLED]/[WEATHERED] transitions, preserves the never-fabricate guard, and keeps the approval_status/lost-update guards unchanged; single-night status-change tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Revise test_campaign_approval.py to the per-night contract (4 flips + 2 new tests + docstrings)</name>
  <files>solsys_code/tests/test_campaign_approval.py</files>
  <read_first>
    - solsys_code/tests/test_campaign_approval.py (module docstring 1-16; `_make_pending_run()` 129-143; `TestCalendarProjection` 300-380 incl. the single-night analog 326-334 and the range test 349-354; `TestRunStatusChange` 383-458 incl. `_make_approved_single_night_run` 413-418 and the range test 450-458; `TestSitesNeedingReview` 815-1010 incl. `_make_needs_review_run` 840-848 and the range-resolve test 997-1010; `TestGeminiFtScenario` 2129-2208)
    - solsys_code/campaign_views.py (the rewritten `_project_calendar_event()` and `_calendar_event_title()` from Tasks 1-2 — the source of the url-key scheme and title format the assertions must match)
    - .planning/phases/25-.../25-RESEARCH.md (Phase Requirements -> Test Map FIX-01..FIX-07; Pitfall 3 — the three must-stay-0 tests; re-derived counts 15/15/4/15)
    - CLAUDE.md (Target factories: any new Target fixture must use NonSiderealTargetFactory, never SiderealTargetFactory)
  </read_first>
  <action>
    Revise solsys_code/tests/test_campaign_approval.py. Add `from django.db.models import Q` near the
    top imports if not already present (the combined-key count assertions use it). Fixture windows are
    fixed: TestCalendarProjection/TestRunStatusChange/TestSitesNeedingReview use ground_site F65
    (Australia/Sydney) with 8/1..8/15 = 15 nights; TestGeminiFtScenario uses gemini_south I11
    (America/Santiago) with 7/13..7/16 = 4 nights.

    (1) FIX-01/FIX-02/FIX-03 — rename `test_approve_range_run_creates_no_calendar_event` (349-354) to
    `test_approve_range_run_creates_one_event_per_night`. Keep the 8/1..8/15 fixture and the approval
    POST. Assert the combined-key count
    `CalendarEvent.objects.filter(Q(url=f'CAMPAIGN:{run.pk}') | Q(url__startswith=f'CAMPAIGN:{run.pk}:')).count() == 15`.
    Add FIX-03 span assertions: fetch the first night `CalendarEvent.objects.get(url=f'CAMPAIGN:{run.pk}:2026-08-01')`
    and assert its start_time equals `sun_event(self.ground_site, date(2026, 8, 1), kind='sun')`'s sunset
    (`.to_datetime(timezone=timezone.utc).replace(microsecond=0)`); fetch the last night
    `CalendarEvent.objects.get(url=f'CAMPAIGN:{run.pk}:2026-08-15')` and assert its end_time equals
    `sun_event(self.ground_site, date(2026, 8, 15), kind='sun')`'s sunrise. Assert the window suffix is
    present, e.g. `self.assertIn('(window 2026-08-01..2026-08-15)', first_event.title)`. Keep the
    approval_status == APPROVED assertion.

    (2) FIX-04 — add a NEW test `test_approve_range_run_space_site_creates_single_whole_day_span_event`
    in TestCalendarProjection. Create a SATELLITE Observatory (obscode '250',
    observations_type=Observatory.SATELLITE_OBSTYPE — mirror the existing space fixture at 336-347),
    make a pending run with site_raw='250', window 8/1..8/15, POST approve. Assert the combined-key count
    == 1 (exactly one whole-day event, NOT per-night). Fetch the bare-key event
    `CalendarEvent.objects.get(url=f'CAMPAIGN:{run.pk}')`, assert start_time == datetime(2026, 8, 1, 0, 0,
    tzinfo=timezone.utc) and end_time == datetime(2026, 8, 15, 23, 59, tzinfo=timezone.utc), and assert
    the window suffix `(window 2026-08-01..2026-08-15)` is in event.title (D-05 date-math unchanged, A2
    suffix applied uniformly).

    (3) FIX-05 — rename `test_mark_range_window_run_does_not_crash_and_creates_no_event` (450-458) to
    `test_mark_range_window_run_updates_every_night_event`. Use `_make_approved_single_night_run(window_start=date(2026, 8, 1), window_end=date(2026, 8, 15))`
    (approval now projects 15 events). Assert the combined-key count == 15 before the status change. POST
    action 'mark_cancelled'. After refresh, assert run_status == CANCELLED, the combined-key count stays
    == 15, and EVERY event's title starts with `'[CANCELLED] '` AND contains `'(window 2026-08-01..2026-08-15)'`
    (iterate the queryset). This proves Pitfall 1 is fixed — the suffix survives the prefix transition.

    (4) FIX-06 — rename `test_resolve_range_tbd_run_clears_flag_with_no_calendar_event` (997-1010) to
    `test_resolve_range_run_projects_per_night_calendar_events`. Keep the 8/1..8/15 range resolve_site
    POST. Assert run.site_id == self.ground_site.pk, run.site_needs_review is False, the combined-key
    count == 15, and the success message is `'Site resolved — run added to the calendar.'` (NOT
    'Site resolved.'). Then ADD a genuine-TBD test `test_resolve_tbd_run_clears_flag_with_no_calendar_event`
    that resolves a run with window_start=None, window_end=None: assert site set, site_needs_review False,
    combined-key count == 0, and message `'Site resolved.'` — this preserves the TBD-resolve coverage the
    old misnamed test never actually exercised.

    (5) FIX-02/FIX-05 — rename `TestGeminiFtScenario.test_gemini_ft115_range_window_flows_through_same_mechanism_no_event_fabricated`
    (2162) to `test_gemini_ft115_range_window_projects_per_night_events`. The window is 7/13..7/16 (4
    nights). After approve, assert combined-key count == 4 (replacing the count == 0 at 2183). After
    mark_weather_failure, assert count stays 4 and every event title starts with `'[WEATHERED] '` and
    contains `'(window 2026-07-13..2026-07-16)'` (replacing the count==0 at 2193). After mark_cancelled,
    assert count stays 4 and every event title starts with `'[CANCELLED] '` and contains the same window
    suffix (replacing 2202). Keep the D-06 seed-value anchor assertions at 2204-2208 unchanged.

    (6) Docstrings (Pitfall 4). Update the module docstring (1-16), `TestCalendarProjection` (300-306),
    `TestRunStatusChange` (383-391), and `TestGeminiFtScenario` (2130-2137) by concept so none of them
    describe a range-window run as one that projects nothing / is excluded by design — they now describe
    per-night projection for ground ranges, single whole-day span for satellite ranges, with only
    TBD / missing-telescope_instrument / unresolved-site runs excluded.

    (7) FIX-07 — do NOT modify the bodies of `test_approve_tbd_run_creates_no_calendar_event` (356),
    `test_approve_without_telescope_instrument_creates_no_calendar_event` (363), or
    `test_sun_event_valueerror_skips_projection_without_reverting_approval` (370). They use the default
    single-night fixture window and must keep passing byte-identical (docstring-only edits allowed if
    genuinely needed, but leave the assertions untouched).

    (8) Partial-projection lock (RESEARCH Assumption A3 / Open Question 2) — add a NEW test
    `test_approve_range_run_partial_projection_on_mid_window_sun_event_error` in TestCalendarProjection.
    Make a pending 4-night ground range run (window 8/1..8/4). Patch
    `solsys_code.campaign_views.sun_event` so it returns real values for the first two nights and raises
    ValueError on the third night — the simplest robust mechanism is a side_effect function that
    delegates to the real `sun_event` for dates before 2026-08-03 and raises `ValueError('no crossings')`
    for 2026-08-03 (import the real function under an alias, or capture it before patching). POST approve.
    approve() swallows the ValueError (its existing except-at-call-site behavior), so assert the run's
    approval_status is APPROVED and the combined-key count == 2 (the first two nights' events survive —
    no transaction rollback), proving partial projection is the accepted, no-atomic-wrap behavior. This
    test is REQUIRED — it is the explicit lock RESEARCH Open Question 2 mandates for the A3 decision.
  </action>
  <acceptance_criteria>
    - The four range tests assert per-night combined-key counts of 15, 15, 4, and 15 respectively (TestCalendarProjection range, TestRunStatusChange range, TestGeminiFtScenario, TestSitesNeedingReview resolve).
    - The revised TestCalendarProjection range test asserts the first night's start_time and the last night's end_time against sun_event() for window_start/window_end respectively (FIX-03).
    - `test_approve_range_run_space_site_creates_single_whole_day_span_event` asserts exactly one event with 00:00->23:59 UTC span and the window suffix (FIX-04).
    - `test_resolve_range_run_projects_per_night_calendar_events` asserts the 'Site resolved — run added to the calendar.' message; a separate `test_resolve_tbd_run_clears_flag_with_no_calendar_event` asserts count == 0 and 'Site resolved.'.
    - `test_approve_range_run_partial_projection_on_mid_window_sun_event_error` asserts APPROVED + combined-key count == 2 after a mid-window sun_event ValueError (partial projection, no rollback — RESEARCH Open Question 2 lock).
    - The three FIX-07 tests' assertion bodies are unchanged (`git diff` shows no assertion edits within lines 356-380).
    - Full targeted module green: `./manage.py test solsys_code.tests.test_campaign_approval -v 2` exits 0.
    - `ruff check solsys_code/campaign_views.py solsys_code/tests/test_campaign_approval.py` and `ruff format --check solsys_code/campaign_views.py solsys_code/tests/test_campaign_approval.py` both exit 0.
  </acceptance_criteria>
  <verify>
    <automated>./manage.py test solsys_code.tests.test_campaign_approval -v 2 && ruff check solsys_code/campaign_views.py solsys_code/tests/test_campaign_approval.py && ruff format --check solsys_code/campaign_views.py solsys_code/tests/test_campaign_approval.py</automated>
  </verify>
  <done>The four range tests assert per-night counts (15/15/4/15) with first/last-night span checks and window-suffix survival across status prefixes; a satellite-range single-event test and a genuine-TBD-resolve test are added; stale docstrings are updated; the three guard-exclusion tests are unchanged; the full test_campaign_approval module is green and ruff is clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| staff user -> CampaignRunDecisionView.post() | Authenticated staff (StaffRequiredMixin) submit approve / resolve_site / mark_cancelled / mark_weather_failure actions; only server-side model fields (run.pk, window_start/window_end, campaign.name, telescope_instrument) feed the projection and title/url construction — no request free-text reaches the new code paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-01 | Elevation of Privilege | `_set_run_status()` / `_resolve_site()` staff-only status/site actions | medium | mitigate | Preserve `StaffRequiredMixin` + the `approval_status == APPROVED` business-logic guard and the conditional `.update()` / `updated_count == 0` short-circuit EXACTLY (D-04). Task 2 edits only the calendar-sync block below the guard; acceptance criteria assert the guard block is byte-unchanged. |
| T-25-02 | Tampering | New per-night `CalendarEvent.url` key `CAMPAIGN:{pk}:{date.isoformat()}` and the `_set_run_status()` `url__startswith` lookup | low | mitigate | The url key is built entirely server-side from trusted model fields (`pk` int, `window_start`/`window_end` DateField), never from request text — no injection surface. The trailing-colon prefix `f'CAMPAIGN:{run.pk}:'` (Pitfall 2) prevents a one-vs-two-digit pk substring collision that would cross-mutate another run's events (correctness safeguard). |
| T-25-03 | Information Disclosure | Per-night event titles carrying the window-context suffix | low | accept | Titles expose only campaign name, telescope_instrument, and the window dates — all already public on the per-campaign calendar for single-night runs; a range run's window dates are no more sensitive. No PII (contact_person/contact_email) is added to any title or description. |
</threat_model>

<verification>
Wave-merge gate for this plan:
- `./manage.py test solsys_code` — full app suite green (proves the range flips, the satellite-range
  and TBD-resolve additions, and the three FIX-07 exclusions all pass together).
- `ruff check .` and `ruff format --check .` — clean.
- Manual grep sanity (checker-facing, not an automated gate): after this plan, no docstring/comment in
  `campaign_views.py` or `test_campaign_approval.py` should still describe a range-window run as
  excluded-from / never-getting a calendar event.
</verification>

<success_criteria>
- A ground range-window approve projects (window_end - window_start).days + 1 dip-corrected per-night
  events; a single-night approve is byte-identical to today (bare key, unsuffixed title).
- A satellite range-window approve projects exactly one whole-day-span event (date-math unchanged).
- Marking a range run cancelled/weathered prefixes every night's event and preserves the window suffix.
- TBD, unresolved-site, and missing-telescope_instrument runs still project zero events.
- `./manage.py test solsys_code` is green; ruff is clean.
</success_criteria>

<output>
Create `.planning/phases/25-range-window-calendarevent-projection-allow-approved-site-re/25-01-SUMMARY.md` when done.
</output>
