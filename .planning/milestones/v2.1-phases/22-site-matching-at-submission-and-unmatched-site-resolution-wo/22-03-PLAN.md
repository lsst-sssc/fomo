---
phase: 22-site-matching-at-submission-and-unmatched-site-resolution-wo
plan: 03
type: execute
wave: 3
depends_on: [22-02]
files_modified:
  - solsys_code/campaign_views.py
  - solsys_code/campaign_tables.py
  - src/templates/campaigns/approval_queue.html
  - solsys_code/tests/test_campaign_approval.py
autonomous: true
requirements: [D-06, D-07, D-08, D-10]
must_haves:
  truths:
    - "The approval-queue page shows a third 'Sites Needing Review' table listing approved runs with site_needs_review=True (D-07), built from the same once-per-request candidate_pool"
    - "POSTing action=resolve_site on such a run resolves the site via the existing display-string→obscode pool mapping + resolve_site(create_placeholder=False), then fires the deferred CalendarEvent projection for a single-night run (D-08)"
    - "A projection failure during resolve_site saves the resolved site but KEEPS site_needs_review=True, so the run stays visible in the Sites Needing Review table — its retry surface — with a warning; approval_status stays APPROVED, never reverted (22-REVIEWS.md finding 3)"
    - "site_needs_review flips to False ONLY after _project_calendar_event() returns without raising — never before the projection attempt (22-REVIEWS.md finding 3)"
    - "The site write is a single conditional queryset update filtered on pk + APPROVED + site_needs_review=True + site is null, mirroring the approve/reject staleness guard, so two racing staff resolve POSTs cannot double-write the site (D-06 concurrency-safe — 22-REVIEWS.md finding 5)"
    - "resolve_site never re-resolves a run whose site is already set (D-06); such a run (the projection-failed retry state) skips resolution, re-attempts the projection, and clears the flag only on success (22-REVIEWS.md finding 8); a POST for a run that is not APPROVED+needs-review is rejected with a warning"
    - "_project_calendar_event() returns True when insert_or_create_calendar_event() was called and False when projection was skipped by design, driving the two distinct success messages (22-REVIEWS.md finding 6)"
    - "A single-night ground run gets a dip-corrected event; a range/TBD run just clears the flag with no event"
  artifacts:
    - "solsys_code/campaign_views.py :: _project_calendar_event() -> bool module-level helper (extracted from the approve branch)"
    - "solsys_code/campaign_views.py :: CampaignRunDecisionView.post() resolve_site action branch"
    - "solsys_code/campaign_views.py :: ApprovalQueueView.get_context_data() review_table (third table)"
    - "solsys_code/campaign_tables.py :: ApprovalQueueTable resolve-mode render_site()/render_actions()"
    - "src/templates/campaigns/approval_queue.html :: Sites Needing Review render_table block"
  key_links:
    - "ApprovalQueueView review_qs (APPROVED + site_needs_review=True) → review_table → approval_queue.html — naturally includes projection-failed retry rows (site set, flag still True)"
    - "CampaignRunDecisionView.post() resolve_site → conditional site-claim update → _project_calendar_event(run) → insert_or_create_calendar_event → only then site_needs_review=False"
    - "approve branch also calls _project_calendar_event(run) (ignoring its bool return) inside its UNCHANGED revert-on-failure try/except"
---

<objective>
Close the last Phase 21 gap: approved runs whose site never resolved (`site_needs_review=True`)
are currently a dead end — no CalendarEvent, no UI to fix them. This plan adds (1) a third
"Sites Needing Review" table on the existing approval-queue page listing those runs (D-07,
reusing `ApprovalQueueTable` + the page's once-per-request candidate pool), site-unresolved
rows carrying the Plan 02 live-search widget (retry rows show their already-resolved site as
plain text) plus a single "Resolve" action on every row; and (2) a new `resolve_site`
action on `CampaignRunDecisionView.post()` (D-08) that resolves the site via the existing
display-string→obscode pool mapping + `resolve_site(create_placeholder=False)`, honoring the
Phase 21 D-06 never-re-resolve guard, then fires the deferred CalendarEvent projection in the
same request. The projection block currently inlined in the approve branch is factored into a
shared `_project_calendar_event(run)` helper called by both approve (inside its UNCHANGED
revert-on-failure try/except) and resolve_site (inside its own NON-reverting try/except).

Purpose: "Site failure never blocks approval" stays true, but an approved run with an
unresolved site now has a resolution workflow instead of requiring a manual DB edit — and
resolving it retroactively projects the calendar event approval skipped.

Output: `_project_calendar_event()` helper; a `resolve_site` decision-view branch; a third
table in `ApprovalQueueView`; resolve-mode rendering in `ApprovalQueueTable`; the
`approval_queue.html` third-table block; and a `TestSitesNeedingReview` test class.

## Artifacts this phase produces (Plan 03 scope)
New/changed symbols (net-new for source-drift purposes):
- `_project_calendar_event(run: CampaignRun) -> bool` module-level helper — `campaign_views.py`
  (extracted from the approve branch's inline projection block with logic otherwise verbatim; returns
  True when `insert_or_create_calendar_event()` was called, False when projection was skipped by design;
  MAY RAISE on unexpected failure — callers own error handling; the approve branch ignores the return)
- new `'resolve_site'` action value handled in `CampaignRunDecisionView.post()` — `campaign_views.py`
  (site write via a conditional queryset update; site_needs_review cleared only after successful projection)
- `review_table` context key + `review_qs` in `ApprovalQueueView.get_context_data()` — `campaign_views.py`
- a `mode`/resolve constructor flag (e.g. `mode='pending'|'resolve'`) on `ApprovalQueueTable.__init__`,
  extending the existing `show_actions` boolean convention — `campaign_tables.py`
- resolve-mode branch in `ApprovalQueueTable.render_site()` and a "Resolve" button in `render_actions()` — `campaign_tables.py`
- `Sites Needing Review` `{% render_table review_table %}` block — `approval_queue.html`
- `TestSitesNeedingReview` test class — `test_campaign_approval.py`

## Review disposition (22-REVIEWS.md — Plan 03 findings)

| Finding | Disposition | Where |
|---------|-------------|-------|
| HIGH #3 — as previously planned, `site_needs_review=False` was saved BEFORE `_project_calendar_event(run)`; on projection failure the row vanished from the review table (the only retry surface), recreating the dead end this phase closes | **Incorporated** | Task 1: reordered flow — the conditional claim update writes `site` ONLY; `site_needs_review` flips to False strictly AFTER a successful projection. On projection failure the flag stays True, the resolved site stays saved, a warning tells staff to retry, and a test asserts the run is still listed in review_table. Warning copy amended accordingly (supersedes the 22-UI-SPEC.md "Contact a coordinator" copy — the row itself is now the retry path). T-22-08 amended. |
| MEDIUM #5 — the D-06 never-re-resolve guard (plain re-fetch + `run.site is None` check) races between two staff POSTs | **Incorporated** | Task 1: the site write is a single conditional `.filter(pk=pk, approval_status=APPROVED, site_needs_review=True, site__isnull=True).update(site=site)` claim — the same single-statement guard pattern the approve/reject branch already uses (`updated_count == 1`), chosen over `transaction.atomic()` + `select_for_update()` because it matches the existing codebase discipline and behaves uniformly on SQLite. The losing POST matches 0 rows, warns, and never projects. T-22-07 amended; lost-race test added. |
| MEDIUM #6 — `_project_calendar_event() -> None` cannot distinguish "created event" from "skipped", but two different success messages are wanted | **Incorporated** | Task 1: helper returns bool (True = `insert_or_create_calendar_event()` called; False = skipped by design, including the sun_event ValueError skip); resolve_site picks 'Site resolved — run added to the calendar.' vs 'Site resolved.' from it; approve branch ignores the return. |
| LOW #8c — expected `site_needs_review` state for the "run already has `run.site` set" edge unspecified | **Incorporated** | Task 1 + Task 2: a run with site set + flag True is exactly the projection-failed retry state — resolve_site skips resolution (D-06, resolve_site not called), re-attempts the projection, and clears the flag only on success; repeated projection is safe because `insert_or_create_calendar_event()` creates the event if missing, otherwise updates it in place (no duplicates). Task 2 renders such rows with plain-text site (the existing `site_short_name` early-return) + the Resolve button. |
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-site-matching-at-submission-and-unmatched-site-resolution-wo/22-CONTEXT.md
@.planning/phases/22-site-matching-at-submission-and-unmatched-site-resolution-wo/22-RESEARCH.md
@.planning/phases/22-site-matching-at-submission-and-unmatched-site-resolution-wo/22-PATTERNS.md
@.planning/phases/22-site-matching-at-submission-and-unmatched-site-resolution-wo/22-UI-SPEC.md
@.planning/phases/22-site-matching-at-submission-and-unmatched-site-resolution-wo/22-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract _project_calendar_event() + add resolve_site decision action (D-08, D-06)</name>
  <files>solsys_code/campaign_views.py, solsys_code/tests/test_campaign_approval.py</files>
  <read_first>
    - solsys_code/campaign_views.py — read `CampaignRunDecisionView.post()` in full (lines ~344-477): the action guard (`if action not in ('approve', 'reject')`), the conditional `.update()` staleness pattern (`updated_count == 1`), the D-06 `if run.site is None` guard + CR-01 `build_site_candidates().get(selection, selection)` → obscode mapping (lines ~366-389), the inline projection block to extract (lines ~391-449), and the approve-only `except Exception:` revert-to-PENDING_REVIEW block (lines ~450-465). Confirm module-scope imports already present: `datetime`, `dt_time`, `dt_timezone`, `messages`, `Observatory`, `sun_event`, `insert_or_create_calendar_event`, `build_site_candidates`, `resolve_site`, `logger`, `get_object_or_404`, `redirect`.
    - .planning/phases/22-.../22-RESEARCH.md — Pattern 4 (shared, revert-agnostic `_project_calendar_event` — verbatim logic), the "reverting vs non-reverting split" anti-pattern, Pitfall 2 (re-fetch run fresh; re-check `run.site is None`), Security Domain "business-logic bypass" row.
    - .planning/phases/22-.../22-PATTERNS.md — "CampaignRunDecisionView.post() resolve_site action + _project_calendar_event()" section (the exact extraction target + the resolve_site branch shape; NOTE: this plan's flow ordering and helper signature supersede the PATTERNS sketch per 22-REVIEWS.md findings 3/5/6).
    - .planning/phases/22-.../22-REVIEWS.md — Plan 22-03 review (findings 3, 5, 6, 8c); this task implements all four.
    - solsys_code/tests/test_campaign_approval.py — read `TestApproval` (lines ~156-270), `TestCalendarProjection` (lines ~273-354), and `TestSiteSelectionResolution` (lines ~442-490) for the fixtures (Observatory F65 ground site, space site, `_make_pending_run`), the `patch('solsys_code.campaign_views.resolve_site'|'sun_event'|'build_site_candidates')` conventions, and the CalendarEvent assertion style.
  </read_first>
  <behavior>
    Write these tests FIRST (RED) in test_campaign_approval.py (a new `TestSitesNeedingReview(CampaignApprovalTestBase)`),
    logged in as staff, with an approved + `site_needs_review=True` run and a resolvable Observatory fixture:
    - resolve success (single-night ground run): POST `action=resolve_site` with a `site_selection` that
      maps to an existing Observatory resolves `run.site`, sets `site_needs_review=False`, and creates
      exactly one `CalendarEvent` at `url=CAMPAIGN:{pk}`. approval_status stays APPROVED. Success message
      is 'Site resolved — run added to the calendar.' (helper returned True).
    - never-re-resolve + retry semantics (D-06 / finding 8): for a run whose `run.site` is already set
      (and `site_needs_review=True` — the projection-failed retry state), POST `action=resolve_site`
      does NOT call `resolve_site` (patch it and assert_not_called) and does not overwrite the site —
      but DOES re-attempt the projection and clears `site_needs_review` on success.
    - retryable projection failure (finding 3): patch `_project_calendar_event` (or `sun_event`) to raise;
      POST `action=resolve_site`; the run stays APPROVED (NOT reverted to PENDING_REVIEW), `run.site` keeps
      the resolved value, `site_needs_review` stays True, a warning message is surfaced, AND a subsequent
      staff GET of the approval queue still lists the run in `review_table` (the retry surface is preserved —
      assert on the view context, not just the model field).
    - lost-race no-op (finding 5): simulate a concurrent resolution landing between the fresh fetch and the
      site write — e.g. patch `resolve_site` with a side_effect that ALSO directly sets the row's site in
      the DB (a stand-in for the second staff member's POST winning) before returning a different
      Observatory; the view's conditional claim update then matches 0 rows, so the loser's site value is
      NOT written, no projection fires for the loser, and a warning message is surfaced.
    - state validation (business-logic bypass): POST `action=resolve_site` for a run that is PENDING_REVIEW
      (or already resolved, site_needs_review=False) makes no change and surfaces a "not awaiting resolution" warning.
    - range/TBD run: resolving a run with `window_start != window_end` (or both None) sets the site and
      clears the flag but creates NO CalendarEvent, and is not an error. Success message is the plain
      'Site resolved.' (helper returned False — finding 6).
    - unresolvable selection: `resolve_site` returns (None, True) → messages.error with the UI-SPEC copy,
      site stays None, `site_needs_review` stays True, run stays listed in review_table.
    - approve-path regression: the existing `TestApproval`/`TestCalendarProjection` tests still pass after
      the projection block is extracted into `_project_calendar_event` (approve behavior unchanged; the
      approve branch ignores the helper's new bool return).
  </behavior>
  <action>
    Extract the approve branch's inline projection block (the
    `if run.telescope_instrument and run.site and run.window_start and run.window_start == run.window_end:`
    block, current lines ~391-449) into a NEW module-level function
    `def _project_calendar_event(run: CampaignRun) -> bool:` with logic otherwise VERBATIM (the single-night
    guard, the SATELLITE_OBSTYPE midnight-UTC branch, and the ground-site `sun_event(..., kind='sun')` branch
    with its `except ValueError: logger.debug(...)` skip) plus ONE signature change (22-REVIEWS.md finding 6):
    return `True` immediately after each of the two `insert_or_create_calendar_event(...)` calls (satellite
    branch and ground-success branch); return `False` on every skip path (the single-night/site/telescope
    guard failing, and the sun_event ValueError skip). The helper MAY RAISE on unexpected failure and does
    NO error-handling of its own — callers own revert-vs-non-revert. Replace the approve branch's inline
    block with a call to `_project_calendar_event(run)` IGNORING the return value, keeping it INSIDE the
    approve branch's existing, UNCHANGED `except Exception: ... update(approval_status=PENDING_REVIEW) ...`
    revert block (do not touch that except block's structure, message, or the `if run.site is None:` D-06
    guard above it).

    Extend the action guard to `if action not in ('approve', 'reject', 'resolve_site'): return HttpResponseBadRequest()`.
    Add an `elif action == 'resolve_site':` branch (staff-gated already via StaffRequiredMixin on the view)
    with this EXACT ordering — the flag must never be cleared before the projection succeeds
    (22-REVIEWS.md finding 3), and the site write must be a single conditional statement
    (22-REVIEWS.md finding 5):

    (1) Re-fetch `run = get_object_or_404(CampaignRun, pk=pk)` FRESH from the DB (Pitfall 2). Validate state
    server-side (Security "business-logic bypass"): if `run.approval_status != CampaignRun.ApprovalStatus.APPROVED
    or not run.site_needs_review`, `messages.warning(request, 'This run is not awaiting site resolution.')` and
    `return redirect('campaigns:approval_queue')`.

    (2) Resolution — only when `run.site is None` (D-06 never-re-resolve): read
    `selection = request.POST.get('site_selection', '').strip() or run.site_raw`, map it via
    `obscode_selection = build_site_candidates().get(selection, selection)` (CR-01 display-string→obscode),
    call `site, needs_review = resolve_site(obscode_selection, create_placeholder=False)` (NEVER fabricate a
    placeholder for staff-typed/public text; with create_placeholder=False this returns either a resolved
    site with needs_review False, or (None, True)). If `site is None`: `messages.error` with the UI-SPEC
    "Could not resolve that site..." copy and `return redirect('campaigns:approval_queue')` — nothing was
    written, the flag is already True, the row stays in the review table. Otherwise CLAIM the site write
    with a single conditional queryset update mirroring the approve/reject staleness guard
    (`updated_count == 1` discipline, campaign_views.py line ~350):
    `claimed = CampaignRun.objects.filter(pk=pk, approval_status=CampaignRun.ApprovalStatus.APPROVED, site_needs_review=True, site__isnull=True).update(site=site)`
    — deliberately writing `site` ONLY, never `site_needs_review` (finding 3). If `claimed == 0` (a racing
    staff POST resolved it first — finding 5): `messages.warning(request, "This run's site was already resolved by someone else.")`
    (mirrors the WR-01 "already decided" message convention) and `return redirect('campaigns:approval_queue')`
    — no projection for the loser. Else `run.refresh_from_db()`. Do NOT use `transaction.atomic()` +
    `select_for_update()` here — the conditional-update claim is the codebase's established guard and
    behaves uniformly on SQLite; document this choice in a comment.

    (3) If `run.site` was already set at step (2)'s guard (the projection-failed retry state — finding 8):
    skip resolution entirely (resolve_site is never called) and fall through to (4); re-projection is safe
    because insert_or_create_calendar_event creates the event if missing, otherwise updates it in place.

    (4) Projection, inside its own NON-reverting try/except:
    `try: created = _project_calendar_event(run)` / `except Exception: logger.exception(...); messages.warning(request, "Site resolved, but the calendar entry couldn't be created automatically — the run stays in Sites Needing Review; use Resolve to retry."); return redirect('campaigns:approval_queue')`.
    This warning copy deliberately supersedes the 22-UI-SPEC.md Copywriting Contract's "Contact a
    coordinator..." projection-failure copy: under the finding-3 design the row itself remains in the
    review table as the retry path, so the message points staff at Resolve instead. On the failure path
    `site_needs_review` is NEVER touched (stays True → the run stays listed in review_table) and
    approval_status is NEVER reverted — reverting an already-APPROVED run would resurrect it into the
    pending queue, reintroducing the exact dead-end this phase closes (anti-pattern).

    (5) Only after the projection call returned without raising: `run.site_needs_review = False;
    run.save(update_fields=['site_needs_review'])`, then `messages.success` per the UI-SPEC Copywriting
    Contract driven by the helper's bool (finding 6): `'Site resolved — run added to the calendar.'` when
    `created` is True, else `'Site resolved.'` (range/TBD, projection skipped by design).
    `return redirect('campaigns:approval_queue')`.

    Materialize the RED tests from `<behavior>` as `TestSitesNeedingReview`. Reuse `CampaignApprovalTestBase`,
    the ground `Observatory` fixture pattern from `TestApproval.setUpTestData`, the space-site pattern from
    `TestCalendarProjection`, and the `patch('solsys_code.campaign_views....')` conventions. Never fixture a
    `Target` (CampaignRun.target stays unset here, as elsewhere in this module); if one were ever needed use
    `NonSiderealTargetFactory` per CLAUDE.md.
  </action>
  <verify>
    <automated>./manage.py test solsys_code.tests.test_campaign_approval -v2</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def _project_calendar_event" solsys_code/campaign_views.py` matches (module-level, not nested in post()) and the def line's return annotation is `-> bool` (22-REVIEWS.md finding 6).
    - `grep -n "resolve_site" solsys_code/campaign_views.py` shows the action handled: `grep -c "'resolve_site'" solsys_code/campaign_views.py` ≥ 1 and the action guard tuple includes it.
    - `grep -n "site__isnull=True" solsys_code/campaign_views.py` matches inside the resolve_site branch (the conditional site-claim update — 22-REVIEWS.md finding 5).
    - POST `action=resolve_site` on an APPROVED+needs-review single-night run with a resolvable `site_selection` sets `run.site`, sets `site_needs_review=False`, keeps approval_status APPROVED, creates exactly one CalendarEvent at `CAMPAIGN:{pk}`, and surfaces 'Site resolved — run added to the calendar.'.
    - A patched `_project_calendar_event`/`sun_event` raising during resolve_site leaves the run APPROVED (never PENDING_REVIEW) with `run.site` saved and `site_needs_review` still True, surfaces the retry warning, AND the run is still present in a subsequent approval-queue GET's `review_table` context (22-REVIEWS.md finding 3).
    - resolve_site is NOT called for a run whose `run.site` is already set (assert_not_called) — D-06; for such a run with `site_needs_review=True` the projection is re-attempted and the flag clears on success (22-REVIEWS.md finding 8).
    - The lost-race test passes: when a concurrent write sets the site between the fresh fetch and the claim update, the claim matches 0 rows, the loser's site value is not written, no projection fires, and a warning is surfaced (22-REVIEWS.md finding 5).
    - A range/TBD resolve surfaces the plain 'Site resolved.' message (helper returned False) with no CalendarEvent created.
    - POST `action=resolve_site` on a PENDING_REVIEW or already-resolved (site_needs_review=False) run makes no change and warns.
    - Existing `TestApproval` + `TestCalendarProjection` tests still pass (approve path unchanged by the extraction; approve ignores the helper's bool return).
    - `./manage.py test solsys_code.tests.test_campaign_approval` exits 0; `ruff check solsys_code/campaign_views.py` clean.
  </acceptance_criteria>
  <done>The shared projection helper exists (returning a created/skipped bool), the approve path is unchanged, and the resolve_site action resolves via a concurrency-safe conditional site claim, projects, and clears site_needs_review only after a successful projection — so a projection failure leaves the run APPROVED, site saved, and still visible in the review table for retry — all covered by passing tests.</done>
</task>

<task type="auto">
  <name>Task 2: Third "Sites Needing Review" table (D-07) + resolve-mode rendering (D-10)</name>
  <files>solsys_code/campaign_views.py, solsys_code/campaign_tables.py, src/templates/campaigns/approval_queue.html, solsys_code/tests/test_campaign_approval.py</files>
  <read_first>
    - solsys_code/campaign_views.py — read `ApprovalQueueView.get_context_data()` (lines ~286-329): the `pending_qs`/`decided_qs` construction, the once-per-request `candidate_pool = build_site_candidates()` (Pitfall 5, lines ~306-310), the `list(decided_qs)` materialization + `order_by=()` pattern, and how the two tables are configured and put in context.
    - solsys_code/campaign_tables.py — read `ApprovalQueueTable.__init__` (lines ~202-206, the `show_actions`/`request`/`candidate_pool` kwargs), `render_site()` (now the Plan 02 live-widget for pending rows), and `render_actions()` (lines ~248-269, the single decide-form + CSRF token + named submit buttons).
    - .planning/phases/22-.../22-UI-SPEC.md — "Sites Needing Review — new third table" section (reuse ApprovalQueueTable, filter APPROVED+site_needs_review, order -pk, NO row cap, same live widget + Create-new link, single "Resolve" `btn-primary btn-sm` action, empty_text copy, failure-UX via messages banner) and the Color section rationale (Resolve = `--primary`, not `--success`).
    - .planning/phases/22-.../22-PATTERNS.md — "ApprovalQueueView.get_context_data() third table" section (the review_qs + review_table snippet reusing the SAME candidate_pool).
    - src/templates/campaigns/approval_queue.html — the existing two `{% render_table %}` blocks + section-heading convention.
  </read_first>
  <action>
    In `ApprovalQueueView.get_context_data()`, after the existing `candidate_pool = build_site_candidates()`
    line, build a third queryset `review_qs = CampaignRun.objects.filter(approval_status=CampaignRun.ApprovalStatus.APPROVED,
    site_needs_review=True).select_related('campaign', 'site').order_by('-pk')` (D-07 — NO row cap per UI-SPEC:
    this is a live work queue, capping would hide actionable rows). Build
    `review_table = ApprovalQueueTable(list(review_qs), prefix='review-', request=self.request,
    candidate_pool=candidate_pool, mode='resolve', empty_text='No sites currently need review.', order_by=())`,
    REUSING the SAME `candidate_pool` variable already computed for `pending_table` (never call
    `build_site_candidates()` again — Pitfall 5). `RequestConfig(self.request).configure(review_table)`;
    `context['review_table'] = review_table`. Materialize with `list(...)` and `order_by=()` exactly like
    `decided_table` (avoids "Cannot reorder a query once a slice has been taken" and preserves -pk order).

    In `ApprovalQueueTable.__init__`, add a `mode='pending'` keyword parameter stored as `self.mode`,
    extending the existing `show_actions` boolean convention (do not remove `show_actions`; the decided
    table still passes `show_actions=False`). In `render_site()`, the resolve-mode row (`self.mode == 'resolve'`)
    with an UNRESOLVED site must render the SAME live-search widget as the pending row (Plan 02) — same
    input attrs INCLUDING the corrected `input[this.value.length >= 2] changed delay:300ms` trigger
    (22-REVIEWS.md finding 1 propagates here via the shared markup), suggestions container, and retained
    "Create new Observatory" link — but with the input's `form=` targeting the row's resolve-form id
    (e.g. `resolve-form-{pk}`) and the widget shown even though the run is APPROVED (the pending-only
    `not self.show_actions` early-return must not suppress it for resolve rows). KEEP the `site_short_name`
    early-return for resolve-mode rows whose site IS already set (the projection-failed retry state —
    22-REVIEWS.md finding 8): such a row shows its resolved site as plain text via `super().render_site()`,
    and its Resolve button alone re-attempts the projection — no site input is needed or rendered. Factor
    the widget-markup construction so pending and resolve modes share it, differing only in the target
    `form=` id.
    In `render_actions()`, add a resolve-mode branch: when `self.mode == 'resolve'`, render a single
    `<form id="resolve-form-{pk}" method="post" action="{decide_url}">` with the hidden CSRF token
    (`get_token(self.request)`) and ONE submit button `name="action" value="resolve_site"` styled
    `btn btn-sm btn-primary` labelled "Resolve" (no confirmation dialog, no Approve/Reject — UI-SPEC
    Destructive Confirmation Contract). Keep the pending-mode Approve/Reject rendering unchanged. All values
    interpolated via `format_html`/`format_html_join` auto-escaping.

    In approval_queue.html, add after the "Recently Decided" `{% render_table decided_table %}` block:
    `<h5 class="font-weight-bold mb-3 mt-4">Sites Needing Review</h5>` then `{% render_table review_table %}`.
    No new `{% load %}` (render_table already loaded).

    Extend `TestSitesNeedingReview` (from Task 1) with display/markup tests: (a) `ApprovalQueueView` staff GET
    context contains `review_table`, and it lists exactly the APPROVED+site_needs_review=True runs (not
    pending, not resolved-approved) — INCLUDING a run whose site is already set but whose flag is still True
    (the projection-failed retry state, 22-REVIEWS.md findings 3/8: the D-07 filter is on the flag alone, so
    retry rows appear naturally); (b) the rendered page contains the "Sites Needing Review" heading and a
    "Resolve" button with `value="resolve_site"`; (c) the site-unresolved resolve row's Site cell contains
    the live-search `hx-get` widget targeting `campaigns:site_search` with the corrected
    `input[this.value.length >= 2] changed delay:300ms` trigger (finding 1 propagation) and the Create-new
    link; (d) a retry row (site set, flag True) renders its site as plain text with NO site input, and still
    carries the Resolve button; (e) empty state renders 'No sites currently need review.'. Reuse the staff
    login + candidate-pool patch conventions.
  </action>
  <verify>
    <automated>./manage.py test solsys_code.tests.test_campaign_approval -v2</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "review_table" solsys_code/campaign_views.py` matches and `grep -n "site_needs_review=True" solsys_code/campaign_views.py` matches (D-07 queryset).
    - `grep -c "build_site_candidates()" solsys_code/campaign_views.py` in `ApprovalQueueView.get_context_data` shows it is called exactly once (the review table reuses `candidate_pool`, not a second call).
    - `grep -n "value=\"resolve_site\"" solsys_code/campaign_tables.py` matches (Resolve button) and `grep -n "resolve-form-" solsys_code/campaign_tables.py` matches.
    - `grep -n "Sites Needing Review" src/templates/campaigns/approval_queue.html` matches.
    - Staff GET of `campaigns:approval_queue` renders three tables; the review table lists only APPROVED+needs-review runs, including projection-failed retry rows (site set, flag True); a site-unresolved resolve row shows the live widget (hx-get → campaigns:site_search, corrected trigger) + Create-new link + a single "Resolve" btn-primary button; a retry row shows plain-text site + the Resolve button and no input; empty state shows 'No sites currently need review.'.
    - `./manage.py test solsys_code.tests.test_campaign_approval` exits 0; `ruff check .` and `ruff format --check .` clean for touched files.
  </acceptance_criteria>
  <done>The approval-queue page surfaces a third "Sites Needing Review" table of approved needs-review runs (unresolved rows with the live-search widget + Create-new link; projection-failed retry rows with plain-text site), each with a single Resolve action, reusing the once-per-request candidate pool — covered by passing tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| staff browser → CampaignRunDecisionView.post() (resolve_site) | Authenticated staff POST crosses here; StaffRequiredMixin gates the view, but the target `pk`/state must still be validated server-side. |
| resolved site + window → CalendarEvent projection | Resolving fires a deferred CalendarEvent write; a projection failure must not corrupt the run's APPROVED state. |
| MPC/local candidate + site_raw strings → resolve-mode row markup | Untrusted strings interpolated into the third table's Site cell + Resolve form. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-06 | Tampering / Elevation of Privilege | resolve_site action on a non-eligible pk | medium | mitigate | The branch re-fetches the run fresh and validates `approval_status == APPROVED and site_needs_review is True` before acting (mirrors the `updated_count == 1` staleness discipline); a mismatched/stale/tampered pk gets a warning and no change. Already requires staff auth (StaffRequiredMixin). |
| T-22-07 | Tampering | D-06 never-re-resolve guard in the new branch | high | mitigate | The site write is a SINGLE conditional queryset update — `.filter(pk=pk, approval_status=APPROVED, site_needs_review=True, site__isnull=True).update(site=site)` — mirroring the approve/reject `updated_count == 1` staleness discipline, not just a re-fetch + in-Python check: two racing staff POSTs cannot both claim the write (the loser matches 0 rows, warns, and never projects). Prevents silently clobbering a correct resolution — the exact SITE-03 bug class. (22-REVIEWS.md finding 5) |
| T-22-08 | Denial of Service (self-inflicted) | projection failure stranding the run out of its retry surface | high | mitigate | resolve_site's projection runs in its OWN non-reverting try/except; a failure warns, keeps the run APPROVED with the resolved site saved, and KEEPS `site_needs_review=True` — so the run remains listed in the Sites Needing Review table (its retry surface) and the flag is cleared ONLY after a successful projection. It never reverts to PENDING_REVIEW (which would resurrect a decided run into the pending queue) and never vanishes from the workflow (which would recreate the "approved but no event and no UI path" dead end). Test asserts post-failure visibility in review_table. (22-REVIEWS.md finding 3) |
| T-22-09 | Tampering | resolve-mode row markup | high | mitigate | All interpolated values go through `format_html`/`format_html_join` auto-escaping (T-21-01); CSRF token minted per row via `get_token`; the resolve POST reuses the existing CSRF machinery. |
| T-22-SC | Tampering | npm/pip/cargo installs | n/a | accept | No packages installed this phase — no supply-chain checkpoint required (Phase 21 T-21-SC precedent). |
</threat_model>

<verification>
- `./manage.py test solsys_code.tests.test_campaign_approval` green (including the new `TestSitesNeedingReview` and unchanged approve/reject/projection tests).
- `./manage.py test solsys_code` green (full app suite — no regression across submission/views/gap/models).
- `ruff check .` and `ruff format --check .` clean.
- Manual sanity: a staff GET of the approval queue shows three tables; resolving a needs-review single-night run projects its CalendarEvent; a projection failure warns without un-approving AND the run stays in the Sites Needing Review table for a retry via Resolve.
</verification>

<success_criteria>
- Approved runs with `site_needs_review=True` appear in a third "Sites Needing Review" table on the existing approval-queue page (no new page/navbar): site-unresolved rows carry the live-search widget + Create-new link; projection-failed retry rows show their resolved site as plain text — every row has the single Resolve action.
- Resolving a run resolves its site via the existing pool-mapping + `resolve_site(create_placeholder=False)`, honors the D-06 never-re-resolve guard through a concurrency-safe conditional site-claim update (22-REVIEWS.md finding 5), and fires the deferred CalendarEvent for a single-night resolved run — clearing `site_needs_review` only after the projection succeeds (22-REVIEWS.md finding 3).
- A projection failure during resolve does not revert approval AND does not remove the run from the review table; "site failure never blocks approval" is preserved and the retry surface is never lost.
- Success messaging distinguishes event-created from projection-skipped via `_project_calendar_event()`'s bool return (22-REVIEWS.md finding 6).
- The approve path's projection behavior is unchanged after extraction into the shared helper (approve ignores the new bool return; its revert-on-failure block is untouched).
</success_criteria>

<output>
Create `.planning/phases/22-site-matching-at-submission-and-unmatched-site-resolution-wo/22-03-SUMMARY.md` when done.
</output>
