---
phase: 22-site-matching-at-submission-and-unmatched-site-resolution-wo
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/templates/campaigns/approval_queue.html
  - solsys_code/tests/test_campaign_approval.py
autonomous: true
gap_closure: true
requirements: [D-07]
must_haves:
  truths:
    - "The 'Sites Needing Review' section is visually distinct from the historical 'Recently Decided' table — it reads as an actionable work queue (e.g. a bordered/coloured card with an 'action required' header + short helper line), not another read-only audit list"
    - "D-07's locked document order (pending / decided / sites-needing-review) is preserved — the section is NOT reordered ahead of 'Recently Decided'; the fix is presentation-only visual grouping, honouring the locked decision rather than overriding it"
    - "The change is presentation-only: no queryset, view, table-logic, or resolve-action change — ApprovalQueueView.get_context_data() and ApprovalQueueTable are untouched"
    - "The empty-state message ('No sites currently need review.') and the review_table rendering still work; when the table is empty the section still reads sensibly"
  artifacts:
    - "src/templates/campaigns/approval_queue.html :: the 'Sites Needing Review' block wrapped in distinguishing card/section markup, still the third section per D-07"
    - "solsys_code/tests/test_campaign_approval.py :: a rendering test asserting the Sites Needing Review section carries the distinguishing wrapper and keeps its D-07 position after 'Recently Decided'"
  key_links:
    - "ApprovalQueueView review_table (context) → approval_queue.html Sites Needing Review card section (rendered for staff)"
---

<objective>
Close UAT gap 2, sub-issue A (placement/grouping). Users find the actionable 'Sites Needing
Review' table "unhelpful" because it renders as a plain DOM sibling after the historical
read-only 'Recently Decided' table with zero visual differentiation — it looks like more
audit history, not a work queue. This is a faithful implementation of locked decision D-07,
which fixed the table order as "pending / decided / sites-needing-review"; the order itself is
a decision, not a bug.

Purpose: address the "unhelpful" complaint WITHOUT contradicting D-07. Apply a
presentation-only visual grouping — wrap the 'Sites Needing Review' section in a distinct
Bootstrap card (bordered/coloured header signalling "action required", plus a one-line helper
describing what a staff member does here) so it stands out from the historical 'Recently
Decided' table above it, while keeping it in D-07's document order (still the third section,
still after 'Recently Decided'). This honours the locked decision rather than silently
reordering the tables. (If a genuine reorder were ever wanted, that would be a CONTEXT.md D-07
revision, not a template edit — explicitly out of scope here.)

Output: a restyled 'Sites Needing Review' section in approval_queue.html and a rendering
regression test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-site-matching-at-submission-and-unmatched-site-resolution-wo/22-CONTEXT.md
@.planning/debug/sites-needing-review-placement-and-placeholder-correction.md

# The template to restyle and the view that feeds it (context only — do not modify the view):
@src/templates/campaigns/approval_queue.html
@solsys_code/campaign_views.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Give 'Sites Needing Review' distinct actionable visual weight (presentation-only, D-07 order preserved)</name>
  <files>src/templates/campaigns/approval_queue.html</files>
  <action>
Restyle ONLY the 'Sites Needing Review' section (the third `<h5>` + `{% render_table review_table %}`
block) so it reads as an actionable work queue distinct from the historical 'Recently Decided'
table above it. Keep it as the THIRD section, after 'Recently Decided' — D-07 locked the
document order "pending / decided / sites-needing-review", so do NOT move it ahead of the
decided table; differentiate it by presentation only.

Wrap the section in a Bootstrap 4 card that signals "action required": e.g. a
`card border-warning` with a `card-header` (warning-toned, bold "Sites Needing Review —
action required" heading) and a `card-body` containing a short one-line helper (e.g. "Approved
runs whose observing site still needs a staff member to resolve it before it lands on the
calendar.") followed by `{% render_table review_table %}`. Use only Bootstrap 4 utility/card
classes already available in this TOM/Bootstrap4 project (mirror the existing
`font-weight-bold`/`mb-*`/`mt-*` usage) — no new CSS files, no JS. Optionally lift the two
actionable sections' visual grouping consistency (Pending Review is also actionable), but at
minimum the 'Sites Needing Review' card must be clearly differentiated from the plain
'Recently Decided' section.

Leave the `{% extends %}`, `{% load render_table %}`, title block, the 'Pending Review'
render_table call, and the 'Recently Decided' render_table call in place. Do not add any
context variable — the template already receives `review_table`. This is a template-only edit;
touch no Python.
  </action>
  <verify>
    <automated>python -c "import django, pathlib; s=pathlib.Path('src/templates/campaigns/approval_queue.html').read_text(); assert 'review_table' in s and 'card' in s and s.index('decided_table') < s.index('review_table'), 'D-07 order or card wrapper missing'; print('ok')"</automated>
  </verify>
  <done>The Sites Needing Review section is wrapped in a distinguishing card with an action-required header + helper line, still positioned after Recently Decided (D-07 order intact); no Python changed.</done>
</task>

<task type="auto">
  <name>Task 2: Rendering regression test for the grouping + preserved D-07 order</name>
  <files>solsys_code/tests/test_campaign_approval.py</files>
  <action>
Add a staff-client rendering test (reuse the file's existing staff login + ApprovalQueue GET
pattern; the approval queue is StaffRequiredMixin-gated, so authenticate a staff user first).
GET `campaigns:approval_queue` and assert on the rendered HTML:

- The response contains the distinguishing wrapper for the Sites Needing Review section (assert
  the card class/marker you added, e.g. `border-warning`, and the "action required" header
  text) — proving the section is visually differentiated.
- D-07 order is preserved: the 'Recently Decided' heading appears BEFORE the 'Sites Needing
  Review' heading in the response body (assert the index of the decided heading text is less
  than the index of the review heading text).
- Sanity: the page still renders 200 for staff and still contains the 'Pending Review' and
  'Recently Decided' headings.

Keep the test independent of row contents (it should pass with empty tables) so it verifies
presentation, not data. Follow the file's existing NonSiderealTargetFactory / fixture
conventions only if a CampaignRun is needed — an empty-queue assertion needs none.
  </action>
  <verify>
    <automated>python manage.py test solsys_code.tests.test_campaign_approval 2>&1 | tail -5</automated>
  </verify>
  <done>A rendering test asserts the Sites Needing Review card wrapper and the preserved decided→review ordering; the full test_campaign_approval module passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| staff browser → ApprovalQueueView (GET, StaffRequiredMixin) | authenticated staff only; this change is presentation of already-authorised content |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-05-01 | Information Disclosure | approval_queue.html restyle | low | accept | Purely visual regrouping of content already gated behind StaffRequiredMixin; no new data, queryset, or field is exposed, and the review_table context/PII handling is unchanged. |
</threat_model>

<verification>
- `python manage.py test solsys_code.tests.test_campaign_approval` passes (existing + new test).
- The 'Sites Needing Review' section renders inside a distinguishing card and remains the third
  section after 'Recently Decided' (D-07 order intact).
- `ruff check solsys_code/tests/test_campaign_approval.py` clean; `ruff format --check` clean.
- Manual (developer, optional): load `/campaigns/approval-queue/` as staff — the Sites Needing
  Review section is now visually obvious as an action queue.
</verification>

<success_criteria>
- The actionable Sites Needing Review section is clearly differentiated from the historical
  Recently Decided table (UAT gap 2A addressed).
- D-07's locked table order is preserved — no reorder, no decision override.
- The change is presentation-only; no view/table/queryset logic changed.
</success_criteria>

<output>
Create `.planning/phases/22-site-matching-at-submission-and-unmatched-site-resolution-wo/22-05-SUMMARY.md` when done
</output>
