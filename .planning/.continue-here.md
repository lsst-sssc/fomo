---
context: research
phase: null
task: 9
total_tasks: 16
status: in_progress
last_updated: 2026-06-24T21:26:17.374Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Parallel research agents can silently fail without erroring — 2 of 4 parallel `gsd-project-researcher` agents (Architecture, Pitfalls) hit a session/usage limit mid-run and returned a "completed" task-notification despite NOT having written their output file (stale prior-milestone content was left in place, no error surfaced). **Mitigation:** always verify output file mtimes are fresh (today's date) after any parallel research/synthesis spawn before treating that step as done — never trust the notification text alone.

**Do not proceed until this box is checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Silent research-agent failure on session limit | `gsd-project-researcher` agents can return "completed" with near-zero token usage after hitting a session limit, without writing their designated output file. Caught only by checking file mtimes, not by trusting the notification. | blocking | After every parallel research spawn: `ls -la .planning/research/*.md` and confirm today's date before moving on. Re-spawn any stale file with an explicit "a prior attempt failed and left stale content — you must overwrite it" note. |
| Synthesizer Write-tool hard block | `gsd-research-synthesizer`'s Write tool call was rejected ("Subagents should return findings as text, not write report files") despite writing SUMMARY.md being its designated job. It eventually worked around this via Bash heredoc, but took ~9 minutes. | advisory | If a writer-role subagent stalls after a Write-tool rejection, extract the content it already produced from its transcript/result and write the file directly yourself rather than waiting for its workaround. |

_The second row is advisory only — it does not block resumption, just noting what happened._

<current_state>
Mid `/gsd:new-milestone` workflow for **v1.4 "Calendar Visual Clarity"** (DISPLAY-01 proposal-keyed `CalendarEvent` color + status visual treatment; DISPLAY-02 fallback-vs-verified telescope-label sidecar field). No phase directory exists yet — we are still in Step 9 (Define Requirements), specifically: an `AskUserQuestion` proposing the REQUIREMENTS.md categories (DISPLAY-02 sidecar field / DISPLAY-01 color / DISPLAY-01 status treatment) was presented, but the user rejected the tool call specifically to clarify something first — then asked to suspend instead of explaining what. **The clarification itself was never captured.**
</current_state>

<completed_work>

Completed Tasks:
- Gathered milestone goals via questioning (DISPLAY-01, DISPLAY-02) — Done
- Determined version v1.4 "Calendar Visual Clarity", confirmed milestone summary — Done
- Updated PROJECT.md (Current Milestone section, Active requirements, footer) — Done, commit `d42c136`
- Reset STATE.md via `gsd_run query state.milestone-switch` — Done, commit `d42c136`
- Archived v1.0–v1.3 phase directories to `.planning/phases-archive/` — Done, commit `83b3727`
- Ran 4 parallel `gsd-project-researcher` agents (Stack/Features/Architecture/Pitfalls) — Done (2 needed a retry after silently failing, see Blocking Constraint above), commit `6fa75d5`
- Synthesized research into SUMMARY.md — Done, commit `5ac5879`
- Resolved 3 research-flagged scope gaps via `AskUserQuestion`:
  - N+1 mitigation on DISPLAY-02's sidecar reverse-accessor: **accept as-is** this milestone (no batching tag)
  - Proposal coloring scope: **both** all-day and timed event branches (not just all-day, today's status quo)
  - Empty-proposal events: **dedicated neutral palette slot** (not a raw hash of the empty string)
- Step 9 (Define Requirements): presented the DISPLAY-01/02 scoping `AskUserQuestion` — **interrupted, unanswered, clarification needed**
</completed_work>

<remaining_work>

- Ask the user what they wanted to clarify about the DISPLAY-01/02 requirements-scoping question, then re-present it
- Write `REQUIREMENTS.md` (REQ-IDs under a DISPLAY category) per template, present full list for confirmation
- Commit `REQUIREMENTS.md`
- Spawn `gsd-roadmapper` for v1.4 — phase order per research: **DISPLAY-02 first** (new migration, isolate schema risk), **DISPLAY-01 second** (template-only). Continue phase numbering from v1.3 (no `--reset-phase-numbers` was passed) — v1.0–v1.3 used phases 1–7.1, so v1.4 should start at Phase 8.
- Present roadmap for approval, commit `ROADMAP.md`/`STATE.md`/`REQUIREMENTS.md`
- Step 10.5: scan pending todos in `.planning/todos/pending/` and tag any matching the new phases with `resolves_phase: N`
- Step 11: present the Next Up block pointing to `/gsd:discuss-phase` or `/gsd:plan-phase` for the first new phase
</remaining_work>

<decisions_made>

- Milestone v1.4 picks up **both** DISPLAY-01 and DISPLAY-02 from the v1.3-deferred candidates — DISPLAY-02 built now rather than waiting for its original "only if operators report confusion" trigger condition
- Color is keyed on **proposal**, not telescope — an LCO proposal can span 2m0/1m0/0m4 telescopes/instruments and should render as one consistent color regardless of which telescope a block lands on; non-LCO proposals already bind ~1:1 to a telescope so this subsumes that case
- Status visual treatment (opacity/border vs. striping) is explicitly **deferred to a `/gsd:sketch` session** during phase planning, not decided now — user wants a visual comparison before committing; research pre-vetted border-style as the accessibility-favored option for that session
- Phase order: **DISPLAY-02 before DISPLAY-01** — DISPLAY-02 is `solsys_code`'s first-ever migration/cross-app model extension (riskier), isolate it from DISPLAY-01's purely-additive template work
- N+1 query risk on DISPLAY-02's sidecar reverse-accessor: **accept as-is**, no batching template tag this milestone
- DISPLAY-01 proposal coloring applies to **both** all-day and timed calendar-event branches
- Empty-proposal events get a **dedicated neutral palette slot**
</decisions_made>

<blockers>
None technical. One open human-input gap: the user's intended clarification on the requirements-scoping question was never stated before they asked to suspend.
</blockers>

## Required Reading (in order)
1. `.planning/PROJECT.md` — Current Milestone section (v1.4 goal + target features) and Active requirements
2. `.planning/research/SUMMARY.md` — full synthesis: recommended stack, architecture, pitfalls, suggested phase structure (DISPLAY-02 then DISPLAY-01)
3. `.planning/STATE.md` — confirms milestone switch already applied (`status: planning`, `current_phase: null`)
4. `.claude/gsd-core/workflows/new-milestone.md` — the workflow being executed; resume at Step 9 ("Define Requirements")

## Infrastructure State
- No servers/watchers running. All background research/synthesis agents have completed (some required a retry — see Blocking Constraint above).
- Node: this repo's `gsd-tools.cjs` requires Node 20 — run `source ~/.nvm/nvm.sh && nvm use 20` before any `gsd_run`/`node .../gsd-tools.cjs` call (default `node` on PATH is v14 and throws a SyntaxError).
- `.planning/phases/` is currently empty (v1.0–v1.3 phase dirs archived to `.planning/phases-archive/`) — ready for v1.4's new phase directories.

<context>
We're in the requirements-definition step of starting milestone v1.4. All upstream work (questioning, version confirmation, PROJECT.md/STATE.md update, phase archiving, 4-way research, synthesis, and 3 scope-gap resolutions) is done and committed. The only thing blocking writing REQUIREMENTS.md is: the user clicked "reject" on the requirements-scoping AskUserQuestion specifically to add a clarification, then asked to suspend before stating what that clarification was. Nothing is lost — just re-open that conversation thread on resume.
</context>

<next_action>
Start with: ask the user directly — "What would you like to clarify about the DISPLAY-01/02 requirements scoping?" — before re-presenting the AskUserQuestion. Once resolved, write `.planning/REQUIREMENTS.md` (DISPLAY-01 color + status treatment, DISPLAY-02 sidecar field, with REQ-IDs), get confirmation, commit, then spawn `gsd-roadmapper` per workflow Step 10.
</next_action>
